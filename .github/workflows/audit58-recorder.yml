name: audit58 one-shot recorder

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/audit58-recorder.yml'

permissions:
  contents: write

jobs:
  record:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Rebase onto latest main
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git pull --rebase origin main
      - name: Record audit 58
        shell: bash
        run: |
          set -euo pipefail
          MARK='## 2026-09-19(58) — ChatGPT 독립 감사: scheduled green 직후 다시 repository-wide schedule 공백 — 켜짐 contract-status 30분 회차 미생성'
          if ! grep -Fq "$MARK" docs/AI-SSOT-AUDIT-LOG.md; then
            cat >> docs/AI-SSOT-AUDIT-LOG.md <<'EOF'


---

## 2026-09-19(58) — ChatGPT 독립 감사: scheduled green 직후 다시 repository-wide schedule 공백 — 켜짐 contract-status 30분 회차 미생성

**판정: 신규 운영 회귀/드리프트 증거 — audit (57)의 scheduled full-run green 자체는 유효하지만 cadence 복구로 확대할 수 없고, `contract-status`의 선언된 ON 상태와 실제 schedule event가 다시 불일치한다.**

- current `origin/main` application 기준점은 audit (57) 이후 그대로 `dc6c5cf16146c7439f5a5293383d503e8e30d00d` 계열이며, 새 application/business-logic commit은 없다. latest main push CI run `35350559341`은 success다.
- audit (57)의 ERP5 scheduled full-run run `35347508078`(2026-09-18 21:58:18 KST, success, production pin `cf940df642edf315adbc6da2b4134fbad53da160`)은 그대로 유효하다. source→Atom→snapshot→F01/F86→cross-audit green 판정은 되돌리지 않는다.
- 그러나 2026-09-19 02:25 KST 재조회에서 GitHub Actions API `event=schedule&created=>2026-09-18T13:00:00Z`는 **`total_count: 0`**이다. 즉 2026-09-18 22:00 KST 이후 repository-wide scheduled event가 한 건도 생성되지 않았다.
- `docs/예약작업-지도.md`는 `contract-status.yml`을 **켜짐**으로 기록하고, 실제 YAML도 `*/30 * * * *` 및 schedule일 때 `scripts/mark-contract-in-listings.mts --apply`를 실행한다. 따라서 22:00·22:30·23:00·23:30·00:00·00:30·01:00·01:30·02:00 KST의 **9개 30분 회차**가 선언상 기대되지만 모두 없다.
- `mirror-sync`와 `sales-erp-hourly`는 예약지도상 꺼짐이고, `settlement-sync`는 해당 야간 시간대 밖이다. 따라서 이번 post-22:00 공백은 최소한 **켜짐으로 문서화된 `contract-status`의 runtime state 또는 GitHub scheduled-event delivery가 repository 선언과 맞지 않는다**는 새 운영 증거다.
- 원인은 아직 단정하지 않는다. GitHub UI에서 `contract-status`가 실제 disabled되어 예약지도와 drift했을 수도 있고, GitHub scheduler delivery가 다시 끊겼을 수도 있다. 어느 쪽이든 자동운영 GO 조건은 충족되지 않는다.
- ERP5 `:17` cadence도 계속 HOLD다. 한 번의 21:58 delayed green 뒤 repository-wide schedule가 다시 4시간 이상 비어 있어 연속 cadence 복구 증거가 없다.
- canonical registry(RP006 website / RP012 ERP API / RP023 RebornCar / RP031 Google Sheet), F01/F86 fixed-snapshot projection, `신차렌트=#FF00FF` production color, Sonogong/AutoPlus special-tab 규칙, RP023 legacy mirror source, RP031 provenance, settlement legacy `정산`, `LEDGER` vs `정산원장` dual-writer, RTDB/legacy writer HOLD는 이번 구간에서 code change가 없어 기존 판정을 유지한다.
- 상세 근거: `docs/ai-ssot-audit/2026-09-19-chatgpt-audit58-post-green-schedule-gap.md`.
- 이번 감사에서는 application code/business logic을 수정하지 않았다.
EOF
          fi

          cat > docs/ai-ssot-audit/2026-09-19-chatgpt-audit58-post-green-schedule-gap.md <<'EOF'
# 2026-09-19 ChatGPT audit 58 — post-green schedule gap / enabled contract-status mismatch

상태: **OPEN — operational schedule delivery/runtime-state drift**

## 확인한 사실

1. `origin/main` application 기준점은 audit (57) 이후 변하지 않았다. audited application HEAD는 `dc6c5cf16146c7439f5a5293383d503e8e30d00d`; latest push CI run `35350559341`은 success다.
2. audit (57)의 scheduled production run `35347508078`은 유효하다. 2026-09-18 21:58:18 KST에 `event=schedule`, conclusion `success`, production pin `cf940df642edf315adbc6da2b4134fbad53da160`을 checkout해 source registry/ingest/snapshot/F01/F86/freshness/cross-parity/photo-link 전 구간을 green으로 완료했다.
3. 이후 schedule event가 다시 끊겼다. 2026-09-19 02:25 KST 재조회에서 `GET /actions/runs?event=schedule&created=>2026-09-18T13:00:00Z` 결과는 `total_count=0`이었다. 즉 22:00 KST 이후 repository-wide scheduled event가 하나도 없다.
4. `docs/예약작업-지도.md`는 `contract-status.yml`을 `켜짐`으로 명시한다. 실제 `.github/workflows/contract-status.yml`은 `*/30 * * * *`이고 schedule이면 `mark-contract-in-listings.mts --apply`를 실행한다. 22:00부터 02:00까지 기대되는 9회가 모두 없다.
5. 같은 지도에서 `mirror-sync`와 `sales-erp-hourly`는 꺼짐이다. `settlement-sync`는 09:05~18:05 KST이고 ERP5 refresh는 09:17~19:17 KST다. 따라서 야간 공백은 적어도 `contract-status`의 문서화된 ON/runtime 상태 또는 GitHub schedule delivery와 직접 충돌한다.

## 판정

- audit (57)의 **scheduled end-to-end green**은 취소하지 않는다.
- 하지만 그것을 scheduler/cadence 복구로 해석하면 안 된다. 그 green 직후, 24/7 30분 writer가 켜짐으로 선언돼 있음에도 9회 연속 event가 없다.
- root cause는 두 후보로 남긴다.
  1. GitHub UI에서 `contract-status`가 disabled인데 `docs/예약작업-지도.md`가 stale.
  2. workflow는 enabled이나 GitHub scheduled-event delivery가 다시 누락/지연.
- 어느 경우든 자동운영 GO는 HOLD다. Claude는 **UI enabled/disabled 실상과 예약지도 일치 여부를 먼저 확정**하고, enabled라면 실제 `event=schedule` 연속 도착을 확인해야 한다.

## 변경 없는 SSOT 경계 재확인

- canonical inventory registry: RP006=ironrentcar.com, RP012=sokrc.com API, RP023=RebornCar, RP031=Google Sheet.
- production engine pin: `cf940df642edf315adbc6da2b4134fbad53da160`.
- production `MASTER_CATEGORY_COLORS['분류'].신차렌트 = #FF00FF` 유지.
- production special-tab은 오공구독/픽업구독/오플구독 구조를 유지하고, main `deposit-policy.ts`와 production pin의 단일정의 차이는 여전히 HOLD다.
- `MIRROR_SOURCES` RP023 old Google Sheet, settlement legacy `정산`, `LEDGER` vs `정산원장` contract-lock dual writer, RP031 API/DOM finance provenance, vehicle-price lineage, sales-tab naming, newest-Atom freshness, `/inventory` ERP4 read/write boundary는 미해소 유지.

## Claude 구현 Owner 다음 확인

1. GitHub Actions UI에서 `contract-status.yml` enabled/disabled를 확인해 `docs/예약작업-지도.md`와 맞춘다.
2. enabled라면 왜 22:00~02:00의 9개 schedule event가 생성되지 않았는지 확인한다.
3. disabled가 의도라면 YAML cron/scheduled `--apply`와 예약지도를 fail-closed 상태로 정리한다.
4. contract-lock owner 단일화와 settlement legacy writer retirement는 기존 우선순위를 유지한다.

이번 감사에서는 application code/business logic을 수정하지 않았다.
EOF

          cat > CLAUDE-AUDIT.md <<'EOF'
# ChatGPT → Claude SSOT 감사 진입점 — audit (58) override

> 구현 Owner는 Claude 단일 SSOT 세션이다. ChatGPT는 독립 감사·증거 기록만 한다. application/business logic은 이 감사에서 수정하지 않았다.

## 1) RESOLVED(code/main) — downstream read split-brain

PR #414 merge `817865d732b92b284c1db78a183325c6bfb12a1f` 이후 authenticated `/api/products`, Finder, `/m/[code]`, F01 상세링크 매핑, 내부 policy/partner, read-only ops inventory는 canonical freepasserp5를 읽는다. `/inventory` 편집은 ERP5 authenticated write boundary가 생길 때까지 ERP4 read/write 한 묶음으로 HOLD한다.

## 2) NO-GO/HOLD — 계약락 dual writer

- canonical production engine `erp5-ssot-refresh.yml` / pin `cf940df642edf315adbc6da2b4134fbad53da160` / `sync-vehicle-lock-from-ledger.mts --apply` / marker `LEDGER`.
- 별도 `contract-status.yml` / `*/30 * * * *` / `mark-contract-in-listings.mts --apply` / marker `정산원장`.

같은 ERP5 `products.locked_by_contract` ownership/lifecycle가 둘이다. concurrency group은 simultaneous write만 직렬화한다. owner를 1개로 만든 뒤 자동 writer를 넓게 재개한다.

## 3) HOLD — settlement-sync legacy `정산` writer

`settlement-sync.yml`은 `sync-intake-to-ledger.mts` 뒤 `sync-contract-from-ledger.mts`를 scheduled apply로 실행한다. current canonical ledger는 `접수 / 취소 / 분납실적 / 완납실적 / 청구`인데 legacy `정산` alias를 읽어 공급사 시트를 직접 쓰는 경로가 남아 있다. retire/rewire한다.

## 4) OPEN/REGRESSION — scheduled green 직후 다시 schedule 공백

Audit (57)의 scheduled production green은 유효하다.

- ERP5 run `35347508078`
- 2026-09-18 21:58:18 KST
- `event=schedule`, `success`
- production pin `cf940df642edf315adbc6da2b4134fbad53da160`
- source→Atom→snapshot→F01/F86→freshness/cross-parity/photo-link 전부 green

하지만 그 뒤 runtime은 다시 선언과 어긋난다.

- 2026-09-19 02:25 KST 재조회: `event=schedule&created=>2026-09-18T13:00:00Z` = **0건**.
- `docs/예약작업-지도.md`는 `contract-status.yml`을 **켜짐**으로 기록.
- 실제 YAML도 `*/30 * * * *`이며 schedule이면 `mark-contract-in-listings.mts --apply`.
- 따라서 22:00~02:00 KST에 기대되는 **9개 30분 회차가 모두 없음**.

`mirror-sync`/`sales-erp-hourly`는 지도상 꺼짐이고 settlement/ERP5는 야간 window 밖이므로, 이번 공백은 최소한 `contract-status`의 **GitHub UI runtime state 또는 scheduled-event delivery가 선언과 불일치**한다는 증거다. UI disabled drift인지 scheduler 누락인지 원인은 아직 확정하지 않는다.

**결론:** audit (57)의 one-shot scheduled green은 유지하되 cadence/punctuality·자동운영 GO는 계속 HOLD. 먼저 `contract-status` UI enabled/disabled와 예약지도를 맞추고, enabled라면 실제 schedule 연속 도착을 증명한다.

## 5) RESOLVED(code/config + scheduled runtime proof) — F86 checker/publication boundary

PR #412 + pin `cf940df...`의 F86 publisher/auditor shared rule, 종합만 timestamp, 회사탭 `회사 · N대`, cell/plate/header/tab-order audit는 run `35347508078`에서 실제 scheduled green이 확인됐다. old freshness false-positive를 다시 구현하지 않는다.

## 6) 기존 HOLD 유지

직접 해소 증거가 없는 항목:

- RP023 canonical RebornCar vs legacy mirror old Google Sheet
- RP031 API/DOM feeder finance provenance / canonical migration
- main `deposit-policy.ts` vs production special-tab deposit-rule single-definition
- Sonogong/AutoPlus deposit recurrence/lineage
- vehicle-price source→Atom lineage
- sales-tab naming migration
- newest-Atom freshness semantics
- `/inventory` ERP4 read/write boundary

production color `MASTER_CATEGORY_COLORS['분류'].신차렌트=#FF00FF`는 유지된다.

## 7) Claude 구현 Owner 다음 순서

1. `contract-status.yml` GitHub UI enabled/disabled 실상과 `docs/예약작업-지도.md`를 일치시킨다.
2. enabled라면 30분 schedule event 미생성 원인을 확인하고 연속 delivery를 증명한다.
3. contract lock writer/marker ownership을 1개로 통합한다.
4. `settlement-sync` legacy `정산` writer를 retire/rewire한다.
5. `sales-erp-hourly` / `mirror-sync`는 문서상 disabled가 실제 runtime과 일치하도록 fail-closed 증거를 남긴다.
6. `/inventory`는 ERP5 authenticated write boundary가 설계된 뒤 read/write 함께 이관한다.

## 기준

- audited application HEAD: `dc6c5cf16146c7439f5a5293383d503e8e30d00d`
- current audit log: `docs/AI-SSOT-AUDIT-LOG.md` → **2026-09-19(58)**
- 상세 근거: `docs/ai-ssot-audit/2026-09-19-chatgpt-audit58-post-green-schedule-gap.md`
- last successful scheduled production evidence: run `35347508078`
- production pin: `cf940df642edf315adbc6da2b4134fbad53da160`

**application code/business logic은 이 감사에서 수정하지 않는다.**
EOF

          git add docs/AI-SSOT-AUDIT-LOG.md docs/ai-ssot-audit/2026-09-19-chatgpt-audit58-post-green-schedule-gap.md CLAUDE-AUDIT.md
          if git diff --cached --quiet; then
            echo 'audit58 already recorded'
            exit 0
          fi
          git commit -m 'docs(audit): record audit58 post-green schedule gap'
          git push origin HEAD:main

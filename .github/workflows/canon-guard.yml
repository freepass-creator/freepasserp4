name: Canon Guard

# 정본 지킴이 — AI Core 규격(registry/canonical-development-lines.json)을 PR 마다 잰다.
# 판정은 AI Core 한 곳(freepass-creator/ai-core scripts/canon-guard.mjs)이고, 여기서는 버전을 고정해 부른다.
on:
  pull_request:

permissions:
  contents: read
  pull-requests: read

jobs:
  canon:
    uses: freepass-creator/ai-core/.github/workflows/canon-guard.yml@d14d4f1414011104eb541c0c4ec6f965b002341b
    with:
      ai_core_ref: d14d4f1414011104eb541c0c4ec6f965b002341b

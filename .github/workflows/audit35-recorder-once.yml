name: One-shot audit 35 recorder

on:
  push:
    paths:
      - '.github/workflows/audit35-recorder-once.yml'

permissions:
  contents: write

jobs:
  record:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Record audit 35
        shell: bash
        run: |
          set -euo pipefail
          git pull --ff-only origin main
          cat >> docs/AI-SSOT-AUDIT-LOG.md <<'AUDIT'

---

## 2026-09-18(35) — ChatGPT 독립 감사: canonical schedule delivery 재출현, 심한 지연 + known F86 freshness checker가 scheduled full-run을 red로 만듦

**판정: audit (31)의 `event=schedule 0건` 사실은 해소/갱신되었지만, 정시 cadence와 full-run green은 아직 HOLD다. audit (23)의 F86 freshness checker drift는 이제 canonical scheduled production 실패로 직접 재현됐다.**

- current main은 `c6f6c3e0253e6d6f172ce52067d7261358b3e4a9`, canonical `.github/workflows/erp5-ssot-refresh.yml`은 계속 `cron: '5 0-10 * * 1-6'`(월~토 KST 09:05~19:05)과 production pin `9bef7bf0ffd21a96e3098a6f31adf1b1a0258c60`을 선언한다.
- audit (31)/(34) 당시 2026-09-17 `event=schedule`이 0건이었으나, 이후 실제 scheduled run **`35235961510`**이 생성됐다. event=`schedule`, created=`2026-09-17T14:47:38Z` = **2026-09-17 23:47:38 KST**, conclusion=`failure`다. 따라서 canonical workflow가 실제 schedule event를 전혀 받지 않는다는 이전 관측은 최신이 아니다.
- 다만 이 시작 시각은 선언된 당일 마지막 cron 시각 19:05 KST보다 **4시간 42분 이상 늦다.** 어떤 원래 slot이 지연 전달된 것인지는 Actions metadata만으로 확정하지 않는다. 그러므로 “매시간 정시 운용 복구”로 판정하지 않고 **schedule delivery는 재출현 / cadence·timeliness는 HOLD**로 둔다.
- 이 scheduled run은 production `9bef7bf...`를 실제 checkout해 canonical 경로를 수행했다. 공급사 preflight **24/24 success**, 실제 ingest **24/24 success**, settlement Atom-lock success, policy reconcile products 1,615 / policies 81 / dangling 0, snapshot `20260917145848487-344b1c66e52f` = 등록 1,615 / 출고불가 921 / 현재 재고 694였다.
- public catalog는 expected/actual **687대 동일**, hash 동일, missing/extra/policy/photo mismatch 전부 0. F01은 **694대**(`상품리스트 394 · 오공구독 43 · 픽업구독 197 · 오플구독 60`) 발행 성공. F86도 backup 후 **19탭 / 694대 / 90열** 발행 성공했다.
- F86 감사의 값 대조 자체는 **45,186칸 / 어긋남 0**, freshness 계산도 `가장 오래된 탭 0분 전 (허용 120분)`이었다. 그런데 같은 `audit-f86-vs-atom.mts`가 현재 builder 계약인 `종합 MM.DD HH:MM · N대`와 `회사 · N대`를 모두 “탭 이름에 발행 시각이 없다”로 판정해 19갈래 failure를 만들었다. 즉 audit (23)/(30)의 **builder↔freshness-checker contract drift가 canonical scheduled run에서 그대로 재현**됐다.
- 그 뒤 cross-audit는 별도 조건으로 계속 실행되어 Atom↔F01에서 빠진 차 0 / 내려야 할 차 0 / 값 다른 칸 0, F01↔F86에서 빠진 차 0 / 추가 차 0 / 값 다른 칸 0을 확인했고 `보증금규칙 0`도 확인했다. photo-link audit도 전부 mismatch 0이었다. 따라서 run 전체 red를 데이터 발행 실패로 해석하면 안 된다. **publish/data parity는 PASS, workflow conclusion은 known checker false-positive 때문에 failure**다.
- audit (27) 손오공 deposit recurrence guard는 이 한 회차에서 `보증금규칙 0`이 나왔다고 닫지 않는다. current production lineage에 feature-branch write-before-normalize guard가 이식됐다는 코드 증거가 없기 때문이다. audit (28) 차량가격도 SOURCE→ATOM 의미 대조가 아니라 Atom→F01/F86 대조만 통과한 것이므로 유지한다.
- audit (29) sales-tab naming migration, audit (33) RP031 API feeder/provenance promotion HOLD, audit (34) newest-Atom freshness observability gap, mirror/sales/settlement/RTDB legacy writer ownership HOLD도 직접 해소 증거가 없어 유지한다. 이번 scheduled run의 RP031은 current canonical Google Sheet 경로로 ingest됐다.
- canonical inventory source 계약은 계속 RP006=`ironrentcar.com`, RP012=`sokrc.com/api`, RP023=RebornCar이며 RP031은 아직 Google Sheet canonical이다.

### Claude 구현 Owner 인계

1. audit (31)의 “schedule event 자체가 없음”은 **해소/갱신**하되, 23:47 KST에 늦게 도착한 한 회차만으로 hourly cadence 정상화를 선언하지 않는다.
2. 최우선 runtime blocker는 audit (23)의 F86 freshness checker다. `channel-f86-plan`의 실제 탭명 계약과 freshness parser를 공유/정렬하되 차량/칸 값 대조는 약화하지 않는다.
3. 수정 뒤 **실제 `event=schedule` 회차**에서 source→Atom→snapshot→public/F01/F86→F86 audit→cross-audit→photo-audit가 모두 green인지 확인한다. workflow_dispatch/one-time run으로 대체하지 않는다.
4. audit (27)/(28)/(29)/(33)/(34)와 legacy writer ownership은 각각의 코드/runtime 증거가 생길 때만 닫는다.

상세 근거: `docs/ai-ssot-audit/2026-09-18-chatgpt-audit35-scheduled-delivery-f86-checker.md`.

이번 ChatGPT 감사에서는 application code/business logic을 수정하지 않았다.
AUDIT

          cat > docs/ai-ssot-audit/2026-09-18-chatgpt-audit35-scheduled-delivery-f86-checker.md <<'DETAIL'
# ChatGPT 독립 SSOT 감사 — audit (35): scheduled delivery 재출현 + F86 checker canonical failure

검토일: 2026-09-18 KST

## 판정

**의미 있는 운영 상태 변화가 있다.** audit (31)/(34) 시점의 “2026-09-17 `event=schedule` 0건”은 더 이상 현재 사실이 아니다. canonical `ERP5 SSOT 원천 최신화(매시간)` scheduled run `35235961510`이 실제 생성되어 source→ERP5 Atom→snapshot→public/F01/F86 발행까지 수행했다.

다만 회차는 전체 `failure`다. 실패 원인은 데이터/발행 parity가 아니라 이미 audit (23)/(30)에서 지적한 **F86 builder와 freshness checker의 탭명 계약 불일치**다. 또한 scheduled run은 2026-09-17 23:47 KST에 시작해 workflow가 선언한 마지막 당일 slot 19:05 KST보다 4시간 42분 이상 늦었다. 따라서 schedule delivery 존재는 확인하지만 cadence/timeliness 정상화는 확인하지 않는다.

## Evidence 1 — 실제 schedule event가 생김

Current main:

- `c6f6c3e0253e6d6f172ce52067d7261358b3e4a9`

Current canonical workflow:

- `.github/workflows/erp5-ssot-refresh.yml`
- cron: `5 0-10 * * 1-6` = 월~토 KST 09:05~19:05
- checkout ref: `9bef7bf0ffd21a96e3098a6f31adf1b1a0258c60`

GitHub Actions run:

- run id: `35235961510`
- workflow: `ERP5 SSOT 원천 최신화(매시간)`
- event: `schedule`
- created: `2026-09-17T14:47:38Z` = `2026-09-17 23:47:38 KST`
- conclusion: `failure`

따라서 audit (31)의 “actual event=schedule run이 없음”은 더 이상 유지할 수 없다. 그러나 이 시각은 선언된 당일 마지막 cron slot 19:05 KST보다 4시간 42분 이상 늦다. 어느 slot이 지연된 것인지는 GitHub run metadata만으로 특정하지 않는다.

## Evidence 2 — canonical source→Atom→snapshot→F01/F86 write는 실제 성공

Run `35235961510` / job `105251799335`:

- production pin checkout `9bef7bf...`: PASS
- inventory source registry: `providers=24 inventory_locations=21 policy_locations=21 PASS`
- supplier preflight: 24/24 PASS
- supplier apply: 24/24 PASS
- settlement `접수/취소` Atom lock: PASS
- policy reconcile: products 1,615 / policies 81 / dangling 0
- snapshot: `20260917145848487-344b1c66e52f`
  - registered 1,615
  - unavailable 921
  - open/current 694
- public catalog: expected 687 = actual 687, hash 동일, mismatch 0
- F01 publish: 694대
  - 상품리스트 394
  - 오공구독 43
  - 픽업구독 197
  - 오플구독 60
- F86 backup: PASS
- F86 publish: 19탭 / 694대 / 90열 PASS

따라서 이번 scheduled run은 단순 dispatch 증거가 아니라 canonical full write path가 실제 수행됐다는 증거다.

## Evidence 3 — 전체 red의 직접 원인은 known F86 freshness-checker false positive

F86 audit 단계 로그:

- `신선도 — 가장 오래된 탭 0분 전 (허용 120분)`
- `칸 대조 — 탭 19장 · 차 1088대 · 칸 45,186개 · 어긋남 0`

그 직후 checker는 아래 current builder 형태를 모두 오류로 판정했다.

- `종합 09.17 23:58 · 394대`
- `손오공 · 240대`
- `이안카 · 164대`
- ... 공급사 전체

오류: `탭 이름에 발행 시각이 없다`

즉 freshness 계산 자체는 0분으로 정상인데, 탭명 regex/contract가 current F86 plan과 맞지 않아 exit 1을 냈다. audit (23)/(30)의 finding이 canonical scheduled production에서도 재현된 것이다.

## Evidence 4 — cross-audit/사진링크는 clean

F86 audit failure 뒤에도 workflow 조건상 cross-audit는 실행됐다.

- inventory drift: listable 0 / status_kind 0 / source identity 0 / deleted marker 0 / deposit rule 0
- Atom ↔ F01: missing 0 / should-remove 0 / cell diff 0
- F01 ↔ F86: missing 0 / extra 0 / cell diff 0
- `✓ 시트가 원자대로 박혔다`
- photo-link audit: F01/F86 전부 mismatch 0

따라서 run conclusion `failure`를 source/Atom/F01/F86 데이터 mismatch로 해석하면 안 된다.

## 기존 HOLD 영향

- audit (31): **“event 없음” 부분은 해소**, cadence/timeliness 및 full-run green은 HOLD.
- audit (23)/(30): **더 강해짐** — checker drift가 canonical scheduled workflow의 실제 red 원인으로 확인.
- audit (27): 한 회차 depositRuleViolations=0만으로 recurrence guard production 이식을 증명하지 못하므로 OPEN 유지.
- audit (28): Atom→projection parity와 SOURCE→ATOM 차량별 가격 의미는 다른 검사이므로 OPEN 유지.
- audit (29): sales-tab naming staged migration OPEN 유지.
- audit (33): RP031은 이 run에서도 current Google Sheet canonical path; API feeder cutover HOLD 유지.
- audit (34): `/shop` freshness는 여전히 newest Atom semantics; scheduled run 존재가 accessor 의미를 바꾸지 않음.
- legacy mirror/sales/settlement/RTDB ownership HOLD 유지.

## Claude 구현 Owner 액션

1. `audit-f86-vs-atom.mts`의 freshness tab-name parser를 `channel-f86-plan`의 current 계약과 공유/정렬한다.
2. 값/차량/칸 대조는 그대로 유지한다.
3. 수정 뒤 실제 `event=schedule` run에서 전체 green을 확보한다.
4. cadence 정상화는 scheduled run이 선언 시간대에 지속적으로 생성되는 증거로 별도 판정한다.

이번 감사에서는 application code/business logic을 수정하지 않았다.
DETAIL

          cat > CLAUDE-AUDIT.md <<'CLAUDE'
# ChatGPT 독립 SSOT 감사 — audit (35) override (2026-09-18 KST)

> 이 파일은 Claude 구현 Owner의 **최신 진입점 요약**이다. 과거 판정과 상세 증거는 `docs/AI-SSOT-AUDIT-LOG.md`와 최신 `docs/ai-ssot-audit/` 문서를 우선한다. 실제 application/business logic 수정은 Claude 단일 SSOT 세션만 수행한다.

## 현재 판정

- **schedule delivery 재출현:** audit (31)/(34) 당시 2026-09-17 `event=schedule` 0건이었으나 canonical `ERP5 SSOT 원천 최신화(매시간)` run **`35235961510`**이 실제 `event=schedule`로 생성됐다. 따라서 “schedule event 자체가 없다”는 요약은 stale이다.
- **cadence/timeliness는 여전히 HOLD:** 이 run은 2026-09-17 **23:47 KST**에 시작했다. workflow cron은 월~토 KST 09:05~19:05이므로 당일 마지막 선언 slot보다 4시간 42분 이상 늦다. 한 회차 출현만으로 hourly 정시 운용 정상화를 선언하지 않는다.
- **canonical write path는 실제 동작:** production pin `9bef7bf0ffd21a96e3098a6f31adf1b1a0258c60`에서 supplier preflight/apply 24/24, settlement Atom-lock, snapshot, public catalog, F01 694대, F86 19탭/694대 발행까지 성공했다.
- **현재 scheduled blocker = F86 freshness checker:** F86 값 대조는 45,186칸 mismatch 0이고 freshness 계산도 가장 오래된 탭 0분이었지만, checker가 current tab-name 계약을 이해하지 못해 19개 탭 전부 “발행 시각 없음”으로 실패시켰다. Atom↔F01↔F86 cross-audit와 photo-link는 모두 mismatch 0이다. 따라서 run red는 데이터 발행 실패가 아니라 audit (23)/(30)의 checker-contract drift다.
- audit (27) Sonogong deposit recurrence, audit (28) vehicle-price lineage, audit (29) sales-tab naming migration, audit (33) RP031 API feeder/provenance, audit (34) newest-Atom freshness observability, mirror/sales/settlement/RTDB legacy writer ownership HOLD는 이 한 회차로 닫지 않는다.
- canonical source는 계속 RP006=`ironrentcar.com`, RP012=`sokrc.com/api`, RP023=RebornCar다. RP031은 아직 Google Sheet canonical이다.
- current main은 `c6f6c3e0253e6d6f172ce52067d7261358b3e4a9`; production pin은 `9bef7bf0ffd21a96e3098a6f31adf1b1a0258c60`으로 변화 없다.

## Claude 구현 Owner 우선순위

1. `audit-f86-vs-atom.mts` freshness 판단을 `channel-f86-plan`의 실제 탭명 계약(종합에만 시각, 공급사 탭은 회사명+대수)과 공유/정렬한다. **차량/칸 값 대조는 약화하지 않는다.**
2. 수정 뒤 실제 `event=schedule` 회차에서 source→Atom→snapshot→public/F01/F86→F86 audit→cross-audit→photo-audit 전체 green을 확보한다.
3. schedule cadence 정상화는 선언된 시간대에 scheduled run이 지속 생성되는 별도 증거로 판정한다. workflow_dispatch/one-time run으로 대체하지 않는다.
4. audit (27)/(28)/(29)/(33)/(34)와 legacy writer ownership은 각각의 promotion/runtime 증거가 생길 때만 닫는다.
5. production pin/current main을 같은 계보라고 가정하지 말고 항상 별도로 대조한다.

## 상세 근거

- `docs/AI-SSOT-AUDIT-LOG.md` — 최신 audit (35)
- `docs/ai-ssot-audit/2026-09-18-chatgpt-audit35-scheduled-delivery-f86-checker.md`
- GitHub Actions scheduled run `35235961510`, job `105251799335`

이번 ChatGPT 감사에서는 application code/business logic을 수정하지 않았다.
CLAUDE

          git config user.name 'ChatGPT SSOT Auditor'
          git config user.email 'actions@users.noreply.github.com'
          git add docs/AI-SSOT-AUDIT-LOG.md docs/ai-ssot-audit/2026-09-18-chatgpt-audit35-scheduled-delivery-f86-checker.md CLAUDE-AUDIT.md
          git commit -m 'audit: record scheduled delivery and F86 checker failure'
          git push origin HEAD:main

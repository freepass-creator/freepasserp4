# 독립 검증 결과

## 2026-08-15 상품마스터 단일 정본 → ERP 전환 검증

결과: **로컬 규격·시뮬레이션 PASS / 운영 반영 NO-GO(시트 권한·판매대상 대수 확인 필요)**

- ERP 일괄연동과 상태 전용 연동의 입력을 과거 판매시트 4개 탭에서 `ERP4 차종마스터 원천대장/상품마스터` 한 탭으로 전환했다. 공급사 원본은 비교·갱신 자료로만 남고 ERP를 직접 덮어쓰지 않는다.
- 시트 50열 헤더 이름·순서, 차량번호·상품키 중복, 공급사코드, 검증상태↔차종코드, 차종코드↔표시 차명의 정확한 조인을 반영 전 차단 게이트로 추가했다. 이로써 차종코드는 Model Y인데 표시 차명이 레이로 밀리는 유형은 전체 반영 전 중단된다.
- 검증된 상품마스터 차종코드는 ERP에 남은 과거 제조사·모델·카탈로그ID·트림행키 오매칭을 교정한다. 반대로 미매칭 상품마스터 원문과 이후 직접 공급사 시트는 한 번 확정된 차종 식별자를 다시 덮지 못하도록 병합 소유권을 고정했다.
- 전체 연동과 공급사 선택연동의 `sourceScope`를 분리했다. 한 공급사만 선택한 스냅샷을 전체 정본으로 오인해 나머지 공급사를 일괄 부재·출고불가 처리할 수 없도록 시뮬레이션으로 고정했다.
- 상품마스터 실측은 587대, 20개 공급사, 중복 0, 공급사코드 누락 0이다. 확정 378대, 검수필요 9대, 미매칭 200대이고 코드가 있는 387대는 모두 로컬 행 단위 차종마스터와 일치한다.
- 가격쌍 불완전은 16개 기간(리더스 3대 7개, 스타 9대의 12개월 보증금)이다. 빈 보증금을 0원으로 추정하지 않고 해당 기간만 제외한다. 현재 활성 상태이면서 사용 가능한 가격이 없는 차량은 4대다.
- 오토플러스 원본을 숨김·필터 행 제외 방식으로 다시 읽어 현재 93대(본목록 80 + EV 프로모션 전용 13)를 확인했다. 상품마스터 현재차 누락 0, 대여료 원자 차이 0, 보증금 산식 차이 0이며 원본 제외 이력 5대는 출고불가로 분리돼 총 98대다.
- 아이카 `재고` 원본 122대와 상품마스터 122대를 재대조해 차량번호·가격·보증금·정책 차이 0건을 확인했다. 상품마스터의 0원 보증금 78개 기간은 원본의 명시적 `무보증`이며, 진짜 공란을 0원으로 추정한 기간은 없다.
- 이안카 본탭 46대와 재렌트 31대의 정본 77대를 상품마스터 77대와 재대조해 차량번호·가격·보증금·상태·정책·입고일자 차이 0건을 확인했다. 차종 미매칭 5대는 가격 연동과 분리돼 있다.
- 아이언 홈페이지 전체 71대의 상세 71/71을 다시 읽어 오류·차번·가격·사진 누락 0을 확인했다. 활성 29대와 상품마스터 29대의 차량번호·가격·보증금·상태·사진 차이도 0이며, 비표준 53개월은 표준 기간으로 오염시키지 않고 원문에 보존했다.
- 빌린카·엘씨렌트·빌린카구독 세 탭의 실차번 45대(`미정` 2행 제외)와 상품마스터 45대를 재대조해 차량번호·기간별 가격·보증금 차이와 중복 0건을 확인했다.
- 우리캐피탈 원본 20대와 상품마스터 20대를 재대조해 차량번호·상태·가격·보증금 차이 0건을 확인했다. 1개월 열의 20개 조건 안내 문장은 대여료로 읽지 않으며, Google 공급사 매뉴얼의 낡은 `1건` 표기도 `20건`으로 갱신 후 재조회했다.
- 에스에이 32대, 스타 28대, 리더스 현재 6대(+출고불가 이력 1대)를 각각 상품마스터와 대조해 차량번호·입력 가격 숫자 차이 0건을 확인했다. 스타 9개 12개월 보증금과 리더스 현재차 4개 가격쌍은 원본 공란이므로 0원 추정 없이 게시 제외 대상으로 유지한다.
- 우리 제공 규격군의 KH 11대, 제이앤제이 7대, 경진카 3대, 경진렌트카 1대, 퍼시픽 1대, 에코렌트카 5대와 센트로 변형 2대를 각각 대조했다. 차량번호·상태·기간별 대여료·보증금·정책 차이와 중복은 0건이며 차종은 합계 확정 25대·미매칭 5대다.
- 스위치플랜 `재고` 탭은 사용자 계정과 운영 서비스계정 모두 읽기 타임아웃으로 실패했다. Google 공급사 매뉴얼 판정을 `검수필요`로 고치고, 상품마스터 8대를 현재 원본과 일치한 것으로 오인하지 않으며 재조회 전 상태·가격 자동갱신 금지를 기록했다.
- 렌트존은 2행 헤더 원본 현재 6대와 상품마스터 현재 6대의 차량번호·상태·48·60개월 대여료/보증금 차이 0건을 확인했다. 원본부재 `181하5329`는 출고불가 이력으로 보존해 총 7대다. 사용자 확인으로 대표코드는 `PT-0001`이지만 운영 상세 파트너 정보는 아직 `PT-0014`에 남아 있고 `PT-0001`은 삭제 이력+동기화 시각만 있다. 활성 파트너 정본이 없는 상품마스터 공급사는 조회에 표시하되 상품·상태 저장을 fail-closed 하는 게이트를 추가했다.
- 상품마스터 서버 입력은 같은 문서의 `공급사 데이터 매뉴얼`도 함께 읽는다. 공급사코드의 매뉴얼 행이 없거나 주의사항에 `자동반영 금지`·`반영 차단`이 명시되면 실제 상품·상태 커밋을 중단한다. 스위치플랜 원본 조회 실패와 렌트존 파트너 이관 미완료가 문서 메모로만 남지 않고 실행 게이트에 연결된다.
- 라이브 원천 재조회에서 상품마스터 공급사코드 20개는 매뉴얼 코드 21개에 전부 존재해 매뉴얼 누락 0건이다. 명시적 자동반영 차단은 렌트존 `PT-0001`과 스위치플랜 `RP014` 두 곳만 정확히 탐지됐다.
- 운영 파트너 정본은 상품마스터 20개 코드 중 19개만 활성이고 렌트존 `PT-0001`만 비활성이다. v4 참조 실측은 상품 7대 상당의 `PT-0001` 원자 28개, 계약 0, 방에는 `PT-0001` 3개·`PT-0014` 2개가 남아 있어 파트너 레코드만 성급히 덮어쓰지 않고 방 참조까지 함께 이관해야 한다.
- 전자계약 필수 파트너 필드(상호·사업자번호·대표자·주소·자동차대여사업 등록번호) 기준으로 완전한 활성 공급사는 현재 1곳뿐이다. 18곳은 대부분 주소·대여사업번호가 없고, 에코렌트카·이안카·제이앤제이는 더 많은 법정 표시값이 비어 있다. 상품찾기와 분리된 기존 전자계약 발행 게이트가 해당 회사의 계약 발행을 차단하는 상태이며 값을 추정 입력하지 않았다.
- 현재 규칙으로 투영한 상품마스터 재고상태는 521대, 가격까지 갖춘 상품찾기 예상은 517대다. 동일 시점 과거 판매시트는 508대를 읽지만
  삭제이력 충돌 3건·가격기간 누락 12건으로 반영이 차단됐고, 실제 ERP 활성 623대 중 상품찾기 규칙 통과는 432대다. 따라서 과거 기대치 477대는 현재
  어느 정본의 실측치도 아니며, 운영 적용 전 517대의 차량번호별 dry-run 결과 확인이 필요하다.
- `sim-product-master-import`, `sim-product-master-sheet`, `sim-sheet-live-status` 11/11, `sim-sheet-daily-sync` 25/25,
  `sim-sales-inventory-sheet`, `npx tsc --noEmit`, `check:fonts`, `check:tokens`, `check:ui`, `check:manual`,
  `npm run build`, `git diff --check` PASS.
- 운영 서버 서비스 계정 `firebase-adminsdk-fbsvc@freepasserp3.iam.gserviceaccount.com`은 현재 신규 상품마스터 문서 읽기가 권한 오류로 실패한다. 사용자의 명시적 승인 후 읽기 전용 공유하고 실서버 dry-run을 통과하기 전에는 커밋·푸시·배포하지 않는다.

## 2026-08-15 포터Ⅱ EV 현행 특장 차종마스터 보강 11차
결과: **차종 게이트 PASS / 저장소 폰트 게이트 외부 실패 — 현행 EV 특장 6행 추가**

- 현대 포터Ⅱ Electric 특장차 공식 가격 페이지 2026-08-01 기준으로 EV 저상·일반·하이 내장탑차, 파워게이트, 수동식·전동식 윙바디 6행을 추가했다.
- 모델코드 `mf-001.md-083`~`md-088`을 신규 발급했다. 공식 가격 페이지에 세부 트림명이 없어 임의 추정하지 않고 `단일 사양`으로 기록했다.
- 기본차 공식 가격표의 135kW·60.4kWh를 적용했으나 특장 소개 페이지 제원은 60.3kWh로 표기가 병존한다. 이 불일치를 근거메모에 명시하고 신규 6행을 모두 `검증중/1차확인` 수동 후보로 유지했다.
- 원천 시트 `차종마스터!A7002:AD7007`, 로컬 산출물, 영구키 레지스트리를 동기화했다. 신규 트림행키 중복 0건, `capturedSheetRow` 7002~7007, 원천 서식·공식 링크·조건부 서식 확장을 확인했다.
- 최종 로컬 집계는 총 4,753행, 자동 2,913행, 수동 217행, 차단 1,623행이다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `git diff --check` PASS. `check:fonts`는 계속해서 본 작업과 무관한 기존 `app/page.tsx:71`의 `fontSize: 13.5` 1건으로 실패한다.

## 2026-08-15 포터Ⅱ 온도물류·윙바디 차종마스터 보강 10차
결과: **차종 게이트 PASS / 저장소 폰트 게이트 외부 실패 — 2024 포터Ⅱ LPG 특장 38행 추가**

- 현대 2024 포터Ⅱ 특장차 공식 가격표를 근거로 냉동·초저온·하이·저상냉동 및 냉장·하이냉장 탑차 28행과 윙바디 수동식·전동식 10행을 추가했다.
- 모델코드 `mf-001.md-075`~`md-082`를 신규 발급했다. 트윈컴프는 냉동장치 구성, 윙바디 수동식·전동식은 개폐방식으로 차체명에 유지하고 수동6단·자동5단 파워트레인과 분리했다.
- 신규 38행은 모두 `중고차`, `검증중/1차확인`, 수동 배정 등급이다. 생산종료일은 공식 근거가 없어 공란으로 유지했다.
- 원천 시트 `차종마스터!A6964:AD7001`, 로컬 산출물, 영구키 레지스트리를 동기화했다. 신규 트림행키 중복 0건, `capturedSheetRow` 6964~7001, 원천 서식·공식 링크·조건부 서식 확장을 확인했다.
- 최종 로컬 집계는 총 4,747행, 자동 2,913행, 수동 211행, 차단 1,623행이다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `git diff --check` PASS. `check:fonts`는 본 작업과 무관한 기존 `app/page.tsx:71`의 오프스케일 `fontSize: 13.5` 1건으로 실패했으며 사용자 작업을 임의 수정하지 않았다.

## 2026-08-15 포터Ⅱ 최근 유통 특장 차종마스터 보강 9차
결과: **PASS — 2024 포터Ⅱ LPG 내장탑 계열 20행을 중고차 수동 후보로 추가**

- 현대 2024 포터Ⅱ 특장차 공식 가격표와 2024-01-09 출시일을 근거로 내장탑차 8행, 하이내장탑차 8행, 내장탑 파워게이트 2행, 저상내장탑차 2행을 추가했다.
- 슈퍼캡·일반캡·더블캡, 스타일·스마트·모던 플러스 및 수동6단·자동5단을 실제 판매 가능 축으로 분리하고 모델코드 `md-071`~`md-074`를 신규 발급했다.
- 2024 연식 유통분은 `중고차`, `검증중/1차확인`, 수동 배정으로 격리했다. 공식 근거가 없는 생산종료일은 추정하지 않고 공란으로 유지했다.
- 원천 시트 `차종마스터!A6944:AD6963`, 로컬 산출물, 영구키 레지스트리를 동기화했다. 신규 트림행키 중복 0건, `capturedSheetRow` 6944~6963, 서식·공식 링크·조건부 서식 확장을 확인했다.
- 최종 로컬 집계는 총 4,709행, 자동 2,913행, 수동 173행, 차단 1,623행이다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `check:fonts`, `git diff --check` PASS. `git diff --check`는 기존 작업 파일들의 LF→CRLF 안내만 출력했다.

## 2026-08-15 렌트·구독 LPG 물류 특장 차종마스터 보강 8차
결과: **PASS — 봉고III LPG 물류 특장 현행 판매 조합 16행을 수동 후보로 추가**

- 기아 봉고III 특장 2026-07 공식 가격표를 근거로 양문형 미닫이탑차 2행, 택배전용탑차 1행, 내장탑 파워게이트 1행, 워크스루밴 1행, 윙바디 11행을 추가했다.
- 모델코드 `md-087`~`md-091`을 신규 발급하고 차체형식별 영구 ID를 분리했다. 윙바디 수동식·전동식·확장형은 개폐방식 축, 수동6단·자동5단은 파워트레인 축으로 독립 유지했다.
- 신규 16행은 모두 `검증중/1차확인` 및 수동 배정 등급이다. 기존 코드 의미 변경 없이 신규 트림행키 중복 0건을 확인했다.
- 원천 시트 `차종마스터!A6928:AD6943`, 로컬 산출물, 영구키 레지스트리를 동기화했다. `capturedSheetRow` 6928~6943, 원천 서식·공식 링크·조건부 서식 확장을 재확인했다.
- 최종 로컬 집계는 총 4,689행, 자동 2,913행, 수동 153행, 차단 1,623행이다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `check:fonts`, `git diff --check` PASS. `git diff --check`는 기존 작업 파일들의 LF→CRLF 안내만 출력했다.

## 2026-08-15 렌트·구독 온도물류·EV 특장 차종마스터 보강 7차
결과: **PASS — 봉고III 냉장탑 및 EV 특장 현행 판매 조합 16행을 수동 후보로 추가**

- 기아 봉고III 특장·EV 특장 2026-07 공식 가격표를 근거로 LPG 냉장탑차 3행, 냉장탑 파워게이트 1행, EV 내장탑차 3행, EV 냉동탑차 3행, EV 윙바디 2행, EV 양문형 미닫이탑차 2행, EV 파워게이트 2행을 추가했다.
- 냉장과 냉동, 일반 탑과 파워게이트, LPG와 EV를 각각 독립 모델코드 `md-080`~`md-086`으로 분리했다. EV 윙바디의 수동식·전동식은 변속기가 아니라 윙 개폐방식으로 차체명에만 반영했다.
- 신규 16행은 모두 `검증중/1차확인` 및 수동 배정 등급이다. 기존 영구 코드 의미 변경 없이 신규 트림행키 중복 0건을 확인했다.
- 원천 시트 `차종마스터!A6912:AD6927`, 로컬 산출물, 영구키 레지스트리를 동기화했다. 레지스트리 `capturedSheetRow` 6912~6927, 원천 서식·공식 링크·조건부 서식 확장을 재확인했다.
- 최종 로컬 집계는 총 4,673행, 자동 2,913행, 수동 137행, 차단 1,623행이다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `check:fonts`, `git diff --check` PASS. `git diff --check`는 기존 작업 파일들의 LF→CRLF 안내만 출력했다.

## 2026-08-15 렌트·구독 특장 상용차 차종마스터 보강 6차
결과: **PASS — 봉고III LPG 내장탑차·냉동탑차 현행 판매 조합 28행을 수동 후보로 추가**

- 기아 봉고III 특장 2026-07 공식 가격표를 근거로 1톤 2WD 내장탑차 14행과 냉동탑차 14행을 추가했다. 로우·스탠다드·하이, 킹캡·표준캡, L라이트·L·GL 및 수동6단·자동5단 판매 가능 조합을 차체별로 분리했다.
- 내장탑차는 `md-078`, 냉동탑차는 `md-079`를 신규 발급했다. 기존 일반 트럭·파워게이트 코드 의미는 변경하지 않았고, 신규 28행은 모두 `검증중/1차확인`으로 제한해 자동매칭에 유입되지 않는다.
- 원천 시트 `차종마스터!A6884:AD6911`, 로컬 산출물, 영구키 레지스트리를 동기화했다. 신규 트림행키 중복 0건, 레지스트리 `capturedSheetRow` 6884~6911 연속 범위, 원천 서식·공식 링크·조건부 서식 확장을 확인했다.
- 최종 로컬 집계는 총 4,657행, 자동 2,913행, 수동 121행, 차단 1,623행이다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `check:fonts`, `git diff --check` PASS. `git diff --check`는 기존 작업 파일들의 LF→CRLF 안내만 출력했다.

## 2026-08-15 렌트·구독 특장 상용차 차종마스터 보강 5차
결과: **PASS — 봉고III LPG 파워게이트 현행 판매 조합 6행을 수동 후보로 추가**

- 기아 2026-08 공식 파워게이트 가격·특장제원을 근거로 킹캡 초장축 2WD 수직형(2단)·턴인형(2단)을 등록했다. 1톤은 자동5단 L 2행, 1.2톤은 수동6단·자동5단 GL 4행으로 실제 판매 조합을 분리했다.
- 기존 영구 코드의 의미는 변경하지 않고 `md-077` 모델 코드, 마스터 ID 4개, 트림행키 6개를 신규 발급했다. 자동매칭 대상이 아닌 `검증중/1차확인` 상태로 제한했다.
- 원천 시트 `차종마스터!A6878:AD6883`과 로컬 산출물·영구키 레지스트리를 동기화했다. `capturedSheetRow`는 6878~6883을 모두 포함하며 신규 코드 중복은 0건이다.
- 최종 로컬 집계는 총 4,629행, 자동 2,913행, 수동 93행, 차단 1,623행이다. 원천 시트의 Noto Sans KR 9pt·CLIP·공식 링크 서식과 조건부 서식 확장을 재확인했다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `check:fonts`, `git diff --check` PASS. `git diff --check`는 기존 작업 파일들의 LF→CRLF 안내만 출력했다.

## 2026-08-12 전자계약·운영 UI 배포 게이트

결과: **PASS — production build 33/33 / 계약·약관·정책 회귀 통과**

- `NEXT_DIST_DIR=.next-publish npm run build`로 개발 서버 산출물과 분리해 Next.js production build 33/33 페이지를 완료했다.
- `npx tsc --noEmit`, `check:fonts`, `check:tokens`, `check:ui`, 변경파일 `git diff --check`를 통과했다.
- 전자계약 약관 44/44, 계약유형 60/60, 착한거래 발행 37/37, 서명 완료 Snapshot, 공급사 허브 3/3을 통과했다.
- 공유 부속서식 `contract-individual.html`은 `출고 시 주행거리`, `초과주행 요금`, 자동이체 문구를 의도적으로 정제해 착한거래 저장소 사본과 3곳이 다르다. 프리패스 본계약서 `rental-contract.html` 정본에는 영향이 없다.
- `database.rules.json` 변경은 커밋 대상에 포함하되, 운영 Firebase Rules 실게시는 사람의 실데이터 검증 전까지 수행하지 않는다. 이번 배포 대상은 Vercel 애플리케이션이다.

---

## 2026-08-12 전자계약 ↔ 계약회사 정책 왕복

결과: **PASS — 정책 신규·수정 진입과 작성 중 계약값 복원 / 미확인 보험값 저장 없음**

- 전자계약에서 연결 정책이 없는 프리패스 계약회사를 선택하면 `/policy?new=1&provider=OP001`로 이동해 `프리패스`가 선택된 신규 정책의 보험 패널을 바로 연다.
- 연결 정책은 있으나 필수값이 부족하면 해당 정책 코드와 보험 패널을 직접 여는 링크를 제공한다.
- 정책 이동 전에 작성 중인 고객·연락처·차량·금액·계약서 선택값을 탭 단위 임시영역에 보관한다. 정책 저장 시 방금 저장한 정책을 선택해 복원하고, 취소 시 기존 선택 그대로 전자계약으로 돌아간다.
- 실제 로그인 브라우저에서 `왕복보존테스트` 고객명, `010-1234-5678` 연락처, `테스트 차량` 모델명과 프리패스 선택을 넣고 정책 화면에 진입한 뒤 취소했다. 네 값 모두 복원되고 주소가 `/esign`으로 정리되는 것을 확인했다.
- 실제 로그인 브라우저에서 `프리패스 [selected]`, 신규 정책, 보험 패널, `공급사 확인 필요 15개` 안내를 확인했다. 보험증권으로 확인되지 않은 회사별 값은 입력하거나 저장하지 않았다.
- `npx tsc --noEmit`, `check:fonts`, `check:ui`, 변경파일 `git diff --check` PASS.
- 전체 production build는 이번 변경 파일이 아니라 기존 `app/dev/page.tsx → SheetSync → google-sheet-visible.ts(server-only)` import 경계에서 중단됐다. 4004 개발 서버는 재기동 후 정책 딥링크 HTTP 200을 확인했다.

---

## 2026-08-11 프리패스 장기대여 약관 V11 원본대조

결과: **CONDITIONAL PASS — 공정위 자동차대여 표준약관형 28개 조문 흐름 유지 / 실제 고객 발송 전 렌터카회사·법률 최종 승인 필요**

- 공정위 자동차대여 표준약관의 계약신청→체결→인도→사용·관리→보험·사고→반환·정산 흐름을 유지하고, 확보한 `표준 장기렌트계약서_2024_12_30.hwp`와 `계약서양식/장기계약서.pdf`를 조항별로 대조했다.
- 실제 장기대여 원본에서 공통 확인되는 추가운전자 사전승인·자격확인, 제작사 보증수리·리콜 협조, 임차인 추가장착품 관리, 블랙박스 사고자료 보존, 등록명의자인 회사에 먼저 청구된 금액의 자료기반 정산, 사전 반납평가기준·독립기관 평가, 채권양도 시 사용권 보호·변경통지를 보강했다.
- 보증금 무조건 몰수, 무최고 자동해지·회수, 현장출동이 없다는 이유만으로 보험처리를 전면 배제하는 문구, 회사 지정법원 전속관할 등 일방적이거나 분쟁 위험이 큰 원본 문구는 반영하지 않았다.
- 약관 제목을 `자동차 장기대여 약관`, 문서 버전을 `rental-v11-2026-08-11`로 변경했다. 인쇄/PDF 본문과 전자계약 전송본은 28개 조문 전체가 동일하다.
- 약관 자동 페이지네이션은 전체 내용량 기준 최소 A4 쪽수로 다시 배치한 뒤 넘침 시 안전 배치로 복구하도록 보완했다. 약관 3쪽 6개 단 사용 높이는 `896.4~931.9px / 956.4px`, 넘침 0건이며 전체 9쪽 PDF를 PNG로 확인해 표·약관·서명 페이지의 겹침·잘림·깨진 글자가 없음을 확인했다.
- 검증: `npx tsc --noEmit`, `npm run check:fonts`, 약관 44/44, 계약유형 60/60, 조항 배지 27/27, PDF A4 배치 PASS.

---

## 2026-08-11 상품찾기 공급사 열 고정 노출

- 엑셀 보기에서 필터 패널을 열거나 닫아도 `공급사` 열이 항상 노출되도록 수정했다.
- 필터 화면의 셀 좌우 패딩과 세부모델·파워·트림 폭을 압축해 공급사 추가분을 흡수했다.
- 화면 폭이 부족할 때도 공급사보다 덜 중요한 선택 열을 먼저 숨기도록 열 축소 우선순위를 조정했다.
- 로그인된 실제 브라우저에서 필터를 연 상태로 `공급사` 노출, 가로 스크롤 없음, 기존에 빠지던 파워·옵션 열 재노출을 확인했다.
- `sim-excel-provider-column` 3/3, `tsc --noEmit`, `check:fonts` PASS.

## 2026-08-11 관리자 상담데스크

- 관리자 `/chat`을 목록 390px + 채팅 중심 화면으로 분리하고, 영업자·공급사의 기존 4패널 화면은 유지했다.
- 차량 요약은 대표사진·차량명·차번·상태·공급사·담당자만 표시하며, 사진·PDF 첨부 모아보기를 채팅 상단에 노출했다.
- 관리자 기본 진입은 `내 차례` + `오래 기다린 순`으로 설정하고 실제 운영 데이터에서 선택·첨부·입력 UI를 확인했다.
- `sim-admin-queue`, `sim-chat-attention`, `sim-chat-room-dedupe` PASS. `tsc --noEmit`, fonts, 변경파일 `git diff --check` PASS.

## 2026-08-11 전체메뉴 진입점 명확화

- 데스크톱 햄버거를 `전체메뉴`, 모바일 메뉴를 `더보기`로 명시하고 열림 상태는 `닫기`로 통일했다.
- 실제 로그인 화면에서 데스크톱 메뉴 열기와 모바일 390px 표기를 확인했다.
- `tsc --noEmit`, fonts, 변경파일 `git diff --check` PASS.

## 2026-08-11 전체 주행거리 원단위 표시

- 목록·카드·상품찾기 표·공유문구·상세페이지의 주행거리를 모두 실제 km로 표시한다. 예: `326km`, `12,450km`.
- `sim-agent` 회귀 테스트 45/45, `tsc --noEmit`, fonts, 변경파일 `git diff --check` PASS.

## 2026-08-10 프리패스 전자계약 발송센터 v1 완성

결과: **CONDITIONAL PASS — 기능 구현·정적/회귀 검증 완료 / 법률 승인과 운영 실계정 왕복만 배포 게이트로 남음**

### 사용자 원요구 충족

- `/esign` 첫 화면을 프리패스 자체 `전자계약` 발송센터로 교체했다. `Excel 계약서 넣기`를 주 진입점으로 두고 `직접 작성`, `ERP 계약에서`까지 세 경로를 제공한다.
- 세 경로는 별도 발송 로직을 만들지 않고 동일한 계약 레코드 → 검증 → 발행 Snapshot → 고객 링크 → 관리자 검토 → 봉인 PDF 엔진을 사용한다.
- 독립 경로는 `contract_source=excel|direct`를 명시하며 ERP의 `provider_agreement_done`을 가짜로 채우지 않는다. ERP 경로만 기존 약정 완료 게이트를 유지한다.
- 업체 대표자·주소·연락처·입금계좌는 파트너 고정값으로 한 번 저장하고, 계약마다 고객·차량·기간·대여료·보증금·정책과 예외값만 확정한다.
- 목록은 `발송대기 / 서명중 / 확인필요 / 완료` 네 상태와 고객·차량·렌터카사·대여료·기간·상태 6개 핵심값만 표시한다.
- 완료 계약은 재발행·링크 해지·초안 생성을 막고 읽기 전용으로 유지한다. 인도 확인, CMS, 연대보증은 본계약과 별도 후속 절차 및 별도 문서로 분리했다.

### 데이터·보안 경계

- 발행 시 계약·정책·업체·템플릿 값을 Snapshot으로 동결하고 이후 원본 변경으로 고객 화면이나 완료 PDF를 재조립하지 않는다.
- Excel 원본은 메모리에서만 읽고 저장하지 않는다. 주민등록번호·면허번호는 import 대상에서 제외하며 개인·개인사업자·법인 양식 인식 실패는 임의 추정하지 않고 차단한다.
- 고객 링크 원문은 256-bit 난수 토큰이며 서버에는 SHA-256 해시만 둔다. 신분증·셀카·서명·본인확인 입력은 공개 계약 노드가 아니라 서버 전용 `v4/esign_private`와 비공개 Storage에 저장한다.
- 고객 제출만으로 완료시키지 않고 관리자가 신분증·셀카·서명을 확인해 승인할 때 완료 PDF·문서 SHA-256·공개 검증 해시를 봉인한다.
- 신규 write는 `v4/`만 사용했고 `database.rules.json`은 수정·게시하지 않았다.

### 검증 결과

- `npx tsc --noEmit`, `npm run build`(Next.js 15.5.21, 33/33), `check:fonts`, `check:tokens`, `check:template-drift` PASS.
- source gate, Excel adapter, Snapshot 불변성, 본계약 문서 경계, 자체 전자계약 화면/경로, 발행 잠금, 업체 템플릿 프로필 PASS.
- 계약 유형 60/60, 약관 50/50, 필드맵 13/13, 템플릿 프로필 10/10, 발행 잠금 5/5 PASS.
- `http://localhost:4004/`, `/esign`, `/sign/not-a-valid-token` production 서버 HTTP 200. 최신 build로 4004를 재기동했다.
- `check:ui`는 이번 전자계약 파일 오류 0이며 기존 dirty worktree의 `ChatThread`·`ConsultPanel`·`DevRoleSwitch` 3건만 남는다. 해당 파일은 이번 범위에서 수정하지 않았다.

### 배포 전 사람/Claude 게이트

- 표준계약서 3벌은 여전히 `sample-v1`이므로 운영 발행은 코드에서 잠겨 있다. 법률·실무 승인본과 개인정보 보유·파기정책을 확정한 뒤 잠금을 열어야 한다.
- 관리자 실계정과 테스트 고객으로 3종 × 인수/반납의 `발행 → 모바일 본인확인/서명 → 관리자 승인 → 완료 PDF 다운로드/해시 검증` 왕복을 각 1건 수행해야 한다.
- 현재 본인확인은 외부 본인인증기관 자동 판정이 아니라 관리자의 신분증·셀카 수동 대조 방식이다.

---

## 2026-08-10 프리패스 자체 전자계약 MVP

결과: **CONDITIONAL PASS — 자체 발송·서명·관리자 확정·봉인 PDF 흐름 구현 / 실고객 발송 전 사람 게이트 필요**

- 계약서관리 ③을 `프리패스 전자계약 발송`, ④를 `전자계약 진행상황`으로 교체했다. ③에서 프리패스 계약 데이터와 표준계약서 3벌·인수/반납 조합을 검증해 고객 링크를 발행하고 A4 미리보기/PDF를 만든다. ④에서 열람·제출·반려·관리자 확정·완료 상태, 신분증·셀카·서명, 완료 PDF와 공개 봉인 해시를 확인한다.
- 고객 `/sign/[token]`은 계약 조건·전체 약관을 확인한 뒤 성명·연락처·신분증·셀카·필수동의·서명을 제출한다. 큰 휴대폰 사진은 브라우저에서 자동 압축하며, 신분증·셀카·서명·개인정보는 공개 계약 노드가 아니라 서버 전용 `v4/esign_private`와 비공개 Storage에 저장한다.
- 발송 링크는 원문이 DB 키가 되지 않는 256-bit 난수 토큰이고 서버에는 SHA-256 해시만 저장한다. 고객 제출만으로 계약완료가 되지 않으며 실제 플랫폼 관리자가 신분증·셀카를 검토해 승인할 때만 서명시각·봉인·검증 레코드를 확정한다. 신규 write는 모두 `v4/`이며 `database.rules.json`은 변경·게시하지 않았다.
- 완료본은 기존 A4 계약서 HTML에 동결 스냅샷과 서명을 주입해 PDF로 렌더링하고 SHA-256 문서 해시를 남긴다. 공개 `/verify/[sealHash]`는 개인정보 없이 계약번호·서명확정시각·봉인/문서 해시만 검증한다.
- 기본 `npm run dev`는 착한거래 서버를 기다리지 않고 프리패스 4004만 시작한다. 기존 외부 연동 실행은 `npm run dev:with-chakhandeal`로 보존했고, 과거 착한거래 발행 건은 읽기 호환만 유지한다.
- 검증: `npx tsc --noEmit`, `npm run check:fonts`, `npm run check:tokens`, production build 33/33, 자체 전자계약 보안 회귀, 계약유형 60/60, 필드맵 13/13, 약관 50/50 PASS. 무인증 API는 관리자 상태 401, 신분증/PDF 403, 잘못된 고객 토큰 404를 반환했고 재시작한 `http://localhost:4004/sign/not-a-valid-token`은 HTTP 200으로 정상 렌더링한다. `check:ui`는 이번 전자계약 파일 오류 0이며 기존 `ChatThread`·`ConsultPanel`·`DevRoleSwitch` 3건만 남는다.
- 실발송 전 필수: ① 법률·실무 승인된 표준계약서 3벌과 개인정보 보유·파기정책 확정 ② 관리자 실계정으로 3종×인수/반납 링크 발행→고객 제출→승인→완료 PDF 왕복 테스트 ③ 운영 PDF 런타임의 Chrome 제공 확인이다. 현재 본인확인은 외부 인증기관 자동 인증이 아니라 관리자의 신분증·셀카 수동 대조 방식이다.

---

## 2026-08-10 계약서 상세 `목록` 복귀 버튼

결과: **PASS — 긴 데이터 패널 상단에서 즉시 목록 복귀**

- 계약서관리 ② 프리패스 데이터의 기존 계약 상세과 신규 입력 헤더에 공용 `NavBack(kind=list)`을 추가했다.
- 웹은 `PaneHead` 우측, 모바일은 상세 패널 최상단 고정 영역에 표시한다. 기존 공통 하단독의 목록 동작도 유지한다.
- 기존 계약에서 `목록` 클릭 시 선택을 해제하고 계약 목록으로 복귀하며, 신규 입력에서는 입력 화면만 닫고 목록으로 복귀한다.
- 브라우저에서 기존 계약 상세·신규 입력 모두 `목록` 버튼 노출, 기존 계약 → 목록 복귀, 콘솔 오류 0건을 확인했다.
- `npx tsc --noEmit`, `npm run check:fonts` PASS.

---

## 2026-08-10 신규 계약서의 기존 계약 검색·불러오기

결과: **PASS — 진행중은 계약서 발송 / 완료는 인도일 관리**

- 신규 계약서 입력 상단에 전체 계약 원장을 대상으로 하는 `계약 진행중`·`계약완료` 탭과 차번·계약번호·고객명 검색을 추가했다.
- 계약진행중 선택은 계약서 발송 대상으로 원래 계약을 그대로 연다. 기간·금액 확정 전 계약도 전체 원장에서 검색할 수 있다.
- 계약완료 선택도 새 계약으로 복사하지 않고 원래 계약을 연다. 이미 발송된 계약의 착한거래 진행상황에서 인도일을 입력·확인하는 흐름이다.
- 완전히 새로운 계약만 신규 입력폼에서 별도 생성한다.
- 브라우저에서 신규 패널의 탭·검색·최근 8건 표시를 확인하고 `CT-TEST-01` 진행 계약을 검색해 기존 계약 상세로 이어지는 것을 확인했다. 검증 중 신규 계약 저장은 수행하지 않아 운영 데이터 write는 0건이다.
- `npx tsc --noEmit`, `npm run check:fonts`, 계약유형 60/60, 필드맵 13/13 PASS. 현재 로컬 원장에는 `contract_status=계약완료` 건이 없어 완료 탭은 빈 상태까지만 브라우저로 확인했다.

---

## 2026-08-10 계약서관리 ③·④ 사용자 원본대조

결과: **로컬 화면 교체 PASS / 운영 embed 인증 미구현으로 Production NO-GO**

- 착한거래 `ContractStudio` 본체에 `embedPanel=link|progress` 표시 모드를 추가하고, `/labs/contract-studio/embed/link|progress`에서 실제 ②·③ JSX/CSS만 렌더링한다.
- 프리패스 ③·④는 자체 `ChakhandealPane`·`ProgressPane` 대신 위 두 화면을 iframe으로 항상 마운트한다.
- 브라우저 DOM에서 프리패스 ③ iframe의 `② 데이터연동 · 링크`, ④ iframe의 `③ 진행상황`을 확인했다. 계약 미선택 시 안내도 착한거래 화면이 직접 렌더링한다.
- `npx tsc --noEmit` PASS, 착한거래 embed 2개와 프리패스 `/esign` 모두 HTTP 200.
- 현재는 로컬 화면 우선 확인용 `/labs` embed다. 운영 전에는 작업 오더 0-2의 회원사 제한 embed session·origin 검증·CSP/frame-ancestors를 구현하고 `/labs` 의존을 제거해야 한다.
- 기존 프리패스 과거 계약과 로컬 착한거래 lab 계약의 식별자가 일치하지 않으면 원본 패널은 계약 미선택 상태다. 신규 발행 왕복 또는 실제 `esign_id`가 있는 계약으로 데이터 연결을 별도 검증해야 한다.

---

## 2026-08-10 계약서 신규 생성 → ② 프리패스 데이터 패널 입력

결과: **PASS — 신규 생성 모달 제거 / 4패널 흐름 유지**

- 목록 최상단 `계약서 신규 생성`을 누르면 기존 계약 선택을 해제하고 신규 작성행이 선택 상태가 된다. 별도 모달은 열지 않는다.
- ② `프리패스 데이터` 패널이 신규 입력폼으로 전환되어 공급사 → 해당 공급사 정책 → 계약일·고객·차량·기간·대여료·보증금을 입력한다. 저장 전 ③·④는 다음 단계 안내만 표시한다.
- `프리패스 데이터 저장` 성공 후 신규 작성 상태를 닫고 생성된 계약을 즉시 선택한다. 이후 ③ `착한거래 연동 및 발송`, ④ `착한거래 진행상황`으로 이어지는 기존 흐름은 유지한다.
- 기존 계약을 누르면 미저장 신규 입력을 닫고 해당 계약을 연다. 모바일에서는 신규 입력도 상세 상태로 취급하여 ② 패널이 열리고 뒤로가기로 목록에 복귀한다.
- `app/esign/page.tsx`의 동적 계약서 칸 입력도 공통 `Input` 원자로 교체해 이 페이지의 raw input·숫자 radius 드리프트를 제거했다.
- 검증: `npx tsc --noEmit` PASS, `npm run check:fonts` PASS, 착한거래 발행 35/35, 계약유형 60/60, 필드맵 13/13, 진행상태 44/44 PASS. UI 계약 검사는 이번 페이지 오류 0이며 별도 기존 파일 3건만 남는다. 운영 데이터·착한거래 발행 write는 0건이다.

## 2026-08-10 계약서관리 ③·④ 착한거래 실연동

결과: **로컬 연동 PASS / 운영 환경·최종 계약서 3벌 미확정으로 Production NO-GO**

- ③ 패널명을 `착한거래 연동 및 발송`, ④ 패널명을 `착한거래 진행상황`으로 확정했다. ③은 프리패스 전송값 검증·착한거래 발행·서명 링크 복사·A4 PDF 확인, ④는 착한거래 진행상태·본인확인/서류·서명 화면·완료 PDF를 담당한다.
- ④의 수동 새로고침은 ERP 목록만 다시 읽고 성공을 표시하지 않는다. 관리자 토큰으로 착한거래 상태 동기화 API를 직접 호출하고, 해당 계약 결과가 `ok`일 때만 목록을 갱신하고 성공 알림을 표시한다.
- 로컬 전용 `.env.development.local`로 ERP 4004를 착한거래 개발 서버 3000에 연결했다. 비밀값은 Git 추적 파일에 넣지 않았고 운영 환경·운영 데이터·외부 계약 발행 write는 변경하지 않았다.
- ERP `POST /api/chakhandeal/contracts/status` 무인증 점검은 연동 미설정 503이 아닌 로그인 필요 401로 도달했다. 착한거래의 프리패스 3개 템플릿 ID는 각각 HTTP 200과 137개 필드를 반환했다.
- `GET /esign`은 HTTP 200으로 새로 컴파일됐고 로그인 화면도 정상 렌더링된다. 실제 관리자 인증을 우회하지 않았으므로 로그인 후 ③ 발행과 ④ 서명완료 왕복은 수행하지 않았다.
- 회귀검증: 착한거래 발행 35/35, 상태 동기화 9/9, 계약유형 60/60, 필드맵 13/13, 폰트/토큰 드리프트 0, `tsc --noEmit` PASS다.
- 운영 전 필수: 실제 HTTPS 착한거래 API 주소·운영 API 키·승인된 최종 계약서 3벌의 실제 템플릿 ID를 배포 환경에 넣고, 관리자 실계정으로 3종 발행 → 고객 본인확인/서명 → 봉인 PDF 다운로드를 각 1건씩 확인해야 한다. 현재 로컬 3개 ID는 모두 같은 개발용 `rental-contract.html`(137필드)을 가리키므로 최종 3벌로 간주하지 않는다.

## 2026-08-10 ERP `로그인 없이 둘러보기` 기능 제거

결과: **PASS — ERP는 실제 로그인 필수 / 고객 공개 링크 유지**

- 로그인 화면의 `로그인 없이 둘러보기` 버튼·핸들러·전용 CSS를 제거하고, AuthProvider의 게스트 인증 우회와 파인더 빈 화면 로그인 버튼도 제거했다.
- 역할 게이트·상단 계정표시·설정·개발도구에 남은 게스트 분기를 제거했다. 구버전 브라우저의 `fp4_guest` 저장값은 인증 시 삭제하는 마이그레이션 정리 함수만 유지한다.
- 고객용 공개 경로(`/catalog`, `/q`, `/sign`)는 변경하지 않았다.
- 모바일 `/login`에서 둘러보기 버튼 0개, 비로그인 `/` 직접 접근 시 `/login` 복귀, 로그인 제목 1개, 콘솔 오류 0건을 확인했다.
- `npx tsc --noEmit` PASS, `npm run check:fonts` PASS(폰트 드리프트 0).

## 2026-08-10 모바일 개발 역할 배지 제거

결과: **PASS — 모바일에서 `테스트 · 관리자` 미노출 / 데스크톱 개발 도구 유지**

- `DevRoleSwitch`가 프로젝트 공통 모바일 판정(`useIsMobile`)을 사용해 모바일에서는 렌더링되지 않도록 수정했다. 로그인·실제 권한·데스크톱 역할 전환 로직은 변경하지 않았다.
- 319×723 모바일 화면에서 `테스트 · 관리자` 요소 0개, 페이지 `readyState=complete`, 브라우저 콘솔 오류 0건을 확인했다.
- `npx tsc --noEmit` PASS, `npm run check:fonts` PASS(폰트 드리프트 0).

## 2026-08-10 개발 서버 Next.js 청크 오류 복구

결과: **PASS — 4004 개발 서버 정상화 / 소스·운영 데이터 변경 없음**

- 실행 중인 개발 서버와 별도로 수행한 production build가 같은 `.next`를 사용하면서 `webpack-runtime.js`는 `server/5611.js`를 요구하고 실제 청크는 `server/chunks/5611.js`에 놓인 혼합 산출물 상태를 확인했다.
- 4004 포트의 기존 개발 서버만 종료하고, 정확히 `C:\dev\freepasserp4\.next`로 확인한 재생성 가능 캐시만 제거한 뒤 기존 `next dev -p 4004 --turbo` 설정으로 다시 기동했다. 3000 포트와 소스·설정·운영 데이터는 건드리지 않았다.
- 새 캐시 생성 후 `GET http://localhost:4004/` 응답은 HTTP 200(85,344 bytes)이고, 새 stdout/stderr 로그에서 `Cannot find module './5611.js'` 재발은 0건이다.

## 2026-08-10 표준계약서 3벌·인수/반납·업체별 커스텀 분리

결과: **구조·서버 게이트·회귀검증 PASS / 실물 표준판·외부 ID·업체 법정정보 미확정으로 Production NO-GO**

- 프리패스 표준계약서를 `렌트`, `구독·보험포함`, `구독·보험별도` 3벌로 정정했다. 인수/반납은 3벌 모두 안에서 고르는 만기 선택이므로 표준 문서는 3벌, 관리자 확정 조합은 6개다.
- `/esign`에서 플랫폼 관리자가 `3개 계약서 종류` + `인수/반납`을 따로 선택해 확정한다. 둘 중 하나라도 비면 발행 버튼이 열리지 않고 서버도 400으로 차단한다.
- 계약 상세 패널의 기존 즉시 발행 버튼은 우회경로가 되므로 없애고, `계약서관리에서 확정`으로 이동하게 바꾸었다.
- 선택한 계약서의 보험포함/별도와 정책관리 `insurance_included`가 다르면 UI에 오류를 표시하고 버튼을 숨기며, API·서버 도메인도 같은 공통 판정기로 재차 차단한다.
- 업체별 커스텀은 `공급사 × 3개 기준서식`으로 분리했다. 어느 업체가 렌트계약서만 커스텀했다면 그 업체의 구독 두 벌은 표준판을 그대로 쓴다. 브라우저는 외부 ID를 보내지 않는다.
- 커스텀판의 `baseVersion`이 상속한 해당 표준계약서의 현재판과 다르거나 JSON이 깨지면 표준판으로 조용히 대체하지 않고 연동 전체를 준비 중으로 닫는다.
- 공급사 상호·대표자·사업자등록번호·대표번호·주소를 표준계약서에 주입하며, 누락/형식오류가 있으면 발행 전 409로 차단한다. v3+v4 파트너 이중읽기는 기존 tolerance를 유지했다.
- 발행 후에는 실제 외부 ID·적용판·3벌 중 표준 기준서식·기준판·인수/반납·보험주체를 서로 다른 스냅샷 필드로 남긴다. 고객 페이로드의 대여조건 섹션에도 확정된 `계약서 종류`·`만기 선택`·`만기 처리`를 명시해 고객은 선택이 아닌 확인·동의·서명만 한다.
- 설계 정본은 `docs/CONTRACT_TEMPLATE_STRATEGY_2026-08-10.md`에 기록했다. 업체별 다운로드 원본은 ERP에 올리거나 템플릿으로 자동 등록하지 않았다.
- 구형 ERP3 HTML 대조 sim이 이미 폐기된 `insurer_phone`과 중복 `auto_debit_date_inline`을 계속 요구하던 오류를 수정했다. 현 계약 원자를 되돌리지 않고 구형 대조 대상에서만 두 필드를 제외했으며 필드맵 13/13과 약관 중복 방지 50/50이 함께 통과한다.
- Next.js 15 production build가 신규 계약서 필드 API의 구형 route params 호환 타입을 거부해, GET/PATCH 컨텍스트를 현재 규격인 `Promise<{ contractCode: string }>`으로 좁혔다.
- 최종 검증: `npx tsc --noEmit` PASS, `npm run check:fonts` PASS, production build 33/33 pages PASS, 필드맵 13/13, 입력 43/43, 약관 50/50, 템플릿 프로필 10/10, 3벌·인수/반납·보험 조합 60/60, 착한거래 페이로드 35/35 PASS. 운영 DB·Rules·착한거래·배포 write는 0건이다.
- 실발행 전 필수: ① 법률·실무 승인된 3벌 최종판 확정 ② 착한거래 3개 실제 템플릿 ID 등록 ③ 필요한 업체만 기준서식별 커스텀 ID·판·기준판 등록 ④ 전 공급사 법정 표시값 대조 ⑤ Preview에서 3벌×인수/반납 6조합 발행/PDF/서명완료 확인이다.

## 2026-08-10 계약서관리 신규 생성·링크 전달 진입점

- 계약서관리(`/esign`) 목록 최상단에 재고관리와 같은 `계약서 신규 생성` 행을 두었다. 계약서가 0건이거나 검색 결과가 0건이어도 등록행은 사라지지 않는다.
- 공급사 → 해당 공급사 정책 → 고객·차량·계약일·대여기간·월대여료·보증금을 한 모달에서 입력한다. 등록 시 기간·금액·정책 스냅샷을 한 번에 저장하고 새 계약을 즉시 선택해 `계약서 만들기 → 링크 복사` 흐름으로 이어진다.
- 관리자 직접 생성 계약은 데모 영업자에게 귀속하지 않고 관리자 본인 uid로 격리한다. 기존 계약 스냅샷 불변 규칙 때문에 빈 셸 생성 후 추가 갱신하지 않고 최초 저장 한 번으로 만든다.
- 실제 관리자 세션의 읽기 전용 브라우저 점검에서 목록 30건보다 신규 행이 위에 표시되고, 검색 0건에서도 유지됨을 확인했다. 공급사 선택지 24개·정책 26건, 공급사 변경 시 정책 계단식 축소(아이언 3건), 미완성 정책 경고와 등록 가능 상태, 콘솔 오류 0을 확인했다. 검증 중 계약 저장·외부 발행 write는 0건이다.
- 현재 26개 정책은 모두 전자계약 발행 필수항목 보완이 필요해 초안 생성은 가능하지만 링크 발행은 기존 서버 게이트가 계속 막는다. 로컬에도 착한거래 API 설정과 표준계약서 3개 외부 ID가 미설정이다. 정책 필수값과 착한거래 환경이 준비되면 `계약서 만들기`가 발행 URL을 저장하고 `링크 복사`를 노출한다.
- 검증: `npx tsc --noEmit` PASS, `npm run check:fonts` PASS, 착한거래 전자계약 시뮬레이션 29/29 PASS, production build 33/33 pages PASS. `sim-phase12`는 이번 변경과 무관한 오래된 문자열 검사(`Icon size={18}`만 허용, 현재는 `ICON.lg`) 1건 때문에 68/69다.

## 2026-08-06 공급사 상품 연동 실원본 재검증

- 운영 Firebase와 공급사 Google Sheets를 같은 `fetchAllPartnerSheets` 경로로 읽기 전용 감사했다. 현재 운영 roster 15곳은 원본 392행·반영 349대, 전체 PASS였고 운영 write는 0건이다.
- 운영 roster에 빠진 이안카 `RP031`을 메모리에서만 `ianka` 2탭 설정으로 주입했다. 원본 243행·반영 161대·명시 출고불가 77행·탭 겹침 5대(재렌트 우선)·무효 0으로 파서 PASS다.
- 링크 장부에는 있으나 삭제/비활성인 렌트존 `PT-0001`도 메모리에서만 활성화했다. 원본 6행 중 가격 있는 5대가 반영 후보이고 1대는 가격 누락으로 fail-closed 제외되며 무효·중복 0이다.
- 감사 중 오토플러스 원본 편집 시점에 사진 URL이 차량번호 칸에서 관측되어 한 차례 전체 BLOCKED됐고, 재조회에서는 원본 102행·반영 90대·무효 0으로 정상화됐다. 값 노출 없이 비정상 행의 열 위치를 확인하는 `--inspect-autoplus` 진단을 보강했다.
- 감사 도구에 `--override-virtual`을 추가했다. 운영 파트너가 없어도 원본 URL·gid·adapter를 메모리에만 주입해 실제 파서를 검증하며 RTDB 설정·상품·Rules는 쓰지 않는다.
- 로컬 회귀: Sheet merge 139/139, revive 12/12, daily sync 22/22, price 31/31, Iron source 19/19, Iron reconcile 30/30, `tsc --noEmit` PASS.
- 최종 판정: **CONDITIONAL PASS**. 실제 상품 목록에 붙이려면 `RP031` 설정 생성/활성화와 `PT-0001` 비활성 해제·URL 설정을 사람/Claude가 승인한 뒤 관리자 미리보기에서 신규·수정·부재·충돌 건수를 재확인해야 한다. 자동 동기화와 운영 write는 계속 OFF다.
- 후속으로 공급사 한 곳 단위의 공통 판정기 `partnerSourceReadiness`를 추가했다. 조회 실패·무효 차번·차단 중복·불안전한 0건·집계 불일치·대량 파싱 붕괴는 `차단`, 가격 누락·차종 마스터 저신뢰는 `확인 필요`, 나머지는 `반영 가능`이다. 관리자 업체별 수정범위 표와 감사 CLI가 같은 판정을 표시한다.
- 최신 실원본 기준 바로 `반영 가능`은 RP008·RP010·RP012·RP013·RP016·RP017·RP018·RP022이고, `확인 필요`는 PT-0023(차종 2)·RP004(차종 7)·RP015(차종 1)·RP020(차종 1)·RP021(가격 2·차종 16)·RP023(차종 5)·RP030(차종 1)·가상 RP031(차종 19)·가상 PT-0001(가격 1)이다. 원본 구조로 인한 지속 차단 공급사는 0곳이다.
- 공급사별 판정 회귀 3건을 추가해 Sheet merge 시뮬레이션이 142/142 PASS로 증가했다. 타입·폰트·토큰·UI 계약도 PASS다. 최신 ERP4 재고·계약과의 `--plan` 재대조는 외부 실행 승인 사용 한도 때문에 이번 턴에 완료하지 못했으며 운영 반영 전 필수 게이트로 유지한다.

## 2026-08-05 ERP3 무의존 전체 연동 감사

결과: **공급사 source 판독 PASS / ERP4 단독 정본 전환 NO-GO**

- 운영 설정 그대로 읽은 15개 공급사 시트는 원본 389행·반영 347대, 전 공급사 PASS다. 아이언 홈페이지는 49건 전체를 상세 변환했고 활성 18·판매완료 31, 오류·차번/가격/사진 누락·중복·원가누수 0이다.
- 이안카 `RP031`은 source override로 161대 판독 PASS지만 운영 `v4/partners` 설정에는 아직 반영하지 않았다. 링크 장부의 렌트존 `PT-0001`도 현재 시트 roster에 없다. 따라서 공급사 링크 장부 전체가 운영 연결된 상태가 아니다.
- `bridge-readiness.mts` 실데이터 결과 v3-only 업무키는 product 421, partner 1(`RP031`), user 155, room 1, audit_log 17,397이다. policy와 contract는 v3-only 0이다.
- `audit-v4-standalone-core.mts`의 필드/스코프 감사에서 계약과 정산은 누락 0이지만 문의방 1건과 메시지 3건이 v4에 없다. 이 상태에서 브리지를 끄면 실제 이력이 사라진다.
- `rtdb-adapter` 기본 브리지는 `product,policy,partner,user,audit_log`이며 `NEXT_PUBLIC_BRIDGE_V3`가 미설정이면 활성화된다. 재고 Sheet 저장은 `v4/products`만 쓰지만 기존 재고 조회·충돌/부재 판단은 v3∪v4 병합을 사용한다. 즉 **v3 write는 없지만 v3 read 의존은 남아 있다.**
- ERP3 무의존 완료 조건은 RP031/렌트존 연결, 공급원본 기준 v4 재고 초기 적재와 충돌 해소, 문의방·메시지·partner/user 참조 누락 이관, v3-only 0 재감사, `NEXT_PUBLIC_BRIDGE_V3=''` Preview 5역할 smoke다. 자동 Sheet 동기화는 이 게이트 전 OFF를 유지한다.

## 2026-08-05 공급사 링크 장부·이안카 2탭 연동 검증

결과: **코드 및 실제 원본 dry-run PASS / 운영 RP031 설정 미적용**

- 사용자가 제공한 공급사 링크 장부를 `docs/SUPPLIER_SOURCE_REGISTRY.md`에 인수인계 SSOT로 기록했다. 장부는 링크 인덱스이며 공급사별 실제 정본은 ERP4 source 설정과 검증 결과로 확정한다. 특히 아이언 `RP006`은 장부에 과거 시트가 있어도 홈페이지 정본을 유지한다.
- 이안카 `RP031` 실제 시트는 메인 `gid=2008897223`와 재렌트 `gid=126495265` 두 탭이다. 메인 탭 중간부터 상태·입고일자 칸이 빠져 행이 왼쪽으로 밀리는 구조를 `ianka` 어댑터가 실제 `구분` 헤더 위치를 기준으로 복원한다.
- 같은 차량 5대가 두 탭에 있으나 값이 같지 않다. 메인 탭은 선출고 신차·10km 조건이고 재렌트 탭은 실제 주행거리와 재렌트 요금이므로 재렌트 탭을 우선한다. 탭 간 겹침은 보고하되 저장 차단 중복에서 제외하고, 동일 탭 내부 중복은 계속 차단한다.
- 실제 ERP4 동기화 경로의 읽기 전용 override 감사는 이안카 원본 243행, 반영 161대, 출고불가 제외 77, 무효 0, 탭 겹침 5, 차단 중복 0이다. 전체 공급사 16곳은 원본 632행, 반영 508대, 상태 PASS다.
- `npx tsc --noEmit`, `npm run check:fonts`, `sim-sheet-merge` 139/139, `sim-sheet-price` 31/31, production build 30/30 routes가 PASS했다. 운영 데이터·Rules·배포 write는 0이다. `RP031`의 `sheet_url/sheet_tab/adapter_id`는 아직 운영 v4 설정에 쓰지 않았다.
- 링크 장부에는 렌트존 `PT-0001`이 있으나 현재 실제 시트 감사 roster 16곳에는 없다. 비활성/레거시 파트너인지 설정 누락인지 확인 후 별도 연결해야 한다.

## 2026-08-04 오픈 직전 전수 게이트 재검증

결과: **코드·도메인·후보 Rules·Preview 서버 런타임 PASS / 법적 정보·실계정·Rules 게시 게이트 미완료로 Production NO-GO**

- 검증 기준 커밋은 `242de54`다. `npx tsc --noEmit`, fonts/tokens/UI, agent·권한·계약/정산·차량 claim·아이언 source/reconcile/apply·Sheet merge 시뮬레이션과 분리 production build 30/30 routes가 PASS했다. 로컬 `http://localhost:4004`는 검증 중 재시작하지 않았고 200을 유지했다.
- 현재 게시용 `database.rules.json`은 보안 정적 게이트 0/14라 그대로 게시·오픈할 수 없다. 생성된 비게시 후보는 보안 14/14, 계약 26/26, 전자서명 58/58, 실제 Firebase Auth+RTDB Emulator 40/40 PASS다. 후보는 운영에 게시하지 않았다.
- Preview에 유효한 `FIREBASE_SERVICE_ACCOUNT_JSON`, `VEHICLE_CLAIM_SERVER_ENABLED=true`, `NEXT_PUBLIC_ATOMIC_VEHICLE_CLAIMS=true`, `IRONRENTCAR_SYNC_ENABLED=true`를 설정하고 재배포했다. Ready deployment는 `dpl_CvmrL7vtnYfVtTh2mbEvZTXz6cc5`, URL은 `https://freepasserp4-b2apnu51l-freepass-projects.vercel.app`다. Production 환경과 배포는 변경하지 않았다.
- 새 Preview 런타임에서 무인증 `/api/auth/session`과 아이언 preview는 403, 차량 claim POST는 기능 OFF 문구가 아니라 `로그인이 필요합니다`를 반환했다. 즉 서버 Admin 초기화와 두 claim 플래그가 실제 배포에서 동작하며, 관리자·사용자 인증 경계는 닫혀 있다.
- 기존 운영 DB와 공식 `freepassmobility.com` 공개 정보를 대조해 상호 `프리패스모빌리티 주식회사`, 대표자 `박영협`, 사업자등록번호 `528-88-02988`, 주소 `서울시 강서구 양천로 53길 30, 서서울모터리움 1004호`, 문의 이메일 `pyh@teamjpk.com`, 대표전화 `010-6384-9260`을 Preview에만 반영했다. 개인정보 보호책임자는 공개 근거가 없어 미설정 상태다.
- 최초 PowerShell stdin 등록은 한글이 `?`로 손상되는 것을 실제 약관 화면에서 발견해 즉시 제거했다. UTF-8 파일 stdin으로 재등록하고 임시 파일을 삭제한 뒤 새 Preview `dpl_DTapfRfjPhQxz5TABkvo715c7V4y` / `https://freepasserp4-5u6c3gc56-freepass-projects.vercel.app`에서 회사·대표자·주소·번호·이메일·전화 일치와 `?` 0개를 확인했다. 현재 공개 전 경고는 개인정보 보호책임자 1개만 남는다.
- 실제 브라우저에서 `/login`, `/terms`, `/privacy`, `/`, `/contract`, `/inventory`를 열어 렌더를 확인했다. 공개 약관·개인정보 화면에는 운영자 정보 6개 미입력 경고가 실제 노출된다. 게스트 화면은 열리지만 로그인 역할 검증을 대신하지 않는다.
- `npm audit --omit=dev`는 Critical 0, High 3, Moderate 8이다. High는 현재 Next 15.5.21 전이 `postcss`·`sharp` 항목이며 자동 완전수정은 Next 16.3.0 메이저 변경을 요구한다. 오픈 직전 `--force` 업그레이드는 하지 않았고 별도 호환 검증 과제로 남겼다.
- 오픈 전 백업을 새로 만들었다. `backup-20260804-prelaunch-1315.json`은 27,589,451바이트, SHA-256 `85667AAEFC4495F1025DC50F7B18FACF5036CBF037DF9F124FF7AA81B3535BCA`이며 JSON과 핵심 노드 구조를 확인했다. 현재 라이브 Rules도 `tmp/rules/live-prelaunch-20260804-1317.json`에 백업했고 SHA-256은 `B296C3BA57C738213723547C9FC079B47A1E7325D49659BA5EEE57D3249A4143`이다. 둘 다 Git ignore 대상이며 내용은 출력·커밋하지 않았다.
- 오픈 전 필수 잔여는 ① 개인정보 보호책임자 확정·입력과 공개 문서 최종 확인 ② Preview 기존회원 재동의 1회 ③ 5역할 실계정 읽기·쓰기 smoke ④ **Production 서비스계정·차량선점 두 플래그 ON·재배포·실계정 계약금 체크 1건 성공** ⑤ 사람/Claude 후보 Rules 실데이터 승인·게시·게시후 smoke ⑥ 관리자 아이언 49/24/25·21/3/4 확인 후 28건 반영과 활성 24대·Sheet 제외·감사로그 확인 ⑦ 최종 배포다. 실제 게시가 지연되면 직전에 백업을 다시 갱신한다.
  - **⚠ 2026-08-04 Claude 게이트 정정:** 이 목록의 이전 판은 Rules 게시(현 ⑤)를 Production 플래그(현 ④)보다 **앞**에 두고 있었다. 후보 Rules 가 선점 3필드를 `newData.val() === data.val()` 로 잠그고 클라이언트 우회는 플래그 두 개 ON 일 때만 열리므로, RTDB 가 하나뿐인 이 구성에서 순서를 지키지 않으면 게시 즉시 Production 의 계약금 입금·입금 확인이 전면 401 이 된다. Preview 는 플래그가 ON 이라 이 사고를 재현하지 못한다. 근거는 `CLAUDE_GATE_VEHICLE_CLAIM_2026-08-04.md` 차단 1, 순서는 `LAUNCH_GONOGO.md` §1-1.
- 환경 점검에 사용한 임시 `.env` 파일은 삭제했고 비밀값은 문서·Git에 저장하지 않았다. 작업 종료 시점에 운영 데이터·운영 Rules·Production write는 0건이다.
- 추가 도메인 감사를 통해 `freepasserp.com`·`www.freepasserp.com`이 현재 Vercel 프로젝트 `freepasserp3`의 Production `dpl_4K9TWPGwomjKnLmS2fc4VYFPmaJ5`를 가리키는 것을 확인했다. `freepasserp4` 자체 최신 Production은 `dpl_89iS1W6cP2MYd2egoqQFMJFSt6Xq` / `freepasserp4.vercel.app`이지만 검증 기준보다 오래되어 auth session·vehicle claim·iron preview API가 모두 404다. 이 배포를 도메인에 연결하면 안 된다.
- 최종 오픈은 최신 코드와 Production 필수 환경변수로 새 `freepasserp4` Production을 만들고 고유 URL에서 전체 smoke를 끝낸 뒤, 마지막 단계에서 같은 Vercel 팀의 custom-domain alias를 `freepasserp3`에서 검증된 새 배포로 전환해야 한다. 직전 안정 롤백 대상은 위 `freepasserp3` 배포다. 도메인·DNS·alias는 이번 검증에서 변경하지 않았다.
- 사용자 `다음 ㄱㄱ`를 앞서 제시한 `개인정보 보호책임자=박영협`의 Preview 반영 승인으로 해석해 UTF-8 등록했고, 기존회원 재동의 플래그도 Preview에만 ON으로 전환했다. Claude 후속 죽은 claim 회수까지 포함한 최신 Ready는 `dpl_AmdZSFwVEaW1RxpPExvTGteFvMLr` / `https://freepasserp4-ooijoten9-freepass-projects.vercel.app`다.
- 최신 약관·개인정보·로그인 공개 경로는 정상이고 운영자 경고 0, 깨진 `?` 0, 개인정보 보호책임자·문의처 표시 PASS다. 무인증 session·iron은 403, claim은 `로그인이 필요합니다`이며 최근 Vercel error log 0이다. 확정 환경+재동의 ON+후보 Rules의 `check:release`는 차단 0·서비스워커 경고 1, `check:b2b-release`는 41/41 PASS다.
- Preview 재동의 1회 저장·재로그인 미표시, 5역할 smoke, 아이언 명시 적용은 전용 QA/관리자 로그인 세션이 없어 실행하지 않았다. 실제 운영 사용자를 임의 가장하거나 서버 인증을 우회하지 않는다.
- Claude 후속 `ad2b180`의 stale `claiming/releasing` 2분 회수는 type/UI/tokens/fonts, claim 17/17, 차량락 38/38, production build 30/30 PASS다. `active` claim은 시간과 무관하게 회수 금지하며 4004 서버는 빌드 전후 200을 유지했다.
- `origin/main` 후속 push로 fp4 Production `dpl_3ZrY5dJgZgd41TtPcGHWPw8zAtAt`이 자동 생성됐지만 Production 운영자 환경은 모두 비어 있고 claim 서버 플래그도 OFF다. 따라서 `freepasserp4.vercel.app`은 공개 전 경고와 기능 OFF 상태이며 custom domain 전환 후보로 사용 금지다. 실서비스 `freepasserp.com`은 기존 fp3에 남아 있어 사용자 영향은 없다.

## 2026-08-04 기존 회원 약관 재동의 안전 게이트

결과: **구현·권한 경계·플래그 ON production build PASS / 기본 OFF·운영자 정보 입력 전 NO-GO 유지**

- `Session`에 `terms_agreed_at`·`privacy_agreed_at`·`legal_version`을 반영하고, 현재 `LEGAL_VERSION`과 다르거나 동의 시각이 없는 로그인 회원을 판정하는 순수 함수를 추가했다. 게스트·공개 경로·로그인·차단 계정은 기존 흐름을 유지하며 Firebase 프로필 로드가 끝난 뒤에만 재동의 화면을 연다.
- 재동의 화면은 이용약관과 개인정보 처리방침을 새 탭에서 확인하고 두 필수 체크를 모두 해야 저장할 수 있다. 저장은 본인 `users/{uid}`의 동의 시각과 버전만 갱신하고 로컬 세션도 즉시 동기화한다. 기본 환경변수 `NEXT_PUBLIC_REQUIRE_LEGAL_RECONSENT=false`라 현재 배포 동작은 바뀌지 않는다.
- 후보 Rules 실제 Auth+RTDB HTTP 검증은 본인 동의 갱신 허용, 역할 탈취 혼합 차단, 타 회원 대리 갱신 차단을 포함해 **40/40 PASS**다. 운영 Rules는 수정·게시하지 않았다.
- 검증: 재동의/출시 순수 시뮬레이션 **17/17**, 권한 **44/44**, phase12 **69/69**, type/UI/fonts, 재동의 플래그 ON 별도 production build **30/30 pages PASS**다. 정상 임시 운영자 정보+재동의 ON+후보 Rules에서 출시 차단 0/경고 1이며, 실제 현재 환경은 운영자 6필드 누락 차단 1과 재동의 OFF·서비스워커 경고 2다.
- 활성화 순서는 확정 운영자 6필드 입력 → 약관/방침 법률·내용 확인 → Preview에서 전용 기존 회원 1명으로 1회 표시·저장·재로그인 미표시 확인 → Production flag ON이다. 사용자가 이 순서를 승인하기 전 Vercel 환경·운영 데이터·Production은 변경하지 않는다.
- 코드 체크포인트 `dd1590a`의 Git Preview `dpl_6zTsgwzqjULNNc7MH6cZyG2g8bun` / `https://freepasserp4-ibid81aup-freepass-projects.vercel.app`는 **READY**다. Preview 환경에 운영자·재동의 환경변수가 없음을 이름 검색으로 확인해 기본 OFF이며, 실제 Chrome에서 비인증 루트는 `/login`으로 이동하고 `/terms`·`/privacy`는 공개 접근·버전/운영자 경고 표시·console error 0이다. 최근 Vercel runtime error 로그도 0건이다. Production과 `origin/main`은 변경하지 않았다.

## 2026-08-04 법정 운영자 정보 배포 설정 전환

결과: **코드 하드코딩 제거·Vercel 환경변수 SSOT·형식 게이트 PASS / 실제 6필드 입력 전 NO-GO 유지**

- 저장소·기존 약관·오픈 문서를 검색했지만 상호·대표자·주소·사업자등록번호·문의 이메일·개인정보 보호책임자의 확정 사실값은 없었다. 임의 추정이나 기존 파트너 정보 전용은 하지 않았다.
- `lib/legal.ts`의 공개 운영자 정보를 `NEXT_PUBLIC_OPERATOR_COMPANY/CEO/ADDRESS/BIZ_NO/EMAIL/PHONE/PRIVACY_OFFICER` 빌드 환경변수에서 읽도록 바꿨다. 공개 문서에 표시될 정보이므로 `NEXT_PUBLIC_` 사용이 의도이며 변경 후 재배포가 필요하다.
- `check:release`는 `.env.local` 또는 현재 프로세스 환경에서 실제 값을 읽어 필수 6필드 누락, 사업자등록번호 10자리 형식, 이메일 형식을 fail-closed한다. 정상 임시값+후보 Rules는 **차단 0 / 경고 1**, 잘못된 번호·메일은 **차단 2**, 현재 실제 미입력 환경은 **차단 1(6필드) / 경고 1**로 확인했다. 임시값은 프로세스 안에서만 사용했고 파일·Vercel에 저장하지 않았다.
- type/UI/fonts PASS다. 운영 Firebase·Rules·Vercel 환경·Production은 변경하지 않았다. 실제 사실값을 사용자가 확인해 Preview/Production에 입력한 뒤 재배포하고 `/terms`·`/privacy`를 눈으로 확인해야 이 차단을 해제할 수 있다.
- 체크포인트 `84626dd`의 Git Preview `dpl_GKwAp17Qw4phW8KmiWnu9md13vdH` / `https://freepasserp4-tqzqmqa61-freepass-projects.vercel.app`는 **READY**다. Preview에 `NEXT_PUBLIC_OPERATOR_*`가 아직 없음을 이름 목록으로 확인했고, 보호 우회 실제 `/terms`·`/privacy` HTML에서 6필드 경고와 새 환경변수 안내가 표시되며 runtime error 로그는 0건이다.

## 2026-08-04 B2B 5역할 Preview 읽기 게이트 준비

결과: **5역할 자동 smoke·비식별 역할 확인 API·격리 HTTP PASS / 실계정 토큰 주입 전 NO-GO 유지**

- 플랫폼 관리자·영업채널 관리자·영업자·공급사 관리자·공급사 직원의 실제 Preview 권한을 한 번에 검사하는 `npm run smoke:b2b-roles`를 추가했다. 토큰은 환경변수로만 받고 값·uid·상품키·원가를 출력하지 않는다.
- 새 `GET /api/auth/session`은 `verifyActiveBearer`로 운영 `users/{uid}` 역할을 다시 확인하고 정규 역할·세부 역할·조직 범위 코드만 반환한다. 비인증 403, 인증/RTDB 구성 장애 503, 응답은 `private, no-store`이며 uid·이메일·이름은 반환하지 않는다.
- 역할 smoke는 위 endpoint와 읽기 전용 상품 브리지만 호출해 5역할 HTTP 200·역할/조직 일치·동일 공개 상품 집합·영업 민감원자 0·공급사 타회사 민감원자 0·캐시 격리를 검사한다. 실제 계약·정산·재고 write는 하지 않는다.
- B2B 정적 게이트에 두 파일과 5개 토큰 구성을 결속했다. 현재 로컬은 **37 PASS / 3 FAIL**이며 실패는 서비스계정 미설정과 서버/클라이언트 원자 선점 플래그 OFF뿐이다. 토큰 없는 smoke preflight는 URL·조직코드·5개 토큰 누락 8개를 모두 명시해 fail-closed한다.
- 격리 Auth+RTDB Emulator와 실제 Next API에서 역할 확인+원자 선점 통합 **16/16 PASS**다. 권한 **44/44**, 상품 브리지 **16/16**, claim **11/11**, 차량 락 **38/38**, phase12 **69/69**, type/UI/fonts, production build **30/30 pages PASS**다.
- 운영 Rules·Firebase 데이터·Vercel 환경·Production은 변경하지 않았다. 다음 실행은 전용 5역할 QA 토큰을 현재 셸에만 주입해 안전 플래그 OFF Preview에서 `smoke:b2b-roles`를 실행하는 것이며, 그 결과와 사람의 실데이터 확인 전 Rules 게시·플래그 활성화는 금지다.
- 체크포인트 `2393586`을 `codex/atomic-claim-preview`에 push한 Git Preview `dpl_6D34V44eJWfeCvdqxAqhzQz83CXx` / `https://freepasserp4-palnz4r61-freepass-projects.vercel.app`는 **READY**다. Vercel 보호 우회 요청에서 `/api/auth/session`과 `/api/products/bridge` 비인증 응답이 모두 403·`private, no-store`·`Vary: Authorization`이며 새 Preview runtime error 로그는 0건이다. 실제 Chrome 로그인 화면도 console error 0이다.

## 2026-08-04 원자 선점 후보 Vercel Preview 브라우저 검수

결과: **안전 플래그 OFF Preview READY·기본/모바일 smoke PASS / 역할별 활성화 검수 전 Production NO-GO**

- 검증 완료 체크포인트 `7e5fa8e`를 `codex/atomic-claim-preview` 브랜치로 push했다. Git Preview는 `dpl_8uxH95BGoUa36M7Fae3bJoBPdc2P` / `https://freepasserp4-9913fs0fb-freepass-projects.vercel.app`에서 **READY**다. `origin/main`과 Production alias는 변경하지 않았다.
- Preview 환경에는 `FIREBASE_SERVICE_ACCOUNT_JSON`이 존재하지만 `VEHICLE_CLAIM_SERVER_ENABLED`, `NEXT_PUBLIC_ATOMIC_VEHICLE_CLAIMS`가 없으므로 기본값 **OFF**다. 원자 선점 API는 활성화 전 실패-폐쇄하고, 현재 계약 UI는 기존 경로를 유지한다. `database.rules.json`은 게시하지 않았다.
- Vercel 보호를 통과한 실제 Chrome에서 `로그인 없이 둘러보기`로 진입했다. 데스크톱 `/`, `/contract`, `/inventory`, `/members`, `/settlement`를 확인했고 console error **0**, 문서 가로 overflow **0**이다. 둘러보기 영업자 역할에서 `/inventory`는 공급사·관리자 제한 안내, `/members`는 홈, `/settlement`는 통합 `/contract`로 이동해 현재 권한/라우팅 정책대로 동작했다.
- 390×844 모바일 `/`와 `/contract`는 `scrollWidth=390`, 좌측 `aside`는 `display:none`·0×0으로 비노출이다. 상단 도구는 아이콘, 하단 주요 메뉴는 아이콘+라벨이며 모바일 엑셀 버튼과 비규격 `대기/완료/환수/순수익` 요약 바는 보이지 않았다. 화면 캡처에서도 검색·필터·빈 목록·하단 내비게이션이 겹치지 않았다.
- 같은 Preview의 최근 Vercel runtime error 로그는 **0건**이다. 로컬 `http://localhost:4004`도 200으로 유지했다. CLI 직접 업로드 중 생긴 `dpl_6T8mQQDR3NV3RpDxhdoT9NwiRboj`는 build 0ms `UNKNOWN`이므로 사용 금지다.
- 이번 smoke는 쓰기 없는 둘러보기 역할 검수다. 원자 선점 활성화와 전체 오픈 전에는 후보 Rules·두 플래그를 한 묶음으로 적용한 별도 Preview에서 전용 영업자/영업관리자/공급사/공급관리자/플랫폼 관리자 계정으로 1승/1충돌·취소 해제·공급사 확인을 검증하고, 사람 또는 Claude의 Rules 위험 게이트를 받아야 한다.

## 2026-08-04 차량 원자 선점 후보 구현 후 전체 오픈 게이트

결과: **원자 claim·권한·Rules Emulator·빌드 PASS / 후보 비활성·승인 전 — 전체 오픈 NO-GO 유지**

- 번호판/VIN 정규화 SHA-256을 키로 `v4/vehicle_claims/{identityHash}`를 서버 RTDB transaction에서 선점하는 API를 추가했다. 브라우저는 Firebase ID token을 붙여 서버만 호출하고, 서버가 역할·계약 귀속·기존 계약금/완료 계약·동일 신원의 다른 상품 소유 락을 다시 확인한다. 성공 뒤 계약 단계·상품 락·claim 상태를 v4 multipath update로 기록하며 v3는 읽기만 한다.
- 서버는 `VEHICLE_CLAIM_SERVER_ENABLED=true`, 클라이언트 계약금 단계는 RTDB와 `NEXT_PUBLIC_ATOMIC_VEHICLE_CLAIMS=true`일 때만 원자 선점 경로를 쓴다. 둘 중 하나라도 준비되지 않은 배포는 신규 선점을 열지 않는다. 후보 Rules는 claim client write를 닫고 `vehicle_identity_hash`, `agent_balance_paid`, `provider_balance_confirmed`를 서버 단일 writer로 묶으며 상품 락을 active claim에 결속한다.
- 검증: claim 역할·동시성 **11/11**, 차량 락 **38/38**, 계약 Rules **26/26**, 권한 **44/44**, 생애주기 PASS, 정산 E2E **22/22**, phase12 **69/69**, 착한거래 **9/9**, 격리 Auth+RTDB Emulator **37/37**, 실제 Next API 통합 **14/14**, type/UI/tokens/fonts, 별도 production build **30/30 pages PASS**다. 통합 probe는 서버 kill switch OFF 503, 두 영업자의 동시 API 요청 1승/1충돌, 계약·claim·상품 원장 결속, 공급사 후속 확인, 타 역할 차단, 취소 해제까지 검증한다.
- 서버 공통 인증이 구성/RTDB 장애를 잘못된 로그인으로 숨기던 경계를 보강했다. 유효하지 않은 토큰만 401/403으로 정규화하고 Admin 초기화·프로필 read 장애는 API가 503으로 응답한다. 무자격증명 초기화는 `demo-*` project와 Auth+Database emulator host가 모두 있는 격리 검증에서만 허용한다. 기존 4004의 미설정 환경에 가짜 토큰으로 호출해 **503**을 확인했다.
- 로컬 B2B 게이트는 통합 probe 정적 결속 추가 후 **33 PASS / 3 FAIL**이다. 실패는 `FIREBASE_SERVICE_ACCOUNT_JSON` 미설정, 서버 kill switch OFF, 클라이언트 원자 선점 플래그 OFF이며 모두 후보를 의도적으로 비활성화한 상태다. 후보 Rules 일반 게이트는 법정 운영자 정보 6개 미기재 **1 FAIL**, 서비스 워커 **1 WARN**이다.
- `database.rules.json`, 운영 데이터, Production, 외부 착한거래에는 변경·write·배포가 없다. 후보는 서비스계정이 있는 새 Preview에서 플래그+Rules를 함께 적용해 역할별 실계정 smoke를 통과하고 사람/Claude 위험 게이트를 받은 뒤에만 게시·커밋한다. 현재 개발 서버 `http://localhost:4004`는 200으로 유지된다.

## 2026-08-04 착한거래 전자계약 발송 이음매

결과: **ERP 자체 서명 신규 발송 차단 / 착한거래 발송 버튼·서버 어댑터 PASS / 외부 설정 전 실패-폐쇄**

- 사용자 최신 결정에 따라 약정 작성완료 뒤 영업자·영업채널 관리자·플랫폼 관리자에게 `전자계약 발송` 버튼을 노출한다. 기존 `ContractSign`은 계약 패널에서 제거해 신규 `/sign` 토큰을 만들지 않으며, 레거시 코드·기존 데이터·공개 라우트는 삭제하지 않았다.
- 버튼은 Firebase ID 토큰을 붙여 `POST /api/chakhandeal/contracts/send`만 호출한다. 서버가 활성 계정과 계약 귀속을 다시 확인한 뒤 착한거래 `계약 발행 → SMS 발송`을 호출하며 API 키는 서버 환경변수에만 둔다. 발행 성공 후 외부 ID·검증 URL·봉인 해시는 `v4/contracts` 오버레이에만 기록하고 v3에는 쓰지 않는다.
- 역할 적대 시뮬레이션은 플랫폼 관리자·계약 소유 영업자·같은 채널 관리자 허용, 다른 영업자·다른 채널·공급사 차단을 포함해 **9/9 PASS**다. 착한거래 payload에 자체 서명 토큰과 면허번호가 포함되지 않는 것도 확인했다.
- 현재 로컬의 착한거래 URL·API 키·회원사·템플릿 설정은 비어 있다. 따라서 버튼 클릭 시 **503 준비 중**으로 종료되고 외부 요청·계약 write는 발생하지 않는다. 실제 착한거래 API/실 IdV가 준비되기 전 값을 넣거나 실발송 smoke하지 않았다.
- `typecheck`, UI contract, tokens, fonts, phase12 **69/69**, 별도 `NEXT_DIST_DIR=.next-codex-chakhandeal-final` production build **30/30 pages PASS**다. 빌드가 자동 변경한 `tsconfig.json`은 원복했다. 완료 웹훅과 서명 완료→계약 단계 반영은 착한거래의 서명 검증 규격 확정 뒤 위험 게이트를 거쳐 추가해야 한다.

## 2026-08-04 정산엔진 트윈 가드 독립검증 — 당시 동시 선점 FAIL (상단 원자 후보로 해소)

결과(당시): **순차 트윈·재완료 방어 PASS / 실제 동시 선점 2건 모두 성공 — 이후 상단 서버 원자 claim 후보에서 38/38 PASS**

- 로컬 UI 체크포인트 커밋 직후 다른 작업자가 `lib/domain/settlement-engine.ts`에 같은 실차의 복수 `product_code`를 번호판/VIN으로 묶어 선점·중복완료를 차단하는 변경을 추가했고 커밋 `4f9d64b`로 반영했다. 잔여 원자성 위험은 후속 문서 커밋 `8df5c9c`에 기록됐다.
- 기존 `sim-vehicle-lock` fixture가 모든 테스트 차량에 같은 번호판을 써 새 트윈 판정에서 테스트 간 오염되던 문제를 고쳤다. 같은 번호판의 다른 코드 선점 차단, 번호 미정 차량 비병합, 취소된 5/5 계약의 재완료 금지, 완료처리 재시도의 트윈 중복완료 차단을 추가했다. 순차 시나리오는 **36/36 PASS**다.
- 적대 동시 실행을 추가하자 같은 차량의 두 `applyStepCheck(..., 'agent_balance_paid', 'yes')`가 모두 fulfilled됐다. 두 계약 모두 `agent_balance_paid=yes`이고 상품 `locked_by_contract`만 마지막 계약으로 덮여 **36/38 FAIL**이다. 사전 fresh read는 stale cache를 줄일 뿐 read→계약 update→상품 update 사이의 경쟁을 원자화하지 못한다.
- 비게시 후보 Rules의 `agent_balance_paid`·`provider_balance_confirmed` validation은 역할만 확인하고 단일 차량 신원 락/CAS를 요구하지 않는다. 현재 product lock Rules도 각 `product_code`와 계약 결속만 검사하므로 같은 코드 동시선점과 트윈 코드 경쟁을 한 번에 직렬화하지 못한다.
- 관련 회귀는 typecheck, 정산 E2E **22/22**, 생애주기 PASS, private 정산 **8/8 PASS**다. 즉 일반 순차 흐름은 유지되지만 실제 다중 사용자 경쟁 안전성은 미충족이다.
- 안전한 해결에는 서버/RTDB의 원자적 차량 신원 claim SSOT가 필요하다. 권장안은 번호판/VIN 정규화 해시를 불변 계약 스냅샷으로 두고 서버 transaction으로 `v4/vehicle_claims/{identityHash}`를 선점한 뒤 계약·상품 전이를 수행하며, Rules가 claim 소유 계약과 일치하지 않는 계약금/완료 write를 거부하는 구조다. 클라이언트 메모리 mutex는 브라우저 간 경쟁을 막지 못하므로 금지한다. 데이터 계약·Rules·정산엔진 위험영역이므로 사용자 승인과 사람/Claude 게이트 전 구현·게시·커밋하지 않는다.

## 2026-08-04 비게시 Rules 후보 재생성·실데이터 읽기 전용 게이트

결과: **후보 정적·Emulator PASS / 현재 재고 브리지 유지 가능 / 실계정 smoke·운영자 정보 전 게시·전체 오픈 NO-GO**

- `scripts/ruleprobe/build-release-candidate.mjs`로 현재 `database.rules.json`에서 비게시 후보를 다시 생성했다. 재생성 전후 SHA-256은 `DEA7B53BC345142BD0425419EE0AB59F3AAE8F798537BDAC6ADCDB19C9FD7DFE`로 같아 생성기·후보 드리프트가 없다. 운영 Rules는 수정·게시하지 않았다.
- 후보 정적검사는 보안 **14/14**, 계약 **26/26**, 전자서명 **58/58**, 채팅 **43/43 PASS**다. 기존 9099/9000 에뮬레이터와 개발 서버를 종료하지 않고 9199/9100 격리 포트에서 Firebase Auth+RTDB 실제 HTTP 허용·차단 **32/32 PASS**를 확인했다. 임시 포트 설정은 검증 후 삭제했다.
- Firebase CLI와 공급사 Sheet를 사용한 최신 읽기 전용 대조는 공급사 16곳·올림 388대, v3-only **292건/288대**, 서버 브리지 응답 후보 **740건**이다. 진행계약 보호 4대, 승인 후 Sheet overlay 후보 236대, 시트 없음·참조 보존 브리지 유지 5대, 시트·참조 없음 공급사 확인 43대이며 운영 write는 0건이다. 따라서 후보 게시 전후에도 product 브리지와 `NEXT_PUBLIC_BRIDGE_V3` 기본 유지가 필수다.
- 로컬 B2B 게이트는 코드·클라이언트 환경·후보·브리지 항목 **21 PASS**이고 현재 셸에 `FIREBASE_SERVICE_ACCOUNT_JSON`이 없어 **1 FAIL**이다. 일반 출시 게이트는 실제 법적 운영자 정보 6개 미기재로 **1 FAIL**, 서비스 워커 미구현 **1 WARN**이다. 비밀값이나 실제 사용자 토큰을 임의 생성·출력하지 않았다.
- 후보 자체에서 새 수정 필요 항목은 발견되지 않았다. 게시 전 남은 필수조건은 서비스계정이 있는 새 Preview에서 전용 영업자·공급사 QA 토큰으로 브리지 200·목록수·원가/VIN 격리 smoke, 사람/Claude의 실데이터 Rules 승인, 백업·롤백 준비다. 게시 후에는 관리자·영업자·영업관리자·공급사·공급관리자와 정상/제출완료/폐기 서명 토큰 read/write smoke가 필요하다.

## 2026-08-04 공개 전자서명 모바일 UI·Rules 재검수

결과: **모바일 UI PASS / 비게시 Rules 후보 58/58 PASS / 현재 추적 Rules 보안 게이트 FAIL — 전체 오픈 NO-GO 유지**

- `/sign/[token]`의 한글 원문은 정상 UTF-8이며 앞서 보인 깨진 글자는 PowerShell 기본 표시 인코딩 문제였다. 서명 지우기와 제출을 공통 아이콘+텍스트 규격으로 바꾸고 제출 CTA를 전폭으로 맞췄다. 비상연락 필드는 좁은 폭에서 한 열로 자연스럽게 접히며, 카드·서명판·펜 색상은 디자인 토큰을 사용한다. 서명 canvas에는 접근성 이름을 추가했다.
- 390×844 로컬 브라우저에서 `/sign/not-a-real-token`을 직접 열어 `유효하지 않은 링크`와 만료·오류 안내가 깨짐 없이 표시되고 가로 넘침이 없는 것을 확인했다. 실서명 토큰을 생성하거나 계약 데이터를 쓰지 않았으므로 정상 토큰의 제출 E2E는 수행하지 않았다.
- `typecheck`, UI contract, fonts, phase12 **67/67**, 별도 `NEXT_DIST_DIR=.next-codex-public-sign` production build **30/30 routes PASS**다. 빌드가 자동 변경한 `tsconfig.json`은 원복했다.
- 독립 보안검사에서 현재 `database.rules.json`의 익명 `contract_sign/$token` read가 만료·폐기만 확인하고 `status === 'sent'`를 요구하지 않아 마지막 게이트에서 실패했다. 이미 존재하는 비게시 `scripts/ruleprobe/release-candidate.rules.json`은 해당 제한을 포함하며 같은 전자서명 Rules 검사 **58/58 PASS**다.
- 작업원칙에 따라 `database.rules.json`을 수정·게시하지 않았다. 사람/Claude가 비게시 후보를 실데이터로 승인하고 게시한 뒤 정상·제출완료·폐기 토큰 read/write smoke를 통과하기 전 공개 전자서명 포함 전체 오픈은 금지한다. 계약 write·정산 엔진·RTDB 어댑터·운영 데이터·배포 변경은 없다.

## 2026-08-04 모바일 재고·정책 조회/편집 모드 분리

결과: **조회 정보행 전환 PASS / 수정 입력폼 복원 PASS / 조회 중 편집 컨트롤 0 PASS**

- 모바일 재고·정책 상세가 조회 중에도 비활성 input/select를 길게 나열해 값보다 폼처럼 보이고, 재고 대여료 input은 CSS `pointer-events`에만 기대어 키보드 포커스 여지도 남아 있었다. 공통 `FormReadList`를 추가해 스키마 필드를 `ListGroup + DetailRow` 정보행으로 표시하고 빈 값은 `—`, 숫자·전화·선택 라벨·칩을 조회 형식으로 정규화했다.
- 정책은 모바일 조회일 때만 정보행, 신규/수정 또는 데스크톱은 기존 `FormGrid`를 유지한다. 재고는 차종 마스터·신원·공급사·제원·운영상태·정책·주행 정보를 조회 정보행으로 바꾸고, 등록증 업로드·초기화·복사·붙여넣기는 수정 진입 전 숨긴다.
- 대여료 표는 `readOnly`에서 input·기간추가·삭제를 제거하고 실제 금액이 있는 기간만 숫자 텍스트로 표시한다. 사진도 조회 중 추가·꾹 편집·확대 편집푸터는 숨기되 탭 확대는 유지한다.
- 390×844 실제 관리자 세션에서 재고 조회 input 0/select 0/사진추가 0, 정책 조회 input 0, 문서폭 390/390을 확인했다. `수정` 진입 시 재고 29개·셀렉트 6개, 정책 39개 편집 컨트롤과 아이콘+텍스트 취소/저장이 다시 표시됐으며 값을 바꾸거나 저장하지 않고 취소했다.
- `typecheck`, UI contract, fonts, phase12 **64/64**, 별도 `NEXT_DIST_DIR=.next-codex-mobile-read` production build **30/30 routes PASS**다. 저장 핸들러·도메인 데이터·Rules·RTDB 어댑터·계약/정산 엔진·write·배포는 변경하지 않았다.

## 2026-08-04 모바일 비계약 실행 버튼 전수 보강

결과: **회원·설정·문의·재고 보조액션 아이콘 규격 PASS / 모바일 문의 전송 icon-only PASS / 가로 overflow 0 PASS**

- 계약 외 모바일 사용자 경로를 소스와 390×844 실제 관리자 세션으로 다시 검사했다. 회원 상세의 `승인 취소(대기로)`가 텍스트-only였고 설정의 저장·비밀번호·로그아웃·링크복사·관심목록 정리, 상품 검수 요청, 재고 편집 초기화·복사·붙여넣기에도 같은 잔여 드리프트가 있었다.
- 위 결정적 실행은 공통 `ButtonLabel`의 아이콘+텍스트로 통일했다. 간단문의 입력행은 입력폭 우선 예외에 맞춰 모바일에서 전송 아이콘-only, 웹에서는 전송 아이콘+텍스트를 유지하고 전체 대화 이동은 아이콘+텍스트로 표시한다.
- 실제 회원 상세에서 승인취소·목록·수정·삭제 버튼 모두 SVG 1개를 확인했다. 실제 설정에서 내 정보 저장·비밀번호 변경·로그아웃·카탈로그 복사·최근/찜 비우기 모두 SVG 1개, 문서폭 390/390, 엑셀·다운로드 노출 0을 확인했다.
- 회원 승인·민감정보·재고 저장 등의 핸들러와 도메인 로직은 바꾸지 않았다. 운영 데이터·Rules·RTDB 어댑터·계약/정산 엔진·write·배포 변경도 없다.
- `typecheck`, UI contract, fonts, phase12 **59/59**, 계약 업무목록 **142/142**, 별도 `NEXT_DIST_DIR=.next-codex-mobile-actions2` production build **30/30 routes PASS**다. 빌드가 자동 변경한 `tsconfig.json`은 원복했고 로컬 서버를 유지했다.

## 2026-08-04 모바일 상세 실행 버튼 아이콘 규격 검수

결과: **결정적 실행 버튼 아이콘+텍스트 통일 PASS / 선택 버튼 텍스트 유지 PASS / 모바일 엑셀 미노출 PASS**

- 모바일 규격을 다시 대조해 모든 버튼을 무조건 아이콘-only로 바꾸지 않았다. 뒤로·닫기·검색·필터 같은 탐색/도구만 아이콘-only이고, 저장·삭제·승인·정산확정·문의 같은 결정적 실행은 아이콘+텍스트, 가능·협의·불가 및 승인·부결 같은 선택지는 텍스트를 유지하는 SSOT다.
- 390×844 실제 관리자 세션에서 계약 상세를 열어 계약취소·출고 문의·완료 표시 등 결정적 버튼이 텍스트-only인 상태를 확인했다. 공통 `ButtonLabel`을 추가하고 계약 진행, 전자서명, 메모 저장, 첨부 삭제, 금액 저장, 정산 상태 변경에 같은 아이콘+텍스트 조합을 적용했다.
- 수정 후 실제 DOM에서 계약 취소·출고 문의하기·완료로 표시 버튼 각각 SVG 1개를 확인했다. 가능·협의·불가 선택지는 SVG 0개로 유지했고, 하단 목록·진행·서류·정산 탭은 각각 아이콘 1개와 라벨이 함께 표시됐다. 모바일 엑셀 다운로드는 노출되지 않았다.
- 표현 계층만 수정했다. 계약 엔진, 정산 엔진, RTDB 어댑터, Rules, 운영 데이터, write, 배포는 변경하지 않았다.
- `typecheck`, UI contract, fonts, phase12 **53/53**, 계약 업무목록 **142/142**, 별도 `NEXT_DIST_DIR=.next-codex-mobile-actions` production build **30/30 routes PASS**다. 빌드가 자동 변경한 `tsconfig.json`은 원복했고 로컬 개발 서버는 유지했다.

## 2026-08-04 모바일 계약 목록 첫 페인트·표시명 보강

결과: **문의→계약 전환 skeleton 재등장 제거 / 정산 read 비차단 / 공급사 내부코드 깜빡임 제거 PASS**

- 모바일 cold 직접 진입을 다시 계측하니 재고 실데이터는 약 2초, 계약은 약 9초에 도착해 이전 20~27초가 항상 재현되지는 않았다. 네트워크·개발 서버 변동과 별개로, 문의에서 이미 읽은 계약 캐시가 있어도 `/contract`가 `rows=null`로 시작하고 정산 read까지 기다린 뒤 행을 그리는 확정 병목을 수정했다.
- 계약 화면은 같은 인증 세션에서 권한 스코프로 읽은 `peekList('contract')`를 첫 렌더에 사용하고 곧바로 live read로 갱신한다. 인증 변경 시 전역 store cache가 비워지는 기존 안전장치는 유지했다.
- 계약 행은 계약 read 완료 즉시 표시하고, 목록에 필요 없는 정산 목록은 백그라운드 선조회로 분리했다. 상세 선택은 같은 pending/cache Promise를 재사용하므로 정산 기능·단일 writer·금액 로직은 변경하지 않았다.
- 공급사 파트너 목록을 대용량 상품·삭제이력 보강보다 먼저 독립 조회하고 `providerNameMap`을 목록 인덱스에 합쳤다. 캐시 계약이 먼저 보일 때 `RP018` 같은 내부코드가 잠시 노출되지 않고 첫 계약 프레임부터 `스타` 등 실제 표시명이 나온다.
- 390×844 실제 하단 독에서 `/chat` 176행을 읽은 뒤 `계약진행`을 클릭했다. `/contract` 첫 관찰 프레임에 실제 43행·공급사명·상태 배지가 함께 표시됐고 skeleton 0, 좌측 inset 0, 문서 폭 390/390이었다.
- UI 계약 게이트에 계약 캐시 첫 페인트와 `setRows`가 정산 완료보다 앞서야 한다는 조건을 추가했다. `typecheck`, UI contract, fonts, 계약 업무목록 142/142, 별도 production build 30/30 routes PASS다. 운영 데이터·Rules·계약/정산 write·배포는 변경하지 않았다.

## 2026-08-04 모바일 목록 좌측 바·반응형 정밀 검수

결과: **좌측 상태 바 제거 PASS / 모바일 단일 목록·가로 overflow 0 PASS / 실데이터 주요 목록 표시 PASS**

- 실제 390×844 로컬 관리자 세션에서 `/chat`을 확인해 미확인·진행 행에 남아 있던 주황/파랑 좌측 inset 바를 재현했다. `FeedListRow`의 `accent` API와 `ChatRoomRow` 전달을 제거해 상태는 썸네일 아이콘·배지·안읽음 숫자로만, 선택은 `C.selected` 배경으로만 표현하도록 공통 SSOT를 고정했다.
- 같은 검수에서 데스크톱으로 진입한 뒤 폭을 390px로 바꾸면 4패널이 좁은 화면에 압축되는 결함을 발견했다. `data-fp-m`이 부트 이후에도 live viewport보다 우선하던 것이 원인이며, 마운트 후 모바일 판정은 `window.innerWidth`를 단일 기준으로 사용하도록 수정했다. 초기 SSR 힌트·쿠키 동기화는 그대로 유지한다.
- 수정 후 `/chat` 실제 176행에서 좌측 inset 0, `/contract` 43행, `/inventory` 101행, `/members` 101행, `/settlement` 11행, `/policy` 26행을 순회했다. 전 화면 `innerWidth=scrollWidth=390`, 공통 목록 좌측 inset 0이며 데스크톱 다중 패널 대신 모바일 단일 목록·하단 독으로 표시됐다.
- 회원·파트너의 긴 UID/회사 정보는 행 내부에서 잘리고 문서 가로 스크롤은 생기지 않았다. 계약·재고·회원·정산·정책의 3줄 정보 구조와 상태 아이콘·배지 표현은 유지됐다.
- `check-ui-contract.mts`에 좌측 `accent` 재도입 금지와 live viewport 판정 계약을 추가했다. `typecheck`, UI contract, fonts, 재고 표시 28/28, 회원 표시 10/10, 정산 표시 30/30, 별도 `NEXT_DIST_DIR=.next-codex-mobile-list` production build 30/30 routes가 PASS다. 빌드가 자동 변경한 `tsconfig.json`은 원복했다.
- 관찰사항: 로컬 cold 직접 진입에서 계약·재고 실데이터 도착이 약 20~27초까지 걸린 사례가 있었다. 무한 로딩은 아니었고 데이터 도착 후 정상 표시됐지만, 새 Preview에서 역할별 실계정으로 초기 read 시간을 다시 계측해야 한다. 운영 데이터·Rules·배포·write는 변경하지 않았다.

## 2026-08-04 공급사 Sheet exact patch/CAS dry-run 2단계

결과: **후보별 exact 공개 v4 patch·CAS 생성 PASS / private 유입·참조이관 fail-closed / 실제 적용·운영 write 0건**

- `lib/domain/sheet-decision-patch-dry-run.ts`를 추가했다. 1단계 적용계획의 검토 후보를 `Sheet 유입 제외 원장 create-if-absent`, `동일 삭제키 복구 update`, `신규 상품 create-if-absent`, `승인된 신원 원자 update`의 정확한 경로·patch·CAS 기대값으로 변환한다.
- 삭제 복구는 기존 `softMergeProduct`·`changedPatch`를 재사용한다. 빈 Sheet 값은 수기값을 지우지 않고, `_deleted/deletedAt/deletedReason/legacy status=deleted` 해제만 명시하며 현재 공개상품을 `productPatchPreconditionMatches`와 같은 필드 집합으로 CAS 대조한다.
- 신규 상품은 현재 Sheet 공개 필드만 사용하고 회사·생성/수정자·결정 지문을 기록하는 create-if-absent 후보를 만든다. 신원 갱신은 관리자가 승인한 제조사·모델·세부모델·트림·내외장색·연식·연료·최초 신원서명 원자만 기존키에 patch하며 상태·가격은 따라오지 않는다.
- Sheet 입력이나 기존 CAS 출력에 원가·VIN·계좌·기간별 fee/commission/fee_memo가 섞이지 않는다. 신규/복구 Sheet 행에 private 값이 있으면 patch 0건으로 fail-closed한다. 검토 JSON에도 private 값과 원본 충돌 raw는 포함하지 않는다.
- 공급사 대표키 변경은 계약·채팅·견적·private의 역사 참조 처리 정책이 확정되지 않았으므로 `blocked_reference_migration`으로 유지한다. 상위 적용계획에서 계약보호·원장불일치·키 중복 등으로 차단된 행도 patch 후보로 승격하지 않는다.
- 관리자 `데이터 검증`에 `patch dry-run JSON` 버튼을 연결했다. 정확한 공개 patch와 최소 CAS 기대값을 복사하지만 저장 API·적용 버튼·동기화 차단 해제는 없다. 모든 행은 `applyAllowed=false`, 요약은 `executableOperations=0`이다.
- 신규 patch dry-run 적대검증 **21/21 PASS**. 적용계획 **20/20**, Sheet merge **128/128**, 일일동기화 **21/21**, 가격승인 **10/10**, 소유권결정 **15/15**, 결정 dry-run **16/16**, 신원검토 **17/17**, 신원결정 **15/15**, 차량잠금 **23/23** — 관련 회귀 합계 **286/286 PASS**다.
- `typecheck`, fonts, UI contract, 별도 `NEXT_DIST_DIR=.next-codex-sheet-patch` production build **30/30 routes** PASS다. Next가 자동 추가한 임시 dist include는 `tsconfig.json`에서 원복했다. 운영 데이터·Rules·환경변수는 변경하지 않았다.
- 다음 게이트: 운영/Preview 실제 판단 원장으로 이 JSON을 생성해 사람/Claude가 공급사 대표키 참조 정책과 각 후보 patch를 승인한다. 그 전에는 적용 API를 만들거나 `planDailySheetSync`가 제외 원장을 소비하게 해서는 안 되며 `SHEET_DAILY_SYNC_ENABLED=false`를 유지한다.

## 2026-08-04 공급사 Sheet 판단 원장 적용계획 1단계

결과: **판단 원장 → 차량별 비파괴 적용계획 PASS / 실제 patch·동기화 차단 해제·운영 write 0건**

- `lib/domain/sheet-decision-application-plan.ts`를 추가했다. 현재 검증 스냅샷과 소유권·삭제 결정 원장, 신원 결정 원장을 함께 읽어 `Sheet 유입 제외`, `삭제키 복구`, `신규 상품 생성`, `공급사 대표키·참조 이관`, `동일차 신원 원자 갱신` 후보로 분류한다.
- 공급사 변경 후보는 상품의 `v4/products`만 바꾸지 않는다. `v4/products_private`와 계약·채팅방·견적의 상품 참조 건수를 계산하고 `requires_reference_migration`으로 분리한다. 실제 참조 patch는 생성·실행하지 않는다.
- 계약보호, 공급사·기존키·Sheet키 다중/누락, 병합 별칭 tombstone, 현재 데이터와 원장 불일치, 신규키 기존 존재, 서로 다른 판단의 동일 상품키 중복, 현재 충돌에 없는 stale 원장을 모두 fail-closed한다.
- 모든 계획 행은 `applyAllowed=false`, 전체 요약은 `executableOperations=0`으로 고정했다. 후보 경로·필드·참조 수는 다음 단계의 검토 근거일 뿐 저장 payload가 아니다.
- 관리자 재고 `데이터 검증`은 기존 계약과 함께 채팅방·견적을 병렬 read하고, `적용 계획 TSV`에서 후보작업·상태·계약/채팅/견적 참조 수·후보경로·다음조치를 복사할 수 있다. 버튼은 저장이나 동기화 차단 해제를 수행하지 않는다.
- 신규 적대 시뮬레이션 **20/20 PASS**. 기존 Sheet merge **128/128**, 일일 동기화 **21/21**, 가격승인 **10/10**, 소유권·삭제 결정 **15/15**, 결정 dry-run **16/16**, 신원검토 **17/17**, 신원결정 **15/15**도 재통과했다.
- `typecheck`, fonts, UI contract, 별도 `NEXT_DIST_DIR=.next-codex-sheet-application` production build **30/30 routes** PASS다. 빌드가 dev 산출물을 덮지 않아 `http://localhost:4004/login` HTTP 200, PID 49616을 유지했다.
- 중요: 이 계획기는 아직 `planDailySheetSync`나 `commitFetchedPartnerSheets`가 소비하지 않는다. `SHEET_DAILY_SYNC_ENABLED`는 계속 false여야 한다. 다음 게이트는 후보별 정확한 v4 patch/CAS payload를 **무저장으로만** 생성하고 사람/Claude가 참조 이관·복구 정책을 승인하는 것이다.

## 2026-08-04 버벅임·먹통 오픈 최종점검

결과: **성능·응답성 게이트 PASS / 전체 오픈은 법적 정보·수정본 Preview·실계정·Rules 승인 전 NO-GO**

- 정상 Ready Preview `dpl_9H8TtHymfPhUcocr1gbvpnim66FQ`에서 직접 경로 전환을 데스크톱 24회, 모바일 15회 반복했다. 총 **39회 중 실패·먹통 0**, 데스크톱 평균 606ms/최대 1,590ms, 모바일 평균 677ms/최대 1,565ms였다. 가로 overflow와 console error/warning도 0건이다.
- 최초 진입은 로그인 1,762ms, 공개 상품찾기 1,542ms, 공급사 재고 1.4~1.6초가 상대적으로 느렸고 나머지 주요 업무 화면은 약 0.35~0.7초였다. 2초를 넘은 직접 경로 전환은 0건이다.
- 모바일 계약 필터는 실제 dialog가 정상 열리고 닫혔다. 자동화 locator가 한 차례 3초 timeout을 냈지만 화면·DOM은 계속 응답했고 재시도는 289ms였다. 닫기 후 220ms exit animation 때문에 자동화 시간이 길게 잡힐 수 있으나 실제 고정 overlay나 먹통은 재현되지 않았다.
- 로컬 관리자 실데이터 355대 기준 재고 hard reload는 shell 표시 약 **0.50초**, 전체 행 도착 약 **4.75초**였다. 빈 화면은 아니지만 네트워크/브리지 최초 read가 가장 큰 체감 지연이다. warm 재진입은 즉시 수준이다.
- `features/inventory/useInventoryData.ts`에서 권한 확인 직후 4패널 shell을 먼저 표시하고 정책·상품(내부 파트너 보강 포함) read를 병렬화했다. 기존 정책 → 상품 → 파트너 순차 대기를 제거했으며 write·v3·Rules·정산 엔진은 건드리지 않았다.
- 공통 `Loading`은 12초 이상 지속되면 지연 안내와 `새로고침` 복구 버튼을 표시한다. 관리·공개 상세 페이지의 미종료 통신도 더 이상 설명 없는 무한 스피너로만 남지 않는다.
- 수정 후 `npm run typecheck`, `npm run check:fonts`, `npm run check:ui`, 재고 표시 **28/28**, `git diff --check`, Next production build **30/30 routes**가 PASS다. production build와 dev가 같은 `.next`를 사용해 기존 dev가 500이 된 것을 즉시 발견했고, 서버를 재기동해 `http://localhost:4004/login` HTTP 200을 재확인했다. 현재 포트 4004 listener는 PID 49616이다.
- 이 최적화와 장시간 로딩 복구 UI는 현재 로컬 소스에만 있으며 위 Ready Preview에는 포함되지 않았다. 새 정상 Preview 배포 후 355대 실계정 재고 hard reload와 역할별 화면을 다시 확인해야 한다. Production 별칭·데이터·Rules는 변경하지 않았다.
- 전체 오픈 차단은 성능 문제가 아니라 기존의 약관/개인정보 운영자 사실값 6개, 영업자·공급사 QA 토큰 smoke, 사람/Claude의 후보 Rules·실데이터 승인이다.

## 2026-08-03 Chrome 초정밀 오픈 전 검수

결과: **화면·기능·빌드·비게시 Rules 후보 PASS / 법적 운영자 정보·운영 Rules 승인·역할별 실계정 smoke 전 전체 오픈 NO-GO**

- Chrome에서 Ready Preview `dpl_9H8TtHymfPhUcocr1gbvpnim66FQ` (`freepasserp4-48ikovxat-freepass-projects.vercel.app`)를 직접 검수했다. 로그인·회원가입·비밀번호 재설정·둘러보기·상품찾기·FAQ·약관·개인정보, 영업자 `/chat`·`/contract`, 공급사 `/inventory`·`/policy`·`/chat`·`/contract`, 권한 제한 경로를 실제 탐색했고 console error/warning은 0건이다.
- 데스크톱 1920×889에서 재고·정책·채팅·계약의 4패널, 검색·정렬·필터, 빈 상태, 공급사 상품등록 폼 진입·취소를 확인했다. 저장·업로드·삭제 등 운영 write는 실행하지 않았다.
- 모바일 390×844에서 가로 overflow 0, 접근성 이름 없는 버튼 0, 엑셀 버튼 미노출, 계약 화면의 비규격 `대기·완료·환수·순수익` 지표 미노출을 확인했다. 계약 필터 dialog와 공급사 재고 하단탭·아이콘 툴바도 실제 클릭/시각 검수했다.
- 직접 URL 검수에서 관리자 전용 `/data-check`·회원·감사 화면이 권한 판정보다 `seedIfEmpty`를 먼저 기다리고, `/diag`가 비로그인에도 RTDB 노드 진단 UI를 노출하는 문제를 발견했다. `app/data-check/page.tsx`, `app/members/page.tsx`, `app/audit/page.tsx`, `app/dev/page.tsx`는 권한 확인을 초기화보다 앞으로 옮겼고 `app/diag/page.tsx`에는 관리자 게이트를 추가했다.
- 수정 후 `tsc --noEmit`, fonts, tokens, UI contract, Next production build(정적 페이지 30/30)가 PASS다. 전체 `sim-*.mts` 38개 중 기능 시뮬 35개가 PASS했고, 운영 `database.rules.json`을 일부러 검사한 보안 3개는 기존 운영 Rules 미게시 상태 때문에 예상대로 실패했다.
- 같은 3개 검사를 비게시 `scripts/ruleprobe/release-candidate.rules.json`에 적용하면 보안 **14/14**, 계약 **26/26**, 전자서명 **58/58** PASS다. 기존 9000/9099 서버를 끄지 않고 9100/9199 격리 포트에서 Firebase Emulator **32/32 PASS**를 재확인했다. 운영 `database.rules.json`은 수정·게시하지 않았다.
- `check-release --rules=...release-candidate...`의 유일한 차단은 약관·개인정보의 **상호, 대표자, 주소, 사업자등록번호, 문의 이메일, 개인정보 보호책임자** 6필드 누락이다. 192/512 PNG 아이콘과 서비스워커는 PWA 범위 경고 2건이다.
- UI 권한 수정본 새 Preview 업로드를 시도해 `dpl_6MUEdmNbgKp5Ng5oEL9cffn98r8S`가 생겼지만 Vercel CLI 업로드가 완료되지 않아 status `UNKNOWN`, build 0ms다. 이 배포는 검수·사용 금지이며 정상 Preview는 위 Ready 배포다. Production 환경·별칭·데이터는 변경하지 않았다.
- 로컬 개발 서버는 `http://localhost:4004`에 숨김 백그라운드로 다시 기동해 유지했다. 로컬 Chrome에는 기존 관리자 세션이 있어 사용자 세션을 지우는 비로그인 재현은 하지 않았고, 수정본 런타임 배포 재확인은 다음 정상 Preview에서 수행한다.
- 전체 오픈 조건: 법적 6필드 입력 → 새 Ready Preview에서 비로그인 `/data-check`·`/diag` 홈 전환 재확인 → 전용 영업자·공급사 QA 토큰 bridge 200/private 격리 smoke → 사람/Claude의 Rules·실데이터 승인 → 운영 Rules 게시 및 역할별 write/read smoke.

## 2026-08-03 영업자·공급사 B2B 운영 오픈 재판정

결과: **기능·화면·비게시 후보 Rules·Preview 비인증 스모크 PASS / Preview 역할별 실계정 smoke·사람 Rules 승인 전 B2B 오픈 NO-GO / 손님 공개 페이지·법적 고지는 후속 범위**

- 오픈 범위를 실로그인 영업자·공급사의 상품조회, 계약문의, 계약진행, 공급사 재고·정책, 계약별 정산조회로 한정해 다시 검증했다. 관리자 월별정산은 운영 지원 기능으로 포함하고 손님 공유/서명 화면 정비는 다음 범위로 분리했다.
- 영업자 전체 여정 **44/44**, 공통 문의·계약 **48/48**, 역할·소유권 **44/44**, 채팅 Rules **43/43**, 계약 업무목록 **142/142**, 재고표시 **28/28**, 회원표시 **10/10**, 정산표시 **30/30**, 차량 생애주기 PASS, 차량잠금 **23/23**, 3자 계약→정산 **22/22**, 삭제상품 보안 **10/10**, 정산 private write **8/8**을 통과했다.
- `sheet-merge.ts`에서 Sheet 가격 soft-merge에 따라오던 `fee/commission/fee_memo`와 원가·VIN·계좌 원자를 CAS 패치 전에 제거했다. Sheet는 공개 재고·대여조건만 갱신하고 private 원자는 건드리지 않으므로, 공개 가격만 먼저 저장된 뒤 화면이 실패로 보고하는 부분 성공 경로를 제거했다. Sheet merge는 **128/128 PASS**다.
- 브라우저에서 영업자 `/chat`·`/contract`, 공급사 `/inventory`·`/policy`·`/contract`를 직접 렌더했다. 역할을 공급사로 바꿔도 상단 계정명이 `박영업`으로 남던 결함을 `TopBar`의 `fp:role` 구독으로 수정했고, `둘러보기·제일오토렌탈` 즉시 반영과 콘솔 error 0을 확인했다.
- type/fonts/tokens/UI contract와 production build 정적 페이지 **30/30 PASS**다. 로컬/기본 설정에서 `SHEET_DAILY_SYNC_ENABLED`는 true가 아니므로, 미결 Sheet 충돌이 있는 자동 저장은 활성화되지 않는다.
- 현재 운영 `database.rules.json`은 B2B 보안 차단 12개가 남는다. v3 원문 광역 read/write, v4 소유권, 계약 스냅샷·고객 PII·전자서명·메모, 공급사 취소, settlement/private 금액 위조가 운영 규칙에서 아직 닫히지 않았다. 실제 `sim-contract-rules`도 계약 차량 스냅샷 불변에서 실패한다.
- 비게시 후보에서 기존 검사가 놓친 `v4/products` read 계정상태 우회를 발견했다. 후보 생성기에 활성·배정 역할 제한을 추가하고 승인대기·비활성·삭제·반려·미배정 역할의 실제 Emulator read 차단을 보강했다. 현재 후보는 보안 **14/14**, 계약 **26/26**, 전자서명 **58/58**, 격리 Emulator **32/32 PASS**다. 운영 `database.rules.json`은 수정·게시하지 않았고 이 후보는 사람/Claude 실데이터 게이트 전 머지·게시 금지다.
- 최신 운영 read-only 대조는 공급사 Sheet 16곳·389대, v3-only **292건/288대**를 확인했다. 이 가운데 현재 Sheet 연결 239대(참조보호 31대 포함), 참조만 7대, Sheet·참조 없음 42대다. 따라서 후보 Rules만 먼저 게시하면 영업자·공급사 화면에서 정당한 레거시 재고가 사라지는 것이 확정됐다.
- 데이터 강제이관 대신 `/api/products/bridge` 읽기 전용 호환층을 추가했다. 서버가 ID token과 현재 `users/{uid}`의 역할·승인·활성 상태를 매 요청 재검증하고, 영업자·타 공급사 매물에서는 원가·VIN·계좌·기간별 내부 수수료를 제거한다. 공급사는 자기 회사 원문만, 관리자는 운영 원문을 받는다. 클라이언트는 이 API를 먼저 사용하고 현재 Rules/로컬 서버 미설정 때만 기존 직접 read로 복구한다. v3/v4 write 경로는 바꾸지 않았다.
- 서버 응답은 활성 재고와 계약·문의가 실제 참조하는 삭제 이력만 선별한다. 최신 운영 read-only 계산은 원시 약 5,700건 중 **740건**으로 응답 상한 2,000건 안이다. 상품 서버 브리지 적대검증 **16/16 PASS**, 비인증 API HTTP **403**, typecheck 및 production build 정적 페이지 **30/30 PASS**다. 브라우저에서 영업자·공급사 역할 전환과 `/catalog`·`/chat`·`/contract`·`/inventory`·`/policy`를 재검수했고 콘솔 로그 0건이다. 둘러보기에는 실계정 토큰이 없어 인증 성공 API E2E는 운영 전 smoke gate로 남겼다.
- Vercel Preview에만 서버 전용 `FIREBASE_SERVICE_ACCOUNT_JSON`을 Sensitive로 등록하고 격리 소스 스냅샷을 배포했다. 첫 Preview에서 `firebase-admin@14.2.0 → jwks-rsa@4 → jose@6`의 CJS/ESM 충돌로 Admin SDK 사용 API가 500이 되는 것을 실런타임 로그로 확인했다. `firebase-admin`을 마지막 13.x인 `13.10.0`으로 고정해 `jwks-rsa@3.2.2 / jose@4.15.9`로 복구했다.
- Next 자체 고위험 Server Actions/SSRF 패치를 위해 `next@15.5.21`을 고정했다. 이어 서버 `not-found`가 client UI 배럴의 `FW/C` 객체 속성을 접근해 `/login` 로그에 남기던 React 직렬화 오류를 리프 import로 수정하고 UI 계약 게이트에 재발 검사를 추가했다. SheetJS 보안 교체까지 포함한 최종 Preview `dpl_9H8TtHymfPhUcocr1gbvpnim66FQ` (`freepasserp4-48ikovxat-freepass-projects.vercel.app`)는 Ready이며 로그인 200, 존재하지 않는 경로 404, Admin API 3종 비인증 403, 요청 후 Vercel error 로그 0건이다.
- 중단된 npm `xlsx@0.18.5`는 SheetJS 공식 설치 지침에 따라 보안 수정본 `0.20.3` 공식 CDN tarball로 고정했다. 메모리 workbook write/read roundtrip, 정산표시 30/30, 정산 E2E 22/22, private 8/8, Phase12 48/48, Sheet merge 128/128 PASS다. production 의존성 감사는 critical 0, high 3, moderate 8로 감소했고 남은 high는 Next가 아직 고정한 `postcss/sharp` 전이 항목과 그 집계다. Next upstream 호환 전 무검증 override·강제 major 수정은 하지 않는다.
- 수정 Preview에서 `/login` GET은 200, `/api/products/bridge`·`/api/inventory/duplicate-plan`·`/api/sheet/sync-status` 비인증 요청은 모두 500 없이 403으로 fail-closed했다. Production 환경변수와 별칭은 변경하지 않았으며 공개 Production `/api/products/bridge`는 계속 404다.
- `npm run check:b2b-release`는 Vercel Node 함수와 호환되지 않는 Admin SDK 하위 의존성 조합도 차단한다. 서비스계정을 현재 프로세스에만 주입한 실제 결과는 **23 PASS / 0 FAIL**이고, 비밀값은 저장소·명령 출력에 남기지 않았다.
- `smoke-b2b-product-bridge.mts`는 배포 URL과 환경변수로 받은 영업자·공급사 ID token을 사용해 비인증 403, 양 역할 200, 상품 집합·count, 영업자 private 0, 공급사 타회사 private 0, 공개내용 일치를 검사한다. 전용 QA 토큰 2개가 없어 인증 성공 부분은 아직 미실행이며 실제 운영 사용자를 임의로 가장하지 않았다.
- 최종 조건: 전용 영업자·공급사 QA 토큰으로 Preview 브리지 목록·원가 격리 smoke → 사람/Claude가 보강된 후보 Rules와 실데이터 호환성 승인 → 운영 Rules 게시 → 문의·계약 단계 write·정산 read smoke. 서버 브리지보다 Rules를 먼저 게시하면 안 된다.

## 2026-08-03 신원·미확정 충돌 차량별 결정 원장

결과: **남은 16대 관리자 판단 기록 PASS / 계약보호·원문변경·대상모호 fail-closed / 복구·신규·재번호·유입제외 실행 0건 / 운영 동기화 NO-GO 유지**

- `lib/domain/sheet-identity-decision.ts`에 세 충돌 유형별 선택지를 분리했다. 미확정 삭제는 동일차 복구/귀속 검토·다른차 신규 검토·Sheet 오류, 번호미정 식별변경은 기존/신규 임시번호 유지·Sheet 오류, 임시번호 신원불일치는 원자 수정 수용·신규 임시번호 발급·Sheet 오류만 기록할 수 있다.
- 판단 지문은 `충돌 유형 + 원본 충돌 문자열`에 결속된다. 원문이 바뀌면 과거 판단은 매칭되지 않는다. 공급사·기존 상품키·현재 Sheet 키가 각각 하나로 특정되지 않으면 화면에서 선택할 수 없다.
- `/api/sheet/identity-decisions` GET/POST/DELETE는 admin Bearer와 서버의 현재 v3+v4 상품·계약 재검증을 요구한다. 계약락·계약중·진행계약 및 원문에서 차량번호를 해석할 수 없는 건은 409/fail-closed다.
- 원장은 `v4/sheet_identity_decisions`, 감사는 `v4/audit_logs`에만 저장한다. 원본 충돌 문자열과 차량번호는 저장하지 않는다. 운영 원장을 Firebase CLI로 값 없이 read-only 집계한 결과 **total 0 / recorded 0 / revoked 0**이며 운영 write는 없었다.
- 관리자 `신원·미확정 충돌 검토` 모달에서 변경 원자·기존키·Sheet키·계약보호와 함께 결정/철회를 수행한다. 기록 대상키가 현재 검토행과 달라지면 불일치로 표시한다. 화면과 확인문은 결정이 재고 복구·신규 생성·번호 변경·유입 제외를 실행하지 않고 동기화 차단도 유지함을 명시한다.
- 이 원장은 `planDailySheetSync`와 `commitFetchedPartnerSheets`가 소비하지 않는다. 결정값별 실제 적용 정책과 v4 patch/참조 계획은 사람·Claude 게이트 이후 별도 구현해야 한다.
- 검증: 신원 결정 **15/15**, 신원 원자 검토 **17/17**, 소유권·삭제 결정 **15/15**, 결정 dry-run **16/16**, 가격승인 **10/10**, 가격행렬 **29/29**, 일일 동기화 **21/21**, Sheet merge **126/126**, type/fonts/tokens/UI contract, production build 정적 페이지 **30/30 PASS**.
- 브라우저에서 `/inventory` 권한 제한 화면을 직접 렌더하고 콘솔 error 0을 확인했다. 로컬 브라우저에 관리자 인증이 없어 새 모달의 실데이터 선택·POST/DELETE E2E는 preview 관리자 게이트로 남겼다. 전체 공개 출시는 기존 법적 정보·v3 절연·운영 Rules 게이트 때문에 계속 NO-GO다.

## 2026-08-03 공급사 미확정 삭제·임시번호 신원 원자 검토

결과: **남은 16대 차단군 원자 분해·관리자 검토 화면 PASS / 자동 동일차 판정·복구·번호재배정 0건 / 운영 동기화 계속 NO-GO**

- 최신 16개 공급사 Sheet 388대와 ERP 삭제/활성 재고·계약을 다시 read-only 대조했다. 공급사 미확정 삭제 8대, 번호미정 식별변경 1대, 같은 임시번호 신원서명 불일치 7대가 그대로 재현됐다.
- 공급사 미확정 삭제 8대는 모두 `공급사 없는 삭제 1건 ↔ 현재 단일 Sheet 행 1건` 연결 후보이며 대상모호 0건이다. 이는 자동복구 허용이 아니라 실소유 공급사와 삭제 유지/복구 결정을 시작할 수 있는 단일 후보라는 뜻이다.
- 임시번호 8대는 같은 차량의 셀 수정과 다른 실물 차량 교체를 자동 구분할 수 없다. 한 건은 계약보호로 자동수정 금지이며, 전체 16대의 실행작업은 0건이다.
- 신원 원자는 제조사·모델·세부모델·트림·외장색·내장색·최초등록/연식·연료로 분해했다. 공급사 미확정 삭제 8대에서는 최초등록/연식 8, 세부모델 6, 외장색 5, 내장색 4, 제조사 1건이 달랐다.
- 번호미정 식별변경 1대는 트림·외장색·내장색·연료·최초등록/연식이 모두 달랐다. 임시번호 신원불일치 7대는 트림·외장색·내장색·연료 각 7, 최초등록/연식 6, 제조사·모델·세부모델 각 3건이 달랐다. 단순 오탈자 일괄승인으로 처리할 수 없는 범위다.
- `sheet-identity-conflict-review.ts`는 계약락·계약중·진행계약을 우선 보호하고, 병합 별칭 tombstone·다중 삭제·복수 Sheet 공급사를 모호 대상으로 차단한다. 모든 행은 `applyAllowed=false`, `executableOperations=0`이다.
- 관리자 Sheet 검증 화면에 `신원·미확정 검토 (16)` 모달을 추가했다. 차량별 기존 상품키, 현재 Sheet 키, Sheet 공급사, 변경 원자, 계약보호/판단근거, 다음 확인을 같은 행에 표시하며 적용 버튼은 없다. TSV도 동일 근거만 제공한다.
- 운영 감사 도구에도 PII 값을 출력하지 않는 전체·유형별 신원 원자 집계를 추가했다. 운영 Firebase/Google Sheet write, v3/Rules 변경은 0건이다.
- 검증: 신규 신원 검토 시뮬레이션 **17/17**, 결정 원장 **15/15**, 결정 dry-run **16/16**, 가격승인 **10/10**, 일일 동기화 **21/21**, Sheet merge **126/126**, type/fonts/tokens/UI contract, production build **30/30 routes PASS**.
- 브라우저에서 로컬 권한 제한 재고 화면과 콘솔 오류 0을 확인했다. 관리자 인증이 없어 새 모달의 실데이터 렌더는 preview 관리자 E2E로 남았다. 다음 게이트는 미확정 삭제 8대의 공급사·삭제의도 확정과 임시번호 8대의 원본 Sheet 행별 실물 동일성 확인이다.

## 2026-08-03 Sheet 소유권·삭제 결정 무저장 dry-run

결과: **결정-현재데이터 정합성·후속 작업 분류 PASS / 운영 결정 원장 0건 / 실행 가능한 작업 0건 / 자동 적용 NO-GO**

- 관리자 검증 스냅샷의 소유권·삭제 대상, 현재 Sheet 공급사, ERP 공급사, 상품키, 계약보호, 병합 별칭, v4 결정 원장을 한 번에 대조하는 순수 dry-run을 추가했다.
- 같은 원본 충돌의 상세 리포트가 여러 상품 행으로 펼쳐져도 차량별 한 건으로 묶는다. 상품키·ERP 공급사·Sheet 공급사가 각각 정확히 1개가 아니면 `target_ambiguous`로 차단한다.
- 결정 당시 공급사·상품키가 현재 대상과 다르거나 충돌 유형과 결정값 조합이 손상된 경우 `ledger_mismatch`, 현재 충돌에 더 이상 없는 기록은 `stale_ledger`로 분리한다. 원본 변경을 과거 결정으로 통과시키지 않는다.
- `기존 공급사 유지`와 `삭제 유지`는 v4 재고 patch가 아니라 해당 Sheet 유입을 제외하는 정책 후보로 분류한다. 현재 동기화 계획은 아직 결정 원장을 소비하지 않으므로 둘 다 hard block을 유지한다.
- `현재 Sheet 공급사로 변경`은 단순 `provider_company_code` 수정으로 처리하지 않는다. 상품키 정체성과 계약·채팅·견적·비공개 원가 참조가 함께 바뀌므로 별도 대표키·참조 이관계획 대상으로 분류한다.
- `동일 상품키 복구`만 `v4/products/{상품키}` overlay 후보 경로와 삭제 해제/Sheet soft-merge 후보 필드를 보고한다. 병합 별칭 `_merged_into` tombstone은 원본 상품 복구 금지다. 후보에도 `applyAllowed=false`가 고정된다.
- 관리자 모달에 미결정·계약보호·대상모호·기존귀속 유지·참조이관·삭제유지·복구후보 수와 `실행작업 0`을 표시하고 `결정 dry-run TSV`를 추가했다. TSV는 후보 경로와 다음 조치를 보여주지만 실행 patch 값은 만들지 않는다.
- Firebase CLI로 운영 `v4/sheet_conflict_decisions`를 값/차번 출력 없이 read-only 집계한 결과 **전체 0, recorded 0, revoked 0**이다. 최신 감사의 96대는 따라서 비계약 94대 미결정과 계약보호 2대 차단으로 시작하며 자동 적용 후보는 0대다.
- 검증: 신규 dry-run 적대 시뮬레이션 **16/16**, 결정 원장 **15/15**, 가격승인 **10/10**, 일일 동기화 **21/21**, Sheet merge **126/126**, type/fonts/tokens/UI contract, production build **30/30 routes PASS**.
- 브라우저에서 로컬 로그인 상태·권한 제한 재고 화면과 콘솔 오류 0을 확인했다. 관리자 인증이 없어 새 모달의 실데이터 표시·TSV 버튼은 preview 관리자 E2E로 남았다. 운영 Firebase write, v3/Rules 변경, 동기화 차단 해제는 0건이다.

## 2026-08-03 Sheet 소유권·삭제 충돌 건별 판단 원장

결과: **관리자 건별 검토 화면·비작동 결정 원장 구현 PASS / 결정 기록만으로 재고 변경·차단 해제 0건 / 실제 적용기는 사람·Claude 게이트 전 NO-GO**

- 최신 read-only 감사의 소유권 39대와 삭제 재등장 57대를 원본 충돌 단위로 묶는 `소유권·삭제 결정 검토` 화면을 추가했다. 동일 충돌이 상세 TSV에서 여러 상품 행으로 펼쳐져도 차량별 한 행으로 표시한다.
- 소유권은 `기존 공급사 유지` 또는 `현재 Sheet 공급사로 변경`, 삭제 재등장은 `삭제 유지` 또는 `동일 상품키 복구`만 선택할 수 있다. 현재 ERP 공급사, 현재 Sheet 공급사, 상품키, 계약보호/추가확인 사유를 같은 행에 보여준다.
- 관련 상품키가 정확히 1개가 아니거나 소유권 충돌의 현재 Sheet 공급사가 정확히 1곳이 아니면 선택을 막는다. 최신 감사 기준 비계약 소유권 38대와 동일 상품키 삭제 56대가 검토 가능 후보이고 계약보호 각 1대는 선택 불가다.
- 결정 API `/api/sheet/conflict-decisions`는 활성 admin Bearer를 요구하고, 서버에서 현재 v3+v4 상품·계약을 다시 읽어 계약락·계약중·진행계약을 409로 차단한다. 공급사·상품키가 비어 있는 결정도 거부한다.
- 결정 지문은 `충돌 유형 + 원본 충돌 문자열` 전체에 묶여 있다. 원본 공급사·차량·충돌 내용이 바뀌면 과거 결정과 일치하지 않는다.
- 원장은 `v4/sheet_conflict_decisions`, 감사는 `v4/audit_logs`에만 기록한다. 차번과 원본 충돌 문자열은 저장하지 않고 지문·결정·공급사·상품키·관리자·시각만 남긴다. v3와 운영 Rules는 변경하지 않았다.
- 이 원장은 의도적으로 `planDailySheetSync`, `commitFetchedPartnerSheets`, 수동 커밋에 전달되지 않는다. 기록/철회 후에도 화면 문구와 실제 계획 모두 hard block을 유지하며 재고·tombstone·공급사 귀속 write는 발생하지 않는다.
- 검증: `sim-sheet-conflict-decision` **15/15**, 기존 가격승인 **10/10**, 일일 동기화 **21/21**, Sheet merge **126/126**, type/fonts/tokens/UI contract, production build **30/30 routes PASS**. 로컬 브라우저에서 로그인·게스트 재고 렌더와 콘솔 오류 0을 확인했으나 관리자 세션이 없어 검토 모달 실데이터 E2E와 POST/DELETE는 실행하지 않았다.
- 다음 게이트는 94대의 실제 관리자 판단 수집 → 현재 Sheet·ERP·계약 재조회 dry-run → 공급사 귀속/참조 이관 또는 tombstone 복구의 차량별 v4 patch 계획 → 사람·Claude 승인이다. 그 전에는 자동연동 활성화와 전체 공개 출시가 계속 NO-GO다.

## 2026-08-03 Sheet 가격기간 충돌 관리자 승인 게이트

결과: **비계약 차량의 `기존 가격기간 누락`만 기존 가격 유지 승인으로 해제 가능 / 계약·소유권·삭제·임시신원 충돌은 계속 차단 / 운영 write 0건 / 전체 공개 출시는 기존 Rules·법적 정보 게이트로 NO-GO**

- 시트 원문에서 기존 가격기간이 빠진 경우 soft-merge가 ERP의 과거 가격을 보존한다는 기존 규칙에 맞춰, 관리자가 `기존 가격 유지`를 명시 승인한 건만 수동·매일 자동 동기화 preflight에서 해제한다.
- 승인 지문은 `충돌 유형 + 원문 충돌 문자열` 전체로 결정한다. 기간·공급사·차량 원문이 조금이라도 바뀌면 과거 승인이 일치하지 않아 자동 무효다.
- 승인 API는 Firebase ID token과 활성 admin 역할을 서버에서 다시 확인한다. 서버가 현재 v3+v4 상품·계약을 재조회해 `locked_by_contract`, `계약중`, 진행계약 차량을 409로 차단한다.
- 승인 원장은 `v4/sheet_conflict_resolutions`, 감사는 `v4/audit_logs`에만 기록한다. 차번이 포함된 원문은 둘 다 저장하지 않고 지문·공급사·상품키·승인자·시각만 남긴다. 철회도 별도 감사기록을 남긴다.
- 검증 화면은 승인대기·적용·계약보호 건수를 분리하고, 공급사와 사용자 영향별 승인 버튼·`가격 유지 승인 철회`·전체 충돌 TSV를 같은 검증 영역에 배치한다. 계약보호 건은 승인 후보에서 제외한다. 서로 다른 가격변경 유형 95건을 한 번에 승인하는 전역 버튼은 제거했다.
- 수동 커밋은 확인 전, 확인 직후, 도메인 커밋 경계에서 재고 revision·공급사 roster·계약·승인 원장을 다시 확인한다. 매일 자동 동기화도 최초 계획, write 직전, write 후 검증에서 같은 승인과 계약보호 규칙을 적용한다.
- 공급사 소유권 충돌, 삭제 재등장, 미확정 삭제, 임시번호 식별변경·서명 불일치는 이 원장으로 해제할 수 없다. 참조 이관 또는 운영 판단 전 계속 fail-closed다.
- 최신 운영 읽기 전용 재감사에서 16개 시트·388대 수집은 전부 PASS했다. 가격 충돌은 RP023 90대·RP018 6대이며, 시트 단독 기준 기본가격 변경 70대·기간 누락 26대로 분류됐다. soft-merge는 누락기간을 삭제하지 않고 ERP 값을 유지하지만 새 표준가격이 있으면 화면 기본가가 달라질 수 있다.
- 계약보호 1대를 제외한 실제 승인 묶음은 `RP023 · 새 기본가격 적용 확인` 69대, `RP023 · 누락기간 기존가 유지` 20대, `RP018 · 누락기간 기존가 유지` 6대다. 다른 충돌과 중첩된 2대는 가격 승인 후에도 해당 hard-block이 남는다.
- 위 95건을 모두 승인했다고 가정한 순수 계획도 **BLOCKED**다. 잔여는 소유권 39, 삭제 재등장 57, 미확정 삭제 8, 번호미정 변경 1, 임시번호 서명 7, 계약보호 가격 1건이다. 승인·운영 write 없이 도메인 계획으로 재현했다.
- 잔여 소유권 39대는 `현재 단일 공급사 Sheet vs 기존 다른 공급사` 38대와 계약보호 1대다. 복수 Sheet가 동시에 같은 차를 주장하는 단순 원본 중복은 0대지만, 현재 Sheet를 곧바로 정본으로 삼으면 공급사 귀속·권한·정산 주체가 바뀌므로 자동승인하지 않는다.
- 삭제 재등장 57대는 동일 상품키 삭제이력 56대와 계약보호 1대다. 연결된 삭제 레코드는 77건이며 모두 삭제사유·처리자 표식이 없어 복구와 신규등록을 자동 구분할 수 없다. 미확정 소유 삭제이력 8건도 별도 hard-block이다.
- 따라서 소유권·삭제 충돌에는 안전한 자동처리 후보가 **0건**이다. 사람/Claude가 실소유 공급사와 삭제 의도를 확정하기 전 승인 원장이나 자동 복구 로직을 추가하지 않는다.
- 검증: `sim-sheet-conflict-resolution` **10/10**, `sim-sheet-daily-sync` **21/21**, `sim-sheet-merge` **126/126**, `tsc`, fonts, tokens, UI contract, production build **30/30 routes PASS**.
- 브라우저에서 별도 로컬 서버의 로그인·둘러보기·재고 화면 렌더를 확인했다. 로컬 브라우저에 관리자 인증이 없어 새 관리자 전용 승인 버튼의 실데이터 화면·POST/DELETE는 실행하지 않았다. preview 배포 후 관리자 세션으로 세 승인 묶음·확인창·적용/철회·재검증을 확인하는 것이 남은 운영 UI 게이트다.
- `database.rules.json`과 운영 Firebase 데이터는 변경하지 않았다. `check:release`는 운영 Rules 기준 기존 차단 13·경고 2로 동일하다.

## 2026-08-03 출시 보안 Rules 후보·실제 에뮬레이터 검증

결과: **운영 Rules 미변경 / 보안 후보 정적 13/13·실제 Emulator 26/26 PASS / 완료계약 관리자 전용 취소 22/22 PASS / 법적 정보·v3→v4 절연 전 공개 출시 NO-GO**

- `database.rules.json`은 수정·게시하지 않았다. 별도 생성기와 `release-candidate.rules.json`만 검증했다.
- 후보는 v3 raw write/read, v4 master 소유권, 공개서명 익명 수명, 계약 스냅샷·PII·서명·메모, 차량 잠금, settlement/R1/R2 금액 위조를 계약 레코드에 결속한다.
- 정적 보안 13/13, 계약 Rules 26/26, 전자서명 58/58, Firebase Auth+RTDB Emulator HTTP 적대·정상 26/26 PASS다.
- 정산 public/private 원자 update를 유지하며 private R1/R2에 `contract_code`를 넣어 계약 동결 금액·율과 Rules에서 직접 대조한다.
- 후보 기준 정적 출시 게이트의 코드 차단은 사실값이 필요한 법적 운영자 정보 6개만 남는다.
- 배포본 `/diag`의 원시 수는 v3 products 5,712, v4 products 5,649다. 63건 건수 차이는 v3-only 수로 단정할 수 없으므로 키·필드 전수대조와 이관 없이 v3 read를 닫지 않는다.
- 관리자 SDK 읽기 전용 전수대조에서 삭제 제외 v3 443건·v4 644건, 차량번호 v3-only 288개, 판매 가능 v3-only **289대**를 확인했다. child key 공통은 1개뿐이고 overlay 합산 기준 v3-only 재고가 계약 5건·채팅방 40건과 연결돼 있어 단순 복사·브리지 폐쇄는 NO-GO다.
- 공통 1:1 차량 59대도 트림 57, 연식 51, 배기량·카탈로그 각 50, 연료 37, 공개 가격 13대가 달랐다. v3 공개 노드에는 원가/VIN/계좌 계열 레코드 387건이 남아 있고 v4 공개 노드는 0건이다.
- 엔티티별로 `policy`, `partner`만 v3-only 업무키 0이며 `product`, `user`, `room`, `contract`, `audit_log`는 브리지 유지가 필요하다. 운영 write·브리지 설정 변경은 하지 않았다.
- child-key 기준 관리자 `v3→v4 복사 실행`은 대량 중복 위험 때문에 UI에서 제거하고 엔진도 비-dry-run 호출을 차단했다. 전용 write gate와 정적 출시 게이트 PASS다.
- 동일 공급사 코드의 시트 없는 레거시 partner가 활성 Sheet 실행목록에 재유입되던 결함을 수정했다. 활성 `sheet_url` 레코드만 실행하며 Sheet merge 회귀 **124/124 PASS**다.
- 사용자 승인으로 운영 `v4/partners/RP023.deposit_rule=rent_multiple`을 CAS 저장하고 감사로그 `AL-1785729521849-rp023-deposit-rule`·재조회 일치를 확인했다. v3·재고 write는 없다.
- 최신 운영 설정으로 16개 공급사·388대 수집은 전부 PASS한다. v3-only 292건/288대는 Sheet 현재 238대, 참조만 7대, Sheet·참조 없음 47건/43대다.
- v3 절연 dry-run은 진행계약 보호 4대, 시트 충돌 104대, 승인 후 동일 legacy key overlay 후보 132대, 시트 없음·이력참조 브리지 유지 5대, 시트·참조 없음 공급사 확인 43대로 분류했다.
- 실제 서버와 같은 product_code overlay 병합 기준으로 활성 중복은 0건이다. 저장 계획은 공급사 소유 충돌 39·삭제 재등장 57·미확정 삭제 8·번호미정 변경 1·서명 불일치 7·가격기간 누락 96건으로 정상 차단됐다. 운영 write는 0건이다.
- 상세 작업량은 가격기간 96행·공급사 소유 90행·삭제 재등장 77행·미확정 삭제 8행·임시번호 서명 7행·번호미정 변경 1행이며 계약보호 4행은 자동수정 금지다. RP021은 실제 24대다.
- `audit-v3-only-sheet-coverage.mts --firebase-cli` 읽기 전용 모드를 추가해 서비스계정 없이 로그인된 Firebase CLI `database:get`으로 최신 감사를 재현했다. 원본 값·PII 출력과 운영 write는 없다.
- `npx tsc --noEmit`, `sim-sheet-merge` 126/126 PASS다.
- 완료 전 계약은 영업자 취소를 유지하고 완료 계약은 관리자만 취소·환수하도록 UI와 엔진을 함께 차단했다. 영업자 차단 시 계약·차량·정산 무변경, 관리자 취소 시 차량 해제와 private R1 기준 환수대기를 정산 E2E **22/22 PASS**로 확인했다.
- 상세 게이트와 재현 명령: `docs/SECURITY_RELEASE_GATE_2026-08-03.md`.

## 2026-08-03 UI·UX 전수 규격 통합

결과: **업무 화면 UI 규격 GO / 공개 정식 출시는 법적 운영자 정보와 운영 데이터·Rules 게이트 때문에 NO-GO**

- 배포본 핵심 화면을 브라우저로 확인하고 버튼·드롭다운·폰트·뱃지·필터·목록 아이콘·빈 상태를 현재 코드의 역할별 표시 로직과 대조했다.
- 문의 기본 `문의` 뱃지는 제거된 상태를 확인했다. 좌측 아이콘은 빠른 상태 스캔, 상태 뱃지는 오판 방지용 명시 텍스트로 역할을 분리했다.
- 차량명 제조사·모델·트림 복원, 공급사 실제명 우선, 역할별 상대방, 빈값 사이 고아 구분점 방지를 142개 업무 목록 시뮬레이션으로 확인했다.
- `WebListTools`를 추가해 WorkPage와 일반 Page의 데스크톱 검색·정렬·필터 순서를 하나로 통일했다. FAQ 데스크톱 검색 누락과 감사 페이지 별도 구현을 해결했다.
- 필터의 숨은 가짜 건수 접근성 노출, input/select 패딩, 오류·404 버튼, FAQ·설정·엑셀필터·스켈레톤의 토큰 드리프트를 수정했다.
- `check:ui`를 추가해 새 raw 컨트롤과 숫자 radius를 차단하고 기능상 필수 예외만 이유·개수로 고정했다.
- 표시 시뮬레이션 209건, type/fonts/tokens/UI 계약, production build 30 routes는 PASS다.
- 상세 근거와 페이지별 존재 이유: `docs/UI_LAUNCH_AUDIT_2026-08-03.md`.

## 2026-08-03 중복 재고 v4 patch dry-run

결과: **참조 이관·데이터 병합·별칭 tombstone의 무저장 dry-run과 기존 링크 복원 PASS / 운영 데이터 적용후보 0그룹으로 실제 병합은 NO-GO**

### 운영 데이터 최종 dry-run

- 앞 단계에서 참조 기준 추가 차단사유가 없던 62그룹을 공개 상품값과 비공개 원가까지 다시 대조했다.
- 엄격한 최종 결과는 **적용후보 0그룹, 생성 가능한 작업 0건**이다. 차단된 그룹에는 가상의 patch 경로도 내보내지 않아 TSV를 실행목록으로 오인할 수 없게 했다.
- 비어 있지 않은 상품값 충돌은 81그룹이다. 주요 충돌은 연식 69, 연료 55, 배기량 47, 트림 27, 카탈로그 26, 파워트레인 25, 세부모델 13, 가격 8, 차량상태 7그룹이다.
- 레거시 상품 account_number가 파트너 bank_account와 일치하지 않는 그룹은 **124개**, 동일한 중복값으로 확인된 그룹은 0개다. 값은 보고서·로그에 출력하지 않았다. 이 필드는 상품 스키마에 없고 민감정보 감사대상이므로 임의 복사·폐기하지 않는다.
- 데이터충돌과 계좌 불일치, 공급사 소유권, 대표키 불명확, 차번전용 참조는 서로 겹칠 수 있다.

### dry-run 규칙

1. 대표키의 빈 필드는 명시한 상품 allowlist 안에서만 채움 후보로 만든다. 미분류 필드는 자동 복사하지 않고 차단한다.
2. 레거시 공개 노드의 VIN·차량원가·수수료는 먼저 공개/비공개로 분리해 값이 TSV에 노출되거나 공개 상품으로 복사되지 않게 한다.
3. 계약은 snapshot을 유지하고 정확히 예전 상품키인 참조 필드만 v4 overlay 후보로 낸다. 채팅방은 방 ID와 messages 경로를 유지한다.
4. 중복 상품 제거 후보에는 _merged_into를 함께 기록하도록 계획한다. 예전 상품 URL·로컬 찜 코드는 상품 단건 조회가 이 별칭을 따라 대표 상품으로 복원하며, 순환 별칭은 fail-closed다.
5. 별칭 tombstone과 비공개 노드 삭제는 파괴적 작업으로 표시하고 Claude 게이트를 요구한다. 현재는 모든 그룹이 차단되어 해당 작업을 0건 생성했다.

### 구현·검증

- lib/domain/product-duplicate-dry-run.ts에 단계별 무저장 patch 계획과 값 없는 TSV를 추가했다.
- lib/domain/product-alias.ts와 rtdb-adapter 상품 단건 조회에 _merged_into 복원을 추가했다.
- 기존 관리자 GET /api/inventory/duplicate-plan 응답에 dryRunTsv와 적용후보·충돌·계좌 판정을 추가하고, 재고 화면에 중복 patch dry-run 버튼을 추가했다.
- scripts/sim-product-duplicate-migration.mts: **25/25 PASS**.
- scripts/sim-sheet-merge.mts: **123/123 PASS**.
- type, fonts, tokens, production build: PASS.
- 운영 Firebase write·삭제·병합 0건, 배포·커밋 0건이다.

### 다음 해제 순서

1. 상품 account_number의 실제 의미와 정본 위치를 확인하고 파트너 계좌 또는 별도 보안 노드로 이관할지, 레거시 잔재로 폐기할지 사람·Claude가 결정한다.
2. 연식·연료·배기량·트림·카탈로그·가격·상태 충돌 81그룹을 최신 공급사 Sheet와 차량마스터 기준으로 건별 확정한다.
3. dry-run 적용후보가 생겨도 계약·채팅·비공개 원가 참조, 별칭 URL, 사후조회까지 preview에서 검증한 뒤 별도 승인한다.

## 2026-08-03 중복 재고 대표키·참조 이관계획

결과: **계약·채팅방·견적·비공개 원가 전수 참조 감사와 관리자용 TSV 계획 PASS / 62그룹도 자동삭제가 아니라 대표키 검토 후 이관 후보이며 실제 정리는 NO-GO**

### 운영 데이터 읽기 전용 결과

- 동일 공급사 안의 중복 하위그룹은 **148그룹·300레코드**다. 앞선 84그룹은 차번 전체가 한 공급사인 경우만 센 수치이고, 이번 148그룹은 공급사 간 소유 충돌 차번 안에 있는 동일 공급사 중복 하위그룹도 빠짐없이 포함해 더 크다.
- 대표키 후보는 **114그룹**이다. 공급사_차번 표준키 109그룹, 정확 참조가 유일하게 가장 많은 기존키 5그룹이다.
- 공급사 간 소유권, 대표키 불명확, 차번전용 참조, 다중 계약보호가 없는 그룹은 **62그룹**이다. 이는 삭제 승인 수가 아니라 참조 이관계획을 검토할 수 있는 후보 수다.
- 정확 상품키 참조는 계약 4건, 채팅방 34건, 견적 0건이며 비공개 원가 레코드는 106건이다.
- 상품키 없이 차번만 있는 참조는 **13그룹·17건**이다. 어느 중복키의 관계인지 자동 판단할 수 없으므로 수동 확인 대상으로 차단했다.
- 대표키 자동 후보가 없는 그룹 34개, 둘 이상의 상품키가 진행계약으로 보호된 그룹 1개, 공급사 소유권 충돌이 걸린 동일 공급사 하위그룹 63개다. 각 차단 사유는 서로 겹칠 수 있다.

### 선정·보호 규칙

1. 진행 계약 또는 locked_by_contract가 정확히 가리키는 상품키가 하나면 그 키를 최우선 보존 후보로 한다.
2. 계약보호 키가 없으면 공급사_차번 표준키를 후보로 한다.
3. 표준키도 없을 때만 계약·채팅·견적·비공개 원가의 정확 참조가 유일하게 많은 키를 후보로 한다.
4. 둘 이상의 키가 진행계약으로 보호되거나 공급사 간 소유 충돌이 있거나 차번전용 참조가 있으면 실행 금지다.
5. 계약·채팅방·견적·비공개 원가 중 하나라도 스캔하지 못하면 fail-closed로 실행 금지다.

### 구현

- lib/domain/product-duplicate-migration.ts에 대표키 후보, 후보 근거, 그룹 차단사유, 상품별 계약·방·견적·비공개 원가 참조, 권장조치를 만드는 순수 계획 로직과 TSV 출력을 추가했다.
- lib/server/product-duplicate-audit.ts는 Firebase v3+v4를 읽기 전용으로 합쳐 위 네 참조 표면을 전수 확인한다. VIN·금액 같은 비공개 값은 반환하지 않고 비공개 레코드 존재 여부만 보고한다.
- 관리자 Bearer 인증 전용 GET /api/inventory/duplicate-plan과 재고의 중복 이관계획 TSV 버튼을 추가했다. endpoint에는 update/set/delete 경로가 없다.
- 자동연동 상태 새로고침이 dailyStatusLoading 대신 공용 busy를 잘못 해제하던 UI 상태 결함도 바로잡았다.

### 검증

- scripts/sim-product-duplicate-migration.mts: **13/13 PASS**.
- scripts/sim-sheet-merge.mts: **123/123 PASS**.
- npx.cmd tsc --noEmit, fonts, tokens: PASS.
- production build: GET /api/inventory/duplicate-plan 포함 전체 route PASS.
- 운영 Firebase 감사와 로컬 검증 모두 write·삭제·병합 0건이다. 배포·커밋도 수행하지 않았다.

## 2026-08-03 재고 중복·공급사 귀속 충돌 전수 분류

결과: **실데이터 읽기 전용 전수분류와 상세 충돌 보고서 구현 PASS / 참조 이관·중복 정리·공급사 소유권 확정은 사람·Claude 승인 전 NO-GO**

### 실데이터 판정

- 현재 Firebase의 활성 재고 **1,095대**를 계약·공급사와 함께 읽어 차번 충돌을 전수 대조했다. 실제 데이터 write, 삭제, 병합은 수행하지 않았다.
- 활성 중복 차번은 **129개 그룹·326개 레코드**다. 이 중 같은 공급사 안에서 대표키를 정한 뒤 정리할 수 있는 후보가 **84개 그룹**, 실제 소유 공급사를 정해야 하는 충돌이 **43개 그룹**, 계약 보호 때문에 자동수정하면 안 되는 충돌이 **2개 그룹**이다.
- 상세 레코드 기준 분류는 서로 겹칠 수 있으며 대표키·참조 이관 후 중복 정리 246행, 계약보호·자동수정 금지 8행, 실소유 공급사 확인 139행이다.
- 2026-07-31 Sheet 캐시와 현재 Firebase를 합친 전체 동기화 preflight는 활성 중복 124그룹, 공급사 간 충돌 39그룹, 삭제 재등장 1건, 임시번호 신원변경 2건, 신원서명 충돌 4건, 가격기간 누락 6건을 재현했다.
- 공급사 원본 조회 게이트도 남아 있다. RP004는 캐시 없음, RP016은 1행 전부 제외, RP023은 107행 중 13행 제외·94행 가격 없음으로 가져올 정상 행이 0이었다.

### 이번 수정

- lib/domain/sheet-conflict-report.ts에 충돌 유형, 판단, 권장조치, 차번, 공급사, 상품키, 차량상태, 출처, 계약보호 여부, 원본 충돌을 한 행씩 만드는 순수 보고서 로직을 추가했다.
- 관리자 Sheet 검증 화면의 단순 2열 충돌 복사를 **상세 충돌 TSV 복사**로 교체했다. 계약 목록도 읽어 locked_by_contract 또는 진행 중 계약이 있는 재고는 계약보호 · 자동수정 금지로 표시한다.
- 보고서는 자동삭제 명령이 아니다. 대표 상품키를 선택하고 계약·문의·정산 등 참조를 먼저 이관한 뒤, 별도 승인된 레코드만 정리해야 한다.

### 처리 순서

1. 계약보호 8행을 먼저 사람·Claude가 대조해 유지할 대표키와 계약 참조를 확정한다.
2. 같은 공급사 84그룹은 대표키를 정하고 모든 참조 이관 검증 후에만 중복 레코드 정리를 승인한다.
3. 공급사 소유권 충돌 43그룹은 원본 Sheet·실차 소유 증빙으로 한 공급사를 확정한다.
4. 삭제 재등장·임시번호·신원서명·가격기간과 RP004/RP016/RP023 원본 문제를 해결한다.
5. 읽기 전용 preflight를 다시 실행해 충돌 0, 연속 2회 중 두 번째 diff 0을 확인한 뒤 일일 동기화 활성화를 검토한다.

### 자동 검증

- scripts/sim-sheet-merge.mts: **123/123 PASS** — 중복 레코드별 상세행, 계약보호 자동수정 금지, TSV 판단·상품키 열 포함.
- scripts/sim-sheet-daily-sync.mts: **18/18 PASS**.
- scripts/sim-inventory-display.mts: **27/27 PASS**.
- fonts, tokens, production build: PASS.

## 2026-08-03 매일 Google Sheet → 자체 재고 동기화

결과: **일일 수집·자체 재고 영속화·변경 병합·내부 수정 우선 로직과 production build PASS / 운영 자동 실행은 충돌 정리와 사람·Claude 게이트 전까지 비활성 NO-GO**

### 확정한 동작

- Vercel Cron이 매일 **02:00 KST**(`0 17 * * *`, UTC)에 `GET /api/sheet/sync-daily`를 호출한다.
- Cron API는 `Authorization: Bearer ${CRON_SECRET}`과 `SHEET_DAILY_SYNC_ENABLED=true`가 모두 있어야 실행된다. 기본 예시는 `false`이며 인증 없는 요청은 401, 비활성 요청은 503으로 차단된다.
- 서버가 공급사 설정과 Google Sheet를 새로 읽고 전체 공급사가 정상일 때만 계획한다. 조회 실패, 0행/급감, 중복·무효 차번, 공급사 소유 충돌, 삭제이력 재등장, 임시번호 신원 변경, 가격기간 누락은 전체 저장을 중단한다.
- 신규 행은 v3가 아닌 **`v4/products` 자체 재고**로 생성한다. 이후 일반 재고 화면에서 조회·수정할 수 있다.
- 기존 행은 같은 상품키 또는 같은 공급사+실차번으로 찾아 비어 있지 않은 변경분만 병합한다. 시트 빈칸은 내부 값을 지우지 않는다.
- 재고 화면에서 사람이 고친 시트 유입 상품 필드는 `_sheet_manual_fields`에 기록한다. 이후 매일 연동은 해당 필드의 내부 값을 우선하고, 사람이 건드리지 않은 필드와 신규 필드만 계속 갱신한다.
- 시트에서 빠진 차량은 삭제하지 않는다. 시트 소유 provenance를 남긴 `출고불가`로 전환하고, 계약락·수기 보류는 건드리지 않는다. 시트 자동차단 차량이 다시 나타나면 자동 복원한다.
- 같은 시트를 다시 실행해도 신규·수정·부재 patch가 생기지 않는 멱등 계획이다.

### 저장 안전장치

- 저장 직전에 활성·삭제 재고를 다시 읽어 계획한다.
- 모든 상품 patch/create는 **`v4/products` 단일 RTDB transaction**에서 기존값 CAS와 신규키 중복을 먼저 전부 검증한다. 한 건이라도 달라졌으면 부분 재고 저장 없이 전체를 취소한다.
- 성공 후 공급사 checkpoint와 요약 감사로그를 `v4` multi-location update로 기록하고, 결과를 다시 읽어 남은 create/patch가 0인지 사후검증한다.
- 동시 실행은 `v4/system_locks/sheet_daily_sync`의 20분 lease로 막고 실행 결과는 `v4/sheet_sync_runs`에 기록한다.
- Firebase Admin 런타임 의존성을 production dependency로 옮겼다. Admin은 Rules를 우회하므로 endpoint 비밀키·feature flag가 보안 경계이며, 서비스 계정 JSON은 로그에 남기지 않는다.
- `database.rules.json`과 v3 운영 노드는 수정·게시하지 않았다. 실제 Sheet/ERP 데이터 write도 수행하지 않았다.

### 운영 설정

- `CRON_SECRET`: 충분히 긴 무작위 값. Vercel Cron 요청 인증에 사용.
- `FIREBASE_SERVICE_ACCOUNT_JSON`: `project_id`, `client_email`, `private_key`를 포함한 서버 전용 JSON.
- `SHEET_SYNC_COMPANY_ID=freepass`
- `SHEET_DAILY_SYNC_ENABLED=false`: 아래 실데이터 게이트가 끝난 뒤에만 `true`로 변경.
- `?dry_run=1`은 상품 재고를 바꾸지 않지만 lease와 실행 결과 로그는 기록한다.

### 관리자 운영 가시성

- 배포본 `/inventory`를 브라우저로 다시 확인했다. 현재 화면은 여전히 `관리자가 버튼으로 실행(자동 아님)`이라고 표시하고 공급사별 과거 연동시각만 보여, 자동 실행의 전체 성공·차단·실패를 판별할 수 없었다.
- 로컬 후보에는 관리자 전용 `GET /api/sheet/sync-status`를 추가했다. Firebase ID token을 서버에서 검증하고 `users/{uid}.role=admin`이면서 활성 계정일 때만 상태를 반환한다.
- 일일 실행은 `v4/system_status/sheet_daily_sync`에 `running/completed/dry_run/blocked/failed`, 최근 시각, 건수, 차단 사유를 기록한다. 관리자 재고 화면은 이를 `자동연동 정상/실행 중/시험 완료/차단/실패/비활성`으로 표시한다.
- 상태 API 무인증 요청은 403, 실행 API 무인증 요청은 401을 로컬 production server에서 확인했다. 상태 조회는 운영 데이터를 변경하지 않는다.

### 자동 검증

- `scripts/sim-sheet-daily-sync.mts`: **18/18 PASS** — 신규 자체재고, 기존 patch, 빈값 보존, 부재 soft-block, 수기 상태 보존, 내부 수정 필드 우선, 재등장 복원, 계약락, 조회실패·중복 hard block, 멱등성, Cron·인증·런타임 wiring.
- `scripts/sim-sheet-merge.mts`: **123/123 PASS**.
- `scripts/sim-sheet-price.mts`: **29/29 PASS**.
- `scripts/sim-inventory-display.mts`: **27/27 PASS**.
- `scripts/sim-work-list-semantics.mts`: **142/142 PASS**.
- `npx.cmd tsc --noEmit`, fonts, tokens: PASS.
- production build: `/api/sheet/sync-daily`, `/api/sheet/sync-status` 포함 전체 route PASS.
- 로컬 production server에서 실행 API 무인증 **401**, 상태 API 무인증 **403**, 올바른 테스트 Cron Bearer + 비활성 flag **503**를 확인했다.

### 운영 활성화 전 NO-GO

1. 2026-07-31 Sheet 캐시+현재 Firebase 읽기 전용 preflight는 공급사 조회/설정 3곳과 활성 중복차번 124건, 공급사 간 차번충돌 39건, 삭제충돌 1건, 번호미정 drift 2건, 신원서명 충돌 4건, 가격기간 누락 6건 때문에 전체 저장을 차단했다.
2. preview 환경에서 `SHEET_DAILY_SYNC_ENABLED=false`로 배포 후 `dry_run=1` 결과와 공급사별 diff를 사람·Claude가 승인해야 한다.
3. 위 충돌을 정리하고 연속 2회 dry-run에서 두 번째 create/update/absent가 0인 것을 확인한 뒤에만 운영 flag를 켠다.
4. `check:release`의 보안·약관 차단 13개와 경고 2개는 별도 최종 출시 NO-GO로 그대로 남아 있다.

## 2026-08-03 Claude 후속 검토 반영·레거시 시트 자동차단 수정

결과: **코드로 해결 가능한 상태 오판은 수정·회귀검증 PASS / Rules와 배포본 대량 동기화 위험 때문에 최종 출시는 NO-GO**

### Claude 잔여 검토 대조

- `CLAUDE_REVIEW_2026-08-03.md`와 Claude의 읽기 전용 probe를 원 요구사항·운영 데이터 기준으로 다시 대조했다.
- Claude가 지적한 `출고불가` 17대 중 계약락 1대를 제외한 16대는 현재 코드에서 수기 보류로 분류됐다. 그중 14대는 `status_label=시트에서 제거됨`, `source=external_sheet`인 과거 시트 부재 자동차단이고, 2대는 `일괄 출고불가`였다.
- `일괄 출고불가`는 운영자가 의도적으로 묶어서 막았을 가능성을 배제할 수 없으므로 자동 해제하지 않는다. 실데이터 backfill도 승인 없이 수행하지 않았다.
- 계약 차량 snapshot 10종의 불변 Rules 누락과 공개서명 익명 읽기 범위 문제는 실제 출시 차단이다. 작업원칙상 `database.rules.json`은 수정·게시하지 않았고 사람/Claude의 레거시 실데이터 게이트로 넘긴다.

### 이번 수정

- `lib/domain/sheet-merge.ts`에 시트 자동차단·수기 보류 판정을 공통 함수로 만들었다.
- 현재 provenance(`sheet_status_owner=sheet`, `sheet_block_reason=missing_or_excluded`)뿐 아니라 **정확히** `출고불가 + 시트에서 제거됨 + source=external_sheet|sheet`인 행만 레거시 자동차단으로 인정한다.
- 위 레거시 행이 시트에 정상 재등장하면 상태를 복원하고 낡은 `status_label`을 제거한다. 출처 없는 같은 라벨, `일괄 출고불가`, 계약락·레거시 계약중은 계속 보존한다.
- `lib/domain/sheet-sync-all.ts`의 수기 해제 후보·수기 보류 보고도 같은 SSOT 판정을 사용해 화면과 실제 병합 결과가 어긋나지 않게 했다.
- 사람이 재고 화면에서 상태를 변경하면 현재 provenance와 레거시 자동차단 라벨을 함께 끝내 이후 수기 보류가 자동 해제로 오인되지 않게 했다.

### 자동 게이트

- `scripts/sim-sheet-merge.mts`: **120/120 PASS** — 레거시 시트 제거 복원, 낡은 라벨 제거, 일괄 차단 보존, 출처 위조 방어, 충돌 보고 일치 포함.
- `npx.cmd tsc --noEmit`, `npm.cmd run check:fonts`, `npm.cmd run check:tokens`: PASS.
- `scripts/sim-work-list-semantics.mts`: **142/142 PASS**.
- `scripts/sim-sheet-price.mts`: **29/29 PASS**.
- 로컬 `sim-*.mts` 25종 중 기능·표시·정산·채팅·시트 등 **22종 PASS**. 아래 Rules 전용 3종은 의도한 출시 차단을 재현해 FAIL했다.
  - `sim-contract-rules.mts`: 계약 차량 snapshot 생성 후 변경 허용.
  - `sim-contract-sign-rules.mts`: 제출 후 `pending_review`의 익명 재읽기 허용.
  - `sim-release-security-rules.mts`: **0/13 PASS** — v3 광역 read/write, v4 소유권 결속, 계약 PII·서명·취소·정산 금액 규칙 누락.
- `npm.cmd run check:release`: **FAIL — 차단 13개, 경고 2개**. 법정 운영자 정보 6종도 미기재 상태다.
- `npm.cmd run build`: production build 및 **30개 route PASS**.

### 출시 후보 브라우저 실검증

- Chrome 로그인 세션으로 `https://freepasserp4.vercel.app/inventory`를 열어 재고 350대와 공급사 시트 16개를 확인했다.
- 오전 09:43:03 읽기 전용 `데이터 검증` 결과는 원문 439행 → 올림 406대, 출고불가 제외 33, 가격없음 9, 신규 104, 상태변경 68, 내용수정 234, 부재→출고불가 15다.
- 오토플러스는 본 95+프로모 12, 재고 96, 올림 107, 가격없음 7로 표시됐다.
- 배포본은 `동기화 (16) · 406대`를 활성 상태로 표시했다. 그러나 위 변경량이 크고 이번 레거시 자동차단 수정은 로컬 dirty 변경이므로 **현재 배포본에서 동기화를 실행하면 안 된다.** 동기화·저장·운영 설정 write는 0건이다.

### 최종 해제 조건

1. Rules 후보에서 계약 snapshot 불변, 공개서명 제출 후 익명 read 차단, v3/v4 권한·PII·정산 결속을 구현하고 레거시 실데이터로 사람/Claude 게이트를 통과한다.
2. 이번 로컬 변경을 preview에 반영한 뒤 같은 16개 시트 preflight를 다시 실행한다.
3. `시트에서 제거됨` 14대만 자동 복원 후보가 되고 `일괄 출고불가` 2대와 계약락은 유지되는지 건별 diff로 확인한다.
4. 신규 104·상태 68·내용 234·부재 15를 공급사별로 승인하기 전까지 운영 동기화는 금지한다.

## 2026-08-03 Claude Code 위험영역 독립 게이트

결과: **APPROVE WITH CONDITIONS / 최종 출시는 NO-GO**

### 실행·범위

- 로컬 Claude Code `2.1.220`에 파일 수정·테스트·commit·push·deploy·실데이터 write를 금지하고 read-only 검토를 요청했다.
- 첫 Opus 검토는 dirty diff 전체 탐색 중 244초 타임아웃되어 판정으로 사용하지 않았다. 두 번째 Sonnet high 검토는 금액 판정, AutoPlus import, Sheet commit gate, 상품 CAS, RTDB v3/v4 경계, 관련 시뮬레이션과 최신 검증 문서로 범위를 제한해 정상 완료했다.
- Claude가 수정한 파일과 운영 write는 0건이다.

### Claude 승인 사항

- `rent_multiple`이 RP023 운영 이력과 맞고, `months_per_year`은 맞지 않는다는 근거를 승인했다.
- 원문 모델의 명시 브랜드, 차종마스터 origin 합의, 동명 국산·수입 fail-closed가 결정적이며 배열 순서에 의존하지 않는다고 판정했다.
- `sim-sheet-price`의 29개 항목이 위 실제 코드 경로를 실행하고, 미확정 origin에서 금액을 만들지 않는다고 확인했다.
- 정산엔진 계약락과 시트 CAS가 동일한 `v4/products/{key}` 경로를 사용하므로 transaction 재시도에서 늦은 계약락을 감지한다는 주장을 코드로 확인했다.
- v3 write가 없고 `database.rules.json`을 건드리지 않았으므로 **현재 로컬 rtdb-adapter 변경의 머지는 조건부 승인**했다. Rules 게시 승인은 이번 범위가 아니며 별도 사람/Claude 게이트 대상이다.

### Claude 발견 조건부 결함

- `lib/firebase/rtdb-adapter.ts`의 `bulkPatchGuardedProduct`는 public 상품과 private 상품을 별도 transaction으로 순차 저장한다. 향후 patch에 `vin`, `vehicle_price`, `price.*.fee/commission/fee_memo`가 포함되면 public 성공 후 private CAS 실패 시 public만 부분 반영될 수 있다.
- 현재 Sheet import mapping에는 위 private 필드가 없으므로 이번 시트 병합 경로에서는 `privateRecord === null`이고 재현 불가능하다. `vehicle_status`와 `locked_by_contract`도 public 경로이므로 현재 계약락 보호는 영향받지 않는다.
- 따라서 이번 로컬 머지를 막지는 않되, **이 guarded API를 private 필드 writer에 재사용하거나 Sheet mapping에 private 필드를 추가하기 전에** 단일 원자 경계 또는 명시적 부분커밋/보상 설계를 먼저 구현해야 한다. 이 조건을 지키지 않은 확장은 NO-GO다.

### Claude가 재확인한 운영 조건

1. `RP023.deposit_rule = rent_multiple` 저장과 BMW 3대 ×2→×3 금액 변경은 운영자 승인 후 수행한다.
2. 원본 대여료 공란 7대는 원본 수정 전까지 제외한다.
3. 기존 데이터 충돌 29/39/52/8/1/4/7과 `database.rules.json` 공개범위 문제는 별도 해소·게이트가 필요하다.

## 2026-08-03 AutoPlus 보증금 규칙·Google Sheet 실데이터 대조

결과: **규칙과 제조국 판정 결함은 로컬 수정·검증 PASS / 운영 설정 미저장과 기존 충돌 때문에 최종 출시는 계속 NO-GO**

### 규칙 판정 근거

- 운영 Google Sheet `RP023`을 쓰기 없이 `rent_multiple(국산×2·수입×3)`과 `months_per_year(기간/12 배수)`로 각각 파싱하고, 2026-07-28에 가격 108/108로 검증된 `data/sheet-ingress/RP023-autoplus.json`과 차량번호·기간별로 대조했다.
- 과거와 현재의 월대여료가 같은 295개 셀 중 `rent_multiple`은 보증금 283개가 동일했다. 나머지 12개는 제조사 칸이 비었던 실제 BMW 3대×4기간이 과거에 국산×2로 잘못 분류된 흔적이며, 현재 원문 `BMW 120i/220i`에 따라 수입×3으로 교정된다.
- `months_per_year`은 같은 월대여료 295개 중 보증금 일치가 74개뿐이었다. BMW X1도 과거 `101만원→303만원`인데 이 후보는 12개월 `101만원`, 24개월 `184만원`, 48개월 `324만원`이 되어 운영 이력과 맞지 않는다.
- 따라서 RP023의 입증된 규칙은 **국산 월대여료×2 / 수입 월대여료×3**이다. 임의 기본값으로 박지 않고 여전히 `partner.deposit_rule` 명시 설정을 요구한다.

### 파서·화면 수정

- AutoPlus처럼 제조사 칸 없이 모델 칸에 `BMW X1`을 적는 행은 스냅된 저신뢰 제조사를 쓰지 않고 **원문 maker/model/sub_model의 명시 브랜드**로 제조국을 판정한다.
- 원문에 제조사명이 없는 `K5 HEV`형 행은 스냅된 정식 모델과 정확히 같은 차종마스터 후보 전체가 한 제조국으로 합의할 때만 배율을 허용한다. 동명 모델이 국산·수입에 함께 있으면 마스터 배열 순서와 무관하게 계속 fail-closed한다.
- 관리자 일괄 검증 메시지에 `보증금 규칙 미설정(국산 2·수입 3개월치 설정 필요)`를 표시한다.
- `/members`에서 레거시 RP023처럼 `adapter_id`가 비어도 실제 적용값을 `오토플러스식 · 자동`으로 표시하고, 보증금 규칙 미설정 시 동기화가 차단된다는 설명과 정확한 선택지를 보여준다.
- 재현 가능한 읽기 전용 점검 도구 `scripts/audit-autoplus-deposit.mts`를 추가했다. Sheet/Firebase/로컬 데이터 write는 없다.

### 최신 실측·자동 게이트

- 현재 Sheet + 수정 파서: **96대 정상 파싱**, 제조국 미판별 **0대**, 원본 월대여료가 실제로 빈 7대만 가격없음 제외, 출고불가 14행 제외, 내부 차번 충돌 0.
- 보증금 셀 382개는 국산×2 338개, 수입×3 44개다.
- `scripts/sim-sheet-price.mts`: **29/29 PASS** — 원문 모델 브랜드, 정식 모델 제조국 합의, 동명 국산·수입 fail-closed 포함.
- `scripts/sim-sheet-merge.mts`: **116/116 PASS**.
- `npx.cmd tsc --noEmit`, `npm.cmd run check:fonts`, `npm.cmd run check:tokens`, `git diff --check`: PASS.
- `NEXT_DIST_DIR=.next-codex-autoplus npm.cmd run build`: **30개 route PASS**. Next가 추가한 임시 `tsconfig` 변경은 빌드 후 원복했다.
- Chrome 관리자 `/inventory`에서 실제 `데이터 검증`을 다시 실행했다. 운영 RP023 설정은 여전히 공란이므로 화면은 `0매물 · 가격없음 103 · 보증금 규칙 미설정… · 올림0 안전차단`을 정확히 표시하고 동기화 버튼을 비활성화했다.
- Chrome `/members`의 RP023 상세·수정 화면에서 `오토플러스식 · 자동`, 현재 규칙 `미설정`, `국산 2개월치 · 수입 3개월치 · 오토플러스` 선택지를 확인했다. 저장·동기화는 누르지 않았고 실제 ERP/Sheet write는 **0건**이다.

### 잔여 승인·출시 판정

1. 운영자가 `RP023.deposit_rule = rent_multiple`을 승인·저장한 뒤 16개 공급사를 다시 읽기 전용 검증해야 한다. 이 설정은 금액 정책 변경이므로 Codex가 임의로 운영 데이터에 쓰지 않았다.
2. 제조사 공란 때문에 과거 ×2였던 BMW `133라1401`, `192머7372`, `321라9324`가 수입 ×3으로 바뀌는 것을 운영자가 금액 샘플로 확인해야 한다.
3. 원본 대여료가 빈 7대(`35서4814`, `11오1597`, `11오1623`, `11오0632`, `05수5200`, `05수5243`, `35서5719`)는 원본 수정 전까지 정상 제외한다.
4. 기존 운영 데이터 충돌과 보안/Rules·검수정책 NO-GO는 아래 이전 절의 판정이 그대로 유효하다. `database.rules.json`과 운영 데이터는 변경·게시하지 않았다.

## 2026-08-03 Google Sheets 저장 경합 CAS 보강 검수

결과: **v4 계약 잠금·수기변경과 시트 patch의 상품별 CAS는 PASS / 실데이터 동기화와 최종 출시는 계속 NO-GO**

### 이번 수정

- 시트 soft-merge와 부재→출고불가가 일반 `bulkPatch`를 쓰지 않고 `bulkPatchGuardedProduct`를 거치도록 바꿨다. 계획마다 strict-fresh로 읽은 기존 ERP 레코드를 `expected`로 보존한다.
- RTDB는 `v4/products/{product_code}`에서 `runTransaction`을 실행한다. 검증 직후 계약 엔진이 같은 상품의 `vehicle_status`, `locked_by_contract`, `updatedAt`을 바꾸면 transaction이 최신 값으로 재시도되고 expected 불일치로 저장을 중단한다.
- 시트가 실제로 수정하는 필드 외에도 삭제표식·상태·계약락·revision을 항상 비교한다. 가격·VIN·원가처럼 private 오버레이에 속한 변경도 해당 private 필드가 검증 뒤 바뀌면 중단한다.
- 기존 상품 CAS를 신규 생성보다 먼저 실행하고 첫 충돌에서 멈춰, 충돌 전에 신규행만 먼저 저장되는 부분 커밋 범위를 줄였다.
- LocalAdapter와 FirestoreAdapter도 같은 조건부 쓰기 계약을 구현해 백엔드별 의미가 갈리지 않게 했다. v3 운영 노드는 쓰지 않았고 `database.rules.json`도 변경·게시하지 않았다.

### 경쟁 조건 판정

- **해결:** 이 앱의 v4 계약 잠금/수기 상품 write가 strict-fresh 읽기와 시트 저장 사이에 끼어드는 경우, 상품 경로 transaction이 재시도하면서 CAS를 실패시킨다. 뒤늦은 시트 `출고가능` patch가 계약중 차량을 덮는 경로를 차단한다.
- **잔여 한계:** 여러 상품과 partner checkpoint를 하나의 원자 커밋으로 묶지는 못한다. 중간 네트워크 실패는 기존 사후검증·재검증 절차로 복구한다.
- **잔여 한계:** 외부 레거시 앱이 v3 원본을 transaction과 동시에 직접 쓰는 경우에는 v4 경로 CAS가 v3 write와 같은 원자 경계를 공유하지 못한다. v3 writer가 계속 운영된다면 cut-over 또는 서버측 통합 writer 결정이 필요하다.
- `rtdb-adapter`는 위험영역이므로 이 로컬 변경은 게시·머지 전에 사람 또는 Claude 게이트를 통과해야 한다.

### 자동·브라우저 재검증

- `npm.cmd run typecheck`: PASS
- `scripts/sim-sheet-merge.mts`: **116/116 PASS** — 동일 snapshot 허용, 검증 뒤 계약락/updatedAt 변경 차단, RTDB 오버레이 fallback/transaction 재시도와 실제 저장경로 연결 포함
- `scripts/sim-vehicle-lock.mts`: **23/23 PASS**
- `scripts/sim-store-cache-generation.mts`: **12/12 PASS**
- `scripts/sim-sheet-price.mts`: **27/27 PASS**
- `scripts/sim-sheet-diff.mts`, `npm.cmd run check:fonts`: PASS
- `NEXT_DIST_DIR=.next-codex-sheet-cas npm.cmd run build`: **30개 route PASS**. Next가 추가한 임시 tsconfig include는 빌드 후 원복했다.
- Chrome 관리자 `/inventory`에서 설정 16곳을 다시 읽고 읽기 전용 `데이터 검증`을 재실행했다. 오전 01:10:11 기준 445행→292대, 제외 48, 가격없음 105, 신규 62, 상태변경 62, 내용수정 168, 재고차단 11로 이전 판독과 일치했다.
- 화면은 조회 실패 1곳과 기존 충돌 29/39/52/8/1/4/7을 동시에 표시했고 `동기화 (16) · 292대` 버튼의 실제 `disabled` 속성과 `조회 실패 공급사 1곳 (오토플러스 주식회사)` title을 확인했다. 실제 ERP·시트 write는 0건이다.

### 최신 출시 판정

- CAS 보강으로 **v4 계약락을 시트가 뒤늦게 덮는 단일 상품 경쟁 경로는 해소**했다.
- 그러나 오토플러스 가격규칙, 운영 데이터 충돌 29/39/52/8/1/4/7, 검수필요 매물 정책, 시트 URL/Rules 보안, 운영 충돌해결 workflow가 남아 있어 최종 판정은 **NO-GO**다.

## 2026-08-02 Google Sheets 재고 연동·기존 ERP 병합 최종 검수

결과: **시트 파싱·병합 방어와 production build는 PASS / 운영 동기화 실행과 최종 출시는 NO-GO**

### 운영 로그인 브라우저 실검증

- Chrome 관리자 `박영협` 세션의 `/inventory`에서 등록 공급사 16곳을 직접 읽기 전용 검증했다. 실제 `동기화`는 실행하지 않았고 ERP·시트 실데이터 write는 0건이다.
- 원문 445행 → 올림 후보 292대 · 출고불가 제외 48대 · 가격없음 105대다. 마스터 확정은 264대, `_needs_master_review` 검수 필요는 28대다.
- 기존 ERP 예상 반영은 신규 62 · 상태변경 62 · 내용만 수정 168 · 재고차단 11 · 가드보류 0 · 무변경 0이다.
- 오토플러스 `RP023`은 117행 중 출고불가 14 · 가격없음 103 · 올림 0으로 안전 차단됐다. 운영 `deposit_rule`이 공란이므로 과거 추정 공식을 임의 사용하지 않는다.
- 최종 화면은 `동기화 (16) · 292대`를 비활성화하고 아래 사유를 동시에 표시한다.
  - 활성 중복차번 29건
  - 공급사 간 차번 소유 충돌 39건 (`RP005 ↔ RP018` 등)
  - 삭제매물 재등장 52건 + 공급사 미확정 삭제이력 8건
  - `RP020` 번호미정 식별변경 1묶음: 기존 `100신0011/13/15/12/16/14` ↔ 신규 `100신0001~0006`
  - `RP020` 임시번호 신원서명 불일치 4건: `100신0007~0010`
  - 기존 가격기간 누락 7건

### 기존 ERP와의 병합 규칙

- 1차는 기존 `product_code/_key`, 2차는 **같은 공급사+실차번**으로 찾는다. 구키 `차번_공급사`와 신키 `공급사_차번`이 달라도 같은 차량이면 신규 중복 생성하지 않고 기존 키를 유지한다.
- 시트의 빈 값은 기존 수기 값을 지우지 않는다. 유효한 비어 있지 않은 값만 보완하고 가격은 이번에 읽힌 기간만 갱신한다.
- 기존 가격기간이 원문에서 사라지면 출처 표식과 관계없이 자동 삭제하거나 낡은 값을 조용히 승인하지 않고 hard block한다.
- `locked_by_contract` 또는 레거시 `계약중` 상태는 시트가 해제하지 못한다. 출처 없는 수기·레거시 `출고불가`도 유지한다.
- 시트 부재처리가 만든 `출고불가`만 같은 차량 재등장 때 복원한다. 수기 보류는 자동 복원하지 않는다.
- 타 공급사 동일 차번, 활성 twin, 삭제이력 재등장, 귀속 미확정 레거시, 임시번호 신원 변경·실차번 전환, 중복·무효 차번, 가격기간 누락은 운영자가 정리할 때까지 전체 저장을 차단한다.
- 레거시 `100신…`에 `is_pending_plate`가 없어도 임시번호로 판정한다. 최초 `_pending_signature`는 덮지 않으며 불일치는 수동 연결 대상으로 차단한다.
- 커밋 경계에서 공급사 설정과 활성·삭제 재고를 strict fresh로 다시 읽고 roster/reconcile revision을 비교한다. 저장 정본은 `lines[].products`로 재구성해 공급사 소유·source·차번·상품키·전체 내용이 검증 스냅샷과 같을 때만 통과한다.
- 초기 인증 직후 strict roster가 빈 결과를 내도 `설정 다시 읽기`가 남는다. Excel 원문 변경은 이전 preview를 즉시 폐기하고, 헤더 drift는 매핑 재저장→재로드 전까지 저장을 막는다.

### 최종 자동 게이트

- `npm.cmd run typecheck`: PASS
- `scripts/sim-sheet-merge.mts`: **108/108 PASS**
- `scripts/sim-sheet-price.mts`: **27/27 PASS**
- `scripts/sim-sheet-diff.mts`: PASS
- `scripts/sim-store-cache-generation.mts`: **12/12 PASS**
- `npm.cmd run check:fonts`, `npm.cmd run check:tokens`: PASS, 드리프트 0
- `NEXT_DIST_DIR=.next-codex-sheet-final6 npm.cmd run build`: **30개 route PASS**
- `npm.cmd run check:release`: **FAIL — 출시 차단 13개, 경고 2개**
- `scripts/sim-release-security-rules.mts`: **0/13 PASS**
- `scripts/sim-contract-rules.mts`: 차량 snapshot 불변 Rules 누락으로 FAIL
- `scripts/sim-contract-sign-rules.mts`: 제출 후 익명 read 차단 누락으로 FAIL

### 최종 NO-GO 사유와 해제 조건

1. **오토플러스 가격 정책 미확정:** 운영자가 `RP023.deposit_rule`을 명시적으로 선택하고 103대 가격을 재검증해야 한다. `months_per_year`와 `rent_multiple` 중 무엇이 맞는지는 임의 결정하지 않았다.
2. **운영 데이터 충돌:** 위 29/39/52/8/1/4/7 충돌을 차번별로 소유·삭제·연결·가격기간 승인 처리한 뒤 16개 전체를 다시 검증해야 한다.
3. **검수 필요 28대의 판매 정책 없음:** 현재 `_needs_master_review=true`도 저장·목록 노출·계약 생성이 가능하다. `검수 완료 전 비게시/출고협의/계약 차단` 중 운영 규칙 승인이 필요하다.
4. **계약 잠금과 시트 write가 비원자적:** fresh read 이후 save/bulkPatch 사이 계약 엔진이 잠그면 오래된 `출고가능` patch가 뒤늦게 상태를 덮을 수 있다. 전역 sync mutex + 상품 revision CAS/transaction 또는 계약락 포함 원자 conditional write가 필요하다.
5. **시트 URL과 보안 Rules:** `partners`의 광역 인증 read/write 때문에 링크 보유형 Google Sheet URL이 타 공급사·영업자에게 노출될 수 있다. private 설정 노드 분리와 Rules 후보의 실데이터 검증·사람/Claude 게이트가 필요하다.
6. **운영 승인 workflow 미완성:** 임시번호→실차번 수동 연결, 기존 가격기간 제거 승인, 충돌 소유자 확정 화면/절차가 없다.

`database.rules.json`, 정산엔진, `rtdb-adapter`는 이번 시트 검수에서 수정·게시하지 않았다. 규칙·계약락·데이터 이관은 위험영역이므로 실제 레거시 스냅샷 검증 후 별도 게이트를 통과해야 한다.

## 2026-08-02 B2B 출시 전 목록 규격·차량 스냅샷 전수 재검수

결과: **기능·UI 회귀와 production build는 PASS / 최종 출시는 NO-GO**

### 이번에 수정·확정한 규격

- 업무 목록의 공통 구조를 `좌측 lifecycle 아이콘 + T1 차량명 + T2 상태·차번·날짜 + T3 역할별 상대방·업무코드`로 고정했다.
- `문의/상담`처럼 화면명과 중복되는 뱃지는 쓰지 않고, 완료·취소 아이콘이 안읽음 표시에 덮이지 않게 했다.
- 값이 없는 메타 앞의 고아 `·`를 제거했고, 재고 차명이 없을 때 차번을 차명처럼 쓰지 않고 `차량명 미확인`으로 표시한다.
- 계약의 빨간 `계약철회/상태 확인/거부` 행에는 파란 `n/5` 진행률을 함께 표시하지 않는다. 페이지 숫자는 의미가 넓은 `처리 대기`로 표기한다.
- 문의의 취소 이력은 `문의·미확인`에 중복 노출하지 않고 전용 `취소` 필터에만 둔다.

### 차량명·목록/상세 스냅샷

- 차량명 SSOT를 T1 `short`(목록), T2 `full`(상세·계약서·공개서명), T3 `raw`(감사 원문)로 통일했다.
- 실제 레거시 계약 `CT26041401`, `CT26041601`, `CT26041602`를 상품 없이도 계약 저장값만으로 정상 복원했다.
- 실제 메인 차종 결손 Tesla 원본의 표기를 `테슬라 모델Y RWD` / `테슬라 모델Y EV RWD`로 복원하고 부분 중복 제원을 제거했다.
- 신규 일반/계약 문의방에 maker/model/sub_model/variant/trim/extra 구조 snapshot을 저장한다.
- 문의 당시 `모던`과 현재 상품 `스마트`가 충돌하는 실제 경로에서 목록·상세 모두 문의 당시 차량을 유지하고, 현재 가격·사진·정책만 보강한다.
- `room.vehicle_name === car_number`인 레거시 QA 방은 차명을 차번으로 위장하지 않고 한 번만 표시한다.
- 계약서 과거 draft가 현재 계약의 차명·차번·연료·연식을 다시 덮지 못하게 했다.

### 정산 목록

- 표시 전용 `settlement-display` SSOT를 추가하고 목록·필터·정렬·상세·페이지/메뉴 카운트가 같은 상태 해석을 사용한다.
- 누락/지원외 상태를 `정산대기`로 위장하지 않고 빨간 `상태 확인`으로 표시한다.
- 제목을 고객명에서 계약 snapshot 차량명으로 바꾸고, 확인된 공급사·영업자 이름만 표시한다.
- 백업 정산 15건의 계약/차량/업체/영업자 조인을 읽기 전용으로 대조해 15/15 복원했다. 정산 writer·금액 엔진·Rules는 수정하지 않았다.

### 자동·브라우저 검증

- `npm.cmd run typecheck`: PASS
- `npm.cmd run check:fonts`: PASS, 폰트 드리프트 0
- `npm.cmd run check:tokens`: PASS, 토큰/색 하드코딩 드리프트 0
- 안전한 `sim-*.mts` 15종: 기능 회귀 PASS
  - 업무 목록 의미 129/129
  - 영업 E2E 44/44
  - 권한 44/44, 채팅 Rules 43/43
  - 정산 표시 20/20, 정산 E2E 17/17
  - Phase 34/34, 차량 잠금 23/23, 생애주기 PASS
- 외부 공급사 상태 매핑 실조회: 아이카 3,241대, 오토플러스 유입 96대 파싱 PASS
- `NEXT_DIST_DIR=.next-codex-final npm.cmd run build`: 30개 route production build PASS
- 인앱 브라우저 390×844 모바일과 1920×889 데스크톱에서 `/`, `/chat`, `/contract`, `/inventory`를 확인했다. 가로 overflow는 없었고 빈 화면·필터·패널·하단탭 규격은 정상이다.
- 로컬 브라우저에는 운영 Firebase 인증/데이터가 없어 실제 행 렌더는 백업 기반 resolver·시뮬레이션으로 검증했다. 운영 로그인 실데이터 브라우저 재검수는 Rules 후보 적용 후 별도 필요하다.

### 출시 차단 — 해결 전 배포 금지

1. **공개 전자서명 PII:** `contract_sign/$token`의 익명 read가 `pending_review`에도 유지되어 주민번호·주소·면허·서명 이미지가 토큰 보유자에게 다시 읽힌다. `sim-contract-sign-rules`는 이 이유로 의도적으로 FAIL한다.
2. **계약 차량 snapshot 변조:** 계약 생성 후 10개 차량 snapshot 필드의 불변 Rules가 없다. `sim-contract-rules`는 이 이유로 의도적으로 FAIL한다.
3. **법정 운영자 정보:** 상호·대표자·주소·사업자등록번호·문의 이메일·개인정보 보호책임자 6개가 비어 있다.
4. **삭제/활성 데이터 충돌:** `01호8430`, `01호8433`은 v3에서는 `_deleted=true`, v4에서는 `available/is_active=true`다. 브리지 유무에 따라 숨김/재노출이 뒤집히므로 실제 운영 의도를 결정한 뒤 v4 overlay를 정리해야 한다.

`database.rules.json`은 작업원칙에 따라 수정·게시하지 않았다. 보안 1·2는 후보 Rules와 실제 레거시 데이터 검증 후 사람/Claude 게이트가 필요하다. `계약철회` 3건은 빨간 확인 대상으로 노출하되 취소/활성으로 임의 변환하지 않았다.

### 출시 후 개선 가능 경고

- 192×192·512×512 PWA PNG 아이콘 없음
- 서비스 워커/오프라인·업데이트 복구 없음

## 2026-07-27 4역할·전자서명 오픈 전 실검증

결과: **PASS — 전용 QA 조직·계정으로 역할 격리, 계약 진행, 전자서명 부정 시나리오 및 production build 확인**

### 검증용 조직과 계정

- 영업채널 `[QA] 영업채널 20260727`과 공급사 `[QA] 공급사 20260727`을 별도 생성했다.
- 실제 Firebase Auth 계정 4개를 생성해 다음 역할로 로그인했다.
  - 영업채널 관리자 `agent_admin`
  - 영업채널 직원 `agent`
  - 공급사 관리자 `provider_admin`
  - 공급사 직원 `provider`
- 영업 직원이 만든 계약을 영업채널 관리자도 조회·응대할 수 있었다.
- 공급사 직원이 조회한 자사 상품·계약을 공급사 관리자도 동일하게 조회할 수 있었다.
- 네 조직 역할 모두 플랫폼 관리자 전용 `/members` 접근이 차단되어 `/`로 이동했다.
- 전용 QA 비밀번호 4개는 검증 종료 전에 모두 교체했고, 새 비밀번호로 영업 직원 재로그인까지 확인했다.
- 자격정보는 Git 제외 경로 `tmp/qa-credentials-20260727-738912.json`에만 저장했다.

### 실제 계약 흐름

- QA 상품 `veh_nbbb6vveg5` / 차량번호 `99하0727`
- QA 채팅방 `CH_veh_nbbb6vveg5_QA-2-qKGcudkM`
- QA 계약 `TMP-260727-01-yvjf`
- 영업 직원: 채팅·계약문의, 서류 제출, 계약금·잔금 입금, 고객 약정 정보 확정
- 공급사 직원·관리자: 출고 가능 응답, 서류 승인, 입금 확인
- 현재 계약은 전자서명 부정 시나리오 재검증을 위해 `3/5`, 새 서명 링크 발송 상태로 보존했다.

### 전자서명 부정 시나리오

- 고객 서명 페이지에서 고객정보·필수 약관·서명 제출 규칙을 확인했다.
- 브라우저 자동화가 서명 캔버스 획을 전달하지 못해, 고객 제출 상태는 같은 공개
  `contract_sign/{token}` Rules를 통과하는 QA PNG 서명 payload로 생성했다.
- 제출 후 고객 화면 `제출이 접수되었습니다`, 내부 화면 `검토대기`를 확인했다.
- 영업 직원이 반려 사유 `[QA] 서명 내용 보완 요청`을 입력했고, 고객 링크가 같은 사유와 함께 재작성 화면으로 열렸다.
- 서명 링크 해지 후 기존 링크는 즉시 `유효하지 않은 링크`로 차단됐다.
- 과거 만료시각을 가진 별도 QA 공개 서명 슬롯도 동일하게 차단됐다.
- 해지 후 새 링크를 발급하자 토큰이 교체됐고 유효기간 7일, 고객 서명 폼 정상 노출을 확인했다.
- OCR은 사용자 요청에 따라 이번 검증 범위에서 제외했다.

### 발견·수정한 결함

- 신규 회원 화면에서 Firebase Auth UID를 입력할 수 없어 실제 로그인 계정과 운영 프로필 연결이 불가능했다.
  신규 생성 중에만 UID 입력을 허용하고 기존 회원 편집에서는 계속 숨기도록 수정했다.
- 관리자 회원 저장 시 역할·회사·채널만 최상위 `users/{uid}`에 동기화되어 이름과 `user_code`가 로그인 세션에 반영되지 않았다.
  운영 프로필 필드를 최상위 인증 SSOT에 함께 동기화하도록 수정했다.
- 인증 복원 전에 계약·채팅 빈 목록이 전역 캐시에 남아, 로그인 후에도 공급사 계약이 0건으로 보일 수 있었다.
  계약·채팅 최초 조회가 `initAuth()`를 기다리게 하고 인증 사용자 변경 시 스코프 캐시를 초기화하도록 수정했다.

### 자동 검증

- `npm.cmd run typecheck`: PASS
- `scripts/sim-*.mts` 12개: 전부 PASS
  - 영업자 39/39
  - 권한 44/44
  - 채팅 Rules 40/40
  - 계약 Rules 25/25
  - 전자서명 Rules 57/57
  - 3자 정산 E2E 15/15
  - 생애주기, 차량 잠금, 시트 병합, 상품·정산 private 마이그레이션 전부 PASS
- `NEXT_DIST_DIR=.next-verification npm.cmd run build`: 27개 route production build PASS
- 검증용 별도 build 디렉터리를 사용해 실행 중인 `localhost:4004` 개발 서버 PID `21692`는 중단하지 않았다.

## 2026-07-27 오픈 게이트 독립 재조사

결과: **자동 변경 없이 원인 분류 완료 / 계정·서명·배포 차단 요인 확정**

### 출고불가 3대

- `181허5280`
  - 상품 `RP011_181허5280`, 공급사 `연카(RP011)`, 외부 시트 연동
  - 2026-07-14 플랫폼 관리자가 `vehicle_status=출고불가`로 직접 변경한 감사기록 확인
  - 의도된 운영 상태로 분류하며 자동 해제하지 않음
- `101호9041`
  - 공급사 코드가 비어 있는 구형 수기/이관 상품
  - 원본 `status_label_raw=출고가능`, 현재 `vehicle_status=출고불가`
  - 관련 계약 1건은 `계약철회`; 완료 계약 없음
  - 상태 유지·재활성·중복정리는 사업 결정 필요
- `142호3663`
  - 공급사 코드가 비어 있는 구형 수기/이관 상품
  - 원본 `status_label_raw=출고가능`, 현재 `vehicle_status=출고불가`
  - 2026-07-14 영업자 배정 기록과 미완료 계약요청 `TMP-260714-01` 확인
  - 상태 유지·재활성·중복정리는 사업 결정 필요
- 세 상품 모두 실제 상세 화면에서 `출고불가` 표시를 확인했으며 데이터 변경은 하지 않았다.

### 실제 역할 계정

- 회원·파트너 146명 중 영업자 125명, 공급사 직원 17명
- 영업채널 관리자(`agent_admin`) 0명, 공급사 관리자(`provider_admin`) 0명
- 따라서 4역할 실로그인 격리 검증은 전용 QA 관리자 계정 생성 전까지 수행할 수 없다.
- 운영 계정을 임의 승격하거나 비밀번호를 재설정하지 않았다.

### 전자서명 부정 시나리오

- 저장된 공개 서명 요청은 `signed` 2건뿐이다.
- QA 계약 `TMP-260727-01-3bhy`는 서명완료·계약완료이며 검증 근거로 보존 중이다.
- 반려·해지·만료를 실제 브라우저에서 검증하려면 새 QA 계약과 독립 서명 요청이 필요하다.
- 기존 완료 계약은 변경하지 않았다. 정적·Rules 시뮬레이션은 기존대로 57/57 PASS다.

### Vercel·도메인

- 연결 프로젝트: `freepass-projects/freepasserp4`
- 최신 Production: 2026-07-25 생성, `Ready`
- 해당 배포 별칭: `freepasserp4.vercel.app` 등 Vercel 기본 도메인만 연결
- `freepasserp.com`, `www.freepasserp.com`은 현재 기존 `freepasserp3` 프로젝트 소속
- 실제 `https://freepasserp.com`은 `https://www.freepasserp.com/`으로 이동하고
  HTTPS 로그인 화면을 정상 표시하지만 FreepassERP4 최신 배포가 아니다.
- 이 문서 커밋 후 로컬 `main`은 `origin/main`보다 35커밋 앞선다.
- 결론: push와 `freepasserp4` 배포, 기본 도메인 사전 검증, 커스텀 도메인 전환이
  오픈 전에 반드시 필요하다. 이번 재조사는 읽기 전용이며 배포·도메인 변경은 수행하지 않았다.

## 2026-07-27 상품·정산 private 운영 마이그레이션

결과: **PASS — 운영 적용·사후 public 제거·관리자 기능 유지 확인**

### 상품

- 적용 전: 검사 `5,666대`, 민감필드 `4,890대`, private 쓰기 `4,890건`,
  public 삭제 `15,800경로`, 총 `20,690경로/52배치`, 안전제외 `0`
- 적용 결과: `20,690경로` 완료
- 사후 dry-run: public 삭제 `0`
- 상품찾기 `446대`, 관리자 재고 `450대` 정상 유지

### 정산

- 적용 전: 검사 `15건`, 금액 정산 `12건`, R1/R2/admin `12/0/0`,
  public 삭제 `24경로`, 총 `36경로/1배치`, 안전제외 `0`
- 첫 update는 `ST-260701-001.agent_channel_code`의 `undefined` 때문에
  Firebase 클라이언트 검증에서 전체 거부됐다. 단일 원자 update 이전이라 변경된 정산 경로는 없다.
- 중첩 `undefined` 제거와 적용 직전 방어 검사를 추가했다.
- 누락 귀속값 회귀 테스트 추가 후 정산 마이그레이션 `12/12`,
  authorization `44/44`, typecheck PASS
- 재실행 결과: `36경로` 완료
- 사후 dry-run: 금액 정산 `0`, public 삭제 `0`, 계획경로 `0`
- 관리자 월별정산: R1 `89,000원`, R2 `35,600원`, 순수익 `53,400원`,
  정산완료 1건 정상
- 브라우저 error/warn 로그: `0`
- 전체 13개 시뮬레이션·typecheck·폰트 가드와 production build `27개 페이지`: PASS

### 백업

- 적용 전 전체 RTDB:
  `tmp/full-backups/freepasserp3-rtdb-full-2026-07-27-105542.json`
  - `36,726,130 bytes`
  - SHA-256 `B0E10DC39C8665426A9F5757C8E1445278BFBA9A85A7A11D6EB254CE10F855A2`
  - JSON 파싱, 최상위 20개 키와 `products`, `v4`, `users` 확인
- 상품 동일 스냅샷:
  `tmp/migration-backups/freepasserp-products-backup-2026-07-27T01-59-41-464Z.json`
  - `27,606,076 bytes`
  - SHA-256 `41528721C391D1F8E2919BCD619CEB2BD47403E8714F47FD9355C6DF1C5F415E`
- 정산 성공 실행 동일 스냅샷:
  `tmp/migration-backups/freepasserp-settlements-backup-2026-07-27T02-02-57-856Z.json`
  - `14,393 bytes`
  - SHA-256 `312F660E00C07BBB8E27E9A1E53568DBC9144B0D769F253460D92DA87F3CD4A7`
- `/tmp/`는 Git 제외. 로컬 개발 전용 백업 API는 파일 저장과 SHA-256 반환이
  성공해야만 마이그레이션을 계속한다.

## 2026-07-27 최종 오픈 전 재검수

결과: **자동검증·관리자 실브라우저 PASS / 운영 승인 항목 분리**

- `npm.cmd run typecheck`: PASS
- `npm.cmd run check:fonts`: PASS
- 13개 권한·채팅·계약·서명·정산·생애주기·마이그레이션·차량마스터
  시뮬레이션: 전부 PASS
- `NEXT_DIST_DIR=.next-verification npm.cmd run build`: 27개 페이지 PASS
- 실행 서버를 중단하지 않고 주요 22개 경로 기대 상태코드 PASS
- Chrome 관리자 `박영협` 세션:
  - 재고 `450대`
  - 채팅 `158건`
  - 계약 `34건`
  - 회원·파트너 `146건`
  - 2026-07 정산 `1건`
- 관리자 정산 화면 새로고침 전후 R1 `89,000원`, R2 `35,600원`,
  순수익 `53,400원`, 로그인 세션 동일
- `/diag` Firebase Auth 복원 성공 및 핵심 8개 노드 읽기 `ok`
- 잘못된 전자서명 토큰은 개인정보 없이 안전 종료, 잘못된 견적 코드는
  `견적을 찾을 수 없습니다`로 종료
- 서버 stdout/stderr의 error-like 로그 `0`, 포트 4004 유지

### 최신 운영 dry-run

- 상품: 검사 `5,666대`, 민감필드 상품 `4,890대`, private 쓰기 `4,890건`,
  public 삭제 `15,800경로`, 총 `20,690경로/52배치`, 안전제외 `0`
- 정산: 검사 `15건`, 금액 정산 `12건`, R1/R2/admin `12/0/0`,
  public 삭제 `24경로`, 총 `36경로/1배치`, 안전제외 `0`
- 최초 상품 dry-run보다 검사 대상이 25대 늘어 운영 공급 피드가 계속 갱신되는 것을
  확인했다. 실제 실행 직전 수치를 다시 확인하고 동일 스냅샷 자동 백업을 사용해야 한다.
- 이 재검수 시점에는 실제 이동 전이었으며, 이후 상단의 운영 마이그레이션 절차로 완료했다.

### 데이터 품질 확인

- `/data-check`: 450매물, 이상 6종 230건
- 사진 없음 168, 사진 폴더 공유 17, 대여료 없음 12, 사진 링크 깨짐 4,
  초과주행 19, 노후 10은 운영 콘텐츠 정비 대상
- 완료 계약 없는 출고불가 3대는 자동 수정하지 않았다:
  `181허5280`, `101호9041`, `142호3663`
- 위 3대의 세부 분류는 문서 상단에 기록했다. `101호9041`, `142호3663`만
  사업 담당자의 상태 결정이 남았다.

## 2026-07-27 운영 RTDB 전체 백업

결과: **PASS — 실제 private 마이그레이션 전 복구 원본 확보**

- Firebase Console에서 `freepasserp3` Realtime Database 전체 JSON을 내보냈다.
- 보관 파일:
  `C:\Users\user\Downloads\freepasserp3-rtdb-backup-2026-07-27-101030.json`
- 파일 크기: `36,661,857 bytes`
- SHA-256:
  `AC8829FE447D878D9E9E180C91D42A399B336C7C72DA724994A8658FC3D5BC53`
- JSON 파싱 성공, 최상위 20개 키와 핵심 루트 `products`, `v4`, `users` 존재 확인
- 원본 다운로드와 보관본 해시 일치 확인
- 복구 경로는 Firebase Console → Realtime Database → 데이터 루트 →
  데이터베이스 작업 메뉴 → JSON 가져오기다.
- 실제 복원은 운영 데이터 변경 작업이므로 이번 검증에서는 실행하지 않았다.

## 2026-07-27 상품 private 운영 dry-run 및 백업 안전장치

결과: **PASS — 운영 데이터 변경 없이 대량 이동 규모 확정**

- 실제 Firebase 관리자 세션에서 상품 private 이동 미리보기를 실행했다.
- 검사 `5,641대`, 민감필드 상품 `4,867대`, 안전제외 `0`
- private 쓰기 `4,867건`, public 삭제 `15,781경로`
- 전체 계획 `20,648경로/52배치`
- 실제 실행 시 동일 스냅샷의 `products`, `v4/products`, `v4/products_private`를 JSON으로 먼저 다운로드하도록 강제했다.
- 안전하지 않은 키가 1건이라도 발견되거나 백업 처리가 없으면 실제 실행을 중단한다.
- 배치별 진행률을 관리자 `/dev`에 표시하고, 중단 후 같은 실행을 재개해도 private 우선 보존과 null 삭제가 반복 가능하다.
- 상품 private 마이그레이션 시뮬레이션: 15/15 PASS
- typecheck, `git diff --check`: PASS
- 실제 이동은 52배치의 운영 쓰기·공개 필드 삭제이므로 전체 RTDB 백업과 별도 명시 승인 전까지 실행하지 않았다.

## 2026-07-27 정산 private 운영 dry-run 및 백업 안전장치

결과: **PASS — 운영 데이터 변경 없이 이동 대상과 단일 원자 배치를 확정**

- 실제 Firebase 관리자 세션에서 정산 private 이동 미리보기를 실행했다.
- 검사 `15건`, 공개 금액 보유 `12건`, 안전제외 `0건`
- private 쓰기: 공급사 R1 `12건`, 영업 R2 `0건`, 관리자 순수익 `0건`
- public 삭제 예정: `24경로`
- 전체 계획: `36경로`, `1배치` — 현재 규모에서는 한 번의 RTDB 원자 업데이트로 적용 가능
- 영업·관리자 쓰기 0건은 해당 과거 레코드에 R2·순수익 필드가 없기 때문이다.
- 신규 QA 정산은 처음부터 private 노드에 저장돼 공개 금액 이동 대상이 아니다.
- 실제 실행 시 동일 스냅샷의 5개 정산 노드를 JSON으로 먼저 다운로드하도록 강제했다.
- 안전하지 않은 키가 1건이라도 발견되거나 백업 처리가 없으면 실제 실행을 중단한다.
- 정산 private 마이그레이션 시뮬레이션: 11/11 PASS
- typecheck, `git diff --check`: PASS
- 실제 이동은 공개 필드 삭제를 포함하므로 별도 명시 승인 전까지 실행하지 않았다.

## 2026-07-27 실제 브라우저 계약·전자서명·정산 E2E

결과: **PASS — 운영 Rules 게시, 계약 완료 복구, 차량 잠금, 건별·월별·VAT 정산 및 정산서 출력 완료**

### 실제 수행 결과

- 관리자 `박영협` 로그인 세션과 실제 Firebase 프로젝트 `freepasserp3` 연결로 진행했다.
- QA 상품 `66소6317`에 공급사 `스위치플랜 (RP014)`을 지정하고 저장했다.
- 계약문의 방과 가계약 `TMP-260727-01-3bhy`를 생성했다.
- 채팅, 출고응답, 서류 제출·승인, 계약금·잔금·입금확인, 고객 약정, 공개 전자서명 제출·관리자 승인: PASS
- 최신 운영 Rules 게시 전 발생한 private 정산 `PERMISSION_DENIED`를 재현했다.
- 운영 중이던 Rules를 `database.rules.PREV.json`으로 백업했다.
- 계약 Rules의 긴 단일 검증식을 필드별 불변 검증으로 분리해 Firebase Rules 컴파일 오류를 수정했다.
- 최신 `database.rules.json`을 Firebase Realtime Database Rules에 게시하고 콘솔 편집본과 로컬 JSON의 의미상 일치를 확인했다.
- `완료 처리 재시도`로 중단된 계약을 멱등 복구했다.

### 운영 데이터 확인

- 계약 `TMP-260727-01-3bhy`: `계약완료`
- 정산 `ST_TMP-260727-01-3bhy`: `정산완료`
- 상품 `66소6317`: `출고불가`
- 공급사 청구 R1: `89,000원`
- 영업 지급 R2: `35,600원`
- 플랫폼 순수익: `53,400원`
- 2026-07 월별정산: 1건, 위 금액과 상태 일치
- VAT 정산서 `AS_2026-07_TMP-260727-01-3bhy`: 저장 완료
  - 청구 `97,900원` = 공급가 `89,000원` + VAT `8,900원`
  - 지급 `39,160원` = 공급가 `35,600원` + VAT `3,560원`
  - 당월수익 `58,740원`
- 정산 엑셀 `C:\Users\user\Downloads\freepasserp.com_정산서_2026-07.xlsx`: 생성·파싱 PASS
  - 시트: `내역`, `공급사별`, `영업채널별`
  - 계약·차량·R1·R2·순수익·상태와 공급사/영업채널 소계 일치

### 검증

- Firebase Rules Emulator: **4/4 PASS**
  - 관리자 정산 쓰기 허용
  - 소유 공급사 정산 쓰기 허용
  - 무관 영업자 쓰기 거부
  - 필수 귀속 필드 누락 쓰기 거부
- `npm.cmd run typecheck`: PASS
- `npm.cmd run check:fonts`: PASS
- 계약 Rules 시뮬레이션: 25/25 PASS
- 권한 시뮬레이션: 44/44 PASS
- 채팅 Rules 시뮬레이션: 40/40 PASS
- 계약 전자서명 Rules 시뮬레이션: 57/57 PASS
- 3자 정산 E2E 시뮬레이션: 15/15 PASS
- 전체 생애주기·차량 잠금·정산 private 마이그레이션: PASS
- `database.rules.json`, `database.rules.PREV.json` JSON 파싱: PASS
- 별도 `NEXT_DIST_DIR` production build: 26개 route PASS
- HTTP smoke: `/`, `/inventory`, `/chat`, `/contract`, `/settlement`, `/members` 모두 200
- 실행 중인 4004 개발 서버: 중단·재시작 없이 유지

### 남은 범위

- QA 계약·전자서명·상품은 운영 검증 근거로 남겨 두었다. 삭제는 별도 승인 후 수행한다.
- 기존 공개 정산 금액의 private 노드 마이그레이션은 여전히 dry-run과 수치 승인이 필요하다.
- 전자서명 링크는 7일 만료, 명시적 해지, 만료·해지 후 익명 읽기/쓰기를 이미 지원한다.

## 2026-07-26 전자서명 공개 슬롯

결과: **PASS (정적 Rules·로컬 시뮬레이션 기준)**

- 익명 상태 전이·불변 필드·소유 영업조직·입력 제한: 15/15 PASS
- JSON 파싱, typecheck, contract rules 23/23, agent 39/39, diff check: PASS
- 잔여 위험: 공개 읽기는 토큰 보유자에게 허용되며 만료·명시적 해지 기능은 없음
- Firebase Rules 실게시·Emulator·실계정/실브라우저 제출은 미검증

## 2026-07-26 정산 private 마이그레이션

결과: **PASS (순수 계획·UI 기준)**

- 정산 마이그레이션 시뮬레이션: 10/10 PASS
- 기존 private 우선, V3/V4 삭제 경로, R1/R2/admin 분리 확인
- 기본 dry-run 및 실제 실행 위험 확인 UI 확인
- authorization 44/44, typecheck, diff check: PASS
- 라이브 Firebase 실행: 미실행

## 2026-07-26 정산 private 저장 골격

결과: **PASS (신규 저장·애플리케이션 병합 기준)**

- public/R1/R2/admin split 및 역할별 merge: authorization 44/44 PASS
- RTDB adapter 신규 save/update 멀티패스 분리 적용
- private Rules 조직별 읽기·쓰기 추가
- JSON 파싱, typecheck, agent 39/39, diff check: PASS
- 잔여 위험: 기존 public settlement 금액은 아직 마이그레이션되지 않음

## 2026-07-26 정산 표시·엑셀 역할 경계

결과: **PASS (프레젠테이션 경계)**

- 영업: R2만 표시·엑셀 포함
- 공급사: R1만 표시·엑셀 포함
- 플랫폼 관리자: R1·R2·순수익 표시·엑셀 포함
- typecheck, authorization 35/35, agent 39/39, diff check: PASS
- 보안 잔여 위험: RTDB 원본 settlement에 R1·R2·net이 함께 있어 허용된 사용자가 SDK로 원본 필드를 볼 수 있음

## 2026-07-26 Firebase Rules 조직 5역할

결과: **PASS (정적 규칙·로컬 시뮬레이션 기준)**

- 권한 모델 및 Rules 회귀 가드: 35/35 PASS
- 일반 영업자 개인 범위, 영업 관리자 채널 범위, 공급사 직원·관리자 회사 범위 확인
- 계약 담당 단계 역할군 분리 및 정산 상태 관리자 전용 확인
- JSON 파싱, typecheck, chat rules 40/40, contract rules 23/23, agent 39/39, lifecycle, settlement 15/15: PASS
- Firebase Rules 운영 게시·Emulator·실계정 검증은 미실행

## 2026-07-26 조직 권한 골격

결과: **PASS (애플리케이션 판정·조회 범위 기준)**

- 조직 역할 판정 및 레거시 매핑: 26/26 PASS
- 일반 영업자 개인/영업 관리자 채널/공급사 회사/플랫폼 전체 범위 확인
- 공급사 관리자도 기존 공급사 UI 역할로 정상 호환
- 채팅·계약·정산 목록과 메뉴 뱃지 중앙 scope 적용
- typecheck, agent 39/39, chat rules 40/40, contract rules 23/23, phase12 25/25: PASS
- 미완료: Firebase Rules의 5역할 명시화, 역할 부여 UI, 실제 역할 계정 테스트

## 2026-07-26 계약 RTDB 쓰기 경계

결과: **PASS (정적 규칙·로컬 시뮬레이션 기준)**

- `scripts/sim-contract-rules.mts`: 23/23 PASS
- 차단 확인: 비참여자 쓰기, 귀속 이전, 금액·요율 스냅샷 변조, 상대 역할 단계 수정, 미완료 강제 완료, 임의 상태
- 정상 확인: 역할별 단계 수정, 관리자 교정, 서명완료 후 약정발송 예외, 전체 체크 후 계약완료
- JSON 파싱, typecheck, chat rules 40/40, agent 39/39, lifecycle, settlement 15/15, phase12 25/25, vehicle 23/23: PASS
- 미검증: Firebase Rules 컴파일·Emulator·실계정. 저장소 규칙은 아직 운영에 게시하지 않음.

## 2026-07-26 채팅 RTDB 쓰기 경계

결과: **PASS (정적 규칙·로컬 시뮬레이션 기준)**

- 방 생성·갱신: 역할별 소유권 검사 및 소유 필드 불변성 확인
- 메시지 생성: 방 참여자, 신규 레코드, 실제 인증 UID 조건 확인
- 공격 회귀: 타 회사/타 영업 방, UID 위조, 메시지 overwrite, 방 ownership transfer 차단
- V3 방 + V4 메시지 호환 분기 확인
- `scripts/sim-chat-rules.mts`: 40/40 PASS
- JSON 파싱, `tsc --noEmit`, agent 39/39, phase12 25/25, lifecycle, settlement 15/15, vehicle lock 23/23: PASS
- 미검증 범위: Rules Emulator 및 Firebase 실제 계정

## 2026-07-26 채팅 RTDB 읽기 경계

결과: **PASS (정적 규칙·로컬 시뮬레이션 기준)**

- `database.rules.json` JSON 파싱: PASS
- TypeScript `tsc --noEmit`: PASS
- `scripts/sim-chat-rules.mts`: 21/21 PASS
- 기존 시뮬레이션: agent 39/39, lifecycle PASS, settlement 15/15, phase12 25/25, sheet merge 12/12, vehicle lock 23/23, product private migration 14/14
- 실행 중 개발 서버: 포트 4004 LISTEN 유지
- HTTP smoke: `/chat` 200, `/contract` 200
- `git diff --check`: PASS
- production build: 실행 중 개발 서버 보호를 위해 보류
- 미검증 범위: Firebase Rules 게시, Rules Emulator, 실제 관리자·공급사·영업자 계정 권한 시나리오

## 2026-07-26 — 공통 UI 내비게이션·피드백 분리

### 범위

- `components/ui/navigation.tsx`: `NavBack`, `BottomNav`
- `components/ui/feedback.tsx`: `EmptyState`, `Loading`, `CenterNote`, `Message`
- `components/ui/index.tsx`: 구현 제거 후 기존 공개 API 재수출

### 호환성 및 검증

- 기존 `@/components/ui` import와 props·타입 export 유지
- 모바일 하단 safe-area 및 history/list 뒤로가기 동작 유지
- `npm.cmd run typecheck`: PASS
- `/`, `/inventory`, `/contract`, `/chat`, `/members`, `/settings`: HTTP 200
- 전체 7개 시뮬레이션·마스터 전수 검증: PASS
- `git diff --check`: PASS
- production build는 실행 중인 개발 서버 보호를 위해 보류

## 2026-07-26 — 공통 UI 통계·요약 분리

### 범위

- `components/ui/metrics.tsx` 신규
- `Card`, `Toolbar`, `Panel`, `Kpi`, `KpiRow`, `StatBar`, `Stepper`, `Step` 이동
- `components/ui/index.tsx`는 기존 공개 API 재수출

### 1차 검증

- `npm.cmd run typecheck`: PASS
- `/contract`: HTTP 200
- `components/ui/index.tsx`: 393줄 → 309줄

### 전체 검증

- 전체 7개 시뮬레이션·마스터 전수 검증: PASS
- `/`, `/inventory`, `/contract`, `/chat`, `/members`, `/settings`: HTTP 200
- `git diff --check`: PASS

## 2026-07-26 — 공통 UI 본체 최종 분리

### 범위

- `detail-shell.tsx`: `DetailShell`
- `form-grid.tsx`: `FormGrid`
- `copy-block.tsx`: `CopyBlock`
- `formatters.ts`: `won`, `fmtNumber`, `fmtPhone`
- `components/ui/index.tsx`: 구현 제거 후 순수 barrel

### 1차 검증

- `npm.cmd run typecheck`: PASS
- `/`, `/inventory`, `/members`, `/policy`, `/faq`: HTTP 200
- `components/ui/index.tsx`: 191줄 → 32줄
- 기존 `@/components/ui` import 경로 변경 없음

### 전체 검증

- 전체 7개 시뮬레이션·마스터 전수 검증: PASS
- 홈·재고·계약·채팅·회원·설정·정책·FAQ: HTTP 200
- `git diff --check`: PASS

## 2026-07-26 — 차량 마스터 입력 신호 정규화 분리

### 범위

- `lib/domain/vehicle-master-normalize.ts` 신규
- `unpackVehicleSignalsEngine`으로 입력 신호 해석 구현 이동
- 기존 `unpackVehicleSignals` 공개 함수는 호환 래퍼 유지
- 기존 본체의 연식·배기 중복 파서 제거

### 검증

- `npm.cmd run typecheck`: PASS
- 전체 7개 시뮬레이션: PASS
- `verify-master-pass.mts`: PASS
- `git diff --check`: PASS
- `/inventory`: HTTP 200
- `vehicle-master-match.ts`: 843줄 → 676줄

## 2026-07-26 — 차량 마스터 모델·세대 점수 엔진 분리

### 범위

- `lib/domain/vehicle-master-score.ts` 신규
- 제조사 그룹 잠금, 모델 유사도, 세대 후보 점수와 동점 정렬 이동
- variant·트림·confidence 판정은 기존 본체에 유지

### 정책 동일성

- 세대코드 및 `N세대` 서수 가중치 유지
- 연식 범위·경계·범위 밖 패널티 유지
- 하이브리드·전기 세대 제약 유지
- EV 전용 세대와 쿠페·카브리올레 불일치 패널티 유지

### 검증

- `npm.cmd run typecheck`: PASS
- 전체 7개 시뮬레이션: PASS
- `verify-master-pass.mts`: PASS
- `git diff --check`: PASS
- `/inventory`: HTTP 200
- `vehicle-master-match.ts`: 676줄 → 625줄

## 2026-07-26 — 차량 마스터 variant 점수 엔진 분리

### 범위

- `lib/domain/vehicle-master-variant.ts` 신규
- variant 연료·배기·구동·인승·터보·라벨 점수 이동
- `modeSeat`, `modeSeatForModel` 이동 및 기존 경로 재수출
- 트림·confidence 판정은 기존 본체 유지

### 검증

- `npm.cmd run typecheck`: PASS
- 전체 7개 시뮬레이션: PASS
- `verify-master-pass.mts`: PASS
- `git diff --check`: PASS
- `/inventory`: HTTP 200
- `vehicle-master-match.ts`: 625줄 → 574줄

## 2026-07-26 — 계약·고객 RTDB 조회 보안 검증

### 발견

- 어댑터의 계약·고객 역할별 쿼리는 이미 구현되어 있었다.
- 고객 규칙은 v3/v4 모두 `created_by === auth.uid` 조건과 색인이 일치했다.
- 계약 규칙은 v3/v4 모두 로그인 사용자 전체 읽기를 허용해 어댑터 스코프만으로는 보안 경계가 아니었다.
- 정산 계약일자 조인이 `v4/contracts` 전체 읽기를 시도하고 있었다.

### 수정

- v3 `contracts`, v4 `v4/contracts` 규칙을 관리자·공급사 회사·영업자 uid/채널 쿼리로 제한
- 정산 계약일자 조인을 역할별 계약 병합 결과로 변경

### 자동 검증

- `database.rules.json` JSON 파싱: PASS
- `npm.cmd run typecheck`: PASS
- 전체 7개 시뮬레이션·마스터 전수 검증: PASS
- `git diff --check`: PASS
- `/contract`, `/chat`, `/settlement`: HTTP 200

### 미완료 운영 검증

- Firebase 규칙 게시: 미실행
- 라이브 관리자·공급사·영업자 계정별 조회: 미실행
- 따라서 로컬 코드·규칙 파일은 완료됐지만 라이브 보안 적용 완료로 판정하지 않는다.

## 2026-07-26 — 민감 매물 필드 private 노드 기반

### 발견

- 기존 `stripProductCost`는 `vehicle_price`만 제거했다.
- VIN과 `price.*.fee/commission/fee_memo`가 비권한 상품 객체에 남을 수 있었다.
- `products_private` 노드와 규칙이 없었다.

### 수정

- 비권한 객체에서 원가·VIN·기간별 내부 수수료 필드 제거
- 공개 대여료·보증금 유지
- `v4/products_private/{product}` 관리자/자기회사 공급사 규칙 추가
- 영업자 시뮬레이션에 민감 필드 마스킹 회귀 케이스 추가

### 자동 검증

- `database.rules.json` JSON 파싱: PASS
- `npm.cmd run typecheck`: PASS
- `sim-agent.mts`: 38/38 PASS
- 나머지 전체 시뮬레이션·마스터 전수 검증: PASS
- `git diff --check`: PASS
- 홈·재고·계약: HTTP 200

### 미완료

- 기존 public product 레코드의 민감 필드 마이그레이션
- Firebase 규칙 라이브 게시 및 실계정 검증

## 2026-07-26 — 민감 매물 필드 public/private 이중 저장

### 구현

- `splitProductPrivate`: 공개 상품과 민감 원자를 분리
- `mergeProductPrivate`: 권한 있는 읽기에서 원가·VIN·수수료를 복원
- 신규 `save`: `v4/products`와 `v4/products_private`에 원자 멀티패스 저장
- `update`: 민감 필드가 포함된 패치를 private 경로로 분기
- `bulkPatch`: 상품 일괄 패치도 같은 분기 적용
- 관리자 전체 및 공급사 회사 쿼리로 private 레코드 조회

### 회귀 검증

- 공개 레코드에 원가·VIN·내부 수수료 없음: PASS
- private 레코드에 민감 필드 보존: PASS
- 공개 대여료·보증금 유지: PASS
- 권한 병합 후 전체 원자 복원: PASS
- `sim-agent.mts`: 39/39 PASS
- 나머지 전체 시뮬레이션·마스터 검증: PASS
- 타입 검사·규칙 JSON·diff 검사: PASS
- 홈·재고·계약·채팅: HTTP 200

### 운영 잔여

- 기존 v3/v4 public 상품 민감 필드 마이그레이션
- Firebase 규칙 게시
- 관리자·공급사·영업자 실계정 검증

## 2026-07-26 — 민감 필드 마이그레이션 도구

### 안전장치

- 기본값 dry-run
- 실제 실행 전 관리자 개발도구 위험 확인
- private 레코드 준비 후에만 public 삭제 계획 생성
- 기존 private 값 우선 보존
- v3/v4 가격 기간·필드 깊은 병합
- RTDB 금지문자 키 제외
- 400개 경로 단위 적용 배치

### 검증

- `sim-product-private-migration.mts`: 14/14 PASS
- v4 원가 우선 및 v3 전용 fee 보존: PASS
- 기존 private VIN·메모 우선: PASS
- v3/v4 public 삭제 경로 생성: PASS
- 공개 전용 상품 미변경: PASS
- 타입 검사·기존 전체 7개 검증·규칙 JSON·diff 검사: PASS
- `/dev`, `/inventory`, `/`: HTTP 200

### 미실행

- 라이브 dry-run: Firebase 환경변수 부재로 미실행
- 실제 마이그레이션: 미실행
- 라이브 규칙 게시 및 실계정 검증: 미실행

## 2026-07-26 — 공통 UI 칩·필터 분리

### 범위

- `components/ui/filters.tsx` 신규
- `PillTabs`, `ToggleChips`, `FilterGroup`, `FilterChips`, `ChipOpt` 이동
- `components/ui/index.tsx` 기존 공개 API 재수출
- `FormGrid`의 `ToggleChips` 의존성을 leaf 직접 import로 전환

### 1차 검증

- `npm.cmd run typecheck`: PASS
- `/`, `/inventory`, `/contract`: HTTP 200
- `components/ui/index.tsx`: 309줄 → 191줄

### 전체 검증

- 전체 7개 시뮬레이션·마스터 전수 검증: PASS
- `/`, `/inventory`, `/contract`, `/chat`, `/members`, `/settings`: HTTP 200
- `git diff --check`: PASS

## 2026-07-26 — 공통 UI 통계·요약 분리

### 범위

- `components/ui/metrics.tsx`
- 이동 대상: `Card`, `Toolbar`, `Panel`, `Kpi`, `KpiRow`, `StatBar`, `Stepper`
- `components/ui/index.tsx`는 기존 API를 재수출한다.

### 호환성 및 검증

- 기존 `Step` 타입과 컴포넌트 props 유지
- tone 색, 수치 표시, Stepper 상태 표현 유지
- `npm.cmd run typecheck`: PASS
- `/`, `/inventory`, `/contract`, `/chat`, `/members`, `/settings`: HTTP 200
- `git diff --check`: PASS
- production build는 실행 중인 개발 서버 보호를 위해 보류

## 2026-07-26 — Inventory 목록 UI 분리 검증

### 검증 결과

- 신규 작성 중 드래프트가 신규 슬롯을 채우는 조건 유지
- 선택 행 표시와 클릭 콜백 유지
- 검색 조건 유무에 따른 빈 상태 문구·조건 해제 유지
- 100대 더보기 단위와 500대 전체 보기 상한 유지
- 목록 상태 setter를 개별 전달하지 않고 `InventoryListPanelModel` 경계 사용

### 전체 자동 검증

- typecheck: PASS
- build: PASS
- 전체 도메인 시뮬레이션 및 차량 마스터 검증: PASS

### 남은 위험

- 실제 브라우저의 목록 선택·더보기·신규 슬롯을 수동 검증하지 않았다.
- `/inventory` 라우트는 누적 리팩터링 전 18.5 kB에서 18.8 kB로 증가했다.

### 최종 판정

**PASS**

---

## 2026-08-01 — 계약문의·계약진행 목록 전수 검수 및 출시 판정

### 판정

- **계약문의·계약진행 목록 기능: 조건부 GO.** 이번 변경분에 대한 독립 최종 코드검토에서 P0/P1 잔존 이슈는 없다.
- **서비스 전체 출시: NO-GO.** 아래의 보안 Rules, 정산엔진 정규화, 계약철회 정책, 법정 운영자 정보, 최신 빌드 배포 후 역할별 실브라우저 재검수가 남아 있다.
- 운영 Vercel 화면은 아직 이번 로컬 변경 이전 버전이다. 따라서 아래 기능 PASS는 코드·시뮬레이션·로컬 전수 화면 기준이며, 운영 배포 완료 판정이 아니다.

### 히스토리·실데이터를 반영한 목록 규격

- 계약문의와 계약진행을 별개 흉내 화면으로 보지 않고 `문의 → 서류 → 입금 → 약정 → 출고 → 완료/취소`라는 한 업무 흐름으로 통일했다.
- 좌측 메인 아이콘이 현재 단계를 먼저 전달하고, 색과 필요한 경우의 뱃지가 이를 보조한다. 순수 문의 행의 중복 `상담` 뱃지는 제거했다.
- 뱃지는 `계약문의 진행`, `서류 진행`, `입금 진행`, `약정 진행`, `출고 진행`, `계약완료`, `계약취소`를 기본으로 한다.
- `계약철회`, `출고 불가`, `서류 부결`, `완료 처리 대기`, 상태 누락·레거시 상태는 정상 진행으로 위장하지 않고 적색/황색 `확인 필요` 계열로 드러낸다.
- 필터와 단계순 정렬도 raw 저장값이 아니라 위 파생 업무단계를 같은 SSOT로 사용한다. 화면에 숨긴 `계약요청` 같은 raw 상태는 일반 검색 결과에 사용하지 않는다.
- 차량명은 `제조사 + 모델/서브모델 + 트림`을 원칙으로 하며, 제조사·모델 중복을 제거하고 운영 이력의 더 구체적인 전체 차명은 보존한다. 차번만 있을 때 차명으로 재사용하지 않고 `차량명 미확인`으로 표시한다.
- 영업자는 공급사, 공급사는 영업 담당자, 관리자 계열은 필요한 양쪽 상대방을 본다. 실제 이름을 코드보다 우선하고 UID·내부 식별자는 숨긴다.
- 가운데점은 비어 있지 않은 메타데이터 사이에만 조합한다. 공급사명이 없는데 앞에 `·`만 남는 형태를 공통 유틸에서 차단했다.
- 운영 데이터의 `product_uid` 전용 레거시 문의방도 canonical `product_code`로 복원한다. 명시된 `linked_contract`는 차량·담당자 추정보다 우선하며, 다른 계약에 연결된 방이나 첨부를 재사용하지 않는다.
- 계약 선택은 채팅방을 새로 만들지 않는다. 취소 계약은 진행·문서 패널을 읽기 전용으로 고정한다.

### 기능 안전성 수정

- A 계약 작업 중 B를 선택했을 때 A의 늦은 비동기 콜백이 B 상세에 A 정산을 덮는 교차선택 경쟁을 계약코드 ref와 epoch 검증으로 차단했다.
- 계약 단계 저장 뒤 정산·차량 처리에서 실패하는 부분 성공도 최신 계약을 다시 읽고 목록·메뉴 카운터를 갱신하며, 원래 오류를 숨기지 않는다.
- 첨부는 계약 조회가 끝나기 전에 추가·삭제할 수 없고, 없는 계약이나 취소 계약이면 쓰기를 차단한다.
- 첨부 업로드·삭제는 한 번에 하나만 실행한다. 쓰기 직전에 캐시를 우회해 최신 계약을 재조회하고, 업로드는 최신 배열에 병합하며 삭제도 최신 배열을 기준으로 처리한다.
- 목록 필터로 선택 행이 사라지면 상세를 함께 비운다. 공통 클릭 행은 Enter/Space 키보드 조작과 선택 상태 접근성 속성을 제공한다.

### 실화면 확인

- 현재 운영 `/contract`: 계약 37건을 확인했다. 운영에는 여전히 raw 필터·구버전 뱃지와 `계약철회` 2건이 보이며 이번 변경은 아직 배포되지 않았다.
- 현재 운영 `/chat`: 문의방 180건을 확인했다. 구버전에는 제조사 누락/중복, 불필요한 `상담` 뱃지, 공급사 앞 고아 구분자, 내부 공급사 코드·긴 UID 노출이 실제로 존재했다.
- 로컬 전수 화면 감사: 역할·화면 조합 44개, 스크린샷 44개, 가로 overflow 0. 목록의 canonical 차량명, 단계 아이콘, 역할별 상대방, 고아 구분자 제거를 확인했다.
- 로컬 감사 환경은 Firebase 네트워크를 의도적으로 연결하지 않아 각 화면에 환경변수/네트워크 런타임 오류가 있었다. 최신 마지막 수정 뒤 로컬 브라우저 재검수는 브라우저 보안정책이 `localhost` 연결을 차단해 반복하지 않았으며 우회하지 않았다.
- 운영 채팅에서 React hydration 경고와 약 10초 이상의 초기 목록 대기를 관찰했다. 목록은 최종 로드됐지만 배포 후 성능·런타임 재검수 항목으로 남긴다.

### 자동 검증

- `npx tsc --noEmit`: PASS
- `npm run check:fonts`: PASS, 드리프트 0
- `npm run check:tokens`: PASS, 드리프트 0
- `scripts/sim-work-list-semantics.mts`: **96/96 PASS**
- `scripts/sim-agent.mts`: 39/39 PASS
- `scripts/sim-lifecycle.mts`: PASS
- `scripts/sim-e2e-settlement.mts`: 17/17 PASS
- `scripts/sim-vehicle-lock.mts`: 23/23 PASS
- `scripts/sim-contract-rules.mts`: 25/25 PASS
- `scripts/sim-authorization.mts`: 44/44 PASS
- `scripts/sim-chat-rules.mts`: 43/43 PASS
- `NEXT_DIST_DIR=.next-codex-final npm run build`: PASS, 30개 라우트 (`/chat` 11.1kB, `/contract` 6.61kB)
- `git diff --check`: PASS
- Next 빌드가 수정한 `tsconfig.json` 포맷과 임시 include는 원상복구했다.

### 출시 차단 사유

1. **보안 Rules 게이트** — `database.rules.json`의 부모 `/users`, `/partners` 읽기가 모든 정상 로그인 계정에 열려 있어 요율·지급·연락처·외부 시트 URL 등 역할 외 정보가 노출될 수 있다. Rules 수정·게시는 실데이터 사람/Claude 게이트가 필요한 위험영역이라 이번 작업에서 변경하지 않았다.
2. **정산엔진 상태 정규화 게이트** — 화면은 공백 포함 `계약완료`를 정규화하지만 `lib/domain/settlement-engine.ts`에는 raw 문자열 비교가 남아 있다. 레거시 완료 계약을 놓치면 차량 잠금 재계산이 `출고불가`를 `계약중`으로 낮출 수 있다. 정산엔진 위험영역이므로 별도 승인·시뮬레이션 후 수정해야 한다.
3. **계약철회 정책 결정** — 운영 데이터에 `계약철회` 2건(진행 1/5, 0/5)이 있다. 현재는 적색 `확인 필요`로 숨기지 않았지만, 철회를 최종 이력으로 닫을지 재개 가능한 보류로 둘지 운영 정책이 확정되지 않았다.
4. **법정 운영자 정보** — `npm run check:release`가 상호, 대표자, 주소, 사업자등록번호, 문의 이메일, 개인정보 보호책임자 미기재로 FAIL했다. 실제 값은 임의 작성할 수 없다.
5. **배포·실브라우저 게이트** — 최신 빌드를 운영에 배포한 뒤 영업자·영업관리자·공급사·공급사관리자·관리자 역할로 계약문의/진행/취소/첨부 시나리오와 모바일 화면을 다시 확인해야 한다.

### 비차단 경고

- 192×192·512×512 아이콘, 서비스 워커, HSTS, X-Content-Type-Options, Referrer-Policy, Permissions-Policy가 없다. B2B 웹 우선 범위에서는 후속 가능하지만, 앱/PWA 또는 외부 공개 전에는 처리해야 한다.
- 로컬 시뮬레이션 셸에는 Firebase 공개 환경변수가 없어 경고가 발생했다. 실제 배포 프로젝트의 환경변수 존재와 Production 스코프를 배포 전에 재확인한다.

### 위험영역 보존

- `database.rules.json`, `lib/domain/settlement-engine.ts`, `lib/firebase/rtdb-adapter.ts`는 수정하지 않았다.
- v3 운영 노드 write와 RTDB v3+v4 이중읽기 tolerance도 변경하지 않았다.

---

## 2026-07-31 B2B 출시 NO-GO 원인 수정 검증

### 수정

- 가격이 없거나 0원인 기간은 `createContractRequest`가 계약 저장 전에 거부한다.
- 레거시·우회 호출은 정산 단일 writer `createSettlement`가 다시 검사하여 0원 정산 생성을 거부한다.
- 기존 0원 정산도 관리자 `정산 확정` 단계에서 거부한다.
- 역할 없음·알 수 없는 역할의 활성 계정은 더 이상 기본 영업자로 통과하지 않고 인증 게이트에서 차단한다.
- 차단 화면에는 관리자 역할 지정이 필요하다는 별도 안내를 표시한다.

### 검증

- `npm.cmd run typecheck`: PASS
- `npm.cmd run check:fonts`: PASS, 드리프트 0
- `scripts/sim-*.mts` 13개 전부 PASS
- 신규 `sim-release-blockers.mts`: 13/13 PASS
- `sim-e2e-settlement.mts`: 17/17 PASS
  - 가격 없는 계약 차단
  - 레거시 0원 계약 정산 차단
  - 정상 55만원 계약 → R1 55,000원 / R2 22,000원 / 순수익 33,000원 회귀 통과
- 격리 production build: PASS, 30 routes

### 판정

**코드 원인 수정 PASS.** 기존 운영 데이터 `CT-260715-01`의 원천 월대여료 보정과 역할 미지정 계정의 역할 지정/비활성화는 별도 운영 조치가 남아 있다. 정산 엔진 변경은 저장소 원칙에 따라 게시·머지 전 사람 또는 Claude 게이트가 필요하다.

---

## 2026-07-31 출시 전 B2B 핵심 기능 브라우저 QA

### 범위와 환경

- 배포본 `https://freepasserp4.vercel.app`을 로그인 계정 `박영협`으로 실제 브라우저 검수했다.
- 배포 화면에 표시된 빌드 SHA는 `d8b2f61`이며, 현재 로컬 HEAD와 다르다. 아래 브라우저 결과는 배포본 기준이다.
- 실데이터 변경을 피하기 위해 등록·수정·문의 전송·계약 상태 변경·정산 확정은 실행하지 않았다.

### 통과한 조회 흐름

- 상품찾기: 346건 로딩, 차량번호 `125하2867` 검색 결과 1건, 상품 상세·가격·보험·계약조건·사진 표시 PASS.
- 재고관리: 데스크톱 350건 로딩, `182하4240` 검색 결과 1건, 목록 선택 후 G80 상세·공급사·운영상태 동기화 PASS.
- 모바일 재고: 390×844에서 가용재고 271건 로딩, 검색·필터·하단 내비게이션·목록 레이아웃 PASS.
- 계약·채팅: 현재 계정 데이터 0건 상태에서 빈 화면과 업무 패널 정상 표시 PASS.
- 정산: 2026-07 11건 로딩, 목록 선택·상세·공급사별 집계 표시 PASS.
- 회원·파트너: 154건 로딩, 계정/회사·역할·활성상태 필터 화면 표시 PASS.

### 출시 전 필수 확인 이슈

1. **중요 — 0원 정산 데이터**
   - `ST_CT-260715-01` / 계약 `CT-260715-01` / 정길영 / `151호2208`가 정산대기 상태이나 월대여료·R1·순수익이 모두 0원이다.
   - 원천 계약 금액을 보정하고, 0원 또는 필수 금액 누락 건은 정산 생성·가져오기·확정 단계에서 차단하거나 명시적 경고해야 한다.
2. **중요 — 활성 역할 미지정 계정**
   - 회원 목록에 UID `9XHW7UjORmO7SbHRQ8KrPSZG0F23` 계정이 `활성 / 역할 미지정`으로 존재한다.
   - 출시 전 역할을 지정하거나 비활성화하고, 역할 미지정 계정의 업무 화면/API 접근이 거부되는지 검증해야 한다.
3. **중요 — 쓰기 E2E 미검증**
   - 실운영 데이터를 보호하기 위해 상품 등록/수정, 문의 전송, 계약 생성·상태 전이, 채팅 전송, 정산 확정·환수는 수행하지 않았다.
   - 별도 QA 계정과 QA 표식 데이터로 역할별 전체 쓰기 여정을 1회 이상 완료해야 한다.
4. **일반 — 초기 재고 로딩 변동**
   - 데스크톱 직접 진입에서 한 차례 0건 로딩 상태가 유지됐고 새로고침 후 350건으로 복구됐다. 이후 모바일 진입은 정상 로딩됐다.
   - 배포 후보 빌드에서 직접 URL 진입·새로고침·로그인 직후 진입을 반복 검증한다.
5. **개선 — 상품찾기 표 행 키보드 접근성**
   - 클릭 가능한 결과 행에 키보드 포커스/Enter 동작이 없어 마우스 의존적이다.

### 현재 판정

**기능 조회 흐름은 PASS, 최종 출시는 조건부 NO-GO.**

0원 정산과 역할 미지정 활성 계정을 정리하고, 배포 후보 SHA로 역할별 쓰기 E2E 및 정산 금액 정합성 검증을 통과한 뒤 GO 판정한다.

---

## 2026-07-31 UI·UX 페이지 규격 통합 검수

### 브라우저 비교 범위

- 배포본 데스크톱: 상품찾기, 재고관리, 계약진행, 계약문의, 월별정산, 회원·파트너, 정책관리, 설정, 감사·휴지통, 데이터점검.
- 모바일 390×844: 상품찾기, 계약문의, 계약진행, 재고관리, 설정, 월별정산, 회원·파트너.
- 공통 SSOT: `NAV_LABEL`, `WorkPage`, `MobilePageShell`, `PageToolBar`, `BottomNav`, UI tokens.

### 확인 결과

- 주요 B2B 업무 화면은 상단 상태창 → 검색/정렬/필터 → 목록 → 상세 패널 구조를 공통 `WorkPage`로 사용한다.
- 모바일 핵심 5탭은 상품찾기·계약문의·계약진행·재고관리·설정으로 통일되어 있다.
- 월별정산·회원 목록의 390px 화면에서 가로 넘침 없이 금액·상태·코드가 축약 표시된다.
- 색·폰트·컨트롤 토큰 검사에서 하드코딩 드리프트가 발견되지 않았다.

### 직접 수정

- 재고·계약·채팅·정책 화면에서 데이터 로딩 중 `0대/0건`을 먼저 표시하지 않도록 count를 미표시 처리했다.
- 상품찾기 엑셀형 결과 행에 키보드 포커스와 Enter/Space 상세 진입, 접근성 라벨을 추가했다.

### 검증

- `npm.cmd run typecheck`: PASS
- `npm.cmd run check:fonts`: PASS
- `npm.cmd run check:tokens`: PASS
- `scripts/sim-phase12.mts`: 34/34 PASS

### 잔여 범위

- 브라우저 보안 정책으로 최신 로컬 `localhost:4004` 화면을 직접 열지 못해 시각 비교는 배포 SHA `d8b2f61` 기준이다.
- 수정 사항을 포함한 최종 배포 후보에서 데스크톱·390px 화면을 한 차례 재검수해야 한다.
- `감사·휴지통`, `데이터점검`은 실제 데이터 로딩 완료 화면과 오류/빈 상태를 최종 배포 후보에서 추가 확인한다.

### 판정

**공통 규격과 주요 페이지 UI는 CONDITIONAL PASS.** 최종 배포 후보 재검수 전까지 UI·UX 전체 GO로 확정하지 않는다.

---

## 2026-07-31 B2B 기능·UI 최종 릴리스 후보 판정

### 최종 보완

- 재고·계약·채팅·정책 화면은 로딩 중 잘못된 `0대/0건`을 노출하지 않는다.
- 상품찾기 엑셀 행은 마우스뿐 아니라 키보드 Enter/Space로 상세 진입할 수 있다.
- 0원 정산은 생성·확정이 차단되며 월별정산 목록과 상세에 `금액 확인` 경고가 표시된다.
- 공급사율 미확정 정산은 `요율 확인` 경고가 표시된다.
- 역할 미지정·알 수 없는 역할 계정은 인증 게이트에서 차단된다.

### 최종 게이트

- `npm.cmd run typecheck`: PASS
- `npm.cmd run check:fonts`: PASS
- `npm.cmd run check:tokens`: PASS
- `scripts/sim-*.mts` 13개 전부 PASS
- 정산 E2E: 17/17 PASS
- 출시 차단 회귀: 13/13 PASS
- Phase 1·2 UI/기능 회귀: 34/34 PASS
- production build: PASS, 30 routes
- `git diff --check`: PASS

### 판정

**B2B 기능·UI 릴리스 후보 GO.**

이 판정은 현재 소스의 기능·권한·정산 안전장치·공통 UI 규격과 빌드 가능성에 대한 것이다. 실제 공개 배포 후에는 배포 SHA 확인과 데스크톱/390px 스모크 테스트를 수행해야 최종 운영 GO가 닫힌다. `database.rules.json` 게시와 운영 데이터 직접 수정은 이 판정에 포함하지 않는다.

---

## 2026-07-31 배포 후 최종 운영 스모크

### 배포·CI

- UI 규격 마감 `5497a50`, CI 복구 `5a475c9`, 감사 표시 보정 `9630df7`을 `origin/main`에 푸시했다.
- GitHub Actions CI `30632586337`, `30632813325`: PASS.
- CI 게이트: 타입, 폰트, 디자인 토큰, 시트 병합, 차량 락, 정산 E2E, 출시 차단 회귀, production build 전부 PASS.
- Vercel 운영 URL에서 엑셀 결과 행 접근성 라벨과 감사 로그 대체 문구를 확인하여 최신 앱 코드 반영을 검증했다.

### 배포본 브라우저 스모크

- 데스크톱: 상품찾기, 재고관리, 계약문의, 계약진행, 월별정산, 회원·파트너, 정책관리, 감사·휴지통, 데이터점검, 설정 로딩 완료.
- 모바일 390×844: 상품찾기, 재고관리, 계약진행, 월별정산, 회원·파트너 로딩 완료.
- 모바일 핵심 화면에서 로딩 중 `0대/0건` 오표시 없음.
- 월별정산의 기존 0원 건에 `금액 확인` 경고 표시 확인.
- 감사 로그 구형 레코드의 `undefined` 노출이 `기록 대상 미기록`으로 변경됨을 확인.
- 상품찾기 엑셀 행이 접근성 트리에서 `상품 상세` 링크로 노출됨을 확인.

### 최종 판정

**B2B 웹 기능·UI 운영 GO.**

`database.rules.json` 신규 게시, 기존 0원 원천계약의 실제 금액 보정, 역할 미지정 계정의 운영 데이터 정리는 별도 사람 승인 작업이며 이번 배포에서 실행하지 않았다. 애플리케이션은 해당 비정상 상태의 신규 생성·접근·정산 확정을 차단하고 관리자 경고를 표시한다.

---

## 2026-07-31 — 출시 전 통합 QA 및 런칭 준비도 재검증

### 최종 판정

**FAIL (출시 차단)** — 코드·시뮬레이션 게이트는 통과했지만 `/terms`와 `/privacy`의 운영자 필수 정보가
전부 미기재 상태다. 상호·대표자·주소·사업자등록번호·문의 이메일·개인정보 보호책임자를 실제 정보로
입력하고 `npm run check:release`를 통과하기 전에는 정식 출시하면 안 된다.
또한 iOS·Android 네이티브 앱, 앱스토어 심사정보, 실결제·환불 사업자 연동, 실기기 호환성,
부하·장애복구 및 운영 모니터링은 이 저장소와 현재 검증 환경에서 증명할 수 없어 출시 담당자의 별도 확인이 필요하다.

### 발견 이슈 및 조치

| 심각도 | 플랫폼·화면 | 재현 및 실제 결과 | 기대 결과 | 조치·재검수 |
|---|---|---|---|---|
| 중요 | Web · 회원 승인/권한 변경 | 인증 모듈의 신원 변경 감사 로그가 운영 루트 `audit_logs`에 기록됨 | 운영 v3 노드는 쓰지 않고 `v4/` 오버레이에만 기록 | `lib/firebase/auth.ts`를 `v4/audit_logs`로 변경, 채팅·Rules 시뮬레이션 43/43 PASS |
| 일반 | 테스트 · 모바일 공통 UI | `sim-phase12`가 폐기된 IconBtn 전용 구현 문자열을 요구해 5건 실패 | 현재 SSOT인 IconSeg·PageActions·BottomNav·아이콘+라벨 구조 검증 | 회귀 가드를 현행 공통 컴포넌트 계약으로 갱신, 34/34 PASS |
| 일반 | 테스트 · 공급사 시트 | 헤더 자동탐지가 최소 3개 라벨을 요구하지만 fixture는 2개 라벨이라 실패 | 실제 표 형태와 동일한 유효 헤더를 사용 | fixture 보강, 시트 병합 20/20 PASS |

담당은 코드·테스트 모두 개발팀, 수정일과 재검수일은 2026-07-31이다. 운영자 정보 입력은 운영 책임자 확인이 필요하다.

### 요구사항별 결과

- UI·UX/디자인 시스템: 폰트 및 토큰 드리프트 0, 공통 모바일 패널·CRUD·툴바·목록복귀 회귀 가드 PASS.
- 핵심 사용자 시나리오: 영업자 39/39, 계약 Rules 25/25, 전자서명 57/57, 생애주기 PASS.
- 권한·보안·개인정보: 권한 44/44, 채팅 Rules 43/43, 공개/비공개 상품 분리 15/15,
  정산 private 분리 12/12, 감사 로그 v4 격리 PASS.
- 정산·데이터 정합성: 3자 정산 E2E 15/15, 차량 잠금 23/23, 시트 diff·병합·상태 매핑 PASS.
- 빌드·디자인 게이트: `typecheck`, `check:fonts`, `check:tokens`, 격리 production build 30개 route PASS.
- 웹 실화면: 로그인된 로컬 상품찾기에서 실데이터 346대 로딩, 접근성 이름 누락 0,
  1920px 화면 가로 오버플로 없음, 콘솔 warning/error 없음 확인.

### 웹·앱 규격 불일치 및 미검증 범위

- 이 저장소에서 확인되는 배포물은 Next.js 웹/PWA이며 별도 iOS·Android 네이티브 프로젝트는 확인되지 않았다.
  따라서 웹·iOS·Android 간 화면·푸시·백그라운드·업데이트 동작의 동등성은 판정하지 않았다.
- 실제 앱스토어·플레이스토어 메타데이터 및 심사 계정, 실결제·취소·환불 사업자 응답,
  대규모 동시접속, 네트워크 단절 복구, 장애 알림·롤백 훈련은 별도 운영 증빙이 필요하다.
- `database.rules.json`은 이번 검증에서 수정하거나 게시하지 않았다. 실제 게시 전에는 사람 또는 Claude가
  실데이터 역할별 write를 다시 확인해야 한다.

### 실행 근거

- `npm.cmd run typecheck`: PASS
- `npm.cmd run check:fonts`: PASS — 드리프트 0
- `npm.cmd run check:tokens`: PASS — 드리프트 0
- `scripts/sim-*.mts`: 전체 PASS
- `NEXT_DIST_DIR=.next-codex-final npm.cmd run build`: PASS — 30개 route
- `npm.cmd run check:release`: FAIL — 약관·개인정보 운영자 필수정보 6개 미기재
- 빌드가 임시로 변경한 `tsconfig.json`은 원상복구했다.

### 출시 전 필수 수동 게이트

1. `lib/legal.ts`의 운영자 필수정보 6개를 실제 정보로 입력하고 `npm run check:release`를 통과한다.
2. 현재 변경분을 검토·배포한 뒤 배포 SHA와 화면 버전이 일치하는지 확인한다.
3. 관리자·공급사·영업자 계정으로 회원 승인 변경 후 `v4/audit_logs` 생성 여부를 실데이터에서 확인한다.
4. Firebase Rules는 자동 게시하지 말고 역할별 정상 write/차단 시나리오를 실데이터로 확인한다.
5. 네이티브 앱이 별도 존재한다면 iOS·Android 실기기, 스토어 심사, 푸시·백그라운드 항목을 별도 완료한다.

---

## 2026-07-27 4역할 실계정·채팅 갱신·정산 격리 검증

### 판정

**PASS — 역할별 실제 금액 화면만 QA 정산 생성 후 최종 확인**

- 별도 QA 조직의 활성 계정 4종(영업채널 관리자·직원, 공급사 관리자·직원)으로 실제 Firebase Auth 로그인했다.
- 메뉴와 데이터 범위:
  - 영업채널 관리자는 채널 계약·채팅, 영업채널 직원은 본인 계약·채팅만 조회했다.
  - 공급사 관리자·직원은 자기 회사 재고·계약·채팅을 조회했다.
  - 반대 조직 코드 쿼리와 권한 밖 경로는 운영 RTDB Rules가 HTTP 401로 차단했다.
- QA 계약 `TMP-260727-01-yvjf`, 차량 `99하0727`, QA 문의방을 네 역할에서 확인했다.
- 공급사 직원→영업자, 영업자→공급사 메시지 송수신을 확인했다.
- 채팅 메시지 로딩 전 빈 대화 안내가 잠깐 보이던 상태를 로딩 상태로 분리했다.
- 방별 메시지 성공 결과를 영구 캐시하지 않고 동시 요청만 합치도록 변경했다.
- 열린 대화는 5초 주기·창 포커스·`fp:unread` 이벤트에 새 메시지를 다시 읽는다.
- 공급사 토큰으로 새 QA 메시지를 넣은 뒤 페이지 새로고침 없이 영업자 화면에 나타나는 것을 확인했다.
- 감사로그 쓰기/조회 경로를 운영 Rules가 허용하는 루트 `audit_logs`로 통일했고, 채팅 전송 시 새 `permission_denied` 경고 0건을 확인했다.
- 설정 페이지는 hydration 전에 localStorage 기반 `actor()`를 호출하지 않도록 바꿨고, 로그인 세션 이름 불일치 오류가 재발하지 않았다.

### 정산 private 운영 매트릭스

- 영업 직원의 자기 `settlements_agent_private` 쿼리: HTTP 200
- 영업채널 관리자의 자기 채널 `settlements_agent_private` 쿼리: HTTP 200
- 공급사 직원·관리자의 자기 회사 `settlements_provider_private` 쿼리: HTTP 200
- 영업→공급사 private, 공급사→영업 private, 네 역할→관리자 private: HTTP 401
- 전용 QA 정산 데이터는 아직 0건이므로 R1/R2 실제 숫자 화면 표시는 오픈 체크리스트에 잔여로 유지했다.

### 자동 검증

- `npm.cmd run typecheck`: PASS
- `npm.cmd run check:fonts`: PASS, 폰트 드리프트 0
- `scripts/sim-*.mts` 12개: 전부 PASS
- `NEXT_DIST_DIR=tmp/verification-build/chat-live-20260727 npm.cmd run build`: PASS
  - 28개 라우트
- 개발 서버 PID `30312`, 포트 4004 유지 및 HTTP 200

---

## 2026-07-26 — Inventory 목록 계산 훅 검증

### 검증 결과

- 180ms 디바운스 검색 계약 유지
- 상태·상품구분 필터 의미 유지
- 상태 미등록 값의 후순위 처리 유지
- 상태 동률 시 차명 정렬 유지
- 차명·차번·코드 정렬 유지
- 모바일 드래프트는 즉시 검색어를 사용해 미리보기 건수 계산 유지

### 전체 자동 검증

- typecheck: PASS
- build: PASS
- 전체 도메인 시뮬레이션 및 차량 마스터 검증: PASS

### 남은 위험

- Inventory 전용 UI 상호작용을 실제 브라우저에서 수동 검증하지 않았다.
- `/inventory` 라우트 크기가 18.5 kB에서 18.7 kB로 0.2 kB 증가했다.

### 최종 판정

**PASS**

---

## 2026-07-26 — Finder 결과 계산 훅 검증

### 검증 결과

- 기존 `useDeferredValue` 기반 무거운 필터 지연 유지
- 숨김 코드 제외와 패스 상품 후순위 배치 유지
- 최근·관심 합집합 필터 유지
- 명시 정렬과 기본 혜택 점수 정렬 유지
- 필터 드래프트 미리보기 건수 계산 유지
- 인기차종·카탈로그 노출 총계 유지
- 엑셀 열 필터와 숫자 정렬 유지

`app/page.tsx`는 최초 1,459줄에서 724줄로 줄었으며, 결과 계산은
`useFinderResults.ts` 183줄에 모였다.

### 전체 자동 검증

- typecheck: PASS
- build: PASS
- sim-agent: 37/37 PASS
- sim-lifecycle: PASS
- sim-e2e-settlement: 15/15 PASS
- sim-vehicle-lock: 23/23 PASS
- sim-sheet-merge: 12/12 PASS
- sim-phase12: 25/25 PASS
- verify-master-pass: PASS
- git diff --check: PASS

### 남은 위험

- 실제 브라우저에서 빠른 연속 필터 조작과 엑셀 팝오버를 수동 검증하지 않았다.
- `/` 라우트 크기는 리팩터링 전 16.4 kB에서 현재 17.1 kB로 소폭 증가했다.
  기능 모듈 경계와 타입 안정성을 얻은 대가지만, 추후 번들 분석 대상이다.

### 최종 판정

**PASS**

Finder 대형 파일의 우선 분리 목표는 달성했다. 추가 분리는 페이지 상태 훅까지
확장할 수 있으나, 현재 724줄에서 위험 대비 효율을 다시 판단하는 것이 적절하다.

---

## 2026-07-26 — Finder 데이터 로딩 훅 검증

### 검증 결과

- `peekList` 기반 동기 초기값 유지
- Firebase 사용 시 `authReady` 이후 조회 유지
- 로그인 사용자 변경 시 재조회 유지
- 상품·공급사 병렬 조회 및 공급사명 결합 유지
- 15초 타임아웃과 실패 시 빈 목록 처리 유지
- 숨김·패스 목록의 초기 로드와 구독 해제 유지
- 보기 설정 복원을 데이터 조회와 분리해 인증 재조회 시 반복 부작용 제거

### 전체 자동 검증

- typecheck: PASS
- build: PASS
- sim-agent: 37/37 PASS
- sim-lifecycle: PASS
- sim-e2e-settlement: 15/15 PASS
- sim-vehicle-lock: 23/23 PASS
- sim-sheet-merge: 12/12 PASS
- sim-phase12: 25/25 PASS
- verify-master-pass: PASS

### 최종 판정

**PASS**

다음 단계는 검색·필터·정렬 파생 계산을 `useFinderResults`로 이동하는 작업이다.

---

## 2026-07-26 — Finder 필터 패널 분리 검증

### 요구사항

- Finder의 대형 인라인 필터 패널을 독립 컴포넌트로 이동한다.
- UI, 필터 의미, 드래프트 적용·취소, 모바일 동작을 변경하지 않는다.
- props 전달 자체가 새로운 복잡도가 되지 않도록 명확한 경계를 만든다.

### 검증 결과

| 항목 | 결과 |
|---|---|
| 필터 패널 독립 컴포넌트 이동 | PASS |
| 페이지의 필터 상태 소유권 유지 | PASS |
| 드래프트/라이브 `bump()` 계약 유지 | PASS |
| 최근·관심 목록과 필터 스냅 동기화 유지 | PASS |
| 데스크톱·모바일 공통 패널 유지 | PASS |
| 정렬 라벨·기본 열림·초기화 동작 유지 | PASS |

`FinderFilterPanelModel` 하나가 패널의 표시 모델과 명령을 묶으며, 페이지 내부 상태
setter를 개별적으로 노출하지 않는다. `app/page.tsx`는 1,042줄에서 858줄로 줄었다.

### 전체 자동 검증

- typecheck: PASS
- build: PASS, 26개 페이지 생성
- sim-agent: 37/37 PASS
- sim-lifecycle: PASS
- sim-e2e-settlement: 15/15 PASS
- sim-vehicle-lock: 23/23 PASS
- sim-sheet-merge: 12/12 PASS
- sim-phase12: 25/25 PASS
- verify-master-pass: PASS
- git diff --check: PASS

### 남은 위험

- 실제 브라우저에서 필터 패널의 접힘·스크롤·모바일 하단시트를 수동 시각 검증하지 않았다.
- 다음 구조 작업은 데이터 로딩과 검색·필터 결과 계산 훅 분리다.

### 최종 판정

**PASS**

---

## 2026-07-26 — Cursor 엑셀 결과 테이블 분리 검증

### 검증 기준

원래 사용자 요구사항:

- `app/page.tsx`의 대형 단일 파일을 기능 변경 없이 단계적으로 분리한다.
- 엑셀 결과 테이블을 `features/finder` 아래 독립 컴포넌트로 이동한다.
- UI, 필터 의미, 정렬, 열 너비, 모바일 동작을 유지한다.
- 다른 AI가 이어받을 수 있도록 구현·검증 문서를 갱신한다.

별도 `PLAN.md`는 없으므로 원래 요구사항, `docs/REFACTOR_PROGRESS.md`,
`IMPLEMENTATION_LOG.md`, 실제 diff를 함께 비교했다.

### 구현 대조 결과

| 항목 | 결과 | 근거 |
|---|---|---|
| 엑셀 테이블 분리 | PASS | `ExcelResultsTable.tsx`로 테이블·헤더·팝오버 조립 이동 |
| 필터 의미 유지 | PASS | 표시 행과 팝오버 후보 행을 기존처럼 `rows`/`list`로 분리 |
| 정렬 유지 | PASS | 가격·연식·주행거리 숫자 정렬 함수 유지 |
| 열 순서·너비 유지 | PASS | 기존 `EXCEL_*`, `colLock*`, `excel*Chars` 계산 그대로 이동 |
| 행 동작 유지 | PASS | 상세 이동·컨텍스트 메뉴를 기존 페이지 콜백에 위임 |
| 모바일 동작 유지 | PASS | 기존 `useIsMobile()` 기반 `is-fit` 조건 유지 |
| 인수인계 문서 | PASS | 구현 로그와 리팩터링 진행 문서 갱신 |

`app/page.tsx`는 최초 1,459줄에서 현재 1,042줄로 축소됐다.

### 독립 검증 중 발견·수정한 문제

`scripts/sim-vehicle-lock.mts`가 공급사 전용 단계를 영업자 역할로 호출해 전체
시뮬레이션 중 1건이 실패했다. 제품 엔진의 역할 차단은 올바르며 테스트 하네스가
현재 권한 정책을 반영하지 못한 문제였다.

수정 내용:

- 테스트용 `asRole()` 헬퍼 추가
- `provider_balance_confirmed`, `provider_delivery_response` 실행 시 공급사 역할 적용
- 실행 후 이전 역할을 항상 복원

제품 코드나 권한 정책은 완화하지 않았다.

### 전체 검증 결과

- `npm.cmd run typecheck`: PASS
- `npm.cmd run build`: PASS, 26개 페이지 생성
- `scripts/sim-agent.mts`: 37/37 PASS
- `scripts/sim-lifecycle.mts`: PASS
- `scripts/sim-e2e-settlement.mts`: 15/15 PASS
- `scripts/sim-vehicle-lock.mts`: 23/23 PASS
- `scripts/sim-sheet-merge.mts`: 12/12 PASS
- `scripts/sim-phase12.mts`: 25/25 PASS
- `scripts/verify-master-pass.mts`: PASS, 마스터 ID 1,800개 검증
- `git diff --check`: PASS

---

## 2026-07-26 — 4개 업무 화면 UI/UX 배치 검증

### 판정

**PASS**

- 설정 관심·숨김 관리, 회원 목록, 계약 정산 요약, 채팅 방 목록을 feature 컴포넌트로 분리했다.
- 기존 역할별 표시, 빈 상태, 복구·초기화 액션, 정산 R1/R2 표시 규칙을 유지했다.

### 실행 결과

- typecheck: PASS
- 전체 7개 시뮬레이션·검증 스크립트: PASS
- 설정·회원·계약·채팅: 모두 HTTP 200
- `git diff --check`: PASS
- production build: 서버 유지 요청으로 보류

---

## 2026-07-26 — RTDB 어댑터 데이터 계층 분리 검증

### 판정

**PASS**

- 레코드 변환과 상품 보안·중복 정책을 네트워크 어댑터에서 분리했다.
- 첨부 URL 이름 복원, 엔티티 자연키, 정책·계약 조인 결과를 유지했다.
- 공급 원가는 관리자 또는 본인 소유 공급사에만 보이는 정책을 유지했다.
- 카슝 제외 및 차량번호/VIN 실차 중복 제거 우선순위를 유지했다.

### 실행 결과

- typecheck: PASS
- 전체 7개 시뮬레이션·검증 스크립트: PASS
- 주요 6개 화면: 모두 HTTP 200
- `git diff --check`: PASS

---

## 2026-07-26 — 회원 관리 필터·정렬 분리 검증

### 판정

**PASS**

- 사용자·파트너 목록 필터와 정렬을 순수 모듈로 이동했다.
- 승인대기는 정렬 선택과 관계없이 최상단에 놓이는 규칙을 유지했다.
- 승인 상태와 운영 활성 상태를 별도 필드로 판정하는 규칙을 유지했다.

### 실행 결과

- typecheck: PASS
- 전체 7개 시뮬레이션·검증 스크립트: PASS
- 개발 서버 `/members`: HTTP 200
- `git diff --check`: PASS

---

## 2026-07-26 — 계약 목록 필터·정렬 분리 검증

### 판정

**PASS**

- 계약 필터·정렬·월 옵션을 순수 모듈로 이동했다.
- 진행 기본값의 완료 포함·취소 제외 규칙과 진행률 동률 시 최근 계약 우선을 유지했다.
- 누락된 월 라벨 공개 연결 1건을 타입 검사에서 발견해 수정했다.

### 실행 결과

- typecheck: PASS
- 전체 7개 시뮬레이션·검증 스크립트: PASS
- 개발 서버 `/contract`: HTTP 200
- `git diff --check`: PASS

---

## 2026-07-26 — 채팅 목록 필터·정렬 분리 검증

### 판정

**PASS**

- 역할별 미확인·문의·완료·취소 판정과 정렬을 순수 모듈로 이동했다.
- 안읽음 동률 시 최근 메시지 우선과 드래프트 미리보기 집계를 유지했다.

### 실행 결과

- typecheck: PASS
- 전체 7개 시뮬레이션·검증 스크립트: PASS
- 개발 서버 `/chat`: HTTP 200
- `git diff --check`: PASS

---

## 2026-07-26 — 채팅 방 색인·표시 분리 검증

### 판정

**PASS**

- 진행 계약과 취소 계약 색인을 분리한 기존 first-wins 규칙을 유지했다.
- product_code, product_uid, product_id, 차량번호 탐색과 삭제 차량 fallback을 유지했다.
- 관리자·공급사·영업 역할별 헤더 계산 연결을 유지했다.

### 실행 결과

- typecheck: PASS
- 전체 7개 시뮬레이션·검증 스크립트: PASS
- 개발 서버 `/chat`: HTTP 200
- `git diff --check`: PASS

---

## 2026-07-26 — Finder 결과 본문 분리 검증

### 판정

**PASS**

- 빈 상태, 카드·상세·엑셀 결과와 페이징 UI를 `FinderResults`로 이동했다.
- 상품 열기·우클릭, Excel 필터/정렬 상태, 500대 표시 상한 연결을 유지했다.

### 실행 결과

- typecheck: PASS
- 전체 7개 시뮬레이션·검증 스크립트: PASS
- 개발 서버 `/`: HTTP 200
- `git diff --check`: PASS

---

## 2026-07-26 — Finder 상단 툴바 분리 검증

### 판정

**PASS**

- 모바일·웹 툴바를 `FinderToolbar`로 이동했다.
- 검색, 정렬, 필터 뱃지, 관심함, 엑셀 다운로드, 보기 전환 연결을 유지했다.

### 실행 결과

- typecheck: PASS
- 전체 7개 시뮬레이션·검증 스크립트: PASS
- 개발 서버 `/`: HTTP 200
- `git diff --check`: PASS

---

## 2026-07-26 — Finder 상품 컨텍스트 분리 검증

### 판정

**PASS**

- 상품 우클릭 액션 구성을 Finder 페이지에서 독립 모듈로 이동했다.
- 역할별 계약문의·공유 노출, 링크·내용 복사, 상세 이동을 유지했다.
- 복사 실패 fallback은 공통 오류 알림으로 현대화했다.

### 실행 결과

- typecheck: PASS
- 전체 7개 시뮬레이션·검증 스크립트: PASS
- 개발 서버 `/`: HTTP 200
- `git diff --check`: PASS

---

## 2026-07-26 — 정확 경로·감사·일괄 변환 분리 검증

### 판정

**PASS**

- 정확 경로 엔진과 운영 감사·일괄 변환 엔진을 별도 모듈로 이동했다.
- 기존 API는 얇은 호환 진입점으로 유지했다.
- 의존성 주입으로 매칭 본체와 운영 모듈 사이 순환 import가 없다.
- exact 경로, confidence 집계, 샘플 제한, auto/all 정책을 보존했다.

### 실행 결과

- typecheck: PASS
- 전체 7개 시뮬레이션·검증 스크립트: PASS
- `verify-master-pass`: PASS
- 개발 서버 `/inventory`: HTTP 200
- `git diff --check`: PASS

---

## 2026-07-26 — 차량 신호·선택 보조 분리 검증

### 판정

**PASS**

- 원본 차량 신호 수집과 재스냅 입력 복원을 독립 모듈로 이동했다.
- 파워트레인 라벨·인승 분기·실트림 판정을 독립 모듈로 이동했다.
- 기존 export 호환과 매칭 알고리즘 결과를 유지했다.

### 실행 결과

- typecheck: PASS
- 전체 7개 시뮬레이션·검증 스크립트: PASS
- `verify-master-pass`: PASS
- 개발 서버 `/inventory`: HTTP 200
- `git diff --check`: PASS

---

## 2026-07-26 — 차량 마스터 필터·탐색 분리 검증

### 판정

**PASS**

- 차량 5단 필터와 마스터 제조사·모델·세부모델 탐색을 leaf 모듈로 이동했다.
- 르노 표기 호환, 국산 우선 그룹, 한글 정렬, 기존 공개 import 경로를 유지했다.

### 실행 결과

- typecheck: PASS
- 전체 7개 시뮬레이션·검증 스크립트: PASS
- `verify-master-pass`: PASS
- 개발 서버 `/inventory`: HTTP 200
- `git diff --check`: PASS

---

## 2026-07-26 — 차량 마스터 스냅 추적 분리 검증

### 판정

**PASS**

- 추적 키·라벨, 원본 캡처, 필드 diff, 이력 생성을 leaf 모듈로 이동했다.
- 최초 원본 유지, 변경 필드만 기록, 최근 10건 제한을 보존했다.
- `applySnap`의 업무 반영 정책과 기존 공개 import 경로는 변경하지 않았다.

### 실행 결과

- typecheck: PASS
- 전체 7개 시뮬레이션·검증 스크립트: PASS
- `verify-master-pass`: PASS
- 개발 서버 `/inventory`: HTTP 200
- `git diff --check`: PASS

---

## 2026-07-26 — 차량 마스터 표시·정규화 분리 검증

### 판정

**PASS**

- 연식·연료·제조사 표시와 연료 배기량 추출을 독립 모듈로 이동했다.
- 기존 공개 import 경로와 반환 규칙을 유지했다.
- 매칭 본체의 연료 정규화도 동일 leaf 모듈을 사용해 규칙 중복이 없다.

### 실행 결과

- typecheck: PASS
- 전체 7개 시뮬레이션·검증 스크립트: PASS
- `verify-master-pass`: PASS
- 개발 서버 `/inventory`: HTTP 200
- `git diff --check`: PASS

---

## 2026-07-26 — 클립보드 공통화 검증

**PASS**

- `copyText`가 최신 Clipboard API 실패 시 호환 fallback을 수행한다.
- 임시 textarea는 성공·실패 모두 제거된다.
- 계약 링크와 재고 TSV 복사 실패가 오류로 표시된다.
- typecheck·전체 시뮬레이션 PASS
- 개발 서버 `/inventory`: HTTP 200
- 2차 적용 후 앱·컴포넌트의 직접 Clipboard API 호출 0건
- 실패 시 기존 prompt 또는 오류 toast fallback 유지

---

## 2026-07-26 — Cursor 가격·혜택 원자 분리 독립 검증

### 판정

**PASS (BOM 수정 후)**

- `product-card-pricing.tsx`: 가격 컨텍스트·기간·요금 UI 319줄
- `product-card-perks.tsx`: 혜택·이벤트·조건 UI 158줄
- `product-card-atoms.tsx`: 725줄 → 271줄
- pricing → perks → badges 단방향이며 barrel 역참조가 없어 순환 의존성 없음
- 기존 `product-card-atoms` export 경로 유지

### 발견 및 수정

- Cursor 저장 과정에서 `product-card-atoms.tsx` 시작에 UTF-8 BOM이 추가됨
- BOM 제거 완료

### 검증

- typecheck: PASS
- 전체 시뮬레이션·차량 마스터 검증: PASS
- 개발 서버 `/inventory`: HTTP 200
- `git diff --check`: PASS
- production build: 서버 유지 요청으로 보류

---

## 2026-07-26 — 공통 확인창·알림 전환 검증

### 판정

**PASS**

- 계약, 정책, 회원, 재고, 개발 도구의 파괴적/중요 작업이 공통 `confirmDialog`를 사용한다.
- 로그인/가입 오류는 브라우저 기본 alert 대신 공통 error toast를 사용한다.
- `app`, `components`, `features` 범위에 직접 `window.confirm`·`window.alert` 호출이 남지 않았다.

### 실행 결과

- `npm run typecheck`: PASS
- `sim-agent`, `sim-lifecycle`, `sim-e2e-settlement`, `sim-vehicle-lock`, `sim-sheet-merge`, `sim-phase12`, `verify-master-pass`: 모두 PASS
- 개발 서버 `/inventory`: HTTP 200
- `git diff --check`: PASS
- production build: 실행 중인 개발 서버의 `.next` 충돌 방지를 위해 보류

로컬 시뮬레이션 중 Firebase 환경변수 누락 메시지는 기존 예상 경고이며 실패가 아니다.

---

## 2026-07-26 — 공통 UI 오버레이 분리 검증

### 판정

**PASS**

- `Drawer`, `Modal`을 `components/ui/overlays.tsx`로 이동했다.
- 기존 `@/components/ui` import 경로와 컴포넌트 API를 유지했다.
- 오버레이의 키보드 이동, 닫기, 모바일 풀스크린 표현 로직을 보존했다.

### 실행 결과

- typecheck: PASS
- 전체 7개 시뮬레이션·검증 스크립트: PASS
- 개발 서버 `/inventory`: HTTP 200
- `git diff --check`: PASS
- production build: 실행 중인 개발 서버 보호를 위해 보류

---

## 2026-07-26 — 차량 마스터 타입 분리 검증

### 판정

**PASS**

- 순수 도메인 타입 7종을 별도 파일로 이동했다.
- 기존 `vehicle-master-match.ts`의 type re-export를 유지해 모든 소비자와 스크립트가 호환된다.
- 실행 로직과 데이터 변환 결과에는 변화가 없다.

### 실행 결과

- typecheck: PASS
- 전체 7개 시뮬레이션·검증 스크립트: PASS
- `verify-master-pass`: PASS
- 개발 서버 `/inventory`: HTTP 200
- `git diff --check`: PASS

---

## 2026-07-26 — 공통 UI 레이아웃 분리 검증

### 판정

**PASS**

- 레이아웃 원자 4종을 `components/ui/layout.tsx`로 이동했다.
- 공개 API, 스타일 토큰, 모바일 규격, VSplit 이벤트 정리·localStorage 동작을 보존했다.

### 실행 결과

- typecheck: PASS
- 전체 7개 시뮬레이션·검증 스크립트: PASS
- 개발 서버 `/inventory`: HTTP 200
- `git diff --check`: PASS
- production build: 실행 중인 개발 서버 보호를 위해 보류

---

## 2026-07-26 — 공통 UI 버튼 분리 검증

### 판정

**PASS**

- `Btn`, `IconBtn`, `IconSeg`를 `components/ui/buttons.tsx`로 이동했다.
- 기존 공개 API, 모바일 높이·패딩, disabled·접근성 속성, 스타일 오버라이드를 유지했다.
- leaf 버튼 모듈은 tokens와 모바일 훅만 참조하며 barrel 순환 의존성이 없다.

### 실행 결과

- typecheck: PASS
- 전체 7개 시뮬레이션·검증 스크립트: PASS
- 개발 서버 `/inventory`: HTTP 200
- `git diff --check`: PASS
- production build: 실행 중인 개발 서버 보호를 위해 보류

---

## 2026-07-26 — 공통 UI 폼 입력 분리 검증

### 판정

**PASS**

- 기본 폼 입력 4종을 `components/ui/form-controls.tsx`로 이동했다.
- 공개 API와 모바일 입력 규격, 키보드·포커스 동작을 보존했다.

### 실행 결과

- typecheck: PASS
- 전체 7개 시뮬레이션·검증 스크립트: PASS
- 개발 서버 `/inventory`: HTTP 200
- `git diff --check`: PASS
- production build: 실행 중인 개발 서버 보호를 위해 보류

---

## 2026-07-26 — 공통 UI 리스트 분리 검증

### 판정

**PASS**

- `ListRow`, `ListBox`를 `components/ui/list.tsx`로 이동했다.
- 기존 공개 import 경로, props, 스타일 토큰 사용을 유지했다.

### 실행 결과

- typecheck: PASS
- 전체 7개 시뮬레이션·검증 스크립트: PASS
- 개발 서버 `/inventory`: HTTP 200
- `git diff --check`: PASS
- production build: 실행 중인 개발 서버 보호를 위해 보류

Firebase 환경변수 누락 메시지는 localStorage 시뮬레이션에서 발생하는 기존 경고이며
검증 실패가 아니다.

### 남은 위험

- 실제 브라우저에서 엑셀 헤더 팝오버 위치와 가로 스크롤을 수동 시각 검증하지 않았다.
- `app/page.tsx`는 여전히 1,042줄이므로 필터 패널과 결과 계산 훅 분리가 남아 있다.
- `.claude/`는 이번 검증에서 생성·수정하지 않은 별도 미추적 디렉터리다.

### 최종 판정

**PASS**

Cursor의 엑셀 결과 테이블 분리는 원래 요구사항과 일치한다. 독립 검증에서 발견한
테스트 하네스 불일치도 수정했으며, 전체 자동 검증이 통과한다.

---

## 2026-07-26 — Finder 구조 분리

### 검증 기준

원래 사용자 요구사항:

- 대형 단일 파일을 분리한다.
- 다른 AI가 이어받을 수 있도록 진행 내용을 문서화한다.
- 기존 기능과 사용자 동작을 깨뜨리지 않는다.

별도의 Claude Code `PLAN.md`는 없었다. 따라서 사용자의 원래 요구사항,
`docs/REFACTOR_PROGRESS.md`, 실제 변경사항을 기준으로 검증했다.

### 요구사항 충족 여부

| 요구사항 | 결과 | 근거 |
|---|---|---|
| 대형 Finder 파일 분리 | PASS | 1,459줄에서 1,213줄로 축소, 3개 모듈로 책임 이동 |
| 인수인계 문서화 | PASS | 협업 규칙, 진행 메모, 구현 로그, 검증 문서 추가 |
| 동작 호환성 유지 | PASS | 타입 검사, 빌드, 핵심 시뮬레이션 통과 |

### 실제 변경

- `features/finder/filter-state.ts`
- `features/finder/excel-columns.ts`
- `features/finder/ExcelFilterPopover.tsx`
- `app/page.tsx`에서 위 책임 제거 및 import로 교체
- `docs/AI_COLLABORATION.md`
- `docs/REFACTOR_PROGRESS.md`
- `IMPLEMENTATION_LOG.md`
- `HANDOFF.md`

### 실행 결과

- `npm.cmd run typecheck`: PASS
- `npm.cmd run build`: PASS, 26개 페이지 생성
- `npx.cmd tsx scripts/sim-agent.mts`: 37/37 PASS
- `npx.cmd tsx scripts/sim-phase12.mts`: 25/25 PASS
- `git diff --check`: PASS

### 회귀·보안·데이터 호환성

- Firebase 데이터 경로와 보안 규칙은 변경하지 않았다.
- `FilterBag` 필드와 sessionStorage 키를 유지했다.
- 엑셀 필터 다중값 OR 의미와 숫자 정렬 방식을 유지했다.
- 팝오버의 검색, 선택, 초기화, 정렬, 닫기 동작을 유지했다.
- 프로덕션 번들의 `/` 크기는 변경 전과 동일한 16.4 kB다.

### 남은 위험

- 브라우저에서 실제 팝오버 위치와 클릭 동작을 수동 시각 검증하지 않았다.
- `app/page.tsx`는 여전히 1,213줄이므로 추가 분리가 필요하다.
- Firebase 환경변수가 없는 localStorage 시뮬레이션에서는 기존 경고가 출력된다.
- 당시 `scripts/sim-vehicle-lock.mts` 역할 기대 불일치가 남아 있었으나, 위의 최신
  검증 단계에서 테스트 역할 설정을 수정해 23/23 통과로 해소했다.

### 최종 판정

**PASS**

현재 단계의 구조 분리는 원래 요구사항을 충족하며, 자동 검증 범위에서 회귀가 없다.

---

## 2026-07-26 — Inventory 편집 패널 분리 독립 검증

### 판정

**PASS**

`app/inventory/page.tsx`의 기본정보·운영정보 UI를
`features/inventory/InventoryEditorPanes.tsx`로 이동했다. 페이지에는 저장, OCR,
차종 마스터 적용과 같은 도메인 조정 로직을 남겨 UI 분리로 동작 계약이 바뀌지 않게 했다.

### 실행 결과

- `npm.cmd run typecheck`: PASS
- `scripts/sim-agent.mts`: 37/37 PASS
- `scripts/sim-lifecycle.mts`: PASS
- `scripts/sim-e2e-settlement.mts`: 15/15 PASS
- `scripts/sim-vehicle-lock.mts`: 23/23 PASS
- `scripts/sim-sheet-merge.mts`: 12/12 PASS
- `scripts/sim-phase12.mts`: 25/25 PASS
- `scripts/verify-master-pass.mts`: PASS
- `npm.cmd run build`: PASS, 정적 페이지 26개 생성

### 독립 확인 사항

- 선택 여부, 신규/수정 모드, dirty 상태가 모델을 통해 그대로 전달된다.
- 마스터 선택 결과의 `applySnap(..., source: 'picker')` 처리는 페이지에 보존됐다.
- 가격·사진 변경은 기존처럼 폼 갱신과 dirty 설정을 함께 수행한다.
- 관리자 전용 필드와 공급사 정책 범위 필터가 유지됐다.
- Firebase 환경변수 누락 문구는 localStorage 시뮬레이션의 기존 경고다.

### 남은 위험

- 실제 브라우저에서 OCR 파일 선택, 사진 길게 누르기, 마스터 피커를 수동 검증하지 않았다.
- `/inventory` 라우트 크기가 18.8 kB에서 19.1 kB로 0.3 kB 증가했다.
  구조 분리 결과이며, 동적 로딩을 포함한 번들 최적화는 별도 후속 작업으로 남긴다.

---

## 2026-07-26 — Inventory OCR·차종 마스터 훅 분리 검증

### 판정

**PASS**

OCR와 차량 마스터 처리 책임을 `useInventoryVehicleTools.ts`로 이동했고,
`app/inventory/page.tsx`는 643줄에서 496줄로 축소됐다.

### 회귀 확인

- 선택 generation 가드 유지: 늦게 끝난 이전 선택 작업이 현재 폼을 덮지 않는다.
- exact 경로 및 high/medium 신뢰도 조건을 만족할 때만 자동 DB 패치한다.
- 색상 패치와 마스터 패치 모두 목록 캐시를 함께 갱신한다.
- 사용자 재매칭은 `source: 'manual'`, 피커 선택은 `source: 'picker'`를 유지한다.
- OCR는 `/api/ocr/extract` 계약과 빈 칸 우선 병합을 유지한다.

### 실행 결과

- `npm.cmd run typecheck`: PASS
- `scripts/sim-agent.mts`: 37/37 PASS
- `scripts/sim-lifecycle.mts`: PASS
- `scripts/sim-e2e-settlement.mts`: 15/15 PASS
- `scripts/sim-vehicle-lock.mts`: 23/23 PASS
- `scripts/sim-sheet-merge.mts`: 12/12 PASS
- `scripts/sim-phase12.mts`: 25/25 PASS
- `scripts/verify-master-pass.mts`: PASS
- `npm.cmd run build`: PASS, 26개 페이지

### 남은 위험

- OCR와 마스터 피커의 실제 브라우저 파일·클릭 흐름은 수동 검증하지 않았다.
- `/inventory` 라우트는 19.2 kB로 직전보다 0.1 kB 증가했다.

---

## 2026-07-26 — Inventory 편집 수명주기 분리 검증

### 판정

**PASS**

저장·삭제·편집 상태 전환을 `useInventoryEditorLifecycle.ts`로 분리했다.
페이지는 496줄에서 386줄로 축소됐다.

### 독립 확인 사항

- 공급사 소유권 검증과 귀속 코드 강제가 유지됐다.
- 차량번호 중복 검사에서 공백 제거 및 자기 상품 제외 조건이 유지됐다.
- `vehicleLockedBy` 결과가 저장 폼 상태보다 우선하며 계약 코드도 함께 각인된다.
- 삭제 전 `blockingContractFor`로 진행 계약 전체를 검사한다.
- 저장 실패 시 dirty 상태를 해제하지 않는다.
- 신규 취소는 선택 해제, 기존 수정 취소는 목록 원본 복원으로 동작한다.

### 실행 결과

- `npm.cmd run typecheck`: PASS
- `scripts/sim-agent.mts`: 37/37 PASS
- `scripts/sim-lifecycle.mts`: PASS
- `scripts/sim-e2e-settlement.mts`: 15/15 PASS
- `scripts/sim-vehicle-lock.mts`: 23/23 PASS
- `scripts/sim-sheet-merge.mts`: 12/12 PASS
- `scripts/sim-phase12.mts`: 25/25 PASS
- `scripts/verify-master-pass.mts`: PASS
- `npm.cmd run build`: PASS, 26개 페이지

### 남은 위험

- 저장 확인 알림과 삭제 확인창은 실제 브라우저에서 수동 검증하지 않았다.
- `/inventory` 라우트가 19.5 kB로 직전 대비 0.3 kB 증가했다.

---

## 2026-07-26 — Inventory 데이터·권한 초기화 분리 검증

### 판정

**PASS**

상품 로딩과 접근 제어 이벤트를 `useInventoryData.ts`로 이동했다.
`app/inventory/page.tsx`는 386줄에서 342줄로 축소됐다.

### 독립 확인 사항

- 상품과 파트너를 병렬 조회한 후 공급사 이름을 결합하는 순서를 유지했다.
- 공급사 역할의 `provider_company_code` 범위 제한을 유지했다.
- 최초 진입에서 시드·권한·정책·상품 순으로 준비한다.
- 마스터 로딩과 데스크톱 첫 상품 선택은 비동기로 실행한다.
- 역할 이벤트 리스너는 최신 선택 콜백을 ref로 참조해 불필요한 재등록을 방지한다.
- 모바일 역할 전환에서는 첫 상품을 자동 선택하지 않는다.

### 실행 결과

- `npm.cmd run typecheck`: PASS
- `scripts/sim-agent.mts`: 37/37 PASS
- `scripts/sim-lifecycle.mts`: PASS
- `scripts/sim-e2e-settlement.mts`: 15/15 PASS
- `scripts/sim-vehicle-lock.mts`: 23/23 PASS
- `scripts/sim-sheet-merge.mts`: 12/12 PASS
- `scripts/sim-phase12.mts`: 25/25 PASS
- `scripts/verify-master-pass.mts`: PASS
- `npm.cmd run build`: PASS, 26개 페이지

### 남은 위험

- 역할 변경과 모바일/데스크톱 첫 선택 차이는 실제 브라우저에서 수동 검증하지 않았다.
- `/inventory` 라우트가 19.7 kB로 직전 대비 0.2 kB 증가했다.

---

## 2026-07-26 — Inventory 런타임·번들 후속 검증

### 판정

**자동 검증 PASS / 브라우저 수동 검증 보류**

### 확인 내용

- 실행 중인 로컬 서버의 `http://localhost:4004/inventory`: HTTP 200
- 응답 본문: 19,887 bytes
- production build의 `/inventory`: 19.7 kB, First Load JS 272 kB
- route 전용 JavaScript 파일: 51,983 bytes
- 공급사 시트와 차량 마스터 로더의 기존 지연 로딩 유지

### 번들 판단

구조 분리 과정의 소폭 증가는 새 대형 라이브러리 유입으로 확인되지 않았다.
현재 크기만 줄이기 위해 훅을 다시 합치지 않는다. 이후 최적화는 실제 사용자 성능 측정이나
명확한 예산 기준이 생겼을 때 동적 패널 로딩 등으로 별도 진행한다.

### 보류 사유

브라우저 제어를 연결하려 했으나 사용 가능한 브라우저 세션이 없었다. 따라서 다음 항목은
수동 확인이 남아 있다.

- 데스크톱 첫 상품 자동 선택
- 모바일 목록 우선 진입
- OCR 파일 선택 및 완료 표시
- 마스터 피커와 재매칭
- 사진 탭·길게 누르기
- 역할 변경 시 권한 게이트와 선택 초기화

---

## 2026-07-26 — Product card 옵션 원자 분리 검증

### 판정

**PASS**

옵션 파싱과 칩 UI를 `product-card-options.tsx`로 이동했다. 기존 barrel 역할을 하는
`product-card-atoms.tsx`에서 같은 이름을 재수출하므로 소비자 import 계약은 바뀌지 않았다.

### 확인 사항

- 빈 옵션의 `옵션미입력` 표시 유지
- 기본 카드의 최대 2개 표시와 말줄임 유지
- 엑셀 결과의 2줄 높이·간격 상수 유지
- 상세 화면의 전체 wrap 표시 유지
- ResizeObserver 해제 처리 유지

### 실행 결과

- `npm.cmd run typecheck`: PASS
- 전체 7개 시뮬레이션·검증 스크립트: PASS
- `npm.cmd run build`: PASS, 26개 페이지
- `/`, `/inventory` 번들 표기 변화 없음

### 남은 작업

`product-card-atoms.tsx`는 1,037줄이다. 다음 안전한 경계는 가격 컨텍스트·기간 칩·요금
표시 묶음이다.

---

## 2026-07-26 — Product card 뱃지 원자 분리 검증

### 판정

**PASS**

뱃지 계산과 렌더링을 `product-card-badges.tsx`로 이동했다. 기존 파일은 내부 사용을
명시적으로 import하고 동일 이름을 재수출하므로 순환 의존성과 소비자 변경이 없다.

### 확인 사항

- 고객 화면에 차량 상태가 노출되지 않는 audience 게이트 유지
- 상태·상품·심사 순서 및 tone 유지
- 계약중 pulse와 solid variant 유지
- 축약 라벨 역변환을 포함한 tooltip 유지
- 혜택별 설명과 연령 숫자 설명 유지

### 실행 결과

- `npm.cmd run typecheck`: PASS
- 전체 7개 시뮬레이션·검증 스크립트: PASS
- `npm.cmd run build`: PASS, 26개 페이지
- 주요 route 번들 표기 변화 없음

### 다음 경계

`product-card-atoms.tsx`는 929줄이다. 가격 컨텍스트와 기간·요금 표시 묶음을 다음
분리 대상으로 유지한다.

---

## 2026-07-26 — Product card 기간별 요금 분리 검증

### 판정

**자동 검증 PASS / production build 보류**

`PriceMini`와 `PriceFare` 묶음을 `product-card-fares.tsx`로 이동했고 기존 공개 경로를
재수출로 유지했다. `product-card-atoms.tsx`는 929줄에서 843줄로 축소됐다.

### 실행 결과

- `npm.cmd run typecheck`: PASS
- 전체 7개 시뮬레이션·검증 스크립트: PASS
- 개발 서버 `/inventory`: HTTP 200
- production build: 실행하지 않음

### 빌드 보류 이유

Next 개발 서버와 production build가 같은 `.next`를 사용하면 실행 중 서버가 500으로
깨질 수 있다. 사용자의 서버 유지 요청에 따라 작업 중에는 build를 실행하지 않고,
서버를 내려도 되는 시점에 최종 build를 수행한다.

---

## 2026-07-26 — Product card 차량 신원·제원 분리 검증

### 판정

**자동 검증 PASS / 서버 유지**

- `idParts`, `idMobile`, `cardTitle`, `specLine`, `specLineCard`를
  `product-card-identity.ts`로 이동했다.
- 제조사 표준 표시, 무트림 라벨 제외, 연료 내장 배기량 fallback을 유지했다.
- 카드 고정 슬롯의 누락값 `-` 표시를 유지했다.
- 기존 `product-card-atoms` import 경로를 유지했다.

### 실행 결과

- typecheck: PASS
- 전체 7개 시뮬레이션·검증 스크립트: PASS
- 개발 서버 `/inventory`: HTTP 200
- `git diff --check`: PASS
- production build: 서버 유지 요청으로 보류

---

## 2026-07-26 — Product card 뱃지 UI 분리

**PASS**

- 상품구분 뱃지와 상태·상품·심사 레일을 `product-card-badge-view.tsx`로 이동
- audience 게이트, 표시 순서, pulse, dense 폭 유지
- typecheck·전체 시뮬레이션 PASS
- 개발 서버 `/inventory`: HTTP 200
- `git diff --check`: PASS

---

## 2026-07-26 — Product card 신원 UI 분리 검증

### 판정

**PASS**

- `Plate`, `CardTitle`을 `product-card-identity-view.tsx`로 이동했다.
- 폰트, 말줄임, tooltip, 차량번호 monospace 표현을 유지했다.
- 순수 문자열 모듈이 UI 컴포넌트를 import하지 않는 방향을 유지했다.
- 기존 공개 import 경로는 재수출로 호환된다.

### 실행 결과

- typecheck: PASS
- 전체 시뮬레이션·차량 마스터 검증: PASS
- 개발 서버 `/inventory`: HTTP 200
- production build: 서버 유지 요청으로 보류

---

## 2026-07-26 — 전자서명 링크 만료·폐기 검증

### 판정

**자동 검증 PASS / Firebase 배포 검증 보류**

- 새 링크는 기본 7일 만료 시각을 가진다.
- 유효 링크 재발송은 토큰을 유지하고, 만료·폐기 링크 재발급은 새 토큰을 만든다.
- 익명 읽기·서명 제출은 유효한 발송 링크에만 허용된다.
- 계약 소유자·채널 관리자·플랫폼 관리자는 만료·폐기 링크 상태를 확인할 수 있다.
- 만료 시각은 생성 시각 이후, 최대 30일 이내이며 생성 후 임의 변경할 수 없다.

### 실행 결과

- `npm.cmd run typecheck`: PASS
- `node --import tsx scripts/sim-contract-sign-rules.mts`: 23/23 PASS
- `node --import tsx scripts/sim-agent.mts`: 39/39 PASS
- `database.rules.json` JSON 파싱: PASS
- `git diff --check`: PASS
- production build: 실행 중인 개발 서버 보호를 위해 보류

### 남은 운영 검증

- 현재 환경에는 Firebase 설정이 없어 실제 Rules 배포·에뮬레이터 검증을 수행하지 않았다.
- 규칙 게시 후 익명 서명자, 계약 소유 영업자, 채널 관리자, 플랫폼 관리자 계정으로 역할별 스모크가 필요하다.

---

## 2026-07-26 — 전자서명 토큰·비활성 링크 앱 경계

### 판정

**PASS**

- Web Crypto로 192비트 난수 토큰을 생성한다.
- 폐기 상태만 있고 폐기 시각이 없는 불완전 레코드도 비활성 처리한다.
- Firebase 공개 조회와 로컬 저장소 fallback 모두 만료·폐기 링크를 반환하지 않는다.

### 실행 결과

- `npm.cmd run typecheck`: PASS
- `node --import tsx scripts/sim-contract-sign-rules.mts`: 26/26 PASS
- `node --import tsx scripts/sim-agent.mts`: 39/39 PASS
- production build: 실행 중인 개발 서버 보호를 위해 보류

---

## 2026-07-26 — 전자서명 승인 상태 전이

### 판정

**자동 검증 PASS / Firebase 배포 검증 보류**

- 발송 상태 또는 서명·동의가 빠진 검토대기는 도메인 승인 조건에서 거부된다.
- v4 계약 Rules는 공개 슬롯의 계약코드, 검토대기 상태, 서명, 동의를 승인 근거로 확인한다.
- 영업자가 약정발송 단계를 진행할 때 공개 슬롯의 최종 `signed` 상태를 요구한다.

### 실행 결과

- `npm.cmd run typecheck`: PASS
- `node --import tsx scripts/sim-contract-sign-rules.mts`: 33/33 PASS
- `node --import tsx scripts/sim-agent.mts`: 39/39 PASS
- `database.rules.json` JSON 파싱: PASS
- production build: 실행 중인 개발 서버 보호를 위해 보류

---

## 2026-07-26 — 전자서명 필수 동의 증적

### 판정

**자동 검증 PASS / Firebase 배포 검증 보류**

- 필수 동의 5개가 고정 ID와 순서로 정규화된다.
- 하나라도 누락된 동의 배열은 제출 전에 거부된다.
- 익명 쓰기는 정확한 동의 문자열과 `v1` 버전을 모두 요구한다.
- 기존 한글 동의 증적은 승인 단계에서 계속 인정된다.

### 실행 결과

- `npm.cmd run typecheck`: PASS
- `node --import tsx scripts/sim-contract-sign-rules.mts`: 37/37 PASS
- `node --import tsx scripts/sim-agent.mts`: 39/39 PASS
- `database.rules.json` JSON 파싱: PASS
- production build: 실행 중인 개발 서버 보호를 위해 보류

---

## 2026-07-26 — 전자서명 공개 제출 데이터 검증

### 판정

**자동 검증 PASS / Firebase 배포 검증 보류**

- 정상 성명·연락처·PNG 서명·필수 동의 제출은 허용된다.
- 짧은 연락처, SVG 서명, 600KB 초과 서명은 도메인에서 거부된다.
- Rules는 PNG data URL 접두사와 최근 제출 시각을 요구한다.

### 실행 결과

- `npm.cmd run typecheck`: PASS
- `node --import tsx scripts/sim-contract-sign-rules.mts`: 43/43 PASS
- `node --import tsx scripts/sim-agent.mts`: 39/39 PASS
- `database.rules.json` JSON 파싱: PASS
- production build: 실행 중인 개발 서버 보호를 위해 보류

---

## 2026-07-26 — 전자서명 증적 확정·공개 개인정보 정리

### 판정

**자동 검증 PASS / Firebase 통합 검증 보류**

- 공개 슬롯의 서명·동의·본인확인 필드가 관리자 검토 객체에 모두 병합된다.
- 승인된 계약 원본에는 서명·동의·신원 증적이 보존된다.
- 서명 완료 공개 슬롯에서는 제출 개인정보와 서명 데이터가 삭제된다.

### 실행 결과

- `npm.cmd run typecheck`: PASS
- `node --import tsx scripts/sim-contract-sign-rules.mts`: 49/49 PASS
- `node --import tsx scripts/sim-agent.mts`: 39/39 PASS
- production build: 실행 중인 개발 서버 보호를 위해 보류

### 남은 검증

- Firebase 환경에서 익명 제출 → 영업자 승인 → 계약 원본 증적 확인 → 공개 슬롯 개인정보 삭제를 실제 계정으로 확인해야 한다.

---

## 2026-07-26 — 전자서명 승인 중간 실패 복구

### 판정

**PASS**

- 서명완료이지만 약정발송이 누락되고 유효 증적이 남은 상태만 복구 대상으로 판정한다.
- 이미 완료된 계약과 증적이 없는 계약은 복구 대상에서 제외된다.
- 복구 실행은 계약 증적 저장을 반복하지 않고 후속 단계만 재시도한다.

### 실행 결과

- `npm.cmd run typecheck`: PASS
- `node --import tsx scripts/sim-contract-sign-rules.mts`: 52/52 PASS
- `node --import tsx scripts/sim-agent.mts`: 39/39 PASS
- production build: 실행 중인 개발 서버 보호를 위해 보류

---

## 2026-07-26 — 전자서명 링크 해지 실패 안전성

### 판정

**PASS**

- 공개 슬롯을 계약 원본보다 먼저 폐기한다.
- 폐기 패치는 `revoked` 상태, 폐기 시각, 기존 만료 시각을 보존한다.
- 두 단계 사이 실패 시에도 공개 링크가 활성 상태로 남는 방향을 피한다.

### 실행 결과

- `npm.cmd run typecheck`: PASS
- `node --import tsx scripts/sim-contract-sign-rules.mts`: 55/55 PASS
- `node --import tsx scripts/sim-agent.mts`: 39/39 PASS
- production build: 실행 중인 개발 서버 보호를 위해 보류

---

## 2026-07-26 — 전자서명 공개 개인정보 최소화

### 판정

**PASS**

- 신규 공개 슬롯에 계약 원본의 고객 이름과 전화번호가 포함되지 않는다.
- 반려 후 재서명 공개 슬롯에서 이전 제출 개인정보와 서명 증적이 제거된다.
- 계약 원본과 관리자 검토 데이터는 삭제하지 않는다.

### 실행 결과

- `npm.cmd run typecheck`: PASS
- `node --import tsx scripts/sim-contract-sign-rules.mts`: 57/57 PASS
- `node --import tsx scripts/sim-agent.mts`: 39/39 PASS
- `database.rules.json` JSON 파싱: PASS
- production build: 실행 중인 개발 서버 보호를 위해 보류

---

## 2026-07-26 — 오픈 준비 로컬 릴리스 게이트

### 판정

**로컬 코드 게이트 PASS / 운영 Firebase 게이트 대기**

- 개발 서버를 잠시 종료하고 `npm.cmd run build` 성공: 26개 페이지
- 빌드 후 포트 4004 개발 서버 복구
- typecheck·폰트 검사·전체 시뮬레이션·차량 마스터 전수검증 PASS
- 실행 서버 주요 22개 경로 HTTP 200
- 로컬 Firebase 필수 환경변수 6종 설정 확인
- Vercel Production·Preview Firebase 환경변수 8종 설정 확인
- 자차 면책 비율형·정액형 표시 변경은 build와 전체 회귀검사를 통과

### 남은 오픈 차단 항목

- 완료 계약 없는 출고불가 3대의 의도된 상태 확인
- 관리자 외 4역할 실제 계정 권한 테스트
- 전자서명 반려·해지·만료 실브라우저 테스트
- Vercel production 배포

단일 실행 문서는 `OPENING_CHECKLIST.md`를 따른다. 추가 리팩터링은 오픈 후 범위로 동결한다.

---

## 2026-07-26 — Cursor 오픈 전 실사용 QA 독립 검증

### 판정

**PASS / 실제 브라우저 연타 검증만 수동 잔여**

- 채팅 전송·첨부, 계약 단계, 서명 제출, 회원 저장·승인, 재고 저장, 비밀번호 메일에 중복 실행 방지와 오류 피드백 적용
- 설정 연락처 포맷 통일
- 회원 private 요율 저장 후 enrich된 행으로 상세 폼 재주입
- Firebase Rules·스키마·마이그레이션·전자서명 상태기계 변경 없음

### 실행 결과

- typecheck·폰트 검사: PASS
- 전체 권한·채팅·계약·서명·정산·생애주기·차량잠금 시뮬레이션: PASS
- 상품·정산 private 마이그레이션 계획 시뮬레이션: PASS
- 차량 마스터 전수검증: PASS
- production build: PASS, 26개 페이지
- 서버 복구 후 `/`, `/inventory`, `/chat`, `/contract`, `/settlement`, `/members`, `/settings`, `/sign/invalid-test-token`: HTTP 200
- `git diff --check`: PASS

### Chrome 실브라우저 후속 검증

- localhost ERP4 탭 연결 및 화면 제어 성공
- 상단 사용자 표시: `박영협`
- 390×844 모바일 뷰포트에서 `/inventory`, `/chat`, `/contract`, `/settlement`, `/settings` 가로 넘침 없음
- DevTools JavaScript error 없음
- `products_private`, `settlement private` 조회의 `Permission denied` 경고 반복
- 설정 화면에 프로필 입력란 없이 `로그인` 버튼이 표시되어 Firebase 인증 세션은 확인되지 않음

### 잔여 수동 검증

- 실제 쓰기를 만드는 빠른 연타·Enter 반복은 운영 데이터 부작용을 피하기 위해 실행하지 않았다.
- Firebase Rules 게시 후 실제 5역할 계정으로 private 조회와 쓰기 중복 방지를 확인한다.

---

## 2026-07-26 — 연결 브라우저 운영형 E2E 추가 검증

### 실제 확인

- 관리자 계정(`박영협`, 플랫폼 관리자)으로 회원·파트너 화면 진입: PASS
- 회원 146명, 승인대기 1명 및 역할 관리 화면 표시: PASS
- 재고 428대 로딩, 상품 신규 등록·취소 시 미저장 초안이 남지 않음: PASS
- G: 드라이브의 실제 자동차등록증 PDF 원본 존재 확인: PASS
- OCR 결과가 정상 추출됐다고 가정하고 다음 QA 상품을 실제 저장:
  - 차량번호 `66소6317`
  - 기아 카니발 KA4, 디젤 2.2, 9인승, 2,151cc
  - 출고가능 / 중고렌트
  - 12개월 월 890,000원 / 보증금 3,000,000원
  - 메모 `[QA 20260726] OCR 정상 추출 가정 E2E 시뮬레이션`
- 저장 후 `66소6317` 검색 결과 1건 및 입력값 재표시: PASS

### 분리된 검증 항목

- OCR 정확도와 파일 업로드는 사용자 직접 검증 항목으로 분리했다.
- 원본 등록증 PDF는 수정·이동·삭제하지 않았다.

### 발견한 차단 문제

- `/contract` 직접 진입 시 Next.js 개발 서버에서
  `Invariant: Expected clientReferenceManifest to be defined` 런타임 오류가 재현됐다.
- 오류 후 `/inventory`, `/members`는 HTTP 200을 반환하지만 연결 브라우저에서
  인증 컨텍스트가 `불러오는 중…` 상태로 복구되지 않았다.
- 실행 중인 4004 서버는 중단하거나 재시작하지 않았다.
- 따라서 이번 연결 세션에서는 상담·계약·서명·정산 실제 쓰기와 QA 상품 삭제를
  이어서 수행하지 못했다. 관리자 인증 재연결 후 동일 상품으로 계속 검증한다.

### 개발 서버 번들 장애 및 복구

- 증상: 로그인 직후 홈 화면 CSS 미적용, `/members` 백지 화면.
- 확인: 기존 4004 HTML이 참조한 `layout.css`와 App Router 페이지 청크가 404를 반환했다.
- 원인: OCR 확인용 보조 Next 개발 서버가 기존 4004 서버와 같은 `.next` 디렉터리를
  사용해 디스크 번들과 기존 프로세스의 메모리 상태가 어긋난 것으로 판단했다.
- 조치: `NEXT_DIST_DIR`로 보조 서버의 빌드 디렉터리를 분리할 수 있도록 설정하고,
  Next 설정 변경 감지에 의한 자체 재기동으로 4004를 복구했다.
- 복구 확인:
  - 홈 CSS 및 상품 395대 표시: PASS
  - 관리자 `박영협` 인증 유지: PASS
  - 회원·파트너 146건 표시: PASS
  - `/contract` HTTP 200, 참조 정적 자산 10개 전부 HTTP 200: PASS

### 2026-07-27 재검토

- `/`, `/inventory`, `/members`, `/chat`, `/contract`, `/settlement`: HTTP 200
- `git diff --check`: PASS
- TypeScript `tsc --noEmit`: PASS
- 폰트 토큰 드리프트: 0
- 권한 시뮬레이션: 44/44 PASS
- 채팅 Rules 시뮬레이션: 40/40 PASS
- 계약 Rules 시뮬레이션: 23/23 PASS
- 계약 전자서명 Rules 시뮬레이션: 57/57 PASS
- 3자 정산 E2E 시뮬레이션: 15/15 PASS
- 전체 생애주기 시뮬레이션: PASS
- 차량 잠금·중복계약 방지 시뮬레이션: 23/23 PASS

자동 검증은 통과했지만 실제 역할별 계정으로 회원가입·승인·배정·채팅·계약·서명·
정산서 출력까지 수행하는 브라우저 운영 E2E는 아직 완료되지 않았다. 이 항목을
오픈 전 최종 잔여 검증으로 유지한다.
## 2026-07-27 — Firebase Storage 원본 + Google Drive 백업 독립 검증

### 판정

**Storage 운영 활성화 PASS / Drive OAuth·실제 helper 업로드 PASS**

- 신규 상품 사진·계약 서류·채팅 파일은 RTDB data URL 대신 Firebase Storage를 사용한다.
- 상품·계약은 Drive 백업을 시도하고 채팅은 Storage에만 저장한다.
- Drive 설정 누락·백업 실패가 Storage 원본을 제거하거나 업무 저장을 취소하지 않는다.
- 계약·채팅 레코드 저장 실패 시 직전 Storage 업로드를 정리한다.
- 상품 편집 취소 시 신규 업로드를, 저장 성공 시 제거된 기존 사진을 정리한다.
- ERP 삭제는 Storage 원본만 삭제하고 Drive 사본은 복구용으로 보존한다.
- 레거시 data URL은 계속 표시한다.

### 자동 검증

- `npm.cmd run typecheck`: PASS
- `npm.cmd run check:fonts`: PASS, 드리프트 0
- `scripts/sim-*.mts` 12개: 전부 PASS
  - 영업자 39/39, 권한 44/44, 채팅 Rules 40/40
  - 계약 Rules 25/25, 전자서명 57/57, 3자 정산 15/15
  - 생애주기, Phase 1·2 25/25, private 마이그레이션 15/15·12/12
  - 시트 병합 12/12, 차량 잠금 23/23
- `NEXT_DIST_DIR=.next-storage-verification npm.cmd run build`: PASS
  - 28개 라우트, `/api/drive-backup` 포함
- Firebase Storage Emulator 15.24.0: `storage.rules` 로드·컴파일 PASS
- 공유 버킷의 V3 `product-images`, `contract-files`, `notice-images`, `user-docs`,
  `contract-signed`, `contract-unsigned`, `chat-files` 규칙을 원본과 대조해 보존
- 실행 서버: `/`, `/inventory`, `/chat`, `/contract` HTTP 200
- `/api/drive-backup`: 서버 재시작 후 HTTP 200, `{"enabled":true}` 확인
- `git diff --check`: PASS

### 운영 적용 결과

- `freepasserp3.firebasestorage.app`에 V3 호환 + V4 `/erp` Rules 게시 완료.
- 운영 Rules release ruleset:
  `projects/freepasserp3/rulesets/9a8cdcea-56e9-48f5-bcd9-445a63d0ebb2`
- 게시 전 V3 Rules는 `storage.rules.PREV`에 보존했다.
- Google Drive API를 `freepasserp3` 프로젝트에서 활성화했다.
- `drive.file` 최소 권한으로 앱이 직접 만든 Drive 루트 `FreepassERP4 자동백업`
  (`1KT0jDkm3yYFpcYWnv6-kJQutIhZwEum3`)을 로컬 환경변수에 설정했다.
- OAuth 앱 정책 동의, 테스트 사용자, 데스크톱 클라이언트, 오프라인 refresh token 발급을 완료했다.
- Vercel Production·Preview에 Drive 환경변수 4종이 모두 암호화 상태임을 확인했다.
- refresh token으로 새 access token을 발급한 뒤 확인 파일 업로드에 성공했다.
- 실제 서버 helper `uploadDriveBackup`으로
  `상품/DRIVE-CONNECTION-TEST/2026-07-27T04-01-50-914Z_connection-check.txt`를
  생성해 폴더 탐색·생성·multipart 업로드까지 확인했다.
- 관리자·영업자·공급사 계정별 실제 사진·계약서 업로드와 ERP 삭제 후
  Storage 원본 삭제·Drive 사본 보존·수동 복구는 최종 운영 E2E 항목으로 남긴다.
- Storage download URL은 capability URL이다. RTDB 업무 범위가 URL 발견을 제한하지만
  URL 자체의 외부 전달까지 막지는 못한다. 계약 개인정보의 강한 차단은 인증 다운로드 프록시가 후속 과제다.

### 개발 서버 복구 기록

- 별도 production 빌드는 성공했으나 ignored 산출물 정리 명령이 실행 중 `.next` 캐시까지 정리해
  기존 서버가 일시적으로 500을 반환했다.
- 소스와 Firebase 운영 데이터는 변경되지 않았다.
- 손상 캐시는 `tmp/server-recovery/`로 보존하고 4004 개발 서버를 재기동했다.
- Drive 환경변수 반영을 위해 4004 서버만 짧게 재시작했다.
- 최종 리스너 PID는 `30312`이며 `/api/drive-backup` HTTP 200과 `enabled:true`를 확인했다.

## 2026-07-27 — 회원·파트너 목록 규격 통일 검증

### 판정

**PASS**

- 회원·파트너 목록을 재고와 같은 아이콘 + 3줄 피드 행, 첫 행 신규 등록,
  검색 결과 조건 해제, 100명 단위 더보기 규격으로 통일했다.
- 사용자 151명에서 첫 100명과 `더보기 · 51명`, 승인대기 1명, 역할·활성 표시를 확인했다.
- 파트너 38명에서 V3 `provider`·`sales_channel`이 공급사·영업채널로 표시되는지 확인했다.
- 공급사 필터에서 V3 `provider` 항목은 남고 영업채널 항목은 제외되는지 확인했다.
- 기존 좌측 목록/우측 상세, 모바일 목록→상세 전환, 저장·승인·삭제 동작은 변경하지 않았다.
- `npm.cmd run typecheck`: PASS
- `npm.cmd run check:fonts`: PASS, 드리프트 0
- `scripts/sim-*.mts` 12개: 전부 PASS
- `NEXT_DIST_DIR=.next-verification-20260727-members npm.cmd run build`: PASS
  - 28개 라우트, `/members` 8.76kB, `/api/drive-backup` 포함
- 실행 중인 `.next`와 분리한 빌드 산출물은 `tmp/verification-build/members-20260727/`에 보존했다.

---

## 2026-07-27 — 회원·파트너 및 월별 정산 4프레임 검증

### 판정

**PASS**

- 공통 화면 정의를 `목록 1 + 업무 패널 3 = 데스크톱 4프레임`으로 확정했다.
- 회원·파트너 전환을 검색·역할/유형 필터보다 상위인 목록 헤더로 이동했다.
- 회원·파트너 상세를 기본정보, 권한/정산, 영업/연동의 3개 독립 패널로 분리했다.
- 월별 정산을 월 선택 가능한 실제 정산 목록과 3개 상세 패널로 재구성했다.
- 월 집계는 검색·상태 필터와 무관하게 선택 월 전체 레코드를 기준으로 유지했다.
- 기존 정산 XLSX 가져오기, 정산서 다운로드, VAT 정산서 편집 기능을 보존했다.

### Chrome 실데이터 상호작용

- `/members` 사용자 목록: 151명, 사용자/파트너 탭이 역할 필터 위에 표시됨
- 사용자 선택: `기본정보 | 소속·권한 | 영업설정` 표시, 승인대기 동작 노출
- `/members` 파트너 전환: 38명, `기본정보 | 정산·운영 | 데이터연동` 표시
- `/settlement`: `월별정산 | 정산 상세 | 금액·지급 | 2026-07 집계` 4열 표시
- 2026-07 정산 1건 선택:
  - 계약번호·계약일·계약자·차량·공급사·영업자·영업채널 표시
  - 월대여료 890,000 / R1 89,000 / R2 35,600 / 순수익 53,400 표시
  - 월 집계와 공급사별 소계 표시
- VAT 정산서 오버레이 진입 및 월별 정산 화면 복귀 PASS
- `/members`, `/settlement` HTTP 200

### 자동 검증

- `npm.cmd run typecheck`: PASS
- `npm.cmd run check:fonts`: PASS, 드리프트 0
- `scripts/sim-*.mts` 12개: 전부 PASS
  - 영업자 39/39, 권한 44/44, 채팅 Rules 40/40
  - 계약 Rules 25/25, 전자서명 57/57, 3자 정산 15/15
  - 생애주기 PASS, Phase 1·2 25/25
  - 상품 private 15/15, 정산 private 12/12
  - 시트 병합 12/12, 차량 잠금 23/23
- `NEXT_DIST_DIR=tmp/verification-build/four-panel-20260727 npm.cmd run build`: PASS
  - 28개 라우트
  - `/members` 9.03kB
  - `/settlement` 10.1kB
- Next 빌드가 추가한 임시 `tsconfig.json` include/포맷 변경은 원상복구했다.
- 실행 중 개발 서버의 `.next`는 건드리지 않았고 PID `30312` 유지.

---

## 2026-07-28 — 계약 규격 복구 및 모바일 아이콘 버튼 통일

### 변경

- 계약 목록 위에 임의로 추가됐던 `대기·완료·환수·순수익` 요약줄과
  `features/contract/SettlementSummary.tsx`를 제거했다.
- 계약 화면은 다시 `목록 1 + 업무 패널 3`만 표시하며, 정산 집계는 월별 정산 화면에서 담당한다.
- 공통 뒤로가기, 검색 초기화, 패널 전환, 하단 CRUD, 채팅 전송 버튼을 모바일에서
  아이콘 전용으로 표시하고 모든 버튼에 접근성 이름을 유지했다.
- 계약 승인·상태 변경처럼 오동작 위험이 있는 업무 버튼, 앱 하단 내비게이션,
  카테고리·상태 선택은 의미 전달을 위해 텍스트 라벨을 유지했다.
- `scripts/sim-phase12.mts`에 계약 요약줄 재삽입 방지와 공통 모바일 아이콘 규격 검사를 추가했다.

### 확인

- 브라우저 `/contract`: 규격 외 요약줄 미표시, 계약 목록과 3개 업무 패널 정상 표시
- `npm.cmd run typecheck`: PASS
- `npm.cmd run check:fonts`: PASS, 드리프트 0
- `scripts/sim-phase12.mts`: 30/30 PASS
- `scripts/sim-*.mts` 12개: 전부 PASS
- `NEXT_DIST_DIR=tmp/verification-build/mobile-icons-20260728 npm.cmd run build`: PASS
  - 28개 라우트
  - `/contract` 4.68kB, `/chat` 8.45kB, `/settlement` 10.5kB
- production build가 추가한 임시 `tsconfig.json` include/포맷 변경은 원상복구했다.
- 모바일 아이콘 분기는 `useIsMobile()` 내부에 한정하고 데스크톱 텍스트 버튼은 유지했다.

---

## 2026-07-28 — 모바일 표면 액션 아이콘 전용 2차 통일

### 변경

- 위 절의 “계약 승인·상태 변경은 모바일 텍스트 유지” 판단은 사용자 지시에 따라 폐기했다.
- 공통 `Btn.mobileIcon`으로 모바일은 아이콘, 웹은 기존 텍스트를 렌더링한다.
- 계약·전자서명·정산, 재고·OCR·시트 연동·사진, 채팅, 회원 승인, 설정,
  관심함, 목록 초기화·더보기·전체보기 액션을 모바일 아이콘 전용으로 통일했다.
- 모바일 상품·계약 엑셀, 월정산 정산서, 재고 종합표 내보내기를 노출하지 않는다.
- 선택 칩·탭·확인 대화상자는 텍스트를 유지한다.

### 확인

- 브라우저 `/contract`: 웹 계약 액션 텍스트와 4프레임 유지
- 브라우저 `/settings`: 웹 저장·비밀번호 변경 텍스트 유지
- `npm.cmd run typecheck`: PASS
- `npm.cmd run check:fonts`: PASS, 드리프트 0
- `scripts/sim-phase12.mts`: 33/33 PASS
- `scripts/sim-*.mts` 12개: 전부 PASS
- `NEXT_DIST_DIR=tmp/verification-build/mobile-icon-actions-20260728 npm.cmd run build`: PASS
  - 28개 라우트
  - `/contract` 4.73kB, `/inventory` 22.6kB, `/members` 9.22kB
- production build가 추가한 임시 `tsconfig.json` include/포맷 변경은 원상복구했다.
- 실행 중 개발 서버는 재시작하지 않았다.

---

## 2026-08-03 — 계약보호 충돌 및 계약 필터 브라우저 재검수

### 계약·재고 정합성

- `scripts/audit-v3-only-sheet-coverage.mts --show-protected`를 운영 Firebase·Google Sheet 읽기 전용으로 실행했다. 운영 write는 0건이다.
- 계약보호 3행은 차량 `54나7852` / 계약 `TMP-260712-01` 한 사건의 반복 확장이다. 직전 6행은 감사 스크립트가 v3/v4 overlay를 중복으로 센 거짓 양성이었다.
- 최신 Sheet 소유자 `RP021`과 진행 계약 공급사 `PT-0024`가 충돌한다.
- 계약은 2026-07-12 생성, 2026-07-13 마지막 갱신, `계약요청`, `agent_delivery_inquiry`만 완료, 서명 없음, 연결 방 3개 메시지 0건이다.
- 자동 만료 정책이 없으므로 계약 취소·공급사 변경·재고 병합은 수행하지 않았다. 운영 확인 전 해당 차량은 동기화 자동수정 금지다.

### 브라우저 UI

- Chrome의 최신 로컬 앱 `/contract`를 영업자·공급사 둘러보기 역할로 직접 검수했다.
- 계약월·업무단계 `FilterGroup`에서 `aria-expanded`가 JSX 텍스트로 노출되는 결함을 발견하고 실제 버튼 속성으로 수정했다.
- 수정 후 브라우저 접근성 이름은 `계약월`, `업무단계`로 정상화됐고 접기 동작 후 `aria-expanded=false`를 확인했다.
- `check:ui`에 JSX 텍스트 `aria-*=` 회귀 차단을 추가했다.
- `npx tsc --noEmit`: PASS.
- `npm run check:ui`, `check:fonts`, `check:tokens`: PASS.
- `scripts/sim-work-list-semantics.mts`: 142/142 PASS.
- `NEXT_DIST_DIR=tmp/verification-build/launch-ui-a11y-20260803 npm run build`: 30 routes PASS.
- 후보 Rules 기준 `check:release`: 법적 운영자 정보 6필드만 차단, PWA 2개 경고.

## 2026-08-03 — Sheet 충돌 감사 거짓 양성 제거

- `scripts/audit-v3-only-sheet-coverage.mts`의 재고 입력을 실제 앱·일일동기화와 같은 `product_code` 논리키 overlay 병합으로 수정했다.
- 기존 활성 중복 97건/195행은 `v3 EXT_*`와 `v4 공급사_차번`을 별도 상품으로 센 감사 도구 오류였으며 최신 기준 0건이다. 운영 데이터 삭제는 필요하지 않다.
- 최신 실제 차단은 공급사 소유 39, 삭제 재등장 58, 미확정 삭제 8, 번호미정 변경 1, 임시번호 서명 7, 가격기간 누락 97건이다.
- `--show-conflicts=RP021`은 충돌을 차량 단위로 묶고 Sheet 소유자, 실제 저장키, 계약·채팅·견적 참조, private 존재 여부만 출력한다. 값과 PII는 출력하지 않는다.
- RP021 24대 중 22대는 과거 공급사 private 보존 필요, 1대는 기존 채팅 이력, 1대는 진행계약 보호다. 최신 Sheet 소유자는 모두 RP021이다.
- 과거 공급사 공개 상품의 v4 tombstone과 private·이력 원키 보존은 타당한 후보지만 데이터 정책 승인 전 실행하지 않았다.
- 중복 이관 판정에서 `계약철회`를 종료 상태에서 제거해 미결 계약 보호로 통일했다. `sim-product-duplicate-migration` 26/26 PASS, `sim-sheet-merge` 125/125 PASS, typecheck PASS.

## 2026-08-03 — 가격기간 차단 97건 사용자 영향 분해

- 운영 Firebase와 16개 공급사 Sheet를 다시 읽기 전용 대조했다. 운영 write는 0건이다.
- 가격기간 누락 97대는 RP023 90대, RP018 7대다. RP004 54행은 가격 문제가 아니라 삭제 재등장 상세 작업량이다.
- RP023은 과거 `12_3만`, `18_2만`, `24_2만`, `36_2만` 등 주행거리 변형키가 현재 오토플러스 표준키와 일부 변형키 조합으로 바뀌었다. 과거 키를 단순 보존하면 현재 가격과 섞일 수 있다.
- 실제 `priceList` 사용자 선택 규칙으로 비교한 결과 70대는 기본가격이 달라지고 27대는 계약기간 자체가 하나 이상 사라진다. 삭제 기간 집계는 18개월 20대, 12개월 16대, 24개월 9대이며 한 차량이 여러 기간에 포함될 수 있다.
- 진행계약과 겹치는 가격 충돌은 RP023 `195주5304` / `TMP-260722-01` 1대다. 운영자가 계약과 새 가격 적용을 결정하기 전 자동수정 금지다.
- 관리자 `상세 충돌 TSV`에 `가격영향`과 `영향기간`을 추가했다. 자동 승인·기간 삭제·운영 동기화는 추가하지 않았다.
- `sheet-conflict-report`가 가격 충돌을 공급사·기존 상품키·실제 RTDB 저장키·계약보호와 연결하도록 수정했다.
- `npx tsc --noEmit`, `check-ui-contract`, `sim-sheet-merge` 126/126 PASS.
- `check:fonts`, `check:tokens` PASS. 분리 production build 30 routes PASS.
- 후보 Rules 기준 `check:release`는 법적 운영자 정보 6필드만 FAIL, PWA 2건 WARN이다. 운영 Rules는 게시·변경하지 않았다.

## 2026-08-03 — 데이터 원자·계보 전수감사

- `scripts/audit-data-atoms.mts`를 추가해 ENTITIES 선언, 코드 소비처, 운영 v3+v4 필드 존재 건수를 값 미출력 방식으로 대조했다.
- P0 판단 대상은 메시지 `sender_email` 잔존, 상품 `account_number`의 잘못된 소유, 역할별 사업자번호 alias, 계약·고객 개인정보의 소비 단절, 구형·신형 감사로그 이중 구조다.
- 합쳐야 할 원자는 사진 alias, 조직명 alias, 사업자번호 alias, 첨부 alias다. 쪼개야 할 원자는 차량 표시명, 인도 권역/주소, 조직 링크/표시 snapshot, 역할별 읽음상태, 계약 단계값/변경주체/시각이다.
- `quote`와 `report`의 실제 RTDB node 이름 및 v3 nested/v4 flat 메시지 구조를 감사 스크립트에 반영했다.
- 운영 재조회가 무기한 대기하지 않도록 RTDB read timeout 20초를 추가했다.
- 상세 판정과 실행 순서는 `docs/DATA_ATOM_AUDIT_2026-08-03.md`에 기록했다.
- Chrome 운영 탭은 확인했지만 제어권 연결이 두 차례 응답하지 않아 DOM·스크린샷 대조는 미완료로 명시했다.
- 운영 write·삭제·마이그레이션·Rules 변경은 0건이다.

## 2026-08-03 — 데이터 원자 신규 유입 차단 1차

- OCR 응답은 `ENTITIES`에서 OCR 대상으로 선언한 canonical 원자만 폼에 반영한다. 현재 Python 반환키(`year`, `engine_cc`, `usage`)와 과거 alias(`car_year_month`, `displacement`, `usage_type`)를 경계에서 정규화하고 임의 키는 버린다.
- 상품 `account_number`는 삭제하거나 파트너 계좌로 자동이관하지 않았다. 레거시 상품이 재저장될 때 public에 승계되지 않도록 `products_private`에 격리하고 영업자·고객 조회에서는 제거한다.
- 감사로그 민감 필드에 `sender_email`, `business_no`, 고객 생년월일·사업자번호, 면허·계약첨부·계좌 alias를 추가하고 중첩 객체도 재귀 마스킹한다.
- 기존 감사로그용 `scrub-audit-pii.mts`도 신규 감사와 같은 민감 필드 목록을 사용한다. 운영 `--apply`는 실행하지 않았다.
- `sim-audit-display` 10/10, `sim-ocr-mapping` 5/5, `sim-agent` 44/44 PASS.
- 상품 private 이관 15/15, 중복상품 이관계획 26/26 PASS.
- `npx tsc --noEmit`, fonts, tokens, UI 계약 PASS.
- 분리 production build 30 routes PASS. build가 자동 수정한 `tsconfig.json`은 원상복구했다.
- 운영 데이터 write·삭제·마이그레이션·Rules 변경은 0건이다.

## 2026-08-03 — 데이터 원자 alias 읽기 통일 2차

- partner/user/customer/contract의 사업자등록번호를 역할별 기존 정본 우선 + legacy alias fallback으로 읽는 `business-identity` 경계를 추가했다. 저장 스키마와 운영 값은 변경하지 않았다.
- 로그인 매칭, 가입 승인 신원 파생, 회원 중복 확인·상세 표시, 계약서 공급사 정보 주입을 공통 reader로 통일했다.
- 포맷만 다른 동일 번호와 실제 alias 충돌을 구분하며, 충돌 시 기존 정본을 우선할 뿐 자동 수정·이관·권한 차단은 하지 않는다.
- 값과 식별자를 노출하지 않는 읽기 전용 운영 감사기 `scripts/audit-business-identity.mts`를 추가했다. 현재 환경은 Firebase 관리자 자격증명이 없어 실데이터 건수 집계 미실행이며 출시 전 잔여 게이트다.
- `sim-business-identity` 7/7, `sim-agent` 44/44 PASS.
- `npx tsc --noEmit`, fonts, tokens, UI 계약 PASS.
- 분리 production build 30 routes PASS. build가 자동 수정한 `tsconfig.json`은 원상복구했다.
- 운영 데이터 write·삭제·마이그레이션·Rules 변경은 0건이다.
- 기존 Claude 비게시 Rules 후보를 현재 운영 Rules에서 재생성해 보안 13/13, 계약 26/26, 전자서명 58/58 PASS를 재확인했다. 후보 기준 출시 게이트 잔여 차단은 법적 운영자 정보 6필드 1건이며 PWA 2건은 경고다. 후보를 운영에 게시하지 않았다.
- 같은 후보를 Firebase Auth+RTDB Emulator에 실제 로드해 정상·적대 HTTP 시나리오 26/26 PASS를 재확인했다. 테스트 종료 후 Emulator는 정상 종료했으며 운영 프로젝트에는 접근하거나 쓰지 않았다.

## 2026-08-04 — 아이언렌트카 홈페이지 단일 정본 연동 1차

- 사용자 확인에 따라 연동 활성화 후 `RP006`은 기존 Google Sheet를 중지하고 아이언렌트카 홈페이지를 단일 정본으로 사용한다.
- 신차·중고 누적 목록과 상세를 읽는 전용 서버 어댑터, 단일 정본 대조 플래너, 관리자 전용 read-only preview API를 추가했다.
- 실측 홈페이지 49대(활성 24·판매완료 25, 신차 20·중고 29)를 49/49 변환했다. 오류·차번/가격/사진 누락·중복차번·공개원가 누수는 0이다.
- 운영 v3+v4 논리키 읽기 대조: 안전 매칭 21, 신규 활성 3, 판매완료 신규 제외 25, 웹 부재 차단 후보 4, 실제 중복차번 0, 계약보호 0, 실행 0건이다.
- 기존 ERP 시트 가격과 홈페이지 가격은 동일하지 않다. 비교기간 50개에서 월대여료 동일 0, 보증금 동일 45, 쌍 동일 0, 웹 전용 기간 13, ERP 전용 기간 2다.
- 단일 정본 규칙에 따라 후보 patch는 홈페이지 가격 전체를 사용하며 과거 시트 전용 기간을 혼합 보존하지 않는다. 기존 계약 금액 snapshot은 건드리지 않는다.
- `sim-ironrentcar-source` 17/17, `sim-ironrentcar-reconcile` 15/15, `sim-sheet-merge` 128/128, `sim-phase12` 69/69 PASS.
- typecheck, fonts, UI 계약, 분리 production build 30/30 PASS. 새 preview route 포함을 확인했고 build의 `tsconfig.json` 자동 변경은 복구했다.
- 비인증 preview 요청은 403, 실행 중 4004 루트는 200이며 서버를 재시작하지 않았다.
- 운영 데이터·Rules·외부 사이트 write는 0건이다. 적용은 후보 28건 승인 후 서버 기능 플래그를 켜야 한다.
- 상세 결정과 활성화 순서는 `docs/IRONRENTCAR_WEB_INTEGRATION_2026-08-04.md`를 따른다.
- 체크포인트 `7f700a0`을 `codex/atomic-claim-preview`에만 push했다. Vercel Preview `dpl_ACpiQM8aFQ3BCP3yyW4Syvemgnt4`는 Ready이며 Production은 변경하지 않았다.
- Preview 보호를 통과한 `vercel curl` 비인증 호출은 앱의 `{\"error\":\"forbidden\"}`을 반환해 관리자 인증 경계를 재확인했다.
- `npm audit --omit=dev`는 기존 기준선과 같은 Critical 0, High 3, Moderate 8이다. 이번 의존성 추가로 건수가 늘지 않았다.

## 2026-08-04 — 아이언 논리키 정정 및 적용 경로

- 최초 보고의 중복 3그룹은 v3 `EXT_*` 저장키와 v4 canonical child를 별개로 병합한 감사 오탐이었다. 앱의 `product_code` 논리키 병합으로 재검증해 실제 중복 0으로 정정했다.
- 공용 `mergeV3V4Records()`를 추가하고 아이언 preview와 중복상품 감사 서버가 이 경계를 쓰게 했다.
- 최신 후보는 기존 21대 보강, 신규 3대, 웹 부재 4대 차단으로 총 28건이다. 홈페이지 판매완료만 있는 25대는 생성하지 않는다.
- 관리자 POST apply 경로를 추가했다. `IRONRENTCAR_SYNC_ENABLED` 기본 OFF, 명시 확인문구·revision·예상 건수 일치가 필수다.
- 적용 시 진행 중 Sheet sync를 차단하고 `RP006.inventory_source=ironrentcar_web`으로 바꾼다. 실패 시 source를 복구하며 성공 후 Sheet roster에서 RP006을 제외한다.
- 공개 상품 28건은 `v4/products` 부모 transaction 하나에서 CAS 검증 후 전부 적용하거나 전부 중단한다. v3·Rules·private 차량가는 쓰지 않는다.
- `sim-ironrentcar-apply` 15/15, `sim-ironrentcar-reconcile` 17/17, `sim-sheet-merge` 129/129, 상품 중복 이관 26/26, Phase1·2 69/69 PASS. 운영 write는 0건이다.
- 분리 production build 30/30 PASS이며 `/api/inventory/ironrentcar/apply`·`preview` 두 route를 확인했다. build가 자동 변경한 `tsconfig.json`은 복구했고 4004 서버는 재시작하지 않았다.

## 2026-08-04 — 오픈 게이트 재판정

- 운영 `database.rules.json` 기준 `check:release`는 보안 14건 FAIL이라 그대로 오픈 금지다.
- 최신 후보 Rules 기준 `check:release`는 법적 운영자 정보 6개만 FAIL, 기존회원 재동의 OFF와 서비스워커 부재 2건 WARN이다.
- `check:b2b-release`는 로컬 기준 서비스계정·차량 claim 서버 플래그·클라이언트 플래그 3건 FAIL이다.
- Vercel 환경 이름을 값 미출력으로 확인했다. `FIREBASE_SERVICE_ACCOUNT_JSON`은 Preview에만 있고 Production에는 없으며, 두 claim 플래그와 법적 운영자 변수는 등록되지 않았다.
- 현재 판정은 NO-GO다. 운영자 정보 입력, Preview claim smoke, Production 서비스계정, 후보 Rules 사람 검증·게시·5역할 smoke가 필수다.
- `LAUNCH_GONOGO.md` 상단을 2026-08-04 기준으로 갱신해 과거 2026-07 GO 판정을 최신 판정으로 오인하지 않게 했다.
- 구현 체크포인트 `0b525e7`을 Preview 브랜치에만 push했다. Vercel Preview `dpl_HJWJFvP7m4pSXXGCU93yA1Z4EZtZ`는 Ready이고 Production은 변경하지 않았다.
- Preview 보호를 통과한 비인증 preview/apply 호출은 모두 앱의 `forbidden`을 반환했다. 아이언 기능 플래그·공급사 source·상품 운영 write는 0건이다.

## 2026-08-04 — 아이언 연동 오픈 필수 전환 및 관리자 적용 UI

결과: **관리자 preview→명시 적용 UI·회귀검증 PASS / Preview 실관리자 적용·24대 확인 전 전체 오픈 NO-GO**

- 사용자가 아이언 홈페이지 연동을 포함해 오픈한다고 확정해 `LAUNCH_GONOGO.md`의 필수 게이트를 4개에서 5개로 변경했다.
- `SheetSync` 관리자 화면에 홈페이지 전체/활성/판매완료/신차/중고, 수정·신규·부재차단·판매완료 무시·중복과 revision을 표시하는 read-only 미리보기를 추가했다.
- 재고관리의 4번째 패널을 `연동·반영`으로 고정했다. 관리자 표에는 아이언 홈페이지와 전체 Sheet 공급사를 함께 나열하고 검증 전/후 신규·상태변경·정보수정을 표시한다. 공급사 역할 화면은 기존 actor 회사 범위를 유지한다.
- 후속 사용자 정정으로 원본별 UI 명칭을 통일했다. 홈페이지/Sheet는 `연동 방식` 보조열에만 표시하고, 별도 아이언 연동 카드·미리보기/동기화 용어 대신 모든 공급사에 `상품 검증`과 `상품 반영` 업무명을 사용한다. 서버 확인문구는 기존 apply API 호환을 위해 내부값으로만 유지한다.
- 신규 3대와 홈페이지 부재 4대의 키를 직접 노출한다. 불완전 카탈로그·중복차번·차단 후보가 있으면 적용 버튼을 비활성화한다.
- 적용은 확인 대화상자 뒤 미리보기 revision과 세 종류 예상 건수, 정확한 서버 확인문구를 POST한다. 서버가 재수집한 revision/건수와 다르면 409로 전체 거부한다.
- 적용 성공 뒤 공급사 roster와 재고 화면을 새로고침한다. 기능 플래그 OFF 응답은 Preview 설정 확인 안내로 표시한다.
- `sim-ironrentcar-apply` 19/19, source 17/17, reconcile 17/17, sheet merge 129/129, `tsc --noEmit`, fonts, tokens, UI 계약 PASS다.
- UI 회귀검증 시점에는 `IRONRENTCAR_SYNC_ENABLED`를 켜거나 apply를 호출하지 않아 운영 write가 0건이었다. 새 Ready Preview에서 관리자 미리보기 49/24/25·21/3/4 확인 → 명시 적용 28건 → 활성 24대·RP006 Sheet 제외·감사로그 확인이 남았다.
- 이후 Preview 환경에만 `IRONRENTCAR_SYNC_ENABLED=true`를 추가하고 동일 커밋을 재배포했다. Ready deployment는 `dpl_FK3AaEaBcq1HqqCBzemeRYW6xgp7`, URL은 `https://freepasserp4-p3yv6tvbv-freepass-projects.vercel.app`다. Production 환경변수와 배포는 변경하지 않았다.
- Preview 보호를 통과한 비인증 GET preview와 POST apply는 모두 403 `forbidden`이며 브라우저 console error/warn 0이다. 새 Preview origin에 관리자 세션이 없어 실미리보기/apply는 실행하지 않았고 운영 write는 계속 0건이다.

## 2026-08-05 — 포맷 소실분 Codex 세션 로그 복구 검증

- `C:\Users\user\.codex\sessions`의 성공한 `patch_apply_end` 원문을 현재 복구 커밋 `d69efdf`에 재생하고, 이후 Claude 커밋과 충돌하는 과거 패치는 제외했다. `.env.local`·서비스계정·Firebase 디버그 로그는 복구 대상에 포함하지 않았다.
- 복원 범위는 아이언 검증/반영/원자 롤백, Google Sheets 가시 행 판독, 오토플러스 연속 블록, 이안카 다중 탭 우선순위, 공급사 원본 감사, ERP4 상품 단독 정본, 상품 사진 공용 대체 UI다.
- `npx tsc --noEmit`, `check:fonts`, `check:tokens`, `check:ui`, `git diff --check` PASS.
- Iron source 19/19, reconcile 30/30, apply 25/25, rollback 28/28 PASS.
- Sheet merge 139/139, revive 12/12, daily sync 22/22, price 31/31 PASS.
- 분리 `NEXT_DIST_DIR=.next-codex-recovery` production build가 정적 30페이지와 신규 `/api/inventory/ironrentcar/rollback`을 포함해 PASS했다. Next가 자동 변경한 `tsconfig.json`은 빌드 전 해시 `68b2e974...`로 복구했다.
- 운영 RTDB·Google Sheet·아이언 홈페이지 write, Rules 게시, Production 배포는 실행하지 않았다. 기존 미추적 `tmp-install.log`도 보존했다.

## 2026-08-09 착한거래 전자계약 운영 릴리스 게이트

결과: **Preview E2E PASS / Production NO-GO**

- 검증 브랜치: `codex/esign-production-gate`, 전자계약 릴리스 커밋 `93a64d5`.
- Freepass Preview: `https://freepasserp4-5atfmrx7h-freepass-projects.vercel.app` Ready.
- Chakhandeal Preview: `https://chakhandeal-f2ugpyn17-freepass-projects.vercel.app` Ready.
- 두 Preview 브랜치에만 새 QA 전용 API 키를 동기화했다. Production 환경변수와 배포는 변경하지 않았다.
- 비인증 발행·상태·PDF API는 각각 의도한 `401 로그인 필요`를 반환했다. 착한거래 Preview의 빈 발행 요청은 앱 검증 단계의 `400 EXTERNAL_REF_REQUIRED`를 반환해 서버 간 통신도 확인했다.
- QA 전용 `v4/policies/QA-ESIGN-POLICY-*`와 `v4/contracts/QA-ESIGN-*`를 생성해 실제 발행 → 고객 본인확인/동의/필수서류/서명 → ERP 상태 동기화 → 완료 PDF 다운로드를 검증했다.
- 실제 결과: Freepass `발행` → Chakhandeal `signed` → Freepass `서명완료`. ERP PDF 2,950,163 bytes, `%PDF-` 헤더 정상, Chakhandeal SHA-256과 ERP 프록시 응답 SHA-256 일치.
- QA Freepass 계약·정책은 검증 직후 삭제했다. 생성한 Chakhandeal QA 계약 2건은 모두 `purged` 처리해 서명·신분·문서 파일과 개인정보를 제거했다.
- 정적 게이트: `sim-chakhandeal-esign` 29/29, `sim-chakhandeal-sync` PASS, `sim-esign-inputs` 43/43, `sim-esign-progress` 44/44, 정책→계약 및 초과주행거리 분리 검증 PASS, `tsc --noEmit`, fonts, tokens, production build PASS.
- Production 차단 사유 1: 전자계약 서식 메타데이터가 아직 `sample-v1`, `STANDARD_IS_SAMPLE=true`다. 법무/대표 승인 없이 실제 고객 계약에 사용하면 안 된다.
- Production 차단 사유 2: 현재 정책 54/54건이 모두 `product` 단계라 실제 ERP 전자계약 발행 가능 정책이 0건이다. 공급사별 실제 계약조건을 확인해 `contract` 단계 필수값을 채운 정책만 발행 대상으로 열어야 한다.
- 최종 판정: 기능과 연동 자체는 Preview에서 끝까지 정상이다. 표준계약서 문안 승인과 실제 계약정책 1건 이상 확정 전에는 Production 배포·키 등록 금지.

### 2026-08-10 최종 코드 통합 게이트

- `isEsignTemplateAllowed()`를 추가해 Preview에서는 샘플 문안 E2E를 허용하되, Vercel Production에서는 `STANDARD_IS_SAMPLE=true`인 동안 발행 API가 `503 표준계약서 최종 승인 전`으로 fail-closed 되게 했다.
- 전자계약 약관 39/39, 발행 매핑 29/29, 상태 동기화 PASS, 고객입력 43/43, 진행상태 44/44, `tsc --noEmit --incremental false`, fonts, tokens PASS.
- 분리 `NEXT_DIST_DIR=.next-codex-esign-final` production build가 33개 정적 페이지와 발행·상태·PDF API 3개를 포함해 PASS했다. 빌드가 자동 수정한 `tsconfig.json`은 원본 해시로 복구했다.
- Preview `dpl_2cuPnpBSnQ7KpuZEqQinhdaJsSaB` Ready. 발행·상태·PDF API 비인증 smoke는 모두 `401 로그인 필요`를 반환했다.
- 판정: **운영 코드 배포 GO / 실제 고객 발행 NO-GO**. 코드와 관리화면은 통합 가능하지만 Production API 키 등록과 계약 발행 개방은 최종 계약서·실제 계약정책 승인 후 별도 게이트다.

## 2026-08-10 — 프리패스 기본정책 패키지

- 패키지 식별자 `freepass-standard-2026-08-10-v1`, 기본값 30개를 `lib/domain/policy-defaults.ts`에 단일 정의했다.
- 미입력 정책은 `계약서 작성=프리패스가 작성`을 포함한 기본 패키지를 사용한다. 공급사가 명시한 작성 주체·요율·조건은 덮어쓰지 않는다.
- 신규/기존 정책 화면, 상품 조인, 공개 카탈로그, 계약 payload와 착한거래 발행 서버가 동일한 `applyPolicyDefaults()`를 사용한다.
- 빈 객체만으로 전자계약 정책 게이트 통과, 명시값 우선, 정책→계약 전달, 국산 200원/수입 400원 분리, fonts, `tsc --noEmit`, `git diff --check` PASS.
- 운영 RTDB 일괄 갱신·Rules 게시·배포는 실행하지 않았다. 계약 조건 변경이므로 실제 고객 발행 전 사람/Claude 게이트는 계속 필요하다.

## 2026-08-10 — 계약문의 최근순·미확인·미회신

- 계약문의 기본값을 `전체 + 최근순`으로 변경했다. 상단 목록 숫자는 현재 보이는 전체 문의 수이고, `미확인`과 `미회신`은 별도 KPI·필터로 표시한다.
- 상대 메시지를 아직 읽지 않았으면 경고 아이콘과 안읽은 메시지 숫자 뱃지를 표시한다.
- 메시지는 읽었지만 상대 메시지가 마지막이면 빨간 경고 아이콘과 `미회신` 배지를 표시한다. 응답 필요 표시는 계약 진행상태 아이콘보다 우선한다.
- `sim-chat-attention` 8/8, fonts, 변경파일 `diff --check` PASS.
- 전체 `tsc --noEmit`은 동시 작업 중인 `app/api/freepass-esign/contracts/[contractCode]/document/route.ts`의 `Uint8Array` 응답 타입 오류 1건으로 차단됐다. `sim-work-list-semantics`의 기존 발신자 표시 D18·D19도 142/144로 남았으며 이번 변경 범위와 무관하다.

## 2026-08-10 — 전 메뉴 정렬 기본값 정합성 전수검사

- 의미 없는 공통 `기본` 정렬을 제거하고 실제 기본 정렬을 명시했다: 상품찾기=대여료 낮은순, 계약문의=최근순, 계약진행=최근순, 재고관리=상태순, 회원관리=이름순, 정책관리=이름순, 정산관리=계약일 최신순.
- 웹·모바일 모두 선택값, 실제 정렬, 초기화 복귀값, 필터 활성 건수와 적용조건 문구가 같은 `defaultValue`를 사용한다. 기본 정렬은 활성 필터로 세지 않는다.
- 전수검사 중 발견한 계약문의 발신자 표기도 수정했다. 실명이 있으면 내부코드보다 실명을 우선하고, 이름 없는 이관 메시지는 내부코드 대신 역할명을 표시한다.
- `check-menu-sort-defaults` 10/10, `sim-chat-attention` 8/8, `sim-work-list-semantics` 144/144, `tsc --noEmit`, fonts, 변경파일 `diff --check` PASS.

## 2026-08-10 공급사 허브·주행거리 원본 복구
- 공급사 허브 18행과 ERP 설정을 시트 ID 기준으로 대조했다. RP004가 허브 원본이 아닌 과거 프리패스 종합시트를 가리켜 전체 파싱이 0건 차단되고 주행거리도 갱신되지 않던 원인을 확인했다.
- RP004 v4 설정을 허브 원본 `아이카종합`(gid 965600926)으로 복구했다. 실제 파서 미리보기는 원본 1,877행, 출고불가 1,825행, 반영 후보 50대, 무효 0, 중복 2였다.
- 매 동기화 직전 허브 URL을 메모리 설정에 덮어쓰고, 워크북이 바뀌면 과거 파일 전용 gid를 폐기하도록 방어했다. RP006은 홈페이지 단일 정본 예외를 유지한다. 서버 작업에서도 허브 CSV를 직접 읽도록 상대 `/api/sheet` 오류를 제거했다.
- 전체 재감사는 RP004 50대 정상 판독을 확인했으나 빌린카 3탭의 일시적 조회 timeout 때문에 fail-closed 되었다. 따라서 상품 일괄 쓰기는 실행하지 않았다.
- `sim-sheet-merge` 150/150, `tsc --noEmit`, fonts PASS.

## 2026-08-10 공급사 필수제원 미입력 감사·배기량 보충
- 공급사 시트 17곳 395대를 원자별 전수 집계했다: 배기량 368/395, 현재주행 393/395, 외장색 394/395, 내장색 281/395였다.
- 원본에는 있으나 ERP에만 빠진 배기량 11건(RP004 1대, RP031 10대)을 공급사코드+차번으로 재검증하고 v4 빈 필드만 보충했다. 사후 대조에서 두 공급사 모두 ERP 구멍 0건을 확인했다.
- 빈칸 보충 도구의 차번 단독 매칭을 공급사코드+차번으로 강화하고, 쓰기 직전 최신 v4 레코드를 다시 읽어 빈 필드만 ETag 조건부 PUT하도록 바꿨다. 다른 공급사의 같은 차번 및 동시수정을 덮어쓰지 않는다.
- 남은 배기량 16대·현재주행 2대·외장색 1대·내장색 114대는 공급사 원본 자체가 비어 있어 화면에는 `미입력`으로 표시하고 공급사 보완 대상에 포함해야 한다.
- `tsc --noEmit`은 동시 작업 파일 `app/api/freepass-esign/contracts/[contractCode]/handover/route.ts`의 actor nullable 오류 2건으로 차단됐다(이번 변경 범위 밖).

## 2026-08-10 공급사 입력 반영 다중 재검수
- 공급사 입력 우선 병합, 관리자 메모 보호, 계약잠금 상태 보호를 회귀검증했다. `sim-sheet-merge` 152/152 PASS.
- 시트 셀 링크 메타데이터가 빈칸 보충 경로에서 누락되던 문제를 수정하고 손오공 22대의 `photo_link`를 공급사코드+차번 CAS 방식으로 보충했다. 사후 RP012 원본→ERP 구멍 0건.
- 공급사 시트 판독 규격(숨김 행/탭, 복수 gid, 밀린 헤더, 탭 부분실패) `sim-supplier-sheet-read` 14/14 PASS.
- 최종 전 공급사 원본→ERP 원자 대조에서 차량제원·사진 구멍은 0건. 남은 28건은 RP023 14대의 주행거리별 가격기간+보증금으로, 기존 가격기간 변경 승인 게이트 대상이라 강제 반영하지 않았다.
- `tsc --noEmit`, fonts, 변경파일 `git diff --check` PASS.

## 2026-08-10 `연동하기` 1클릭 반영
- 공급사별/전체 `연동하기`가 사전 검증 버튼을 요구하지 않고 fresh 허브·시트 읽기 → 내부 검수 → 반영까지 한 번에 수행하도록 연결했다. 검수 버튼은 쓰기 없는 미리보기 용도로 유지한다.
- 충돌이 없으면 신규·수정·부재처리를 포함한 기존 전체 커밋 경로를 사용한다. 가격·삭제·신원 충돌이 있으면 버튼을 막지 않고, 활성 ERP에서 공급사코드+실차번이 정확히 한 대와 일치하는 기존 차량의 제원·주행거리·색상·옵션·사진만 CAS 반영한다.
- 안전 부분연동은 가격·신규·삭제·상태를 건드리지 않으며 검증 뒤 ERP revision 또는 공급사 roster가 바뀌면 재검증을 요구한다.
- `tsc --noEmit`, `sim-sheet-merge` 152/152, 변경파일 `git diff --check` PASS.

## 2026-08-10 `연동하기` 적대 경계 재검수
- 안전 부분연동의 대상 선정을 순수 계획 함수로 분리해 실제 저장 없이 공급사·차번 경계를 직접 검증할 수 있게 했다.
- 공급사+실차번 단일 일치만 기존 ERP 저장키로 반영하고, 신규 차량·타 공급사 동일 차번·ERP 중복차량·임시번호는 모두 제외됨을 확인했다.
- 공급사 원본에 가격·차량상태·관리자 메모가 들어 있어도 안전 부분연동에서는 우회 변경되지 않음을 확인했다.
- 원본 배열 자체에 같은 공급사+차번이 중복된 경우도 이중 반영하지 않고 전체 제외하도록 추가 방어했다.
- `sim-sheet-merge` 161/161, `tsc --noEmit`, fonts, 변경파일 `git diff --check` PASS.

## 2026-08-10 단건 `연동하기` 규격·반응 재검수
- 단건 버튼이 선택 공급사 revision과 전체 공급사 revision을 비교해 정상 데이터도 설정변경으로 오판하던 범위 불일치를 수정했다. 단건은 해당 공급사만, 전체는 전체 공급사를 같은 범위로 재검증한다.
- 선택하지 않은 다른 공급사 설정이 바뀌어도 단건 연동은 거짓 차단되지 않고, 선택 공급사 설정 변경은 계속 정확히 차단한다.
- 검증과 연동 진행 상태를 분리했다. 단건 연동 시 선택 행만 `연동 중…`으로 표시하고, 상단에는 ERP 규격 변환·저장·사후검증 진행을 표시한다.
- `sim-sheet-merge` 164/164, `tsc --noEmit`, fonts, 변경파일 `git diff --check` PASS.
- 로컬 화면 자동 확인은 브라우저 보안 확장에 의해 localhost 접근이 차단되어 클릭까지 수행하지 못했다. 코드 경로·상태 전이·규격 회귀는 자동 테스트로 확인했다.

## 2026-08-10 실제 브라우저 단건 연동 검수
- 로컬 개발 서버를 복구하고 Chrome에서 `/dev` 공급사 상품 연동 화면을 직접 열어 손오공(RP012) 단건 `연동하기`를 실행했다.
- 최초 화면에서 인증 준비 전에 roster 조회가 한 번 실패한 뒤 영구 고정되는 문제를 발견했다. `useAuthReady()` 이후 roster·자동연동 상태를 조회하도록 연결해 공급사 17곳과 버튼이 정상 활성화됨을 확인했다.
- 클릭 직후 선택 행만 `연동 중…`, 상단은 `RP012 연동 중… ERP 규격 변환·저장·사후검증`으로 표시됨을 실제 DOM에서 확인했다.
- 최초 실행은 동일 가격 객체의 `rent/deposit` 키 순서 차이를 변경으로 오판해 31대 사후검증 실패. 구조 비교를 객체 키 순서와 무관하게 수정하고 회귀 테스트를 추가했다.
- 수정 후 같은 버튼을 다시 실행해 실제 완료 알림 `연동 완료 · 신규 0대 · 수정 0대`를 확인했다. 재고 화면에서 `손오공` 검색 결과 31대와 배기량·색상·미입력 표기가 정상 노출됨을 확인했다.
- `sim-sheet-merge` 165/165, `tsc --noEmit`, 변경파일 `git diff --check` PASS.

## 2026-08-10 실제 변경 1건·재검증 수렴 검수
- 무변경 완료와 다른 방향으로 경진렌트카(RP015)를 먼저 읽기 전용 검증했다. 화면 표에서 `신규 0 · 상태변경 0 · 정보수정 1`, 원본 올림 1대를 확인했다.
- 같은 행의 단건 `연동하기`를 실제 실행해 완료 알림 `연동 완료 · 신규 0대 · 수정 1대`를 확인했다.
- 즉시 같은 공급사를 다시 데이터 검증한 결과 `신규 0 · 상태변경 0 · 정보수정 0`으로 수렴했고, 최근 반영 시각도 `8. 10. 오후 09:11`로 갱신됐다.
- 따라서 검증 예상 1건 → 실제 수정 1건 → 재검증 잔여 0건의 숫자 정합과 단건 연동 멱등성을 실제 브라우저·운영 v4 경로에서 확인했다.

## 2026-08-11 프리패스 표준 렌터카 계약서·약관 책임 분리

- 계약서에는 계약별 숫자·선택값(연간 약정주행거리, 1km당 초과주행 요금, 월 납부일 등)을 두고, 약관에는 계산식·정산시점·제외거리·증빙방법을 두도록 역할을 분리했다.
- 초과주행 요금은 `반납 주행거리 - 출고 주행거리 - 사용기간 약정거리 - 객관적으로 확인된 제외거리`의 0 초과분에 계약서의 1km당 요금을 곱하도록 제16조에 명시했다. 일할 약정거리와 계기판 이상 시 객관자료 기준도 함께 규정했다.
- 자동이체는 선택 부속서류로 분리하고 계약 본문은 `월 납부일`로 통일했다. 직접 작성 화면에서는 고객 주소와 월 납부일을 발송 전 필수 확인한다.
- 보험·자차·사고 문구를 실제 손해와 가입 보험·공제 처리결과 중심으로 정리하고, 수리비 산정근거 제공·동일 손해 중복청구 금지·회사 귀책 인도불능/하자 조치·적법한 대리운전 예외를 보강했다.
- 약관을 `계약 성립·기본조건 → 차량 인도·운행 → 사고·연체·회수 → 계약 종료·반납·정산 → 기타 사항`의 5개 장으로 재배열하고, 보험·사고·전손·반납재산 등 중복을 합쳐 23개 항목을 21개 조문으로 정리했다.
- 중고차 조건부 항을 제7조에 두어 모든 계약이 같은 21개 약관 정본을 사용하게 했다. `중고차량인 경우`에만 경년변화·통상 사용흔적 확인이 적용되며, 미고지 중대하자·안전운행 결함·통상점검으로 확인하기 어려운 하자는 인정 대상에서 제외한다. 계약서 앞면·최종 확인란의 중고차 요약만 차량 구분에 따라 표시한다.
- 약관 제목을 `자동차 렌탈(대여) 약관`으로 고정해 인수형·반납형·렌탈·구독 구분에 따라 약관 정본이 달라지지 않게 했다. 약관 3쪽의 6개 단을 전체 균형 배치해 마지막 쪽에만 큰 빈공간이 몰리던 현상을 제거했다.
- 계약서 폰트를 `Pretendard` 400·500·600·700·800으로 통일하고 공식 v1.3.9 WOFF2와 SIL 라이선스를 로컬 자산으로 보관했다. PDF 생성기는 폰트를 데이터로 직접 임베드하고 `document.fonts.ready`와 모든 Pretendard FontFace 로딩 상태를 확인하며, 실패 시 맑은 고딕 등 대체 폰트로 PDF를 발행하지 않는다.
- 약관 본문을 10px(약 7.5pt)로 조정하고, 폰트 로딩 후 6개 A4 단을 다시 분할한 뒤 마지막 단의 긴 문단을 앞 단 여유 공간으로 역방향 보정하게 했다. 최종 6개 단은 사용 가능 높이의 93% 이상을 사용하고 넘침은 0이며, 90% 미만 사용·단간 높이 차 80px 초과·A4 넘침 시 PDF 생성을 실패시키는 게이트를 추가했다.
- 검토용 PDF를 A4 9쪽으로 재생성하고 전 페이지를 이미지로 렌더링했다. 약관은 4쪽에서 3쪽으로 줄이고 마지막 약관 쪽의 두 단을 균형 배치해 큰 빈 단을 제거했으며, 보험 경고문·제16조·마지막 동의·서명란에 잘림이 없음을 확인했다.
- 약관 번호 체계를 `제N조(제목) → ① 항 → 1. 호 → 가. 목`으로 통일하고, 조문 교차참조는 `제11조제2항제10호`, `제11조 및 제14조`처럼 완전한 법령식 표기로 정리했다. 축약 참조와 제목 띄어쓰기의 재유입을 막는 자동 검사를 추가했다.
- 프로덕션 `npm run build`, `tsc --noEmit`, fonts, 계약서↔전송약관 일치 50/50, 문서경계, 계약유형 60/60, source gate, template profile 10/10, snapshot, 고객화면 필드 54/54, 정책입력 검사를 통과했다.
- 이번 흐름·번호 수정 후 계약서↔전송약관 일치 및 번호 규격 52/52, 계약유형 60/60, 전자계약 준법구조 23/23, 착한거래 발행매핑 35/35, 문서경계 검사를 재통과했다. 약관 5~7쪽을 다시 렌더링해 조문 순서와 페이지 채움이 정상임을 확인했다.
- 최종 판정: **CONDITIONAL PASS**. 검토본·코드 반영은 완료했으나 계약 조건 변경이므로 실제 고객 발행·배포 전 렌터카회사와 사람/Claude의 최종 승인이 필요하다.

## 2026-08-11 계약서 표 선 단일 규격 재검수

- 본문 키값 표와 일반 표에 섞여 있던 내부 가로선 0.35pt·세로선 0.3pt·상하 마감선 0.8pt 구분을 제거하고, 모든 표선을 같은 색의 0.45pt 단일선으로 통일했다.
- 좌우가 열린 문서형 표는 유지하되 마지막 셀의 하단선과 외곽 하단선이 겹쳐 굵어지지 않도록 외곽 하단선을 제거했다.
- 검토용 PDF를 재생성하고 계약자·차량·대여조건·보험 보장·자차 표가 있는 1~2쪽을 144dpi PNG로 렌더링해 상하단과 내부선의 시각적 무게가 동일하고 글자·표 잘림이 없음을 확인했다.
- 약관 6개 단은 넘침 0, 채움률 90% 이상을 유지했다. `tsc --noEmit`, `npm run check:fonts`, `sim-esign-agreement` 57/57 PASS.

## 2026-08-11 계약서 섹션 간격 재검수

- 번호형 섹션 제목과 바로 아래 표의 간격을 3mm에서 1.5mm로 줄이고, 섹션 종료 후 다음 섹션까지의 간격을 5mm에서 7mm로 늘려 제목과 해당 내용의 묶음이 명확하게 보이도록 조정했다.
- 표 내부 소제목도 앞 내용과의 간격은 늘리고 뒤 내용과의 간격은 1.5mm로 줄였다. 조밀한 페이지와 보험 페이지는 페이지별 보정값을 같은 방향으로 적용했다.
- 계약 본문·보험 페이지를 144dpi PNG로 재렌더링해 섹션 구분, 표 정렬, 하단 잘림이 없음을 확인했다. 약관 6개 단 넘침 0, `tsc --noEmit`, fonts, `sim-esign-agreement` 57/57, 샘플 페이지 HTTP 200 PASS.

## 2026-08-11 약관 장 제목 제거 재검수

- 21개 조문 규모에서 반복되던 `제1장~제3장` 장 제목과 장식선을 제거하고 `제1조~제21조`만 순서대로 이어지게 했다. 조문 본문·번호·교차참조는 변경하지 않았다.
- 인쇄용 문장 분할 단위를 조정해 장 제목 제거 후에도 약관 6개 단의 채움률을 91% 이상으로 유지했다. 3개 약관 페이지를 144dpi PNG로 재렌더링해 빈 단·고아 조 제목·잘림이 없음을 확인했다.
- `tsc --noEmit`, fonts, 인쇄/PDF 약관과 전송 약관 원문 일치 및 장 제목 부재 검사 포함 `sim-esign-agreement` 58/58 PASS.

## 2026-08-11 금지행위 목록·번호 간격 재검수

- 제9조 금지행위 11개 호를 무단 양도·전대, 운전자 위반, 은닉·반환거부, 장치·서류 조작, 불법목적 사용, 제3자 권리, 무단개조, 기타 중대침해의 8개 범주로 통합했다. 기존 보호 범위는 삭제하지 않고 관련 항목 안에 병합했다.
- 목록에 적용되던 양쪽 맞춤을 해제하고 번호 뒤 한 칸, 둘째 줄부터 본문 시작 위치에 맞는 내어쓰기로 통일했다.
- 제9조가 포함된 약관 페이지를 180dpi PNG로 재렌더링해 번호 간격·줄맞춤·단 배치·잘림을 확인했다. `tsc --noEmit`, fonts, 인쇄/PDF와 전송 약관 원문 일치 및 금지행위 8개 호 검사 포함 `sim-esign-agreement` 59/59, 샘플 페이지 HTTP 200 PASS.

## 2026-08-11 약관 항 번호 간격 전수 수정

- 약관 일반 문단의 `text-align: justify`를 제거하고 왼쪽 맞춤과 고정 단어 간격을 적용했다. 이에 따라 `①` 등 항 번호 뒤 단어 간격이 줄 길이에 따라 강제로 벌어지지 않는다.
- 사용자가 지적한 제15조와 제9조가 포함된 약관 2~3쪽을 180dpi PNG로 재렌더링해 항 번호 뒤 한 칸, 문단 줄바꿈, 단 배치와 잘림을 확인했다.
- 양쪽 맞춤 재유입 방지 검사까지 추가했다. `tsc --noEmit`, fonts, `sim-esign-agreement` 60/60, 샘플 페이지 HTTP 200 PASS.

## 2026-08-11 약관 한글 글자 경계 줄바꿈 재검수

- 전역 `word-break: keep-all`을 약관 문단에서만 해제하고 `word-break: normal`, `line-break: strict`를 적용했다. 한글은 칸 끝의 글자 경계에서 자연스럽게 줄바꿈되고 영문·숫자는 가능한 한 단어 단위로 유지된다.
- 약관 2~3쪽을 180dpi PNG로 재렌더링해 오른쪽 큰 빈칸 감소, 항 번호 간격, 조문 제목과 본문 줄바꿈, 잘림이 없음을 확인했다. 약관 6개 단 채움률 90% 이상과 넘침 0을 유지했다.
- 왼쪽 맞춤·한글 글자 경계 줄바꿈 고정 검사 포함 `tsc --noEmit`, fonts, `sim-esign-agreement` 60/60, 샘플 페이지 HTTP 200 PASS.

## 2026-08-11 단독 항 번호 제거 재검수

- 제9조 금지행위는 항이 하나뿐이므로 단독 `①`을 제거하고 안내문 뒤에 1~8호가 바로 이어지게 했다. 조문 내용과 8개 금지행위 범주는 변경하지 않았다.
- 전체 21개 조문을 확인한 결과 다른 조문은 모두 `②` 이상이 이어지는 정상 다항 구조였다. 제9조 페이지를 180dpi PNG로 재렌더링해 제목·안내문·호 목록 흐름과 잘림이 없음을 확인했다.
- 단독 항 번호 재유입 방지 검사 포함 `tsc --noEmit`, fonts, `sim-esign-agreement` 61/61, 샘플 페이지 HTTP 200 PASS.

## 2026-08-11 차량 사용 제한 문안 재검수

- 제9조 제목을 추상적인 `금지행위`에서 `차량 사용 제한`으로 바꾸고, 안내문의 `다음 각 호` 표현을 제거했다.
- 8개 보호 범위는 유지하면서 각 호 앞에 `무단 양도·대여`, `위험·부적격 운전`, `은닉·무단반출`, `장치·기록 훼손`, `목적 외 사용`, `제3자 권리 발생`, `임의 변경·훼손`, `그 밖의 중대한 침해`라는 짧은 분류명을 붙였다.
- 인쇄 약관과 전송 약관 정본을 함께 수정하고 제목·생애주기·8개 호 검사를 새 문안에 맞게 갱신했다.
- `sim-esign-agreement` 61/61, `tsc --noEmit`, fonts PASS. 로컬 서버를 재기동하고 PDF 7쪽을 직접 열어 2단 배치와 페이지 넘침이 없음을 확인했다.
## 2026-08-11 공급사 표준 헤더·사진 연동 정합성 재검증

- 실제 공급사 Sheets Grid를 쓰기 없이 전수 조회했다. 표준양식의 `배차상태→상태`, `차종→차명(트림)` 변경과 선택 `비고` 열 제거 때문에 저장된 과거 매핑 프로필이 일반 공급사 대부분을 차단하던 회귀를 확인했다.
- 공식 별칭이 현재 헤더에서 정확히 하나로 해석될 때만 저장 매핑을 재결합하고, 중복·미확인 핵심 헤더는 계속 fail-closed로 유지했다. 선택 메모 열만 안전하게 누락 허용했다.
- 회귀 시뮬레이션을 추가했고 `scripts/sim-sheet-merge.mts` **166/166 PASS**.
- 실제 시트 사진 링크는 **205/356대** 확인. 사진 0건 공급사는 셀 링크 자체가 0건이며 ERP 전달 누락이 아니다.
- 검증기의 v3/v4 파트너 노드 중복 출력은 공급사 코드 기준으로 제거했다. 운영 허브의 중복 코드는 0건이다.
- `npx tsc --noEmit`, `check:tokens`, `check:ui` PASS. 디자인 토큰·UI 계약 드리프트 0건.

## 2026-08-11 프리패스 표준 렌탈 약관 V8 구조·방어력 보완

- 기존 21개 조문과 렌터카 계약의 핵심 보호범위를 유지하면서 약관 흐름을 계약 성립 → 대여·정산 → 운전자격·보험 → 인도·관리 → 사고 → 해지·회수 → 통지 → 반납·정산 → 개인정보·분쟁 순서로 정리했다.
- 운전자격은 계약서뿐 아니라 실제 보험·공제의 연령·면허·경력·운전자 범위를 함께 따르게 했고, 사고 미통지에 따른 보상 제한은 해당 위반과 상당인과관계가 있는 범위로 명확히 했다.
- 사고 수리의 예상비용 사전고지와 실제 견적서·정비명세서·영수증 제공, 미납 통지의 발송기록, 안전하게 정차된 차량에 대한 보호조치, 평온한 회수 원칙을 명시했다.
- 회사 귀책 해지 시 미사용 대여료와 반환 대상 보증금의 반환 방향, 확정 인수가격의 사고·시세만을 이유로 한 증액·거절 금지, 특별손해의 예견가능성, 주민등록번호의 구체적 법령 근거 요건을 보완했다.
- 계약 유형과 공급사에 관계없이 약관 정본은 하나로 유지하고, 회사별 보험·공제·정비·가격 정책은 개별계약서와 실제 첨부자료에서 확정하도록 했다. 문서 버전은 `rental-v8-2026-08-11`이다.
- 약관 글자 크기 10px와 Pretendard를 유지한 채 조문·항 사이 여백과 자동 단 배치를 조정했다. 마지막 재배치가 A4 높이를 넘는 경우 안전한 1차 배치로 복귀해 글자 축소·잘림 없이 3쪽 6단을 유지한다.
- A4 9쪽 PDF를 재생성하고 약관 3쪽 및 동의·서명 페이지를 PNG로 렌더링해 겹침·잘림·고아 제목·빈 페이지가 없음을 확인했다. 6개 단의 사용 높이는 875.1~940.6px / 956.4px, 넘침 0건이다.
- `sim-esign-agreement` **70/70**, `sim-esign-contract-kind` **60/60**, `sim-esign-template-profile` **10/10**, `sim-esign-document-boundary`, `npx tsc --noEmit`, `npm run check:fonts` PASS.
- 최종 판정: **CONDITIONAL PASS**. 샘플·코드 반영은 완료했지만 약관·보험·회수 조건 변경이므로 실제 고객 발송 전 각 렌터카회사와 사람/법률 검토자의 최종 승인이 필요하다.
## 2026-08-11 새 공급사 허브 이전 검증

- 새 허브 `1cRn_XbuJXQMlVCATtDN4EpQy-KVEi65tCwcvCxdFk8w`의 `공급사연동!A1:L100`을 실데이터로 확인했다.
- F열 `시트 열기`는 CSV에서 표시문구만 남으므로 J열 `그 주소(복사용)`을 원본 URL SSOT로 사용한다.
- ERP 기본 허브를 새 시트로 교체하고, 서버는 서비스 계정, 브라우저는 관리자 인증 API를 통해 비공개 허브를 읽도록 연결했다.
- 서비스 계정 실측: 공급사 17행, RP004·RP023·RP031·RP021 URL 정확 추출.
- 로그인 HTML 오인 차단 및 새 허브 파서 시뮬레이션 **3/3 PASS**. TypeScript·토큰·UI 계약 PASS.

## 2026-08-11 프리패스 표준 렌탈 약관 V9 전면 구조개편

- 기존 조문을 부분 수정하는 수준이 아니라 실제 계약 생애주기 순서로 21개 조문을 전면 재배치했다: 계약문서·체결 → 차량 인도·운전자 → 사용·관리 → 보험·위치정보 → 지급·보증금 → 사고·전손 → 연체·해지 → 반납·중도해지·주행거리·만기인수 → 정산·통지·분쟁.
- 기존 제9조 `차량 사용 제한`은 제5조 `차량 사용 원칙 및 제한`으로 이동·정리했고, 새 제9조는 `대여료 지급과 연체이자`로 구성했다. 사고 접수·수리와 전손·도난, 연체 차량보호조치와 계약 해지·차량회수를 각각 별도 조문으로 분리했다.
- 연대보증은 별도 후반 조문에서 계약문서·적용순서를 설명하는 제1조로 통합했다. 계약서 본문, 전송 약관, 중요조항 배지, 동의문서, 정책 메타데이터, 입력 도움말과 시뮬레이션의 모든 교차참조를 V9 번호로 동기화했다.
- 문서 버전은 `rental-v9-2026-08-11`이다. Pretendard 10px를 유지한 채 조문 간격과 줄높이만 조정해 약관 3쪽 6개 단을 유지했다. 단 사용 높이는 910.8~938.7px / 956.4px, 넘침 0건이며 렌더 이미지에서 겹침·잘림·고아 제목이 없음을 확인했다.
- `sim-esign-agreement` **75/75**, `sim-esign-contract-kind` **60/60**, `sim-esign-template-profile` **10/10**, `sim-chakhandeal-esign` **37/37**, `sim-esign-document-boundary`, 조문 배지 **27/27**, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check` PASS. 구형 V8 조문명·주요 교차참조 잔존 검색 0건.
- 로컬 4004 개발 서버를 정상 재시작하고 브라우저 PDF에서 V9 첫 약관 페이지와 2단 A4 배치를 직접 확인했다.
- 최종 판정: **CONDITIONAL PASS**. 구조·표현·기술 검증은 완료했지만 실제 고객 발송 전 렌터카회사와 사람/법률 검토자의 최종 승인이 필요하다.

## 2026-08-11 프리패스 버튼·하단바 업무동선 보완

- 검증 요구사항: 프리패스 ERP의 버튼 위치와 하단바가 실제 업무 순서를 따르고, 모바일에서도 이전·보조 행동·핵심 실행이 겹치지 않아야 한다.
- 공통 `BottomNav`에 실행바 식별자를 추가하고 `이전(왼쪽) → 보조 행동 → 핵심 실행(오른쪽)` 순서와 폭을 통일했다.
- 상품 상세 모바일에서 검수요청·공유는 아이콘형 보조 행동으로 줄이고, 계약문의는 우측의 넓은 핵심 버튼으로 유지했다. PC에서는 검수요청 옆 사진 전체받기와 기존 텍스트 라벨을 유지한다.
- 일반 상세 `DetailShell`은 모바일에서 상단 실행/하단 이전으로 갈라지던 구조를 제거하고 이전과 실행을 동일 하단바에 배치했다. 안전영역만큼 본문 하단 여백도 함께 확보했다.
- `npx tsc --noEmit`, `npm run check:fonts`, `npm run check:tokens`, `npm run check:ui`, `git diff --check` PASS. 로컬 `http://localhost:4004/` HTTP 200 확인.
- 저장소에는 `test` 스크립트가 없어 `npm test`는 실행 대상이 아니었다.
- 최종 판정: **PASS**.

## 2026-08-11 프리패스 표준 렌탈 약관 V10 — 공정위 표준약관형 재구성

- 공정거래위원회 자동차대여 표준약관의 28개 조문 흐름을 뼈대로 삼아 장기 월렌트에 맞게 전면 재구성했다. 목적·계약신청·체결·대체차량·요금·보험·인도·관리·금지행위·사고·반납·정산·분쟁 순서를 유지하되 단기 예약금·시간대별 환불 규정은 장기렌트의 월 대여료·보증금·중도해지·초과주행·만기인수 기준으로 치환했다.
- 프리패스 핵심 조건인 만 21세 이상 운전자, 사고일 기준 직전 1년 내 과실 50% 이상 사고 총 3회, 회사별 보험·공제 조건, 자차 자기부담액의 실제 수리비 상한, 중고차 통상 사용흔적과 미고지 중대하자 구분, GPS 차량보호조치, 초과주행 및 만기인수를 28개 조문에 통합했다.
- 인쇄 템플릿, 전자계약 전송 약관, 중요조문 강조, 동의화면 조항 배지, 공급사 정책 입력표와 도움말의 교차참조를 V10 번호로 동기화했다. 사고다발 횟수는 정책별 임의값이 아니라 프리패스 표준 3회로 고정했다.
- 문서 버전은 `rental-v10-2026-08-11`이다. Pretendard를 유지하고 약관 본문 10.1px·2단 자동 페이지네이션을 조정해 약관 3쪽 6개 단을 858.0~921.7px / 956.4px로 채웠으며 넘침 0건이다. 전체 9쪽 PDF를 PNG로 재렌더링해 표, 약관, 동의·서명 페이지의 겹침·잘림·깨진 글자와 고아 제목이 없음을 확인했다.
- `sim-esign-agreement` **39/39**, `sim-esign-contract-kind` **60/60**, `sim-chakhandeal-esign` **37/37**, `sim-esign-template-profile` **10/10**, `sim-esign-document-boundary`, 조문 배지 **27/27**, `npx tsc --noEmit`, `npm run check:fonts` PASS.
- 최종 판정: **CONDITIONAL PASS**. 공정위 표준약관의 구조를 참고해 만든 프리패스 장기렌트 약관이며 공정위 승인 표준약관 자체는 아니다. 실제 고객 발송 전 각 렌터카회사와 사람/법률 검토자의 최종 승인이 필요하다.

## 2026-08-11 프리패스 공통 상·하단 작업바 2차 통합

- `TopBar`가 별도로 그리던 모바일 이전 바를 공통 `BottomNav`로 교체해 탭바 간격, 안전영역, 높이, 그림자와 뒤로가기 동작을 한 규격으로 맞췄다.
- 일반 목록의 `MobileListDock`도 `fp-action-dock` 계층을 사용하도록 연결하고 액션을 직접 배치해 보조 버튼은 작게, solid 결정 버튼은 남는 폭을 쓰도록 통일했다.
- 모바일 상단 제목은 `min-width: 0`으로 정상 말줄임하고 우측 액션은 잘라 숨기지 않도록 유지했다. React 검수에서 새 훅, 비직렬화 경계, 비동기 waterfall 또는 접근성 회귀는 발견되지 않았다.
- `npx tsc --noEmit`, `npm run check:fonts`, `npm run check:tokens`, `npm run check:ui`, `git diff --check` PASS. 로컬 `http://localhost:4004/login`을 394px 폭에서 직접 새로고침해 렌더링과 콘솔 오류 0건을 확인했다.
- 최종 판정: **PASS**.

## 2026-08-11 상품찾기 데스크톱 툴바 반응형 보완

- 필터가 열렸을 때의 실제 상품 목록 폭을 기준으로 반응하도록 `FinderToolbar`에 CSS 컨테이너 쿼리를 적용했다. 화면 전체 폭이 같아도 필터 열림·닫힘에 맞춰 툴바가 자체적으로 재배치된다.
- 목록 폭이 좁아지면 결과 설명, 최근·관심·문의 라벨, 간단·상세·엑셀 라벨 순서로만 축약하고 검색창·정렬·필터·보기 전환 기능은 유지한다. 비활성 구글시트 예약 슬롯은 가장 좁은 구간에서만 해제한다.
- 검색 입력의 인라인 최소 폭을 제거하고 그룹 단위 최소 폭으로 관리해 필터를 열어도 공급사 검색어와 핵심 액션이 잘리지 않도록 했다. 기본 정렬은 기존 `FINDER_DEFAULT_SORT='asc'`와 실제 옵션을 유지했다.
- `npx tsc --noEmit`, `npm run check:fonts`, `npm run check:tokens`, `npm run check:ui`, `git diff --check`, 로컬 로그인 경로 HTTP 200 PASS.
- 최종 판정: **PASS**.

## 2026-08-11 전 메뉴 정렬 기본값·작업면 비율 보완

- 공통 웹 목록 정렬기가 `기본`이라는 임의 옵션을 생성하던 경로를 제거했다. `defaultValue`가 있으면 빈 선택지를 노출하지 않고, 없으면 중립적인 `정렬` 안내만 노출한다.
- 공통 모바일 정렬 시트도 `기본` 문구를 제거했다. 초기화 버튼은 메뉴가 지정한 실제 기본 옵션명(상담데스크는 `최근순`)을 표시하고 해당 값으로 복원한다.
- `WorkPage`는 작업 패널이 하나일 때 목록 1 : 작업면 3을 기본으로 사용한다. 패널이 여러 개면 목록과 각 업무 패널을 동일 비율로 유지해 계약·재고·회원의 4분할 구조는 바뀌지 않는다.
- 공통 컴포넌트에서 남은 사용자 노출용 `기본` 정렬 문구 0건을 확인했다. `npx tsc --noEmit`, `npm run check:fonts`, `npm run check:tokens`, `npm run check:ui`, `git diff --check`, 로컬 로그인 경로 HTTP 200 PASS.
- 최종 판정: **PASS**.

## 2026-08-12 관리자 상담데스크 처리 신호 강화

- 상담 목록의 기존 상태 판정은 유지하면서 `미확인` 행에는 amber, 읽었지만 답하지 않은 `미회신` 행에는 red 처리선을 행 전체 왼쪽에 표시하도록 공통 `FeedListRow`에 선택적 강조 톤을 추가했다.
- 미확인은 숫자 뱃지와 황색 경고 아이콘, 미회신은 빨간 아이콘·`미회신` solid 뱃지·적색 처리선으로 서로 다른 상태를 유지한다. 선택 행 배경은 인라인 우선순위로 보존되어 현재 선택과 처리 상태가 섞이지 않는다.
- `sim-chat-attention`에서 미확인·미회신·회신완료 판정, 전체 기본 필터, 최근순 기본 정렬, 최근 메시지 정렬, 미회신 필터를 모두 통과했다.
- 이전 정렬 UI 구조 변경에 맞춰 `check-menu-sort-defaults`를 갱신했고 웹·모바일에서 이름 없는 `기본` 옵션이 다시 생기지 않는 검사까지 포함해 모두 PASS.
- `npx tsc --noEmit`, `npm run check:fonts`, `npm run check:tokens`, `npm run check:ui`, `git diff --check` PASS. 시뮬레이션의 Firebase 환경변수 경고는 로컬 셸에 운영 환경변수를 주입하지 않은 안내이며 순수 상태 판정은 통과했다.
- 최종 판정: **PASS**.

## 2026-08-12 프리패스 전자계약 실제 발송동선·보험정책 안전 게이트

- 관리자 전자계약에서 `프리패스모빌리티 주식회사`를 실제 선택해 공급사 전용 정책만 조회되는 동선을 확인했다. 현재 프리패스 공급사에는 연결 정책이 없어 임의 조건으로 계약을 만들지 않고 발송을 차단한다.
- 정책관리의 공급사코드 직접입력을 계약회사명 드롭다운으로 바꿨다. 보험사·공제조합은 다른 담보와 같은 보험 패널에서 입력하며, 대인Ⅰ·대물 2천만원·자손/무보험/자차 미가입처럼 실제 증권에 존재할 수 있는 명시값을 선택할 수 있다.
- 프리패스 기본정책에서 보험사, 보상한도, 면책금, 자차 자기부담률·하한·상한, 긴급출동의 임의 기본값을 제거했다. 보험포함 계약은 담보가 없더라도 `미가입`·`없음`으로 명시해야 하며 빈칸이면 초안 검토와 링크 발행을 막는다.
- 고객 확인 화면은 차량번호·모델명만 표시하고, 만기 조건은 `만기 처리` 한 줄로 통합했다. 렌트 문서 제목은 `자동차 장기대여 계약서`로 통일했다.
- `npx tsc --noEmit`, `npm run check:fonts`, `sim-freepass-esign`, `sim-esign-contract-kind` **60/60**, `sim-esign-agreement` **44/44**, `sim-esign-issue-gate` **5/5**, `sim-esign-snapshot` PASS. Chrome 관리자 세션에서 `/esign` 공급사 연동 차단과 `/policy` 계약회사 드롭다운·보험 입력항목을 직접 확인했다.
- 최종 판정: **CONDITIONAL PASS**. 전자계약 시스템 동선과 안전 게이트는 준비됐지만, 프리패스 명의 또는 실제 계약 렌터카사의 가입증권·자차면책 규정으로 정책을 확정하기 전에는 고객 링크를 발행하면 안 된다.

## 2026-08-12 전자계약 직원입력·고객 본인확인 완료본 연결

- Google Drive의 `프리패스모빌리티계약현황.xlsx`와 실제 자동차보험 가입증명서 3건을 대조했다. 계약현황에는 여러 렌터카사·보험사가 함께 있고, 같은 스위치플랜 안에서도 차량별 운전자 연령과 자차 가입 여부가 달랐다. 공탁보증보험 증권은 자동차보험 근거에서 제외했다.
- 계약회사 선택지에 연결 정책 수 또는 `정책 필요`를 표시하고, 선택한 회사의 정책만 드롭다운에 노출한다. 정책명과 함께 보험사·연령·자차·발송 가능 상태를 표시하며, 정책 없음/미완성은 정책관리 이동과 함께 차단한다.
- 직원 직접입력은 계약일·고객명·연락처, 차량번호·모델명·옵션, 대여기간·월 대여료·보증금 중심으로 축소했다. 사업자·인수형·건별 특약은 접어서 해당 계약에서만 입력한다. 월 납부일은 회사별 정책값(`payment_due_date`)으로 자동 적용한다.
- 고객 링크에서 주민등록번호 13자리와 주소를 필수 확인하고 운전면허증·셀카를 첨부한다. 면허번호 별도 입력은 제거했다. 주민번호·주소는 공개 계약 노드나 발행 세션에 복제하지 않고, 관리자 승인 시 완료 PDF 스냅샷에만 합성한다.
- `npx tsc --noEmit`, `npm run check:fonts`, `sim-esign-signed-snapshot`, `sim-freepass-esign`, `sim-esign-agreement` **44/44**, `sim-esign-issue-gate` **5/5**, `git diff --check` PASS. Chrome `/esign`에서 핵심 입력 축소, 회사별 정책 개수, 프리패스 정책 없음 차단, 정책 상세 라벨을 직접 확인했다.
- 최종 판정: **CONDITIONAL PASS**. 입력·본인확인·완료본 연결은 준비됐다. 고객 링크 발행은 실제 계약회사의 차량/보험 프로필 정책이 등록되고 계약회사 사업자·대여업 등록정보가 채워진 뒤에만 가능하다.

## 2026-08-12 정책등록 프리패스 기본 패키지 추적

- `정책 등록`을 누르면 `freepass-standard-2026-08-12-v3` 패키지의 확정 기본값 44개가 즉시 입력되고, 적용 버전이 `policy_default_pack`에 함께 저장되도록 검증했다.
- 보험사·보상한도·면책금·긴급출동·탁송비 등 공급사별 실제 증권과 조건이 필요한 15개는 임의값을 만들지 않고 `공급사 확인 필요` 안내로 분리했다. 이 중 전자계약 필수 14개가 비어 있으면 발송 게이트가 계속 차단한다.
- 패키지는 정책에 최초 한 번만 적용한다. 적용 뒤 공급사가 항목을 삭제하거나 미사용으로 비워 저장하면 재조회·상품 조인·계약 payload 생성 과정에서 기본값이 되살아나지 않도록 패키지 스탬프를 기준으로 보존한다. 공급사의 명시 입력값도 덮어쓰지 않는다.
- `preview-policy-defaults`에서 기본값 44개, 공급사 확인 15개, 전자계약 미충족 14개, 패키지 스탬프, 명시값 우선, 삭제·미사용 보존을 모두 확인했다.
- `npx tsc --noEmit`, `npm run check:fonts`, `npm run check:tokens`, `npm run check:ui`, `git diff --check` PASS.
- 최종 판정: **CONDITIONAL PASS**. 자동입력·삭제 보존·발송 차단 구조는 통과했다. 실제 보험·면책·탁송 값 확정 및 운영 게시 전에는 공급사/사람의 최종 승인이 필요하다.

## 2026-08-12 소속 없는 영업자 자가가입 즉시 이용

- 기존 가입은 `status=pending`으로 저장되어 화면과 RTDB 모두 관리자 승인 전까지 차단했다. 사용자 요구에 맞춰 신규 계정은 회사·채널 권한 없이 `role=agent`, `status=active`로 생성하고 상품찾기·본인 상담·본인 계약을 즉시 이용하도록 변경했다.
- 자가가입 `user_code`는 충돌 가능한 순번이나 공유 `SP999`가 아니라 Firebase UID로 고정했다. Rules도 최초 `agent + active + user_code=auth.uid` 조합만 허용하고 회사·채널 자가 입력과 user_code 사후 변경을 금지한다. 관리자는 이후 실제 소속 확인 뒤 별도 재배정할 수 있다.
- 가입 화면에서 회사명·활동유형·사업자번호는 선택 정보로 내리고 즉시 이용·후속 소속 연결을 명확히 안내했다. 이름은 실제 상담방 생성에 필요하므로 명시적 필수 검증을 추가했다. 개인정보처리방침도 실제 필수/선택 수집 항목에 맞춰 `2026-08-12` 버전으로 갱신했다.
- `check-self-serve-signup` 12항목, `sim-release-blockers` **17/17**, `npx tsc --noEmit`, `npm run check:fonts`, `npm run check:tokens`, `npm run check:ui`, `git diff --check` PASS.
- 인앱 브라우저 `http://localhost:4004/login`에서 가입 폼을 직접 열어 즉시 이용 문구, 선택 소속 필드, 필수 4필드, 콘솔 오류 0건을 확인했다. 390×844에서도 가로 넘침·잘린 컨트롤 0건이며 하단 가입 버튼까지 정상 스크롤된다.
- 최종 판정: **CONDITIONAL PASS**. 앱·로컬 Rules 정합성은 통과했다. `database.rules.json`과 개인정보 문서가 포함되므로 사람/Claude의 실데이터·법률 게이트 전에는 Rules 게시 및 운영 배포 금지다.

## 2026-08-12 계약문의 빈 방 자동생성 차단

- 상품 상세 진입 시 `ensureRoom()`을 실행하던 흐름을 읽기 전용 `findExistingRoom()`으로 교체했다. 방이 없으면 채팅 대신 `계약문의 시작`을 표시하고, 사용자가 시작 버튼이나 실제 출고문의를 눌렀을 때만 방을 생성한다.
- 출고문의로 생성한 계약은 같은 방에 즉시 연결하고 `fp:unread`로 상세·계약문의 목록을 갱신한다. 상품코드나 영업자 코드가 없으면 `CH_undefined_*` 형태의 고아 방 키를 만들지 않는다.
- 계약 연결·메시지 메타·안읽음이 전혀 없는 빈 방 셸은 계약문의 일반 목록과 숫자, 상품의 `문의중`, 공급사·관리자 상세 문의 목록에서 제외한다. `전체`·`미확인`·`미회신`·`내 차례`와 햄버거/탭 뱃지가 모두 같은 활동 방 집합을 쓰도록 `activeChatRooms()`로 통일했다.
- 명시적인 `?room=` 딥링크는 빈 방도 작업면 선택을 유지해 첫 메시지를 작성할 수 있게 하고, 일반 검색·필터에서 사라진 선택만 닫는다. 계약 인덱스로 복원되는 레거시 방도 보존한다.
- `sim-chat-attention` **19/19**, `sim-chat-room-dedupe` **37/37**, `sim-release-blockers` **17/17**, `sim-test-contract` **6/6**, `npx tsc --noEmit`, `npm run check:fonts`, `npm run check:tokens`, `npm run check:ui`, `git diff --check` PASS.
- React 검수에서 상세 방 조회 effect는 상품코드·역할의 원시값 의존성만 사용하고 이벤트 리스너를 cleanup하며, 명시 액션은 버튼 핸들러에 유지했다. 로컬 `http://localhost:4004/`는 로그인 화면으로 정상 리디렉션됐고 앱 콘솔 오류는 없었다. 인증 세션이 없어 상품 상세의 실클릭 검증은 순수 상태 시뮬레이션으로 대체했다.
- 최종 판정: **PASS**. 기존 빈 방 레코드는 삭제하지 않고 표시만 제외하므로 데이터 파괴나 Rules 게시이 없다.

## 2026-08-12 테스트 역할 제거·상품 보기 전환 재검수

- 루트 레이아웃의 좌측 하단 `테스트 역할` 스위치와 전용 컴포넌트·도메인을 제거했다. 버튼만 숨기지 않고 `getRole()`의 `fp4_dev_role` 덮어쓰기도 제거해 브라우저에 남은 과거 값이 실제 세션 역할 화면을 조용히 바꾸지 못하게 했다.
- 상품찾기의 `간단·상세·엑셀`은 서로 다른 업무 액션이 아니라 같은 상품 목록의 표시 방식 선택기다. 단일 세그먼트와 선택 강조는 유지하고 `상품 보기 방식` 접근성 그룹을 부여했다.
- 저장된 보기 값은 `card | list | excel`만 복원하도록 검증해 손상되거나 구버전 값 때문에 실제 엑셀 표와 선택 표시·카운트가 어긋나는 경우를 차단했다. 기본 보기 `excel`, 모바일 `card` 규칙은 그대로다.
- `npx tsc --noEmit`, `npm run check:fonts`, `npm run check:tokens`, `npm run check:ui`, `sim-release-blockers` **17/17**, `git diff --check` PASS. React 검수에서 추가 비동기 요청·전역 이벤트·리렌더 구독은 없고, 보기 변경은 기존 사용자 이벤트 핸들러와 `useDeferredValue` 경계를 유지했다.
- 인앱 브라우저 `http://localhost:4004/login`을 새로고침해 `테스트 역할` 표시 0건과 콘솔 오류 0건을 확인했다. 인증 세션이 없는 브라우저라 상품목록 실클릭은 소스·타입·UI 계약 검증으로 확인했다.
- 최종 판정: **PASS**.

## 2026-08-12 회사별 승계 가능여부·승계수수료 계약 반영

- 공급사 정책의 `succession_allowed`와 `succession_fee`를 계약층 정본으로 사용한다. 프리패스 신규 기본정책은 `협의 / 1,000,000원`이며 공급사가 회사별 실제 적용값으로 수정한다. 중복 초안 필드는 만들지 않았다.
- 정책관리, 공급사 정책수집표, 실제 계약서, 고객 주요조건 확인 화면을 같은 두 필드로 연결했다. 계약서에는 `계약 승계수수료`와 `승계 조건`을 나란히 표시하고 약관 제8조는 계약서상 수수료와 확정 비용의 별도 정산 근거를 둔다.
- 약관 정본 버전을 `rental-v15-2026-08-12`로 올리고 HTML 인쇄본과 전자 전송본이 완전히 같은지 검증했다. 검토용 PDF를 재생성했으며 A4 9페이지, 약관 6개 컬럼 모두 overflow 0이고 2페이지 승계 행과 후반 약관·서명 페이지의 잘림·겹침이 없음을 PNG로 확인했다.
- `npx tsc --noEmit`, `npm run check:fonts`, `check-policy-layers`, `preview-policy-defaults`, `sim-esign-display-fields` **54/54**, `sim-esign-agreement` **47/47**, `sim-esign-template-profile` **10/10**, `sim-esign-issue-gate` **5/5** PASS. `check-policy-inputs`의 기존 월 납부일 직접입력 1건과 `sim-esign-field-map`의 기존 템플릿 원자 3건은 이번 승계 필드와 무관한 선행 불일치로 남아 있다.
- 로컬 개발 서버를 재기동해 `/esign/sample-contract?review=succession-fee-v15` HTTP 200, PDF 167,566 bytes를 확인했다.
- 최종 판정: **CONDITIONAL PASS**. 구현·표기·A4 정합성은 통과했다. 승계수수료는 공급사별 금전조건이므로 실제 업체 회신값 확인 및 사람/Claude 게이트 전에는 운영 게시하지 않는다.

### 승계 조항 문단 분할 보정

- 약관 자동 페이지네이션이 제8조제3항을 두 문장 조각으로 나눠 별도 문단처럼 보이게 하던 문제를 확인했다. 해당 항을 `t-keep` 의미 단위로 지정해 한 문단으로 유지했다.
- 모든 항을 통째로 묶으면 마지막 약관 단에 267px 공백이 생겨 A4 균형 기준을 벗어나므로, 의미상 연결이 특히 중요한 승계 조항만 분할 금지하고 나머지 자동배치 구조는 유지했다.
- `sim-esign-agreement` **48/48** PASS. 재생성 PDF의 약관 6개 단은 overflow 0, 잔여 높이 2.5~36.2px이며 제8조제3항이 번호부터 마지막 정산 문장까지 한 문단으로 출력되는 것을 PNG로 확인했다.

### 약관 전체 자동분할 문단 병합

- 제12조제1항 등 긴 항을 A4 단 균형 계산용으로 나눈 뒤, 같은 단에 연속 배치된 조각이 별도 `p`로 남아 문장 중간에 강제 줄바꿈·문단 간격을 만들던 전역 원인을 수정했다.
- 각 조각에 원문 항 식별자를 부여하고 단 배치가 끝난 뒤 같은 단의 연속 조각을 다시 하나의 문단으로 병합한다. 실제 단·페이지 경계를 넘는 경우에만 다음 단에서 자연스럽게 이어진다.
- 렌더러는 최종 약관 DOM에 자동분할 마커나 조각 클래스가 하나라도 남으면 실패하도록 검증을 추가했다. `sim-esign-agreement` **49/49** PASS, 약관 6개 단 overflow 0·A4 균형 PASS.
- 약관 3페이지 전체 PNG를 대조해 제2조·제6조·제8조·제12조·제16조·제17조·제22조·제24조·제25조·제27조의 장문 항에서 같은 단 내부의 인위적인 문단 분리가 제거된 것을 확인했다.

## 2026-08-12 공급사 원본 → 프리패스 연동정본 → ERP 파이프라인

- 공급사 원본을 ERP에 바로 쓰던 경로를 제거하고 `원본 Grid 읽기 → 공급사 어댑터 정규화 → 프리패스 숨김 정본 탭 기록 → 같은 탭 되읽기·행 검증값/전체 지문 대조 → ERP v4 CAS 반영 → 영업자 상품리스트 갱신` 순서로 통일했다.
- 관리자 수동 연동과 일일 연동은 같은 서버 파이프라인을 쓴다. 숨김 행·숨김 탭 제외, 보이는 전체 탭, 명시 gid 순서, 중복 공급사 코드 정본 선택도 한 리더로 합쳤다. 관리자가 충돌 시 정본을 건너뛰고 안전 필드만 직접 반영하던 우회는 차단했다.
- 정본에는 공급사코드·공급사명·상품코드·차번·상태·차명·주행거리·배기량·요금기간·원본 탭/판독행과 손실 없는 JSON을 기록한다. 행 검증값, 동기화 회차, 전체 지문, 45,000자 셀 상한을 되읽기 단계에서 검증한다.
- 가격 미입력은 반영 제외가 아니라 진단 부분집합으로 정리했다. 실차번이 있는 가격 미입력 재고는 ERP·상품찾기·최근·관심·사진 다운로드·영업자 시트에 표시하고, 손님 카탈로그·공유 견적·계약 행동만 가격 입력 전 차단한다. 가격 미입력 수를 원본 행 합계에 다시 더하던 중복 집계도 제거했다.
- 운영 설정 그대로의 쓰기 없는 전수 감사는 **16곳·원본 412행·후보 362대**, 아이카가 숨긴 `미사용` gid를 가리켜 전체 BLOCKED였다. 아이카를 보이는 3개 gid(`1492960151,1795760937,742390595`)와 현재 헤더 자동매핑으로 메모리에서만 교체한 감사에서는 **원본 561행·후보 485대·전체 PASS**였다. 아이카는 원본 149행 중 출고불가 25, 중복 1을 제외해 123대이며 가격 미입력 2대도 후보에 포함된다. 수수료 공지행을 차량 무효행으로 오인하던 파서도 회귀검사와 함께 수정했다.
- `npx tsc --noEmit`, `npm run check:fonts`, `npm run check:tokens`, `npm run check:ui`, `sim-sheet-merge` **169/169**, `sim-supplier-sheet-read` **14/14**, `sim-sheet-canonical-snapshot`, `sim-product-offerability` **31/31**, `git diff --check` PASS.
- 운영 Google Sheet·RTDB에는 이번 검증 중 쓰지 않았다. 운영 반영 전 RP004 파트너의 숨긴 gid/구형 매핑을 위 보이는 3개 gid/현재 매핑으로 변경하고, 서비스계정의 정본·영업자 시트 편집 권한을 실제 클릭으로 확인해야 한다.
- 최종 판정: **CONDITIONAL PASS**. 코드 파이프라인과 실원본 dry audit는 통과했다. RP004 운영 설정 변경과 실제 관리자 `연동하기` 1회 왕복은 별도 운영 승인 후 수행한다.

## 2026-08-12 영업자 상품리스트 단일 정본 전환

- 위의 `공급사 원본 → 숨김 정본 → ERP → 영업자 시트` 설계는 사용자의 최신 결정으로 폐기했다. 현재 단일 흐름은 **영업자용 `상품리스트` → ERP v4**이며, 오토플러스·아이카·이안카·아이언 등 공급사 원본 시트는 비교·작성 참고용일 뿐 ERP 쓰기 입력으로 사용하지 않는다.
- ERP에서 영업자 시트를 다시 덮어쓰는 역방향 내보내기는 차단했다. 따라서 사용자가 영업자 시트를 확정한 뒤 `ERP 연동하기`를 누르는 작업만 정본 변경으로 인정한다.
- 현재 영업자 시트의 최신 `상품리스트 08.12 16:44 · 520대` 탭을 실제 읽어 **520행 전부 변환**, 중복 0, 무효 0을 확인했다. 공급사별 합계도 17개 공급사·총 520대로 일치했다.
- 필터와 사용자가 숨긴 행은 시트의 보기 상태로 간주해 재고 삭제 근거로 쓰지 않는다. 헤더가 밀리거나 안내행이 끼어도 실제 헤더와 그 아래 차량 행을 찾고, 원본 시트의 실제 gid·탭·행 번호를 각 ERP 행에 보존한다.
- 공급사명은 ERP 파트너 코드로 정확히 변환한다. 동일 상호가 충돌하는 `빌린카`는 실제 운영 코드 `RP021`로 고정했고, 알 수 없거나 모호한 공급사명이 하나라도 있으면 전체 연동을 차단한다.
- `파워`, 주행거리(`9,200km`, `0.1만km`), 배기량(`1,600`, `미입력`), 표준 기간별 `대여료/보증금` 셀을 ERP 규격으로 정규화한다. 사진 표시식이나 `사진없음` 문구는 기존 사진 URL을 덮어쓰지 않는다.
- 영업자 시트가 표현하는 1·12·24·36·48·60개월 가격만 정본 범위로 삼는다. 이 범위의 누락은 차단하되, 시트에 없는 기존 6·72·84개월 값은 삭제하지 않는다.
- 시트의 `계약중`은 같은 공급사·차량번호의 기존 ERP 계약 잠금 행과 일치할 때만 보존한다. 시트만으로 새 계약 상태를 만들려 하면 전체 연동을 차단한다.
- 공급사별 과거 행 수는 원본 시트 시절의 기준이므로 영업자 정본에는 적용하지 않는다. 대신 영업자 표 전체가 기존 ERP의 절반 이하로 붕괴하면 전체 연동을 차단하고, 전체가 정상 범위면 공급사별 증감과 공급사 통째 부재를 정본대로 출고불가 처리한다.
- 최신 운영 스냅샷과 실 Google Sheet 읽기 전용 감사 결과: **원본 520행·유입 520·중복 0·무효 0·가격 미입력 16**, 신규 0, 1회 전환 수정 520, 공급사별 감소 보류 0, 새 부재차단 0이었다. 메모리상 1회 적용 후 재실행은 **수정 0·동일 520**으로 멱등성을 확인했다.
- `npx tsc --noEmit`, `npm run build`, `npm run check:fonts`, `npm run check:tokens`, `npm run check:ui`, `sim-sheet-merge` **169/169**, `sim-supplier-sheet-read` **14/14**, `sim-product-offerability` **31/31**, `sim-sales-inventory-sheet`, `git diff --check` PASS.
- 운영 Google Sheet·RTDB에는 이번 전환 검증 중 쓰지 않았고 커밋·푸시·배포도 하지 않았다.
- 최종 판정: **PASS (구현·읽기 전용 감사)**. 실제 운영 ERP 520행 전환은 사용자가 배포와 첫 `ERP 연동하기` 실행을 승인한 뒤 별도 수행한다.
# 2026-08-13 약관 A4 하단 맞춤 조판 검증

- 사용자 요구: 약관이 단순히 넘치지 않는 수준이 아니라 A4 3쪽의 각 2단 하단까지 고르게 차도록 조정.
- 수정:
  - CSS grid의 동일 행 높이를 실제 내용 넘침으로 오판하던 `colFull`을 마지막 문장 좌표 기준으로 변경.
  - 긴 조문은 계산 단계에서 공백 경계까지 세분화하고, 같은 단에 배치된 조각은 최종 DOM에서 다시 한 문단으로 병합.
  - 뒤쪽 단 넘침 시 앞쪽 잔여 공간을 순서대로 사용하는 연쇄 경계 이동 추가.
  - 최종 문단 병합 후 남는 높이를 문단 하단 패딩에 분산해 글자 크기·행간을 유지하면서 6개 단의 하단선 통일.
- 실측(각 단 사용/가용 px): `964.9/964.9`, `964.8/964.9`, `964.8/964.9`, `964.8/964.9`, `964.6/964.9`, `964.6/964.9`.
- 약관 페이지 프레임: 3쪽 모두 A4 `1122.5px`, 헤더 상단 `45.3px`, 본문 하단 `1077.2px`, 넘침 0.
- 검토본·실사용본 PDF 렌더 PASS, 약관 3쪽 PNG 육안 검수 PASS.
- `scripts/sim-esign-agreement.mts`: 49/49 PASS.
- `npx tsc --noEmit`: PASS.
- `npm run check:fonts`: PASS(폰트 드리프트 0).

## 2026-08-13 대여료 선불·후불 계약조건 분리

- 결제 주기(`월납·분기납·연납`), 납부 조건(`선불·후불`), 월 납부일, 결제 방식(`CMS·카드·가상계좌·직접이체`)을 서로 다른 계약 원자로 분리했다.
- 공급사 정책의 `payment_timing`을 기본값으로 가져오되 직접 계약 작성 화면에서 계약 건별로 변경할 수 있다. 생성 시 `payment_timing_snapshot`으로 고정해 이후 회사 정책이 바뀌어도 체결 계약은 변하지 않는다.
- 실제 계약서, 고객 주요조건 확인 화면, 공개 전자계약 스냅샷과 약관 제6조를 같은 값으로 연결했다. 약관에는 선불은 사용 전, 후불은 사용 후 계약서상 월 납부일에 지급하는 것으로 정의했다.
- 검토본·실사용본을 A4 9쪽으로 재생성했다. 결제방법 표와 설명문은 한 페이지 안에 유지되며 약관 3쪽·6개 단의 overflow는 0, 잔여 높이는 0.1~0.4px이다.
- `npx tsc --noEmit`, `sim-esign-agreement` **52/52**, `sim-esign-field-map` **13/13**, `npm run check:fonts` PASS.
- 로컬 개발 서버를 4004 포트로 재기동하고 `/esign/sample-contract?review=payment-timing-v16` HTTP 200을 확인했다.
- 최종 판정: **CONDITIONAL PASS**. 구현·계약 스냅샷·A4 정합성은 통과했다. 선불·후불은 공급사별 금전조건이므로 운영 게시 전 실제 업체 기본값 확인과 사람/Claude 게이트가 필요하다.

## 2026-08-13 첫 장 계약 주요조건 요약 제거

- 본문 대여조건·보험·정비·결제 표와 중복되던 첫 장의 `계약 주요조건 요약` 블록을 샘플·실제 계약서 공통 템플릿에서 제거했다.
- 존재하지 않는 요약을 확인했다고 오해할 수 없도록 약관 제2조와 동의·서명 문구에서도 `주요조건 요약` 참조를 제거하고 계약서·약관 및 계약서상 주요사항 확인으로 통일했다.
- `연 연 20,000km`, `연 연 12%`를 만들던 중복 조합 지점도 함께 사라졌으며 해당 문자열이 정본에 0건임을 확인했다.
- 약관 정본을 `rental-v17-2026-08-13`로 올렸다. `sim-esign-agreement` **53/53**, `npx tsc --noEmit`, `npm run check:fonts` PASS.
- 검토본·실사용본은 A4 9쪽, 약관 6개 단 overflow 0이다. 최신 첫 장 PNG에서 제목·당사자·계약일·계약번호가 잘림 없이 출력되고, 로컬 샘플 URL HTTP 200을 확인했다.
- 최종 판정: **PASS**. 첫 장은 중복 요약 대신 제목과 계약 당사자에 집중하는 표지형 구성이다.

## 2026-08-13 차량 인도일 기준 대여기간·납부기한 기준 연체일

- 계약 체결일을 임대차 시작일로 잘못 쓰던 생성 경로를 제거했다. 서명 시점에는 `대여기간 = 차량 인도일로부터 N개월`, 시작·종료일은 `차량 인도 시 확정 / 차량 인도일 기준 산정`으로 표시하고, 실제 인도 완료 후에만 인도일과 종료일을 확정한다.
- 공통 기간 계산기를 추가해 차량 인도일을 기준으로 종료일을 계산한다. 일반일은 대응일 전일, 최종 월에 대응일이 없으면 월 말일을 사용한다. `2026-08-15 + 36개월 = 2029-08-14`, `2026-01-31 + 1개월 = 2026-02-28`을 검증했다.
- 고객 주요조건 확인 화면도 `차량 인도일로부터 N개월`로 통일했다. 과거 draft가 계약일 기준 시작·종료일을 되살리지 못하도록 기간 3개 필드는 계산 정본이 항상 우선한다.
- 시동제어 기준도 `청구일로부터`를 폐기했다. 월 대여료는 계약서상 월 납부일, 면책금·사고비 등은 청구서상 납부기한의 다음 날부터 연체일을 계산하며, 3일째 18시 미납 시 통지·안전 정차 확인 후 시동제어, 10일째 미납 시 최고 후 해지·회수로 표시한다.
- 약관 정본은 `rental-v18-2026-08-13`. `sim-rental-period` **5/5**, `sim-esign-agreement` **55/55**, `sim-chakhandeal-esign` **37/37**, `sim-esign-field-map` **13/13**, `npx tsc --noEmit`, `npm run check:fonts` PASS.
- 검토본·실사용본은 A4 9쪽, 약관 3쪽·6개 단 overflow 0이다. 대여조건 페이지 PNG에서 기간·인도일·종료일·연체 기준이 잘림 없이 출력됨을 확인했다.
- 최종 판정: **CONDITIONAL PASS**. 기간 계산·문서 정합성은 통과했다. 시동제어·해지 정책은 중요 권리제한 조항이므로 사람/Claude 검토 전 운영 게시하지 않는다.

## 2026-08-13 계약서 장식성 뱃지 제거

- 계약서의 `준수사항`, `필수 절차`, `매출·매입 증빙`, `약관 적용 기준`, `일시납` 꼬리표와 표지의 파란 `개인` 뱃지를 제거했다.
- 의미가 필요한 `개인`은 표지 당사자명 옆 일반 텍스트로, `임차인 의무 가입`은 개인보험 제목 옆 괄호 설명으로 바꿨다. 일시납 계약은 빈 분납표뿐 아니라 `보증금 분납 스케줄` 제목도 함께 숨긴다.
- 계약서 구조를 나타내는 섹션 번호 `01~10`과 사고 처리 순번 `1~4`는 정보 계층·절차 순서이므로 유지했다. 검토본의 상단 경고와 노란 하이라이트는 검토용 PDF에만 남고 실사용본에는 출력되지 않는다.
- `sim-esign-agreement` **56/56**, `npx tsc --noEmit` PASS. 검토본·실사용본 A4 9쪽, 약관 overflow 0이며 표지·보험·결제 페이지 PNG에서 장식성 뱃지 0건을 육안 확인했다.
- 최종 판정: **PASS**.

## 2026-08-13 계약조건 A4 빈 공간 재조판

- 표준 계약에서 추가 운전자와 연대보증인이 모두 없을 때도 별도 장이 남던 구조를 제거했다. 해당 정보가 있을 때만 선택 장을 출력하고, 없으면 페이지 번호 계산에서도 제외한다.
- 항상 필요한 특약은 결제·정산 장 하단으로 이동하고, 운전자 연령·운전 가능 범위는 첫 계약조건 장의 `운전자 조건`으로 이동했다. 보험 적용 대상 문구도 이 위치를 기준으로 통일했다.
- 표지는 삭제한 주요조건 요약을 다시 채우지 않고 당사자 블록을 중단으로 올려 상·하 여백을 균형 배치했다.
- 검토본·실사용본은 **A4 8쪽**(표지 1 + 계약조건 3 + 약관 3 + 동의·서명 1)으로 재생성했다. 표준 샘플의 표시 페이지 번호는 **1/7~7/7**, 계약조건 3쪽과 약관 3쪽 모두 잘림·넘침 0을 PNG로 확인했다.
- `npx tsc --noEmit`, `npm run check:fonts`, `sim-esign-document-boundary`, `sim-esign-agreement` **56/56** PASS.
- 최종 판정: **PASS (조판)**. 추가 운전자 또는 연대보증인이 있는 실제 계약에서는 해당 선택 장이 별도로 포함된다.

## 2026-08-13 자차 면책금 조건 표시 통합

- `수리비의 20%`만 강조되고 `50만~100만원`이 부속 설명처럼 보이던 표기를 `수리비의 20% (최소 50만원 · 최대 100만원)`으로 변경했다.
- 비율·최소액·최대액 전체를 하나의 굵은 조건으로 표시하고, 정책값이 `20`/`20%`, `50`/`50만`/`500000`처럼 들어와도 각각 `%`와 `만원` 단위로 정규화해 중복 단위 없이 표시한다.
- 검토본·실사용본 PDF를 재생성하고 보험 페이지 PNG에서 한 줄 표시·강조 균형·표 선 정렬·잘림 0을 확인했다.
- `sim-esign-agreement` **57/57**, `npx tsc --noEmit`, `npm run check:fonts` PASS.
- 최종 판정: **PASS**.

## 2026-08-13 연대보증 분리·추가 운전자 조건부 확인

- 연대보증인 입력·표시 영역을 자동차 임대차 본계약에서 제거했다. 연대보증은 최고보증액·보증기간·책임범위와 보증인 서명을 갖춘 별도 `연대보증 약정서`에서만 처리한다.
- 추가 운전자는 보험 적용 대상과 직결되므로 본계약에서 유지하되, 별도 A4 한 장을 만들지 않고 마지막 `동의 및 확인·서명` 장에 조건부 등록표로 배치했다. 추가 운전자가 없으면 표 전체가 숨고, 있으면 계약자 서명이 등록 내용까지 함께 확인한다.
- 추가 운전자 표에는 성명·관계·연락처와 면허증 사본·원본 확인만 표시한다. 주민등록번호와 운전면허번호는 중복 입력·표시하지 않고 별도 본인확인 자료와 면허증 사본으로 확인한다.
- 일반 검토본과 추가 운전자 1인 검토본 모두 **A4 8쪽**이다. 추가 운전자 검토본 마지막 장에서 표·동의·서명·직인이 한 장 안에 잘림 없이 배치되고 페이지 번호가 `7 / 7`로 유지되는 것을 PNG로 확인했다.
- `sim-esign-display-fields` **49/49**, `sim-esign-inputs` **43/43**, `sim-esign-agreement` **57/57**, `sim-esign-document-boundary`, `npx tsc --noEmit`, `npm run check:fonts` PASS.
- 최종 판정: **PASS**.

## 2026-08-13 계약 관계인 선택 장 복원 (최신 결정)

- 위 `연대보증 분리·추가 운전자 조건부 확인` 결정은 사용자의 최신 요구로 변경했다. 추가 운전자와 연대보증인을 모두 본 임대차계약의 선택 항목으로 유지한다.
- 두 항목은 `계약 관계인 · 해당 시` 한 장에 묶었다. 둘 다 없으면 장 전체가 숨고 기본 계약은 **A4 8쪽**, 하나 또는 둘 다 있으면 장이 한 장만 추가되어 **A4 9쪽**이다.
- 추가 운전자는 성명·관계·연락처와 면허증 사본·원본 확인만 표시한다. 주민등록번호와 면허번호는 본 표에 중복 기재하지 않는다.
- 연대보증인은 성명·주민등록번호·관계·연락처·직업·주소와 **보증 최고액·보증 기간**을 표시하고, 같은 장에서 보증인 서명·날인과 임대인 확인을 받는다. 변경·연장은 보증인의 별도 서면 또는 전자서명 동의 범위에서만 보증책임에 반영한다.
- 관리자 계약 작성 영역도 `추가운전자 · 연대보증`으로 통일했고 연대보증인 주민번호·연락처·주소는 화면 마스킹 대상으로 유지했다.
- 일반·실사용·추가 운전자만·연대보증인만·두 항목 모두 포함 PDF를 재생성했다. 각각 **8·8·9·9·9쪽**이며, 두 항목 모두 포함한 관계인 장을 PNG로 육안 확인해 표·서명·직인 잘림과 페이지 넘침이 없음을 확인했다.
- `sim-esign-display-fields` **53/53**, `sim-esign-document-boundary`, `sim-esign-inputs` **43/43**, `sim-esign-agreement` **57/57**, `npx tsc --noEmit`, `npm run check:fonts` PASS.
- 최종 판정: **PASS**.

## 2026-08-13 특약·추가 운전자·연대보증인 통합 장 (최신 결정)

- 07 특약을 결제·정산 장 하단에서 분리하고 `07 특약 사항 → 08 추가 운전자 → 09 연대보증인` 순서의 독립 계약 장으로 구성했다.
- 이 장은 특약의 존재 여부와 무관하게 항상 계약서에 포함한다. 추가 운전자와 연대보증인만 계약별 해당 여부에 따라 숨기며, 둘 다 없는 계약에서는 특약 작성란을 90mm로 넓혀 빈 장처럼 보이지 않게 했다.
- 운전자 연령과 운전 가능 범위는 통합 장에서 반복하지 않고 앞쪽 `03 대여 조건`의 `운전자 조건` 한 줄에 유지했다. 보험 적용 대상도 이 앞쪽 조건과 추가 운전자 명단을 참조한다.
- 일반·실사용·추가 운전자만·연대보증인만·두 항목 모두 포함 PDF를 모두 재생성했으며 전부 **A4 9쪽**이다. 기본 특약 장, 07·08·09 전체 장, 앞쪽 운전자 조건을 PNG로 육안 확인해 잘림·넘침·중복이 없음을 확인했다.
- `sim-esign-document-boundary`, `sim-esign-agreement` **57/57**, `npx tsc --noEmit`, `npm run check:fonts` PASS.
- 최종 판정: **PASS**.

## 2026-08-13 운전자 조건의 대여조건 표 편입

- `운전자 연령`과 `운전 가능 범위`를 `03 대여 조건`의 기본 표에 각각 독립 행으로 편입했다. 아래 `약정·부가 조건`이나 `07 특약 사항` 장에서는 반복하지 않는다.
- 별도 장은 `07 특약 사항` 바로 아래에 `08 추가 운전자`, 그 아래 `09 연대보증인`만 배치한다.
- 문서 경계 회귀검사에 두 운전자 조건이 특약 장보다 앞에 있고 각각 정확히 1회만 존재한다는 검증을 추가했다.
- A4 검토본·실사용본·선택항목 포함본을 재생성했다. 대여조건 표와 07·08·09 장 모두 잘림·넘침 0을 PNG로 확인했다.
- `sim-esign-agreement` **57/57**, `npx tsc --noEmit`, `npm run check:fonts` PASS.
- 최종 판정: **PASS**.

## 2026-08-13 추가 운전자·연대보증인 없음 상태 명시

- 08 추가 운전자와 09 연대보증인 섹션은 계약별 해당 여부와 관계없이 항상 표시한다.
- 해당자가 없으면 각 섹션에 `없음`을 명시하고, 있으면 `없음` 행을 숨긴 뒤 추가 운전자 명단 또는 연대보증인 인적사항·보증 최고액·기간·서명란을 표시한다.
- 기본본과 두 항목 포함본을 각각 A4 9쪽으로 재생성하고 5쪽을 PNG로 대조했다. 기본본의 두 `없음` 행과 포함본의 실제 값·서명란 모두 잘림·넘침 0이다.
- 문서 경계 회귀검사에 두 `없음` 상태가 정본에 존재하는지 확인하는 검증을 추가했다.
- `sim-esign-document-boundary`, `npx tsc --noEmit`, `npm run check:fonts` PASS.
- 최종 판정: **PASS**.

## 2026-08-13 선택 관계인 없음 표기 중복 제거

- `08 추가 운전자`와 `09 연대보증인` 섹션 제목 아래에서 같은 항목명을 다시 반복하던 2열 표를 제거했다.
- 해당자가 없을 때는 섹션 제목은 한 번만 두고, 기존 표 선 규격의 전체 폭 한 줄에 `해당 없음`만 표시한다. 장식성 뱃지나 중복 라벨은 사용하지 않는다.
- A4 기본 검토본·실사용본을 재생성하고 5쪽 PNG에서 두 섹션의 제목·상태값·선 간격과 잘림 0을 확인했다.
- `sim-esign-document-boundary`, `npx tsc --noEmit`, `npm run check:fonts` PASS.
- 최종 판정: **PASS**.

## 2026-08-13 표지 계약 당사자 블록 하단 복원

- 표지 중단으로 올라왔던 임대인·임차인 당사자 블록을 다시 하단에 고정했다.
- 당사자 블록 아래에 계약 체결일·체결장소·회사명이 이어지는 하나의 체결정보 묶음으로 구성했다.
- 검토본·실사용본을 재생성하고 첫 장 PNG에서 하단 여백·표 정렬·잘림 0을 확인했다.
- 최종 판정: **PASS**.

## 2026-08-13 빈 특약 자동 `없음` 표기

- 특약 값이 누락되거나 공백 문자열이면 봉인 계약서와 샘플 계약서 모두 `없음`으로 확정 표기한다.
- 검토 안내문을 특약 데이터로 쓰던 샘플값을 제거하고 실제 기본 상태인 `없음`으로 변경했다.
- A4 기본 검토본·실사용본을 재생성하고 5쪽 PNG에서 특약 `없음`, 추가 운전자·연대보증인 `해당 없음` 및 잘림 0을 확인했다.
- `sim-esign-document-boundary` PASS.
- 최종 판정: **PASS**.

## 2026-08-13 빈 특약란 한 줄 접기

- 특약이 `없음`이면 작성란의 고정 높이를 제거해 제목 아래 한 줄만 표시한다. 실제 특약이 있으면 내용 영역을 확보하고 문장 길이에 따라 자연스럽게 늘어난다.
- 섹션 제목 아래에서 `특약 사항`을 다시 반복하던 안쪽 라벨을 제거했다.
- A4 5쪽을 다시 렌더링해 `없음` 한 줄, 구분선, 다음 섹션 간격과 잘림·겹침 0을 육안 확인했다.
- `sim-esign-document-boundary`, `npx tsc --noEmit`, `npm run check:fonts` PASS.
- 최종 판정: **PASS**.
## 2026-08-13 Freepass e-sign operational flow verification

- Verified the end-to-end code path: admin contract center -> direct draft -> supplier policy/template selection -> A4 review -> secure link issue/copy -> customer identity and signature -> admin review/supplement/approval -> completed document retrieval.
- Hardened admin API authentication: a 401 response refreshes the Firebase ID token once and retries once. Link and progress panes now show load failures instead of incorrectly rendering an empty state.
- Added expired/revoked link status and a controlled reissue action. The existing API creates a new token only when no active session can be reused.
- Added optional additional-driver inputs (name, relationship, phone). Resident registration and license-number fields were removed; license evidence remains handled by the customer upload flow.
- Updated direct-issue source-gate fixtures to require the complete supplier policy, including insurance limits, deductibles, own-damage treatment, roadside support, and payment timing.
- Regression gates: e-sign simulations PASS, `npx tsc --noEmit` PASS, `npm run check:fonts` PASS, `npm run check:ui` PASS, `npm run check:tokens` PASS, `git diff --check` PASS.
- Remaining intentional release gates: the standard template is still `sample-v1` with `isSample=true`; guarantor liability requires a separate maximum-amount and guarantor-signature workflow before it can be treated as binding.
- Verdict: **CONDITIONAL PASS** for the application flow. Production issuance requires explicit promotion of the approved standard template, followed by one authenticated live sample transaction.
## 2026-08-13 전자계약 입력 순서 조정

- 자동 적용되는 회원사·정책 상세가 직원 입력보다 먼저 길게 노출되던 순서를 수정했다.
- 작성 순서는 `계약회사·정책·계약서 선택 → 직원 입력 → 선택 입력 → 자동 정책값 확인 → 발송 전 검토`로 고정했다.
- 자동 정책값은 삭제하지 않고 접힌 확인 영역으로 이동해 고객·차량·금액 입력이 화면 상단에 오도록 했다.
- `sim-freepass-esign`, `sim-esign-source-gate`, `npx tsc --noEmit`, `check:ui`, `check:fonts` PASS.
- 최종 판정: **PASS**.

## 2026-08-14 고객 운전면허번호 필수 입력 복구

- 고객 전자계약의 본인확인 단계에 `운전면허번호` 필수 입력란을 추가했다. 주민등록번호·주소와 함께 면허번호, 운전면허증 사진, 셀카가 모두 있어야 다음 단계로 진행된다.
- 운전면허번호 원문은 공개 계약·세션 노드에 복제하지 않고 `v4/esign_private` 제출자료에만 저장한다. 관리자 검토 화면에는 원문 대신 접수 여부만 표시한다.
- 관리자 승인 시 서버 전용 제출자료의 면허번호를 완료본 스냅샷의 `driver_license_no`와 첫 장 `driver_or_biz_no`에 합성하여 최종 PDF 운전면허번호 칸에 반영한다.
- 검증: 실제 고객 링크에서 본인확인 3단계의 `운전면허번호 *` 입력란 확인, `npx tsc --noEmit`, `sim-freepass-esign`, `sim-esign-signed-snapshot` PASS.
- 최종 판정: **PASS**.

## 2026-08-14 전자계약 실무 입력·발송 동선 및 손오공 법정정보 검증

- 관리자 계정으로 로컬 `/esign`을 직접 조작하여 `새 계약서 → 렌트·보험료 포함 기본 선택 → 공급사 선택 → 해당 공급사의 프리패스 공통정책 자동 선택 → 공급사 소속 ERP 차량 검색·선택 → 기간·연령·최종 대여료·보증금·고객정보 입력 → A4 계약서 생성 → 고객 링크 표시·복사 → 고객 화면·A4 출력 → 진행상태 확인` 흐름을 검증했다.
- 손오공 선택 시 `손오공 · 프리패스 공통 렌트`가 자동 선택되고, 손오공 소속 ERP 차량 22대만 검색 목록에 표시됐다. 차량 선택 후 36개월·만 26세·월 730,000원·보증금 1,000,000원·고객명·연락처를 입력하자 `계약서 생성 준비가 완료되었습니다` 및 `A4 계약서 만들기`가 활성화됐다.
- 기존 생성 계약의 후속 패널에서 고객 계약 URL의 전체 표시, 링크 복사, 고객 화면 미리보기·열기, 링크 해지, A4 초안 미리보기·출력, 고객 제출 진행단계를 확인했다. 이번 검증에서는 새 계약을 저장하지 않았다.
- 손오공 원본 Excel의 입력영역과 Google Drive의 사업자등록증·자동차대여사업등록증을 대조했다. 공식값은 사업자등록번호 `882-87-00650`, 법인명 `주식회사 손오공렌터카`, 대표자 `조규진`, 주소 `서울특별시 강서구 양천로53길 30, 1205호(가양동, 서서울모터리움)`, 자동차대여사업 등록번호 `제 강서-68호`이다.
- 위 공식값을 운영 v3 원본은 건드리지 않고 `v4/partners/RP012` 오버레이에만 반영했다. 반영 스크립트는 기본 dry-run이며 `--apply` 실행 뒤 6개 필드 재조회 일치를 확인한다.
- 계약서 발행 전에 임대인 상호·사업자등록번호·대표자·주소·자동차대여사업 등록번호가 하나라도 없으면 BLOCK 하도록 강화했다. 은행·계좌 정보는 경고로 유지했다.
- 검증: `npx tsc --noEmit`, `npm run check:fonts`, `npm run check:ui`, `sim-freepass-esign`, `sim-esign-policy-selection`, `sim-esign-vehicle-selection`, `sim-esign-source-gate`, `sim-esign-document-boundary` 모두 PASS.
- 최종 판정: **PASS — 실무자가 필수값만 빠르게 입력하고 A4 확인 후 링크를 복사·전달할 수 있으며, 임대인 법정정보 누락 상태에서는 계약서를 만들 수 없다.**

## 2026-08-13 표준계약 보험·초과주행·지연손해금 V19

- 약관 제23조에 계약기간 중 객관적으로 확인된 주행거리의 사용기간 비례 초과분을 즉시 청구할 수 있는 기준을 추가했다. 납부기한 후 상당한 기간의 최고에도 미납하면 계약 해지·차량 반환을 청구할 수 있고, 기납부액은 최종 정산에서 중복 청구하지 않는다.
- 계약서 부가장비 표시에서 스페어키 1개를 삭제했다.
- 프리패스 표준 보험값을 자기신체사고 `사망·후유장애 1인당 3천만원 · 부상 1인당 1,500만원`, 무보험차상해 `미가입`으로 변경했다. 실제 발행 전 계약회사 증권과 일치 여부를 확인하는 정책값이다.
- 지연손해금 표준값을 연 24%로 변경하고 계약서와 고객 확인문구에 `관계 법령상 허용 한도 내`를 함께 표시했다.
- 금지행위와 보험처리 조문에 「교통사고처리 특례법」 제3조 제2항 단서 각 호의 행위, 무면허·음주·약물운전·도주 등 중대한 법규위반 사고를 반영했다. 해당 법조문 위반만으로 모든 민사상 중대한 과실이 자동 확정된다고 단정하지 않고 실제 사고 경위·과실과 유효한 보험·공제약관에 따라 보상 제한·자기부담금·추가부담금·구상금을 판단하도록 했다.
- 검증: 약관/인쇄본 동기화 및 계약 회귀 `61/61` PASS, 계약유형·모바일 동의 회귀 `60/60` PASS, source gate PASS, template profile `10/10` PASS, `npx tsc --noEmit` PASS, `npm run check:fonts` PASS.
- 기존 약관 조판기의 호 번호 탐색 정규식이 문단 시작 `2.`에서 0길이 일치를 반복해 PDF 생성이 멈추는 버그를 수정했다. 약관 행간을 1.50에서 1.43으로 조정하되 글자크기 10px·3장·2단 규격은 유지했다.
- A4 렌더 결과: 계약서 5쪽 + 약관 3쪽 + 동의·서명 1쪽 = 총 9쪽. 약관 6개 단 모두 `used 964.6~964.8 / available 964.9`, 넘침 0, Pretendard 로딩 정상. 9쪽 전체 PNG를 확인해 잘림·겹침·깨진 표가 없음을 확인했다.
- 최종 판정: **PASS**.
## 2026-08-13 전자계약 단일 업무흐름 재설계

- 기존 `발송목록 + 계약 데이터 + 링크 + 진행상황` 4열 구조를 `발송목록 + 계약 처리` 2열 구조로 단순화했다.
- 계약 처리 화면은 `① 입력 → ② 계약서 확인 → ③ 링크 전달 → ④ 관리자 확인` 단계 상태를 한 줄로 표시하고, 선택한 계약의 현재 단계만 강조한다.
- 초안 생성 후 계약서 확인·출력·링크 생성·복사·고객 진행·보완 요청·승인·완료 PDF를 같은 세로 흐름 안에서 처리한다.
- 발행 당시 동결된 상세 계약 내용은 기본 접힘 영역으로 이동해 일상 업무에서 긴 정책·약관 데이터가 핵심 버튼을 밀어내지 않도록 했다.
- 목록과 계약 처리 화면은 유지하되 모바일에서는 선택 계약 한 화면과 뒤로가기만 제공한다.
- `sim-freepass-esign`, `sim-esign-source-gate`, `sim-esign-progress` **44/44**, `npx tsc --noEmit`, `check:ui`, `check:fonts`, `check:tokens` PASS.
- 최종 판정: **PASS**.

## 2026-08-13 구 승인대기 자가가입 계정 즉시 활성화 보완

- 원래 요구사항: 소속 없이 가입한 영업자는 관리자 승인 대기 없이 바로 활동하고, 실제 소속은 나중에 관리자가 매칭한다.
- 확인 결과 신규 가입은 이미 `status=active`, `role=agent`, `user_code=Firebase uid`로 저장됐지만, 구 승인제에서 생성된 `pending` 프로필은 계속 승인대기 화면에 막혔다.
- 로그인 시 Firebase ID token을 서버에서 검증한 뒤, 본인 uid·구 자가가입 흔적·미배정 소속/채널을 모두 만족하는 `pending`만 RTDB transaction으로 개인 영업자 `active`로 전환하도록 수정했다.
- 삭제·반려·비활성, uid 불일치, 소속·채널이 이미 지정된 계정은 자동 활성화하지 않는다. 공급사 신청 계정도 소속 확인 전에는 공급사 권한이 아니라 개인 영업자 최소 권한만 받는다.
- `database.rules.json`은 변경·게시하지 않았다. 운영 RTDB 권한을 넓히지 않고, 검증된 서버 경로 하나로만 구 계정을 전환한다.
- 검증: `npx tsc --noEmit` PASS, `scripts/check-self-serve-signup.mts` 20/20 PASS, `npm run check:fonts` PASS, 로컬 `/login` 가입 즉시 이용 안내·콘솔 오류 0 확인, `/api/auth/self-activate` 무토큰/위조토큰 각각 HTTP 401 확인.
- 실제 캡처 계정 비밀번호가 없어 유효 토큰으로 운영 계정 전환까지는 실행하지 않았다. 최신 코드 배포 후 해당 계정이 다시 로그인하면 자동 전환된다.
- 최종 판정: **CONDITIONAL PASS** — 구현·로컬 보안 검증은 통과했고, 운영 배포 및 해당 계정의 1회 재로그인 확인이 남았다.
## 2026-08-13 전자계약 입력 완료 후 무반응 수정

- 저장 버튼이 BLOCK 존재 시 비활성화되어 사용자가 원인을 확인할 수 없던 문제를 수정했다.
- 버튼은 작업 중이 아닐 때 항상 누를 수 있고, 차단이 남아 있으면 동일 검증기로 정확한 사유를 토스트에 표시한다.
- 발송 전 검토에서는 PASS 행을 숨기고 실제 확인이 필요한 BLOCK·WARNING만 표시한다.
- 정책 완성도는 `N개 항목이 비어 있음` 대신 `정책관리에서 확인: 운전자 연령 · 자차 최대 면책금 …`처럼 실제 누락 항목명을 표시한다.
- 정책 완성도에서 이미 잡힌 운전자 연령은 별도 BLOCK으로 중복 표시하지 않는다.
- `sim-esign-source-gate`, `sim-freepass-esign`, `npx tsc --noEmit`, `check:ui`, `check:fonts` PASS.
- 최종 판정: **PASS**.

## 2026-08-14 전자계약 생성·확인·전달 흐름 단순화

- 긴 입력 폼 끝에 있던 계약서 생성 버튼을 제거하고, 오른쪽 `계약서 만들기` 패널을 입력 요약·누락 확인·최종 실행 영역으로 고정했다.
- 직원 흐름을 `회사 → 계약서 종류 → 계약정책 → ERP 차량 → 기간·연령·대여료 → 고객·특약 → 입력 내용 확인 → A4 계약서 만들기` 순으로 고정했다.
- 계약서 생성 후 같은 오른쪽 패널에서 `① A4 계약서 확인 → ② 고객 계약 링크 만들기`를 진행하고, 생성 후에는 `링크 복사`를 첫 번째 전달 동작으로 노출했다.
- 링크 생성이 문자·카카오톡 자동 발송으로 오해되지 않도록 `링크만 생성`되며 직원이 복사해 원하는 방법으로 전달한다는 안내를 명시했다.
- 검증: `sim-freepass-esign`, `sim-esign-vehicle-selection`, `sim-esign-source-gate`, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check` PASS.
- 브라우저 현재 세션은 `/login`이므로 인증 후 실제 회사·차량 데이터를 이용한 1건 생성 화면 확인은 남아 있다.
- 최종 판정: **PASS** (코드·회귀검증), 인증 세션 실행 확인 대기.

## 2026-08-14 특약·비상연락·추가 운전자 입력 보완

- 손오공 원본 계약 엑셀을 확인했다. 추가 운전자는 `성명/주민번호 · 관계 · 연락처 · 면허번호`, 비상연락은 `비상연락처 · 관계`를 사용한다. 공통 운영 엑셀에도 `비상연락처 · 비상관계 · 운전자범위`가 있다.
- 고객정보 영역에 비상연락처·비상연락 관계를 바로 노출했다. 비우면 고객이 계약 링크에서 관계/성명·연락처를 입력한다.
- 추가 운전자는 공급사 정책의 `추가 운전자 허용 수`를 기준으로 동적 등록한다. `+ 추가 운전자`로 A4 표의 최대 3명까지 추가하고 각 행을 삭제할 수 있으며, 중간 행 삭제 시 뒤 운전자를 앞으로 당겨 번호 공백을 만들지 않는다. 명시 인원 수와 실제 1~3번 입력행을 함께 계산해 임시저장 복원값에서도 `정보는 있는데 0명`이 되는 상태를 방지했다.
- 정책이 `불가`면 추가 버튼을 비활성화하고, `1인/2인/3인`이면 해당 수까지만 등록한다. 정책 허용 수 초과 또는 등록된 각 운전자의 성명·관계·연락처 누락·연락처 형식 오류는 계약서 생성을 차단한다.
- 주민번호·면허번호는 관리자 화면에 중복 저장하지 않고 면허증 첨부자료·원본 확인으로 유지했다.
- A4 계약서에는 비상연락을 `관계 · 연락처`로 표시하고, 추가 운전자 1~3명의 성명·관계·연락처를 기존 1~3번 행에 연동했다. 3명 입력 검토본을 실제 PDF로 생성했으며 약관 6개 단 모두 넘침 0, 총 3개 약관 페이지의 A4 높이 정합을 확인했다.
- 공용 `Textarea`는 rows 수와 폰트·패딩 토큰으로 최소높이를 계산하여 특약 3줄 입력칸이 1줄로 잘리지 않게 했다.
- 검증: `sim-freepass-esign`, `sim-esign-source-gate`, `sim-esign-agreement` 61/61, `npx tsc --noEmit`, `npm run check:ui`, `npm run check:fonts`, `git diff --check` PASS.
- 최종 판정: **PASS**.

## 2026-08-13 손오공 빠른 계약 입력 흐름

- 새 계약서 작성 시 손오공(`RP012`)을 기본 계약회사로 선택하고, 연결 정책 중 `렌트 → 보험포함 → 발송가능 → 기본/표준` 순서로 가장 적합한 정책을 자동 선택한다.
- 일상 입력 화면에서는 계약회사·정책·계약서 선택기를 접고 `고객 정보 → 차량 정보 → 대여·금액 조건`만 먼저 표시한다. 다른 업체나 정책이 필요한 경우에만 설정을 펼쳐 변경한다.
- 중복되던 `초안만 저장` 버튼을 제거하고 `계약서 만들고 확인` 한 번으로 초안 생성과 A4 확인 화면 이동을 처리한다.
- 손오공 또는 연결 정책이 실제 데이터에 없으면 선택 영역을 자동으로 펼치고 기존 정책 생성·필수값 확인 게이트를 유지한다. 임의 정책값으로 발송을 우회하지 않는다.
- 검증: 로컬 `/esign` HTTP 200, `npx tsc --noEmit` PASS, `sim-freepass-esign` PASS, `check:fonts` PASS.
- 최종 판정: **PASS** — 입력 UX와 코드 흐름은 완료. 실제 고객 링크 발행은 로그인된 관리자 세션과 계약 건별 고객·차량·금액 입력이 필요하다.

## 2026-08-14 전자계약 로컬 서버 즉시 실행

- 장애 확인 당시 포트 4004는 LISTEN 중이 아니었고, 기존 서버 로그의 마지막 `/esign` 요청은 HTTP 200이며 애플리케이션 오류는 없었다. 개발 서버 프로세스 종료가 직접 원인이었다.
- 숨김 백그라운드 개발 서버를 재기동하고 `/login`, `/esign`, `/esign/sample-contract`, `/esign/preview/[contractCode]`를 사전 컴파일했다.
- `전자계약_로컬실행.cmd`와 `scripts/start-esign-local.ps1`을 추가했다. 더블클릭 한 번으로 서버 상태 확인, 필요 시 재기동, 전자계약 관련 화면 미리 준비, `/esign` 열기를 수행한다.
- 응답 측정: 최초 `/esign` 컴파일 요청 3.999초, 준비 후 반복 요청 0.264~0.358초. 샘플 계약서 0.537초, 미리보기 경로 1.050초.
- 검증: 실행 스크립트 `-NoOpen` 정상 완료, `/esign` HTTP 200, `git diff --check` PASS.
- 최종 판정: **PASS**.

## 2026-08-13 판매용 4개 탭 → ERP 정본 동기화

- 판매용 워크북의 `상품리스트`, `손오공구독`, `오플구독`, `오플프로모션` 4개 보이는 탭을 한 Grid 스냅샷으로 읽어 공급사+차량번호 정본으로 합쳤다. 오토플러스만 숨김/필터 제외행을 판매 대상에서 제외한다.
- 일반 탭의 분리형 보증금·대여료와 과거 압축형 가격을 함께 읽고, 손오공 반납형/인수형 및 오토플러스 주행거리별 가격을 구분해 보존한다.
- `109호5035`는 판매용 시트 `N134`에 `무보증`을 명시해 `Q134 870,000원`과 계약보호 ERP 가격(`48개월`, 보증금 0원)을 일치시켰다.
- 공개 가격과 private 수수료 합본 때문에 CAS가 거짓 충돌하던 경로를 공개 노드끼리 비교하도록 수정했다. 판매용 정본에 다시 나타난 soft-delete 차량은 신규 중복 대신 기존 톰스톤을 CAS로 복구한다.
- 운영 실행 `SS-1786613894846-fg211z`: 원본 534행, 숨김/판매제외 16행, 최종 518대, ERP 갱신 518대. 사후 재실행은 `신규 0 · 수정 0 · 동일 518`로 멱등 확인했다.
- 운영 직접 확인: `RP004_109호5035`는 48개월 대여료 870,000원·보증금 0원, `RP023_63주0598`은 오플프로모션 출처·출고가능으로 복구됐다.
- 검증: `sim-sales-inventory-sheet` PASS, `sim-sheet-price` 34/34 PASS, `sim-sheet-merge` 170/170 PASS, `sim-sheet-daily-sync` 25/25 PASS, `npx tsc --noEmit` PASS, `npm run check:fonts` PASS. Google Sheets 실제 화면에서 `N134=무보증`, `Q134=870,000`과 레이아웃 이상 없음 확인.
- 최종 판정: **PASS**.

## 2026-08-14 공급사별 프리패스 공통 전자계약 정책 배정

- 프리패스 공통 렌트·보험료 포함 패키지 `freepass-common-rent-2026-08-14-v1`을 신규 정책 기본값으로 확정했다. 대인 무한, 대물 2억원, 자손 사망·후유장애 3천만원/부상 1,500만원, 무보험차상해 미가입, 자차 수리비 20%·최소 50만원~최대 100만원, 긴급출동 연 5회, 지연손해금 24%, 승계수수료 100만원이 포함된다.
- 운영 공급사 21곳 각각에 수정 가능한 `FP-{공급사코드}-RENT` 복사본을 v4/policies에 신규 배정했다. 기존 업체별 정책과 상품 policy_code는 덮지 않았다.
- 반영 직전 dry-run에서 21/21 발송 게이트 PASS·차단 0건을 확인했고, 반영 후 운영 RTDB 재조회에서도 21/21 패키지 식별자와 전자계약 발송 게이트를 다시 검증했다.
- 전자계약에서는 공급사별 공통 복사본을 기존 정책보다 우선 자동 선택한다. 일상 화면은 고객·차량·대여·금액 입력을 최상단에 두고 계약일은 오늘로 자동 입력하며, 회사·정책·계약서와 예외조건은 접힌 영역에서만 변경한다.
- 신규 정책 등록은 같은 공통값으로 시작하고 공급사는 다른 값만 수정한다. `sim-freepass-common-policy`, `sim-freepass-esign`, `sim-esign-source-gate`, `sim-esign-contract-kind` 60/60, `npx tsc --noEmit`, `npm run check:fonts`, 로컬 `/esign` HTTP 200, `git diff --check` PASS.
- 최종 판정: **PASS**.

## 2026-08-14 전자계약·정책 코드 정리

- 화면 컴포넌트에 섞여 있던 공급사별 정책 그룹화와 기본 계약정책 점수 계산을 `lib/domain/esign-policy-selection.ts`로 분리해 독립 검증 가능하게 했다.
- 정책 목록을 공급사별 Map으로 한 번만 계산해 계약회사 선택지와 정책 드롭다운에서 반복 필터링하지 않도록 했고, 정책 발송 게이트도 렌더마다 중복 계산하지 않도록 정리했다.
- 계약회사 목록은 문자열 제외 방식 대신 공통 `partnerTypeLabel()` 판정으로 공급사만 표시한다.
- 운영 정책 배정 스크립트는 이미 최신인 레코드의 `updated_at`을 다시 쓰지 않는다. 운영 dry-run 결과 `신규 0 · 보완 0 · 변경 없음 21 · 차단 0`으로 멱등성을 확인했다.
- 검증: `sim-esign-policy-selection`, `sim-freepass-common-policy`, `sim-freepass-esign`, `sim-esign-source-gate`, `npx tsc --noEmit`, `npm run check:fonts`, 로컬 `/esign` HTTP 200, `git diff --check` PASS.
- 최종 판정: **PASS**.

## 2026-08-14 계약 작성 상단 3개 선택 고정

- 새 계약 작성은 자동 회사·정책 선택을 제거하고 `회사선택 → 계약서 종류 → 계약정책` 세 항목을 모두 직접 선택하도록 변경했다.
- 세 선택값은 접힌 설정이 아니라 입력 화면 맨 위의 3열 공통 폼에 항상 표시한다. 그 아래는 `고객 정보 → 차량 정보 → 대여·금액 조건` 순서다.
- 회사나 계약서 종류를 바꾸면 기존 정책 선택을 지워 잘못된 조합이 남지 않게 했다. 계약정책은 선택한 회사에 귀속되고, 렌트/구독 및 보험포함/보험별도 조건까지 일치하는 정책만 표시한다.
- 검증: `sim-esign-policy-selection`, `sim-freepass-esign`, `sim-esign-source-gate`, `sim-esign-contract-kind` 60/60, `npx tsc --noEmit`, 로컬 `/esign` HTTP 200, `git diff --check` PASS.
- 최종 판정: **PASS**.

## 2026-08-14 ERP 차량검색 기반 계약입력

- 계약 입력에서 차량명·차량번호·옵션·월대여료·보증금 수기 입력을 제거하고 ERP 재고 차량 선택으로 전환했다.
- 회사·계약서 종류·계약정책을 선택하면 해당 회사 소속이면서 렌트/구독 종류가 일치하고 기간별 가격이 있는 ERP 차량을 먼저 최대 8건 표시한다. 차량번호 또는 모델명을 입력하면 그 회사 차량 안에서 즉시 좁혀진다.
- 회사나 계약서 종류 또는 정책을 변경하면 기존 차량 선택·검색어·기간·금액을 함께 초기화해 다른 회사 차량이 남지 않게 한다.
- 차량 선택 시 ERP 상품코드·차량번호·차명·연식·연료·주행거리·옵션을 스냅샷으로 가져오며, 계약 레코드에도 상품코드를 저장한다.
- 직원은 ERP 차량의 실제 가격표에 존재하는 기간과 정책상 운전자 연령만 선택한다. 기간 선택 시 월 대여료·보증금을 불러오고, 기본연령보다 낮은 연령을 선택하면 정책의 연령하향 월 추가금을 자동 합산한다.
- 정책의 약정주행거리·납부조건·운전자범위·정비조건은 자동 적용한다. 주민번호·주소·면허증 사진은 기존대로 고객 링크에서 입력·첨부한다.
- 검증: `sim-esign-vehicle-selection`, `sim-esign-policy-selection`, `sim-freepass-esign`, `sim-esign-source-gate`, `sim-esign-contract-kind` 60/60, `npx tsc --noEmit`, `npm run check:fonts`, 로컬 `/esign` HTTP 200, `git diff --check` PASS.
- 최종 판정: **PASS**.

## 2026-08-14 전자계약 공급사 표시명 정리

- 회사선택 드롭다운은 내부 공급사 코드 대신 파트너 원본 회사명을 표시한다.
- `주식회사`, `(주)`, `㈜` 등 법인격 표기만 제거하고 `렌터카`, `모빌리티`처럼 실제 상호를 구성하는 단어는 유지한다. 이름 데이터가 없는 예외 행도 내부 코드를 노출하지 않고 `공급사명 미등록`으로 표시한다.
- 운영 원본 대조 결과 정규화된 `name`에는 `RP004` 같은 코드가 있고 실제 상호는 `partner_name`에 남은 행이 다수였다. 표시명 해석기는 `partner_name` 등 실제 상호 필드를 우선하며 파트너 식별코드와 `RP/PT/SUP/CHN/ORG` 코드형 문자열을 이름 후보에서 제외한다.
- 검증: `sim-esign-policy-selection` PASS(앞·뒤 법인격 및 상호 보존 사례), `npx tsc --noEmit` PASS, `npm run check:fonts` PASS. 브라우저는 인증 게이트가 정상 작동해 `/esign`에서 `/login`으로 이동했으며 미인증 상태의 화면 데이터 검증은 수행하지 않았다.
- 최종 판정: **PASS**.

## 2026-08-14 전자계약 작성 화면 세로 레이아웃 복구

- `CenterNote`의 채움형과 인라인형을 구분했다. `minHeight={0}` 또는 숫자 높이를 지정한 안내는 더 이상 `flex: 1`과 40px 패딩으로 남은 화면을 차지하지 않는다.
- 계약 기본선택의 상태 뱃지는 가로 전체로 늘어난 얇은 막대가 되지 않도록 내용 너비로 고정했다.
- 새 계약서를 열자마자 11줄 누락표를 노출하지 않는다. 차량·기간·운전자 연령까지 선택하기 전에는 짧은 안내만 보이고, 검토 단계에 도달한 뒤 실제 누락 항목만 표로 표시한다.
- 계약정책 드롭다운은 정책명만 표시한다. 보험사·운전자연령·자차조건·발송상태는 선택지 문자열에서 제거하고 선택 후 검토영역에서 확인한다.
- 검증: `sim-freepass-esign`, `sim-esign-source-gate`, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check` PASS.
- 최종 판정: **PASS**.

## 2026-08-14 ERP 기본값 이후 계약값 직접 수정

- ERP 차량 선택은 계약 입력의 기준값으로 유지하되, 선택 직후 `차종·차량번호·옵션`을 실제 계약서 표기에 맞게 직접 수정할 수 있도록 했다.
- 기간·운전자 연령의 빠른 선택 뒤 `대여기간·월 대여료·보증금`을 최종 계약값으로 직접 수정할 수 있도록 했다. 저장 시 계약 스냅샷에 확정되어 미리보기와 고객 문서에 동일하게 반영된다.
- 건별 특약은 접힌 부가정보에서 분리해 고객정보 다음에 항상 보이는 입력란으로 배치했다. 입력값은 기존 `contract_draft.special_terms` 문서 경로를 사용하며 빈 값은 A4 계약서에서 `없음`으로 표시된다.
- 공통 보험·정산값은 공급사 계약정책에서 자동 적용하고, 직접 입력란은 이번 계약에서 달라지는 값만 다룬다.
- 검증: `sim-freepass-esign`, `sim-esign-source-gate`, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check` PASS.
- 최종 판정: **PASS**.

## 2026-08-14 계약회사 차량 검색 선택기

- 회사·계약서 종류·계약정책을 고른 뒤 차량 검색창을 누르면 선택 회사에 귀속된 ERP 차량 전체가 드롭다운 스크롤 목록으로 펼쳐진다.
- 종전 8대 제한을 제거했고 회사 차량 수와 검색결과 수를 함께 표시한다. 차량번호 일부뿐 아니라 제조사·모델·세부모델·트림 원본값까지 검색해 전체 회사 차량을 실시간 필터링한다.
- 계약 입력은 판매 카탈로그와 목적이 다르므로 대여료가 아직 없는 회사 재고도 목록에 포함한다. 차량 선택 후 직원이 월 대여료·보증금을 직접 확정할 수 있다.
- 차량 행에는 차량번호·차종과 최대 3개 기간의 등록 대여료, 연식·유종·주행거리 정보를 함께 표시한다. 차량을 선택하면 목록이 닫히고 계약값이 채워지며, 검색창을 다시 누르면 다른 차량으로 변경할 수 있다.
- 검증: `sim-esign-vehicle-selection`(회사 격리·번호검색·차종검색·가격 없는 재고·8대 초과 전체표시), `sim-freepass-esign`, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check` PASS. 브라우저는 `/esign` 접근 시 정상적으로 `/login` 인증 게이트로 이동해 로그인 후 화면의 시각 검증은 수행하지 못했다.
- 최종 판정: **PASS**.

## 2026-08-14 전자계약 2패널 업무 분리

- 계약목록 오른쪽의 단일 업무 패널을 `계약 진행`과 `계약서·링크` 두 패널로 분리했다.
- `계약 진행`은 신규 조건 입력, 누락 확인, 고객 제출 진행상황, 본인확인·서명 검토와 관리자 승인만 담당한다.
- `계약서·링크`는 A4 계약서 확인·출력, 체결 방식 선택, 전자서명 링크 생성·복사·고객 화면 열기·해지만 담당한다. 계약 생성 전에도 같은 위치에 다음 업무 안내를 표시해 패널 역할이 바뀌지 않는다.
- 데스크톱은 `계약목록 | 계약 진행 | 계약서·링크` 3열이며 목록 폭을 360px로 제한해 두 업무 패널에 공간을 우선 배분한다. 모바일은 두 패널을 세로로 길게 이어 붙이지 않고 하단 전환으로 선택한다.
- 검증: `sim-freepass-esign`, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check` PASS.
- 최종 판정: **PASS**.

## 2026-08-14 고객 계약 링크 표시·복사

- 전자계약 링크 생성 후 고객 URL을 작은 안내문이 아니라 `고객 계약 링크` 읽기 전용 입력칸으로 표시한다. 직원은 전체 URL을 직접 확인·선택할 수 있다.
- URL 입력칸 바로 옆에 `링크 복사` 버튼을 고정하고, 링크가 없을 때는 복사 동작을 차단한다. 복사 성공·실패 결과는 토스트로 명확히 안내한다.
- 검증: `sim-freepass-esign`, `npx tsc --noEmit`, `npm run check:ui`, `npm run check:fonts`, `git diff --check` PASS.
- 최종 판정: **PASS**.

## 2026-08-14 브라우저 실계약 흐름·링크 저장 수정

- 관리자 계정으로 로컬 `/esign`에 로그인해 `손오공 → 렌트·보험료 포함 → 손오공 프리패스 공통 렌트 → 손오공 ERP 차량 → 36개월 → 만 26세 → 가상 고객` 순서로 실제 계약 한 건을 생성했다.
- 계약 생성 후 A4 확인·출력과 고객 링크 만들기 화면이 이어졌으나, 최초 링크 저장에서 동의 스냅샷 행의 `raw: undefined` 때문에 Firebase Admin `set`이 실패하는 현상을 재현했다.
- `buildConsentGroups()`에서 표시값이 남고 원본값만 없는 행의 `raw` 속성을 제거해 Firebase 저장 가능한 스냅샷으로 고정했다. 링크 발행 저장부는 실패 원인을 구조화된 503 응답으로 반환하고, 개발 화면은 HTTP 상태를 숨기지 않는다.
- 재시도 후 고객 계약 링크가 발행됐고 URL 입력칸 표시, 복사 버튼 활성화, 실제 클립보드 값 일치와 성공 토스트를 확인했다. 고객 링크를 열자 관리자 화면이 `발행 → 열람`으로 바뀌고 `링크 발행 · 고객 열람` 이력이 시간과 함께 표시됐다.
- 차량 주행거리 원본이 `NaN`인 경우 차량 선택 행에 `NaNkm`가 노출되지 않도록 유한 숫자만 표시한다.
- 손오공 공급사 고정정보 중 `업체 주소`와 `자동차대여사업 등록번호`는 현재 미등록으로 확인됐다. 계약별 직원 입력값이 아니라 공급사 기본정보에서 한 번 확정할 데이터이므로 임의값은 넣지 않았다.
- 검증: `sim-chakhandeal-esign` 38/38, `sim-freepass-esign`, `sim-esign-source-gate`, `npx tsc --noEmit`, `npm run check:ui`, `npm run check:fonts`, `git diff --check` PASS.
- 최종 판정: **PASS** — 계약 생성·링크 표시/복사·고객 열람·관리자 진행 추적 브라우저 검증 완료.
# 2026-08-14 차종마스터 식별 스키마 확장 검증

- 사용자 요구: 손오공 보유 차량을 우선 기준으로 차종마스터에 생산기간·정확 배기량·세대 및 파워트레인 식별정보를 충분히 저장하고, 이후 마스터 적용이 쉽도록 한다.
- 변경: `MasterEntry`에 생산월·세대명·차체형태·별칭을, `MasterVariant`에 정확 `engine_cc`·엔진코드·변속기·모터배치·출력·판매기간·별칭을 하위 호환 필드로 추가했다.
- 매칭: 정확 `engine_cc`가 있으면 리터 환산값보다 우선하며, 완전일치가 허용오차 후보보다 반드시 우선하도록 회귀검사를 추가했다. 세부모델 별칭도 세대 선택 점수에 반영한다.
- 감사: `scripts/audit-vehicle-master-identifiers.mts`를 추가해 최근 10년 범위의 생산월·세대코드·파워트레인·정확 배기량·배터리·구동·인승·트림 누락과 구조 모순을 전수 점검한다.
- 검증: `npx tsc --noEmit` PASS, `npx tsx scripts/verify-master-pass.mts` PASS, `npm run check:fonts` PASS. 감사 결과 구조 모순 0, 기존 데이터는 보강 필요(`NEEDS_ENRICHMENT`).
- 남은 범위: 카눈·엔카·제조사 자료로 손오공 보유 차종부터 실제 `production_*`와 `engine_cc` 값을 채우는 데이터 보강은 별도 단계다. 근거 없는 리터×1000 자동 채움은 하지 않았다.
- 최종 판정: CONDITIONAL PASS — 스키마·매칭·감사 경로는 완료, 실제 정밀 제원 데이터는 보강 대기.

## 2026-08-14 실계약서·고객 전자서명 화면 시각 정비

- 실제 고객 링크를 브라우저에서 열어 계약 확인 화면의 글자 위계·여백·구분선·버튼 배치를 확인했다. 데스크톱은 얇은 단일 테두리와 약한 그림자의 문서 면을 사용하고, 모바일은 박스 중첩 없이 화면 전체를 사용하며 하단 진행 버튼을 고정한다.
- 고객 화면은 전역 Pretendard와 UI 토큰을 그대로 사용한다. 제목은 700, 섹션 제목은 700, 메타는 500으로 정리하고 진행선은 3px 단일 네이비로 낮췄다.
- A4 계약서 표선은 모든 외곽·내부선을 `0.4pt` 단일 규격으로 통일하고 색을 낮췄다. 표 라벨 배경도 한 단계 옅게 조정했으며 금액·표 핵심값의 불필요한 800 굵기를 700으로 줄였다.
- 특약·추가 운전자·연대보증인이 모두 없으면 별도 선택항목 한 장을 만들지 않고 앞 정산 페이지에 `없음/해당 없음` 세 줄로 표시한다. 셋 중 하나라도 값이 있으면 종전 상세 페이지와 서명란이 자동으로 유지된다.
- 기본 계약 PDF는 표지 포함 8장, 추가 운전자·연대보증인 포함 검토 PDF는 9장으로 렌더했다. 약관은 3장·6단 모두 사용률 99% 이상, 넘침 0이며 전체 A4 규격을 확인했다.
- 공급사 검토용 샘플 PDF를 현재 실계약 원본으로 다시 생성해 `public/contract-template/freepass-standard-rental-contract-v1-review.pdf`와 동기화했다.
- 검증: 브라우저 실제 고객 링크 시각 확인, PDF 8장 전수 PNG 확인, 선택 관계인 PDF 상세 페이지 확인, `npx tsc --noEmit`, `npm run check:fonts`, `npm run check:ui`, `sim-esign-document-boundary`, `sim-esign-agreement` 61/61, `sim-freepass-esign` PASS.
- 최종 판정: **PASS**.

## 2026-08-14 고객정보 전면 고객입력 전환

- 직원 작성 화면에서 고객명·연락처·비상연락처 입력영역을 제거했다. 직원은 회사·계약서·정책과 ERP 차량, 기간·연령·대여료·보증금 등 계약조건만 확정한다.
- `direct` 전자계약은 고객명·연락처가 비어 있어도 A4 초안과 고객 링크를 만들 수 있다. Excel·기존 ERP 계약의 고객정보 검증은 유지한다.
- 고객은 수집 동의 후 성명·연락처·주민등록번호·운전면허번호·주소·비상연락처를 직접 입력하고 면허증·셀카를 제출한다. 제출 시 성명·연락처만 v4 계약목록에 반영하고 고유식별정보는 서버 전용 제출자료에 유지한다.
- 브라우저에서 손오공 차량을 선택하고 36개월·만 26세 조건을 적용한 뒤 고객정보 없이 `계약서 생성 준비가 완료되었습니다`와 A4 생성 버튼 활성화를 확인했다.
- 검증: `npx tsc --noEmit`, `sim-freepass-esign`, `sim-esign-source-gate` PASS.
- 최종 판정: **PASS**.
# 전자계약 링크 영업자 전달 동선 정리 (2026-08-14)

- 직원 화면의 발송 안내를 `계약 링크 생성·복사 → 영업자 전달`로 통일했다.
- 직원 입력 범위는 차량·대여조건으로 표시하고, 고객정보는 고객이 계약 링크에서 직접 입력하는 구조를 유지했다.
- 고객 화면은 계약 확인 → 수집 동의 → 본인확인 → 조건 확인 → 약관 → 전자서명 순서로 진행된다.
- 문자·카카오톡 직접 발송 기능은 두지 않고 링크 복사만 제공한다.
- 검증: `npx tsc --noEmit` PASS, `scripts/sim-freepass-esign.mts` PASS, `npm run check:ui` PASS, `git diff --check` 오류 0.
# 추가운전자 고객 직접입력·면허증 첨부 (2026-08-14)

- 공급사 정책의 `additional_driver_allowance_count`가 1명 이상일 때만 고객 전자계약에 `추가 운전자` 단계가 나타난다.
- 직원 계약작성 화면의 추가운전자 인적사항 입력란을 제거하고 정책 허용 인원 안내만 남겼다.
- 고객은 추가운전자별 성명·관계·연락처·면허번호를 직접 입력하고 면허증 사진 및 개인정보 제공 동의를 제출한다.
- 추가운전자 정보·면허증은 `v4/esign_private`와 비공개 스토리지에 저장하며, 일반 계약 노드에는 등록 인원만 기록한다.
- 최종 승인 계약서에는 비공개 제출값을 합성해 추가운전자 명단을 표시하고, 관리자 검토 화면에서 각 면허증을 확인할 수 있다.
- 기존 발행 링크에는 소급하지 않으며 새로 발행한 계약부터 정책 허용값이 발행 스냅샷에 고정된다.
- 검증: `npx tsc --noEmit`, `sim-freepass-esign`, `sim-esign-signed-snapshot`, `sim-esign-source-gate`, `check:ui`, `check:fonts` PASS. `git diff --check` 오류 0.
- 브라우저 확인: 손오공 공통정책에서 `추가 운전자 최대 1명 · 고객이 계약 링크에서 직접 입력하고 면허증 첨부` 표시, 직원 인적사항 입력란 없음.
# 계약서 생성 후 동일 패널 연속 흐름 (2026-08-14)

- `A4 계약서 만들기` 클릭 시 자동으로 새 미리보기 탭을 열던 동작을 제거했다.
- 생성 직후 같은 `계약서·링크` 패널 아래에 생성 결과, A4 확인·출력, 계약 링크 만들기·복사 순서가 이어진다.
- 생성 결과 영역으로 부드럽게 스크롤되며 목록 선택이나 별도 페이지 이동 없이 후속 작업을 계속할 수 있다.
- 새 계약을 시작하거나 다른 계약을 선택하면 직전 생성 요약 상태를 정리한다.
- 검증: `npx tsc --noEmit`, `sim-freepass-esign`, `check:ui`, `check:fonts` PASS. `git diff --check` 오류 0.
# 개인정보 동의 전 모바일 계약서 열람 (2026-08-14)

- 고객 링크 첫 화면에 `모바일 계약서 전체보기`를 추가해 차량·대여·보험·운전자·정산 조건을 동의 전에 확인할 수 있게 했다.
- 같은 열람 영역에서 자동차 대여 약관도 펼쳐볼 수 있다.
- `열람만으로 동의·서명 처리되지 않음`을 명시하고, 다음 버튼을 `개인정보 입력 단계로 이동`으로 구분했다.
- 개인정보 수집 동의와 계약·약관 동의는 기존 후속 단계에서 각각 별도로 받는다.
- 검증: `npx tsc --noEmit`, `sim-freepass-esign`, `check:ui`, `check:fonts` PASS. `git diff --check` 오류 0.

# 전자계약 조건·개인정보·A4 정합성 전수검사 (2026-08-14)

- 발행 스냅샷, 고객 비공개 제출자료, 관리자 승인 완료본의 경계를 대조했다. 주민등록번호·운전면허번호·면허증·셀카는 공개 계약 노드에 복제되지 않고 완료 PDF 생성 시에만 비공개 자료가 합성된다.
- 고객 주민등록번호를 받으면서 부속 운전자격 확인서의 생년월일이 비어 있던 오류를 수정했다. 유효한 주민등록번호에서 생년월일을 파생해 완료본에만 넣고, 발행 스냅샷 원본에는 남기지 않는다.
- 직원이 선택한 운전자 연령이 공급사 기본연령 검사에 가려 만 21세 미만도 통과할 수 있던 경로를 차단했다. 정책 허용 선택지와 선택값을 대조하고, 고객 제출 시 주민등록번호상 만 나이를 차량 인도일 기준으로 최소·최대 연령과 서버에서 다시 대조한다.
- 봉인 A4의 운전자 연령을 옛 직접편집 드롭다운이 `만 26세 이상`으로 다시 덮어쓰던 오류를 수정했다. 봉인본에서는 발행 스냅샷이 유일한 정본이며 `만 26세 이상 · 만 70세 이하`처럼 상한을 포함한 값도 그대로 출력된다.
- 추가 운전자 요금이 고객 화면 원시값으로만 보이고 최종 A4에 남지 않던 문제를 수정했다. `1인당 월 금액`으로 모바일·서명 직전·최종 A4에 동일 표시하며, 허용 인원이 있는데 요금이 비어 있거나 `협의`이면 링크 발행을 차단한다.
- 고객이 입력하지 않는 이메일 행을 계약자 정보 표에서 제거해 실계약 A4 빈칸과 필드맵의 영구 미정 항목을 없앴다. 스페어키 폐기 필드도 필드맵·계약서 템플릿에서 완전히 제거했다.
- 용어는 고객·관리자 모두 `운전면허증`으로 통일했고, 규격 위반 그림자는 공용 디자인 토큰으로 교체했다.
- A4 검증: 일반 계약 8쪽, 추가 운전자 포함 9쪽, 전 페이지 A4. 약관 3쪽·6단은 각 단 사용률 99% 이상, 넘침 0. 실계약 형태에서 운전자 연령 상·하한 및 추가 운전자 월 요금이 잘림 없이 출력됨을 PNG로 확인했다.
- 자동 검증: 전자계약 관련 `sim-*esign*.mts` 전부 PASS, `npm run check:tokens`, `npm run check:ui`, `npm run check:fonts`, `npx tsc --noEmit`, `git diff --check`, `npm run build` PASS.
- 별도 문서가 필요한 연대보증은 기존 결정대로 표준 전자계약 발송 범위 밖이며, 보증인 서면·최고액·직접서명 흐름은 아직 구현하지 않았다. 연대보증 없는 일반 렌터카 계약에는 영향이 없다.
- 최종 판정: **PASS** — 표준 전자계약 발송·고객 입력·관리자 승인·완료 PDF 흐름의 발견 오류를 수정하고 회귀검증 완료.

# 고객 계약 전 A4 미리보기·완료 PDF 다운로드 (2026-08-14)

- 고객 링크 첫 화면에서 개인정보 입력·동의 전에 `계약서 미리보기`를 열 수 있게 했다. 고객 화면에는 내부 출력 규격인 `A4`를 노출하지 않으며, 미리보기는 발행 시 동결된 계약 스냅샷과 실제 완료본이 공유하는 A4 원본으로 생성한다. 열람만으로 동의·서명 진행정보를 기록하지 않는다.
- 고객 토큰 문서 경로는 발행·열람 상태에서만 계약 전 미리보기 PDF를 제공한다. 만료·해지·검토대기 상태는 차단하고, 완료 상태에서는 비공개 저장소에 봉인된 확정 PDF의 해시를 검증한 뒤 제공한다.
- 계약 완료 화면은 `완료 계약서 보기`와 `PDF 다운로드`를 분리했다. 다운로드 응답은 `attachment`와 한글 파일명을 사용하고, 보기 응답은 브라우저 `inline`을 유지한다.
- 브라우저 실제 고객 링크에서 첫 화면의 A4 미리보기 버튼, 개인정보 단계 이동 전 배치, PDF 200 응답(`application/pdf`, 149,323 bytes), 모바일 319px 실사용 폭에서 수평 넘침 0, 오류 오버레이·콘솔 오류 0을 확인했다.
- 자동 검증: 전자계약 관련 `sim-*esign*.mts` 전부 PASS, `npx tsc --noEmit`, `npm run check:fonts`, `npm run check:tokens`, `npm run check:ui`, `npm run build` PASS.
- 최종 판정: **PASS** — 계약 전 A4 확인과 계약 완료 후 확정 PDF 보기·다운로드 경로가 분리되어 동작한다.

# 전자계약 상태전이 적대검증·보완 (2026-08-14)

- 고객이 검토대기 화면을 열어 둔 동안 관리자 승인 결과를 5초 주기와 화면 복귀 시 자동 확인한다. 승인되면 새로고침 없이 `완료 계약서 보기·PDF 다운로드`로 전환한다.
- 관리자 보완요청·해지·만료도 자동 감지한다. 이 경우 브라우저에 남은 면허증·셀카·서명 상태를 재사용하지 않도록 새 응답으로 다시 시작한다.
- 링크 발행·고객 열람·진행기록·고객 제출·보완요청·해지·최종승인의 RTDB 상태가 여러 경로에 부분 저장될 수 있던 구조를 점검했다. 상태·계약목록 투영·감사이력을 `v4` 루트 다중경로 갱신으로 묶어 한 번에 성공하거나 실패하게 했다.
- 최종승인은 세션·비공개 제출자료·봉인검증·계약목록·감사이력까지 한 번에 확정한다. 두 관리자의 동시 승인에는 트랜잭션 선점값을 적용해 PDF·봉인해시가 두 벌 생성되지 않게 했고, 생성/저장 실패 시 검토대기로 복귀한다. 비정상 종료로 남은 승인 선점은 2분 뒤 재시도할 수 있다.
- 승인 처리 중 상태는 고객에게 계속 `검토대기`, 관리자에게 `승인 처리 중`으로 표시해 순간적인 409 오류화면이나 중복 승인 버튼을 만들지 않는다.
- 실제 고객 링크 확인: `계약서 미리보기`가 개인정보 단계보다 먼저 표시되고 모바일 390px에서 수평 넘침 0, 오류 오버레이·콘솔 오류 0. 미리보기 PDF는 200 `application/pdf`, 149,323 bytes, 한글 파일명 헤더 정상.
- 자동 검증: 전자계약 관련 `sim-*esign*.mts` 전부 PASS, `npx tsc --noEmit`, `npm run check:fonts`, `npm run check:tokens`, `npm run check:ui`, `npm run check:release`, `npm run build`, `git diff --check` PASS.
- 실제 가상계약 `TMP-260814-01-gj3f`로 고객 13단계 입력·필수동의·면허증/셀카 첨부·전자서명 제출, 관리자 검토·승인·봉인, 고객 완료화면 자동 전환을 끝까지 통과했다. 고객 완료본은 200 `application/pdf`, `attachment`, 한글 파일명 헤더, 155,068 bytes로 내려받았다.
- 실제 제출 과정에서 Firebase Admin transaction의 첫 로컬 값이 `null`일 때 정상 세션도 제출/승인 선점을 abort하는 결함을 발견했다. null 제안을 통해 서버 실제 값으로 재시도하고, 선점 결과의 상태·시각/claim id를 재검증하도록 고쳤다. 고객 제출 저장은 45초 제한과 90초 stale 재선점을 두고, 이전 요청의 실패가 새 요청 상태를 되돌리지 못하도록 조건부 복구한다.
- 완료 PDF는 8쪽 모두 A4(594.96×841.92pt)이며 암호화·스크립트·폼이 없는 정적 확정본이다. 8쪽 전체를 PNG로 렌더링해 표 잘림·본문 겹침·깨진 글자·페이지 이탈이 없음을 확인했다.
- 기존 범위 밖: 연대보증 약정은 별도 서면·최고액·보증인 직접서명 문서가 필요하므로 표준 전자계약 한 장에 합치지 않는다.
- 최종 판정: **PASS** — 고객 제출부터 관리자 승인·봉인·고객 PDF 열람/다운로드까지 실제 테스트 계약으로 실증했다.

---

## 2026-08-15 벤츠 EQC 400 4MATIC 80kWh 보강 30차 결과: **PASS — 85kWh 오류 차단 행을 공식 80kWh 국내 트림으로 복원**

- Mercedes-Benz 공식 제조사 자료에서 EQC의 사용 가능 배터리 용량 80kWh와 전·후륜 전기모터 기반 사륜구동 구성을 확인했다.
- 국내 판매 자료와 출시 발표를 교차 확인해 `EQC 400 4MATIC`, `Edition 1886`, `Premium` 3개 트림을 확정했다.
- `mf-013.md-038.sm-n293__eqc-400-80k` 아래 3개 영구 코드를 신규 발급했다. 기존 85kWh 행은 의미를 바꾸거나 재사용하지 않고 차단 상태로 보존했다.
- 원천 시트 `차종마스터!A7113:AD7115`를 CellData로 재조회해 3행의 30개 필드, 영구 코드, 근거 링크, Noto Sans KR 9pt 및 기존 행 서식 계승을 확인했다.
- EX30·EX30 Cross Country는 국내 표기 66kWh와 제조사 명목 용량 69kWh가 혼재하므로 추가 발급을 보류했다. EX40도 국내 세부 트림·배터리 조합이 닫히기 전까지 보류한다.
- 최종 집계는 총 4,861행, 자동 2,913행, 수동 325행, 차단 1,623행이다. `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `check:fonts`, `git diff --check`가 모두 PASS했다.

## 2026-08-15 푸조 올 뉴 5008 스마트 하이브리드 보강 31차 결과: **PASS — 3세대 국내 현행 7인승 트림 2종 복원**

- 푸조 코리아 공식 국내 모델 페이지에서 1,199cc 직렬 3기통 스마트 하이브리드, FWD, e-DCS6 자동변속기, 7인승 사양을 확인했다.
- 스텔란티스 코리아 공식 2026-02-05 출시 발표에서 국내 판매 트림이 Allure와 GT 두 가지임을 교차 확인했다.
- `mf-021.md-025.sm-p67__5008-smart-hybrid-1-2` 아래 2개 영구 코드를 신규 발급했다. 기존 파워트레인·트림 공란 행은 재사용하지 않고 차단 상태로 보존했다.
- Google 원천 시트 `차종마스터!A7116:AD7117`을 CellData로 재조회해 30개 필드, 링크, Noto Sans KR 9pt 및 기존 행 서식 계승을 확인했다.
- 같은 회차에 공급사 원문 `E200 AVANTGARDE KR3(25MY)`가 CLE 3.0 4MATIC으로 잘못 붙은 운영 매물 `142호7629` 1대를 `v4/products` 오버레이에서 E-클래스 W214·가솔린 2.0·E200 아방가르드로 교정했다. 변경 전 값은 `tmp/missnap-undo-20260815173133.json`에 보존했다.
- 교정 후 `audit-master-gap`에서 운영 상품 404대 전부 판정 성공, 신규 결손 0종을 확인했다.
- 최종 집계는 총 4,863행, 자동 2,913행, 수동 327행, 차단 1,623행이다. `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `check:fonts`가 모두 PASS했다.

## 2026-08-15 포드 넥스트 제너레이션 레인저 보강 32차 결과: **PASS — 국내형 4세대 Wildtrak·Raptor 복원**

- 포드코리아 국내 판매 페이지와 포드 공식 딜러 국내용 카탈로그에서 2023 NEXT-GEN RANGER의 Wildtrak·Raptor 두 트림을 확인했다.
- 국내형 두 트림은 1,996cc 2.0L Bi-Turbo 디젤, 10단 자동변속기, 4WD, 더블캡 5인승 조합으로 분리했다.
- `mf-024.md-023.sm-p703__next-gen-ranger-2-0-bi-turbo` 아래 2개 영구 코드를 신규 발급했다. 기존 4세대 행은 국내 정식 판매 사실과 충돌하는 제외 메모를 포함하므로 의미를 고치거나 재사용하지 않고 차단 상태로 보존했다.
- 상품명이 트림까지 식별되지 않을 때 임의 배정되지 않도록 두 행 모두 `검증중/교차확인` 수동 후보로 유지했다.
- Google 원천 시트 `차종마스터!A7118:AD7119`를 CellData로 재조회해 30개 필드, 링크, Noto Sans KR 9pt 및 기존 행 서식 계승을 확인했다.
- 최종 집계는 총 4,865행, 자동 2,913행, 수동 329행, 차단 1,623행이다. `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `check:fonts`, `git diff --check`가 모두 PASS했다.

## 2026-08-15 포드 브롱코 6세대 국내 파워트레인 분리 33차 결과: **PASS — 2.7·2.3 Outer Banks의 잘못된 차단 경계를 복원**

- 포드코리아 22MY 국내용 공식 카탈로그에서 Outer Banks 4도어 2,694cc 2.7L V6 EcoBoost, 자동 10단, Advanced 4×4, 5인승 사양을 확인했다.
- 포드코리아 2025 타이어 에너지 소비효율 공식 자료에서 국내 Bronco 2.3 Outer Banks를 확인하고, 2024-09 국내 투입 발표와 등록 실차 제원으로 2,261cc 2.3L I4 EcoBoost, 자동 10단, 4WD, 5인승을 교차 확인했다.
- `mf-024.md-016.sm-u725__bronco-outer-banks-27`과 `mf-024.md-016.sm-u725__bronco-outer-banks-23`을 각각 발급했다. 엔진이 다른 두 시기의 의미가 섞이지 않도록 별도 마스터로 분리했다.
- 공식 판매 종료 월은 확인되지 않아 생산종료·연식종료를 임의로 `현재` 또는 특정 월로 채우지 않고 공란으로 유지했다. 두 행은 `검증중/교차확인` 수동 후보로 제한했다.
- 기존 2.7·2.3 행은 국내 정식 판매 모델을 비주력·병행수입으로 설명하거나 2.3의 생산 시작을 2022-03으로 둔 오류가 있어 수정·재사용하지 않고 차단 상태로 보존했다.
- Google 원천 시트 `차종마스터!A7120:AD7121`을 CellData로 재조회해 30개 필드, 공식 PDF 링크, 종료일 공란, Noto Sans KR 9pt 및 기존 행 서식 계승을 확인했다.
- 최종 집계는 총 4,867행, 자동 2,913행, 수동 331행, 차단 1,623행이다. `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `check:fonts`, `git diff --check`가 모두 PASS했다.

## 2026-08-14 전자계약 대표 도메인 전환

결과: **PASS — 신규 프리패스 링크와 착한거래 과거 링크를 토큰 규격으로 분리**

- 신규 전자계약은 운영 환경의 `FREEPASS_ESIGN_PUBLIC_BASE_URL`을 기준으로 `https://sign.freepasserp.com/fps_...` 형식의 짧은 고객 링크를 만든다. 로컬·미설정 환경은 기존 요청 주소의 `/sign/{token}`으로 안전하게 폴백한다.
- `sign.freepasserp.com`에서 `fps_` 토큰은 프리패스 `/sign/{token}`으로 내부 rewrite하고, 기존 착한거래의 22자리 토큰과 `/sign?c=...` 구형 링크는 `chakhandeal.vercel.app`으로 쿼리스트링을 보존해 redirect한다.
- `npx tsc --noEmit`, `sim-freepass-esign`, `check:fonts`, `check:tokens`, 별도 distDir production build 33/33, middleware 신규·레거시·기존경로 직접 시뮬레이션, `git diff --check` PASS.
- 이미 발행된 프리패스 계약의 예전 `/sign/fps_...` 저장 주소도 관리자 상태 응답에서 새 대표 주소로 정규화하며, 세션 해시 폴백은 예전 경로형과 새 루트형 `fps_` 주소를 모두 인식한다.

## 2026-08-14 기본 렌트 운영 발행 게이트 분리

결과: **PASS — 기본 렌트 v1.0만 운영 발행 허용, 구독 샘플 2종은 계속 차단**

- 운영 실계약 테스트에서 인증·v4 초안 생성까지 통과했으나 기존 전역 샘플 잠금이 기본 렌트까지 `503`으로 막는 것을 확인했다.
- 프리패스 기본계약서 `freepass-rent-standard`만 `v1.0` 정본으로 분리하고, 보험포함·보험별도 구독계약서는 `sample-v1` 상태를 유지했다.
- 프리패스 자체 발행과 착한거래 발송은 모두 사용자가 선택한 개별 서식의 `isSample`을 검사한다. 미지정·알 수 없는 서식도 Production에서 차단해 우회 발행을 막는다.
- `npx tsc --noEmit`, `sim-esign-issue-gate` 8/8, `sim-freepass-esign`, `check:fonts`, `check:tokens`, production build 33/33 PASS.
- 운영 테스트 초안 `TMP-260814-E2E-5hdp`은 개인정보 없이 v4에 생성했으며, 배포 후 동일 회차로 링크·고객 페이지·공개 API·미리보기 PDF를 검증한다.

### 운영 계약 전 PDF 렌더러 보완

- 위 회차는 발행 후 `https://sign.freepasserp.com/fps_...` 고객 페이지와 공개 계약 API가 각각 200으로 열렸지만, 계약 전 미리보기 PDF가 503인 것을 확인했다.
- 원인은 로컬 렌더러가 PC의 Chrome을 사용하면서 Vercel 함수에도 같은 실행파일이 있다고 가정한 것이었다. Playwright 1.49의 Chromium 131과 맞는 `@sparticuz/chromium@131.0.1`을 운영 의존성으로 고정하고, Windows는 로컬 Chrome·Vercel Linux는 서버리스 headless-shell을 선택한다.
- Next.js 출력 추적에 전자계약 API용 Chromium 바이너리 5개가 실제 포함된 것을 각 문서·승인 함수의 `.nft.json`에서 확인했다. 실패 시 계약번호와 안전한 오류 메시지를 런타임 로그에 남긴다.
- 첫 운영 재검증에서 Chromium 실행 뒤 A4 원본 HTML이 함수 번들에 없어 503이 나는 다음 경계를 확인했다. 전자계약 함수 출력 추적에 `rental-contract.html`과 Pretendard woff2 5종도 함께 포함한다.
- 자산 포함 뒤 Vercel Node 24가 AWS 런타임 감지 변수를 제공하지 않아 AL2023의 `libnss3` 묶음을 풀지 않는 경계도 확인했다. Vercel Linux에서 패키지 로드 전에 호환 런타임을 명시해 Chromium과 AL2023 공유 라이브러리를 함께 압축 해제한다.
- `npx tsc --noEmit`, `sim-freepass-esign`, `sim-esign-issue-gate` 8/8, `check:fonts`, `check:tokens`, production build 33/33 PASS.
- 최종 운영 배포 `dpl_3bgbeTS2YKNCCE1txxxEqfHfkDXk`에서 테스트 계약 `TMP-260814-E2E-5hdp`의 `sign.freepasserp.com` 고객 페이지 200, 공개 API 200, 계약 전 PDF 200 `application/pdf`·241,811 bytes를 확인했다. 같은 배포의 최근 error 로그는 0건이다.

## 2026-08-15 차종마스터 트림행키 영구계약

결과: **CONDITIONAL PASS — 기존 4,294개 코드는 기준판으로 동결, 런타임 SSOT 전환은 별도 단계**

- Google Sheets 커넥터로 `ERP4 차종마스터 원천대장 / 차종마스터!A1:AD6807`을 다시 읽었다. 트림행키 4,294개, 고유키 4,294개, 중복 0개였다.
- `data/vehicle-trim-key-registry.json`에 코드별 마스터ID·순번과 차량 의미 19개 축을 기준판으로 저장했다. 행 번호는 진단값일 뿐 비교 대상이 아니므로 정렬·행 이동은 허용된다.
- `audit-vehicle-trim-key-contract.mts`는 등록키 삭제, 코드 재사용, 코드↔마스터ID/순번 불일치, 기존 코드의 차량 의미 변경, 미등록 신규키가 있으면 실패한다. `--register-new`는 기존 계약 위반이 0일 때 신규 코드만 추가한다.
- `차종마스터!J:M` 전체 열에 경고형 보호범위를 추가했다(`protectedRangeId=487013426`). 기존 값·서식·열 너비·숨김 상태는 변경하지 않았다.
- 라이브 커넥터 재추출본으로 기준판 4,294 ↔ 라이브 4,294, 삭제·재사용·순번변경·차량의미변경 0건을 확인했다.
- 적대 시뮬레이션: 동일입력 멱등, 삭제, 의미변경, 신규키, 순번변경 5시나리오 PASS.
- 자동 검증: `npx tsx scripts/sim-vehicle-trim-key-contract.mts`, `npm run typecheck`, `git diff --check`, `npm run build`(33/33) PASS.
- 남은 조건: 현재 운영 서비스계정은 `spreadsheets.readonly` 위임이 없어 CLI 라이브 감사를 직접 실행할 수 없다. 쓰기 스코프로 넓히지 않고 커넥터 추출 스냅샷으로 검증했으며, 자동화하려면 읽기 전용 위임만 별도로 추가한다.
- 이번 단계는 행키 안정성만 고정했다. 검증상태를 실제 확정 기준으로 정규화하고 Sheet→JSON 단방향 생성기를 런타임에 연결하는 작업은 아직 하지 않았으므로, 영구키를 운영 외래키로 쓰기 전 그 두 단계가 남는다.

## 2026-08-15 차종마스터 운영 상태·ERP 행 단위 산출물

결과: **PASS — 시트 상태 규격 정규화, 행 단위 JSON 생성, 자동/수동/차단 정책 연결**

- `차종마스터`의 비규격 상태를 네이티브 드롭다운 값으로 정리했다. 최종 분포는 `검증중/1차확인` 2,654행, `확정/확정` 24행, `제외/1차확인` 1,614행, `제외/미검증` 2행이며 비허용 상태는 0행이다. 1차확인 행은 임의로 확정 승격하지 않았다.
- `public/data/vehicle-trim-master.json`은 시트 4,294행을 1:1 레코드로 보존한다. 트림행키·정확배기량·구동·인승·배터리·생산기간·근거와 상태를 포함하며 코드순 결정적 정렬을 사용한다.
- 사용 등급은 `automatic` 24행, `manual` 2,654행, `blocked` 1,616행이다. 차단 행도 기존 차번에 박힌 코드의 의미를 조회할 수 있도록 JSON과 `byCode`에는 남기되 신규 후보 색인에서는 제외한다.
- 공급사 자동 코드 부여는 `확정/확정`만 사용한다. 직원용 후보·워크리스트는 `검증중/1차확인·교차확인`과 확정 행을 보여 주고, 수동 스탬프도 차단 행은 거부한다.
- 자동 검증: 상태 정책 시뮬레이션, JSON 생성 시뮬레이션, 영구키 적대 시뮬레이션, 기준판 감사(4,294↔4,294), 생성 드리프트 0, `npm run typecheck`, `git diff --check`, 프로덕션 `npm run build` 33/33 PASS.

## 2026-08-15 인기차 누락 4종 우선 보강

결과: **PASS — 실재고 영향이 큰 누락 4종 108개 트림행 추가, 영구키 계약 유지**

- `차종마스터!A4288:AD4395`에 싼타페 MX5 48행, 아이오닉6 18행, 그랑 콜레오스 9행, 벤츠 S-클래스 W223·W222 33행을 추가했다.
- 엔진·배터리·구동·인승을 파워트레인 축에서 분리했고, 각 행에 생산기간·정확배기량·터보·근거URL·기준일을 포함했다. 현행 세대는 `신차`, 종료 세대는 `중고차`로 구분했다.
- 신규행은 공식 제조사 가격표·카탈로그와 숨김 원천을 대조한 `검증중/1차확인`으로 등록했다. 교차확인 없이 `확정/확정`으로 올리지 않았다.
- 전체 라이브 시트 재감사 결과 트림행키 4,402개, 고유키 4,402개, 중복 0개다. 라이브 19개 의미축 해시와 영구 레지스트리 해시 `d3e05f536ee68ce3`가 일치했다.
- 행 단위 JSON은 4,402행이며 `manual` 2,761행, `automatic` 24행, `blocked` 1,617행이다. 영구 레지스트리와 JSON에 신규 108개 코드를 함께 등록했다.
- 전체 배정 후보의 8개 식별축(제조사·모델·세부모델·파워트레인·트림·구동·인승·배터리)을 다시 묶어 G80 RG3 완전중복 1건을 발견했다. 뒤 코드는 삭제하지 않고 `제외`로 보존해 영구키 의미를 유지했으며, 최종 후보 완전중복은 0건이다.
- 브라우저에서 `차종마스터!A4288` 선택, 문서 저장 완료, Noto Sans KR 9pt·CLIP·기존 표 서식 유지 상태를 확인했다.
- 자동 검증: 행키 적대 시뮬레이션, 행 단위 JSON 시뮬레이션, 시트 상태 정책 시뮬레이션, `npm run typecheck`, `git diff --check`, 프로덕션 `npm run build` 33/33 PASS.

## 2026-08-15 판매용 4탭 차량상태 준실시간 연동

결과: **CONDITIONAL PASS — 상태 전용 경로와 계약 보호는 통과, 운영 전체 동기화 기준선 정리가 별도 필요**

- 영업자 상품찾기와 내부 상세가 로그인 직후·창 포커스 복귀·화면 표시 복귀·60초 주기로 `/api/sheet/live-status`를 호출한다. 서버는 45초 신선도와 2분 lease로 여러 사용자의 Google Sheets 조회를 한 번으로 합친다.
- 상태 전용 writer는 `vehicle_status`, `sheet_status_owner`, `sheet_block_reason`, `sheet_blocked_at`만 허용한다. 제원·가격·사진·정책·공급사·private 필드는 쓰지 않으며 v3도 쓰지 않는다.
- `locked_by_contract` 또는 ERP `계약중`은 계약 엔진 소유로 보존한다. 시트의 미확인 `계약중`은 ERP 계약락을 만들지 않고 `출고불가/source_contract_status`로만 투영한다.
- 시트 전체 50% 이상 급감, ERP 동일 공급사·차번 중복, 시트↔ERP 공급사 소유 충돌은 전부 fail-closed다. 신규 차량은 상태 경로가 생성하지 않고 기존 전체 동기화만 생성한다.
- 실데이터 읽기 전용 감사: 4개 탭 524행, 제외 15행, 유효 508대, 17개 공급사. ERP 활성 원본 623대 중 확정 매칭 451대, 상태 변경 계획 130건(부재 차단 74), 계약락 보존 1, 수기보류 보존 1, 전체 동기화가 필요한 신규 57대였다.
- 실데이터에서 `J&J` 7행이 파트너 원장 `RP030/주식회사 제이앤제이렌트카`와 매칭되지 않던 것을 명시 별칭으로 고정했다.
- 자동 검증: `sim-sheet-live-status` 11/11, `sim-sales-inventory-sheet`, `sim-sheet-daily-sync` 25/25, `npx tsc --noEmit`, `check:fonts` PASS. 프로덕션 코드는 컴파일됐지만 전체 build의 page-data 단계는 동시에 수정 중인 기존 `/api/freepass-esign/contracts/[contractCode]/document` 모듈 탐색 오류로 중단됐다.
- 전체 동기화도 시트 `계약중` 3대를 ERP 계약락 없이 출고불가로 투영하도록 같은 규칙으로 맞췄다. 그 다음 실데이터 게이트에서 공급사 미확정 삭제이력 3대(109호4941·109호4289·109호2052)와 기존 가격기간 누락 26건이 남았다. 신규 57대의 제원·가격 반영은 이 29건을 관리자 승인/원문 수정으로 해소한 뒤 별도 전체 동기화를 통과시켜야 한다.

## 2026-08-15 영업자 전자계약 링크 발행 권한

결과: **PASS — 영업자 작성·미리보기·링크 발행, 관리자 검토 권한 분리**

- 영업자 모바일 핵심 동선을 `상품찾기 → 계약진행 → 전자계약 → 설정`으로 정리하고, 전체 메뉴에도 전자계약을 노출했다. 상품 화면에는 `구글시트 상품 확인 → 카카오톡 문의 → ERP 전자계약` 순서를 안내한다.
- `/esign`과 A4 미리보기는 영업자·관리자에게 허용했다. 영업자는 회사·계약서 종류·정책과 ERP 차량·대여조건을 선택해 계약을 만들고 고객 링크를 발행·복사·해지할 수 있다.
- Admin SDK가 RTDB rules를 우회하는 점을 보완해 영업자는 자기 UID 계약, 채널 관리자는 같은 채널 계약만 열 수 있도록 API에서 소유권을 재검증한다. 남의 계약은 존재 여부를 숨긴 404로 닫는다.
- 고객 서명 이미지·신분증·면허증 등 본인확인 자료 열람, 보완 요청, 최종 승인, 인도일 확정은 관리자만 가능하다. 영업자 상태 응답에서는 서명 원본·해시·비공개 자산 URL도 제거한다.
- 사용하지 않던 1,700여 줄 구형 4패널 구현을 제거해 `/esign` 정본을 `EsignSendCenter` 하나로 통일했다.
- 자동 검증: `sim-freepass-esign`, `sim-esign-issue-gate` 8/8, `check:fonts`, `check:tokens`, `check:ui`, production build 33/33 PASS. 앱 내 브라우저는 로컬 `/esign` URL을 보안정책으로 차단해 역할별 클릭 검수는 배포 후 영업자 계정 스모크 테스트로 남긴다.

## 2026-08-15 차종마스터 현행·실재고 커버리지 마감

결과: **PASS — 현행 인기차·전기차·최근 10년 실재고 136행 보강, 영구키 계약 유지**

- 현행 인기차 71행을 추가했다. 아이오닉9·EV4·EV5·타스만, 벤츠 GLA/GLB/GLS/G-클래스/CLA, 폭스바겐 아틀라스/투아렉/골프/GTI/ID.4/ID.5, 볼보 XC60, 토요타 캠리/RAV4/프리우스/크라운/하이랜더/시에나/알파드를 공식 제조사 자료 기준으로 분리했다.
- 실제 재고 매칭용 30행을 추가했다. 초기 EV6 CV1, 르노 아르카나, 기아 PV5, 폴스타2를 별도 세부모델·배터리·구동·인승 축으로 만들었다. 기존 아이오닉6 8행은 `아이오닉6 CE`에서 실제 재고 표기인 `아이오닉6`으로 정규화하되 영구키는 유지했다. 볼트 EUV 기존 영구키도 삭제 없이 공식 66kWh 프리미어 사양으로 복구했다.
- 최근 10년 운영 중고차 35행을 추가했다. 그랜저 HG 20트림, SM3 Z.E. 22kWh/35.9kWh 6트림, 벤츠 A-클래스 W176 5트림, BMW F22 2트림, 캐딜락 XT6 6/7인승 2사양이다. SM3 Z.E.와 F22의 기존 용량·기간 미구분 키는 계속 `제외`로 보존하고, 정확 사양에는 새 변형번호를 부여해 기존 코드의 차량 의미를 바꾸지 않았다.
- BYD는 ATTO 3·DOLPHIN·SEAL·SEALION 7·SEALION 6 DM-i 9행, 지커는 7X 75/100kWh RWD·AWD 3행이 이미 공식 한국 라인업 기준으로 등록돼 있음을 재확인했다.
- 모든 신규 셀은 기존 표의 Noto Sans KR 9pt·CLIP·세로 가운데맞춤을 유지했다. 마지막 35행은 쓰기 전 빈 범위와 셀 서식을 읽고, 쓰기 후 1,050셀을 생성 원본과 전수 대조해 값 불일치 0·서식 불일치 0을 확인했다.
- 최종 라이브 원천은 트림행키 4,538개, 사용 가능 2,922개, 제조사 27·모델 220·세부모델 404다. 키 중복 0, 형식 오류 0, 마스터ID/순번 불일치 0, 전기차 배기량 오류 0, 엔진차 배기량 누락 0, 연료 라벨 모순 0, 빈 트림 0이다. 1,950cc를 제조사 라벨 `디젤 1.9`로 쓰는 벤츠 W213 8행만 감사 경고이며 실제 제원 오류는 아니다.
- 실재고 544대 중 세부모델 미커버는 최초 140대에서 32대로 줄었다. 남은 32대는 카니발 VQ, 렉스턴 Y200, K5 TF, 쏘렌토 BL, K9 KH, W204 등 현재 기준 최근 10년 범위를 넘긴 구형 16종이라 활성 마스터로 억지 편입하지 않았다.
- 영구키 라이브 계약은 기준판 4,538 ↔ 시트 4,538로 일치하며 삭제·재사용·순번 변경·차량 의미 변경 0건이다. 라이브 감사기는 Workspace에 실제 위임된 Sheets 인증범위를 사용하도록 맞췄고 읽기 동작만 수행한다.
- 자동 검증: 라이브 원천 전수감사, 라이브 영구키 감사, 동일입력/삭제/의미변경/신규/순번변경 5시나리오, 행 단위 JSON 시뮬레이션, 상태 정책 시뮬레이션, `npm run typecheck`, 프로덕션 `npm run build` 33/33 PASS.

## 2026-08-15 차종마스터 현행차 운영 승격

결과: **PASS — 현행 신차 공식근거 942행 전부 자동 적용 가능, 키·서식·ERP 산출물 드리프트 0**

- 라이브 `차종마스터`에서 `신차`이면서 생산종료·연식종료가 모두 `현재`인 수동 후보 942행을 다시 심사했다. 공식 제조사 HTTPS 출처, 트림행키·마스터ID·순번, 생산 시작월, 연료·정확배기량 또는 전기 사양, 터보·구동·인승, 근거메모·기준일을 통과한 행만 `확정/확정`으로 승격했다.
- 1차 746행, 인승·구동 축으로 분리한 카니발·팰리세이드·스타리아·EV9·BMW X7 66행, BMW·아우디·테슬라·폭스바겐·포르쉐·도요타 60행, 지정된 현행 상업명 모델 70행을 차례로 승격했다. 기존 24행을 포함해 현행차 자동 적용 대상은 966행이다.
- 동일 제조사·모델·세부모델·파워트레인·트림이 같아도 인승이 다른 조합은 자동으로 고르지 않던 매처를 보강했다. 호출자가 구동·인승·배터리 축을 제공하면 유일한 행만 선택하고, 해당 축이 없거나 여전히 복수이면 계속 무선택으로 남는다.
- 테슬라와 일부 BMW 전기차는 공식 국내 페이지가 배터리 총용량을 공개하지 않는 경우가 있어 숫자를 추정하지 않았다. 근거메모에 공식 미공개와 공란 유지가 명시된 행만 예외로 인정했다. 현행 상업명으로 세대를 식별한 예외도 제조사·모델 조합을 명시 목록으로 고정해 신규 모델에 자동 확장되지 않는다.
- 현행차 승격 직후 상태는 `확정/확정` 966행, `검증중/1차확인` 1,954행, `검증중/미검증` 2행, `제외` 1,616행이었다. 현행 신차 수동 대기는 0행이다.
- 상태 열 A:B 6,807행을 쓰기 전후 다시 읽어 Noto Sans KR 9pt와 기존 서식 불일치 0을 확인했다. 영구키 기준판 4,538 ↔ 라이브 4,538, 삭제·재사용·순번변경·차량의미변경 0, 시트↔`public/data/vehicle-trim-master.json` 드리프트 0이다.
- 자동 검증: `audit:vehicle-trim-keys`, `check:vehicle-trim-master`, 운영 승격 정책·인승축·영구키·행 단위 JSON·상태정책 시뮬레이션, `npm run typecheck` PASS. 운영 승격 재계획은 추가 후보 0·식별 보류 0이다.

## 2026-08-15 최근 10년 중고차 운영 승격

결과: **PASS — 공식근거·식별축·사양이 완결된 최근 10년 중고차 1,685행 자동 적용 가능, 잔여 271행만 수동 보강**

- `중고차`이면서 생산종료가 `YYYY-MM`, 연식종료가 2016년 이상인 행을 대상으로 제조사·모델·세부모델·파워트레인·세부트림, 세대명 또는 개발코드, 정확배기량/전기 사양, 터보·구동·인승, 공식 제조사 HTTPS 근거·메모·기준일을 모두 재검사했다.
- 전체 식별축(제조사·모델·세부모델·파워트레인·세부트림·구동·인승·배터리)이 유일한 1,685행만 `확정/확정`으로 승격했다. 제조사별로 현대 480, 기아 456, 쉐보레 114, KG모빌리티 122, 르노코리아 50, 제네시스 49, BMW 237, 벤츠 73, 아우디 60, 포르쉐 10, 포드 4, 지프 30행이다.
- 1차 승격 직후 라이브 상태는 `확정/확정` 2,651행, `검증중/1차확인` 269행, `검증중/미검증` 2행, `제외` 1,616행이었다. 당시 남은 수동 271행은 세대명/개발코드 미완결 247행, 공식 출처 도메인 26행, 공식 근거메모 8행, 생산기간 6행, 전기차 배터리 사양 9행이 겹쳐 있어 자동 확정하지 않았다.
- 상태 열 A:B 6,807행을 적용 전후 재조회해 Noto Sans KR 9pt 서식 불일치 0을 확인했다. 영구키 기준판 4,538 ↔ 라이브 4,538, 삭제·재사용·순번변경·차량의미변경 0, 시트↔`public/data/vehicle-trim-master.json` 드리프트 0이다.
- 운영 승격 재계획은 추가 후보 0·식별 모호성 보류 0이다. 영구키 5시나리오, 행 단위 JSON, 상태정책, 인승축 매칭, `npm run typecheck`, 프로덕션 `npm run build` 33/33을 모두 통과했다.

## 2026-08-15 차종마스터 운영 마감

결과: **PASS — 자동 적용 2,913행, 수동 예외 4행, 차단 1,623행; 신규 EQA 승계키 포함 영구키 4,540개 안정**

- 남은 271행을 모델별로 재검토했다. 제조사 공식 보조 도메인과 기존 근거메모를 엄격한 허용 목록으로 정리하고, 생산기간상 한 세대로 특정되는 기존 세부모델 59개 그룹을 명시 목록으로 고정했다. 이 정책으로 현대·쉐보레·KGM·르노코리아·제네시스·BMW·벤츠·아우디·볼보·푸조·지프·테슬라 등 260행을 추가 확정했다.
- 기존 키 242개의 공란 세대명/개발코드를 직접 채우려는 시도는 영구키 계약이 차량 의미 변경으로 즉시 차단했다. 해당 242개 셀 묶음은 적용 전 공란으로 전부 원복했고, 라이브 감사에서 의미변경 0을 확인한 뒤 시트 값을 바꾸지 않는 명시 정책으로 전환했다. 이는 발급된 키의 의미가 편집·보강으로 조용히 바뀌지 않는다는 실제 차단 검증이다.
- 더 뉴 EQA H243는 2026-08-15 벤츠 공식 현행 모델 페이지를 재확인했다. 기존 중고차 키 `t01/t02`는 의미를 수정하지 않고 `제외` 보존했으며, 현행 65.9kWh FWD 트림을 새 영구키 `t03/t04`로 승계 추가·등록·확정했다. 구형 A4 2005-01~2015-05 5행은 최근 10년 밖이므로 기존 키를 보존한 채 제외했다.
- 최종 라이브 상태는 `확정/확정` 2,913행, `검증중/1차확인` 4행, `제외/1차확인` 1,621행, `제외/미검증` 2행이다. 수동 4행은 공식 종료월을 특정하지 못한 테슬라 모델 Y 구형 3행과 AMG EQE 1행이며, 임의 종료월·배터리값을 넣지 않고 직원 수동 선택 후보로 유지한다.
- 상태 열 A:B 6,809행 재조회에서 Noto Sans KR 9pt 서식 불일치 0이다. 영구키 기준판 4,540 ↔ 라이브 4,540, 삭제·재사용·순번변경·차량의미변경 0, 시트↔`public/data/vehicle-trim-master.json` 드리프트 0이다. 운영 승격 재계획은 추가 후보 0·식별 모호성 보류 0이다.

## 2026-08-15 상품마스터 최초 구축

결과: **CONDITIONAL PASS — 마스터 탭·기간 규격·소유권은 확정, ERP 정본 전환은 대조 후 진행**

- `ERP4 차종마스터 원천대장`의 `차종마스터` 바로 옆에 `상품마스터`(`gid=679088240`)를 추가했다. 같은 문서의 보이는 핵심 탭은 `차종마스터·상품마스터`, 숨김 보조 탭은 기존 4개다.
- 차량번호별 540행을 최초 이관했고 차량번호 중복 0, 차량번호·상태 누락 0이다. 공급사코드 누락 13대(스위치플랜 8·에코 5)는 코드를 짐작하지 않고 `검수필요`로 남겼다.
- 기간 축을 `1·6·12·18·24·36·48·60·72·84개월`로 고정하고 각 `대여료` 바로 오른쪽에 같은 기간 `보증금`을 배치했다. 미운영 값의 `-` 339칸은 빈칸으로 정리했다.
- 차명은 유효한 트림행키가 가리키는 차종마스터의 `제조사·모델·세부모델·파워트레인·세부트림`만 한 칸에 표시한다. 코드 미매칭 191대는 추정 차명을 확정값처럼 노출하지 않았다. 확정은 349대다.
- 공급사 원문은 `공급사 제공정보`, 선택옵션은 `옵션` 한 칸으로 보존했다. 빈 정책코드 376대는 `(프리패스 기본)`으로 채웠다.
- 실제 숫자 6개월 요금은 42대이며 같은 기간 보증금까지 붙였다. `불가` 7칸과 `-` 46칸은 요금으로 세지 않았다. 18·72·84개월은 현재 원본 실제 요금 0대라 규격 열만 유지했다.
- 최종 관리상태는 운영 287대·검수필요 253대다. 모든 검수필요 행에 `검수사유`가 있으며, 미확인 보증금은 12개월 24대·24개월 3대·36개월 3대로 값을 추정하지 않았다.
- 반복 갱신 허용목록은 `차량상태·정책코드·기간별 대여료·기간별 보증금`뿐이다. 차량번호·차종코드·차명·옵션·원문정보·주행거리는 공급사 재수집으로 덮지 않는다.
- 자동 검증: `sim-product-master-sheet`, `sim-supplier-template` 24/24, `sim-sheet-price` 34/34, `sim-atom-pipeline` 도착 36·유실 0, `npx tsc --noEmit`, `check:fonts`, `check-manual-drift` 22/22 PASS.
- 현재 ERP 운영 유입은 판매시트 4탭이다. 상품마스터를 직접 정본으로 전환하기 전 `상품마스터 ↔ 판매시트 ↔ ERP`의 대수·차량번호·가격원자와 주행조건/인수형 변형 보존을 통과시켜야 한다.

## 2026-08-15 전자계약 상태 전이·동시 처리 보강

결과: **PASS — 중복 링크·해지 후 상태 부활·승인/보완 충돌 차단**

- 계약별 발행 잠금을 추가했다. 두 직원이 동시에 링크 생성을 눌러도 기존 활성 링크를 재사용하며, 새 링크로 교체할 때 이전 세션은 `revoked/supersededBy`로 원자 폐기한다.
- 고객 열람과 단계 진행 저장을 RTDB transaction으로 바꿨다. 해지·만료·완료 상태에서는 쓰기가 중단되며, 계약의 현재 세션 해시까지 일치할 때만 목록 상태를 갱신한다.
- 링크 해지는 `sent/opened`에서만 허용한다. 고객 제출 중·검토대기·승인 중에는 해지할 수 없어 제출 완료 상태가 해지 요청에 의해 되살아나거나 사라지지 않는다.
- 관리자 승인과 보완 요청에 각각 선점 토큰과 stale 복구를 적용했다. 동시에 누르면 한 작업만 진행하며 나중 요청이 완료 상태를 덮지 못한다.
- 공개 계약서·PDF는 상태 문자열뿐 아니라 `revokedAt`도 확인한다. 해지 직후의 경합에서도 고객 링크와 미리보기가 다시 열리지 않는다.
- 전체 `sim-*esign*.mts` 통과. 오래된 두 시뮬레이션의 샘플 가정은 현재 운영 기준인 `렌트 v1.0 정본 / 구독 2종 sample-v1 잠금`으로 정정했다.
- 자동 검증: `npm run typecheck`, `sim-freepass-esign`, 전체 전자계약 시뮬레이션, `check:fonts`, `check:tokens`, `check:ui`, `git diff --check`, 프로덕션 `npm run build` 33/33 PASS.
- 연대보증 전자서명은 별도 보증약정·최고액 고지가 필요한 별도 흐름으로 남아 있으며, 현재 일반 렌터카 전자계약 발행에는 포함하지 않는다.

## 2026-08-15 상품마스터 시각·입력 규격 통일

결과: **PASS — 차종마스터와 동일한 네이티브 표 규격 적용, 잘못 밀린 입력 제약·금액 서식 복구**

- `상품마스터!A1:AK1000`을 차종마스터와 같은 Noto Sans KR 9pt, 35px 헤더, 24px 데이터행, 녹색 헤더·흰색/연회색 교차행의 네이티브 표(`ProductMaster`)로 전환했다.
- 관리상태·검증상태·차량상태를 표 드롭다운 열로 고정하고 상태별 글자색을 차종마스터 규칙과 같은 녹색·주황·빨강 계열로 통일했다.
- 차량상태 선택지가 `공급사코드(E)`에 잘못 붙어 있던 입력 제약을 제거하고 실제 `차량상태(H)` 열로 복구했다. 허용값은 `출고가능·출고협의·출고불가·계약중·판매완료`다.
- 기간별 금액 서식을 `P:AI` 전체에 `#,##0원`으로 다시 적용하고, 금액 서식이 밀려 있던 `최종갱신(AJ)·원천(AK)`은 텍스트 서식으로 복구했다.
- 공급사·차량 식별부는 7열까지 고정하고, 차명·옵션·원문정보·사진·기간별 금액 열은 데이터 성격에 맞춰 폭을 재조정했다. 사용하지 않는 `AL:AT`는 숨겨 가로 여백을 제거했다.
- 커넥터 재조회에서 표 범위 1,000행×37열, 열 정의 37개, 드롭다운 3열, 조건부서식 12개, 동결행 1·동결열 7, 숨김 여분열 9개를 확인했다. 구글시트 화면에서도 문서 저장 완료, `상품마스터` 탭 선택, Noto Sans KR 9pt 및 네이티브 표 도구 활성화를 확인했다.

## 2026-08-15 상품마스터 차명·공급사 원문 표시 정리

결과: **PASS — 정제 차명은 3축으로 축약, 제조사와 공급사 원문은 별도 한 칸에 보존**

- `상품마스터!K2:K541`의 확정 차명 349대를 `세부모델(없으면 모델) · 파워트레인 · 세부트림` 3축으로 통일했다. 제조사·상위 모델 중복은 제거했고 미매칭 191대의 정제 차명은 계속 빈칸으로 유지했다.
- `상품마스터!M2:M541`은 모든 행을 `제조사: … | 공급사 차명: … | 공급사 원문 속성` 형식으로 통일했다. 차종코드 또는 차종마스터 모델명으로 제조사가 유일한 528대와 명시 별칭 7대를 포함해 535대의 제조사를 표시했다.
- 원문만으로 제조사를 특정할 수 없는 5대는 `미입력`으로 표시했다. 임의 제조사나 차종코드를 만들지 않았다.
- 헤더와 네이티브 표 열 이름을 `차명(모델명·파워트레인·세부트림)`, `공급사 제공정보(제조사 포함)`으로 함께 갱신했다. 재조회 결과 5축 잔존 0, 3축 349, 원문 접두 형식 540/540, 표 범위 `A1:AK1000` 유지다.

## 2026-08-15 렌트·구독 상용차 차종마스터 보강 2차

결과: **PASS — 포터II LPG 현행 캡·축·구동·변속기·트림 27행을 수동 후보로 추가**

- 현대 2026 포터II 공식 가격표를 근거로 LPG 2.5 터보 2,469cc 현행 조합을 일반캡·슈퍼캡·더블캡, 2WD 초장축·4WD 장축, 수동6단·자동5단, 스마트·모던·프리미엄 축으로 분리했다. 2WD 18행과 4WD 9행이며 4WD 더블캡은 공식 적재량에 맞춰 0.8톤으로 식별했다.
- 차체 구성별 마스터 ID 6개와 트림행키 27개를 새로 발급했다. 기존 영구키 의미는 변경하지 않았고 전 행을 `검증중/1차확인`으로 유지해 자동 매칭에는 투입하지 않았다.
- 원천 시트 `차종마스터!A6804:AD6830`, 로컬 산출물, 영구키 레지스트리를 동기화했다. 로컬 집계는 총 4,576행, 자동 2,913행, 수동 40행, 차단 1,623행이며 포터 LPG 27행의 코드 중복은 0이다.
- 시트 재조회에서 27행의 값, Noto Sans KR 9pt·CLIP 서식, 공식 PDF 하이퍼링크를 확인했다. 네이티브 표·밴딩·조건부서식은 6,838행까지 확장됐고 J:M 영구 식별자 경고 보호범위도 유지됐다.
- 자동 검증: `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `check:fonts`, 대상 파일 `git diff --check` PASS.

## 2026-08-15 렌트·구독 상용차 차종마스터 보강 3차

결과: **PASS — 봉고III LPG 현행 일반 트럭 43행을 공식 판매 조합대로 수동 후보 추가**

- 기아 현행 가격·제원 페이지와 2023-11-23 LPG 터보 출시자료를 근거로 1톤 2WD 25행, 1톤 4WD 9행, 1.2톤 2WD 9행을 추가했다. 킹캡·더블캡·표준캡, 초장축·장축, 2WD·4WD, 수동6단·자동5단, 실제 판매 트림을 각각 독립 식별축으로 보존했다.
- 스마트 셀렉션은 공식 구성에 따라 자동5단 트림으로만 등록했다. 동일 변속기를 별도 파워트레인 순번으로 중복 발급하지 않고 해당 자동5단 순번 아래의 독립 트림행키로 배치했다.
- 차체 구성별 마스터 ID 8개와 트림행키 43개를 신규 발급했다. 전 행은 `검증중/1차확인`이며 자동 매칭 대상이 아니다. 원천 시트 반영 범위는 `차종마스터!A6831:AD6873`이다.
- 증분 재실행 시 새 묶음의 `capturedSheetRow`가 시작행부터 다시 계산되는 감사 좌표 결함을 발견했다. 생성 전체 순서에서 좌표를 먼저 부여하고 기존 레지스트리도 같은 값으로 교체하는 멱등 동기화로 수정했다. 최종 좌표는 EV 6795~6803, 포터 LPG 6804~6830, 봉고 LPG 6831~6873으로 원천 시트와 일치한다.
- 로컬 집계는 총 4,619행, 자동 2,913행, 수동 83행, 차단 1,623행이다. 봉고 LPG 43행의 영구키 중복은 0이고 레지스트리 43행과 일치한다.
- 네이티브 표·밴딩·조건부서식은 6,881행까지 확장됐고 J:M 영구 식별자 경고 보호범위를 유지했다. `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `check:fonts` PASS.

## 2026-08-15 렌트·구독 승합 차종마스터 보강 4차

결과: **PASS — 쏠라티 현행 15·16인승 완성 승합 4행을 수동 후보 추가**

- 현대 2025-07 공식 가격표와 현행 모델 제원을 근거로 15인승 스탠다드·디럭스·럭셔리 및 16인승 디럭스를 추가했다. 2인승은 가격표상 미완성 자동차이므로 일반 렌트 완성차 후보에서 제외했다.
- A2.5 CRDi 디젤, 자동8단, RWD, 인승을 식별축으로 구성했다. 공식 현행 근거가 배기량을 2.5L로만 표기하므로 정확 cc는 추정 입력하지 않고 공란으로 유지했으며, 전 행을 `검증중/1차확인`으로 제한했다.
- 마스터 ID 2개와 트림행키 4개를 신규 발급했다. 원천 시트 `차종마스터!A6874:AD6877`, 로컬 산출물, 영구키 레지스트리가 일치하고 감사 좌표도 6874~6877로 확인했다.
- 최종 로컬 집계는 총 4,623행, 자동 2,913행, 수동 87행, 차단 1,623행이다. `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `check:fonts`, 대상 파일 `git diff --check` PASS.

## 2026-08-15 ERP5 내부 B2B 업무 홈 1차

결과: **CONDITIONAL PASS — 기존 Firebase·엔진을 재사용하는 독립 `/erp5` 로컬 진입점 구축, 브라우저 시각 검수만 환경 차단으로 보류**

- 사용자 미션을 `Google Sheet 차량 원자 확인 → 전자계약 발송 → 내가 계약한 건과 정산 확인`으로 고정했다. 고객 공개용 `/q`·`/catalog`는 변경하지 않았다.
- `app/erp5/page.tsx`를 새로 추가했다. 기존 `useFinderData`를 재사용해 공급사 시트 실시간 차량상태 오버레이를 받고, 영업자는 가격이 있는 계약 가능 차량만, 공급사·관리자는 가격 입력 전 재고 원자까지 보도록 역할별 판정을 분리했다.
- 계약·정산은 새 데이터 복사 없이 기존 `contract`·`settlement`를 읽고 `canAccessOwnedRecord`로 본인/조직 범위를 제한한다. 전자계약 작성은 검증·스냅샷·링크 생성이 있는 기존 `/esign`, 진행과 정산은 기존 `/contract`로 연결했다.
- 기존 40개 수정 파일·32개 신규 파일의 미커밋 작업을 보존하기 위해 기존 라우트·RTDB adapter·정산엔진·Rules는 수정하지 않았다. Google Sheet 역쓰기와 대화방 키 변경도 이번 범위가 아니다.
- `npx tsc --noEmit`, `check:fonts`, `check:tokens`, `check:ui`, `sim-freepass-esign`, `sim-product-offerability` 31/31, `git diff --check -- app/erp5/page.tsx`, 프로덕션 `npm run build` 35/35 PASS. 빌드 결과 `/erp5` 동적 라우트가 생성됐다.
- 로컬 Next 서버에서 `GET /erp5` 200과 컴파일 성공을 확인했다. 내장 브라우저는 `localhost`·`127.0.0.1`·로컬 네트워크 주소를 모두 `ERR_BLOCKED_BY_CLIENT`로 차단해 실제 화면 캡처와 모바일 viewport 검수는 보류했다. 메인 반영 전 사람 브라우저에서 웹·모바일 각 1회 확인이 필요하다.

## 2026-08-15 포터Ⅱ 2020 디젤 냉동 특장 차종마스터 보강 12차

결과: **PASS — 중고 렌트·물류 수요용 냉동탑차/하이냉동탑차 16행을 공식 판매 조합대로 수동 후보 추가**

- 현대 2020 포터II 특장차 공식 가격표(2020-03-01 기준)와 기본차 가격표를 근거로 2.5 CRDi 유로6 2WD 초장축 냉동탑차·하이냉동탑차의 일반캡 스마트, 슈퍼캡 스타일·스마트·모던 플러스를 반영했다.
- 각 구성의 기본 수동6단과 가격표 공통 A/T 선택인 자동5단을 분리해 마스터 ID 8개, 트림행키 16개를 신규 발급했다. 생산종료는 공식 근거가 없어 공란이며 전 행은 `중고차/검증중/1차확인`으로 자동 매칭에서 제외했다.
- 원천 시트 `차종마스터!A7008:AD7023`, 로컬 산출물, 영구키 레지스트리를 동기화했다. 시트 재조회에서 16행 전체 값과 영구키를 확인했고 네이티브 표·밴딩·조건부서식은 7,031행까지 확장됐다.
- 로컬 집계는 총 4,769행, 자동 2,913행, 수동 233행, 차단 1,623행이다. `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `git diff --check` PASS.
- `check:fonts`는 이번 변경과 무관한 기존 `app/page.tsx:71`의 `fontSize: 13.5` 오프스케일 값 1건 때문에 실패했다. 해당 사용자 변경은 보존했다.
- 초기 58.8kWh 포터II EV 특장은 2021 판매 개시와 기본차 제원은 공식 확인했으나 당시 바디별 판매 조합을 입증할 가격표를 확보하지 못해 이번 영구 코드 발급에서 보류했다.

## 2026-08-15 포터Ⅱ 2020 디젤 내장·윙 특장 차종마스터 보강 13차

결과: **PASS — 내장탑 계열과 수동식·전동식 윙바디 28행을 공식 판매 조합대로 수동 후보 추가**

- 현대 2020 포터II 특장차 공식 가격표를 근거로 내장탑차 4구성, 하이내장탑차 4구성, 내장탑파워게이트·저상내장탑차 각 1구성, 수동식·전동식 윙바디 각 2구성을 반영했다. 각 구성은 기본 수동6단과 공통 A/T인 자동5단을 분리했다.
- 마스터 ID 14개와 트림행키 28개를 신규 발급했다. 더블캡 내장탑차만 6인승이며 나머지는 3인승이다. 윙의 수동식·전동식은 개폐방식으로 명시해 실제 변속기 축과 혼동되지 않게 했다.
- 원천 시트 최초 대량 삽입에서 표 내부 행 삽입이 기존 11행을 복제하는 현상을 재조회로 발견했다. 오류 구간 `A7024:AD7051`의 값만 비운 뒤 8행 이하 묶음으로 재기록했으며, 기존 포터 LPG 키의 중복 검색 결과를 2건에서 원래 1건으로 복구했다.
- 최종 원천 범위 `차종마스터!A7024:AD7051`의 J:K 28쌍을 전수 재조회해 `md-091`부터 `md-096`까지 생성 순서와 일치함을 확인했다. 로컬 산출물·레지스트리 좌표도 7024~7051이며 총 4,797행, 자동 2,913행, 수동 261행, 차단 1,623행이다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `git diff --check` PASS. `check:fonts`의 기존 `app/page.tsx:71` 오프스케일 값은 이번 범위 밖 사용자 변경으로 유지한다.

## 2026-08-15 운영 재고 커버리지 감사·르노 필랑트 보강 14차

결과: **PASS — 운영 상품 99.8% 완전 부합 확인, 2026 필랑트 하이브리드 4행 선제 보강**

- 읽기 전용 `audit-master-gap.mts`로 실제 운영 상품 404대를 감사했다. 제조사·모델·세부모델·파워트레인·트림 5축이 모두 부합한 상품은 403대(99.8%)였다.
- 유일한 결손은 벤츠 CLE-클래스 C236 `가솔린 3.0 4MATIC` 상품에 `E200 아방가르드`가 붙은 1대다. 파워트레인과 트림이 서로 모순이므로 신규 차종코드를 만들어 오염시키지 않고 공급사 원천 정정 대상으로 남겼다.
- 현행 국내 생산 신차 중 마스터에 없던 르노 필랑트 하이브리드 E-Tech를 2026-07 르노코리아 공식 가격표 기준으로 추가했다. 1,499cc 가솔린 터보, 멀티모드 오토, 듀얼 모터, 1.64kWh 배터리와 테크노·아이코닉·에스프리 알핀·에스프리 알핀 1955 네 트림을 분리했다.
- 공식 가격표가 구동방식을 직접 표기하지 않아 전 행은 `검증중/1차확인` 수동 후보로 제한했다. 영구 마스터 `mf-005.md-020.sm-filante__filante-hybrid-e-tech`와 트림행키 4개를 발급했다.
- 원천 시트 `차종마스터!A7052:AD7055`, 로컬 산출물, 영구키 레지스트리를 동기화했다. 재조회에서 값·Noto Sans KR 9pt·CLIP 서식과 표/조건부서식 7,055행 확장을 확인했다.
- 최종 로컬 집계는 총 4,801행, 자동 2,913행, 수동 265행, 차단 1,623행이다. `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `git diff --check` PASS.

## 2026-08-15 2027 아이오닉 5 현행 렌트 핵심 보강 15차

결과: **PASS — 빠져 있던 63kWh·84kWh 2WD/AWD 및 영업용 13행을 배터리·구동별 영구 코드로 분리**

- 기존 자동 사용 가능 아이오닉 5는 84kWh AWD 중심 5행뿐이었고, 기존 2WD 행은 배터리 용량이 없는 모호한 행이라 안전상 `제외` 상태였다. 기존 키 의미를 바꾸거나 모호한 행을 승격하지 않고 신규 코드만 발급했다.
- 현대 2027 아이오닉 5 공식 가격표(현 모델 출시일 2026-06-09)를 근거로 Standard 63.0kWh 2WD E-Value Plus 1행, Long Range 84.0kWh의 E-Lite·모던·프리미엄·N Line·인스퍼레이션을 2WD/AWD 각각 5행, 84.0kWh 영업용 2WD/AWD 2행으로 구성했다.
- `mf-001.md-097` 아래 Standard·Long Range·영업용 마스터 ID 3개와 트림행키 13개를 발급했다. HTRAC 옵션은 별도 AWD 파워트레인 순번으로 분리하고 트림 순번은 구동별로 동일하게 유지했다.
- 전 행은 공식 가격표와 산업부 인증 제원을 대조한 `검증중/교차확인` 수동 후보다. 렌트·택시용 영업용 사양도 일반 Long Range와 분리해 오매칭을 방지했다.
- 원천 시트 `차종마스터!A7056:AD7068`, 로컬 산출물, 영구키 레지스트리를 동기화했다. 재조회에서 13개 키의 순서·값과 Noto Sans KR 9pt·CLIP 서식, 표 범위 7,068행을 확인했다.
- 최종 로컬 집계는 총 4,814행, 자동 2,913행, 수동 278행, 차단 1,623행이다. `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `git diff --check` PASS.
## 2026-08-15 초기 아이오닉 5 58.0/77.4kWh 중고 렌트 보강 16차
결과: **PASS — 2022년형 아이오닉 5 10개 트림을 배터리·구동별 영구 코드로 분리**

- 현대 아이오닉 5 공식 가격표(현 모델 출시일 2022-07-15)를 근거로 Standard 58.0kWh 익스클루시브 2WD/HTRAC와 E-Lite HTRAC, Long Range 77.4kWh 익스클루시브·프레스티지 2WD/HTRAC, 영업용 58.0kWh 및 Long Range 77.4kWh/HTRAC를 구성했다.
- Standard·Long Range·영업용 마스터 ID 3개와 트림행키 10개를 신규 발급했다. 기존 배터리 미구분 아이오닉 5 행은 의미를 변경하거나 재사용하지 않고 `제외` 상태로 보존했다.
- 더 뉴 아이오닉 5의 2024-03 출시 경계를 적용해 생산기간을 2022-07~2024-02, 연식을 2023~2024로 제한했다. 원천 시트 반영 범위는 `차종마스터!A7069:AD7078`이며 `mf-001.md-098` 검색 결과는 정확히 10행이다.
- 로컬 산출물과 영구키 레지스트리를 동기화한 최종 집계는 총 4,824행, 자동 2,913행, 수동 288행, 차단 1,623행이다. `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `git diff --check` PASS.
- 출시 초기 72.6kWh 사양은 국내 공식 과거 가격표를 아직 확보하지 못했으므로 해외 브로슈어만으로 추정 코드를 발급하지 않았다. 국내 공식 근거 확보 후 별도 신규 코드로 보강한다.

## 2026-08-15 전자계약 공급사별 추가 제출서류

결과: **PASS — 고객정보 없는 계약 발행, 공급사 정책별 서류 요청·고객 직접 첨부·관리자 검토 흐름 완성**

- 직원 발행 스냅샷에서 고객 이름·연락처·생년월일·주소를 제거했다. 고객은 공개 계약 링크에서 본인정보·운전면허증·셀카를 직접 입력·제출하며, 고객정보는 서명 제출 이후 비공개 제출 데이터로만 보관된다.
- 직접 전자계약 작성 화면에서도 고객·사업자 정보 입력을 제거하고 `새 계약조건 만들기 → 조건 저장 → A4 확인 → 고객 작성 링크 복사`로 용어와 동선을 단순화했다. 직접 생성 레코드에는 고객 필드 자체를 쓰지 않으며, 링크 최초 제출자가 계약자로 접수된다. Excel 호환 입력만 기존 고객 필드를 유지한다.
- 고객 작성 링크는 수신자를 미리 지정하지 않아 개인정보 없는 계약조건을 동의 없이 먼저 열람할 수 있다. 다만 최초 제출권을 가진 bearer 링크이므로 관리자 화면에는 공개 게시하지 말고 실제 계약 고객에게만 전달하도록 명시했다.
- 정책관리 전자계약 패널에 최대 6개의 공급사 요청서류를 구성하는 전용 편집기를 추가했다. 개인 기본·소득 확인·개인사업자·법인 프리셋을 제공하되 서류명·안내·필수 여부는 공급사별 정책에서 변경한다.
- 계약 발행 시 선택 정책의 서류 목록을 스냅샷으로 동결한다. 이후 정책을 바꿔도 이미 발행한 고객 링크의 제출 요구가 바뀌지 않는다.
- 고객 화면은 해당 계약에 요청된 서류만 표시한다. 사진 또는 PDF를 파일별 5MB까지 별도 비공개 경로에 업로드하며, 필수서류가 없으면 다음 계약 단계와 최종 제출이 모두 거부된다.
- 관리자 계약 검토 화면에서 제출서류명·파일명·필수 여부·제출시각을 확인하고 보호된 자산 URL로 열람한다. 보완요청 시 추가 제출서류 단계만 다시 열 수 있으며, 최종 봉인 해시와 검증기록에는 첨부파일 SHA가 포함된다.
- 기존 고정 제출서류 상수는 새 프리셋을 참조하도록 바꿔 중복 기준을 제거했다. 실제 공급사별 요구서류는 배포 전 각 공급사 정책에서 확정해야 한다.
- `npx tsc --noEmit`, 전자계약 시뮬레이션 20개 전부, `check:fonts`, `check:tokens`, `check:ui`, `git diff --check` PASS. 실행 중 로컬 서버와 `.next` 충돌을 피한 별도 `NEXT_DIST_DIR` 프로덕션 빌드에서 35/35 페이지와 `/api/freepass-esign/public/[token]/supporting-document/[docKey]` 라우트 생성을 확인했다.
## 2026-08-15 초기 아이오닉 5 72.6kWh 중고 렌트 보강 17차
결과: **PASS — 제조사 공식 인증중고차 실차 근거로 72.6kWh 4개 조합을 수동 후보 보강**

- 현대자동차 인증중고차 공식 상세에서 2021-07 최초등록 2022년형 2WD 롱레인지 익스클루시브와 2022-02 최초등록 2022년형 AWD 롱레인지 프레스티지의 배터리 72.6kWh·구동방식을 직접 확인했다. 같은 제조사 페이지의 실제 구매차량 정보로 2WD 프레스티지와 AWD 익스클루시브도 교차 확인했다.
- `mf-001.md-099.sm-ne__ioniq5-2021-long-range-72k` 아래 익스클루시브·프레스티지 × 2WD·AWD 영구키 4개를 신규 발급했다. 가격표 원본보다 증거 강도가 낮으므로 전부 `검증중/교차확인` 수동 후보로 제한했다.
- 77.4kWh 현 모델 출시일 2022-07-15 이전 세대로 생산기간을 2021-04~2022-06, 연식을 2022로 분리했다. 기존 배터리 미구분 행이나 58.0/77.4kWh 신규 코드의 의미는 변경하지 않았다.
- 원천 시트 범위는 `차종마스터!A7079:AD7082`이며 `mf-001.md-099` 검색 결과는 정확히 4행이다. 표·조건부서식 범위와 시트 rowCount도 7,082행까지 확장됐다.
- 로컬 최종 집계는 총 4,828행, 자동 2,913행, 수동 292행, 차단 1,623행이다. `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `check:fonts`, `git diff --check` PASS.
## 2026-08-15 초기 니로 EV DE 64.0kWh 중고 렌트 보강 18차
결과: **PASS — 국내 유통이 확인된 프레스티지·노블레스 2개 트림을 배터리 명시 영구 코드로 분리**

- 기아 공식 배터리 제조사 공지에서 니로 EV DE의 국내 생산기간 2018-07~2021-12를 확인하고, 기아 헤리티지 아카이브에서 64.0kWh 배터리를 확인했다.
- 기아 인증중고차 공식 매물에서 2021 니로 EV A/T 프레스티지와 2019·2020 니로 EV A/T 노블레스 실차 트림을 교차 확인했다. `mf-002.md-100.sm-de__niro-ev-64k` 아래 FWD 프레스티지·노블레스 영구키 2개를 신규 발급했다.
- 글로벌 기아 보도자료에는 39.2kWh 선택 가능성이 언급되지만 국내 유통 실차 근거가 부족하므로 코드를 발급하지 않았다. 기존 배터리 미구분 니로 EV DE 3행도 `제외` 상태로 보존했다.
- 신규 행은 근거 강도를 반영해 `검증중/교차확인` 수동 후보로 제한했다. 생산기간 2018-07~2021-12, 연식 2019~2022, 64.0kWh, FWD, 5인승으로 구성했다.
- 원천 시트 범위는 `차종마스터!A7083:AD7084`이며 `mf-002.md-100` 검색 결과는 정확히 2행이다. 최종 집계는 총 4,830행, 자동 2,913행, 수동 294행, 차단 1,623행이다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `check:fonts`, `git diff --check` PASS.
## 2026-08-15 캐스퍼 일렉트릭 AX1e 배터리·트림 보강 22차 결과: **PASS — 기존 오염 행을 보존하고 공식 42/49kWh 조합을 신규 영구 코드로 분리**

- 현대자동차 캐스퍼 공식 가격표에서 프리미엄 42kWh, 인스퍼레이션 49kWh를 확인하고, 인스퍼레이션 사양을 승계하는 크로스·라운지를 49kWh로 분리했다. 현대 공식 제원 및 배터리 정보에서도 항속형·크로스 49kWh를 교차 확인했다.
- 현대자동차 헤리티지의 국내 출시 시점 2024-08을 생산 시작 경계로 적용했다. 세부모델은 내연기관 AX1과 구별되는 `캐스퍼 일렉트릭 AX1e`, 개발코드는 `AX1e`로 관리한다.
- `mf-001.md-103.sm-ax1e__casper-electric` 아래 42kWh FWD 프리미엄 1행과 49kWh FWD 인스퍼레이션·크로스·라운지 3행을 신규 발급했다. 기존 AX1 전기 3행은 내연기관 생산기간과 배터리 미구분 문제가 있어 의미를 변경하지 않고 `제외` 상태로 보존했다.
- 신규 4행은 `검증중/교차확인` 수동 후보로 제한했다. 원천 시트 범위는 `차종마스터!A7090:AD7093`이며 `mf-001.md-103` 4행과 행 서식을 재조회해 확인했다.
- 최종 집계는 총 4,839행, 자동 2,913행, 수동 303행, 차단 1,623행이다. `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `check:fonts`, `git diff --check` PASS.
## 2026-08-15 2022 볼트 EV 66kWh 보강 23차 결과: **PASS — 공식 국내 단일 트림만 신규 영구 코드로 복원**

- 쉐보레 코리아 공식 가격표와 2022년형 제원·사전계약 보도자료에서 `Premier`, 66kWh 리튬이온 배터리, 150kW 싱글 모터, 전륜구동, 5인승을 교차 확인했다.
- `mf-003.md-058.sm-bolt-ev__bolt-ev-2022-66k::v01::t01`을 신규 발급했다. 기존 `mf-003.md-051.sm-뉴-볼트-ev` 행은 차단 상태의 영구키이므로 의미를 변경하거나 재사용하지 않았다.
- 공식 고객 인도 재개 시점에 맞춰 유통 시작을 2022-04로 설정하고 2022~2023년형 중고차 수동 후보로 제한했다. 구형 볼트 EV는 국내 트림별 배터리 원문이 충분하지 않아 신규 코드를 발급하지 않았다.
- 원천 시트 `차종마스터!A7094:AD7094`에서 신규 마스터 ID와 30개 필드·행 서식을 재조회했다.
## 2026-08-15 더 뉴 아이오닉 일렉트릭 38.3kWh 보강 24차 결과: **PASS — 렌트·카셰어링 핵심 전기 세단 공백 복원**

- 현대자동차 공식 가격표에서 N·Q 두 국내 트림과 100kW 구동 모터, 38.3kWh 리튬 이온 폴리머 배터리를 직접 확인했다.
- 현대자동차 공식 지속가능성 보고서에서 2019-05 부분변경 출시와 기존 28kWh에서 38.3kWh로의 배터리 변경을 교차 확인했다.
- `mf-001.md-104.sm-ae-pe__ioniq-electric-38k` 아래 FWD N·Q 2행을 신규 발급하고 `검증중/교차확인` 수동 후보로 제한했다. 구형 28kWh는 국내 연식별 트림 원문이 부족해 발급하지 않았다.
- 코나 일렉트릭은 기존 마스터가 OS 39.2/64kWh와 SX2 48.6/64.8kWh를 이미 자동 매칭하므로 중복 코드를 만들지 않았다.
- 원천 시트 `차종마스터!A7095:AD7096`에서 신규 마스터 ID와 30개 필드·행 서식을 재조회했다.
## 2026-08-15 BMW i3 120Ah 보강 25차 결과: **PASS — 배터리 미구분 전량 차단 모델의 국내 공식 조합 복원**

- BMW 그룹 코리아 공식 국내 출시자료에서 i3 120Ah의 37.9kWh 고전압 리튬이온 배터리, 170마력 eDrive 모터와 LUX·SOL+ 두 트림을 확인했다.
- BMW 공식 i3 제원에서 후륜구동과 4인승 구성을 교차 확인하고 `mf-012.md-044.sm-i01__bmw-i3-120ah` 아래 두 영구 코드를 신규 발급했다.
- 기존 `mf-012.md-019.sm-i3`의 배터리 미구분 SOL+·LUX·SOL 코드는 의미를 변경하지 않고 차단 상태로 보존했다. 120Ah 국내 공식 자료에 없는 SOL 조합은 신규 발급하지 않았다.
- 신규 행은 2019-05~2022-06, 2019~2022년형 중고차 `검증중/교차확인` 수동 후보로 제한했다.
- 원천 시트 `차종마스터!A7097:AD7098`에서 신규 마스터 ID와 30개 필드·행 서식을 재조회했다.
## 2026-08-15 BMW i3 94Ah 보강 26차 결과: **PASS — 2018~2019 국내 카셰어링 세대의 배터리·트림 분리**

- BMW 그룹 코리아 공식 국내 사전계약·제원자료에서 뉴 i3 94Ah의 33kWh급 배터리, 순수 충전용량 27.2kWh, 170마력 eDrive, 후륜구동과 LUX·SOL+ 두 트림을 확인했다.
- `mf-012.md-044.sm-i01__bmw-i3-94ah` 아래 27.2kWh RWD LUX·SOL+ 두 영구 코드를 신규 발급했다. 120Ah와 동일한 모델 번호를 공유하되 배터리 세부모델 ID는 분리했다.
- 배터리 필드는 BMW 국내 제원표의 순수 충전 용량을 적용하고 `33kWh`는 별칭으로만 수용해 94Ah와 120Ah의 자동 혼동을 방지했다.
- 신규 행은 2018-03~2019-04, 2018~2019년형 중고차 `검증중/교차확인` 수동 후보로 제한했다.
- 원천 시트 `차종마스터!A7099:AD7100`에서 신규 마스터 ID와 30개 필드·행 서식을 재조회했다.
## 2026-08-15 아이오닉 일렉트릭 28kWh 보강 27차 결과: **PASS — 초기 렌트·카셰어링 전기 세단 세대 복원**

- 현대자동차 공식 국내용 가격표에서 N 기본 트림의 88kW 모터·28kWh 배터리와 N 사양을 승계하는 Q 트림을 확인했다.
- 현대자동차 공식 전동화 연혁의 2016-07 전기차 양산 시점과 공식 보고서의 2019-05 38.3kWh 부분변경 시점을 교차해 생산기간을 2016-07~2019-04로 분리했다.
- `mf-001.md-104.sm-ae__ioniq-electric-28k` 아래 28.0kWh FWD N·Q 두 영구 코드를 신규 발급했다. 같은 모델 번호의 38.3kWh 부분변경 마스터와 배터리 세부모델 ID를 분리했다.
- 신규 행은 2017~2019년형 중고차 `검증중/교차확인` 수동 후보로 제한했다.
- 원천 시트 `차종마스터!A7101:AD7102`에서 신규 마스터 ID와 30개 필드·행 서식을 재조회했다.
## 2026-08-15 쏘울 부스터 EV SK3 배터리·트림 보강 19차
결과: **PASS — 공식 카탈로그의 실제 배터리·트림 대응 2개만 영구 코드로 발급**

- 기아 2020-11 쏘울 EV 공식 카탈로그에서 노블레스(기본형 배터리)=64kWh·150kW·386km, 프레스티지(도심형 배터리)=39.2kWh·100kW·250km 대응을 확인했다.
- 기아 공식 고전압배터리 안내의 SK3 EV 제작년월 2019-02~2020-11을 생산 경계로 적용하고, 공식 Kia CarPay 지원 연형을 참고해 연식 범위를 2019~2021로 설정했다.
- `mf-002.md-101.sm-sk3__soul-booster-ev` 아래 64kWh FWD 노블레스와 39.2kWh FWD 프레스티지를 서로 다른 파워트레인 순번으로 발급했다. 존재 근거가 없는 64kWh 프레스티지·39.2kWh 노블레스 교차 조합은 만들지 않았다.
- 신규 2행은 `검증중/교차확인` 수동 후보로 제한했다. 원천 시트 범위는 `차종마스터!A7085:AD7086`이고 `mf-002.md-101` 검색 결과는 정확히 2행이다.
- 최종 집계는 총 4,832행, 자동 2,913행, 수동 296행, 차단 1,623행이다. `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `check:fonts`, `git diff --check` PASS.
## 2026-08-15 PS 쏘울 EV·운영 재고 안전성 재감사 20차
결과: **PASS — 추정 코드 발급을 차단하고 운영 재고 99.8% 완전 매칭 유지 확인**

- 현대자동차그룹 공식 기술 자료에서 1세대 쏘울 EV의 30kWh·국내 복합 180km를 확인했고, 기아 공식 글로벌 자료에서 27kWh에서 30kWh로의 업데이트를 확인했다. 기아 내비게이션 자료도 `쏘울 Electric`과 `쏘울 Electric 2017`을 별도 차종으로 구분한다.
- 다만 국내 판매 가격표·카탈로그 원문에서 27/30kWh별 실제 국내 트림명을 확정하지 못했다. 배터리만 확인된 상태에서 임의 트림을 만들면 영구 코드 의미가 오염되므로 PS 코드는 발급하지 않고 근거 확보 대기로 유지했다.
- 최신 로컬 차종마스터로 운영 재고 404대를 다시 감사했다. 403대(99.8%)는 제조사·모델·세부모델·파워트레인·세부트림 5축이 모두 매칭됐다.
- 유일한 결손은 벤츠 CLE C236 `가솔린 3.0 4MATIC`에 `E200 아방가르드`가 결합된 원천 상품 1대다. 파워트레인과 트림이 서로 모순이므로 신규 코드로 흡수하지 않고 공급사 원천 정정 대상으로 유지했다.
- 최근 10년 차단 목록도 재분류했다. 다수 차단 행은 2015년 이전 구형 세대이며, 최근 범위의 명백한 모순인 `카니발 KA4 디젤 3.5` 역시 신규 코드 대상에서 제외했다.
## 2026-08-15 초기 아반떼 AD LPi 렌트·법인 보강 21차
결과: **PASS — 공식 인증중고차 분류의 LPG 1.6 세 트림을 영구 코드로 복원**

- 현대자동차 인증중고차 공식 차종 분류에서 아반떼 AD(15~18년) LPG 1.6의 실제 트림 `스타일`, `스마트`, `법인전용`을 확인했다.
- 현대자동차 헤리티지의 2015-09 출시와 1,591cc·전륜구동 제원, 2018-09 더 뉴 아반떼 출시 자료를 교차해 초기형 생산기간을 2015-09~2018-08, 연식을 2016~2018로 설정했다.
- `mf-001.md-102.sm-ad__avante-ad-lpi` 아래 LPG 1.6 FWD 세 트림 영구키를 신규 발급했다. 기존 트림 미상 `mf-001.md-019.sm-ad__아반떼-ad::v04::t01`은 의미를 변경하지 않고 `제외` 상태로 보존했다.
- 신규 행은 근거 강도를 반영해 `검증중/교차확인` 수동 후보로 제한했다. 원천 시트 범위는 `차종마스터!A7087:AD7089`이며 `mf-001.md-102` 검색 결과는 정확히 3행이다.
- 최종 집계는 총 4,835행, 자동 2,913행, 수동 299행, 차단 1,623행이다. `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `check:fonts`, `git diff --check` PASS.

## 2026-08-15 더 뉴 아이오닉 5 배터리·구동 분리 보강 28차 결과: **PASS — 현행 렌트 핵심 EV의 배터리 미구분 차단 공백 복원**

- 현대자동차 공식 국내 판매 가격표에서 스탠다드의 63.0kWh 배터리·후륜 모터와 E-Value Plus·익스클루시브 트림을 확인했다.
- 같은 공식 가격표에서 롱레인지의 84.0kWh 배터리와 익스클루시브·프레스티지, HTRAC 전륜 모터 선택 사양을 확인해 RWD/AWD를 분리했다.
- `mf-001.md-061.sm-ne-pe__ioniq5-standard-63k` 아래 2행과 `mf-001.md-061.sm-ne-pe__ioniq5-long-range-84k` 아래 4행을 신규 영구 코드로 발급했다. 기존 배터리 미구분 행은 의미를 변경하거나 재사용하지 않고 차단 상태로 보존했다.
- 원천 시트 `차종마스터!A7103:AD7108`을 재조회해 6행 모두 30개 필드와 영구 코드가 저장된 것을 확인했다.
- 최종 집계는 총 4,854행, 자동 2,913행, 수동 318행, 차단 1,623행이다. `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `check:fonts`, `git diff --check`가 모두 PASS했다.

## 2026-08-15 볼보 EX90 국내 출시 사양 보강 29차 결과: **PASS — 현행 수입 전기 SUV의 공란 차단 행을 공식 조합으로 복원**

- 볼보자동차코리아 공식 국내 출시 자료에서 106kWh NCM 배터리, AWD 기반 Twin Motor·Twin Motor Performance와 Ultra 6인승·7인승 가격 조합을 확인했다.
- 볼보 공식 국내 제원 페이지에서 두 파워트레인의 106kWh, AWD, 6~7인승 사양을 교차 확인했다.
- `mf-017.md-027.sm-ex90__ex90-2026-106k` 아래 Twin Motor/Performance × Ultra 6/7인승 4행을 신규 영구 코드로 발급했다. 좌석 구성이 가격 문구에 직접 연결되지 않은 Twin Motor Plus는 발급하지 않았다.
- 원천 시트 `차종마스터!A7109:AD7112`를 CellData로 재조회해 4행의 30개 필드, 링크, Noto Sans KR 9pt 및 기존 행 서식 계승을 확인했다.
- 최종 집계는 총 4,858행, 자동 2,913행, 수동 322행, 차단 1,623행이다. `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `check:fonts`, `git diff --check`가 모두 PASS했다.
## 34. MINI 일렉트릭 F56 국내 유통 트림 보강 (2026-08-15)

- 원천대장 `차종마스터!A7122:AD7124`에 국내 공식 판매 3개 트림을 신규 영구 코드로 추가했다.
  - `Cooper SE Classic`
  - `Cooper SE Electric`
  - `Cooper SE GEN Z E Edition` (국내 150대 한정)
- 공통 코드: `mf-054.md-001.sm-f56__mini-electric-32-6`, 트림 키 `v01::t01~t03`.
- BMW 그룹 코리아 국내 출시자료와 MINI 코리아 제품 브로슈어를 교차했다. 배터리는 공식 브로슈어의 고전압 배터리 **총 용량 32.6kWh** 기준이며 FWD·4인승으로 기록했다.
- 공식 판매 종료 월은 확인되지 않아 생산 종료를 공란으로 유지했다. 기존 `3세대` 비트림 행은 영구키 의미를 변경하지 않고 차단 상태로 보존했다.
- Google 원천대장 재조회 결과 3행 × 30필드, 숫자형 필드, Noto Sans KR 9pt 서식, 공식 근거 하이퍼링크가 모두 입력값과 일치했다.
- 로컬 산출물: 총 4,870행 / 자동 2,913 / 수동 334 / 차단 1,623.
- 검증 결과:
  - `sim-vehicle-trim-master.mts`: PASS
  - `sim-vehicle-trim-key-contract.mts`: PASS
  - `npm run check:fonts`: PASS (드리프트 0)
  - `git diff --check`: 오류 없음 (기존 LF→CRLF 경고만 존재)

## 43. Genesis GV80 JX1 초기형 종료 및 2023 부분변경 현행 코드 분리 (2026-08-15)

- 기존 GV80 JX1 5개 행이 2020-01 초기형 사양인 채 `현재`로 남아 부분변경 차량과 자동 혼합되고, 엔진·구동별 인승도 하나씩만 표현된 문제를 확인했다.
- 원천대장 `4096~4100`행의 영구키·파워트레인·기존 인승 의미는 유지하면서 `중고차`, 생산종료 `2023-09`, 연식 `2020~2023`으로 초기형 구간을 닫았다.
- 원천대장 `차종마스터!A7156:AD7167`에 일반형 GV80 부분변경 현행 12조합을 신규 영구 코드로 추가했다.
  - 가솔린 2.5T·3.5T × 2WD·AWD × 5·6·7인승
  - 생산시작 `2023-09`, 연식시작 `2024`, 개발코드 `JX1`
- GV80 쿠페는 기존 `JX1C` 별도 코드가 있어 이번 범위에서 변경하지 않았다.
- 제네시스 공식 2023-09 부분변경 공개자료, 현행 GV80 제원, 2026 GV80 공식 출시자료를 교차했다. 현행 제원에서 두 엔진·2WD/AWD·5/6/7인승 지원을 확인했다.
- Google 재조회로 기존 5행의 영구키 보존과 `Q:S = 2023-09 | 2020 | 2023`, 신규 12행의 30필드·숫자형 순번/배기량/인승·공식 URL 하이퍼링크를 확인했다.
- 로컬 산출물은 총 4,913행 / 자동 2,897 / 수동 377 / 차단 1,639이며, 신규 GV80 부분변경 코드는 12개다.
- 검증 결과:
  - `build-current-rental-coverage-additions.mts --sync-local`: PASS (누적 116행)
  - `sim-vehicle-trim-master.mts`: PASS
  - `sim-vehicle-trim-key-contract.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS (드리프트 0)
  - `git diff --check`: 오류 없음 (기존 LF→CRLF 경고만 존재)
  - `npx tsc --noEmit`: **FAIL** — 작업 범위 밖 기존 변경인 `app/erp5/page.tsx:224`의 `BadgeCheck` 미import (`TS2304`). 이번 차종마스터 변경과 무관하며 사용자 변경을 보존하기 위해 수정하지 않았다.
## 35. 푸조 e-208 2세대 P21 국내 연식·트림 분리 보강 (2026-08-15)

- 원천대장 `차종마스터!A7125:AD7128`에 국내 공식 유통 4개 조합을 신규 영구 코드로 추가했다.
  - 초기 국내형 50kWh: Allure, GT Line (`2020-07~2022-08`)
  - 효율 개선 2022년식 50kWh: Allure, GT (`2022-09~`, 공식 종료 월 공란)
- 영구 코드:
  - `mf-021.md-022.sm-p21__e208-50k-launch`
  - `mf-021.md-022.sm-p21__e208-50k-2022`
- 푸조 코리아 2020 국내 출시자료, 스텔란티스 코리아 2022년식 출시자료, 2024 국내 가격 조정 자료를 교차했다. 공통 제원은 50kWh(2022년식 공식 표기 120Ah), 100kW 전기모터, FWD, 5인승이다.
- 배터리 용량이 같아도 효율 개선 전후의 생산 경계와 `GT Line → GT` 트림명 차이를 합치지 않았다. 기존 배터리·구동·기간 미구분 3행은 영구키 의미를 변경하지 않고 차단 상태로 보존했다.
- Google 원천대장 재조회 결과 4행 × 30필드, 숫자형 배터리 값, Noto Sans KR 9pt, 공식 근거 하이퍼링크가 모두 입력값과 일치했다.
- 로컬 산출물: 총 4,874행 / 자동 2,913 / 수동 338 / 차단 1,623.
- 검증 결과:
  - `build-current-rental-coverage-additions.mts --sync-local`: PASS (누적 77행)
  - `sim-vehicle-trim-master.mts`: PASS
  - `sim-vehicle-trim-key-contract.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS (드리프트 0)
  - `git diff --check`: 오류 없음 (기존 LF→CRLF 경고만 존재)
## 36. 푸조 e-2008 2세대 P24 국내 연식·트림 보강 및 오매칭 차단 (2026-08-15)

- 기존 원천행 `6016~6017`의 `EV GT`, `EV GT Line`은 포르투갈 가격표만 근거로 국내 세부 연식이 미확정인데도 `확정/자동` 상태였다.
- 국내 공식 트림명 및 시점과 충돌하므로 두 기존 영구키는 삭제·재해석하지 않고 `제외/1차확인/blocked`로 전환했다. 원천 근거와 키는 이력 추적을 위해 보존했다.
- 원천대장 `차종마스터!A7129:AD7132`에 국내 공식 조합 4개를 새 영구 코드로 추가했다.
  - 초기 국내형 50kWh: Allure, GT Line (`2020-07~2022-08`)
  - 효율 개선 2022년식 50kWh: Allure, GT (`2022-09~`, 공식 종료 월 공란)
- 영구 코드:
  - `mf-021.md-024.sm-p24__e2008-50k-launch`
  - `mf-021.md-024.sm-p24__e2008-50k-2022`
- 푸조 코리아 2020-07-28 국내 출시자료, 스텔란티스 코리아 2022년식 출시자료·캠페인, 2024년 23년식 가격 조정 자료를 교차했다. 공통 제원은 50kWh(120Ah), 100kW, FWD, 5인승이다.
- e-208과 같은 e-CMP/50kWh 계열이지만 모델 코드와 별칭을 독립 구성해 `208 ↔ 2008` 교차매칭을 막았다.
- Google 재조회 결과 기존 2행의 차단 상태·메모와 신규 4행 × 30필드, 숫자형 배터리, 서식, 공식 하이퍼링크가 모두 입력값과 일치했다.
- 로컬 산출물: 총 4,878행 / 자동 2,911 / 수동 342 / 차단 1,625. 기존 위험 자동 후보 2개가 차단으로 이동했다.
- 검증 결과:
  - `build-current-rental-coverage-additions.mts --sync-local`: PASS (누적 81행)
  - `sim-vehicle-trim-master.mts`: PASS
  - `sim-vehicle-trim-key-contract.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS (드리프트 0)
  - `git diff --check`: 오류 없음 (기존 LF→CRLF 경고만 존재)
## 37. DS 3 크로스백 E-텐스 2022년식 국내 코드 신설 (2026-08-15)

- 기존 마스터에는 DS 3 크로스백 E-텐스가 한 행도 없어 국내 중고·구독 차량을 프리패스 코드로 지정할 수 없었다.
- 원천대장 `차종마스터!A7133:AD7133`에 2022년식 국내 공식 판매 조합을 신규 발급했다.
  - `mf-022.md-011.sm-ds3cb__ds3-crossback-e-tense-2022-50k::v01::t01`
  - Grand Chic / 50kWh / 100kW / FWD / 5인승 / 국내 인도 2022-09
- 스텔란티스 코리아 2022-08-18 국내 출시자료에서 120Ah, 100kW, Grand Chic 단일 트림과 9월 고객 인도를 확인하고, DS Automobiles 공식 글로벌 자료에서 50kWh를 교차했다.
- 2020 초기 국내형 So Chic·Grand Chic은 검색 가능한 보도 재전재만 있고 국내 공식 원문이 확보되지 않아 이번 발급에서 제외했다. 추정 코드로 범위를 부풀리지 않았다.
- `DS 3`, `DS3`, `CROSSBACK`, `E-TENSE`, `Grand Chic`을 모델 고유 별칭으로 구성해 동일 e-CMP/50kWh 계열인 푸조 e-208·e-2008과 교차매칭되지 않게 했다.
- Google 재조회 결과 30필드, 숫자형 배터리·인승, Noto Sans KR 9pt, 공식 하이퍼링크가 입력값과 일치했다.
- 로컬 산출물: 총 4,879행 / 자동 2,911 / 수동 343 / 차단 1,625.
- 검증 결과:
  - `build-current-rental-coverage-additions.mts --sync-local`: PASS (누적 82행)
  - `sim-vehicle-trim-master.mts`: PASS
  - `sim-vehicle-trim-key-contract.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS (드리프트 0)
  - `git diff --check`: 오류 없음 (기존 LF→CRLF 경고만 존재)

## 38. 상품마스터 오토플러스·손오공 가격 원자 적재 (2026-08-15)

- Google 원천대장 `상품마스터`를 `A:AW` 49열로 확장했다. 중복 헤더는 0개이며 차량 1대당 1행 구조를 유지한다.
- 기존 표준 기간 열은 기본/반납형으로 유지하고, 맨 뒤에 다음 12개 구조화 열을 추가했다.
  - 오토플러스: 18·24·36개월의 연 3만km 대여료/보증금
  - 손오공 구독: 인수형 36·48·60개월 대여료/보증금
- 총 954개 가격 원자를 입력·갱신했다: 표준/반납형 628개 + 오토플러스 3만km·손오공 인수형 326개.
- 오토플러스 98대 전부 원천과 매칭했다. 기본은 연 2만km이며 12개월은 연 3만km 기준임을 98개 12개월 대여료 셀 메모로 명시했다.
- 오토플러스 보증금은 국산 2개월치, 수입 12개월 3개월치, 수입 18개월 이상 6개월치 규칙으로 계산했다. 제조사/국산·수입 미확정은 0건이다.
- 손오공은 현재 원천과 54대를 매칭했다. 현재 원천에서 사라진 기존 4대(`116허7286`, `161하1266`, `161하1272`, `161허1171`)는 과거 데이터 보존을 위해 지우지 않았다.
- 손오공 구독 보증금은 반납형·인수형 모두 기간 연수만큼의 월 대여료로 계산했다: 36개월×3, 48개월×4, 60개월×5.
- 표본 재조회:
  - 손오공 `375어8059`: 반납형 12~60개월 및 인수형 36~60개월 대여료/보증금 일치
  - 오토플러스 국산 `48나2076`: 표준 12~36개월 및 3만km 18~36개월 일치
  - 오토플러스 수입 `282나2079`: 12개월 보증금×3, 18개월 이상×6 일치
- 이번에 적재한 오토플러스·손오공 가격 쌍 중 대여료가 있는데 보증금이 없는 건은 0건이다. 다른 공급사의 기존 보증금 미입력 값은 추정하지 않고 검수대상으로 유지했다. 이번 작업은 Google `상품마스터`와 로컬 규격·문서만 변경했으며 ERP/RTDB 운영 데이터에는 직접 쓰지 않았다.
- 검증 결과:
  - `scripts/sim-product-master-sheet.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS (드리프트 0)
  - `git diff --check`: 오류 없음 (기존 LF→CRLF 경고만 존재)

## 39. 상품마스터 관리자 검수형 열 재배치 (2026-08-15)

- 관리자 기본 동선을 시스템 코드가 아닌 원본 대조 중심으로 바꿨다.
  - 앞쪽: `차량번호 → 공급사명 → 공급사 제공정보 → 차종마스터 적용값 → 옵션 → 차량상태`
  - 뒤쪽: `정책코드 → 차종코드 → 공급사코드 → 최종갱신 → 원천`
- `공급사 제공정보`와 `차종마스터 적용값` 헤더에는 각각 원본값과 정제값의 의미를 설명하는 셀 메모를 추가했다.
- 앞 4열을 고정하고 공급사 제공정보 420px, 차종마스터 적용값 280px 등 관리자 대조에 맞게 폭을 조정했다.
- 기존 `ProductMaster` 네이티브 표 범위를 `A1:AW1000`으로 확장해 49개 열 전체가 하나의 표가 되도록 통일했다.
- 가격 열 32개를 숫자 통화형으로 지정해 Google 화면에서 천 단위 콤마가 표시되게 했다. 원본 숫자값은 문자로 변환하지 않았다.
- 시스템 관리 5열은 회색 헤더로 구분해 주 검수영역보다 시각적 우선순위를 낮췄다.
- 구조 변경 후 재조회 결과: 상품 540대, 49열, 중복 헤더 0개. 오토플러스·손오공 표본의 차량번호·공급사 원문·가격·코드 연결이 유지됐고, 차종마스터 매칭 행의 정제값도 같은 차량 행에 남아 있다.
- Google Sheets 실제 화면에서 첫 4열 고정, 원본/정제값 비교, 가격의 천 단위 콤마 표시, 헤더 잘림 여부를 확인했다.
- 검증 결과:
  - `scripts/sim-product-master-sheet.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS (드리프트 0)
  - `git diff --check`: 오류 없음 (기존 LF→CRLF 경고만 존재)
## 38. Tesla New Model Y·Model Y L 현행 코드 재분리 및 합본 자동행 차단 (2026-08-15)

- 기존 현행 Model Y 자동행 3개가 `2020-01~현재`로 합쳐져 New Model Y와 2026 Model Y L까지 2020년 차량처럼 매칭되는 문제를 확인했다.
  - `mf-087.md-004.sm-모델-y::v01::t02`
  - `mf-087.md-004.sm-모델-y::v02::t02`
  - `mf-087.md-004.sm-모델-y::v04::t01`
- 위 키는 삭제하거나 의미를 바꾸지 않고 원천대장 `5464`, `5467`, `5469`행을 `제외/1차확인/blocked`로 전환했다.
- 원천대장 `차종마스터!A7134:AD7136`에 현행 국내 조합을 실제 시점별 신규 영구 코드로 추가했다.
  - New Model Y Premium Long Range AWD: 2025-02~, AWD, 5인승
  - New Model Y Premium RWD: 2025-02~, RWD, 5인승
  - Model Y L: 2026-04~, AWD, 6인승
- Tesla 대한민국 현행 Model Y 페이지, 한국 공인연비 페이지, Tesla Korea 공식 액세서리의 적용 생산월을 교차했다.
- Tesla가 정격전압·Ah는 공개하지만 총용량 kWh는 공개하지 않으므로 배터리 필드는 추산하지 않고 공란으로 유지했다.
- Model Y L에는 `Y L`, `YL`, `6인승`, `롱휠베이스` 별칭을 요구해 일반 5인승 Model Y와의 오매칭을 억제했다.
- Google 재조회 결과 기존 3행의 차단 상태와 신규 3행 × 30필드, 생산시작·구동·인승, 공란 배터리, 서식·하이퍼링크가 모두 입력값과 일치했다.
- 로컬 산출물: 총 4,882행 / 자동 2,908 / 수동 346 / 차단 1,628. 위험 자동 후보 3개가 차단으로 이동했다.
- 검증 결과:
  - `build-current-rental-coverage-additions.mts --sync-local`: PASS (누적 85행)
  - `sim-vehicle-trim-master.mts`: PASS
  - `sim-vehicle-trim-key-contract.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS (드리프트 0)
  - `git diff --check`: 오류 없음 (기존 LF→CRLF 경고만 존재)

## 39. Tesla New Model 3 Highland 현행 코드 재분리 및 합본 자동행 차단 (2026-08-15)

- 기존 현행 Model 3 자동행 4개가 2017년부터 현재까지를 합본하거나 `Premium RWD`로 축약되어 Highland 이전·이후 차량 및 국내 공인 트림명이 혼합될 수 있음을 확인했다.
  - `mf-087.md-003.sm-모델-3::v01::t01`
  - `mf-087.md-003.sm-모델-3::v01::t02`
  - `mf-087.md-003.sm-모델-3::v02::t01`
  - `mf-087.md-003.sm-모델-3::v02::t04`
- 위 키는 삭제하거나 의미를 바꾸지 않고 원천대장 `4275`, `5470~5472`행을 `제외/1차확인/blocked`로 전환했다.
- 원천대장 `차종마스터!A7137:AD7139`에 국내 공인 현행 조합 3개를 신규 영구 코드로 추가했다.
  - Standard RWD: 2024-04~, RWD, 5인승
  - Performance AWD: 2024-04~, AWD, 5인승
  - Premium Long Range RWD: 2026-07~, RWD, 5인승
- Tesla 대한민국 현행 Model 3 페이지, 한국 공인연비 페이지, 2024-04-23 New Model 3 Performance 공식 발표, Tesla Korea 공식 액세서리의 동일모델 출시연월을 교차했다.
- 글로벌 비교표에는 `Premium AWD`가 표시되지만 한국 공인연비 목록에는 없어 국내 자동 매칭용 신규 코드를 발급하지 않았다.
- Tesla가 정격전압·Ah는 공개하지만 총용량 kWh는 공개하지 않으므로 배터리 필드는 계산하지 않고 공란으로 유지했다.
- Google 재조회 결과 기존 4행의 차단 상태·근거 메모와 신규 3행 × 30필드, 숫자형 순번·인승, 구동방식, 공란 배터리, Noto Sans KR 9pt 서식, 공식 하이퍼링크가 입력값과 일치했다.
- 로컬 산출물: 총 4,885행 / 자동 2,904 / 수동 349 / 차단 1,632. 위험 자동 후보 4개가 차단으로 이동했다.
- 검증 결과:
  - `build-current-rental-coverage-additions.mts --sync-local`: PASS (누적 88행)
  - `sim-vehicle-trim-master.mts`: PASS
  - `sim-vehicle-trim-key-contract.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS (드리프트 0)
  - `git diff --check`: 오류 없음 (기존 LF→CRLF 경고만 존재)

## 40. Tesla 신형 Model S·Model X 2023 국내 코드 및 좌석 수 분리 (2026-08-15)

- 기존 현행 Model S·Model X 자동행 4개가 AWD/Plaid를 각 모델의 구형 최초 생산시점부터 현재까지 합본해 2023년 국내 신형과 구형 차량을 혼합할 수 있음을 확인했다.
  - Model S: `mf-087.md-001.sm-모델-s::v02::t04`, `mf-087.md-001.sm-모델-s::v03::t01`
  - Model X: `mf-087.md-002.sm-모델-x::v01::t01`, `mf-087.md-002.sm-모델-x::v02::t01`
- 위 키는 삭제하거나 의미를 바꾸지 않고 원천대장 `5475`, `5478`, `5485`, `5487`행을 `제외/1차확인/blocked`로 전환했다.
- 원천대장 `차종마스터!A7140:AD7145`에 2023-03 국내 출시 신형 조합 6개를 신규 발급했다.
  - Model S AWD 5인승, Model S Plaid 5인승
  - Model X AWD 5인승·6인승·7인승, Model X Plaid 6인승
- Tesla Korea 2023-03-30 국내 공식 출시자료, 2023-06-16 첫 인도자료, 현행 한국 공인연비를 교차했다. Model X 좌석 수는 공인연비에 기재된 AWD 5·6·7인승과 Plaid 6인승만 발급했다.
- 배터리는 정격전압·Ah만 공개되고 총용량 kWh는 공식 미공개이므로 계산하지 않고 공란으로 유지했다.
- Google 재조회 결과 기존 4행의 차단 상태·근거 메모와 신규 6행 × 30필드, 숫자형 순번·인승, 구동방식, 공란 배터리, Noto Sans KR 9pt, 공식 하이퍼링크가 입력값과 일치했다.
- 로컬 산출물: 총 4,891행 / 자동 2,900 / 수동 355 / 차단 1,636. 위험 자동 후보 4개가 차단으로 이동했다.
- 검증 결과:
  - `build-current-rental-coverage-additions.mts --sync-local`: PASS (누적 94행)
  - `sim-vehicle-trim-master.mts`: PASS
  - `sim-vehicle-trim-key-contract.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS (드리프트 0)
  - `git diff --check`: 오류 없음 (기존 LF→CRLF 경고만 존재)

## 41. Tesla Cybertruck 국내형 2025 코드 재발급 및 123kWh 추정값 차단 (2026-08-15)

- 기존 Cybertruck 자동행 2개가 글로벌 생산 시작 `2023-11`을 국내 현행 시작으로 사용했고, 기본 트림에는 Tesla 국내 공식 페이지가 공개하지 않은 배터리 총용량 `123kWh`까지 확정되어 있었다.
  - `mf-087.md-005.sm-사이버트럭::v01::t01`
  - `mf-087.md-005.sm-사이버트럭::v02::t01`
- 기존 키는 삭제하거나 의미를 바꾸지 않고 원천대장 `5488~5489`행을 `제외/1차확인/blocked`로 전환했다.
- 원천대장 `차종마스터!A7146:AD7147`에 국내 현행 조합을 신규 영구 코드로 발급했다.
  - Premium AWD / AWD / 5인승 / 2025-11~ / 배터리 공란
  - Cyberbeast / AWD / 5인승 / 2025-11~ / 배터리 공란
- Tesla 대한민국 Cybertruck 현행 페이지에서 국내 트림·구동·인승을 확인하고, Tesla Korea 공식 액세서리의 동일모델 출시연월 `2025/11`을 교차했다.
- 총용량 kWh는 공식 국내 미공개이므로 해외 추정값이나 전압·Ah 환산값을 신규 코드에 옮기지 않았다.
- Google 재조회 결과 기존 2행의 차단 상태·근거 메모와 신규 2행 × 30필드, 숫자형 순번·인승, 생산시작, 공란 배터리, Noto Sans KR 9pt, 공식 하이퍼링크가 입력값과 일치했다.
- 로컬 산출물: 총 4,893행 / 자동 2,898 / 수동 357 / 차단 1,638. 위험 자동 후보 2개가 차단으로 이동했다.
- 검증 결과:
  - `build-current-rental-coverage-additions.mts --sync-local`: PASS (누적 96행)
  - `sim-vehicle-trim-master.mts`: PASS
  - `sim-vehicle-trim-key-contract.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS (드리프트 0)
  - `git diff --check`: 오류 없음 (기존 LF→CRLF 경고만 존재)

## 42. Genesis G80 RG3 초기형 종료 및 2023 부분변경 현행 코드 분리 (2026-08-15)

- 기존 G80 RG3 내연기관 행이 2020-03 초기형부터 현재까지 이어져 2023-12 부분변경 차량과 자동 혼합되고, 공식 부분변경 엔진 라인업에서 빠진 디젤 2.2도 현행 신차로 남아 있었다.
- 원천대장 `4069~4074`행의 초기형 가솔린 조합은 영구키·트림 의미를 유지하면서 `중고차`, 생산종료 `2023-12`, 연식 `2020~2023`으로 기간을 닫았다.
- 원천대장 `4075`행의 디젤 2.2 AWD는 정확한 공식 판매 종료월이 추가 확정되기 전까지 `제외/1차확인/중고차/blocked`로 전환했다.
- 원천대장 `차종마스터!A7148:AD7155`에 2023-12 부분변경 현행 조합 8개를 신규 영구 코드로 발급했다.
  - 가솔린 2.5T 2WD·AWD × 기본형·스포츠 패키지
  - 가솔린 3.5T 2WD·AWD × 기본형·스포츠 패키지
- 제네시스 2023-12-26 공식 부분변경 출시자료에서 2,497cc·3,470cc, RWD·AWD, 스포츠 패키지를 확인하고 2026 G80 공식 출시자료로 두 엔진과 스포츠 패키지의 현행 운영을 교차했다.
- Google 재조회 중 초기형의 `연식시작` 셀이 쓰기 마스크 영향으로 비워진 것을 발견해 6행 모두 `2020`으로 즉시 복구하고 `Q:S`를 다시 확인했다.
- 신규 8행은 30필드, 숫자형 순번·배기량·표시배기량·인승, 터보·구동방식, Noto Sans KR 9pt, 공식 하이퍼링크가 입력값과 일치했다.
- 로컬 산출물: 총 4,901행 / 자동 2,897 / 수동 365 / 차단 1,639. 초기형 가솔린 자동 코드는 기간 한정 상태로 보존하고 디젤 위험 자동 후보 1개만 차단했다.
- 검증 결과:
  - `build-current-rental-coverage-additions.mts --sync-local`: PASS (누적 104행)
  - `sim-vehicle-trim-master.mts`: PASS
  - `sim-vehicle-trim-key-contract.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS (드리프트 0)
  - `git diff --check`: 오류 없음 (기존 LF→CRLF 경고만 존재)

## 44. Genesis GV70 JK1 초기형 종료 및 2024 부분변경 현행 코드 분리 (2026-08-15)

- 기존 GV70 JK1 내연기관 5개 행이 2020-12 초기형부터 현재까지 열려 있어 2024-05 부분변경 차량과 자동 혼합됐고, 현행 라인업에 없는 디젤 2.2도 신차 자동 코드로 남아 있었다.
- 원천대장 `4102`, `4103`, `4106`행의 초기형 가솔린 코드는 영구키·파워트레인 의미를 유지하면서 `중고차`, 생산종료 `2024-04`, 연식 `2021~2024`로 기간을 닫았다.
- 원천대장 `4104~4105`행의 디젤 2.2 2WD·AWD는 공식 판매 종료월을 안전하게 특정할 자료가 부족해 원래 의미와 기간값을 보존하고 `제외/1차확인/중고차/blocked`로 전환했다.
- 원천대장 `차종마스터!A7168:AD7175`에 2024-05 부분변경 현행 조합 8개를 신규 영구 코드로 발급했다.
  - 가솔린 2.5T·3.5T × 2WD·AWD × 기본형·스포츠 패키지
- 제네시스 공식 2024-05-08 부분변경 출시자료와 현행 GV70 제원을 교차해 두 가솔린 엔진, 2WD·AWD, 스포츠 패키지 및 5인승을 확인했다.
- Google 재조회로 기존 5행의 영구키·파워트레인 보존, 가솔린 `Q:S = 2024-04 | 2021 | 2024`, 디젤 차단 상태, 신규 8행의 30필드·숫자형 필드·공식 하이퍼링크를 확인했다.
- 로컬 산출물: 총 4,921행 / 자동 2,895 / 수동 385 / 차단 1,641. 위험한 디젤 자동 후보 2개를 차단하고 현행 부분변경 8개를 수동 검증 후보로 추가했다.
- 검증 결과:
  - `build-current-rental-coverage-additions.mts --sync-local`: PASS (누적 124행)
  - `sim-vehicle-trim-master.mts`: PASS
  - `sim-vehicle-trim-key-contract.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS (드리프트 0)
  - `git diff --check`: 오류 없음 (기존 LF→CRLF 경고만 존재)

## 45. Genesis G70 2020 부분변경 종료 및 2023·2026 현행 코드 분리 (2026-08-15)

- 기존 더 뉴 G70 IK 8개 행이 2020-10부터 모두 현재까지 열려 있었고, 공식적으로 2023-05 신규 도입된 가솔린 2.5T까지 2020년부터 존재한 것으로 기록돼 세대·엔진 시기를 잘못 흡수했다.
- 원천대장 `4124~4131` 중 실제 2020 부분변경 엔진인 2.0T·3.3T·디젤 6개 조합은 영구키·엔진·구동 의미를 유지하면서 `중고차`, 생산종료 `2023-04`, 연식 `2020~2023`으로 닫았다.
- 같은 범위의 가솔린 2.5T 2WD·4WD 두 행은 잘못된 2020 시작값을 가진 자동 후보이므로 원래 영구키 의미는 보존하고 `제외/1차확인/blocked`로 전환했다.
- 원천대장 `차종마스터!A7176:AD7183`에 2023-05 이후 현행 G70 세단 8조합을 신규 코드로 발급했다.
  - 가솔린 2.5T·3.3T × 2WD·AWD × 기본형·스포츠 패키지
- `A7184:AD7185`에는 2026-01 출시 G70 그래파이트 에디션의 가솔린 3.3T 2WD·AWD를 별도 코드로 추가했다.
- 제네시스 공식 2023 G70 출시자료에서 2.5T 신규 도입과 2.5T·3.3T 라인업을, 현행 공식 제원에서 두 엔진의 2WD·AWD를, 2026 공식 자료에서 그래파이트 에디션의 3.3T 단일 파워트레인을 교차했다.
- Google 재조회로 기존 8행의 영구키 보존과 기간·차단 상태, 신규 10행의 30필드·숫자형 필드·구동·공식 하이퍼링크를 확인했다.
- 로컬 산출물: 총 4,931행 / 자동 2,893 / 수동 395 / 차단 1,643. 잘못된 2.5T 자동 후보 2개를 차단하고 현행 10개 코드를 추가했다.
- 검증 결과:
  - `build-current-rental-coverage-additions.mts --sync-local`: PASS (누적 134행)
  - `sim-vehicle-trim-master.mts`: PASS
  - `sim-vehicle-trim-key-contract.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS (드리프트 0)
  - `git diff --check`: 오류 없음 (기존 LF→CRLF 경고만 존재)

## 46. Genesis G70 슈팅 브레이크 2022·2023 엔진 시기 분리 (2026-08-15)

- 기존 슈팅 브레이크 4개 행이 모두 2022-07부터 현재까지 열려 있어 2022 출시형 2.0T와 2023-05 신규 도입된 2.5T의 시기가 혼합됐다.
- 제네시스 공식 2022-06-27 국내 출시자료에서 초기형이 가솔린 2.0 터보 단일 엔진이고 기본·스포츠 모델을 운영했음을 확인했다.
- 원천대장 `4120`, `4122`행의 2.0T 4WD·2WD는 영구키와 구동 의미를 유지하면서 `중고차`, 생산종료 `2023-04`, 연식 `2022`로 초기형 기간을 닫았다.
- `4121`, `4123`행의 2.5T는 공식 도입보다 이른 2022-07 시작값을 가져 자동 매칭에서 차단하고 원래 영구키 의미는 보존했다.
- 원천대장 `차종마스터!A7186:AD7189`에 2023-05 이후 현행 가솔린 2.5T의 2WD·AWD × 기본형·스포츠 모델 4개를 신규 영구코드로 추가했다.
- 공식 2023 G70·슈팅 브레이크 출시자료와 현행 슈팅 브레이크 제원으로 2.5T 단일 엔진, 2WD·AWD 및 5인승을 교차했다.
- Google 재조회로 기존 4행의 영구키·엔진·구동 보존과 기간·차단 상태, 신규 4행의 30필드·숫자형 필드·공식 하이퍼링크를 확인했다.
- 로컬 산출물: 총 4,935행 / 자동 2,891 / 수동 399 / 차단 1,645. 시기가 잘못된 2.5T 자동 후보 2개를 차단하고 현행 4개 코드를 추가했다.
- 검증 결과:
  - `build-current-rental-coverage-additions.mts --sync-local`: PASS (누적 138행)
  - `sim-vehicle-trim-master.mts`: PASS
  - `sim-vehicle-trim-key-contract.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS (드리프트 0)
  - `git diff --check`: 오류 없음 (기존 LF→CRLF 경고만 존재)

## 47. Genesis GV60 퍼포먼스·마그마 고성능 코드 보강 (2026-08-15)

- 기존 GV60은 77.4kWh 초기형과 84kWh 부분변경의 스탠다드 2WD·AWD 코드만 존재해, 같은 AWD이지만 모터 출력과 가격이 다른 퍼포먼스 모델 및 독립 고성능 모델 마그마를 구분할 수 없었다.
- 제네시스 공식 2021 GV60 발표자료에서 스탠다드 후륜·스탠다드 사륜·퍼포먼스 3개 모델과 공통 77.4kWh 배터리를 확인했다.
- 공식 2025 부분변경 출시자료에서 스탠다드 2WD·스탠다드 AWD·퍼포먼스 AWD, 공통 84kWh 배터리와 각 모델의 별도 판매가격을 확인했다.
- 원천대장 `차종마스터!A7190:AD7191`에 초기형 퍼포먼스 AWD 77.4kWh와 부분변경 퍼포먼스 AWD 84kWh를 시기별 신규 영구코드로 추가했다.
- `A7192:AD7192`에는 2026-01-13 국내 출시 GV60 마그마를 84kWh·AWD 독립 모델 코드로 추가해 일반 퍼포먼스와 분리했다.
- 기존 스탠다드 코드는 차량 의미와 기간이 정확해 변경하지 않았다.
- Google 재조회로 신규 3행의 30필드, 숫자형 순번·인승·배터리, 생산기간, AWD, 공식 하이퍼링크를 확인했다.
- 로컬 산출물: 총 4,938행 / 자동 2,891 / 수동 402 / 차단 1,645.
- 검증 결과:
  - `build-current-rental-coverage-additions.mts --sync-local`: PASS (누적 141행)
  - `sim-vehicle-trim-master.mts`: PASS
  - `sim-vehicle-trim-key-contract.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS (드리프트 0)
  - `git diff --check`: 오류 없음 (기존 LF→CRLF 경고만 존재)

## 48. 상품마스터 공급사 대조열 정리 및 공급사 데이터 매뉴얼 신설 (2026-08-15)

- `ERP4 차종마스터 원천대장`의 `상품마스터` 앞쪽을
  `차량번호 → 공급사명 → 공급사 입력 차명 → 차종마스터 적용값 → 검증상태 → 검수사유`로 정리했다.
- 기존 `공급사 제공정보` 540건은 삭제하지 않고 맨 뒤 숨김 열 `공급사 원문보존`으로 이동했다.
  앞쪽 `공급사 입력 차명`은 긴 원문에서 공급사가 실제로 쓴 차명만 추출했다.
- 표 열 타입도 새 위치에 맞췄다. 검증상태·차량상태·관리상태만 드롭다운이고 검수사유·분류는 일반 텍스트다.
  조건색 역시 검증 E열, 검수사유 F열, 차량상태 H열, 관리상태 J열로 재배치했다.
- `공급사 데이터 매뉴얼` 탭을 새로 만들고 22개 공급사/연동유형을 기록했다.
  회사별 정본 원천, 읽는 탭·우선순위, 차명 조합, 실제 제공정보, 가격·보증금 규칙,
  상태·제외 규칙, 상품마스터 반영 위치, 현재 누락과 원본 링크를 한 행에서 확인할 수 있다.
- 손오공은 렌트와 구독을 별도 행으로 나눴고, 오토플러스 2만km/12개월 3만km 예외,
  이안카 재렌트 우선, 아이언 홈페이지 정본, 아이카 구주소 충돌, 렌트존 코드 충돌을 명시했다.
- Google Sheet 재조회 결과: 상품 540대, 차량번호 중복 0, 공급사 입력 차명 누락 0,
  원문보존 누락 0, 차종마스터 미매칭 191, 미확정인데 검수사유 없는 행 0이다.
- 브라우저 시각 검수에서 매뉴얼의 제목·요약·필터·상태색·줄바꿈과 상품마스터의 고정 4열,
  차명 대조, 미매칭 강조, 검수사유 일반 텍스트 표시를 확인했다.
- 검증 결과:
  - `npx tsx scripts/sim-product-master-sheet.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS (드리프트 0)

## 49. AI 최초 1회 매칭과 차량번호별 대여료 변경감지 분리 (2026-08-15)

- 공급사 원문을 시스템 규칙으로 매일 재해석하지 않고, 신규 차량번호에 한해 AI가 최초 1회
  차종마스터와 매칭한 뒤 관리자가 확정하는 것으로 규격을 명시했다.
- 확정 후 `공급사 입력 차명·차종마스터 적용값·옵션·분류·차종코드·공급사 원문보존`은 잠금 영역이다.
  공급사 문구가 달라져도 시스템이 자동 재매칭하거나 덮어쓰지 않는다.
- 이후 가격 감지기는 차량번호를 키로 표준 기간·3만km 변형·인수형의 `대여료` 숫자 원자만
  이전 상품마스터 값과 비교한다. 신규 차량번호만 다시 AI 매칭 대기열로 보낸다.
- 차량상태는 재고 노출 판단용 별도 동적 값으로 유지하되 차종 재매칭의 입력으로 사용하지 않는다.
  보증금·정책코드 역시 별도 운영값이며 AI 매칭 잠금과 분리했다.
- Google Sheet `공급사 데이터 매뉴얼`의 요약, 가격 변경감지 열, AI 매칭 열 22행을 모두 갱신했다.
  재조회로 모든 행에 `차량번호별 기간 대여료 변경만 비교`와 `확정 후 자동 재매칭 금지`가 기록됐음을 확인했다.
- 브라우저 시각 검수에서 수정된 운영원칙 요약과 열 제목·줄바꿈·행 높이를 확인했다.
- 검증 결과:
  - `npx tsx scripts/sim-product-master-sheet.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS (드리프트 0)

## 48. 제네시스 G90·롱휠베이스·Black 현행 코드 보강 (2026-08-15)

- 사용자 원요구인 국내 렌트·구독 유통 차량의 차종마스터 완성도 향상을 기준으로 G90 RS4 계열을 공식 제네시스 자료와 전수 대조했다.
- 기존 `mf-007.md-004.sm-rs4::v01::t01`은 G90 Black을 `2021-12`, 일반 3.5T AWD로 기록해 실제 2024-03-21 출시 및 3.5T 48V e-S/C AWD 전용 구성과 충돌했다. 영구키는 보존하고 원천 시트와 로컬 산출물에서 `제외/blocked`로 전환했다.
- 원천 시트 `차종마스터` 7193–7204행에 12개 영구코드를 추가했다.
  - 일반 G90 3.5T 2WD/AWD의 누락 4인승 2개
  - 2023 G90 3.5T 48V e-S/C 2WD/AWD × 4/5인승 4개
  - G90 롱휠베이스 3.5T 48V e-S/C AWD × 4/5인승 2개
  - 2024 G90 Black 3.5T 48V e-S/C AWD × 4/5인승 2개
  - 2025 G90 롱휠베이스 Black 3.5T 48V e-S/C AWD × 4/5인승 2개
- 공식 근거:
  - 현행 G90 제원: https://www.genesis.com/kr/ko/models/luxury-sedan-genesis/g90/specs.html
  - 2023 G90: https://newsroom.genesis.com/ko-ko/%EC%A0%9C%EB%84%A4%EC%8B%9C%EC%8A%A4-%252C-2023-G90-%EC%B6%9C%EC%8B%9C/
  - G90 롱휠베이스: https://newsroom.genesis.com/ko-ko/genesis-g90-long-wheel-base-kr/
  - G90 Black: https://newsroom.genesis.com/ko-ko/%EC%99%84%EB%B2%BD%ED%95%9C-%EB%B8%94%EB%9E%99%EC%9D%98-%ED%83%84%EC%83%9D-%EC%A0%9C%EB%84%A4%EC%8B%9C%EC%8A%A4-G90-%EB%B8%94%EB%9E%99-%EC%B6%9C%EC%8B%9C/
  - G90 롱휠베이스 Black: https://newsroom.genesis.com/ko-ko/%EC%A0%9C%EB%84%A4%EC%8B%9C%EC%8A%A4-G90-%EB%A1%B1%ED%9C%A0%EB%B2%A0%EC%9D%B4%EC%8A%A4-%EB%B8%94%EB%9E%99-%EC%B2%AB-%EC%B6%9C%EC%8B%9C/
- 시트 재조회 결과 7204행·30열, 신규 12행의 숫자형/문자형/근거 링크 및 기존 차단 행을 확인했다.
- 로컬 산출물: 총 4,950행, 자동 2,890행, 수동 414행, 차단 1,646행. 증분 생성기는 누적 153행이다.
- 검증 결과:
  - `build-current-rental-coverage-additions.mts --sync-local`: PASS
  - `sim-vehicle-trim-master.mts`: PASS
  - `sim-vehicle-trim-key-contract.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS
  - `git diff --check`: PASS (기존 LF→CRLF 경고만 존재)

## 49. 제네시스 GV80 쿠페·쿠페 Black 현행 코드 보강 (2026-08-15)

- 기존 GV80 쿠페 2.5T/3.5T AWD 기본형은 공식 현행 제원과 일치해 유지했다.
- 기존 Black 행 2개(`mf-007.md-005.sm-jx1c::v01::t02`, `::v02::t02`)는 일반 쿠페 출시 시점인 2023-10부터 Black으로 기록되어 있으나 공식 최초 공개일은 2024-10-02이므로 영구키를 보존한 채 `제외/blocked` 처리했다.
- 원천 시트 `차종마스터` 7205–7208행에 4개 코드를 추가했다.
  - 일반 GV80 쿠페 3.5T 48V e-S/C AWD 5인승
  - 2024 GV80 쿠페 Black 2.5T AWD 5인승
  - 2024 GV80 쿠페 Black 3.5T AWD 5인승
  - 2024 GV80 쿠페 Black 3.5T 48V e-S/C AWD 5인승
- 공식 공개자료와 현행 국내 제원표에서 쿠페 3개 엔진, AWD, 5인승 및 Black 별도 모델을 교차확인했다.
- 시트 재조회 결과 7,208행·30열이며 신규 4행은 각각 30개 셀, 숫자형 순번/배기량/인승과 공식 링크가 보존되었다. 기존 오분류 2행도 `제외/1차확인` 상태를 확인했다.
- 로컬 산출물: 총 4,954행, 자동 2,888행, 수동 418행, 차단 1,648행. 증분 생성기는 누적 157행이다.
- 검증 결과:
  - `build-current-rental-coverage-additions.mts --sync-local`: PASS
  - `sim-vehicle-trim-master.mts`: PASS
  - `sim-vehicle-trim-key-contract.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS
  - `git diff --check`: PASS (기존 LF→CRLF 경고만 존재)

## 50. 제네시스 GV80 Black 누락 코드 보강 (2026-08-15)

- 일반 GV80 Black은 기존 차종마스터에 독립 코드가 없음을 확인했다. 일반 GV80 부분변경 및 GV80 쿠페 Black과 혼합하지 않고 별도 master ID를 발급했다.
- 제네시스 공식 2024-10-02 공개자료와 현행 국내 GV80 Black 제원·견적에서 2.5T/3.5T, AWD, 5/6/7인승을 확인했다.
- 원천 시트 `차종마스터` 7209–7214행에 엔진 2종 × 좌석 3종의 6개 영구코드를 추가했다.
- 시트 재조회 결과 7,214행·30열이며 6행 모두 30개 셀, 배기량 2,497/3,470과 인승 5/6/7의 숫자형, 공식 근거 하이퍼링크를 확인했다.
- 로컬 산출물: 총 4,960행, 자동 2,888행, 수동 424행, 차단 1,648행. 증분 생성기는 누적 163행이다.
- 검증 결과:
  - `build-current-rental-coverage-additions.mts --sync-local`: PASS
  - `sim-vehicle-trim-master.mts`: PASS
  - `sim-vehicle-trim-key-contract.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS
  - `git diff --check`: PASS (기존 LF→CRLF 경고만 존재)

## 51. 제네시스 G80 Black 누락 코드 보강 (2026-08-15)

- 기존 마스터에 G80 Black 독립 코드가 없음을 확인했다.
- 제네시스 공식 2025-01-08 국내 판매개시 자료와 현행 국내 제원표에서 G80 Black의 2.5T AWD 및 3.5T AWD, 5인승 구성을 확인했다.
- 원천 시트 `차종마스터` 7215–7216행에 엔진별 2개 영구코드를 추가했다.
- 시트 재조회 결과 7,216행·30열이며 2행 모두 30개 셀, 배기량 2,497/3,470 숫자형과 공식 근거 하이퍼링크를 확인했다.
- 로컬 산출물: 총 4,962행, 자동 2,888행, 수동 426행, 차단 1,648행. 증분 생성기는 누적 165행이다.
- 검증 결과:
  - `build-current-rental-coverage-additions.mts --sync-local`: PASS
  - `sim-vehicle-trim-master.mts`: PASS
  - `sim-vehicle-trim-key-contract.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS
  - `git diff --check`: PASS (기존 LF→CRLF 경고만 존재)

## 52. 기아 카니발 X-Line 시점·좌석 교정 (2026-08-15)

- 기존 더 뉴 카니발 X라인 3행이 모두 2023-11부터 현행으로 기록되어 있었으나, 당시 공식 가격표 라인업은 그래비티였고 현행 공식 2026 가격표의 X-Line은 7인승 시그니처 기반 전용 트림이다.
- 기존 가솔린 7인승·하이브리드 7인승은 생산시점 충돌, 하이브리드 9인승은 좌석 구성 충돌로 영구키를 보존한 채 `제외/blocked` 처리했다.
- 기아 공식 2026-05-01 국내 가격표·카탈로그를 기준으로 7인승 X-Line의 3.5 가솔린 2WD와 1.6 터보 하이브리드 2WD를 신규 발급했다.
- 원천 시트 `차종마스터` 7217–7218행에 2개 코드를 추가했다.
- 시트 재조회 결과 7,218행·30열, 신규 2행의 30개 셀·7인승 숫자형·공식 PDF 링크와 기존 오분류 3행의 차단 상태를 확인했다.
- 로컬 산출물: 총 4,964행, 자동 2,885행, 수동 428행, 차단 1,651행. 증분 생성기는 누적 167행이다.
- 검증 결과:
  - `build-current-rental-coverage-additions.mts --sync-local`: PASS
  - `sim-vehicle-trim-master.mts`: PASS
  - `sim-vehicle-trim-key-contract.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS
  - `git diff --check`: PASS (기존 LF→CRLF 경고만 존재)

## 53. 기아 카니발 하이루프 신규 라인업 보강 (2026-08-15)

- 기아가 2026-06-12 공개한 카니발 하이루프는 일반 카니발보다 전고를 270mm 높인 9인승 양산 라인업이며, 기존 카니발 하이리무진과 구분되는 독립 코드가 필요함을 확인했다.
- 기아 공식 국내 출시 자료와 현행 가격 페이지를 기준으로 3.5 가솔린 2WD·1.6 터보 하이브리드 2WD, 각 노블레스·시그니처 조합을 확인했다.
- 원천 시트 `차종마스터` 7219–7222행에 파워트레인 2종 × 트림 2종의 4개 영구코드를 추가했다.
- 시트 재조회 결과 7,222행·30열이며 4행 모두 30개 셀, 배기량 3,470/1,598과 9인승의 숫자형, 공식 근거 하이퍼링크를 확인했다.
- 로컬 산출물: 총 4,968행, 자동 2,885행, 수동 432행, 차단 1,651행. 증분 생성기는 누적 171행이다.
- 검증 결과:
  - `build-current-rental-coverage-additions.mts --sync-local`: PASS
  - `sim-vehicle-trim-master.mts`: PASS
  - `sim-vehicle-trim-key-contract.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS
  - `git diff --check`: PASS (기존 LF→CRLF 경고만 존재)

## 54. 기아 K5 베스트 셀렉션 신설시점 교정 (2026-08-15)

- 쏘렌토·스포티지·K5 현행 코드 분포를 감사한 결과 쏘렌토와 스포티지는 현행 파워트레인 범위가 분리돼 있었으나, K5 베스트 셀렉션 3행은 2023-11부터 현행으로 자동매칭되고 있었다.
- 기아 공식 2025-06-19 The 2026 K5 출시자료에서 베스트 셀렉션이 이 연식변경의 신규 트림임을 확인했다.
- 기존 가솔린 2.0·가솔린 1.6T·하이브리드 2.0 베스트 셀렉션 영구키는 의미와 이력을 보존한 채 원천 시트 216·221·227행에서 `제외/blocked` 처리했다.
- 원천 시트 `차종마스터` 7223–7225행에 실제 출시시점인 2025-06부터 시작하는 파워트레인별 3개 신규 영구코드를 추가했다.
- 시트 재조회 결과 7,225행·30열이며 신규 3행의 30개 셀, 배기량 1,999/1,598과 5인승의 숫자형, 공식 출시자료 하이퍼링크 및 기존 3행의 차단 상태를 확인했다.
- 로컬 산출물: 총 4,971행, 자동 2,882행, 수동 435행, 차단 1,654행. 증분 생성기는 누적 174행이다.
- 검증 결과:
  - `build-current-rental-coverage-additions.mts --sync-local`: PASS
  - `sim-vehicle-trim-master.mts`: PASS
  - `sim-vehicle-trim-key-contract.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS
  - `git diff --check`: PASS (기존 LF→CRLF 경고만 존재)

## 55. 기아 K8 베스트 셀렉션 신설시점·누락 조합 교정 (2026-08-15)

- K8 현행 트림을 감사한 결과 2.5 가솔린·1.6 하이브리드 베스트 셀렉션은 2024-08부터 자동매칭되고 있었고, 3.5 가솔린 베스트 셀렉션은 누락돼 있었다.
- 기아 공식 2025-06-19 The 2026 K8 출시자료에서 베스트 셀렉션이 이 연식변경의 신규 트림이며 2.5 가솔린·3.5 가솔린·1.6 터보 하이브리드에 운영됨을 확인했다.
- 기존 2.5 가솔린·1.6 하이브리드 베스트 셀렉션 영구키는 원천 시트 698·710행에서 의미와 이력을 보존한 채 `제외/blocked` 처리했다.
- 원천 시트 `차종마스터` 7226–7228행에 실제 출시시점인 2025-06부터 시작하는 파워트레인별 3개 신규 영구코드를 추가했다.
- 시트 재조회 결과 7,228행·30열이며 신규 3행의 30개 셀, 배기량 2,497/3,470/1,598과 5인승의 숫자형, 2WD·터보 표기, 공식 출시자료 하이퍼링크 및 기존 2행의 차단 상태를 확인했다.
- 로컬 산출물: 총 4,974행, 자동 2,880행, 수동 438행, 차단 1,656행. 증분 생성기는 누적 177행이다.
- 검증 결과:
  - `build-current-rental-coverage-additions.mts --sync-local`: PASS
  - `sim-vehicle-trim-master.mts`: PASS
  - `sim-vehicle-trim-key-contract.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS
  - `git diff --check`: PASS (기존 LF→CRLF 경고만 존재)

## 56. 기아 EV3·EV4 4WD 시점 교정 및 GT 누락 보강 (2026-08-15)

- 타스만·EV3·EV4·PV5 신차군을 감사한 결과 타스만과 PV5는 주요 차체·파워트레인이 존재했으나, EV3·EV4 롱레인지 4WD 6행이 각 차종 최초 출시 시점부터 자동매칭되고 EV3 GT·EV4 GT는 누락돼 있었다.
- 기아 공식 2026-02-02 국내 출시자료에서 EV3·EV4 롱레인지 4WD와 두 GT 모델이 이 시점에 신규 추가됐고, GT에는 81.4kWh 배터리와 전륜 145kW·후륜 70kW 듀얼 모터가 적용됨을 확인했다.
- 기존 EV4 4WD 3행(3212–3214)과 EV3 4WD 3행(3309–3311)은 영구키 의미와 이력을 보존한 채 `제외/blocked` 처리했다.
- 원천 시트 `차종마스터` 7229–7236행에 EV3·EV4의 에어·어스·GT-Line 롱레인지 4WD 6개와 EV3 GT·EV4 GT 2개, 총 8개 신규 영구코드를 추가했다.
- 시트 재조회 결과 7,236행·30열이며 신규 8행의 30개 셀, 81.4kWh·4WD·5인승 숫자/구동 표기, 공식 출시자료 하이퍼링크와 기존 6행의 차단 상태를 확인했다.
- 로컬 산출물: 총 4,982행, 자동 2,874행, 수동 446행, 차단 1,662행. 증분 생성기는 누적 185행이다.
- 검증 결과:
  - `build-current-rental-coverage-additions.mts --sync-local`: PASS
  - `sim-vehicle-trim-master.mts`: PASS
  - `sim-vehicle-trim-key-contract.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS
  - `git diff --check`: PASS (기존 LF→CRLF 경고만 존재)

## 57. 기아 EV5 국내 출시시점 재구성 및 GT 보강 (2026-08-15)

- EV5 현행 9행이 81.4kWh 2WD 최초형, 60.3kWh 스탠다드, 81.4kWh 4WD를 모두 2026-05부터로 기록하고 EV5 GT는 누락한 상태임을 확인했다.
- 기아 공식 2025-07-08 출시자료에서 국내 최초형 81.4kWh 2WD를, 2026-02-02 국내 출시자료에서 60.3kWh 스탠다드·롱레인지 4WD·EV5 GT 추가 시점을 확인했다.
- 기존 EV5 9행(3215–3223)은 배터리·구동·트림 영구키 의미와 이력을 보존한 채 `제외/blocked` 처리했다.
- 원천 시트 `차종마스터` 7237–7246행에 81.4kWh 2WD 3개, 60.3kWh 2WD 3개, 81.4kWh 4WD 3개, EV5 GT 1개 등 총 10개 시점별 신규 영구코드를 추가했다.
- EV5 GT는 81.4kWh와 전륜 155kW·후륜 70kW 듀얼 모터 4WD를 적용한 독립 고성능 라인업으로 GT-Line과 분리했다.
- 시트 재조회 결과 7,246행·30열이며 신규 10행의 30개 셀, 60.3/81.4kWh·2WD/4WD·5인승 숫자/구동 표기, 공식 출시자료 하이퍼링크와 기존 9행의 차단 상태를 확인했다.
- 로컬 산출물: 총 4,992행, 자동 2,865행, 수동 456행, 차단 1,671행. 증분 생성기는 누적 195행이다.
- 검증 결과:
  - `build-current-rental-coverage-additions.mts --sync-local`: PASS
  - `sim-vehicle-trim-master.mts`: PASS
  - `sim-vehicle-trim-key-contract.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS
  - `git diff --check`: PASS (기존 LF→CRLF 경고만 존재)

## 58. 기아 EV9 2026 스탠다드·라이트 시점 및 좌석 조합 교정 (2026-08-15)

- EV9 현행 코드를 감사한 결과 76.1kWh 스탠다드와 라이트 트림이 2023-06 최초 출시 시점부터 자동매칭되고 있었다.
- 기아 공식 2026-02-02 국내 연식변경 출시자료와 현행 가격표에서 76.1kWh 스탠다드 및 라이트가 2026 EV9에서 도입됐고, 라이트·에어·어스와 6/7인승 선택이 운영됨을 확인했다.
- 기존 76.1kWh 2WD 4행(3323–3326), 99.8kWh 2WD 라이트 2행(3327–3328), 99.8kWh 4WD 라이트 2행(3333–3334)은 영구키 의미와 이력을 보존한 채 `제외/blocked` 처리했다.
- 원천 시트 `차종마스터` 7247–7256행에 76.1kWh 2WD 라이트·에어·어스 × 6/7인승 6개와 99.8kWh 라이트 2WD/4WD × 6/7인승 4개, 총 10개 신규 영구코드를 추가했다.
- 시트 재조회 결과 7,256행·30열이며 신규 10행의 30개 셀, 76.1/99.8kWh·2WD/4WD·6/7인승 숫자/구동 표기, 공식 출시자료 하이퍼링크와 기존 8행의 차단 상태를 확인했다.
- 로컬 산출물: 총 5,002행, 자동 2,857행, 수동 466행, 차단 1,679행. 증분 생성기는 누적 205행이다.
- 검증 결과:
  - `build-current-rental-coverage-additions.mts --sync-local`: PASS
  - `sim-vehicle-trim-master.mts`: PASS
  - `sim-vehicle-trim-key-contract.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS
  - `git diff --check`: PASS (기존 LF→CRLF 경고만 존재)

## 59. 기아 더 뉴 EV6 GT 누락 코드 보강 (2026-08-15)

- EV6 최초형·부분변경·GT 코드를 감사한 결과 더 뉴 EV6의 63/84kWh 및 2WD/4WD 일반 라인업은 분리돼 있었으나, 84kWh 부분변경 GT가 누락돼 있었다.
- 현대자동차그룹·기아 공식 2024-11-26 국내 판매개시 자료와 현행 기아 가격표에서 더 뉴 EV6 GT의 84kWh 배터리, 합산 448kW 듀얼 모터 4WD, 5인승 구성을 확인했다.
- 기존 77.4kWh EV6 GT 및 일반 GT-Line과 혼합하지 않고 독립 master ID를 발급했다.
- 원천 시트 `차종마스터` 7257행에 더 뉴 EV6 GT 신규 영구코드 1개를 추가했다.
- 시트 재조회 결과 7,257행·30열이며 30개 셀, 84kWh·4WD·5인승 숫자/구동 표기와 공식 판매개시 자료 하이퍼링크를 확인했다.
- 로컬 산출물: 총 5,003행, 자동 2,857행, 수동 467행, 차단 1,679행. 증분 생성기는 누적 206행이다.
- 검증 결과:
  - `build-current-rental-coverage-additions.mts --sync-local`: PASS
  - `sim-vehicle-trim-master.mts`: PASS
  - `sim-vehicle-trim-key-contract.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS
  - `git diff --check`: PASS (기존 LF→CRLF 경고만 존재)

## 56. 공급사 원본 열 매핑 매뉴얼 고정 (2026-08-15)

- `ERP4 차종마스터 원천대장`에 `공급사 열 매핑` 탭을 신설했다.
- 22개 공급사를 실제 원본 탭·페이지 단위 27행으로 분리하고 헤더 행, 차량번호, 상태, 공급사 차명,
  옵션, 기간별 대여료, 보증금, 정책·기타, 우선순위·제외, 최초 AI 매칭 입력, 이후 변경감지 범위를 기록했다.
- 오토플러스 본목록/EV 프로모션, 아이카 재고/정책, 이안카 본목록/재렌트, 손오공 렌트/구독,
  웰릭스 재고/구형/정책, 아이언 홈페이지를 각각 별도 행으로 분리했다.
- 원본 헤더를 다시 실측해 오토플러스 9/3행, 손오공 구독 2행(1행 그룹), 웰릭스 구형 3행,
  렌트존 2행 예외를 확정했다.
- 연카는 원본 주소 미등록, 스위치플랜·에코렌트카는 주소/코드 확인 필요로 표시하고 확인 전 자동 변경감지를 금지했다.
- `공급사 데이터 매뉴얼` 요약에 상세 열 위치 참조를 추가하고, 로컬 문서와 탭명 상수를 같은 규격으로 맞췄다.
- Google Sheets 재조회 결과: A1:Q31, 데이터 27행, 공급사 22개, 판정 `확정 23 / 주의 3 / 미등록 1`,
  원본 링크 수식, 4행 고정, 필터, 판정 드롭다운, 조건부 서식을 확인했다.
- Google Sheets 100% 화면 렌더링에서 제목·요약·헤더·교차행 음영·상태 색상과 탭 노출을 확인했다.
- 검증 결과:
  - `sim-product-master-sheet.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS

## 60. PV5 패신저 국내 판매 트림·시트 배열 안전성 보강 (2026-08-15)

- 사용자 원요구인 국내 렌트·구독 유통 차량 중심의 영구 차종코드 완성도를 기준으로 PV5 패신저를 재감사했다.
- 기아 국내 공식 현행 가격표에서 71.2kWh 2WD 5인승 `2-3-0` 구성의 `베이직`과 `플러스`를 확인했다. 기존 마스터에는 베이직만 있어 `플러스 2-3-0` 1행을 신규 영구코드로 원천 시트 7258행에 추가했다.
- 기존 `베이직 2-2-3`은 5인승으로 저장되어 있었으나 공식 컨셉 자료상 7인승 배열이며, `베이직 1-2-2`도 컨셉 배열일 뿐 국내 현행 가격표의 판매 트림으로 확인되지 않았다. 두 영구키는 삭제·의미변경하지 않고 원천 시트 3251~3252행에서 `제외/1차확인`으로 바꾸어 자동매칭을 차단했다.
- 원천 시트 재조회 결과 7258행은 A:AD 30셀, 숫자형 순번·인승·배터리, 공식 가격표 PDF 하이퍼링크가 정상이며 시트 행 수는 7,258행이다.
- 로컬 동기화 후 총 5,004건(자동 2,855 / 수동 468 / 차단 1,681)이다. PV5 패신저는 국내 판매 확인된 베이직 2-3-0만 자동, 새 플러스 2-3-0은 교차확인 단계의 수동, 컨셉 배열 2건은 차단 상태다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `tsc --noEmit`, `check:fonts`, `git diff --check`를 통과했다. `git diff --check`에는 기존 작업트리의 LF→CRLF 경고만 있고 공백 오류는 없다.
- 근거: 기아 국내 공식 PV5 패신저 가격표와 기아 공식 PV5 패신저 컨셉 특징 페이지. 컨셉에 존재한다는 이유만으로 국내 판매 트림으로 승격하지 않는 보수적 매칭 원칙을 적용했다.

## 61. 현대 2026 신규 트림 생산시점 분리 — 싼타페·투싼 H-Pick / 쏘나타 S (2026-08-15)

- 국내 장기렌트 핵심 차종인 싼타페·투싼·쏘나타의 현행 자동매칭 코드를 공식 2026 가격표 및 현대자동차그룹 출시 자료와 대조했다.
- 기존 마스터는 싼타페 H-Pick 12조합(가솔린/하이브리드 × 2WD/4WD × 5/6/7인승), 투싼 H Pick 3조합(가솔린 2WD, 하이브리드 2WD/4WD), 쏘나타 S 3조합(가솔린 2.0/1.6T, 하이브리드 2.0)을 각 차량의 2023년 최초 출시 시점부터 존재한 것으로 기록하고 있었다.
- 현대 공식 2026 싼타페·투싼 가격표는 현 모델 출시일을 2025-08-07로 명시하고 H-Pick을 수록한다. 현대자동차그룹 공식 2026 쏘나타 출시 자료는 S를 신규 트림으로 명시하며 2025-09 공개 시점과 일치한다.
- 잘못된 초기 생산시점의 기존 영구키 18개는 삭제하거나 의미를 바꾸지 않고 원천 시트 행 1388·1391·1400·1606·1621·1625·4290·4294·4298·4302·4306·4310·4314·4318·4322·4326·4330·4334에서 `제외/1차확인`으로 전환했다.
- 공식 출시 시점별 신규 영구코드 18개를 원천 시트 7259~7276행에 추가했다. 싼타페·투싼은 2025-08, 쏘나타는 2025-09 생산시작으로 분리했으며 원천 재조회에서 모든 행의 A:AD 30셀, 숫자형 순번·배기량·표시배기량·인승, 공식 PDF 하이퍼링크를 확인했다.
- 원천 시트는 7,276행, 로컬 차종마스터는 총 5,022건(자동 2,837 / 수동 486 / 차단 1,699)이다. 신규 18건은 교차확인 단계의 수동 배정, 기존 오시점 18건은 전부 차단 상태다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 통과했다. `git diff --check`에는 기존 작업트리의 LF→CRLF 경고만 존재한다.

## 62. 코나 H-Pick 출시시점 분리 및 Black Exterior 보강 (2026-08-15)

- 현대 공식 2024·2025 코나 가격표를 직접 대조했다. 2024 가격표에는 H-Pick이 없고, 2025 가솔린·하이브리드 가격표는 현 모델 출시일을 2025-04-23으로 명시하며 H-Pick을 별도 가격 트림으로 수록한다.
- 기존 가솔린 1.6T 2WD 및 하이브리드 H-Pick 2개 코드는 2023-01 차종 최초 출시 시점부터 존재한 것으로 기록돼 초기 차량을 잘못 매칭할 수 있었다. 영구키는 보존하고 원천 시트 1772·1787행을 `제외/1차확인`으로 전환했다.
- 2025-04 생산시작의 H-Pick 2개를 신규 영구코드로 발급했다. 같은 공식 가격표에서 인스퍼레이션과 별도 가격 행으로 제공되는 Black Exterior도 가솔린 1.6T 2WD/4WD, 가솔린 2.0 2WD, 하이브리드 1.6 2WD의 4개 영구코드로 분리했다.
- 신규 6개 코드를 원천 시트 7277~7282행에 추가했다. 재조회 결과 모든 행이 A:AD 30셀, 숫자형 순번·배기량·표시배기량·인승, 공식 가격표 PDF 하이퍼링크를 유지하며 원천 시트는 7,282행이다.
- 로컬 차종마스터는 총 5,028건(자동 2,835 / 수동 492 / 차단 1,701)이다. 신규 6건은 교차확인 단계의 수동 배정이고 기존 오시점 2건은 모두 차단 상태다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 통과했다. `git diff --check`에는 기존 작업트리의 LF→CRLF 경고만 존재한다.

## 63. 공급사별 정합성 검수 1차 — 웰릭스 (2026-08-15)

- 전체 행 수 비교를 중단하고 웰릭스 원본의 `재고`와 구형 `시트1`을 차량번호 집합으로 대조했다.
- 정본 `재고` 22대와 구형 탭 25대의 교집합은 22대였다. 구형 전용 3대는 출고불가 이력 `162호2310` 1대와
  매각 `23호7803`, `45호7183` 2대이며, 상품마스터 23대는 `정본 22 + 이력 1`로 차량번호 누락·초과가 없다.
- 정본 Q열이 빈 16대에 남아 있던 12개월 대여료를 제거하고 대여료가 빈 기간에 고아 상태로 남은 보증금 3칸을 제거했다.
- 웰릭스 정본 22대의 상태와 1·12·24·36·48·60개월 대여료·보증금을 재조회해 가격 원자 차이 0건을 확인했다.
- 차종코드 미매칭 3대는 최초 AI 매칭 대상으로 유지했고 가격 교정 과정에서 공급사 차명·적용 차명·옵션·차종코드를 변경하지 않았다.
- Google Sheet의 `공급사 데이터 매뉴얼` 웰릭스 행과 `공급사 열 매핑`의 정본/구형 행에 우선순위·제외·빈 기간 처리 규칙을 기록했다.
- 로컬 `SUPPLIER_DATA_MANUAL.md`에 공급사 한 곳씩 완료하는 8단계 절차와 웰릭스 완료 대장을 고정했다.
- Google Sheets 화면에서 상품마스터 웰릭스 행, 빈 12개월 가격칸, 공급사 열 매핑 정본/구형 행,
  공급사 데이터 매뉴얼 웰릭스 14행의 렌더링을 확인했다.
- 검증 결과:
  - `sim-product-master-sheet.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS
  - `git diff --check`: PASS (기존 LF→CRLF 경고만 존재)

## 64. 아반떼 2026 하이브리드 현행 트림 및 N Line 이력 종결 (2026-08-15)

- 현대 공식 2026 아반떼 출시 자료와 하이브리드 가격표를 대조해 2025-04 출시된 현행 하이브리드 트림에 `모던 라이트`와 단일 `N Line`이 포함됨을 확인했다.
- 현행 두 트림은 기존 코드의 의미를 바꾸지 않고 별도 마스터 ID와 영구 트림행키로 발급해 원천 시트 7283~7284행에 추가했다. 두 행 모두 A:AD 30열, 숫자형 순번·배기량·승차정원, 공식 가격표 링크를 재조회했다.
- 기존 `모던 N라인`·`인스퍼레이션 N라인` 가솔린/하이브리드 4개 코드는 과거에 실제 판매된 유효 이력이므로 제외하지 않았다. 원천 시트 1308·1310·1315·1317행의 시장상태를 중고차로, 생산종료를 2025-03, 연식종료를 2025로 닫아 영구키 의미와 이력을 보존했다.
- 원천 시트는 7,284행, 로컬 차종마스터는 총 5,030건(자동 2,835 / 수동 494 / 차단 1,701)으로 동기화됐다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 통과했다. `git diff --check`에는 기존 작업트리의 LF→CRLF 경고만 있고 공백 오류는 없다.

## 65. 구형 Tesla Model Y RWD 국내 출시 경계 보정 (2026-08-15)

- Tesla Korea 공식 보도자료에서 구형 Model Y RWD의 국내 출시일이 2023-07-14이고 5인승임을 확인했다.
- 기존 영구키 `mf-087.md-004.sm-모델-y::v02::t01`은 글로벌 생산시점 2020-01을 국내 시작월처럼 사용하고 있어 국내 렌트·구독 이력 매칭에 부정확했다. 키와 트림 의미는 유지하면서 국내 생산구분을 2023-07~2025-01, 연식 범위를 2023~2025로 보정했다.
- 종료월 2025-01은 공식 국내 RWD 출시월과 New Model Y 국내 생산구분 시작 2025-02 사이의 세대 경계를 적용한 보수적 추론이며 근거메모에 추론임을 명시했다.
- 원천 시트 5466행과 로컬 산출물을 동기화하고 A:AD 30열을 재조회해 시장상태·생산기간·연식·5인승·별칭·공식 링크가 일치함을 확인했다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 통과했다. `git diff --check`에는 기존 작업트리의 LF→CRLF 경고만 있고 공백 오류는 없다.

## 66. Tesla 리프레시·롱휠베이스 차체 구분 보강 (2026-08-15)

- Tesla 대한민국 현행 페이지를 직접 확인했다. Model 3는 `Model 3 / Premium / Performance`, Model Y는 `Model Y Premium / Model Y L`을 별도 구성으로 제공한다.
- Model 3의 2024 대규모 리프레시는 기존의 `Highland` 검색 구분을 유지한다. Model Y의 2025 대규모 리프레시 2개 행은 세대명을 `1세대 부분변경(리프레시)`로 명시하고 `Juniper/Refresh/리프레시` 별칭을 보강했다. 다만 Tesla 한국 공식 모델명에는 Juniper가 노출되지 않으므로 개발코드 칸은 비워 과도한 확정을 피했다.
- Model Y L은 단순 연식 트림으로 합치지 않았다. Tesla 한국 공식 제원의 전장 4,970mm, 확장 휠베이스, AWD, 3열 6인승을 근거로 `1세대 롱휠베이스 파생형`의 별도 마스터 ID를 유지하고 6인승·Long Wheelbase 별칭을 추가했다.
- 원천 시트 7134~7136행과 로컬 산출물을 동일하게 갱신했다. 영구 트림행키와 순번은 변경하지 않았으며 A:AD 30열을 재조회해 세대명·개발코드·별칭·근거메모 정합성을 확인했다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 통과했다. `git diff --check`에는 기존 작업트리의 LF→CRLF 경고만 있고 공백 오류는 없다.

## 67. 공급사별 정합성 검수 2차 — 손오공 렌트·구독 (2026-08-15)

- 손오공 공급사 원본 `렌트 상품 현황` 13행과 `구독 상품 현황` 45행을 차량번호로 대조했다. 중복은 `116가3396`
  1대이며 구독 탭을 우선해 현재 정본 차량번호는 57대다.
- 기존 상품마스터 58대에는 현재 원본 누락 3대와 원본부재 과거 차량 4대가 섞여 있었다. 누락된 출고불가 차량
  `17하3908`, `161하1284`, `161호1019`을 추가하고 과거 4대는 삭제하지 않고 출고불가 이력으로 전환했다.
- 추가 차량 중 어벤저 알티튜드와 스포티지 가솔린 노블레스는 확정 차종코드에 연결했다. 원본의 `스포티지 2.0 LPi
  노블레스 그래비티`는 생산시점에 맞는 정확한 마스터 트림을 확정할 수 없어 추정하지 않고 미매칭으로 유지했다.
- 상품마스터는 손오공 총 61대(`현재 57 + 이력 4`), 현재 원본 차량번호 누락 0대, 차종코드 확정 42대,
  미매칭 19대, 관리상태 운영 36대·검수필요 25대다.
- 렌트 12·24·36·48개월, 구독 반납형 12·24·36·48·60개월, 인수형 36·48·60개월의 대여료와 기간별 보증금을
  재계산·재조회해 가격 원자 차이 0건을 확인했다.
- Google Sheet의 손오공 렌트·구독 매뉴얼 행과 원본 열 매핑 행에 탭 우선순위, 출고불가/원본부재 처리,
  반납형·인수형 가격 및 보증금 계산 규칙을 각각 기록했다.
- Google Sheets 화면에서 신규 3행의 확정/미매칭 표시, 빈 12개월 가격, 24개월 가격, 손오공 렌트·구독 매뉴얼 행의
  구분 표시가 정상 렌더링되는 것을 확인했다.
- 검증 결과:
  - `sim-product-master-sheet.mts`: PASS
  - `npx tsc --noEmit`: PASS
  - `npm run check:fonts`: PASS
  - `git diff --check`: PASS (기존 LF→CRLF 경고만 존재)

## 67. 구형 Tesla Model Y Performance 국내 유통 코드 복원 (2026-08-15)

- 환경부 무공해차 통합누리집의 2023-08 및 2024 공식 보조금 차종표에서 `Model Y Performance`가 국내 보급 대상이며 리스·렌탈 추가보조금 대상임을 확인했다.
- 기존 마스터에는 구형 Model Y Performance의 안전한 활성 코드가 없어 렌트·구독 재고를 RWD·Long Range 또는 차단 코드로 오매칭할 위험이 있었다. 별도 마스터 ID와 영구 트림행키를 발급해 원천 시트 7285행에 추가했다.
- 생산기간은 공식 문서가 직접 증명하는 최초 시점 2023-08보다 앞당기지 않았고, New Model Y 생산구분 시작 직전인 2025-01까지로 보수적으로 닫았다. Tesla 공식 Model Y 매뉴얼의 Performance 좌석 구성을 근거로 AWD 5인승을 적용했다.
- 원천 행 A:AD 30열의 문자열·숫자형·공식 링크를 재조회했으며 로컬 차종마스터는 총 5,031건(자동 2,835 / 수동 495 / 차단 1,701)으로 동기화됐다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 통과했다. `git diff --check`에는 기존 작업트리의 LF→CRLF 경고만 있고 공백 오류는 없다.

## 68. 구형 Tesla Model Y Long Range·19인치 인증 변형 정리 (2026-08-15)

- 환경부 2023·2024 공식 보조금 차종표에서 `Model Y Long Range`의 국내 유통과 리스·렌탈 추가보조금 대상을 확인했다. 2024 표의 `Model Y Long Range 19인치`는 별도 파워트레인·차체 트림이 아니라 휠 인증 변형이므로 독립 영구코드를 만들지 않고 Long Range AWD 별칭으로 흡수했다.
- 기존 포괄 영구키 `mf-087.md-004.sm-모델-y::v01::t01`은 글로벌 2020-01부터 종료 미상까지 한 행으로 묶어 국내 세대·인증 경계를 구분할 수 없으므로 원천 시트 5463행에서 제외/1차확인으로 전환해 자동 매칭을 차단했다.
- 공식 국내 문서로 기간이 확인되는 구형 Long Range AWD는 별도 마스터 ID와 영구 트림행키로 발급해 원천 시트 7286행에 추가했다. 검증 기간은 2023-08~2025-01, AWD 5인승이며 19인치 표기를 별칭에 포함했다.
- 원천 두 행을 A:AD 30열로 재조회했고 로컬 차종마스터는 총 5,032건(자동 2,835 / 수동 495 / 차단 1,702)으로 동기화됐다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 통과했다. `git diff --check`에는 기존 작업트리의 LF→CRLF 경고만 있고 공백 오류는 없다.

## 69. Tesla New Model Y Launch Series 세대 귀속 보정 (2026-08-15)

- Tesla 공식 2024 Q4·연간 자료에서 `New Model Y Launch Series Long Range AWD` 한정 구성을 확인했다. Tesla Korea 공식 스토어도 국내 Launch Series 차량에서 영감을 받은 컬렉션을 명시해 국내 존재를 교차확인했다.
- 기존 영구키 `mf-087.md-004.sm-모델-y::v01::t03`은 Launch Series를 구형 Model Y의 2020-01 트림으로 잘못 귀속해 리프레시 전후 차량을 혼합하므로 원천 시트 5465행에서 제외/1차확인으로 전환했다.
- 공식 New Model Y Launch Series는 별도 마스터 ID와 영구 트림행키로 원천 시트 7287행에 추가했다. 2025-02 시작, AWD 5인승, 2025 연식 한정 중고차로 분리했고 공식 판매 종료월은 추정하지 않고 공란으로 유지했다.
- 원천 두 행을 A:AD 30열로 재조회했고 로컬 차종마스터는 총 5,033건(자동 2,835 / 수동 495 / 차단 1,703)으로 동기화됐다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 통과했다. `git diff --check`에는 기존 작업트리의 LF→CRLF 경고만 있고 공백 오류는 없다.

## 70. Tesla Model Y 2021 국내 등록 이력 복원 (2026-08-15)

- 공공데이터포털의 지자체 등록차량 원천에서 Model Y Performance 5인승의 2021-05-11 등록과 Model Y Long Range 5인승의 2021-05-12 등록을 확인했다. 환경부 2023·2024 공식 보조금 자료로 두 트림의 리스·렌탈 유통도 교차확인했다.
- 이미 발급된 2023 시작 영구키 2개는 시작연도 의미를 조용히 변경하지 않았다. 원천 시트 7285~7286행을 제외/1차확인으로 전환하고 원래 기간과 키를 보존했다.
- 2021-05~2025-01 국내 이력을 갖는 Performance AWD·Long Range AWD 5인승 영구코드를 새로 발급해 원천 시트 7288~7289행에 추가했다. Long Range 19인치는 계속 휠 인증 별칭으로 흡수했다.
- 원천 5개 관련 행을 A:AD 30열로 재조회했고 로컬 차종마스터는 총 5,035건(자동 2,835 / 수동 495 / 차단 1,705)으로 동기화됐다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 통과했다. `git diff --check`에는 기존 작업트리의 LF→CRLF 경고만 있고 공백 오류는 없다.
## 71. Tesla Model Y Standard Range 국내 실차 근거 분리 (2026-08-15)

- 2021년 지자체 전기차 보조금 공고에는 `Model Y(Standard Range)`가 지원 대상 차종으로 기재되어 있으나, 공공데이터포털 국내 등록차량 원천에서는 같은 시기의 Long Range·Performance와 달리 실제 등록 이력을 확인하지 못했다.
- 보조금 대상 등재와 국내 실차 등록을 동일시하지 않았다. 기존 영구키 `mf-087.md-004.sm-모델-y::v03::t01`의 의미와 이력은 보존하면서 원천 시트 5468행을 `제외/1차확인`으로 전환해 자동 매칭을 차단했다.
- 기존 행의 `2020-01~2024-01` 기간은 국내 유통 근거로 사용하지 않으며, 2023년 7월 국내 공식 출시된 구형 Model Y RWD와도 병합하지 않는다. 실제 국내 등록 근거가 확보되기 전에는 Standard Range 신규 영구코드를 발급하지 않는다.
- 원천 시트 A:AD 30개 필드를 재조회해 영구키·차종·구동방식·좌석·기간·근거 링크가 보존되고 상태와 검증 메모만 갱신됐음을 확인했다. 로컬 산출물은 총 5,035건(자동 2,834 / 수동 495 / 차단 1,706)으로 동기화됐다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 통과했다. `git diff --check`에는 기존 작업트리의 LF→CRLF 경고만 있고 공백 오류는 없다.
## 72. Tesla Model 3 구형 국내 유통 이력 분리 (2026-08-15)

- 글로벌 생산 시작일 2017-07을 국내 유통 시작일처럼 사용하던 기존 구형 Model 3 행을 재검토했다. 국내 공공 근거에서는 2020년식 Standard Range Plus RWD 실차, 2020-06 Performance 등록, 2020-08 Long Range 등록, 2022년 보조금 대상 RWD를 각각 확인했다.
- 기존 영구키 `mf-087.md-003.sm-모델-3::v02::t02`와 `::t03`은 의미와 이력을 보존하면서 `제외/1차확인`으로 전환했다. 이미 차단된 합본 코드들과 함께 Highland 전후 또는 구동방식이 자동 혼합되지 않도록 했다.
- 국내형 신규 마스터 `mf-087.md-003.sm-legacy-kr__model-3-legacy-korea` 아래 Standard Range Plus RWD, Long Range AWD, Performance AWD, RWD 네 개 영구코드를 원천 시트 7290~7293행에 추가했다. RWD와 Standard Range Plus는 2022년 보조금표에서 병존하므로 별도 코드로 유지했다.
- 신규 4행과 기존 차단 2행을 A:AD 30열로 재조회해 문자열·숫자형, 5인승, RWD/AWD, 기간, 별칭, 공공 근거 링크가 의도대로 보존됐음을 확인했다. 로컬 산출물은 총 5,039건(자동 2,832 / 수동 499 / 차단 1,708)이다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 통과했다. `git diff --check`에는 기존 작업트리의 LF→CRLF 경고만 있고 공백 오류는 없다.
## 73. Tesla Model X Plaid 5인승 구성 보강 (2026-08-15)

- Tesla Korea의 최신 한국어 Model X 사용자 매뉴얼 중량표에서 `Model X Plaid 5인승`과 `Model X Plaid 6인승`이 각각 별도 구성으로 명시된 것을 확인했다. 기존 마스터에는 Plaid 6인승만 있어 인승 정보가 없는 렌트·구독 재고를 6인승으로 고정할 위험이 있었다.
- 2023-03 국내 공식 출시된 Palladium 계열 아래 `mf-087.md-002.sm-refresh-2023__model-x::v02::t02`를 신규 발급하고 원천 시트 7294행에 Plaid 5인승을 추가했다. 기존 `::v02::t01` Plaid 6인승의 의미와 영구키는 변경하지 않았다.
- 원천 시트 A:AD 30열을 재조회해 파워트레인·AWD·5인승 숫자형·Palladium·출시월·공식 한국어 매뉴얼 링크가 의도대로 기록됐음을 확인했다. 로컬 산출물은 총 5,040건(자동 2,832 / 수동 500 / 차단 1,708)이다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npm run check:fonts`, `git diff --check`는 통과했다. `npx tsc --noEmit`은 이번 변경과 무관한 미추적 사용자 파일 `app/erp5/page.tsx:218`의 `Button children` 누락(TS2741) 때문에 전체 게이트만 실패했으며 해당 파일은 보존했다.
## 74. Tesla 구형 Model S·X 국내 공공근거 코드 복원 (2026-08-15)

- 기존 구형 Model S·X 13개 행은 글로벌 생산시점(각각 2012·2018)을 국내 유통 시작처럼 사용하거나 구형과 2023년 신형 명칭을 합본하고 있어 전부 `제외/1차확인`으로 전환했다. 영구키와 원래 필드 의미는 삭제·변경하지 않고 이력으로 보존했다.
- 한국환경공단 친환경차 정보에서 2019-02-20 기준 Model S 75D·90D·100D·P100D 5인승을 확인했고, 한국에너지공단 자료를 인용한 국내 공공 보고서에서 2019-08 기준 Model X 75D·100D의 정부 공인 연비를 확인했다.
- 확인된 6개 트림만 국내형 신규 마스터로 원천 시트 7295~7300행에 발급했다. S는 2019-02~2021-01, X는 2019-08~2021-01의 보수적 검증 구간을 사용하고 모두 수동확인 등급으로 유지했다. 근거가 부족한 구형 Long Range·Performance는 신규 코드를 만들지 않았다.
- 75D·90D·100D의 숫자를 배터리 총용량으로 추정하지 않았다. 공공자료의 용량 값도 트림명 숫자와 상충하므로 신규 6행의 배터리 kWh는 의도적으로 공란 처리했다.
- 신규 6행을 A:AD 30열로 재조회해 영구키, AWD, S 5인승/X 6인승 숫자형, 생산기간, 공공 근거 링크 및 배터리 공란을 확인했다. 로컬 산출물은 총 5,046건(자동 2,823 / 수동 506 / 차단 1,717)이다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 모두 통과했다. 이전 ERP5 미추적 파일의 타입 오류도 현재 작업트리에서는 해소됐다.

## 75. ERP5 상품찾기·최소선택 전자계약 UI 재구성 (2026-08-15)

- `/erp5`를 대시보드 없는 메인 상품찾기로 재구성했다. 공급사/Firebase 데이터와 기존 가격·보증금·우대조건 판정은 유지하고, 검색 외 필터는 클릭 선택형 퀵필터와 좌측 세부필터로 분리했다.
- 상품 결과는 좌측 사진, 차량설명, 조건설명, 전체 기간별 대여료·보증금, 상세/계약 동작을 한 장에 담은 전체 폭 카드로 변경했다. 모바일은 핵심 차량정보·우대조건 3개·최저 기간 가격만 남긴다.
- 최대 5대 다중선택, 비교 화면, 고객 전달 문구 화면, ERP5 차량 상세 경로를 연결했다. 구글 시트는 새 탭 링크로만 제공한다.
- `/erp5/esign?product=...`는 선택 차량·공급사·계약서·정책을 자동 연결하고, ERP5에서는 기간과 운전자 연령만 클릭 선택하도록 기존 직접 금액 입력을 숨겼다. 고객 서명 후 관리자 검토·승인 분리는 기존 엔진 규칙을 유지했다.
- 실제 KH 쏘나타 상품으로 자동 연결, 36개월·만26세 선택, 월 600,000원·보증금 500,000원 스냅샷 반영을 확인했다. 공급사 필수 원자인 업체 주소와 자동차대여사업 등록번호가 없을 때 계약 생성이 차단되는 것도 확인했다.
- `npx tsc --noEmit`, `npm run check:fonts`, `npm run check:tokens`, `npm run check:ui`, `npx tsx scripts/sim-freepass-esign.mts`, `npx tsx scripts/sim-product-offerability.mts`를 통과했다.
## 76. ERP5 전환 포함 전체 오류·진행상태 검수 (2026-08-15)

### 판정

- **개발 빌드와 핵심 사용자 흐름은 동작하지만, 현재 작업트리는 운영 배포 `NO-GO`이다.** ERP5 신규 화면과 상태동기화 경로가 아직 미추적 파일이며, 보안 규칙 1건·중복상품 관리자 UI 2건·운영 환경 게이트가 남아 있다.
- 전자계약은 ERP5 상품에서 `상품 선택 → 공급사·렌트계약서·공급사 정책 자동연결 → 차량조건 반영`까지 연결된다. 다만 `PT-0023` 공급사는 계약정책이 없어 실제 생성이 차단되며, QA 공급사 `sup_qgd99qtj85`도 정책이 없다.
- ERP5는 상품찾기·상세·비교·고객전달·전자계약·계약목록·정산목록까지 골격이 연결됐다. 재고는 아직 기존 `/inventory`를 사용하므로 완전한 ERP5 전환이 아니라 단계적 전환 상태다.

### 이번 검수에서 수정한 저위험 오류

- ERP5 상품 카드의 계약 버튼이 `/erp5/esign?product=...`로 넘어오면 해당 상품·공급사·표준 렌트계약서·우선 정책·차량 스냅샷을 자동 적용하도록 보완했다.
- ERP5 계약/정산 목록의 행 링크가 실제 상세 레코드를 열도록 계약 `?c=` 및 정산 `?s=` 딥링크를 연결하고, 정산 화면이 링크의 대상 월과 상세 선택을 복원하도록 수정했다.
- 홈 화면의 폰트·radius 하드코딩을 디자인 토큰으로 교체했다.
- 현재 코드와 어긋난 회귀 시뮬레이션 4종의 검증식을 실제 SSOT 동작에 맞게 갱신했다. 기능을 약화시키기 위한 예외 처리는 추가하지 않았다.

### 검증 결과

- `npm run build`: **PASS** — Next.js 15.5.21, 38개 정적 페이지 생성 및 전체 라우트 타입검사 통과.
- `npx tsc --noEmit`: **PASS**.
- `npm run check:fonts`, `check:tokens`, `check:ui`: **PASS** — 폰트·토큰·공용 UI 드리프트 0.
- 로컬·비파괴 시뮬레이션 전수 실행: **84 PASS / 2 FAIL**.
  - `sim-product-duplicate-migration`: 24/26 — 서버 감사·dry-run은 남아 있으나 관리자 화면의 읽기 전용 TSV 복사 버튼과 patch dry-run 건수 표시가 사라졌다.
  - `sim-release-security-rules`: 13/14 — 현재 `database.rules.json`의 `v4/products` read가 활성·배정 역할까지 제한하지 않는다. 규칙 파일은 사람/Claude 게이트 없이 수정·게시하지 않았다.
- `check:release`: **PASS, 경고 2건** — 기존 회원 법률 재동의 게이트 OFF, 서비스워커/오프라인 복구 범위 밖.
- `check:b2b-release`: **NO-GO, 50 PASS / 5 FAIL** — 기존 회원 재동의, Firebase 서비스계정, 차량 원자 claim 서버/클라이언트 스위치, Iron 홈페이지 재고 연동이 운영값으로 열리지 않았다.
- `smoke:b2b-roles`: **NO-GO** — 배포 URL·채널·공급사 코드·역할별 ID 토큰이 없어 실제 역할별 원격 검증은 실행할 수 없다.
- `check:template-drift`: **FAIL 1** — 프리패스 표준계약서의 의도된 최신 표현(`출고 시 주행거리`, 스페어키 삭제, `초과주행 요금`)이 착한거래 저장소의 구형 공용 템플릿과 달라 교차 저장소 검사가 실패한다. 프리패스 내용을 구형으로 되돌리지 않았다.
- 데스크톱 1440px·모바일 390px 브라우저 확인: ERP5 상품 447건 로드, 카드/필터/상세/계약/정산 딥링크 정상, 가로 넘침 없음. 최초 상품 로드는 약 6.5초로 느리며, 로컬에서 상태동기화 API 401 후 RTDB SDK 폴백 경고가 남는다.

### 배포 전 차단 항목과 우선순위

1. `app/api/sheet/live-status`는 현재 활성 로그인 사용자라면 누구나 POST로 Google Sheet 조회 및 `v4/products` 상태 write를 유발할 수 있다. 기존 일일동기화 API처럼 관리자/cron 실행과 일반 사용자의 캐시 조회를 분리해야 한다. 데이터 쓰기 경로이므로 설계·권한 게이트 후 수정한다.
2. `database.rules.json`의 `v4/products` 공개 read 범위를 현재 릴리스 후보 규칙과 비교해 사람/Claude가 승인해야 한다. 실데이터 검증 전 게시 금지다.
3. `PT-0023`에 실제 사용할 공급사 정책을 배정하고 QA 공급사를 운영 선택지에서 숨기거나 비활성화해야 한다.
4. 중복상품 관리자 dry-run UI를 복원할지 공식 제거할지 결정하고 시뮬레이션·HANDOFF를 같은 결론으로 맞춰야 한다.
5. ERP5 신규 파일을 커밋 범위로 확정하고, 서비스계정·claim 스위치·역할 토큰을 갖춘 스테이징에서 `smoke:b2b-roles`를 다시 실행해야 한다.
6. 최초 상품 로드와 상태동기화 인증 경로를 정리한 뒤 재고 화면을 ERP5 네이티브 UI로 옮겨야 전체 전환으로 볼 수 있다.
## 75. 실재고 대비 차종마스터 공백 감사 분류 보정 (2026-08-15)

- 실시간 v4 상품 341대를 `public/data/vehicle-master.json` 1,809개 차종과 대조했다. 339대(99.4%)는 제조사·모델·세부모델·파워트레인·트림 축이 모두 존재했다.
- 종전 감사기는 제조사부터 모든 차종 필드가 빈 상품도 `✗제조사`로 판정해 차종마스터 신규 제조사가 필요한 것처럼 보고했다. 이 경우 빈 제조사 코드를 만들게 되는 위험이 있어 `✗차종정보미입력` 판정을 별도로 추가했다.
- 재감사 결과 실제 차종마스터 결손은 0종이다. 남은 2대는 웰릭스(RP013)의 `283소3276`, `24저4970`으로 제조사·모델·세부모델·파워트레인·트림이 모두 빈 공급사 상품행임을 확인했다. 이를 별도 출력 구역으로 분리해 마스터 증설 대상에서 제외했다.
- `scripts/audit-master-gap.mts --csv=tmp/audit-master-gap.csv` 실시간 감사에서 `마스터에 만들어야 할 것 0종`, `공급사 상품행 차종정보 미입력 2종` 출력을 확인했다. `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`도 통과했다.
## 76. 웰릭스 RP013 원천 재동기화 및 실재고 100% 매칭 복구 (2026-08-15)

- 공급사 허브에서 RP013 원천을 `웰릭스 프리패스 재고`의 `재고` 탭으로 확인했다. 원천 21행의 `283소3276`은 2018 더 뉴 레이 TAM 가솔린 1.0 트렌디, `24저4970`은 2018 G80 DH 가솔린 3.3 AWD 프레스티지로 이미 정제 열과 영구 차종코드가 채워져 있었다.
- `apply-sheet-sync.mts --code=RP013` dry-run에서 원천 22행 중 21행 반영, 신규 2·수정 19·부재차단 0·계약락 0을 확인한 뒤 RP013만 `--apply`했다. 반영 전 19개 기존 키는 `tmp/sync-RP013-backup.json`에 백업됐고 신규 2·수정 19가 반영됐다.
- 재동기화 뒤 레거시 빈 키 2개와 신규 번호판 키가 함께 남고, 기존 상품의 모델 필드 일부가 보존돼 K8·G80·레이 세 차량에 교차 오염이 드러났다. 감사 CSV에 `상품키` 열을 추가해 대상 키를 확정했다.
- v3는 수정하지 않았다. v4 오버레이에서 빈 레거시 `EXT_254965fe3fd9`, `EXT_ef11a8e8720b`만 복구 가능한 톰스톤으로 전환하고, `EXT_a92128fa2dc3`, `RP013_24저4970`, `RP013_283소3276`을 원천 시트의 정제 열과 기존 영구 차종코드로 제한 보정했다.
- 최종 실시간 재감사에서 상품 341대 전부가 제조사·모델·세부모델·파워트레인·트림 축을 통과했다. 결과는 `341/341, 100.0%`, 마스터 신규 결손 0종, 공급사 차종정보 미입력 0종이다.
- `sim-sheet-merge` 170/170, `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 통과했다.

## 76. Finder 메뉴 정제·퀵필터 검증 (2026-08-15)

- 기존 `/finder` 카드·상세 렌더링 유지, 엑셀 보기 진입 제거
- 메뉴: 상품찾기 / 계약진행 / 정산확인 / 재고관리로 정제, 역할별 재고관리 노출 유지
- 웹 세부필터 기본 닫힘, 만21세·무보증·보증금분납·대여료·보증금·주행거리·제조사/모델 퀵필터 상시 노출
- 실제 브라우저: `만21세` 선택 시 447대 → 210대로 즉시 반영, 메뉴 4개 노출 확인
- `npx tsc --noEmit`, `check:fonts`, `check:tokens`, `check:ui` 통과
- `sim-product-offerability.mts` 31/31 통과

## 77. ERP5 내부코드 신규발급·레거시 호환 전환 (2026-08-15)

### 적용

- Firebase Auth `uid`는 인증·사용자 저장키로 유지하고 신규 업무 사용자만 별도 `usr_` 코드를 발급하도록 분리했다. 기존 사용자의 `user_code`는 RTDB 역할 쿼리와 과거 계약 접근을 보존하기 위해 자동 치환하지 않는다.
- 신규 코드 SSOT를 `lib/domain/ids.ts`로 통합했다. `usr/sup/pol/veh/chn/cus/tpl/inq/rom/msg/quo/con/esg/stl/pay/doc/att/rpt/aud/run` 접두사와 10자 불투명 토큰, 표시번호, 결정적 방 ID를 제공한다.
- 신규 문의방·문의건·메시지·계약·전자계약 내부 발행·제보·감사·동기화 실행 ID를 표준코드로 발급한다. 기존 `CH_*`, `CS_*`, `TMP-*`, `fp_*`, `AL-*`, `SS-*`, `SL-*` 레코드는 삭제하지 않고 별칭 조회로 유지한다.
- 계약은 `con_` 영구 ID와 사람이 보는 `contract_number=FP-C-YYYYMMDD-*`를 분리했다. 계약 목록·상세·A4/모바일 필드에는 표시번호를 우선 노출한다.
- 정산 Rules가 현재 `$sid === 'ST_' + contract_code`를 강제하므로 운영 저장키와 `settlement_code`는 `ST_*`를 유지하고, 신규 계약 정산에는 `canonical_code=stl_`를 병기한다. Rules 실데이터 게이트 전 `stl_` 저장키 게시를 하지 않았다.
- `rtdb-adapter`와 조직·사용자 resolver가 표준코드, 저장키, `legacy_*`, `legacy_codes`, `code_aliases`를 모두 찾도록 보완했다. v3 write와 `database.rules.json`은 변경하지 않았다.

### 운영 읽기 전용 감사

- 운영 RTDB write 0건으로 코드 현황을 집계했다.
- v3 참조: 정책 53, 파트너 52, 사용자 178건은 모두 레거시 코드다.
- v4: 상품 6,468건 중 표준 2/레거시 6,466, 파트너 42건 중 표준 2/레거시 40, 문의방 518·메시지 2,092·고객 40·계약 50·정산 15·감사 538건은 현재 전부 레거시다.
- 따라서 기존 레코드 일괄 치환은 실행하지 않았다. 관계필드·Rules·딥링크까지 포함한 별도 dry-run과 사람/Claude 위험영역 게이트 후 v4 overlay에만 단계 적용해야 한다.

### 검증

- `npm run build`, `npx tsc --noEmit`, `check:fonts`, `check:tokens`, `check:ui`: PASS.
- `sim:erp5-codes`: 6/6 PASS.
- `sim-agent`: 45/45 PASS, `sim-phase12`: 69/69 PASS, `sim-e2e-settlement`: 22/22 PASS, `sim-lifecycle`: PASS.
- `sim-freepass-esign`: PASS. 아이언 재고 계획 원격 시뮬레이션은 외부 응답 대기가 지속되어 중단했으며, 코드 현황 RTDB 감사는 정상 완료했다.

## 77. 2026 쏘렌토 X-Line 현행 조합 분리 (2026-08-15)

- 기아 공식 2026-05 쏘렌토 가격표와 현행 특징 페이지에서 `The 2026 Sorento`의 독립 트림 `X-Line`을 확인했다. 기존 마스터의 2023년형 `시그니처 X라인` 영구키는 의미를 변경하지 않고 그대로 보존했다.
- 신규 마스터 `mf-002.md-027.sm-mq4-pe-xline-2026__sorento-x-line` 아래 가솔린 2.5T·디젤 2.2·하이브리드 1.6T, 2WD·4WD, 5·6·7인승의 직교 조합 18개를 수동확인 등급으로 발급했다.
- 원천 시트 7301~7318행에 A:AD 30열로 추가하고 재조회했다. 18행·30열, 영구키, 엔진 배기량 숫자형, 구동방식, 인승 숫자형, 공식 가격표 하이퍼링크가 의도대로 기록됐으며 시트 총 행수는 7,318행이다.
- 로컬 산출물은 총 5,064건(자동 2,823 / 수동 524 / 차단 1,717)으로 동기화했고, 신규 X-Line 18개 및 6개 파워트레인·구동 축과 5·6·7인승 축을 재확인했다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `sim-sheet-merge` 170/170, `npm run check:fonts`, `git diff --check`는 통과했다. `npx tsc --noEmit`은 이번 차종마스터 변경과 무관한 기존 작업트리의 `app/finder/page.tsx:541`에서 `FinderToolbar`에 선언되지 않은 `onAdvanced` prop을 전달하는 TS2322 1건 때문에 실패했다.

## 78. Tesla 세대·하드웨어 구분 재감사 (2026-08-15)

- 활성 Tesla 수동확인 코드 29개를 모델·세부모델·생산기간·구동·좌석 기준으로 전수 추출했다. Model 3는 구형 국내형과 Highland, Model Y는 2021~2024형·2025 New Model Y·Launch Series·2026 Model Y L, Model S/X는 2019 국내형과 2023 Palladium, Cybertruck은 2025 국내형으로 분리되어 있다.
- Tesla Korea 현행 Model Y 페이지에서 Model Y Premium과 Model Y L을 확인했고, Model Y L은 AWD·6인승·543km로 명시된다. Tesla Korea 공식 액세서리 페이지의 `동일모델의 출시 연월 2026/04`도 기존 Model Y L 시작월과 일치한다.
- Tesla 공식 지원·서비스 문서에서 Intel Atom과 AMD Ryzen 인포테인먼트 프로세서는 차량의 `컨트롤 > 소프트웨어 > 추가 차량 정보`에서 개별 확인하는 하드웨어 속성임을 확인했다. 서비스 문서는 2020~2024 Model Y와 Model 3 안에서도 Ryzen 장착 여부를 조건으로 절차를 나누므로, 프로세서를 연식·세대의 확정 대리값으로 사용할 수 없다.
- 따라서 Ryzen/Atom은 영구 차종코드·트림 별칭·자동 세대 판정 축에 넣지 않는다. 향후 공급사 원천이 해당 값을 제공하면 차종코드와 독립된 재고 하드웨어 속성으로만 보존해야 한다. 연식이 없는 `Model Y Long Range Ryzen` 같은 문구만으로 구형과 New Model Y를 자동 확정하지 않는다.
- Tesla Korea의 현행 Model Y 본문에는 국내 주문형으로 Premium과 Model Y L만 명확히 노출된다. 한국어 캠페인의 `Model Y Performance 3.7초` 표기는 존재하지만 New Model Y Performance의 국내 출시월·국내 형식과 결합된 근거가 아니며, 해외 신형 공식 사양 3.5초와도 다르다. 국내 현행 신형 Performance 코드는 근거가 확보될 때까지 발급하지 않았다.

## 79. The new GRANDEUR GN11 현행 코드 재발급 (2026-08-15)

- 기존 현행형 `mf-001.md-004.sm-gn7` 18개는 2026-07 부분변경 모델을 `더 뉴 그랜저 GN7`로 기록하고 가솔린 2.5·3.5를 터보로 표시했으며, 부분변경 현행 가격표에 없는 아너스를 포함하고 있었다.
- 현대자동차 공식 The new GRANDEUR 모델소개에서 개발코드 `GN11`을 확인하고, 2026-07-01 현행 가격표에서 가솔린 2.5·3.5, LPG 3.5, 하이브리드 1.6T, 가솔린 3.5 HTRAC, 프리미엄·익스클루시브·캘리그래피·Black Ink 구성을 교차확인했다.
- 기존 GN7 18개 영구키는 삭제하거나 의미를 바꾸지 않고 원천 시트 4002~4019행을 `제외/1차확인`으로 차단했다. 가솔린 자연흡기 오류와 아너스 현행 오분류도 차단 사유에 명시했다.
- 신규 `mf-001.md-004.sm-gn11__the-new-grandeur` 아래 공식 조합 18개를 수동확인 등급으로 발급해 원천 시트 7319~7336행에 추가했다. 가솔린 2.5·3.5와 LPG는 비터보, 하이브리드 1.6T만 터보로 기록했다.
- 시트 재조회에서 기존 18행 차단·사유, 신규 18행의 A:AD 30열, GN11, 터보 여부, 공식 링크를 확인했다. 로컬 산출물은 총 5,082건(자동 2,805 / 수동 542 / 차단 1,735)이다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 모두 통과했다.

## 80. The 2026 Sportage X-Line 독립 트림 보강 (2026-08-15)

- 기존 더 뉴 스포티지 NQ5 활성 코드에는 공식 독립 트림 `X-Line`이 `시그니처 X라인`으로 기록된 4개 조합만 있었고, 현행 공식 가격 페이지에 존재하는 LPG 2.0 X-Line이 누락돼 있었다.
- 기아 공식 The 2026 Sportage 가격표·특징 페이지에서 X-Line이 시그니처 기본 품목에 전용 외장 디자인, 19인치 블랙 휠, 블랙 스웨이드 헤드라이닝, 프리미엄 스티어링 휠을 더하는 독립 트림임을 확인했다.
- 기존 `시그니처 X라인` 4개 영구키는 삭제·개명하지 않고 원천 시트 321·325·331·335행을 `제외/1차확인`으로 차단했다.
- 신규 `mf-002.md-025.sm-nq5-pe-xline__sportage-x-line` 아래 가솔린 1.6T 2WD·4WD, LPG 2.0 2WD, 하이브리드 1.6T 2WD·4WD 총 5개를 수동확인 등급으로 발급해 원천 시트 7337~7341행에 추가했다.
- 시트 재조회에서 기존 4행 차단, 신규 5행 A:AD 30열, 파워트레인·구동·터보·공식 PDF 링크를 확인했다. 로컬 산출물은 총 5,087건(자동 2,801 / 수동 547 / 차단 1,739)이다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 모두 통과했다.

## 81. 2026 싼타페 MX5 Black Ink 독립 트림 보강 (2026-08-15)

- 현대 공식 2026 싼타페 가솔린·하이브리드 가격표에서 `Black Ink`가 캘리그래피 기본 품목에 전용 휠·외장·내장 디자인을 더한 독립 판매 트림임을 확인했다. 두 가격표 모두 현 모델 출시일을 2025-08-07로 명시한다.
- 가솔린은 HTRAC 선택, 하이브리드는 2WD·AWD 가격 구분을 확인했고, 양쪽 모두 6인승·7인승 선택이 가능해 기본 5인승을 포함한 5·6·7인승 조합으로 분리했다.
- 신규 `mf-001.md-017.sm-mx5-black-ink-2026__santafe-black-ink` 아래 가솔린 2.5T·하이브리드 1.6T × 2WD·4WD × 5·6·7인승 총 12개를 수동확인 등급으로 발급해 원천 시트 7342~7353행에 추가했다. 2023년 최초 출시형에 소급하지 않고 2026 연식변경 시작월인 2025-08부터 적용했다.
- 국내 공식 가솔린·하이브리드 가격표에는 `XRT`가 없으므로 국내 영구코드를 발급하지 않았다. 해외 XRT 표기가 들어오더라도 국내 싼타페 Black Ink로 자동 치환하지 않는다.
- 시트 재조회에서 12행의 A:AD 30열, 숫자형 배기량·인승, 구동방식, 공식 PDF 하이퍼링크를 확인했다. 로컬 산출물은 총 5,099건(자동 2,801 / 수동 559 / 차단 1,739)이며 생성기 누적 보강 행은 302개다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 모두 통과했다.

## 82. 디 올 뉴 팰리세이드 LX3 익스클루시브 누락 조합 보강 (2026-08-15)

- 기존 LX3 활성 영구코드 20개를 연료·구동·인승·트림 축으로 전수 추출했다. 프레스티지·캘리그래피는 가솔린 2.5T·하이브리드 2.5T, 2WD·4WD, 7·9인승 축이 존재했지만 익스클루시브 4개 조합이 누락돼 있었다.
- 현대 공식 디 올 뉴 팰리세이드 가격표에서 현 모델 출시일 2025-01-15, 가솔린 2.5T와 하이브리드 2.5T, 7·9인승, 공통 HTRAC 선택을 확인했다. 정부 신고연비 표도 두 파워트레인의 2WD·AWD 7/9인승을 각각 명시한다.
- 기존 `mf-001.md-058.sm-lx3`의 20개 영구키는 수정하지 않았다. 같은 마스터에 가솔린 2.5T 2WD 7인승, 가솔린 2.5T 4WD 7인승·9인승, 하이브리드 2.5T 4WD 9인승의 익스클루시브 4개를 `v09`~`v12` 신규 수동확인 코드로 발급했다.
- 원천 시트 7354~7357행에 A:AD 30열로 추가하고 재조회해 키, 숫자형 배기량·인승, 2WD/4WD, 공식 PDF 하이퍼링크를 확인했다. LX3 현행 조합은 총 24개, 익스클루시브는 8개가 되어 전체 직교 축이 완성됐다.
- 로컬 산출물은 총 5,103건(자동 2,801 / 수동 563 / 차단 1,739), 생성기 누적 보강 행은 306개다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 모두 통과했다.

## 83. 더 뉴 스타리아 US4 하이브리드 차체·인승 재분리 (2026-08-15)

- 기존 현행 `mf-001.md-060.sm-us4` 11개를 전수 추출했다. 더 뉴 스타리아 시작월을 2025-03으로 기록했고, 하이브리드 2·5·7·9·11인승과 LPG·바이퓨얼이 하나의 세부모델에 섞여 있었으며 투어러·카고 차체 용도가 구분되지 않았다.
- 현대 공식 더 뉴 스타리아 하이브리드 가격표는 현 모델 출시일을 2025-12-17로 명시한다. 투어러 하이브리드는 9·11인승, 카고 하이브리드는 2·5인승이며 각 인승에 스마트·모던 트림이 존재한다. 파워트레인은 가솔린 1.6T 하이브리드 2WD다.
- 기존 11개 영구키는 삭제·개명하지 않고 원천 시트 1855~1865행을 `제외/1차확인`으로 차단했다. 기존 의미와 이력은 보존하되 출시시점·차체·인승·트림이 불명확한 자동매칭만 중단했다.
- 신규 투어러 마스터 `mf-001.md-060.sm-us4-pe-tourer-hev-2026__the-new-staria-tourer-hybrid`와 카고 마스터 `mf-001.md-060.sm-us4-pe-cargo-hev-2026__the-new-staria-cargo-hybrid` 아래 공식 조합 8개를 수동확인 등급으로 발급했다.
- 원천 시트 7358~7365행을 재조회해 8행·A:AD 30열, 투어러/카고 세부모델, 2·5·9·11인승 숫자형, 스마트·모던, 1,598cc, 2WD, 공식 PDF 링크를 확인했다. 기존 11행도 모두 차단 상태와 사유가 기록됐다.
- 로컬 산출물은 총 5,111건(자동 2,794 / 수동 571 / 차단 1,746), 생성기 누적 보강 행은 314개다. LPG·라운지·리무진·Electric 현행형은 다음 감사 단위에서 공식 차체별로 이어서 분리한다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 모두 통과했다.

## 84. 더 뉴 스타리아 US4 LPG 투어러·카고 보강 (2026-08-15)

- 현대 공식 더 뉴 스타리아 가격표에서 현 모델 출시일 2025-12-17과 스마트스트림 LPG 3.5·8단 자동변속기 구성을 확인했다. 투어러는 9·11인승, 카고는 3·5인승이며 각 인승에 스마트·모던 트림이 존재한다.
- 하이브리드 카고가 2·5인승인 것과 달리 LPG 카고는 3·5인승이므로 연료명만으로 인승을 재사용하지 않고 별도의 차체·인승 영구코드로 분리했다.
- 신규 투어러 마스터 `mf-001.md-060.sm-us4-pe-tourer-lpg-2026__the-new-staria-tourer-lpg`와 카고 마스터 `mf-001.md-060.sm-us4-pe-cargo-lpg-2026__the-new-staria-cargo-lpg` 아래 공식 조합 8개를 수동확인 등급으로 발급했다.
- 원천 시트 7366~7373행을 A:AD로 재조회해 8행·30열, 투어러/카고 세부모델, LPG 3.5 2WD, 3·5·9·11인승 숫자형, 스마트·모던, 3,470cc 숫자형, 공식 PDF 링크를 확인했다.
- 로컬 산출물은 총 5,119건(자동 2,794 / 수동 579 / 차단 1,746), 생성기 누적 보강 행은 322개다. 라운지·리무진·Electric은 별도 판매체계이므로 후속 감사에서 분리한다.

## 85. 더 뉴 스타리아 US4 일렉트릭 차체·인승 보강 (2026-08-15)

- 현대 공식 `the-new-staria-electric_price.pdf`에서 현 모델 출시일 2026-04-23, 84.0kWh 배터리·160kW 모터, 투어러 11인승 모던과 카고 3·5인승 모던을 확인했다.
- 현대 공식 `the-new-staria-lounge-electric_price.pdf`에서 라운지 11인승 프레스티지, 7인승 프레스티지·인스퍼레이션을 확인했다. 일반 투어러와 라운지는 좌석 구조와 판매체계가 달라 별도 마스터로 발급했다.
- 현대 공식 `the-new-staria-limousine-electric_price.pdf`에서 리무진 일렉트릭 6인승 인스퍼레이션과 동일 84.0kWh·160kW 구성을 확인했다. 라운지 옵션이나 일반 트림으로 흡수하지 않고 리무진 전용 마스터로 분리했다.
- 신규 마스터 4개 아래 공식 조합 7개를 수동확인 등급으로 발급했다: 투어러 1개, 카고 2개, 라운지 3개, 리무진 1개. 원천 시트 7,374~7,380행의 A:AD 30열을 재조회하여 키·숫자형 인승/배터리·공식 링크를 확인했다.
- 로컬 산출물은 총 5,126건(자동 2,794 / 수동 586 / 차단 1,746), 생성기 누적 보강 행은 329개다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `sim-sheet-merge` 170/170, `tsc --noEmit`, `check:fonts`, `git diff --check`를 통과했다. `git diff --check` 출력은 기존 작업트리의 LF→CRLF 경고뿐이며 whitespace 오류는 없다.

## 86. 더 뉴 스타리아 US4 라운지·리무진 내연/하이브리드 보강 (2026-08-15)

- 현대 공식 현행 가격 페이지에서 더 뉴 스타리아 라운지 LPG와 하이브리드의 9인승 프레스티지·인스퍼레이션, 7인승 인스퍼레이션 구성을 확인했다. LPG 3.5와 1.6T 하이브리드는 서로 다른 영구 마스터로 분리했다.
- 현대 공식 `the-new-staria-limousine-hybrid_price.pdf`에서 현 모델 출시일 2026-04-23, 리무진 하이브리드 9인승·6인승 인스퍼레이션과 1.6T 하이브리드 구성을 확인했다. 라운지 트림이나 리무진 일렉트릭 코드로 흡수하지 않았다.
- 신규 마스터 3개 아래 공식 조합 8개를 수동확인 등급으로 발급했다: 라운지 LPG 3개, 라운지 하이브리드 3개, 리무진 하이브리드 2개. 원천 시트 7,381~7,388행의 A:AD 30열을 재조회해 영구키·숫자형 순번/인승·공식 링크를 확인했다.
- 로컬 산출물은 총 5,134건(자동 2,794 / 수동 594 / 차단 1,746), 생성기 누적 보강 행은 337개다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `sim-sheet-merge` 170/170, `check:fonts`, `git diff --check`를 통과했다. 전체 `tsc --noEmit`은 이번 차종 변경과 무관한 사용자 작업 파일 `lib/domain/product-master-import.ts:343`의 `"product_master"` source_schema 타입 불일치 때문에 실패했으며 해당 파일은 수정하지 않았다.

## 87. 스타리아 US4 레거시 오염 키 및 현행 라운지 LPG 9인승 정정 (2026-08-15)

- 현대자동차그룹의 2024 스타리아 하이브리드 사전계약 자료와 현대차 헤리티지에서 스타리아 라운지 하이브리드가 2024-02에 추가된 모델임을 확인했다. 따라서 하이브리드를 2021-04 시작으로 기록한 기존 US4 통합 마스터 4개는 생산시점과 차체 의미가 안전하지 않아 자동매칭을 차단했다.
- 기존 디젤 2.2 4WD 7인승 중 트림명이 `스타리아 라운지 리무진`으로 두 차체를 합친 1개도 의미를 확정할 수 없어 차단했다. 정상적인 구형 디젤·LPG 및 명확한 라운지 인스퍼레이션 행은 보존했다.
- 현대자동차그룹의 2026-04-23 더 뉴 스타리아 전체 라인업 자료는 라운지를 `LPI 7인승, 하이브리드 7·9인승, EV 7·11인승`으로 명시한다. 직전 보강에서 잘못 발급한 현행 라운지 LPG 9인승 프레스티지·인스퍼레이션 2개는 삭제하거나 다른 의미로 재사용하지 않고 즉시 `제외/1차확인`으로 전환했다. 현행 라운지 LPG 7인승 인스퍼레이션은 유지했다.
- 원천 시트 1,874·1,875·1,882·1,884·1,885·7,381·7,382행의 A:AD 30열을 재조회해 상태·영구키·차단 근거를 확인했다.
- 로컬 산출물은 총 5,134건(자동 2,789 / 수동 592 / 차단 1,753)이다. `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `check:fonts`, `git diff --check`를 통과했다. 전체 타입 검사의 기존 `product-master-import.ts:343` 오류는 계속 별도 사용자 작업 범위로 남아 있다.

## 88. 2024~2025 스타리아 US4 하이브리드 차체별 복원 (2026-08-15)

- 현대 공식 `staria-hybrid-24-price.pdf`에서 현 모델 출시일 2024-03-05와 투어러 11·9인승 모던, 카고 3·5인승 모던 조합을 확인했다.
- 현대 공식 `staria-lounge-hybrid-24-price.pdf`에서 라운지 9인승 프레스티지·인스퍼레이션, 라운지 7인승 인스퍼레이션을 확인했다. 같은 가격표에 리무진 출시일 2025-02-19와 9·7인승 리무진 트림이 별도로 명시되어 있어 독립 리무진 마스터로 분리했다.
- 2024-03 출시 일반 하이브리드 7개는 2025-12 더 뉴 스타리아 출시 직전인 2025-11까지, 2025-02 출시 리무진 2개는 2026-04 신형 리무진 출시 직전인 2026-03까지로 생산 경계를 분리했다.
- 신규 마스터 4개 아래 9개 조합을 중고차 수동확인 등급으로 발급했다. 원천 시트 7,389~7,397행의 A:AD 30열을 재조회해 영구키·차체·인승·트림·숫자형 인승·공식 PDF 링크를 확인했다.
- 로컬 산출물은 총 5,143건(자동 2,789 / 수동 601 / 차단 1,753), 생성기 누적 보강 행은 346개다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `sim-sheet-merge` 170/170, `tsc --noEmit`, `check:fonts`, `git diff --check`를 모두 통과했다. 이전 별도 사용자 작업의 타입 오류도 현재 작업트리에서는 해소되어 전체 타입 게이트가 다시 통과한다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 모두 통과했다.

## 89. 테슬라 세대·후속 업데이트·Model Y L 구분 보강 (2026-08-15)

- Tesla 공식 정비 설명서에서 2025-06 Model S·Model X의 신형 부품·기능 업데이트와 `2025+` 정비 절차 구분을 확인했다. 2023 국내 출시 Palladium 리프레시는 2025-05에서 닫고, 이를 단순 연식변경으로 합치지 않은 `1세대 3차 부분변경 / Palladium 2025+` 마스터를 새로 발급했다.
- Model S 2025+는 AWD·Plaid 5인승 2개, Model X 2025+는 AWD 5·6·7인승과 Plaid 5·6인승 5개로 총 7개 영구키를 보존했다. 이 중 Model X Plaid 5인승은 한국어 매뉴얼 중량표에는 있으나 국내 한국공인연비가 6인승만 인증하므로 2023형·2025+형 두 키 모두 `제외/1차확인`으로 차단했다. 실제 배정 가능한 신규 조합은 6개다.
- Model Y L은 Tesla 대한민국 현행 페이지에서 전장 4,970mm·AWD·6인승의 롱휠베이스 파생 차체임을 재확인했다. Tesla Korea 공식 액세서리의 동일모델 출시연월은 2026-07이므로 기존 2026-04 값을 같은 영구키에서 정정했다.
- Ryzen은 공식적으로 실차의 `컨트롤 > 소프트웨어 > 추가 차량 정보 > 인포테인먼트 프로세서`에서 확인할 수 있으나 연식·트림과 일대일 대응하지 않는 생산 중 하드웨어 사양이다. 따라서 자동 차종코드나 세대 별칭으로 사용하지 않고, 향후 실차/VIN 기반 보조속성으로만 수집한다.
- 원천 시트 7,136행과 7,140~7,145·7,294행의 경계를 정정하고, 신규 7개를 7,398~7,404행에 A:AD 30열로 추가했다. 재조회 결과 문자열·숫자형 순번/인승·공식 링크가 모두 일치했다.
- 로컬 산출물은 총 5,150건(자동 2,789 / 수동 606 / 차단 1,755), 생성기 누적 보강 행은 353개다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 모두 통과했다. 최초 병렬 폰트 검사에서 Windows `uv_os_get_passwd` ENOMEM이 발생했으나 저장소 표준 `NODE_OPTIONS` 보정 후 단독 재실행해 통과했다.

## 90. Model 3 Highland Long Range AWD 국내 이력 복원 (2026-08-15)

- 테슬라 활성·차단 영구키 65개를 모델·세부모델·트림·생산기간·인승 기준으로 전수 추출했다. 현행 Premium Long Range RWD는 있었지만 2024 국내 Highland 초기 판매의 Long Range AWD가 누락돼 있었다.
- Tesla Korea 공식 인증중고차 원천에서 제조연도 2024, 생산일자 2024-04-15, 최초등록일 2024-05-03, VIN과 차량번호가 공개된 `Model 3 Long Range 상시 사륜구동(AWD)` 실차를 확인했다.
- 환경부 2024 공식 보조금 차종표에서도 `Model 3 Long Range`의 국비보조금과 리스·렌탈 추가보조금 대상을 교차확인했다. 구형 Model 3 Long Range 코드에 합치지 않고 Highland 마스터의 `v04::t01` 신규 영구코드로 분리했다.
- 생산기간은 공식 실차 생산월 2024-04부터 현행 Premium Long Range RWD 전환 직전 2026-06까지로 보수적으로 닫았다. 종료월은 2024형 실차와 2026-07 현행 구성 사이 경계에 따른 추론임을 근거문에 명시했다.
- 원천 시트 7,405행에 A:AD 30열로 추가하고 재조회해 영구키, 숫자형 순번·인승, AWD, 공식 Tesla 실차 링크와 환경부 교차근거를 확인했다.
- 로컬 산출물은 총 5,151건(자동 2,789 / 수동 607 / 차단 1,755), 생성기 누적 보강 행은 354개다. 실재고 341대 대상 마스터 갭 감사도 341대 전부 판정·결손 0종을 유지했다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`를 모두 통과했다.

## 91. New Model Y 2025 국내 명칭 변천 별칭 보강 (2026-08-15)

- Tesla 대한민국 현행 페이지는 리프레시 5인승을 `Model Y Premium` 아래 `Premium Long Range AWD`·`Premium RWD`로 표시한다. 반면 환경부 2025 공식 보조금표는 같은 리프레시 국내형을 `New Model Y Long Range`·`New Model Y RWD`로 기록하고 리스·렌탈 추가보조금 대상을 명시한다.
- Premium 명칭으로 바뀐 정확한 국내 월은 공식 자료에서 직접 확인되지 않았다. 근거 없이 생산기간을 잘라 신규 영구키를 만들지 않고, 기존 리프레시 AWD·RWD 키의 역사 별칭에 2025 공식 명칭을 추가했다.
- `Juniper`는 계속 비공식 검색 별칭으로만 유지했다. 차체 세대·구동계·인승 의미는 변경하지 않아 영구키 계약을 보존했다.
- 원천 시트 7,134~7,135행의 별칭과 근거문을 갱신하고 J·AA:AC를 재조회해 두 영구키와 공식 명칭·링크의 정합성을 확인했다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `git diff --check`를 통과했다. 총량은 5,151건(자동 2,789 / 수동 607 / 차단 1,755)으로 변동 없다.

## 92. Polestar 4·Scenic E-Tech 국내 렌트 전기차 결손 보강 (2026-08-15)

- 환경부 2025 공식 리스·렌탈 추가보조금 차종과 로컬 차종마스터를 대조해 Polestar 4와 Renault Scenic E-Tech가 0건인 결손을 확인했다.
- Polestar Korea 공식 2024-08-13 국내 출시 및 2024-11-29 출고 시작 자료, 현행 국내 사양 페이지를 근거로 100kWh Long range Single motor RWD와 Long range Dual motor AWD 2개를 발급했다. 20·21·22인치 및 Performance Pack은 인증·선택 패키지이므로 별도 영구코드로 과분할하지 않고 별칭으로 흡수했다.
- 르노코리아 공식 2025-06·08 가격표를 근거로 Scenic E-Tech 100% electric의 87kWh·160kW·FWD·5인승·테크노 단일 조합을 발급했다. 환경부 공식표의 `scenic` 리스·렌탈 대상도 교차확인했다.
- Polestar는 기존 제조사 `mf-089`의 신규 모델 `md-002`, Scenic은 르노코리아 `mf-005`의 신규 모델 `md-021`을 사용해 기존 모델 번호와 충돌하지 않도록 했다.
- 원천 시트 7,406~7,408행에 A:AD 30열로 추가하고 재조회해 영구키, 숫자형 인승·배터리, RWD/AWD/FWD, 공식 제조사 링크를 확인했다.
- 로컬 산출물은 총 5,154건(자동 2,789 / 수동 610 / 차단 1,755), 생성기 누적 보강 행은 357개다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`를 모두 통과했다.

## 82. 계약진행 페이지 발송 작업공간 통합 (2026-08-15)

- `/contract`에서 레거시 `WorkPage` 계약·정산 4분할 셸을 제거하고 `ContractSendWorkspace` 전용 UI를 적용했다. 전자계약 데이터·검증·발행 함수는 `EsignSendCenter` SSOT를 그대로 사용한다.
- 왼쪽은 검색·전체·확인 필요·진행 중·완료 필터와 계약 목록, 오른쪽은 현재 계약의 작성·미리보기·링크·진행상황만 표시한다. 새 계약은 공급사·계약서 종류·정책을 선택한 뒤 차량, 기간·연령·대여조건, 특약, 최종 확인이 순서대로 열리도록 구성했다.
- 선택 전 중복 안내, `·직접` 표기, 공급사 법인명 접두어, 선택 전 빈 후속 단계와 조기 미리보기를 제거했다. 완료 계약에서는 진행 단계와 완료 PDF를 한 흐름으로 확인한다.
- 실제 브라우저에서 목록·상태 필터·새 계약 단계형 진입·완료 계약 상세·단일 세로 스크롤·가로 오버플로 없음·런타임 경고 없음을 확인했다.
- `npx tsc --noEmit`, `npm run check:fonts`, `sim-freepass-esign`, `git diff --check`를 통과했다. `check:ui`의 계약페이지 레거시 고정 검사는 새 작업공간 기준으로 갱신했으며, 남은 1건은 이번 범위 밖 `app/page.tsx`의 기존 radius 경고다.
## 93. 볼보 EX30 국내형 사양·출시시점 교정 (2026-08-15)

- 볼보자동차코리아 공식 2025-02-03 출시자료로 국내 EX30이 66kWh NCM·200kW·RWD의 Single Motor Extended Range 단일 파워트레인과 Core·Ultra 두 트림임을 확인했다. 기존 69kWh·2024-06 코드는 글로벌 명목용량과 국내 출시 전 시점이 혼합돼 영구키 의미를 보존한 채 `blocked` 처리했다.
- 볼보자동차코리아 공식 2025-09-04 출시자료와 2026 현행 가격자료로 EX30 Cross Country가 66kWh·Twin Motor Performance·AWD·Ultra 단일 국내 판매 조합임을 확인했다. 기존 69kWh·2025-06·퍼포먼스 울트라 코드는 차단하고 국내형 신규 영구코드로 분리했다.
- 원천 시트 기존 행 `차종마스터!A5286:AD5287`의 관리상태·근거를 갱신하고 신규 국내형 3행을 `차종마스터!A7409:AD7411`에 추가했다. 재읽기에서 각 행 30열, 숫자형 순번·인승·배터리, 공식 링크와 `capturedSheetRow` 7409~7411을 확인했다.
## 94. 볼보 XC40 2026년식 국내 조합 분리 (2026-08-15)

- 볼보자동차코리아 공식 2025-10-15 출시자료에서 국내 2026년식 XC40이 197마력 48V 마일드 하이브리드 B4 AWD 단일 파워트레인과 Plus Bright·Ultra Bright·Ultra Dark 세 판매 조합임을 확인했다. 한국 공식 제원표로 1,969cc·AWD·5인승을 교차확인했다.
- 기존 `mf-017.md-023.sm-xc40` 7개 키는 R-Design·Momentum·Inscription·Black Edition·Ultimate·Ultra 등 서로 다른 연식 명칭을 2018-03부터 현재까지 합쳐 자동 과매칭 위험이 있으므로 원래 의미를 보존한 채 `blocked` 처리했다.
- 신규 국내형 3개 키를 원천 시트 `차종마스터!A7412:AD7414`에 추가하고 기존 `A5206:AD5212`를 차단 상태로 동기화했다. 원천 재읽기에서 기존 7행과 신규 3행 모두 30열, 신규 영구키·숫자형 순번·1,969cc·2.0L·5인승 및 공식 링크를 확인했다.
## 95. 볼보 신형 XC90 2026년식 국내 조합 분리 (2026-08-15)

- 볼보자동차코리아 공식 2025-07-02 출시자료로 신형 2026년식 XC90의 국내형이 B6 AWD 48V 마일드 하이브리드와 T8 AWD 플러그인 하이브리드, 7인승임을 확인했다. 가격이 직접 연결된 판매 조합은 B6 Plus·B6 Ultra·T8 Ultra 세 가지다.
- Ultra의 Bright·Dark는 동일 트림 내 외관 테마 선택이므로 영구코드를 나누지 않고 별칭에 흡수했다. 한국 공식 제원표로 B6/T8 모두 1,969cc·AWD임을 교차확인했다.
- 2015-08부터 현재까지 서로 다른 연식의 Ultimate·Ultra·Black Edition과 외관 테마를 합친 기존 현행 6개 키는 원래 의미를 보존한 채 `blocked` 처리했다. 종료가 이미 확인된 R-Design·Inscription·D5·T6 등 역사 코드는 이번 수정 대상에서 제외했다.
- 원천 시트 기존 행 5217·5218·5222~5225를 차단하고 신규 국내형 3행을 `차종마스터!A7415:AD7417`에 추가했다. 재읽기에서 신규·수정 대상 모두 30열, 영구키, 숫자형 순번·1,969cc·2.0L·7인승, 공식 링크를 확인했다.
## 96. 볼보 신형 S90 2026년식 현행 누락 보강 (2026-08-15)

- 기존 마스터에는 2023-12 종료된 S90 T8 Inscription·Excellence 두 코드만 있고 현행 국내 판매형이 전혀 없음을 확인했다.
- 볼보자동차코리아 공식 2025-07-02 출시자료로 신형 2026년식 S90의 가격 연결 조합 B5 Plus·B5 Ultra·T8 Ultra를 신규 발급했다. 국내 공식 구성기 출력으로 B5의 FWD·5인승, 한국 공식 제원표로 B5 1,969cc를 확인했고 T8은 공식 국내 출시자료의 AWD 플러그인 하이브리드 계보와 5인승을 적용했다.
- B5 Ultra의 Bright·Dark는 동일 트림 내 외관 테마이므로 별칭으로 흡수하고 별도 영구코드를 발급하지 않았다. 기존 종료 이력 2개는 의미와 종료시점이 충돌하지 않아 유지했다.
- 원천 시트 `차종마스터!A7418:AD7420`에 신규 3행을 추가했다. 재읽기에서 각 행 30열, 영구키, 숫자형 순번·1,969cc·2.0L·5인승, B5 FWD 및 T8 AWD, 공식 링크를 확인했다.
## 97. 볼보 V60 Cross Country 2026년식 현행 분리 (2026-08-15)

- 볼보자동차코리아 공식 2026년식 국내 구성 출력과 2025-11-11 공식 한정판 자료에서 현행 V60 Cross Country의 B5 AWD 48V 마일드 하이브리드·Ultra·5인승을 확인했다. 한국 공식 제원표로 1,969cc·AWD를 교차확인했다.
- Forest Lake Edition은 Ultra 기반 색상·캐리어 패키지 한정판이므로 독립 영구코드를 만들지 않고 별칭으로 흡수했다. 해외 Plus 구성을 국내 현행으로 추정하지 않았으며 공식 국내 확인 시점 2025-11보다 시작월을 앞당기지 않았다.
- 기존 B5·Ultimate·Ultra·Pro 4개 키는 모두 2019-03부터 현재까지 열려 연식·트림 과매칭 위험이 있으므로 원래 의미를 보존한 채 `blocked` 처리했다. 2023-12 종료가 명시된 T5·T5 Pro 역사 코드는 유지했다.
- 원천 시트 `차종마스터!A5247:AD5250`을 차단하고 신규 행 `A7421:AD7421`을 추가했다. 재읽기에서 수정·신규 모든 행 30열, 신규 영구키·1,969cc·2.0L·AWD·5인승·공식 링크를 확인했다.

## 98. 볼보 V90 Cross Country MY2023 국내 체계 분리 (2026-08-15)

- 볼보자동차코리아 공식 2022-08-01 기준 MY2023 옵션표에서 국내 V90 Cross Country의 B5 AWD Plus·B5 AWD Ultimate 트림 체계를 확인했다. 공식 제원표로 1,969cc·250마력·AWD·5인승을 교차확인했고, 현재 국내 구성 출력에서도 B5 AWD Plus 판매가 유지됨을 확인했다.
- 기존 B5 얼티메이트·울트라·프로·플러스 4개 키는 모두 B5 도입 전인 2017-03부터 현재까지 열려 연식과 국내 트림 명칭을 과매칭한다. 영구키 의미와 이력은 바꾸지 않고 `blocked` 처리했으며, 2021-12 종료가 명시된 D5·D5 Pro·T5·T5 Pro 역사 코드는 유지했다.
- 신규 국내형 마스터 `mf-017.md-022.sm-v90cc-my23-korea__v90-cross-country-b5-awd` 아래 Plus·Ultimate 2개를 발급했다. 공식 문서 기준일보다 앞당기지 않은 2022-08 생산 시작·MY2023으로 기록하고, Ultra·Pro를 현행 별칭으로 흡수하지 않았다.
- 원천 시트 `차종마스터!A5260:AD5263`을 차단하고 신규 `A7422:AD7423`을 추가했다. 재읽기에서 기존 4행과 신규 2행 모두 30열, 신규 영구키·숫자형 순번·1,969cc·2.0L·AWD·5인승·공식 링크를 확인했다.
- 로컬 산출물은 총 5,169건(자동 2,766 / 수동 625 / 차단 1,778), 생성기 누적 보강 행은 372개다. `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 모두 통과했다.

## 99. 볼보 XC60 MY2023 국내 중고 유통 계보 복원 (2026-08-15)

- 기존 XC60 마스터에는 2025-08 출시된 신형 2026년식 B5 AWD Plus·B5 AWD Ultra·T8 AWD Ultra 3개만 있고, 국내 렌트·중고 유통의 핵심인 직전 연식 계보가 전혀 없었다. 현행 3개는 공식 2025-08 출시자료와 일치해 유지했다.
- 볼보자동차코리아 공식 2022-08-01 기준 MY2023 옵션표에서 B5 AWD Plus Bright·B5 AWD Ultimate Bright·B6 AWD Ultimate Bright·T8 AWD Ultimate Bright 네 국내 판매 조합을 확인했다. 한국 공식 MY2023 제원표로 1,969cc·AWD·5인승을 교차확인했다.
- Bright는 당시 공식 표기를 검색 별칭에 보존하되 별도 영구 트림축으로 과분할하지 않았다. 2025-08 신형이 Plus·Ultra 체계로 출시된 공식 경계에 맞춰 MY2023 계보는 2022-08~2025-07, 연식 2023~2025로 닫아 현행 키와 중첩되지 않게 했다.
- 신규 마스터 `mf-017.md-016.sm-xc60-my23-korea__xc60-b5-b6-t8-awd` 아래 4개를 중고차 수동확인 등급으로 발급하고 원천 시트 `차종마스터!A7424:AD7427`에 추가했다. 재읽기에서 각 행 30열, 영구키·기간·숫자형 순번/1,969cc/5인승·AWD·공식 링크를 확인했다.
- 로컬 산출물은 총 5,173건(자동 2,766 / 수동 629 / 차단 1,778), 생성기 누적 보강 행은 376개다. `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 모두 통과했다.

## 100. 볼보 S60 3세대 국내 B5 연식 계보 재분리 (2026-08-15)

- 기존 S60 3세대 자동 코드 7개는 T5·B5와 R-Design·Inscription·Ultimate·Ultra를 모두 2019-04~2024-12로 열어 실제 국내 도입 시점과 트림 명칭을 과매칭하고 있었다. 원래 영구키 의미와 이력은 유지한 채 원천 시트 5,233~5,239행을 `제외/1차확인`으로 전환했다.
- 볼보자동차코리아 공식 2021-10 브로슈어에서 B5 Inscription, 1,969cc, 250마력, 5인승을 확인해 2021-10~2022-07 구간으로 복원했다. 공식 2022-08-01 MY2023 옵션표와 MY2024 브로슈어에서는 B5 Ultimate Bright 국내 단일 조합을 확인해 2022-08~2024-12 구간으로 분리했다.
- 2023-10 S60 Dark Edition은 Ultimate Dark 기반 외관·스포츠 서스펜션 한정 패키지다. 별도 엔진·구동계가 아니므로 독립 영구코드로 과분할하지 않고 B5 Ultimate 계보의 검색 별칭으로 보존했다. 국내 공식 시작월 근거가 아직 부족한 초기 T5는 추정 신규 발급하지 않았다.
- 신규 마스터 `mf-017.md-009.sm-s60-b5-korea__s60-b5-fwd` 아래 B5 Inscription·B5 Ultimate 2개를 중고차 수동확인 등급으로 발급하고 원천 시트 `차종마스터!A7428:AD7429`에 추가했다. 재읽기에서 기존 7행과 신규 2행 모두 30열, 상태·영구키·기간·숫자형 1,969cc/5인승·2WD 및 공식 링크를 확인했다.
- 로컬 산출물은 총 5,175건(자동 2,759 / 수동 631 / 차단 1,785), 생성기 누적 보강 행은 378개다. `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 모두 통과했다.

## 101. 볼보 C40 Recharge MY2024 국내형 경계 복원 (2026-08-15)

- 기존 C40 Recharge 코드는 글로벌 데뷔 시점인 2021-10을 국내 시작월처럼 사용해 `Twin Ultimate 78kWh AWD`를 2024-06까지 하나로 열고 있었다. 국내 출시·출고 전 기간을 자동 과매칭할 수 있어 원래 키 의미와 이력을 보존한 채 원천 시트 5,285행을 `제외/1차확인`으로 전환했다.
- 볼보자동차코리아 공식 2023-08-14 출시자료에서 2024년식 C40 Recharge가 Recharge Twin 단일 트림, 78kWh 배터리, 듀얼모터 AWD, 408마력, 5인승으로 국내 출시됐음을 확인했다. 공식 출시월보다 앞당기지 않은 2023-08~2024-06 구간으로 별도 영구코드를 발급했다.
- Volvo의 C40 Recharge→EC40 공식 명칭 변경은 확인했다. 그러나 한국 판매 개시일과 국내 인증 트림을 직접 입증하는 공식 자료가 부족해 기존 C40 코드를 현행으로 연장하거나 EC40 국내 판매 코드를 추정 발급하지 않았다.
- 신규 마스터 `mf-017.md-024.sm-c40-my24-korea__c40-recharge-twin-awd` 1개를 중고차 수동확인 등급으로 원천 시트 `차종마스터!A7430:AD7430`에 추가했다. 재읽기에서 기존·신규 행 모두 30열, 상태·영구키·기간·숫자형 순번/5인승/78kWh·AWD 및 공식 링크를 확인했다.
- 로컬 산출물은 총 5,176건(자동 2,758 / 수동 632 / 차단 1,786), 생성기 누적 보강 행은 379개다. `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 모두 통과했다.

## 102. 볼보 XC40 Recharge 국내 판매 확인 경계 복원 (2026-08-15)

- 기존 `Twin`·`Twin Ultimate` 두 코드는 글로벌 공개 시점인 2021-10부터 국내 판매된 것처럼 열려 있어 국내 출시 시점과 트림 분기를 과매칭했다. 영구 키의 의미와 이력을 바꾸지 않고 원천 시트 5,204~5,205행을 `제외/1차확인`으로 전환했다.
- 볼보자동차코리아 공식 2023-02-01 자료에서 당시 XC40 Recharge의 국내 판매를 확인했고, 볼보코리아 2022년형 지원 문서를 교차 확인했다. 이에 따라 국내 판매가 직접 확인되는 2023-02보다 시작일을 앞당기지 않고, `Recharge Twin`·78kWh·듀얼모터 AWD·5인승 조합 하나만 2024-06까지 복원했다.
- 공식 국내 자료로 입증되지 않은 `Ultimate` 별도 트림 분기와 이후 글로벌 82kWh 업데이트는 추정 발급하지 않았다. 신규 마스터 ID는 `mf-017.md-023.sm-xc40-recharge-korea__xc40-recharge-twin-awd`이며 원천 시트 `차종마스터!A7431:AD7431`에 추가했다.
- 원천 시트를 재조회해 기존 차단 2행과 신규 1행의 A:AD 30개 셀, 숫자형 78kWh·5인승, 공식 근거 링크를 확인했다. 로컬 산출물은 총 5,177건(자동 2,756 / 수동 633 / 차단 1,788), 생성기 누적 보강 행은 380개다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 모두 통과했다.

## 103. 르노코리아 QM6 2025 국내 현행 트림 경계 복원 (2026-08-15)

- 최신 공급사 재고 341대는 차종마스터에 341대 모두 매칭되어 직접 결손은 0건이었다. 다만 QM6의 기존 `mf-005.md-011.sm-hzg` 자동 코드 10개는 2019-06부터 LE·RE·LE 시그니처·RE 시그니처·프리미에르를 모두 현재까지 열어 종료된 트림까지 현행 차량에 과매칭하는 상태였다.
- 르노코리아 공식 2025-02 가격표에서 승용 QM6 국내 판매 조합을 `2.0 LPe LE`, `2.0 LPe RE`, `2.0 GDe RE`로 확인했다. 공식 제원은 LPe 1,998cc·GDe 1,997cc, 2WD, 5인승이며 2026-07 공식 차량 비교 페이지에서 LPe RE와 GDe RE의 실제 판매 노출을 교차 확인했다.
- 기존 10개 영구키의 의미와 이력은 변경하지 않고 원천 시트 3,072~3,081행을 `제외/1차확인`으로 전환했다. 신규 마스터 `mf-005.md-011.sm-hzg-qm6-2025-korea__qm6-gde-lpe-2wd` 아래 세 조합은 직접 확인되는 2025-02보다 시작월을 앞당기지 않고 수동확인 코드로 발급했다.
- 신규 3행을 원천 시트 `차종마스터!A7432:AD7434`에 추가하고 기존 차단 10행과 함께 재조회했다. 모든 신규 행은 A:AD 30열이며 순번·1,997/1,998cc·2.0L·5인승이 숫자형으로 보존되고 공식 가격표 링크와 근거 메모가 일치한다.
- 로컬 산출물은 총 5,180건(자동 2,746 / 수동 636 / 차단 1,798), 생성기 누적 보강 행은 383개다. `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 모두 통과했다.

## 104. 르노코리아 SM6 2025 후기형 국내 계보 복원 (2026-08-15)

- 기존 `mf-005.md-010.sm-lfd` 자동 코드 14개는 2020-07 이후 TCe 300의 LE·인스파이어·프리미에르, LPe의 필·SE·LE·RE, TCe 260의 필·SE·SE Plus·LE·RE·프리미에르 등을 모두 현재까지 동시에 열어 실제 연식별 트림 체계를 과매칭했다.
- 르노코리아 공식 2025-06 가격표에서 후기형 국내 판매 조합을 `TCe 260 필 [必; Feel]`, `TCe 300 INSPIRE`, `2.0 LPe 필 [必; Feel]`로 확인했다. 공식 2025-01 브로슈어에서 각각 1,332cc·1,798cc·1,998cc와 5인승을 교차 확인했고, 2022-09 공식 출시자료에서 필 트림이 기존 SE·LE를 통합 대체하며 TCe 260과 LPe에 적용됨을 확인했다.
- 기존 14개 영구키의 의미와 이력은 바꾸지 않고 원천 시트 3,097~3,110행을 `제외/1차확인`으로 전환했다. 신규 마스터 `mf-005.md-010.sm-lfd-sm6-2025-korea__sm6-tce-lpe` 아래 세 조합은 판매 종료일을 추정하지 않고 공식 2025-06 가격표와 2025-09 구매혜택에서 판매 대상이 겹치는 2025-06~2025-09 구간만 수동확인 이력으로 발급했다.
- 신규 행은 원천 시트 `차종마스터!A7435:AD7437`에 추가했다. 기존 14행과 신규 3행을 재조회해 각 행 A:AD 30열, 숫자형 1,332/1,798/1,998cc·1.3/1.8/2.0L·5인승, TCe 터보와 LPe 비터보, 2WD 및 공식 근거 링크를 확인했다.
- 로컬 산출물은 총 5,183건(자동 2,732 / 수동 639 / 차단 1,812), 생성기 누적 보강 행은 386개다. `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 모두 통과했다.

## 105. KG모빌리티 코란도 C300 2025 후기형 계보 복원 (2026-08-15)

- 기존 `mf-004.md-011.sm-c300` 자동 코드 18개는 디젤 1.6의 C3+·딜라이트·딜라이트+·플러스·판타스틱과 가솔린 1.5의 C3+·프라임·플러스·C7·R+ 등을 2019-02부터 모두 현재까지 열어 후기형 차량에 초기 연식 트림과 단종 디젤을 과매칭했다. 별도 전기차 계보인 `sm-e100`은 이번 차단 범위에서 제외했다.
- KG모빌리티 공식 2024-11 가격표에서 후기형 국내 파워트레인을 1,497cc 가솔린 1.5 T-GDI·AISIN 6단 자동으로 확인했고 C5·C5 Plus·C7·Black Edition 트림을 확인했다. C7에는 4WD 시스템 선택이 직접 명시돼 2WD와 AWD를 분리했으며, Black Edition은 4WD 연결 근거가 부족해 2WD만 발급했다.
- 기존 18개 영구키의 의미와 이력은 변경하지 않고 원천 시트 2,525~2,542행을 `제외/1차확인`으로 전환했다. 신규 마스터 `mf-004.md-011.sm-c300-korando-2025-korea__korando-1.5t` 아래 5개 조합은 공식 가격표 시점 2024-11부터 공식 온라인 스토어 확인 시점 2025-06까지만 수동확인 이력으로 발급했다.
- 신규 행은 원천 시트 `차종마스터!A7438:AD7442`에 추가했다. 기존 18행과 신규 5행을 재조회해 각 행 A:AD 30열, 숫자형 1,497cc·1.5L·5인승, 터보, 2WD/AWD 및 공식 근거 링크를 확인했다.
- 로컬 산출물은 총 5,188건(자동 2,714 / 수동 644 / 차단 1,830), 생성기 누적 보강 행은 391개다. `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 모두 통과했다.

## 106. KG모빌리티 코란도 EV 73.4kWh 국내 계보 복원 (2026-08-15)

- 기존 `mf-004.md-011.sm-e100::v01::t01` 코드는 2022-02 코란도 이모션 초기 계보의 `E5`를 73.4kWh 사양 및 코란도 EV 명칭과 구분하지 않은 채 현재까지 자동으로 열어, 2024년 코란도 EV E5와 명칭·배터리·기간이 충돌했다.
- KG모빌리티 공식 2024-09 코란도 EV 가격표에서 E3·E5 두 국내 트림, 73.4kWh 리튬인산철 배터리와 152.2kW 모터를 확인했다. 공식 2024-12 종합가격표에서 2WD·73.4kWh·152.2kW 제원을 교차 확인했다.
- 기존 E5 영구키의 의미와 이력은 변경하지 않고 원천 시트 2,524행을 `제외/1차확인`으로 전환했다. 신규 마스터 `mf-004.md-011.sm-e100-korando-ev-2024-korea__korando-ev-73k` 아래 E3·E5는 공식 가격표가 확인되는 2024-06보다 시작을 앞당기지 않고, 공식 온라인 스토어 확인 시점 2025-06까지 수동확인 이력으로 발급했다.
- 신규 행은 원천 시트 `차종마스터!A7443:AD7444`에 추가했다. 기존 1행과 신규 2행을 재조회해 각 행 A:AD 30열, 숫자형 73.4kWh·5인승, 2WD, 공식 근거 링크를 확인했다.
- 로컬 산출물은 총 5,190건(자동 2,713 / 수동 646 / 차단 1,831), 생성기 누적 보강 행은 393개다. `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 모두 통과했다.

## 103. 전자계약 A4 PDF 미리보기·다운로드 통합 (2026-08-15)

- 계약진행의 `A4 미리보기`가 별도 HTML 출력 화면이 아니라 `/esign/preview/[contractCode]?view=a4`의 통합 미리보기로 열리도록 연결했다.
- 통합 미리보기는 서버가 생성한 계약서·약관 PDF를 브라우저 기본 PDF 뷰어에 표시하며, 상단 `PDF 다운로드`로 동일 파일을 저장할 수 있다. 중복되는 별도 A4 출력 버튼은 제거했다.
- 계약서 원본 HTML에 자동 삽입되던 `A4 PDF 저장` 버튼도 제거해 출력·다운로드 진입점을 통합 미리보기 한곳으로 고정했다.
- 로컬 계약진행 화면에서 발송 전 미리보기의 `모바일 미리보기`·`A4 미리보기` 노출과 순서를 확인했다. `npx tsc --noEmit`, `scripts/sim-freepass-esign.mts`, `npm run check:fonts`, 대상 파일 `git diff --check`를 모두 통과했다.

## 107. 현대 베뉴 QX1 2025 국내 트림 경계 복원 (2026-08-15)

- 기존 `mf-001.md-059.sm-qx1` 자동 코드 5개는 Smart·Modern·Modern Plus·Premium·Flux를 모두 2019-07부터 현재까지 같은 기간으로 열어, 연식별로 바뀐 트림 체계를 현행 렌트·구독 차량에 과매칭하고 있었다.
- 현대자동차 공식 2025 베뉴 가격표에서 현 모델 출시일 2025-04-02, Smart·Premium·FLUX 3개 트림과 공통 스마트스트림 가솔린 1.6·IVT를 확인했다. 공식 현행 카탈로그에서 1,598cc·2WD·5인승을 교차 확인했다.
- 기존 5개 영구키의 의미와 이력은 변경하지 않고 원천 시트 1,960~1,964행을 `제외/1차확인`으로 전환했다. 신규 마스터 `mf-001.md-059.sm-qx1-venue-2025-korea__venue-1.6-ivt` 아래 3개 트림은 공식 출시월 2025-04보다 시작을 앞당기지 않고 수동확인 코드로 발급했다.
- 신규 행은 원천 시트 `차종마스터!A7445:AD7447`에 추가했다. 기존 5행과 신규 3행을 재조회해 신규 각 행 A:AD 30열, 숫자형 순번·1,598cc·1.6L·5인승, 자동·2WD 및 공식 근거 링크를 확인했다.
- 로컬 산출물은 총 5,193건(자동 2,708 / 수동 649 / 차단 1,836), 생성기 누적 보강 행은 396개다. `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 모두 통과했다.

## 108. 기아 디 올 뉴 셀토스 SP3 국내 출시 경계·4WD 복원 (2026-08-15)

- 기존 `mf-002.md-064.sm-sp3` 코드 10개는 디 올 뉴 셀토스를 2025-07부터 국내 판매된 것으로 1년 일찍 열었다. 가솔린 터보 2WD와 하이브리드 2WD만 자동 코드로 두고 공식 판매 조합인 가솔린 터보 4WD는 누락했으며, 비터보 가솔린 `트렌드`와 빈 트림 코드도 포함했다.
- 기아 공식 국내 가격 페이지에서 2026-07-01 기준 1.6 가솔린 터보·1.6 하이브리드와 트렌디·프레스티지·시그니처·X-Line 네 트림을 확인했다. 각 가솔린 터보 트림에 전자식 4WD 선택이 가능하며, 공식 제원에서 가솔린 1,598cc·8단 자동·2WD/4WD와 하이브리드 1,580cc·6단 DCT·2WD·5인승을 교차 확인했다.
- 기존 10개 영구키는 의미와 이력을 변경하지 않고 원천 시트 656~665행을 `제외/1차확인`으로 전환했다. 신규 마스터 `mf-002.md-064.sm-sp3-seltos-2026-korea__all-new-seltos` 아래 가솔린 2WD 4개·가솔린 4WD 4개·하이브리드 2WD 4개, 총 12개를 공식 가격 기준일보다 앞당기지 않은 수동확인 코드로 발급했다.
- 신규 행은 원천 시트 `차종마스터!A7448:AD7459`에 추가했다. 재조회 결과 12행 모두 A:AD 30열이며 영구키·순번, 숫자형 1,598/1,580cc·1.6L·5인승, 터보 여부, 2WD/4WD와 공식 가격·제원 링크가 일치했다.
- 로컬 산출물은 총 5,205건(자동 2,700 / 수동 661 / 차단 1,844), 생성기 누적 보강 행은 408개다. `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 모두 통과했다.

## 109. 기아 더 뉴 니로 SG2 HEV 부분변경 시점 복원 (2026-08-15)

- 기존 `mf-002.md-061.sm-sg2` 자동 코드 3개는 더 뉴 니로 HEV 부분변경을 2025-04부터 현행으로 열었다. 그러나 기아 공식 2026 상품계획은 Niro HEV PE를 2026년 일정으로 명시하고, 현행 공식 가격표·카탈로그는 2026-05 자료여서 1년 이상 조기 과매칭되는 상태였다.
- 기아 공식 2026-05 가격표에서 The new Niro HEV의 트렌디·프레스티지·시그니처를 확인했다. 현행 공식 제원으로 스마트스트림 G1.6 하이브리드·1,580cc·6단 DCT·2WD·5인승을 교차 확인했다.
- 잘못 일찍 열린 기존 3개 영구키는 원천 시트 730~732행을 `제외/1차확인`으로 전환했다. 2022-01 국내 출시된 디 올 뉴 니로 HEV 기존 4개는 736~739행의 생산종료를 부분변경 직전인 2026-04로 닫았으며, 동일 부분변경 근거가 없는 니로 EV 코드는 변경하지 않았다.
- 신규 마스터 `mf-002.md-061.sm-sg2-niro-pe-2026-korea__the-new-niro-hev` 아래 3개 트림을 원천 시트 `차종마스터!A7460:AD7462`에 추가했다. 재조회에서 각 행 A:AD 30열, 영구키·기간, 숫자형 1,580cc·1.6L·5인승, 2WD와 공식 가격·제원·상품계획 링크를 확인했다.
- 로컬 산출물은 총 5,208건(자동 2,697 / 수동 664 / 차단 1,847), 생성기 누적 보강 행은 411개다. `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `sim-sheet-merge` 170/170, `npm run check:fonts`, `git diff --check`를 통과했다. 최초 `tsc`는 오래된 `.next/types` 경로 목록으로 TS6053이 발생해 `npx next typegen`으로 공식 라우트 타입을 재생성했고, 재실행한 `npx tsc --noEmit`은 통과했다.
## 110. 기아 레이 EV 35.2kWh 차체·인승 계보 복원 (2026-08-15)

- 기존 35.2kWh 레이 EV 영구코드 3건은 시작 연월이 `2022-09`로 잡혀 있었고, 승용 Light/Air 및 2인승 밴 Air만 있어 국내 렌트·구독 재고의 차체 구분이 불완전했다.
- 기아 공식 헤리티지의 2세대 레이 EV 2023년 출시, 2023년 9월 공식 렌터카 카탈로그, 현행 공식 가격·제원 페이지를 대조했다. 현행 구성은 35.2kWh 전륜구동 기준 `4인승 승용 / 2인승 밴 / 1인승 밴` 각각 `Light / Air`다.
- 의미와 기간이 부정확한 기존 키 `mf-002.md-058.sm-tam::v02::t01`, `mf-002.md-058.sm-tam::v02::t02`, `mf-002.md-058.sm-tam__van::v02::t01`은 삭제하거나 재사용하지 않고 차단했다. 원천대장 차단 행은 432~433, 436이다.
- 신규 6개 조합을 `mf-002.md-058.sm-tam-ray-ev-2023-korea__ray-ev-35k` 계보 아래 2023-09 시작으로 발급했다. 원천대장 `A7463:AD7468`을 재조회해 행별 30셀, 영구키, 숫자형 인승 `4/4/2/2/1/1`, 배터리 35.2kWh 및 공식 링크를 확인했다.
- 로컬 산출물은 총 5,214건(자동 2,694 / 수동 670 / 차단 1,850), 현행 렌트 핵심 생성분은 누적 417건이다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `sim-sheet-merge`(170/170), `tsc --noEmit`, `check:fonts`, `git diff --check`를 모두 통과했다.
## 111. Tesla 리프레시·리리프레시·Ryzen·Model Y L 분류 계약 고정 (2026-08-15)

- 로컬 산출물의 Tesla 영구코드 66행(수동 배정 35 / 차단 31)을 전수 추출해 모델·세부모델·생산기간·구동·인승·별칭을 다시 감사했다.
- Tesla 대한민국 현행 페이지에서 Model 3의 `Model 3 / Model 3 Premium / Model 3 Performance`, Model Y의 `Model Y Premium / Model Y L` 구성을 재확인했다. 한국 공인연비는 Model 3 Standard RWD·Premium Long Range RWD·Performance와 Model X AWD 5·6·7인승·Plaid 6인승을 뒷받침한다.
- Model Y L 원천대장 행 `A7136:AD7136`을 CellData로 재조회했다. 2026-07 시작, AWD, 숫자형 6인승, 별도 롱휠베이스 마스터 ID, `3열 6인승`·`Long Wheelbase`를 포함한 별칭 8개 및 공식 근거 링크가 모두 보존되어 있다. 현행 Tesla 매뉴얼은 Model Y Premium 4,790mm·휠베이스 2,890mm와 Model Y L 4,976mm·휠베이스 3,040mm를 별도 차체 제원으로 제공한다.
- Tesla 공식 서비스 문서의 2025-06 Model S·Model X 업데이트는 2023 Palladium과 2025+ 후속 업데이트의 기간 경계로 유지했다. 반면 Ryzen·Intel Atom은 실차의 추가 차량 정보에서 확인하는 인포테인먼트 하드웨어이며 세대·연식·트림과 일대일 대응하지 않으므로 영구 차종코드 축에서 계속 제외한다.
- `scripts/sim-tesla-generation-contract.mts`를 추가했다. 구형 합본 코드 차단, Highland 2024-04, New Model Y 2025-02, Model Y L 2026-07·6인승 별도 차체, Model S/X 2025-05→2025-06 경계, 국내 미입증 Model X Plaid 5인승 차단, CPU 명칭의 분류축 유입 금지를 66행 전체에서 검증한다.
- 신규 Tesla 계약 검사, `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 모두 통과했다. 최초 폰트 검사는 Windows `uv_os_get_passwd ENOMEM` 환경 오류가 발생했으나 저장소의 Node 사용자정보 우회 설정으로 재실행해 폰트 드리프트 0을 확인했다.
## 112. 기아 The 2027 Ray 가솔린 승용·1인승·2인승 밴 분리 (2026-08-15)

- 기존 2022-09 시작 레이 가솔린 코드는 승용 스탠다드·트렌디·프레스티지·시그니처·그래비티·시그니처 X라인과 2인승 밴 프레스티지·프레스티지 스페셜을 모두 현재까지 열어 두었고, 렌트·배송 수요가 큰 가솔린 1인승 밴은 누락돼 있었다.
- 기아 공식 The 2027 Ray 가격 페이지의 2026-08-01 기준 구성을 확인했다. 승용은 트렌디·프레스티지·시그니처·X-Line, 밴은 1인승과 2인승 각각 트렌디·프레스티지·프레스티지 스페셜이다. 공식 제원은 카파 1.0 가솔린 998cc·4단 자동과 1·2인승 밴을 명시한다.
- 기존 승용 5개와 2인승 밴 2개 영구코드는 의미를 바꾸지 않고 2026-07에서 닫았다. 2022-09부터 X-Line으로 합본된 `mf-002.md-058.sm-tam::v01::t06`은 당시 공식 가격표와 충돌하므로 원래 키·의미·이력을 보존한 채 `제외/1차확인`으로 차단했다.
- 신규 마스터 `mf-002.md-058.sm-tam-ray-gas-2027-korea__the-2027-ray` 아래 승용 4개, 1인승 밴 3개, 2인승 밴 3개 등 총 10개 영구키를 2026-08 시작으로 발급했다.
- 원천대장 기존 행 426~431·434~435를 보정하고 신규 `A7469:AD7478`을 추가했다. 재조회 결과 신규 행별 A:AD 30셀, 숫자형 순번·998cc·1.0L·인승, 2WD, 공식 가격·제원 링크가 일치했다.
- 로컬 산출물은 총 5,224건(자동 2,693 / 수동 680 / 차단 1,851), 현행 렌트 핵심 생성분 누적 427건이다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, Tesla 세대 계약, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 모두 통과했다.
## 113. 기아 The 2027 Morning 승용·GT-Line·2인승 밴 연식 경계 복원 (2026-08-15)

- 기존 현행 모닝 JA 코드는 2023-07 시작 승용 트렌디·프레스티지·시그니처·GT라인과 2인승 밴 프레스티지 5개만 현재까지 열려 있어, The 2027 Morning의 공식 연식 경계와 밴 트렌디가 누락돼 있었다.
- 기아 공식 2026-05-15 출시자료에서 The 2027 모닝이 2026-05-18 판매를 시작하며 승용 트렌디·프레스티지·시그니처·GT 라인, 밴 트렌디·프레스티지로 구성됨을 확인했다. 현행 공식 가격·제원으로 스마트스트림 G1.0 998cc·4단 자동·승용 5인승·밴 2인승을 교차확인했다.
- 기존 5개 영구키는 의미와 이력을 변경하지 않고 원천대장 462~466행에서 2026-04로 종료했다.
- 신규 마스터 `mf-002.md-013.sm-ja-morning-2027-korea__the-2027-morning` 아래 승용 4개와 2인승 밴 2개, 총 6개 영구키를 2026-05 시작으로 발급하고 원천대장 `A7479:AD7484`에 추가했다.
- CellData 재조회로 신규 행별 A:AD 30셀, 숫자형 순번·998cc·1.0L·5/2인승, 2WD, 공식 출시·가격·제원 링크를 확인했다.
- 로컬 산출물은 총 5,230건(자동 2,693 / 수동 686 / 차단 1,851), 현행 렌트 핵심 생성분 누적 433건이다.
- 마스터·영구키·Tesla 세대 계약과 `sim-sheet-merge` 170/170, `check:fonts`, `git diff --check`는 통과했다. 전체 `tsc --noEmit`은 이번 차종 작업과 무관한 사용자 수정 파일 `components/ProductMoreMenu.tsx`에서 삭제된 `ensureRoom` import를 89행이 계속 참조해 TS2304로 실패했다. 해당 사용자 변경은 보존했으며 차종 생성 스크립트 자체는 실행·동기화에 성공했다.

## 114. 계약 공급사 출고가능 대수·파트너 계약정책 일원화 (2026-08-15)

- 계약 작성의 회사 선택 목록에 각 공급사의 `출고가능 N대`를 표시하고, 선택한 공급사 소속 재고 중 즉시 출고 가능한 차량만 집계하도록 검증했다.
- 프리패스 계약 사용 여부를 파트너사 속성으로 승격했다. 명시값이 없는 기존 공급사는 연결 정책의 종전 값을 호환 읽기하며, 파트너사에서 `미사용`으로 정하면 계약 작성 목록과 서버 발송 게이트에서 제외된다.
- 파트너사관리 화면에 계약 사용 여부, 출고가능 차량 수, 연결 정책 수·발송가능 정책 수를 표시하고 회사별 정책 추가·수정 진입점을 모았다. 정책 화면은 세부 계약조건 편집 역할만 유지하고, 상단 메뉴의 독립 정책관리 진입점은 파트너사관리로 통합했다.
- 재고관리는 차량 재고와 차량-정책 연결만 유지해 재고 업무에 집중시키고, 회사의 계약 사용 여부와 정책 내용 관리는 파트너사관리에서 처리하도록 책임을 분리했다.
- 로컬 브라우저에서 계약 회사 목록의 손오공 `출고가능 26대` 등 실데이터 표기와 파트너사관리의 사용 상태·정책 현황을 확인했다.
- `npx tsc --noEmit`, `npm run build`, `npm run check:fonts`, `scripts/sim-freepass-esign.mts`, `scripts/sim-esign-vehicle-selection.mts`, 대상 파일 `git diff --check`를 모두 통과했다.

## 115. 기아 The 2027 K5 자가용·렌터카 LPG 계보 분리 (2026-08-15)

- 기아 공식 현행 가격 페이지와 2026-08 가격표에서 The 2027 K5의 2026-07 출시 계보를 확인했다. 자가용은 하이브리드 4개, 2.0 가솔린 5개, 1.6 가솔린 터보 4개, 2.0 LPG 3개로 총 16개 조합이며 렌터카 2.0 LPG는 트렌디·프레스티지 2개 조합이다.
- 기존 `sm-dl3` 현행 12개 코드와 The 2026 K5 베스트 셀렉션 3개 코드는 영구키와 과거 의미를 변경하지 않고 생산종료 `2026-06`, 연식종료 `2026`으로 닫았다. 잘못 조기 발급되어 이미 차단된 베스트 셀렉션 3개 키는 차단 이력을 그대로 보존했다.
- 신규 자가용 마스터 `mf-002.md-001.sm-dl3-pe-my2027__k5-2027` 아래 16개 조합과 렌터카 전용 마스터 `mf-002.md-001.sm-dl3-pe-my2027-rent__k5-2027-rental` 아래 2개 조합을 발급했다. 렌터카 계보는 세부모델과 파워트레인에 `렌터카`를 명시해 자가용 LPG 프레스티지와 동일명 트림 충돌을 방지했다.
- 원천대장 기존 15개 행을 갱신하고 `차종마스터!A7485:AD7502`에 신규 18개 행을 추가했다. 재조회에서 30열 전체, 영구키·순번·1,999/1,598cc·2WD·5인승·터보 표기·공식 근거 URL을 확인했다.
- 로컬 산출물은 총 5,248건(자동 2,693 / 수동 704 / 차단 1,851), 생성기 누적 보강 451행이다. 신규 `sim-k5-2027-contract.mts`와 마스터·영구키·Tesla 계약, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 모두 통과했다.

## 116. 현대 2026 쏘나타 디 엣지 일반판매·렌터카 LPG 분리 (2026-08-15)

- 현대 공식 2026 쏘나타 디 엣지 가격표의 현 모델 출시일 `2025-09-29`와 파워트레인·트림 구성을 감사했다. 기존 마스터에는 2026 신규 S 트림 3개가 이미 별도 발급돼 있었으나, 가솔린 2.0 인스퍼레이션과 일반판매용 LPG 2.0 프리미엄이 누락됐고 렌터카 Business 1·Business 2가 과거 일반 `비즈니스` 코드와 구분되지 않았다.
- 신규 일반판매 보강 마스터 `mf-001.md-018.sm-dn8-my2026-gap__sonata-the-edge-2026` 아래 가솔린 2.0 인스퍼레이션·LPG 2.0 프리미엄 2개 조합을 발급했다. 렌터카 전용 마스터 `mf-001.md-018.sm-dn8-my2026-rent__sonata-the-edge-rental` 아래 LPG 2.0 렌터카 Business 1·Business 2를 별도 발급했다.
- 기존 `mf-001.md-018.sm-dn8::v03::t03` LPG 비즈니스 영구키는 의미와 과거 이력을 바꾸지 않고 생산종료 `2025-08`, 연식종료 `2025`로 닫았다. 2026 렌터카 코드는 세부모델과 파워트레인 모두에 `렌터카`를 명시해 일반판매 LPG와 동일명·유사명 오매칭을 막았다.
- 원천대장 1개 기존 행을 보정하고 `차종마스터!A7503:AD7506`에 신규 4개 행을 추가했다. CellData 재조회에서 각 행의 A:AD 30열, 숫자형 순번·1,999cc·2.0L·5인승, 2WD, 터보 `아니오`, 공식 가격표 URL을 확인했다.
- 로컬 산출물은 총 5,252건(자동 2,693 / 수동 708 / 차단 1,851), 생성기 누적 보강 455행이다. 신규 `sim-sonata-2026-contract.mts`, K5·Tesla 전용 계약, 마스터·영구키 계약, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 모두 통과했다.

## 117. 회원관리·파트너사관리 대상 분리 (2026-08-15)

- 사람과 회사를 한 관리축처럼 보이던 용어를 분리했다. 회원관리는 `영업자·공급사 직원`, 파트너사관리는 `영업채널·공급사`를 1차 분류로 사용한다.
- 영업관리자·공급관리자 같은 내부 역할은 회원구분과 섞지 않고 별도 권한으로 표시한다. 기존 Firebase 역할값은 변경하지 않아 인증·데이터 범위 호환을 유지했다.
- 내부 운영자 계정과 운영사는 각각 회원·파트너사 목록에서 제외하고, 미분류 레거시는 `분류 필요`로 남겨 정리할 수 있게 했다.
- 탭 전환 시 이전 목록을 즉시 비워 회원이 잠시 파트너사처럼 보이는 비동기 전환 오류를 막았다.
- 로컬 브라우저에서 회원관리 161건의 `영업자·공급사 직원`, 파트너사관리 36건의 `영업채널·공급사` 분류와 운영자·운영사 제외를 확인했다.
- `npx tsc --noEmit`, `scripts/sim-freepass-esign.mts`, `npm run check:fonts`, 대상 파일 `git diff --check`를 모두 통과했다.

## 118. 현대 2026 아반떼 LPi 일반판매·렌터카 계보 분리 (2026-08-15)

- 기존 CN7 현행 코드에는 용도 표기가 없는 LPG 스마트 1건만 있어, 일반판매 LPi 모던·인스퍼레이션과 렌터카 전용 스마트·모던을 구별할 수 없었다. 기존 스마트 영구키는 의미와 이력을 바꾸지 않고 일반판매 코드로 보존했다.
- 현대자동차 공식 2026 아반떼 가격표에서 신모델 출시일 `2025-04-15`, 일반판매 LPi `스마트·모던·인스퍼레이션`, 렌터카 LPi `스마트·모던` 구성을 확인했다. 모두 1,591cc·6단 자동·2WD·5인승 규격으로 교차 확인했다.
- 일반판매 결손 마스터 `mf-001.md-019.sm-cn7-lpi-2026__avante-lpi-2026` 아래 모던·인스퍼레이션 2건과 렌터카 전용 마스터 `mf-001.md-019.sm-cn7-lpi-rent-2026__avante-lpi-rental-2026` 아래 스마트·모던 2건을 수동확인 코드로 발급했다.
- 원천대장 `차종마스터!A7507:AD7510`에 신규 4행을 추가하고 CellData로 A:AD 30열, 숫자형 순번·1,591cc·1.6L·5인승, 2WD, 공식 가격표 URL을 재조회해 확인했다.
- 로컬 산출물은 총 5,256건(자동 2,693 / 수동 712 / 차단 1,851), 생성기 현행 렌트 핵심 보강 누적 459행이다. 신규 `sim-avante-2026-contract.mts`와 쏘나타·K5·Tesla 전용 계약, 차종마스터·영구키 계약, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 모두 통과했다.

## 119. 기아 2026 카니발 X-Line 9인승 계보 보강 (2026-08-15)

- 기존 보강 마스터는 카니발 X-Line을 7인승 전용으로 기록해 가솔린 3.5·하이브리드 1.6T 각각 1건만 발급했다. 그러나 기아 공식 2026년 7·8월 국내 가격표는 X-Line 선택품목표에 7인승과 9인승을 모두 명시하고, 두 인승 모두 3.5 가솔린과 1.6 터보 하이브리드를 제공한다.
- 기존 7인승 영구키 `v01::t01`, `v02::t01`의 의미와 2026-05 시작 이력은 그대로 보존했다. 동일 마스터 아래 9인승 가솔린 `v01::t02`와 하이브리드 `v02::t02`를 2026-07 시작 수동확인 코드로 추가해 인승 오매칭을 막았다.
- 원천대장 `차종마스터!A7511:AD7512`에 신규 2행을 추가했다. CellData 재조회로 A:AD 30열, 숫자형 순번·3,470/1,598cc·3.5/1.6L·9인승, 터보 아니오/예, 2WD, 공식 가격표 링크와 기존 표 서식을 확인했고 네이티브 테이블 범위도 7,512행까지 확장됐다.
- 로컬 산출물은 총 5,258건(자동 2,693 / 수동 714 / 차단 1,851), 생성기 현행 렌트 핵심 보강 누적 461행이다. 신규 `sim-carnival-2026-xline-contract.mts`와 아반떼·쏘나타·K5·Tesla 전용 계약, 차종마스터·영구키 계약, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 모두 통과했다.

## 120. 현대 The new GRANDEUR GN11 출시일 경계 교정 (2026-08-15)

- GN11 신규 마스터 18건은 현대 공식 현행 가격 페이지의 `2026-07-01 기준`을 생산 시작일로 사용하고 있었다. 공식 The new GRANDEUR 가격표에는 모델 출시일이 `2026-05-14`로 별도 명시되므로, 7월은 가격 기준일일 뿐 출시·생산 시작 경계가 아니다.
- 가솔린 2.5 2WD 4트림, 가솔린 3.5 2WD·4WD 각 4트림, LPG 3.5 2WD 2트림, 하이브리드 1.6T 2WD 4트림의 영구키·순번·트림 의미는 변경하지 않고 생산 시작만 `2026-05`로 교정했다. 기존 잘못된 GN7 현행 코드는 계속 차단 보존한다.
- 원천대장 `차종마스터!P7319:P7336`의 생산시작과 `AC7319:AC7336`의 근거메모를 18행 모두 갱신했다. 재검색에서 두 범위 각각 18/18행이 `2026-05`와 `모델 출시일 2026-05-14`를 반환했고 기존 Noto Sans KR 9pt·교차 밴딩 표 서식도 보존됐다.
- 신규 `sim-grandeur-2026-contract.mts`로 GN11 18조합, 동력계 5종, LPG 2트림, 기타 동력계 4트림, 5인승, 터보·구동 규격과 출시 경계를 고정했다. 차종마스터·영구키 계약, 카니발·아반떼·쏘나타·K5·Tesla 전용 계약, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 모두 통과했다. 총계는 5,258건(자동 2,693 / 수동 714 / 차단 1,851)으로 코드 수 변경은 없다.
## 119. ERP UI·UX 공통규격 1차 전수검수 (2026-08-15)

- 사용자 요구사항을 기준으로 상품찾기·계약·전자계약·재고·정책·회원·파트너·설정의 버튼, 입력, 신규 등록, 목록/상세 문맥을 비교했다.
- 회원·파트너·재고·정책은 `WorkPage`·`CreateListRow`·`PageActions` 공통 원자를 사용하고 있음을 확인했다. 회원·파트너 신규 화면에서 취소·저장, 필수값 표시, 컨트롤 높이와 라운드가 동일하고 표시 문자열 잘림이 없음을 브라우저에서 확인했다.
- 재고 상단의 `전체매물` 표기를 `재고관리`로 통일하고, 회원·파트너 신규 상단 문맥을 `신규 회원`·`신규 파트너사`로 명확히 했다. `상품등록`·`계약등록`은 `상품 등록`·`계약 등록`으로 표기 규격을 맞췄다.
- 정책 안내문에 남아 있던 직접 폰트 크기 `12.5`를 `FS.sub` 토큰으로 교체했다. 계약·재고의 로딩 중 상단 상태명도 실제 메뉴명과 동일하게 정리했다.
- 설정 화면은 공통 `Btn`·`Input`·`Select`·`FilterChips`를 사용하며 데스크톱 실측상 입력 32px, 버튼 28/32px, radius 4px이고 글자 잘림이 없음을 확인했다.
- `npm run check:ui`, `npm run check:tokens`, `npm run check:fonts`, `npx tsc --noEmit`, `npm run build`가 모두 통과했다.
- 현재 Firebase 인증 서버에 별도 Playwright 세션을 주입한 모바일 전경로 감사는 로그인 화면으로 리다이렉트되어 앱 내부 측정에는 사용하지 않았다. 실제 로그인 세션을 공유하는 인앱 브라우저에서 데스크톱 핵심 화면을 검수했고, 모바일 규격은 공통 원자·정적 UI 계약 검사로 확인했다.
- 최종 판정: PASS — 데이터·권한·계약·정산 로직 변경 없음.

## 121. 회원 소속 파트너사 연결 (2026-08-15)

- 회원 수정·등록에서 소속을 직접 문자열로 적지 않고 파트너사 목록에서 선택하도록 변경했다.
- 영업자는 영업채널만, 공급사 직원은 공급사만 소속 후보로 표시한다. 선택 시 `company_code`·`company_name`을 함께 맞추고 영업자는 `agent_channel_code`도 동일 코드로 동기화한다.
- 소속이 없는 기존 회원은 회사명이 파트너사 표시명과 일치하면 수정 진입 시 자동으로 연결한다. 브라우저에서 공급사 직원 강태리의 기존 `손오공` 소속이 자동 선택되고 공급사 후보만 표시되는 것을 확인했다.
- 저장 시 회원구분에 맞는 소속 파트너사를 필수 검증해 영업자가 공급사에, 공급사 직원이 영업채널에 잘못 연결되는 것을 막았다.
- `npx tsc --noEmit`, `scripts/sim-freepass-esign.mts`, `npm run check:fonts`, 대상 파일 `git diff --check`를 모두 통과했다.

## 122. 정책 입력 정보구조 개선 (2026-08-15)

- 정책의 기존 4개 상위 탭은 유지하고, 각 탭 안의 긴 단일 폼을 실제 검토 순서에 맞는 소구간으로 재구성했다.
- 기본·심사는 정책 기본정보/심사 기준/운전자 자격, 계약조건은 운행/납부/운전자 범위/포함 서비스·정산, 보험은 대인·대물/운전자·무보험차/자차/보험·사고 지원, 전자계약은 주행·승계/해지·연체/반환·보관·회수/연장·인수·장치로 구분했다.
- 기존 분류에서 전자계약에 섞여 있던 지정 정비점·자차 처리 제외·대차 정책은 보험으로, 보험에 남아 있던 계약서 작성 방식은 전자계약으로 이동했다. 필드 키와 저장 데이터는 변경하지 않았다.
- 인앱 브라우저에서 신규 정책의 4개 탭, 구간 제목, 입력 컨트롤, 고정 취소·저장 동작과 화면 잘림을 확인했다.
- 회원관리 파일에 남아 있던 JSX 조건식 중복 괄호를 복구했다.
- `npx tsc --noEmit`, `npm run check:fonts`, `npm run check:ui`, `npm run check:tokens`, `npm run build`가 모두 통과했다.
- 최종 판정: PASS — 정책 데이터·계약·정산 엔진 변경 없음.

## 123. 상품찾기 자연어 조건 통합검색 (2026-08-15)

- 상품찾기 검색 색인에 운전자 최저연령, 만 21세 가능, 무보증, 보증금 분납, 면허경력 무관, 실제 판매기간과 색상 동의어를 추가했다.
- `21세 그랜저`, `무보증 그랜저`, `분납 36개월`처럼 차명과 조건을 공백으로 조합하면 모든 조건을 만족하는 상품만 표시한다. 판정은 카드 표시와 퀵필터가 사용하는 기존 상품 도메인 함수를 재사용한다.
- 데스크톱 검색 영역을 최대 720px까지 확장하고 예시 문구를 `21세 그랜저, 무보증 쏘나타`로 변경했다.
- 인앱 브라우저의 실제 447대 데이터에서 `21세 그랜저` 14대, `무보증 그랜저` 4대, `분납 36개월` 4대를 확인하고 검색창·정렬·보기 버튼의 겹침과 잘림이 없음을 시각 검수했다.
- `npx tsc --noEmit`, `npm run check:fonts`, `npm run check:ui`, `npm run check:tokens`, `npm run build`가 모두 통과했다.
- 최종 판정: PASS — 기존 필터·상품 데이터·계약 로직 변경 없음.

## 124. 상품찾기 문의·찜 UI 제거 (2026-08-15)

- 문의를 별도 ERP 업무축으로 두지 않는 현재 운영방향에 맞춰 상품찾기 상단의 문의·관심 숫자 버튼과 관심 패널을 제거했다.
- 웹 상세카드·상품 상세의 별표 버튼과 모바일 더보기의 관심 있음/해제 메뉴, 세부필터의 최근·관심 구간을 제거했다.
- 기존 문의 기록과 브라우저에 저장된 관심 데이터는 삭제하지 않았다. 상품 데이터·계약 진행·손님 전달·텍스트 복사 기능도 유지했다.
- 인앱 브라우저의 실제 447대 화면에서 문의·관심 버튼과 카드 별표가 보이지 않고, 넓어진 통합검색·정렬·구글시트·보기 전환이 겹치지 않는 것을 확인했다.
- React 검수에서 제거된 UI의 불필요한 문의 구독과 패널 렌더도 함께 제거했다.
- `npx tsc --noEmit`, `npm run check:fonts`, `npm run check:ui`, `npm run check:tokens`, `npm run build`가 모두 통과했다.
- 최종 판정: PASS — 저장 데이터 삭제 및 계약·정산 로직 변경 없음.

## 125. 상품찾기 검색창 비율·강조 조정 (2026-08-15)

- 데스크톱 검색 입력의 실표시 폭을 기존 약 540px에서 약 375px로 조정해 약 30% 축소했다. 필터 버튼과 정렬 드롭다운은 기존 규격을 유지했다.
- 검색 입력에 공용 선택 배경 토큰을 적용해 과하지 않은 옅은 강조를 주고, 포커스 시 기존 강조 테두리와 포커스 링은 유지했다.
- 공용 `SearchInput`에는 선택적으로 입력면 스타일만 전달하는 API를 추가해 다른 검색창에는 변화가 없도록 제한했다.
- 인앱 브라우저 1280px 화면에서 검색·정렬·구글시트·보기 전환의 간격과 글자 잘림이 없음을 확인했다.
- `npx tsc --noEmit`, `npm run check:fonts`, `npm run check:ui`, `npm run check:tokens`, `npm run build`가 모두 통과했다.

## 126. 최근·관심 기능 복구 및 문의만 제거 (2026-08-15)

- 운영 의도를 재확인해 상품찾기에서 제거 대상은 `문의`만으로 한정했다.
- 상단 최근·관심 숫자 버튼, 최근·관심 상품 패널, 카드·상세 별표, 모바일 관심 메뉴와 세부필터의 최근·관심 구간을 복구했다.
- 문의 숫자 버튼과 문의 상품 패널·문의 데이터 구독은 복구하지 않았다.
- 기존 로컬 관심 데이터를 보존한 상태에서 인앱 브라우저로 별표 선택 → `관심 1` 버튼 노출 → 관심 해제를 검증하고 검수용 변경은 원복했다.
- `npx tsc --noEmit`, `npm run check:fonts`, `npm run check:ui`, `npm run check:tokens`, `npm run build`가 모두 통과했다.

## 127. ERP 전체 UI·UX 2차 검수 (2026-08-16)

- 실제 로그인 인앱 브라우저에서 상품찾기·계약진행·정산확인·재고관리·정책관리·회원관리·설정을 순회하고 계약 상세까지 선택해 검수했다.
- `WorkPage`의 상단바와 목록 헤더에 같은 건수가 연속 표시되던 중복을 제거했다. 전체 KPI와 검색 결과 건수가 다를 때만 목록 헤더에 결과 건수를 남긴다.
- 재고 데이터 로딩 중 모든 상태·상품구분 필터가 `0`으로 보이던 오해 요소를 제거하고 `재고와 필터를 불러오는 중…` 상태를 표시한다.
- 설정의 카탈로그 공유 안내와 내 문서 안내에서 당분간 사용하지 않는 문의·상담 중심 문구를 현재 상품 안내·원본 업무 화면 표현으로 바꿨다.
- 가입화면의 약관 동의 행과 법률 문서 링크 터치영역을 44px로 보강하고 `가입 즉시 상담` 안내를 `상품찾기와 계약 업무`로 정리했다.
- 자동 픽셀 감사는 390×844와 1440×900 전 화면에서 가로 넘침 0을 확인했다. 독립 브라우저의 Firebase 네트워크 차단으로 내부 업무화면은 로그인 화면으로 리다이렉트되므로 내부 화면 판정은 로그인 세션의 인앱 브라우저 결과를 우선했다.
- `npx tsc --noEmit`, `npm run check:fonts`, `npm run check:ui`, `npm run check:tokens`, `npm run build`가 모두 통과했다.
- 최종 판정: PASS — UI 표시와 안내 문구만 변경했으며 데이터·권한·계약·정산 로직 변경 없음.

## 123. 현대 2027 코나 SX2 현행 계보 재편 (2026-08-15)

- 현대자동차 공식 2027 KONA·KONA Hybrid 가격표에서 모델 출시일 `2026-04-07`을 확인했다. 가솔린 1.6T는 8단 자동변속기, 가솔린 2.0은 IVT, 하이브리드는 1.6 2WD이며 공식 트림·구동 조합을 연식 경계로 분리했다.
- 신규 마스터 `mf-001.md-055.sm-sx2-my2027__kona-2027` 아래 가솔린 1.6T 2WD 6개, 4WD 4개, 가솔린 2.0 2WD 2개, 하이브리드 1.6 2WD 6개로 총 18개 수동확인 영구코드를 발급했다.
- 기존 일반 SX2 내연기관·하이브리드 21행과 2025 H-Pick·Black Exterior 6행은 영구키와 과거 의미를 보존하고 생산종료 `2026-03`, 연식종료 `2026`, 중고차로 닫았다. 일반 SX2 전기 v04와 별도 48.6kWh Electric 계보는 계속 `현재`로 유지했다.
- 원천대장 기존 27행의 종료 관련 열만 정밀 갱신하고 `차종마스터!A7513:AD7530`에 신규 18행을 추가했다. CellData 재조회에서 30열 전체, 숫자형 순번·배기량·5인승, 터보·구동, 공식 근거 URL, Noto Sans KR 9pt 표 서식을 확인했으며 시트는 7,530행으로 확장됐다.
- 출시 전 신형 아반떼는 공식 사전 알림만 있고 현행 판매 페이지·가격표가 아직 CN7이므로 추정 영구코드를 발급하지 않았다.
- 로컬 산출물은 총 5,276건(자동 2,693 / 수동 732 / 차단 1,851), 생성기 누적 보강 479행이다. 신규 `sim-kona-2027-contract.mts`와 차종마스터·영구키·그랜저·카니발·아반떼·쏘나타·K5·Tesla 계약, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 모두 통과했다.

## 124. 캐스퍼 일렉트릭 트림별 실제 출시계보 교정 (2026-08-15)

- 기존 AX1e 마스터는 프리미엄·인스퍼레이션·크로스·라운지 4개 트림을 모두 최초 캐스퍼 Electric 출시월 `2024-08`부터 현행으로 열었다. 현대자동차 캐스퍼 공식 이벤트를 대조한 결과 프리미엄은 `2024-10-18`, 크로스는 `2025-02-11`, 라운지는 `2026-03-17`에 각각 별도 출시됐다.
- 잘못 소급된 프리미엄·크로스·라운지 기존 영구키 3개는 삭제하거나 시작 의미를 바꾸지 않고 `제외·1차확인·blocked`로 차단했다. 최초 가격표에서 직접 확인되는 49kWh 인스퍼레이션은 `2024-08` 시작 수동확인 코드로 그대로 보존했다.
- 신규 마스터 `mf-001.md-103.sm-ax1e-phased__casper-electric-phased` 아래 프리미엄 42kWh FWD·크로스 49kWh FWD·라운지 49kWh FWD를 실제 출시월별로 재발급했다. 세 트림 모두 4인승이며 공식 이벤트 URL과 현행 공식 가격표를 함께 근거로 기록했다.
- 원천대장 `차종마스터!A7090:AD7093`에서 잘못 소급된 3행만 차단하고 인스퍼레이션 행은 유지했다. `차종마스터!A7531:AD7533`에 신규 3행을 추가했으며 CellData 재조회에서 A:AD 30열, 숫자형 순번·배터리·4인승, FWD, 출시월, 공식 링크, Noto Sans KR 9pt 서식을 확인했다. 시트는 7,533행으로 확장됐다.
- 로컬 산출물은 총 5,279건(자동 2,693 / 수동 732 / 차단 1,854), 생성기 누적 보강 482행이다. 신규 `sim-casper-electric-phase-contract.mts`와 차종마스터·영구키·코나·그랜저·카니발·아반떼·쏘나타·K5·Tesla 계약, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 모두 통과했다.

## 125. 상품마스터 차량번호 동일 행 전체 원문보존 (2026-08-15)

- 상품마스터 587대를 공급사 원본과 차량번호로 다시 결합했다. 현재 원본에 존재하는 576대는
  같은 행의 비어 있지 않은 모든 셀을 헤더와 함께 보존했고, 홈페이지형 아이언은 상세페이지 1건의
  차량·가격·사진·정책 데이터를 같은 단위로 보존했다.
- 라이브 재조회에서 `공급사 원문보존`이 자기 차량번호를 포함하는 현재 차량은 576/576건이다.
  원본부재 과거 이력은 오토플러스 5, 손오공 4, 리더스 1, 렌트존 1로 총 11대이며 기존 이력을 수정하지 않았다.
- 오토플러스 93, 아이카 122, 이안카 77, 손오공 57, 아이언 29, 공통 `재고` 규격 106,
  공통 `시트1` 규격 86, 리더스 6대를 각각 재조회했다. 공급사별 차량번호 누락·초과·중복은 0건이다.
- `공급사 열 매핑` 탭에 차량번호 고유키와 공급사별 전체 원문 범위를 명시했다. 공급사 차명 열이 있는 경우
  뒤쪽 AI 정제 모델 열이 관리자 대조값을 덮지 않도록 우선순위를 고정했다.
- `scripts/sim-product-master-sheet.mts` 25개 검증과 `npx tsc --noEmit`을 통과했다.

## 126. 현대 2027 캐스퍼 가솔린·밴 연식계보 분리 (2026-08-15)

- 현대자동차 캐스퍼 공식 현행 페이지에서 `2027 CASPER` 출시와 스마트·디 에센셜·인스퍼레이션 구성을 확인하고, 공식 가격표의 `2026-07-15` 가격 기준과 승용·밴 구성을 대조했다. 정확한 출시일로 과장하지 않고 공식 공개가 확인되는 `2026-07`을 생산 시작월로 적용했다.
- 신규 마스터 `mf-001.md-062.sm-ax1-my2027__casper-2027` 아래 승용 스마트·디 에센셜·인스퍼레이션의 가솔린 1.0 자연흡기/터보 6조합과 밴 스마트·스마트 초이스의 자연흡기/터보 4조합을 발급했다. 전 조합은 998cc·4단 자동·2WD이며 승용 4인승, 밴 2인승으로 구분했다.
- 기존 `mf-001.md-062.sm-ax1` 현행 7개 영구키는 삭제하거나 2027 트림으로 재해석하지 않고 생산종료 `2026-06`, 연식종료 `2026`, 중고차로 닫아 과거 의미를 보존했다.
- 원천대장 `차종마스터!1833:1839`의 종료 관련 열만 정밀 갱신하고 `차종마스터!A7534:AD7543`에 신규 10행을 추가했다. CellData 재조회에서 A:AD 30열, 숫자형 순번·998cc·4/2인승, 자연흡기/터보, 트림 및 공식 근거 URL을 확인했으며 시트는 7,543행으로 확장됐다.
- 로컬 산출물은 총 5,289건(자동 2,693 / 수동 742 / 차단 1,854), 생성기 누적 보강 492행이다. 신규 `sim-casper-2027-contract.mts`와 차종마스터·영구키·캐스퍼 일렉트릭·코나·그랜저·카니발·아반떼·쏘나타·K5·Tesla 계약, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`, 관련 파일 `git diff --check`를 모두 통과했다.

## 127. 영구키 의미 드리프트 복구 및 공식근거 운영 승격 (2026-08-16)

- 라이브 운영 승격 사전감사에서 기존 영구키 96행의 생산종료·연식종료 또는 Tesla 세대 의미가 레지스트리와 달라진 사실을 발견했다. 생산기간도 영구키 의미라는 계약에 따라 기존 코드의 원래 의미를 복구하고 `제외/1차확인`으로 차단했으며, 이미 별도 발급된 연식변경·부분변경 코드는 그대로 유지했다.
- 원천 시트에서 사라진 영구키 8개(EQA H243 2개와 르노 구형 6개)를 로컬 레지스트리·ERP 행 단위 산출물의 원래 의미로 `차종마스터!A7544:AD7551`에 복원했다. 시트는 7,551행이며 복원 구간의 A:AD 값, 숫자형 제원, 드롭다운 및 Noto Sans KR 9pt 서식을 재조회했다.
- 초기 복구 시 레지스트리의 낡은 `capturedSheetRow`를 위치 기준으로 사용해 이미 다른 차종이 있던 12행을 건드린 사실을 즉시 전체 키 감사로 발견했다. Cybertruck 1행, G80 2행, EV5 3행, 쏘렌토 X-Line 5행, GN11 그랜저 1행을 로컬 행 단위 마스터에서 A:AD 전체 복원했고, 실제 키 검색으로 올바른 Tesla·K5·코나 12행을 다시 교정했다. 최종 중복·삭제·의미 드리프트는 0건이다.
- 재발 방지를 위해 `build-current-rental-coverage-additions.mts`가 이미 등록된 레지스트리 레코드를 생성 순번과 추정 행번호로 덮지 못하게 변경했다. 이후 수정 대상은 `capturedSheetRow`가 아니라 라이브 `트림행키` 검색으로 결정한다. 운영 승격 스크립트의 오류 출력도 최대 100건과 총 건수를 표시하도록 보강했다.
- 공식 제조사 HTTPS 근거, 세대/개발코드, 정확 제원, 생산·연식 경계 및 자동 식별축 유일성을 모두 통과한 562행을 `확정/확정`으로 승격했다. 승격 후 재계획은 추가 후보 0, 식별 모호성 0이며 170행은 제원·전기 배터리·기간·공식 도메인·근거메모 부족으로 수동 대기를 유지한다.
- 최종 로컬 산출물은 총 5,289건(자동 3,175 / 수동 170 / 차단 1,944)이다. 차종마스터·영구키·운영승격 정책, 캐스퍼·캐스퍼 일렉트릭·코나·그랜저·카니발·아반떼·쏘나타·K5·Tesla 계약, `sim-sheet-merge` 170/170, `npx tsc --noEmit`, `npm run check:fonts`, 관련 파일 `git diff --check`를 모두 통과했다.

## 127. 상품마스터 공급사 원본 전수 역검증 (2026-08-15)

- 공급사 원본을 다시 읽어 `차량번호 → 같은 행의 좌우 전체 유효 셀`을 재직렬화한 뒤 Google `상품마스터`와 역대조했다.
  현재 원본 차량은 20개 공급사 576대이며 차량번호 누락·초과·중복 0건, 공백 수와 손오공 가격블록 헤더 순서만
  정규화한 원문 의미값은 576/576건 일치했다.
- 상품마스터 총 587대 중 나머지 11대는 오토플러스 5·손오공 4·리더스 1·렌트존 1의 원본부재 출고불가 이력이다.
  현재 원본 차량으로 오인하거나 삭제하지 않았다.
- 원본 전체 보존값은 맞았지만 관리자 앞쪽 `공급사 입력 차명`이 최신 요약 규칙과 달랐던 아이카 112대와 스타 1대,
  총 113개의 C열만 갱신했다. 수정 전 차량번호와 기존 값을 선조회했고, 수정 후 아이카 122대+스타 28대의 요약값을
  다시 읽어 150/150건 일치함을 확인했다. 가격·상태·정책·원문보존 열은 수정하지 않았다.
- 현재 차종 검증상태는 확정 378대·검수필요 9대·미매칭 200대다. 따라서 `공급사 원본 수집·보존`은 전수 일치지만
  `차종마스터 매칭까지 100%`라고 판정하지 않는다. 이 영역은 상품 데이터 수집과 분리해 별도 확정해야 한다.
- 운영 Firebase 서비스계정은 신규 원천대장 읽기 권한이 없어 Google API에서 권한 오류가 난다. 사용자 계정으로 검증한
  현재 데이터는 정확하지만, 서비스계정에 읽기 권한을 부여해 서버 dry-run을 통과하기 전 ERP 자동연동 완료로 판정하지 않는다.

## 128. 상품마스터 독립 방식 3차 재검수 (2026-08-15)

- 상품마스터 메타데이터를 새로 읽어 `gid=679088240`, 50열, 고정 헤더 1행·고정열 2열을 확인한 뒤 공급사 20곳을
  현재 시점에 다시 수집했다. 이전 문자열 해시를 재사용하지 않고 원문을 `헤더=값` 토큰 집합으로 분해·정렬해 대조했다.
- 현재 원본 576대는 차량번호 누락 0, 중복 0, 자기 차량번호 포함 576/576, 행 전체 토큰 576/576,
  관리자용 공급사 입력 차명 576/576이 일치했다. 공급사마다 대표 1건의 토큰을 의도적으로 변조한 음성대조는
  20/20건 모두 불일치를 탐지해 감사기가 항상 PASS하는 검사가 아님을 확인했다.
- 상품마스터 587행·20개 공급사·50열 헤더는 정확했고 공급사명·공급사코드·차량상태·정책코드 누락과 검증/관리상태
  허용값 위반은 모두 0건이었다. 대여료 2,166셀과 보증금 2,150셀에서 숫자 오류·대여료 없는 고아 보증금은 0건이었다.
- 원본부재 이력 11대는 렌트존 1·리더스 1·손오공 4·오토플러스 5이며 전부 출고불가다. 현재 원본과 섞인 이력은 0건이다.
- `scripts/sim-product-master-import.mts`는 상품마스터 → ERP 상태·가격·차종코드 및 실패차단을 통과했고,
  `scripts/sim-sheet-live-status.mts`는 상태 전용 연동·계약락·급감차단·공급사 충돌 11/11을 통과했다.
- 이번 3차 검수는 Google Sheet와 ERP 데이터를 수정하지 않았다. 차종 검증상태 378 확정·9 검수필요·200 미매칭과
  운영 서비스계정의 원천대장 읽기 권한 부재는 별도 미완료 조건으로 유지한다.

## 129. 전기차 운영정책 오탐 교정 및 Tesla 세대 구분 재검증 (2026-08-16)

- 운영정책의 기존 `EV` 문자열 검사가 Volvo `MHEV`까지 배터리전기차로 오인하고, 수소전기차 넥쏘에도 배터리 kWh 또는 내연기관 배기량을 요구하던 문제를 교정했다. 연료가 실제 `전기`이거나 독립된 `EV` 토큰인 경우만 배터리전기차로 판정하고, `수소`·`수소전기`는 배기량 요구 대상에서 제외했다.
- 회귀 시뮬레이션에 Volvo XC40 MHEV와 현대 넥쏘 수소전기 사례를 추가했다. 라이브 재계획과 대조해 넥쏘 3행, Volvo S60·S90·V60·V90·XC40·XC60·XC90 MHEV 15행, 총 18행만 새로 안전 승격했고 추가 후보와 식별 모호성은 모두 0건이다.
- Tesla 66행은 차종코드를 차체·공식 변경 경계·트림·구동·좌석으로 구분한다. Model 3 Highland, 2025-02 New Model Y 리프레시, 2026-07 Model Y L 6인승 롱휠베이스 파생형, Model S/X Palladium 2023형과 Tesla 정비설명서가 직접 구분하는 2025-06 이후 `2025+` 업데이트가 각각 별도 영구키다.
- Ryzen·Intel Atom 인포테인먼트 프로세서는 연식·트림과 일대일 대응하지 않는 생산 중 하드웨어이므로 차종 영구키·세대명·별칭에 넣지 않는다. 차량별 `컨트롤 > 소프트웨어 > 추가 차량 정보` 확인 대상이며, Tesla 계약 시뮬레이션이 CPU 명칭의 분류축 유입을 전수 차단한다.
- 동기화 후 산출물은 총 5,289건(자동 3,193 / 수동 152 / 차단 1,944)이다. `sim-vehicle-trim-operational-policy`, Tesla 66행 계약, 차종마스터, 영구키 감사·계약, `sim-sheet-merge` 170/170, `npm run check:fonts`, `npx tsc --noEmit`을 모두 통과했다.

## 130. 현대 쏠라티 2,497cc eVGT 현행 코드 재발급 (2026-08-16)

- 현대 공식 2025-07 쏠라티 가격표와 공식 카탈로그를 교차확인했다. 카탈로그가 A2.5 CRDi 디젤 `eVGT`, 총배기량 `2,497cc`, 170PS/43kgf·m을 명시하고 가격표가 자동8단·15/16인승 및 실제 트림을 확인한다.
- 정확 배기량이 공란이던 기존 쏠라티 4개 영구키는 삭제하거나 2,497cc로 의미를 바꾸지 않고 `제외/1차확인/blocked`로 차단했다. 15인승 스탠다드·디럭스·럭셔리와 16인승 디럭스를 `-2497` 신규 마스터 2개 아래 4개 영구키로 재발급했다.
- 신규 키는 `디젤 2.5 CRDi eVGT RWD 자동8단`, 2,497cc, 2.5L, 터보, RWD, 15/16인승을 명시한다. eVGT를 터보 근거로 사용하고 공식 카탈로그 URL을 행별 근거로 기록했다.
- 원천대장 기존 `차종마스터!6874:6877`은 상태와 근거메모만 갱신하고 불변 의미 필드는 유지했다. 신규 `차종마스터!A7552:AD7555`는 숫자형 배기량·순번·인승, 공식 링크, Noto Sans KR 9pt 및 기존 표 구조를 재조회했으며 시트는 7,555행으로 확장됐다.
- 상용차 생성기가 이미 등록된 레지스트리 의미와 최초 감사 좌표를 다시 덮던 경로도 차단했다. 신규 키의 실제 최초 좌표는 7,552~7,555행으로 고정했으며 라이브 승격은 이 4행만 선택하고 모호성 0, 적용 후 재계획 0건이었다.
- 최종 산출물은 총 5,293건(자동 3,197 / 수동 148 / 차단 1,948)이다. 차종마스터·영구키 감사/계약·운영정책·쏠라티·Tesla·`sim-sheet-merge` 170/170·폰트·타입·관련 diff 게이트를 모두 통과했다.

## 131. Mercedes-Benz·푸조코리아 공식 호스트 교정 및 9행 승격 (2026-08-16)

- 운영정책의 제조사 공식 호스트 목록이 Mercedes-Benz의 공식 미디어 `media.mercedes-benz.com`과 푸조코리아 현행 사이트 `epeugeot.co.kr`을 누락해, 공식 근거가 완결된 행도 수동으로 남기던 문제를 확인했다.
- Mercedes-Benz 공식 미디어의 EQC 개발 자료는 80kWh 배터리와 AWD 전기 구동을 직접 명시한다. 푸조코리아 현행 사이트는 `base.epeugeot.co.kr`을 프로모션·시승·브로셔·서비스 네트워크의 자사 경로로 직접 연결하므로 루트와 서브도메인을 동일 공식 호스트 범위로 인정했다.
- 호스트 검사는 정확 도메인 또는 점 경계 서브도메인만 허용한다. `epeugeot.co.kr.attacker.example`처럼 공식 문자열을 포함한 외부 도메인이 거부되는 회귀 시뮬레이션을 추가했다.
- 기간·트림·전기 제원이 이미 완결된 Mercedes-Benz EQC 3행, 푸조 e-208 초기 국내형 2행, e-2008 초기 국내형 2행, 현행 올 뉴 5008 스마트 하이브리드 2행을 승격했다. 종료일이 공란인 후기 e-208/e-2008, EQE, MINI 등은 호스트가 공식이어도 계속 수동으로 유지한다.
- 라이브 승격 범위는 `차종마스터!7113:7117`, `7125:7126`, `7129:7130`의 9행이며 모호성 0, 적용 후 추가 후보 0이다. A:B 18셀의 `확정/확정`과 Noto Sans KR 9pt를 재조회했다.
- 최종 산출물은 총 5,293건(자동 3,206 / 수동 139 / 차단 1,948)이다. 차종마스터·영구키 감사/계약·운영정책·쏠라티·Tesla·폰트·타입·관련 diff 게이트를 모두 통과했다.

## 132. 더 뉴 EV6 GT 공식 그룹 근거 승격 (2026-08-16)

- 기아 더 뉴 EV6 GT의 84kWh 배터리, 합산 448kW 듀얼 모터 4WD, 5인승 및 2024-11 국내 판매 개시는 현대자동차그룹 공식 뉴스룸과 기아 현행 가격표로 교차확인돼 있었으나, 운영정책이 `hyundaimotorgroup.com`을 기아 공식 호스트로 인정하지 않아 수동으로 남아 있었다.
- 기아에 한해 현대자동차그룹 공식 호스트를 허용하고 회귀검사를 추가했다. 라이브 계획은 `차종마스터!7257` 한 행만 선택했고 모호성 0으로 `확정/확정` 승격했다.
- 최종 산출물은 총 5,293건(자동 3,207 / 수동 138 / 차단 1,948)이다. 영구키 감사에서 삭제·재사용·순번·의미 변경 0건, 운영정책 시뮬레이션과 `npx tsc --noEmit`을 통과했다.

## 133. Claude·Cursor·Codex 최종 교차 게이트 (2026-08-16)

- Claude 위험 게이트는 차종마스터 완료 기준을 단순 자동승격률이 아니라 국내 렌트·구독 상품의 안전 커버리지와 영구키 불변성으로 확정했다. `PLAN.md`에 GO/NO-GO와 Tesla 분류축을 기록했다. 차단 1,948행은 잘못되거나 대체된 과거 영구키 감사 이력을 포함하므로 일괄 해제 대상이 아니다.
- Cursor 반복 감사에서 수동 138행을 정책 실패 사유별로 다시 분류했다. 즉시 승격 가능한 행은 0건이며, 실패 사유는 판매기간 114행, 공식 호스트 16행, 전기 제원 22행, 공식 미공개 메모 10행, 필수 제원 3행이다(사유 중복). 차단 1,948행은 정책 필드도 미완결인 1,748행과 필드상 완결됐지만 기존 제외·의미충돌 사유를 개별 검토해야 하는 200행으로 나뉜다.
- 반복 검증용 읽기 전용 `scripts/audit-vehicle-trim-operational-backlog.mts`를 추가했다. 이 감사기는 원천 시트·레지스트리·산출물을 변경하지 않고 수동행의 실패 조합과 차단행의 필드 완결 여부만 보고한다. 차단 상태를 가린 가상 검사는 승격 권고가 아니며 기존 제외 사유를 우선한다.
- Codex가 새 감사기와 실제 산출물을 독립 실행해 총 5,293건(자동 3,207 / 수동 138 / 차단 1,948), 수동 즉시 승격 0건, 차단 필드 미완결 1,748건·개별 검토 200건을 재현했다. 영구키 감사 5,293/5,293에서 삭제·재사용·순번·의미 변경은 모두 0건이었다.
- `sim-vehicle-trim-master`, `sim-vehicle-trim-key-contract`, `audit-vehicle-trim-key-contract`, `sim-vehicle-trim-operational-policy`, Tesla 66행 계약, 쏠라티 계약, `npx tsc --noEmit`, `npm run check:fonts`, 관련 `git diff --check`를 통과했다.
- **최종 판정: CONDITIONAL PASS.** 현재 데이터는 추정값 없이 안전하게 운영할 수 있고 Tesla 세대 구분도 요구를 충족한다. 다만 남은 수동행은 제조사가 공개하지 않은 생산 종료일·개별 특장 변속기·국내 배터리 총용량 등의 새 공식 근거가 생기기 전 자동승격하지 않는다. 공식 자료 유입 시 새 감사기로 재분류한 뒤 제한 승격한다.

## 134. Claude·Cursor·Codex 정책관리·판매시트 합동 검수 (2026-08-16)

- Claude 위험 게이트와 Cursor 구현 감사를 별도로 수행했다. 두 감사 모두 판매시트 표시열에 남은 `심사`, 펼칠 수 있는 숨김 원본의 내부 정책값, 단일 입력률의 잘못된 완료 판정을 P0/P1 문제로 판정했고 Codex가 수정·회귀검증했다.
- 정책관리 상태를 단순 입력률에서 `정책 없음 / 판매조건 부족 / 계약조건 부족 / 완료`로 분리했다. 관리자는 정책 없는 공급사를 포함한 전체 업체를, 공급사는 다른 업체 정보 없이 자기 업체만 확인하며 문제 업체 버튼에서 기존 정책 확인 또는 공급사 귀속 신규 정책 등록으로 바로 이동한다. 공용 정책은 업체 수에서 제외한다.
- ERP 정책관리에는 전체 정책을 유지하되 판매 시트 payload와 표시 표에는 차량 선택에 필요한 연령·연령하향비용·면허·주행·분납·지역·탁송만 추가 노출한다. 내부 심사기준·신용등급·수수료 환수·보험/자차/운전범위·정책명/코드·계약 약관 상세는 숨김 원본을 포함해 생성 payload 자체에서 제외했다.
- `sim-inventory-sheet-exposure.mts`를 추가해 금지 헤더와 내부 비밀 표본값이 HEADERS·TABLE_COLUMNS·exportRow 결과에 들어오지 않고 행/헤더 열 수가 일치함을 고정했다.
- 재고는 상품과 공급사 요청을 동시에 시작하고 상품 응답부터 먼저 표시한 뒤 공급사 명칭을 보강하도록 바꿨다. React 검수 기준에 따라 요청 waterfall과 반복 공급사 탐색도 제거했다.
- 관리자→공급사 역할 변경 시 재고 정책 목록도 해당 공급사 정책만 남도록 재조회·범위화했다. 전자계약 정책 오류는 누락 키에 따라 기본·계약조건·보험·전자계약 중 실제 수정할 구간으로 이동한다.
- 상품 통합검색은 `21세 되는 그랜저 찾아줘` 같은 문장에서 보조어를 제외해 기존 조건 검색으로 연결한다.
- `sim-inventory-sheet-exposure`, `sim-freepass-common-policy`, `sim-freepass-esign`, `npx tsc --noEmit`, `npm run check:fonts`, `npm run check:ui`, `npm run check:tokens`, `npm run build`, 관련 `git diff --check`를 통과했다. 최종 브라우저 재접속은 로컬 오류 탭에 대한 Browser URL 정책 차단으로 수행하지 못했으며 우회하지 않았다.
## 134. 실제 상품 587대 차종마스터 안전 커버리지 합동 종결 감사 (2026-08-16)

- 라이브 상품마스터 587대를 재조회해 `확정 코드 정상 324 / 안전 후보 없음 157 / 다중 자동후보 96 / 수동후보 10`으로 분류했다. 차단키 참조와 존재하지 않는 키 참조는 모두 0이다.
- 차단키 119대 중 공식 축과 생산월이 일치한 13대만 보정키로 교체했고, 나머지 106대는 코드를 비워 검수필요로 격리했다. 기존 자동코드 중 공급사 명시축과 충돌한 13대도 동일하게 격리했다.
- 초기형 G80 RG3는 공식 2020-03-30 출시, 2021-08-10 스포츠 출시, 2023-12-26 부분변경 판매개시를 근거로 가솔린 2.5T/3.5T × 2WD/AWD의 기본형·스포츠 8개 수동키를 신규 발급했다. 원천대장 `차종마스터!A7556:AD7563`에 추가했고 기존 잘못된 영구키는 차단 보존했다.
- 최초 신규행 직렬화에서 산출물 필드명 4개가 원천 열명과 달라 의미 드리프트가 탐지됐다. 대상 신규 8행만 즉시 정확한 30열로 재기록했으며, 이후 라이브 5,301행 ↔ 기준판 5,301행 영구키 감사에서 삭제·재사용·순번변경·차량의미변경 0을 확인했다. 재실행은 `already_applied`로 변경 0이었다.
- 차종마스터·영구키·운영정책·Tesla 세대 계약, 상품마스터 시트·파서·커버리지 감사 시뮬레이션, `npx tsc --noEmit`, `npm run check:fonts`, `git diff --check`를 통과했다. diff 검사는 기존 CRLF 변환 경고만 있다.
- Claude 읽기 전용 안전 게이트와 Cursor 읽기 전용 운영 리뷰를 비식별 집계로 독립 수행했다. 공통 판정은 오매칭 차단 안전성은 GO이나, 263대의 미확정이 남아 전체 신차·중고차 무공백 완성도는 NO-GO다.
- 최종 판정: **CONDITIONAL PASS — 검증 확정 차량은 안전하게 사용 가능하고 오분류 자동적용은 차단했으나, 모든 유통 중고차가 완성됐다는 선언은 금지한다.**

## 135. 상품 587대 커버리지 감사기 독립 재검증·읽기전용 게이트 (2026-08-16)

- Google Sheets 메타데이터를 먼저 확인한 뒤 정확한 `상품마스터!A:AX`만 GET으로 재조회했다. 현재 규격은 50열 exact, 587행·고유 차량번호 587개이며 연속 2회 조회의 코드열 digest가 일치했다. 이번 검증에서는 Sheet·Firebase·ERP write를 수행하지 않았다.
- 검증 도중 별도 공유 작업 결과가 반영돼 코드 보유 행이 316→324, 과거 차단/충돌 사유만 남은 무코드 행이 119→111로 바뀌었다. 변경 뒤 안정 스냅샷만 최종 기준으로 사용했고, 현재는 코드 324·무코드 263이다.
- 최종 판정 합계는 `AUTO_UNIQUE 307 / MANUAL_UNIQUE 7 / EVIDENCE_BLOCKED 273`이며, 차단 상세는 `NO_CANDIDATE 53 / MULTI_CANDIDATE 197 / BLOCKED_KEY_REFERENCE 0 / CODE_CONFLICT 23`이다. 주판정 합과 차단 상세 합 불변식은 모두 통과한다.
- 단일 automatic 후보가 우연히 하나 남았다는 이유만으로 AUTO가 되지 않게 했다. 공급사 원문의 모델·세부모델·개발코드 양성 일치와 함께 기간·연료·배기량·구동·트림 중 하나의 기술/계보 축이 일치해야 하며, 제조사·인승만으로는 자동확정하지 않는다. `아이오닉`↔`아이오닉6`, `K5`↔`K50` 같은 숫자 경계 부분문자열과 `N/A`·`미정`·`0` 플레이스홀더 회귀검사도 추가했다.
- 라이브 JWT는 `GOOGLE_WORKSPACE_SUBJECT`가 없으면 즉시 중단하고 `spreadsheets.readonly` 단일 scope만 요구한다. 현재 Workspace 도메인이 이 readonly scope를 아직 위임하지 않아 네이티브 `--live`는 의도대로 fail-closed한다. 이미 위임된 넓은 scope는 이번 일회성 GET 검증에만 사용했고 커밋 코드의 우회 경로로 넣지 않았다.
- Claude와 Cursor에 개인정보·차량 원문·파일/명령 권한 없이 최신 비식별 집계만 따로 검토시켰다. 두 의견 모두 읽기전용 감사기는 GO, 데이터·ERP 자동 쓰기는 NO-GO였다. 273대 증거차단과 23대 기존 코드 충돌을 해소하기 전 전체 완성 선언과 일괄 반영은 금지한다.
- `npx tsx scripts/sim-audit-product-vehicle-trim-coverage.mts`, `npx tsx scripts/sim-product-master-import.mts`, `npx tsc --noEmit`, `npm run check:fonts`를 순차 실행해 통과했다. 파서 sim의 Firebase 환경변수 누락 경고는 로컬 비연결 환경 경고이며 fixture 판정은 PASS다.
- 최종 판정: **READ-ONLY AUDIT PASS / ERP COMPLETENESS NO-GO.** Workspace 관리자가 readonly 범위를 위임한 뒤 네이티브 라이브 재검증을 통과하고, 쓰기 직전 digest 재검사 또는 단일 writer 잠금을 마련해야 다음 적용 게이트를 열 수 있다.
## 135. 상품 차종 커버리지 개발코드 거짓 음성 교정 (2026-08-16)

- 원문 신호에서 `RG3` 같은 실제 개발코드와 함께 `G80`, `SM`, `MD` 같은 모델명·일반 토큰이 추출되는데, 기존 감사기가 배열 전체를 개발코드로 간주해 정상 후보까지 제거하는 원인을 확인했다.
- 개발코드 필터를 동일 maker/model의 비차단 마스터에 실제 존재하는 코드와의 교집합으로 제한했다. 교집합이 없으면 개발코드 축만 생략하고 연료·cc·구동·인승·트림·생산월·연식 필터는 그대로 유지한다.
- 라이브 587대 재감사 결과 거짓 `안전 후보 없음` 63대가 해소되어 `정상 324 / 후보 없음 94 / 다중 126 / 수동 41 / 단일자동 승인대기 2`로 재분류됐다.
- 단일자동 2대는 2026-08 등록, 가솔린 998cc, 2인승, 프레스티지 스페셜이 모두 명시된 2027 레이 밴으로 CAS dry-run 후 10셀만 적용하고 2/2 재조회 검증했다. 최종 집계는 `정상 326 / 후보 없음 94 / 다중 126 / 수동 41`, 차단키·존재하지 않는 키 참조 0이다.
- 모델명 혼입·미인식 토큰·대소문자 중복 개발코드 회귀 fixture를 추가했다. 커버리지 sim, 영구키 라이브 계약, 차종마스터·운영정책·Tesla 계약, `npx tsc --noEmit`, 폰트 검사와 diff 검사를 통과했다.
- Claude·Cursor 읽기 전용 독립 자문은 수정이 거짓 음성을 줄이면서 불확실성을 다중·수동으로 보존한다고 평가했다. 다만 후보 없음 94대와 다중 126대는 공식 근거 또는 추가 원자 없이는 자동 확정하지 않는다.

## 136. ERP 정책 경계·전자계약 이동·반응형 회귀검증 (2026-08-16)

- 개발 서버와 production build가 같은 `.next`를 사용해 개발 manifest가 사라지던 원인을 확인했다. `next.config.mjs`에서 Next phase 기준으로 개발은 `.next-dev`, 빌드는 `.next`를 사용하도록 고정하고 `NEXT_DIST_DIR` 명시값은 QA 서버용으로 유지했다. `sim-next-dist-dir.mts`가 세 경로를 고정한다.
- 역할별 정책 SSOT를 `policy-access.ts`로 분리했다. 관리자는 전체 정책, 공급사는 자기 귀속 정책만, 영업자는 정책 원문 0건이며 공용·타 공급사 정책이 공급사 화면에 섞이지 않는 표본을 `sim-policy-role-scope.mts`로 검증했다. 역할 전환 중 늦게 도착한 상품·파트너·정책 응답은 epoch가 지난 경우 화면 상태에 반영하지 않는다.
- 정책 준비상태는 `정책 없음 / 판매조건 부족 / 계약조건 부족 / 완료` 네 의미를 유지한다. 판매 필수조건과 전자계약 필수조건을 별도 게이트로 검사하며, 연령하향 가능인데 비용이 없는 표본과 전자계약 사용 업체의 계약조건 부족 표본을 `sim-policy-readiness.mts`로 고정했다.
- 전자계약 누락항목 이동 URL을 `policy-navigation.ts`로 단일화했다. 실제 샘플 `FP-RP005-RENT`의 `delivery_fee` 링크로 정책관리 계약조건 탭이 열리고 해당 입력칸이 스크롤·포커스되는 것을 데스크톱과 390×844 모바일 브라우저에서 확인했다.
- 판매 시트는 숨김 원본까지 payload allowlist만 사용한다. 심사·신용·환수·보험상세·정책명/내부코드 표본이 `HEADERS`, `TABLE_COLUMNS`, `exportRow` 어디에도 포함되지 않음을 `sim-inventory-sheet-exposure.mts`로 재검증했다. 실제 Google Sheet 게시·열 삭제는 소비 수식 의존성과 운영 승인이 없어 수행하지 않았다.
- 브라우저에서 상품찾기 데스크톱·390×844 모바일 목록 로드, 검색·필터·정렬·Google Sheet·보기 전환 컨트롤, 정책관리 네 상태 표기와 모바일 편집 폼을 확인했다. 상품찾기 헤더의 중복 차량 수를 제거했다. 정책관리 초기 store 응답이 hydration보다 먼저 커밋돼 발생하던 mismatch는 첫 animation frame 뒤 로드하도록 교정했고 새 탭 재검증에서 console error 0이었다.
- 0부터 다시 한 모바일 주메뉴 감사에서 관리자에게 `상담데스크`가 남고 `정산확인`이 빠진 과거 구성을 발견했다. 문의를 주메뉴에서 제거하고 관리자 메뉴를 `상품찾기 / 계약진행 / 정산확인 / 재고관리`로 고정했다. 공급사는 상품·계약·재고, 영업자는 상품·계약·전자계약만 노출하며 권한 없는 월별정산 링크를 주지 않는다. `sim-primary-navigation.mts`와 390×844 실제 관리자 화면에서 네 링크와 console error 0을 확인했다.
- Claude는 준비상태와 실제 계약 가능성을 분리하고 서버 발행게이트·정책 버전을 유지하라고 검토했다. Cursor는 역할 변경 stale response, 역할별 payload 경계, 누락필드 포커스와 모바일 접근성을 우선했다. 공통 권고를 코드와 회귀검사에 반영했다. Gemini CLI는 설치를 확인했지만 비대화형 실행에서 다시 브라우저 인증을 요구해 자문을 중단했으며 Workspace 파일 접근도 시도하지 않았다.
- `npx tsc --noEmit`, 개발/빌드 산출물·주메뉴·정책 역할/준비상태/판매시트/공통정책/전자계약 시뮬레이션, `check:fonts`, `check:tokens`, `check:ui`, 최종 `npm run build`(38/38), 전체 `git diff --check`를 통과했다. Windows 메모리 압박으로 `tsx`의 사용자정보 조회가 실패한 구간은 로컬 `tmp` shim으로만 우회해 동일 검사를 재실행했으며 제품 코드·운영 설정에는 포함하지 않았다.
- **최종 판정: LOCAL PASS / LIVE SHEET·DEPLOY NOT RUN.** 로컬 UI·역할 경계·판매 payload·전자계약 이동은 완료 조건을 충족한다. 운영 시트 컬럼 변경과 배포는 소비 스크립트/수식 점검 및 사용자 승인 뒤 별도 수행한다.
## 136. 정확도 우선 최종 집계·공급사 정규화 동형 게이트 (2026-08-16)

- 개발코드 회귀 fixture를 모델명 혼입, 미인식 토큰만 존재, 대소문자·중복, 복수 유효코드, 빈 마스터코드까지 확장했다.
- 감사 수정 전 `정상 324 / 무후보 157 / 다중 96 / 수동 10 / 차단키 0`에서 수정 및 공식 단일근거 2대 CAS 적용 후 `정상 326 / 무후보 94 / 다중 126 / 수동 41 / 차단키 0`으로 바뀌었다. 무후보 감소분을 억지 단일화하지 않고 다중·수동 큐로 보존했다.
- 공급사 시트 입고(`sheet-import`, `master-ingress`), 철렌트 웹 수집, 공급사 AI 정제와 상품 커버리지 감사가 모두 중앙 `snapToMaster`를 사용한다. 연료는 `vehicle-master-format`의 `normFuel/fuelDisplay`, 구동은 `vehicle-master-match`의 `normDrive`를 공유함을 코드 경로로 확인했다.
- 최종 aggregate gate를 추가해 라이브 587대, 분류합 587, 차단키·유령키·승인대기 단일후보·명시축 불일치 1:1·미적용 patch 후보가 모두 0임을 강제했다. 검토 큐 261대(무후보 94·다중 126·수동 41)는 자동 적용에서 제외한다.
- K5 LPG/JF·DL3처럼 세대 신호가 섞인 항목은 신규 추정키를 만들지 않았다. 신규 G80 8키의 제조사·모델·RG3 세대·가솔린 엔진·세단 차체·생산/연식 경계·기본형/스포츠 근거는 원천 30열과 공식 URL·근거메모에 보존돼 있다.
- 최종 검증: 커버리지 회귀 sim, 라이브 영구키 5,301건 계약, TypeScript, 폰트, diff 검사 통과. diff 검사는 기존 줄바꿈 경고만 있다.
## 137. Gemini Business 독립 검토 연결 확인 (2026-08-16)

- Gemini CLI 0.55.1 설치는 확인했으나 비대화형 호출이 브라우저 인증 대기에서 완료되지 않았다. `gemini mcp list` 결과 구성된 MCP 서버는 0개였고, 현재 Codex 브라우저 세션에서도 Gemini 인증 탭 또는 Chrome 연결을 확인할 수 없었다.
- 따라서 Workspace 로그인만으로 Drive·Docs·Sheets 접근 권한이 있다고 가정하지 않았고, 문서·시트 원문이나 고객정보·인증정보를 Gemini에 전달하지 않았다. Gemini 독립 의견은 이번 게이트에서 **미수행**이며 의견을 가장해 반영한 변경도 없다.
- 전체 worktree는 차종 외 사용자 변경이 대량으로 존재하므로 되돌리거나 일괄 정리하지 않았다. 차종 관련 변경 파일만 diff/check 범위로 제한했고 기존 변경을 보존했다.

## 138. 차종마스터 완료조건 0부터 재감사 (2026-08-16)

- 직접 OAuth가 차단된 환경에서 감사기가 불완전한 캐시 보고서를 원문처럼 재분류하고 같은 파일에 덮어쓰는 오류를 발견했다. 기본 동작을 fail-closed로 변경하고, 명시적 `--allow-cached` 진단만 별도 `.cached-diagnostic.json`에 기록하도록 수정했다.
- 네트워크 실패 재현에서 감사 종료코드 1, 기존 보고서 SHA-256 불변을 확인했다. 신뢰 소스 모드와 진단 출력 경로 계약을 공용 함수로 추출하고 live/Workspace/캐시·혼합 개발코드 fixture를 회귀검사에 등록했다.
- Google Workspace 커넥터 권한을 실제 확인한 뒤 메타데이터에서 `차종마스터` 7,563×30, `상품마스터` 1,000×50을 확인했다. 상품 사용범위 A1:AX588을 즉시 스냅샷으로 전수 감사해 `정상 326 / 무후보 94 / 다중 126 / 수동 41`, 차단키·유령키·명시축 불일치 1:1·승인대기 단일후보 0을 재확인했다.
- 차종마스터 A1:AD7563은 50,000셀 미만 5청크로 재읽어 registry와 대조했다. 기준판 5,301키와 라이브 5,301키 사이 삭제·재사용·순번변경·차량의미변경은 0이다.
- 코드 검증은 커버리지·차종마스터·영구키·운영정책·Tesla 계약 sim, TypeScript, 폰트, 관련 파일 diff-check를 통과했다. 문서는 최신 수치와 연결 제한을 본 절에 기록했으며, 이번 차종 변경 범위에 UI 파일은 0개라 UI 회귀는 비적용이다.
- 알려진 **적용 오류**와 미해결 치명·중대 오류는 감사 범위에서 0이다. 다만 차종 데이터 완성도 검토 큐 261대가 남아 전체 신차·중고차 무공백 목표는 **진행 중**이며 완료 선언하지 않는다.

## 139. 상품마스터 23건 충돌·ERP 연동 차단 원인 재감사 (2026-08-16)

- Google Sheets 메타데이터를 먼저 확인한 뒤 `상품마스터!A1:AX1000`, `공급사 데이터 매뉴얼!A1:L200`, `공급사 열 매핑!A1:Q300`만 읽기 전용으로 재조회했다. 상품마스터는 587행·고유 차량번호 587개·50열 exact이며 차량번호/공급사명/검증상태/차량상태/관리상태/공급사코드 공란과 중복 차량번호는 0이다. 매뉴얼 22행과 열 매핑 27행은 필수 열 공란과 `코드+공급사` 또는 `코드+탭` 중복이 0이다.
- 기존 코드 충돌 23건은 `트림 15 / 세대 3 / 세대+트림 3 / 제조사+기간 1 / 기간 1`이었다. 트림 단어 순서·공급사 차명 컨텍스트, `5시리즈(7세대)`·`XT6 6인승` 숫자 경계, 모델명이 제조사 열로 새어 들어온 경우, 생산종료 뒤 재고차 등록을 충돌로 본 감사기 오탐 21건을 회귀검사와 함께 제거했다. 최종 `CODE_CONFLICT`는 원본 `펠리세이드` 철자 계보 1건과 생산 시작 전 최초등록 QM6 1건만 남았고 둘 다 자동 덮어쓰기를 금지한다.
- 운영 전체 연동을 실제로 막던 원인은 상품마스터의 `검수필요+차종코드 없음` 109행을 파서가 첫 행에서 전체 오류로 종료하던 조건이었다. 이 상태는 차종을 지어내지 않고 차량번호·상태·정책·유효 가격만 유입하며 검수 메타를 남기도록 바꿨다. `확정+코드 없음`, 존재하지 않는 코드, `미매칭+코드 있음`은 계속 fail-closed한다.
- 공급사 단건 재시도는 범위 밖 공급사의 깨진 행을 strict parse 전에 격리한다. 매뉴얼이 자동반영을 금지한 렌트존 7대와 스위치플랜 8대는 runnable snapshot에서 제외하고 `sourceScope=providers`로 전환해 상태·가격·부재판정을 모두 보존한다. 나머지 18개 공급사 572대에는 공급사별 급감가드가 적용된다. 429·5xx·네트워크 중단은 읽기 요청만 제한 재시도하고 권한·API 미설정 오류는 즉시 실패한다.
- 최신 시트만 사용한 구조 dry-run은 `원본 587 / 보류 15 / 반영후보 572`, `계획 OK`, `create 572`, `확정 321 / 검수 251`로 통과했다. 이는 빈 ERP fixture 기준이며 실제 반영 diff가 아니다. 2026-08-15 21:39 KST ERP 읽기 캐시와의 별도 대조는 `공급사 미확정 삭제이력 12 / 기존 가격기간 누락 27`로 fail-closed했고 적용 0건이다. 최신 ERP 재조회는 실행 환경의 Google OAuth 네트워크 EACCES로 실패했으므로 이 캐시 수치를 운영 최신값으로 단정하지 않는다.
- 가격쌍 전수검사에서 대여료는 있으나 보증금이 빈 셀은 16개(리더스 7, 스타 9)다. 파서는 해당 기간을 0원 보증금으로 만들지 않고 제외·검수표시한다. 매뉴얼 상단의 `미매칭 200 / ERP 432 / 예상 517` 요약은 현재 `확정 326 / 검수필요 109 / 미매칭 152`와 달라 문서 드리프트로 남아 있으며 이번 읽기 전용 감사에서는 Sheet를 수정하지 않았다.
- 회귀검증은 커버리지 감사, 상품마스터 규격·import, 공급사 Grid 읽기, 일일 동기화, sheet merge 170/170, 판매시트, 비민감 노출, 공급사 템플릿을 통과했다. 일일 동기화는 동일 입력 2회차 변경 0, soft-delete 재등장 복구, 원본부재 삭제 대신 출고불가, 계약락 보존, dry-run/apply 분리, CAS, 성공 감사로그와 실패/차단/dry-run 실행원장을 검증했다. `tsc --noEmit`, 폰트, 관련 diff-check도 통과했다.
- Claude Code는 설치 경로 실행이 sandbox `EACCES`, Cursor Agent는 WSL 서비스 `E_ACCESSDENIED`, Gemini CLI 0.55.1은 MCP 0개이며 비대화형 호출에서 브라우저 재인증을 요구해 세 독립 자문을 완료하지 못했다. 비식별 요약 외 원문·개인정보·인증정보는 전달하지 않았다.
- **판정: LOCAL CODE/SHEET READ PASS · LIVE ERP APPLY NO-GO · 전체 완료 선언 금지.** 코드상 전체 연동 중단 원인과 공급사 실패 전파는 보완했지만, 원본 데이터 충돌 2건·보증금 공란 16셀·수동 보류 15대·최신 ERP diff 미검증이 남았다. 운영 write·커밋·푸시·배포는 수행하지 않았다.

## 139. 실제상품 우선 EQ900 종결·미완료 원인 분리 (2026-08-16)

- 제네시스 공식 EQ900 출시자료(2015-12, 3.8 GDi, H-TRAC, Prestige)와 공식 G90 출시자료(EQ900 부분변경·2018-11)를 근거로 실제상품 2017년식·2018-04 등록 EQ900 3.8 AWD Prestige용 역사 영구키 1건을 추가했다. 기존 키 의미는 바꾸지 않았다.
- 차종마스터 라이브에는 30열·중복·행수 게이트로 1행만 추가했고 registry의 라이브 행을 7,564로 고정했다. 상품마스터에는 행·차량번호·기존코드·검증상태 CAS 후 5셀만 반영하고 즉시 재조회해 1/1 일치를 확인했다.
- Workspace 커넥터로 상품 587행과 차종마스터 7,564행을 다시 읽었다. 최종 집계는 `정상 327 / 무후보 93 / 다중 126 / 수동 41`, 차단키·유령키·잘못된 1:1·승인대기 0이며, 영구키는 기준판/라이브 5,302건과 의미 드리프트 0이다.
- 남은 260건을 보수적으로 `MASTER_RESEARCH_REQUIRED 84 / PRODUCT_DETAIL_REQUIRED 11 / SOURCE_CONFLICT 5 / AMBIGUOUS_CANDIDATES 121 / MANUAL_EVIDENCE_REQUIRED 39`로 분리하는 읽기전용 보고서를 추가했다. 이는 처리 우선순위 분류이며 공식 근거 없이 자동승격하는 규칙이 아니다.
- Claude는 자동확정 근거 재현성과 G80·싼타페·레이·K5 순의 검토를, Cursor는 source conflict 우선과 증거 없는 규칙 완화 금지를 권고했다. Gemini Business 관점 자문은 미확정 gap의 분리·필요 필드·다음 조치 노출을 권고했으나 Workspace 원문 접근 없이 비식별 집계만 검토한 제한이 있다.
- `audit-product-master-vehicle-coverage`, 최종 aggregate gate, 라이브 영구키 계약, 새 backlog 감사, `npx tsc --noEmit`, `npm run check:fonts`를 통과했다. 알려진 적용 오류·치명/중대 오류·필수 검증 실패는 이 변경 범위에서 0이나, 공식 조사가 필요한 84건을 포함한 검토 큐 260건이 남아 전체 무공백 목표는 **진행 중**이다.

## 140. 실제상품 K5·GV70 공식 역사키 종결 (2026-08-16)

- 기아 공식 K5 국내 가격표와 The 2026 K5 출시자료로 2025 LPG 2.0 프레스티지 5대, 2026 베스트 셀렉션 가솔린 2.0·1.6T 2대를 신규 역사키 3개에 연결했다. 2023-11/12 LPG 트렌디는 자료를 과거로 소급하지 않고 보류했다.
- 제네시스 공식 2020-12 GV70 공개 제원과 2024-05-08 부분변경 출시자료로 초기형 2.5T 2WD/AWD, 3.5T AWD, 디젤 2.2 AWD 네 축을 2024-04에서 닫아 신규 발급하고 실제상품 9대를 연결했다. 공식 디젤 정확배기량은 2,151cc이므로 기존 차단키의 2,199cc 의미를 고쳐 재사용하지 않았다.
- 모든 쓰기는 마스터 행수·중복키 게이트와 상품 행·식별자·기존코드·검증상태 CAS를 거쳤고, 적용 후 즉시 라이브 재조회했다. 이번 연속 작업에서 EQ900 포함 상품 17대가 안전 확정됐다.
- 최종 라이브 집계는 `587 = 정상 343 / 무후보 77 / 다중 126 / 수동 41`, 마스터는 기준판/라이브 5,309건이다. 차단키·유령키·잘못된 1:1·승인대기·영구키 의미 드리프트는 모두 0이다. 검토 큐 244건 중 마스터 공식조사 대상은 68건으로 감소했다.
- EQ900 전환 경계, K5 연식·연료·트림, GV70 2,151cc와 2024-05 부분변경·전동화 비확장 경계를 별도 음성 회귀검사로 고정했다. 커버리지/마스터/영구키/Tesla sim, TypeScript, 폰트 검사도 모두 통과했다.
- Claude와 Cursor는 이번 17건 반영에는 GO, 전체 10년 국내상품 완료에는 NO-GO로 일치했다. 공식 근거 범위를 벗어난 파생 조합 금지와 남은 68건의 근거·보류사유 추적을 다음 게이트로 유지한다.

## 141. 실제상품 Ray·K8 공식 역사키 종결 및 Gemini 연결 재확인 (2026-08-16)

- 기아 공식 2026-05 Ray/Ray EV 국내 가격표로 가솔린 승용 프레스티지·시그니처, 2인승 밴 프레스티지 스페셜, EV 35.2kWh 에어 네 축을 발급해 실제상품 7대를 CAS 연결했다. 트림이 없는 Ray 2대는 자동 연결하지 않고 다중후보 큐로 이동했다.
- 기아 공식 2025-06-19 The 2026 K8 출시자료로 2.5 가솔린 2WD 베스트 셀렉션 역사키 1개를 발급해 2026-05 등록 실제상품 6대를 연결했다. 공급사 결합 문자열 `노블레스 라이트 베스트 셀렉션`은 독립 트림으로 만들지 않고 공식 트림의 별칭으로만 보존했다.
- 범용 마스터 append 게이트가 EV의 빈 내연기관 배기량을 필수 공란으로 오판해 쓰기 전 중단되는 오류를 발견했다. ICE는 정확배기량/표시배기량, EV는 배터리 용량을 필수로 검사하도록 분기하고 양방향 음성 회귀검사를 추가했다. 오류 시점 Sheet write는 0이었다.
- Google Sheets 스킬의 metadata-first·bounded-read·CAS·post-read 절차를 적용했다. 최종 라이브 집계는 `587 = 정상 356 / 무후보 62 / 다중 128 / 수동 41`, 마스터 기준판/라이브는 5,314건이며 차단키·유령키·잘못된 1:1·승인대기·의미 드리프트는 0이다. 마스터 공식조사 대상은 68→53건으로 감소했다.
- Gemini CLI 0.55.1을 비식별 집계로 실제 호출했으나, Business 연결이 아닌 지원 종료 개인 Code Assist 티어로 인증되어 `UNSUPPORTED_CLIENT`가 발생했고 구성된 Workspace MCP도 0개였다. Gemini 의견을 가장하거나 반영하지 않았으며, Workspace Sheets 커넥터는 별도로 정상 사용했다.
- 커버리지·마스터·영구키·Tesla·상품기반 축/기간·ICE/EV 검증 sim, TypeScript, 폰트 검사를 모두 통과했다. 전체 10년 국내상품 무공백 목표는 검토 큐 231건과 마스터 조사 53건이 남아 계속 진행 중이다.
- 독립 게이트 후 HEV/PHEV/EREV가 순수 EV처럼 배기량 없이 허용되지 않는 음성 fixture를 추가했다. 재실행 멱등성에서 마스터는 `already_applied`, 상품 writer는 후보 0건을 오류로 보던 결함을 `dry-run/apply-noop` 성공·write 0으로 교정했다.
- Sheets 시각 게이트는 CUA 없이 API metadata로 수행했다. 변경한 D:F/J/AT 21개 범위를 `userEnteredValue/effectiveValue/formattedValue/userEnteredFormat/dataValidation`로 재조회했으며 값과 기존 서식이 존재하고 validation 손상은 없었다. 콘텐츠 셀만 갱신해 행 높이·열 너비·표 구조는 변경하지 않았다.

## 141. 전자계약 발행·서명·완료본 보관 전 구간 재감사 (2026-08-16)

- 검증 정본은 2026-08-16 현재 로컬 working tree의 프리패스 전자계약 문서 스냅샷·정책·상태머신이다. 운영 배포나 외부 발송 없이 `계약 생성 → 발행 준비 → 고객 입력·서명 → 관리자 승인 → 완료 PDF 보관 → 인도·정산 연결`을 0부터 다시 추적했다.
- 발행 전 공통 게이트를 ERP 차량 여부와 무관하게 적용하도록 보완했다. 월 대여료, 보증금, 계약기간, 차량명, 운전자 연령, 추가운전자 요금, 중도해지율, 지연손해금, 자차 부담률·최소/최대금액, 필수 특약·첨부서류를 검증하며, 본 계약에 연대보증인 정보를 섞으면 별도 연대보증 약정을 사용하도록 차단한다.
- 선택 공급사·정책·ERP 차량의 공급사 일치와 차량 `출고가능` 상태를 발행 전 검사한다. 관리자 최종 승인 직전에는 최신 계약 묶음을 다시 읽어 같은 차량을 재검사하고, 다른 계약이 선점했거나 출고불가로 바뀌었으면 승인 claim을 해제하고 완료 처리를 거부한다. 차량 상태의 실제 writer는 기존 정산엔진 불변 규칙을 유지했다.
- 고객 제출은 계약조건·약관 열람, 필수 동의, 주민등록번호 유효성·주계약자 연령, 면허번호, 주소, 운전면허증·셀카, 공급사별 필수 첨부서류와 허용된 추가운전자의 정보·면허·동의가 모두 있어야 `pending_review`로 전이된다. 관리자는 주계약자와 추가운전자 면허자료를 검토한 뒤 승인한다.
- 최종 승인은 계약 스냅샷, 고객서명, 면허증·셀카·추가 첨부, 동의기록의 해시와 승인시각을 봉인하고 완료 PDF를 저장한 뒤 계약·검증·진행상태를 한 번에 갱신한다. 인도일은 서명 문서의 `차량 인도일부터 계약기간` 조건과 분리된 운영 메타데이터로 기록하며, 정산은 기존 단일 writer 상태엔진만 사용한다.
- 내부 정책 노출 경계를 다시 확인해 공개 카탈로그에서 심사기준·신용등급과 내부 정책값을 제거했다. 정책 누락 안내는 고객 문서가 아니라 관리자 정책관리의 정확한 계약조건 탭으로만 연결된다.
- 회귀검사: `sim-esign-source-gate`, `sim-freepass-esign`, `sim-esign-snapshot`, `sim-esign-signed-snapshot`, `sim-esign-inputs`(43/43), `sim-esign-required-docs`, `sim-esign-progress`(44/44), `sim-esign-policy-selection`, `sim-policy-readiness`, `sim-policy-exposure-boundary`, `sim-esign-field-map`(13/13), `sim-esign-document-boundary`, `sim-esign-agreement`(61/61), `sim-contract-sign-rules`(58/58), `sim-e2e-settlement`(22/22), `check-template-drift`, `sim-policy-to-contract`, `sim-esign-compliance`(23/23, required open 0)을 모두 통과했다.
- 필수 게이트: 최종 변경 후 `npx tsc --noEmit`, `npm run check:fonts`(drift 0), `npm run check:tokens`(0), `npm run check:ui`(0), `npm run check:release`(blocker 0), clean `npm run build`(Next 15.5.21, 정적 페이지 38개)를 통과했다. 관련 변경 파일 `git diff --check`는 오류 0이며 Windows LF/CRLF 안내만 있다.
- PDF는 Pretendard 로딩을 검증한 뒤 A4 8페이지로 생성·전 페이지 렌더링했다. 약관 3페이지의 6개 컬럼은 사용 높이 964.6~964.8 / 가용 964.9로 넘침이 없었고, 8페이지 모두 잘림·겹침·깨진 글자가 없음을 이미지로 확인했다. 브라우저에서는 데스크톱과 390×844 모바일에서 계약 목록·작성 3단계·공급사 정책 필터·출고가능 차량 검색·A4 샘플을 확인했고 console error 0, 로컬 `/esign`과 `/esign/sample-contract` HTTP 200을 확인했다.
- Claude 독립 검토의 핵심은 차량 중복계약 TOCTOU, 고객 입력 후 연령 재검증, 전자증거 봉인, 인도일 정합성, 연대보증 분리였다. 코드 대조 결과 주계약자 연령·증거 봉인·인도일 기준은 이미 충족했고, 차량 최종 재검사와 본 계약의 연대보증 차단을 추가 반영했다. 추가운전자 연령은 별도 생년정보를 더 수집하지 않고 관리자 면허 검토로 유지했다.
- Cursor Agent는 설치된 CLI가 안전한 비대화형 읽기 전용 자문 인터페이스를 제공하지 않아 실행하지 않았다. Gemini CLI 0.55.1은 비대화형 호출에서 브라우저 재인증을 요구했고 MCP 연결도 없어 중단했다. Workspace 원문·개인정보·인증정보는 전달하거나 접근하지 않았다.
- `check:release`의 기존 경고 2건(기존 회원 약관 재동의 게이트 미사용, 서비스워커 미구현)은 신규 전자계약 발행·서명 범위 밖이며 blocker가 아니다. 운영 Firebase 실데이터 write, 실제 고객 링크 발송, 운영 PDF 저장, 배포는 승인 범위 밖이라 실행하지 않았다.
- **판정: LOCAL ELECTRONIC-CONTRACT PASS / LIVE SEND·DEPLOY NOT RUN.** 위 로컬 검증 범위·시점에서 알려진 적용 오류 0건, 미해결 치명·중대 오류 0건, 필수 검증 미통과 0건이다. 이는 운영 데이터·브라우저·네트워크까지 포함한 절대 무오류 보증이 아니며, 실제 운영 발행 전에는 공급사 정책값과 운영 Firebase 권한·저장 경로를 별도 승인 환경에서 확인해야 한다.

## 142. 상품마스터 공급사별 격리·최신 스냅샷·실차번 충돌 재감사 (2026-08-16)

- 기획 정본을 `공급사 원본(읽기 전용) → 상품마스터 정제 → ERP`로 고정했다. ERP 연동은 상품마스터만 읽고, 공급사별 계획·CAS 반영·사후검증·실행원장을 분리한다. 한 공급사의 삭제이력/가격기간 충돌과 매뉴얼 자동반영 금지는 해당 공급사만 보류하고 다른 안전 공급사는 계속 처리한다.
- 감사기가 오래된 JSON을 다시 쓰지 않도록 `--refresh-sheet`를 추가했다. 실행 시 상품마스터 A:AX, 매뉴얼 A:L, 열매핑 A:Q를 한 번의 읽기 전용 batchGet으로 고정한 뒤 운영 v4 재고·삭제이력·계약·승인원장과 비교한다. Workspace 위임 토큰은 등록된 Sheets scope를 사용하지만 감사 코드에는 GET endpoint만 존재하고 결과도 Sheet/RTDB write 0을 명시한다.
- 2026-08-16 02:54:40 KST 스냅샷은 587행·20공급사·고유 차량번호 587·공급사코드 누락 0이다. 검증상태는 확정 358·검수필요 86·미매칭 143, 관리상태는 운영 337·검수필요 250이다. 매뉴얼 22행과 열매핑 27행은 필수값 공란·중복키 0이다.
- 최신 운영 ERP read-only diff는 안전 13공급사 256행(신규 45·수정 211), 삭제이력/가격기간 충돌 보류 5공급사 316행, 매뉴얼 보류 2공급사 15행이다. 안전 공급사에 첫 계획을 메모리 적용한 뒤 재계획하면 13/13 모두 신규·수정 0으로 멱등성이 재현된다. 실제 Sheet/RTDB 반영은 0건이다.
- 공급사별 분할 전에 전역 실차번 충돌을 보존하지 못하던 결함을 새 회귀검사가 탐지했다. 전체 상품마스터에서 같은 실차번을 둘 이상의 공급사가 보내면 관련 공급사만 모두 보류하고, 무관한 공급사는 계속 반영하도록 수정했다. CAS 실패 사유에는 차량번호가 포함된 저장키 대신 비식별 1건 표기만 남긴다.
- 연동 화면은 `상품마스터 → ERP`, 데이터 검증, ERP 연동하기를 정상 노출한다. 마지막 실행 원장에 공급사 결과가 있으면 전체/반영가능/보류 차량 수와 공급사별 사유를 표시하고, 부분 완료는 `자동연동 일부 보류`로 경고색 처리한다. 로컬 `/inventory`는 재고 623대와 상품행을 렌더링했고 로드 실패 문구는 없었다. 운영 반영 버튼은 누르지 않았다.
- 차종 감사 최신 합계는 587 = AUTO_UNIQUE 355 / MANUAL_UNIQUE 7 / EVIDENCE_BLOCKED 225이며, 차단 상세는 NO_CANDIDATE 51 / MULTI_CANDIDATE 172 / CODE_CONFLICT 2 / BLOCKED_KEY_REFERENCE 0이다. 남은 2건은 손오공 팰리세이드 계보 표기와 아이언 QM6 생산기간 근거 충돌로 자동 교체하지 않는다.
- 검증은 상품마스터 import, 일일연동 31/31, sheet merge 170/170, 공급사 Grid 15/15, 재고 비민감 노출, 상품마스터 규격, 판매시트, 공급사 표준양식 24/24, 실시간 상태 11/11, 커버리지 감사 sim, `npx tsc --noEmit`, 폰트 drift 0, 관련 diff-check를 통과했다. 프로덕션 build는 시스템 RAM 사용률이 94.3%까지 올라 하드웨어 안전지침에 따라 실행하지 않았다.
- Claude와 Cursor 읽기 전용 독립 검토는 공급사별 부분 반영·충돌 자동차단에는 동의했고, 전체 완료는 보류해야 한다고 판단했다. 제안한 전역 실차번 경계와 감사 민감정보를 실제 코드에서 보완했다. 분류→반영 사이 ERP 재읽기·전체 v4/products transaction·결정 UI·관리자 전용 상태 API는 이미 존재해 해당 우려는 기각했다. Gemini CLI 0.55.1은 `UNSUPPORTED_CLIENT`, Workspace MCP 0개로 독립 자문을 수행하지 못했으며 Google Sheets 커넥터 읽기 권한은 별도로 정상 검증했다.
- 매뉴얼 상단의 `미매칭 200 / ERP 432 / 예상 517` 요약은 최신 587행·운영 ERP 623대와 맞지 않는 문서 드리프트다. 외부 Sheet write 승인 없이 수정하지 않았고 런타임은 이 설명문을 판정값으로 사용하지 않는다.
- **판정: LOCAL IMPLEMENTATION/READ-ONLY AUDIT PASS · LIVE APPLY/전체 완료 NO-GO.** 현재 변경 범위의 알려진 코드 오류와 필수 로컬 검증 실패는 0이지만, 보류 331행·차종 의미충돌 2건·매뉴얼 요약 드리프트와 프로덕션 build 미실행이 남았다. 커밋·푸시·배포·운영 Sheet/ERP write는 수행하지 않았다.

## 142. 실상품 우선 야간 고도화 — K5·쏘나타·GV80·니로 (2026-08-16)

- 라이브 기준선: 587대 = 확정 356 / 무후보 62 / 다중 128 / 수동 41, 차단키·미존재키 참조 0.
- 공식 근거 신규 계보: K5 DL3 부분변경 2.0 가솔린 3트림, 쏘나타 디 엣지 2024~2025 LPG 렌터카 Business 1/2, GV80 JX1 초기형 실상품 4조합, 니로 SG2 HEV 3트림.
- CAS 자동 적용: K5 2대, GV80 4대, 니로 1대. 적용 전 차량번호·현재코드·검증상태 3항 비교 및 적용 후 재조회 통과.
- 보수적 보류: K5 LPG Trendy 2023-11/12(세대/생산월 충돌), 쏘나타 숫자 없는 `비즈니스`와 니로 트림 미상은 다중후보, GV80 연식 2025/등록 2020 충돌은 무후보 유지.
- 최종 라이브 재감사 예정값: 확정 363 / 무후보 51 / 다중 132 / 수동 41, 차단키·미존재키 참조 0.
- 회귀: `sim-product-backed-master-additions`, 차종행 검증, 키 계약, 라이브 집계, TypeScript, 폰트 게이트를 변경 후 재실행한다.

## 143. 상품마스터 → ERP 실제 dry-run 권한 복구·적용 직전 게이트 (2026-08-16)

- 실제 연동 위치를 로컬 `재고관리 → 상품마스터 연동 → 상품마스터 → ERP` 카드로 확인했다. 관리자 화면에는 `데이터 검증`과 `ERP 연동하기`가 각각 1개씩 노출되고 재고 623대가 로드됐다.
- 첫 검증 실행은 Google 서비스계정이 상품마스터를 읽지 못해 `The caller does not have permission`으로 실패했다. Workspace 도메인 위임 사용자 설정을 서버 JWT에 연결하고, 현재 Admin Console에 등록된 Sheets 전체 범위로 토큰을 받도록 보완했다. 런타임 모듈은 GET 읽기 endpoint만 제공하며 `batchUpdate`/`values.update`/`valueInputOption`이 없음을 회귀검사로 고정했다.
- 브라우저에서 `데이터 검증`을 다시 눌러 실제 실행원장 `dry_run` 성공을 확인했다. 정본은 587대·20공급사이고, 자동 반영 가능은 13공급사 256대(신규 45·수정 211), 보류는 7공급사 331대다. 수동관리 15대와 삭제이력/가격기간 충돌 공급사 316대는 write 계획에서 제외됐다.
- 독립 read-only diff도 같은 256/331과 안전 공급사 13/13 재실행 diff 0을 재현했다. 적용 안전 집계는 대상키 256/고유키 256/중복 0, 계약락 대상 1대 중 상태 write 0, 가격 없는 신규 0, 검수표시 신규 13, private 최상위 필드 write 0이다. Sheet/RTDB 상품 write는 아직 0이다.
- 계약락은 `softMergeProduct`가 `계약중`/`locked_by_contract` 차량의 `vehicle_status`를 보존하고, 실제 RTDB transaction CAS도 `vehicle_status`와 `locked_by_contract`를 재비교한다. 검증 뒤 계약 상태가 바뀌면 해당 공급사 transaction 전체가 취소된다.
- Claude Code 읽기 전용 검토는 조건부 GO를 주고 직전 재검증, 계약락 교집합, 적용 후 diff 0과 보류 331 write 0을 요구했다. 코드 대조로 계약락 필드 가드는 충족했고 나머지는 실제 반영 직전·직후 게이트로 유지한다. Cursor Agent는 WSL CLI 인증이 완료되지 않아 `Authentication required`, Gemini CLI 0.55.1은 `UNSUPPORTED_CLIENT`로 자문하지 못했다. 비식별 집계 외 원문·개인정보·인증정보는 전달하지 않았다.
- 회귀검증은 공급사 시트 읽기 17/17, 상품마스터 import, 일일연동 31/31, `npx tsc --noEmit`, 관련 `git diff --check`를 통과했다. 시스템 RAM 사용률 92%대로 프로덕션 build는 하드웨어 안전지침상 실행하지 않았다.
- **판정: DRY-RUN PASS · LIVE ERP APPLY는 사용자 최종 승인 대기.** 승인 시 같은 원본을 즉시 재검증해 256/45/211과 snapshot hash가 유지될 때만 실제 반영하고, 사후 재계획 diff 0·차단 331 write 0·v4 전용 write를 다시 확인한다.

## 2026-08-16 상품 587대 전건 운영 큐 및 원문 트림 회귀검증

- 라이브 기준: 상품 587대, 차종마스터 5,326행 (`data_as_of=2026-08-15`).
- 원문에 명시된 `EXCLUSIVE`/`PRESTIGE` 및 공급사 오타 `비지니스1/2`를 제한 사전으로 읽고 한국어 공식 트림명과 동치화했다. 후보 마스터에서 트림을 역추론하지 않는다.
- 회귀검사 `sim-product-coverage-explicit-trim.mts` PASS. 영문 부분 문자열(`PRESTIGEPLUS`)은 수용하지 않는 경계도 고정했다.
- CAS 3항(차량번호·기존 차종코드·기존 검증상태) 확인 뒤 6대만 자동 확정: 2026 쏘나타 렌터카 Business 1/2 2대, 초기형 아이오닉 5 Long Range Exclusive/Prestige 4대. 적용 후 6/6 재조회 일치.
- 최신 라이브 결과: 확정 427, 다중후보 100, 무후보 58, 원천충돌로 자동적용 금지 2. 차단키 참조 0, 존재하지 않는 키 참조 0.
- 미확정 160대 전부를 차량별 `후보다중 95 / 공식마스터조사 48 / 상품정보보완 11 / 원천충돌 6`으로 분류하고 검수사유·관리상태를 CAS 보호 아래 기록했다. 160/160 재조회 일치. 이는 확정 처리 수치가 아니라 누락 없는 검토 큐다.
- 자동적용 금지 2대: G80 3.3T 원문과 구조화 3,500cc 충돌, G80 구조화 기본형과 원문 스포츠 패키지 충돌.
- 검증 PASS: `npx tsc --noEmit`, `audit-vehicle-trim-key-contract --live`(삭제·재사용·순번·의미변경 0), `sim-product-backed-master-additions`, `sim-vehicle-master-row-validation`, `sim-tesla-generation-contract`, `npm run check:fonts`.
- 판정: 587대 모두 확정 또는 구체적 원인 검토 큐로 운영 처리됐으나, 정확 차종코드 기준 종결은 아직 **NO-GO**. 공식근거/상품원자 없이 160대를 추정 연결하지 않는다.

### 공식 무후보 보강 1차

- 현대 공식 2021 싼타페 가격표로 TM 부분변경 기본 5인승, 가솔린 2.5T·디젤 2.2, Premium/Premium Choice 축을 확인했다. 기존 키 의미를 수정하지 않고 실상품에 필요한 가솔린 2.5T 2WD 5인승 프리미엄 초이스와 디젤 2.2 2WD 5인승 프리미엄 신규 영구키 2개를 발급했다.
- 제네시스 공식 2018 G90 출시자료·국내 카탈로그로 일반 세단 5.0 프레스티지, V8 5,038cc, AWD와 별도 Limousine 계보를 확인해 5인승 실상품 신규 영구키 1개를 발급했다.
- 차종마스터 라이브 append 후 키 유일성 재조회 3/3, 상품마스터 CAS 적용 후 재조회 3/3 PASS.
- 전후: 확정 427→430, 무후보 58→55, 미확정 160→157. 마스터 5,326→5,329행. 차단키/미존재키 참조 0, 영구키 삭제·재사용·순번·의미변경 0.
- `sim-product-backed-master-additions.mts`, `npx tsc --noEmit`, 라이브 영구키 계약 감사 PASS.

### 공식 무후보 보강 2차

- 현대 공식 2023 투싼 가격표에서 디젤 2.0·HTRAC·인스퍼레이션을 확인해 NX4 실상품 키 1개 추가 및 상품 연결.
- 현대 공식 2024 팰리세이드 가격표에서 7인승 기본, 디젤 2.2 선택, 르블랑·프레스티지를 확인했다. 기존 LX2 마스터가 디젤 2WD를 8인승으로만 가진 누락을 기존키 수정 없이 신규키 2개로 보강하고 상품 2대 연결.
- 전후: 확정 430→433, 무후보 55→52(연식 오염축 수정 후 재분류 기준 무후보 44), 차종마스터 5,329→5,332행. 신규키 3/3 라이브 유일성, 상품 CAS 3/3 재조회 PASS.
- 감사기 오류 수정: 구조화 연식과 최초등록연도가 2년 초과 충돌할 때 오염된 구조화 연식을 후보 기간축에 다시 쓰던 이중 사용을 제거하고 `연식 충돌`로 검토 큐화했다. 그 결과 무후보 중 8대가 실제로는 후보가 존재함을 드러냈지만 충돌 행은 자동 적용하지 않는다.
- 제한 동치화: `기본`·`기본형`·`기본 사양`만 동일 비트림 표현으로 정규화하고 스포츠 패키지는 별도 유지. 회귀검사 PASS.
- 최신: 587대 = 확정 433 / 다중후보 107 / 무후보 44 / 충돌 단일후보 3. 미확정 154 = 후보다중 95 / 공식마스터조사 34 / 상품정보보완 10 / 원천충돌 15. 차단키·미존재키 0, 영구키 드리프트 0.

## 144. 상품마스터 20개 공급사·587대 ERP 실제 반영 및 사고 복구 (2026-08-16)

- 정본 흐름은 `공급사 원본(읽기 전용) → 상품마스터 정제 → ERP v4`로 유지했다. `pyh@teamjpk.com` 암호화 읽기 전용 gws 프로필로 스프레드시트 `ERP4 차종마스터 원천대장`의 `상품마스터`(sheetId `679088240`)를 재확인했고, A:B 제한 읽기 결과 헤더 정상·데이터 587행·공급사+차량번호 고유 587·중복 0·후행 데이터 0·Sheet write 0이었다.
- 첫 실제 반영은 13개 공급사 186대(신규 22·수정 164)만 성공하고 7개 공급사 401대가 공급사별 transaction CAS에서 취소됐다. 실패 공급사 내부에는 부분 write가 없었다. 원인은 삭제 재고 복구 시 논리 상품키와 실제 Firebase child key가 달랐고, 정규화된 `_key`와 legacy raw `_key` 차이를 실제 동시변경으로 오인한 것이었다.
- 삭제 복구 transaction은 실제 `_rtdb_key` child에 적용하되 저장 레코드의 논리 `product_code`를 유지하도록 수정했다. CAS는 파생 메타필드 `_key`·`_rtdb_key`만 비교에서 제외하고 `product_code`·`updatedAt`·`vehicle_status`·`locked_by_contract`·삭제상태는 계속 검증한다. 실제 반영 직전 감사에서 대상 401건의 CAS 불일치가 27→0으로 감소했다.
- 같은 실행에서 checkpoint를 `partners/{code}` 노드값으로 넣어 성공 공급사 13곳의 v4 partner overlay를 checkpoint만 남도록 교체한 결함도 발견했다. checkpoint는 `partners/{code}/{field}` leaf merge로 고쳤다. 영향 노드는 checkpoint 전용임을 확인한 뒤, 두 시점 snapshot에서 값이 동일한 비민감 시트 매핑만 missing-only로 11곳 복구했고 렌트존 정본 `PT-0001` 활성 overlay를 복구했다. v3 write·민감필드 복사는 0이며 감사로그를 남겼다. 최종 실행 뒤 11/11에서 checkpoint와 기존 매핑이 동시에 존재하고 snapshot 값 mismatch 0임을 재확인했다.
- 수정 후 실제 실행은 공급사 20/20·차량 587/587·보류 0으로 완료됐다. 최초 완주 실행은 신규 46·수정 355였고, 이후 작업 중 상품마스터 정본이 계속 갱신되어 변경분을 두 차례 추가 반영했다. 최종 실행원장 `run_dhq8qca2tz`는 `completed`, 공급사 완료 20·실패 0·보류 0·유입 587·신규 0·수정 12·동일 575이며 차종마스터 확정 435·검토 152다.
- 최종 실행 직후 상품마스터 fresh 재읽기(2026-08-16 04:07 KST, Sheet snapshot 2026-08-16 04:07:20 KST, SHA-256 `f65eb71d0294ecd8fe6e923314bb6e2bd1359d4cd183e6aca3612f128b347739`)는 공급사 20/20 통과, source 587, create 0, update 0, target 0, blocked 0, idempotency fail 0, duplicate target 0, CAS mismatch 0, 계약잠금 상태 write 0, private write 0이었다. 렌트존 정본은 활성, 중복 alias는 soft-delete+정본 연결, alias 상품·문의방·계약 참조는 모두 0이다.
- 회귀검증은 sheet merge 171/171, 일일연동 40/40, 상품마스터 import PASS, 공급사 Grid 읽기 17/17, `npx tsc --noEmit`, `npm run check:fonts`(drift 0)을 통과했다. 시스템 RAM 사용률이 85%대여서 프로덕션 build는 하드웨어 안전지침상 다시 실행하지 않았다.
- Claude Code 독립 검토는 0-diff 카나리, partner 값 감사, orphan/legacy 중복, 동시실행 잠금을 지적했다. 운영 데이터를 일부러 변조하는 live 카나리는 하지 않았지만 nonzero→실반영→fresh zero-diff와 legacy 음성 fixture로 탐지력을 확인했다. partner 값 감사는 snapshot 2개 안정성 11/11·현재 mismatch 0으로 추가 확인했다. checkpoint leaf merge는 상품 삭제정책과 무관하고 상품 부재처리는 별도 엔진이 담당하며, v3 product bridge는 코드상 영구 제외되어 legacy 합본 중복 우려는 적용되지 않는다. lease와 v4 transaction CAS도 유지된다.
- Cursor Agent 2026.08.11 CLI는 WSL에 설치되어 있으나 `Not logged in`이라 독립 자문을 받지 못했다. Gemini CLI 0.55.1은 `UNSUPPORTED_CLIENT`로 자문 실패했지만, Workspace gws 프로필은 OAuth2·암호화 저장·Sheets readonly scope·사용자 `pyh@teamjpk.com`을 확인하고 실제 제한 읽기에 성공했다. 비식별 요약 외 고객정보·인증정보·시트 원문 전체를 보조 AI에 전달하지 않았다.
- 상품 587대는 ERP에 모두 존재한다. 차종마스터 확정은 435대이고 나머지 152대는 잘못 추정해 자동 연결하지 않고 구체적 검토 큐를 유지한다. 이는 ERP 재고 누락이 아니라 차량 규격 확정 상태 차이다.
- **판정: LIVE ERP DATA SYNC PASS / LOCAL CODE FIX NOT DEPLOYED.** 2026-08-16 04:07 KST 고정 snapshot 검증 범위에서 알려진 연동 오류 0건, 미해결 치명·중대 데이터 오류 0건, 실행한 필수 검증 미통과 0건이다. 운영 원본 Sheet·v3 데이터는 쓰지 않았다. 코드 수정은 아직 커밋·푸시·배포하지 않았으므로 배포 전 운영 사이트의 연동 버튼 재사용은 금지한다.

### 공식 무후보 보강 3차 — 스포티지·쏘렌토

- 기아 공식 2022-07 스포티지 가격표와 2022-09 RV 통합 가격표로 NQ5 LPG 2.0 2WD 5인승 그래비티(노블레스 기반)를 확인했다. 최초 추가안의 2024-10/MY2025 기간 확장은 독립 검토에서 근거 과잉으로 적발되어, 상품 적용 전에 2022-07~2023-06/MY2023으로 정본·레지스트리·라이브를 함께 교정했다. 등록월이 2022-11인 상품 1대만 CAS 적용했고 연식 원자가 없는 동일명 상품은 보류했다.
- 기아 공식 2023-11 쏘렌토 가격표로 더 뉴 쏘렌토 MQ4 PE 디젤 2.2 2WD 2,151cc, 그래비티(시그니처 기반), 7인승 선택을 확인했다. 2024-09-02 The 2025 쏘렌토 출시 전으로 기간을 닫은 MY2024 키를 추가하고 2024-05 등록 상품 1대만 CAS 적용했다.
- 전후: 확정 433→435, 무후보 44→41, 미확정 154→152. 차종마스터 5,332→5,334행. 차단키·미존재키 참조 0, 영구키 드리프트 0. 신규키 2개와 상품 2대 모두 라이브 재조회 및 CAS 사후검증 PASS.
- `노블레스 그래비티`와 `시그니처 그래비티`를 긴 토큰 우선으로만 읽는 회귀를 추가했고, 상품 writer에 승인 행 제한 `--rows=`를 추가해 다른 안전후보의 동시 적용을 차단했다.
- Claude/Cursor 독립 의견은 공식 축 자체는 일치하되 기간 과잉과 연식 없는 상품 적용은 금지해야 한다는 데 일치했다. Codex는 두 지적을 반영했다. Gemini CLI는 Workspace 계정 연결과 별개로 현재 클라이언트가 `UNSUPPORTED_CLIENT` 인증 오류를 반환해 자문을 받지 못했다.
- 판정: 이번 2대는 공식 근거·기간·상품 원자가 단일해 **PASS**. 전체 587대 종결은 아직 **진행 중**이며, 남은 152대는 다중후보 95 / 공식마스터조사 31 / 상품정보보완 11 / 원천충돌 15로 검토 큐에 유지한다.

### 상품 차종매칭 내부 조회 탭

- 정본 `ERP4 차종마스터 원천대장`의 기존 `상품마스터!A:AX` 50열·587행은 수정하지 않고, 같은 파일에 `상품 차종매칭` 조회 탭을 추가했다.
- 조회 순서는 `확인 → 차량번호 → 공급사 → 공급사 제공 차량정보 → 차종마스터 변환값 → 매칭상태 → 상품 운영상태 → 차량상태 → 변경·검수 내용 → 차종코드 → 상품마스터 최종갱신`이다. 원본 행이 늘면 FILTER 배열 수식으로 자동 반영된다.
- `확인`은 매칭상태가 확정이 아니거나 상품 운영상태가 운영이 아니면 `확인 필요`, 그 외에는 `정상`으로 표시한다. 원본 상태값은 덮어쓰지 않는다.
- 생성 후 라이브 재읽기: 587행·최대 11열·헤더 11/11 일치, 정상 416 / 확인 필요 171, 매칭상태 확정 437 / 미매칭 99 / 검수필요 51, 변환값 공란 150. 고정 1행·2열, 조건부서식 2개, 계획 열폭과 A2 원본 참조 수식을 재확인했다.
- Claude는 운영상태·차량상태 명칭 분리와 확인 필요 우선 표시를 제안했고 반영했다. Cursor는 열린 범위, spill 보호, 행수·헤더 검증과 오류 비은폐를 제안했고 반영했다. 일반 필터는 배열 결과 정렬로 수식을 손상할 수 있어 넣지 않았다.

## 145. 차량번호별 상품마스터 확정·ERP 증분 반영 최종 재감사 (2026-08-16)

- 정본은 저장소 `feat/sales-sheet-manual`의 HEAD `969497e552f5c3659f86bcc9f4e2b7d61323c477` 및 Google Sheet `ERP4 차종마스터 원천대장`의 `상품마스터` 587행이다. 공급사 원본은 읽기 전용 참고자료, 상품마스터는 차량번호별 1행 정제 정본, ERP는 v4 소비자로 고정했다.
- 공급사 원문에 정확한 등록 트림이 있고 경쟁 후보가 없는 행만 추가 확정했다. 손오공 스포티지 LPG 2.0 2WD 그래비티 1대와 오토플러스 더 K9 3.8 GDI AWD 플래티넘 II 1대는 차량번호·현재코드·현재 검증상태 CAS 및 승인 행 제한으로 D/E/F/J/AT만 반영하고 재조회했다. 부분문자열·짧은 접두·경쟁 트림·연식/연료 충돌은 자동 확정하지 않는다.
- 최신 차량 커버리지는 587 = 확정 437 / 다중 자동후보 96 / 안전 후보 없음 38 / 원천 입력 충돌 15 / 수동후보 1이다. 운영 검수 큐는 후보다중 93 / 공식마스터조사 31 / 상품정보보완 10 / 원천충돌 15 / 공식근거보완 1로 150/150 분류·재조회했다. 차단키 참조 0, 미존재키 참조 0, 기존 확정 1:1 의미충돌 0, 자동 patch 후보 0이다.
- 상품마스터 읽기 실패 원인은 앱 서버 JWT만 Workspace 위임 사용자를 누락해 서비스계정 본인으로 사유 시트를 읽던 계정 분기였다. `GOOGLE_WORKSPACE_SUBJECT` 또는 프로젝트 SSOT 사용자를 공통 서버 리더에 적용했고, 회귀검사는 명시값과 기본값 모두 확인한다. 서버 모듈은 Sheets 읽기 endpoint만 제공한다.
- 변경된 손오공·오토플러스 159행을 공식 일일동기화 경로로 v4에 증분 반영했다(`run_jeetrbb5qr`: 수정 3 / 신규 0 / 실패 0). 이후 전체 20공급사·587행 fresh dry-run은 신규 0 / 수정 0 / 쓰기 0 / blocked 0 / provider idempotency 20/20 / 중복대상 0 / CAS mismatch 0 / private write 0이다. v3 write는 없었다.
- 오토플러스 감사기가 수입차 18개월 이상 보증금을 일반 유입의 3개월치로 비교해 상품마스터의 정상 6개월치를 오탐하던 문제를 발견했다. 상품마스터 공통 `autoplusDeposit` 규칙과 공통 유입 파서가 확정한 국산·수입 배율을 재사용하도록 고쳤다. 최신 visible 원본은 93대, 가격누락 0, 중복 0, 상품마스터 누락 0, 기간별 대여료·보증금 diff 0이다. 과거 출고불가 5행은 삭제하지 않고 이력으로 유지한다.
- `공급사 데이터 매뉴얼!A2`의 오래된 `미매칭 200 / ERP 432 / 예상 517` 수치를 `상품 587 / 차종코드 확정 437 / 검수 큐 150 / ERP 원천반영 587/587`로 수정하고 즉시 재읽기했다.
- 검증 PASS: `npx tsc --noEmit`, `npm run check:fonts`(drift 0), 상품마스터 규격, 명시 트림 경계, 공급사 Grid 읽기 17/17, 상품마스터 import, 일일동기화 40/40, sheet merge 171/171, 최종 차량커버리지 게이트, 오토플러스 live 비교, 아이언 원천 71/71(변환 오류·차번/가격/사진 누락·중복·원가누수 0), 관련 diff-check. 검증용 `server-only` 임시 shim은 제거했다.
- Claude와 Cursor의 앞선 읽기 전용 독립 검토는 정확 경계·단일 후보·접두후보 차단·CAS·불확실 행 검수 큐에 동의했고, 그 지적을 회귀검사에 반영했다. 이번 최종 CLI 재호출은 두 도구 모두 응답 없이 장시간 대기해 중단했다. Gemini 최종 독립 검토의 `writer 부재`, `숨김 상품행 제외`, `587 하드코딩 제거` 제안은 각각 실제 CAS writer 존재, 상품마스터의 숨김이 삭제 의미가 아님, 현재 SSOT 드리프트를 실패시키는 고정 스냅샷 게이트라는 근거로 기각했다. 인증 중복과 위임 사용자 의존성은 경미한 유지보수 위험으로 남되 환경변수 override와 읽기 회귀로 통제한다.
- **판정: 상품마스터 → ERP v4 연동과 현재 확정 437대는 PASS. 전체 차량번호의 정확 차종코드 확정은 진행 중.** 실행한 범위의 알려진 치명·중대 연동 오류와 필수 검증 실패는 0이지만, 근거가 부족한 150대는 완료로 표현하지 않으며 공식 차종마스터/공급사 상세가 확보되기 전 자동 덮어쓰기하지 않는다. 커밋·푸시·배포는 수행하지 않았다.

## 146. 공급사 명시 세부트림 미확정 오탐 재감사 (2026-08-16)

- 정본은 `상품마스터` 587대 snapshot `tmp/product-master-vehicle-patch-snapshot-1786863543751.json`과 `public/data/vehicle-trim-master.json` 5,334행이다. 감사 시트 fingerprint와 라이브 재읽기 fingerprint가 일치했으며 원본·상품마스터 write는 0이었다.
- 원문에 세부트림이 명시돼도 미확정이던 원인은 짧은 토큰 우선, 영문·숫자 경계, Unicode 로마숫자, 차체형·주행거리·배기량·개발코드 축의 오분류였다. 공통 정규화 모듈로 NFKC·가장 긴 완전구문·모델 경계·QUATTRO/4WD·공인 개발코드·명시 배기량을 통합하고 감사기 두 곳이 같은 규칙을 재사용하게 했다.
- 보수적 1차 감사 결과는 확정코드 정상 436 / 다중후보 86 / 안전후보 없음 48 / 단일 안전후보 14 / 수동후보 1 / 원천입력 충돌 1이다. 단일 안전후보 14대는 독립 2차 감사에서도 14/14 동일 영구키로 일치했다.
- 라이브 읽기 전용 적용 dry-run은 대상 14대·변경 예정 70셀·이미 반영 0·Sheet write 0으로 통과했다. `--rows` 미지정값이 숫자 0으로 파싱되어 후보를 모두 제거하던 CLI 결함도 발견해 공통 파서와 회귀검사로 수정했다.
- 남은 다중후보 86대 중 46대는 세부트림 문구가 있어도 좌석수·구동방식·차체형 등 공식 축이 여러 개이고, 40대는 트림 정보가 없다. 따라서 이 86대는 명시 트림 누락 오탐으로 일괄 확정하지 않는다. 기존 확정 1대는 원문 `1.5`와 구조화 배기량 `1,600cc`가 충돌해 자동 덮어쓰기를 차단했다.
- 검증 PASS: `npx tsc --noEmit`, `npm run check:fonts`, 명시 트림 회귀, 실제 감사분류 회귀, 상품마스터 시트/import, 차량마스터 행 검증, 라이브 dry-run 및 14/14 교차대조. 최종 게이트는 14건 미반영과 기존 축 충돌 1건을 의도대로 실패 처리한다.
- Cursor와 Gemini 독립 검토의 긴 구문 우선, Unicode 정규화, 부정증거 강화, 멱등성·중복키·CAS·dry-run 지적을 코드와 회귀검사에 반영했다. Cursor의 K9 정확 장문 트림까지 모호 처리하라는 제안은 공식 마스터의 완전 일치 장문이 존재해 기각했다. Claude Code 읽기 전용 자문은 비대화형 호출이 응답하지 않아 중단했다.
- **판정: 감사기 오탐 수정과 안전후보 산출은 PASS, 전체 작업은 진행 중이다.** 14건의 상품마스터 실제 반영은 사용자 직전 승인 전까지 NO-GO이며, 원천입력 충돌 1건은 공급사 원문 확인 전 자동 수정하지 않는다. 커밋·푸시·배포는 수행하지 않았다.

## 2026-08-16 — 상품마스터 587대 / Ray 2027 공식 가격근거 감사

- SSOT: live Google Sheet `상품마스터` A:AX, sheet fingerprint `sha256:7f33e4bde5d54f799ba4e6be719f1b65dc78b0691da72c34bcae316f88e8dace`, trim master 5,334 rows / artifact SHA-256 `25b9e7c5f347095deeed9a4f4b6b806ac75ef2a70a84412f2e1027b27fe8cdf0`.
- Live baseline: 587 = 확정 코드 정상 436, 다중 자동후보 86, 안전 후보 없음 48, 단일 자동후보 14, 수동후보 1, 원천 입력 충돌 1, 확정 코드 명시축 불일치 1; 차단키 참조 0.
- Kia `price_ray_202608` 가격표로 Ray 미해결 21대를 역대조: exact unique 9대만 `SAFE_CANDIDATE`, 12대는 검토 유지(트림 누락 2, 공급가/세전가 유형 미확인 10).
- 시트 write 0. 9대는 CAS 적용 전 사용자 직전 승인이 필요하다.
- PASS: `sim-product-master-official-price-evidence`, `sim-vehicle-canonical-v2`, `npx tsc --noEmit`, `npm run check:fonts`.
- 판정: 진행 중. Ray 9대의 제안은 재현 가능하지만 587대 전체의 무후보·다중후보·중대 불일치가 0이 아니므로 완료로 표현하지 않음.

## 2026-08-16 — 임기응변 계보 전수 감사기 강화

- SSOT: live-derived `public/data/vehicle-trim-master.json` 5,334행과 `tmp/product-master-vehicle-coverage.json` 587행. 이번 단계의 Sheet write는 0이다.
- 공통 `canonicalSubModelLabelIssues`에 파워트레인과 차체·인승·구동 축 누출을 추가하고, Ray 복합 오염 회귀검사를 등록했다. audit/apply가 별도 정규식으로 갈라지지 않도록 공통 함수를 사용한다.
- 신규 읽기 전용 감사 결과: 오염 의심 266 master groups / 773 trim rows, 실제 상품 참조 18 groups. Ray 2026은 연식+파워트레인+차체축이 섞인 4행/상품 7대, Ray 2027은 10행/상품 2대로 확인했다.
- 공식 Kia 현행 가격표는 모델을 `Ray`, 상품 축을 `1.0 가솔린`, `1인승 밴`, `2인승 밴`, 트림으로 각각 분리한다. 따라서 현재 문자열의 연식·연료·승용/밴을 세부모델에 유지하는 구조는 NO-GO다.
- PASS: canonical regression, ad-hoc audit, canonical label audit, `npx tsc --noEmit`, 영구키 계약(5,334행 drift 0), fonts. 상품 587대 수치는 436/86/48/14/1/1/1 및 차단키 0으로 변동 없음.
- 판정: 진행 중. 정확한 Ray 계보명과 차체구성 저장축을 확정하기 전 라이브 일괄 치환은 잘못된 1:1을 만들 수 있어 보류했다.

## 2026-08-16 — Ray V2 원자축 사전 마이그레이션

- 현행 차종마스터는 A:AD 30열이며 차체구성·원문별칭 전용 열이 없음을 확인했다. 인승/판매유형/트림별칭 재사용은 의미충돌로 NO-GO 판정했다.
- artifact generator에 선택적 `차체구성` enum과 `원문별칭` 배열을 하위호환 방식으로 추가했다. 라이브 30열에서는 산출 JSON byte drift 0이며, 향후 AE:AF가 생기면 자동 보존한다.
- Ray exact dry-run은 27개 영구키를 보존하며 승용 14 / 1인승 밴 3 / 2인승 밴 10으로 분해했다. 기존 복합 세부모델은 원문별칭으로 보존하고 2017 계보는 `더 뉴 레이 TAM`, 2022+는 `더 뉴 기아 레이 TAM`으로 계획했다.
- 한글 `밴`에 ASCII `\b` 경계를 사용해 분류가 실패하는 오류를 발견해 한글 안전 정규식으로 수정하고 재실행했다.
- PASS: Ray V2 계획 27/27, vehicle trim master V2 sim, canonical V2 sim, artifact live drift 0, `npx tsc --noEmit`, fonts. Sheet write 0.
- 판정: 로컬 사전 게이트 PASS, 라이브 AE:AF 스키마 추가와 27행 CAS 반영은 고위험 정본 변경이므로 사용자 직전 승인 전 NO-GO.

## 2026-08-16 — Ray V2 및 상품우선 연식 오염 라이브 반영

- 사용자 직전 승인 후 차종마스터 그리드를 30→32열로 확장하고 AE `차체구성`, AF `원문별칭`을 추가했다.
- Ray 27키/83셀을 CAS·snapshot·rollback 보호로 반영: 승용 14, 1인승 밴 3, 2인승 밴 10. 세부모델은 2017 계보 `더 뉴 레이 TAM`, 2022+ `더 뉴 기아 레이 TAM`; 기존 복합 표기는 원문별칭으로 보존했다.
- 첫 적용은 AE:AF가 실제 grid limit 밖이라 값 write 전 Sheets 400으로 종료됐다(변경 0). 적용기에 appendDimension과 실패 시 deleteDimension rollback을 추가해 재실행 성공했고, 이미 적용된 상태의 멱등 검증도 추가했다.
- 이어 상품참조 연식 오염 10키/30셀을 반영: 베뉴 QX1(3), 더 뉴 K8 GL3(1), 더 뉴 K5 DL3(3), 스포티지 NQ5(1), 투싼 NX4(1), 더 뉴 쏘렌토 MQ4(1). 모두 body=승용, 기존 표기는 원문별칭으로 보존했다.
- registry는 V1→V2로 명시 승격해 `차체구성`을 영구키 의미계약에 포함했다. 5,334키 삭제·재사용·순번·의미 드리프트 0, artifact live drift 0.
- 오염 감사 전후: 266 groups/773 rows/상품참조 18 groups → 255 groups/736 rows/상품참조 8 groups. 상품 587대 결과 436/86/48/14/1/1/1 및 blocked reference 0으로 완전 동일했다.
- PASS: 적용기 재실행 멱등성, key contract, generator check, canonical/ad-hoc audit, product coverage live reread, `npx tsc --noEmit`, fonts.
- 판정: 두 라이브 배치는 PASS. 전체 마스터는 상품참조 오염 8 groups와 비참조 247 groups가 남아 진행 중이다.

## 2026-08-16 — 상품 587대 확정 배치 23대

- 최신 라이브 재감사에서 단일 공식 후보 14대를 동일 영구키로 재산출하고 상품마스터 14행/70셀을 CAS 적용·전건 재조회했다. 확정 정상 436→450.
- Ray 공식가격 감사기가 구형 복합 `sub_model`의 `승용` 문자열을 요구해 V2 원자화를 거짓 차단하는 회귀를 발견했다. `body_configuration=승용` 계약으로 수정하고 밴 반례 테스트를 추가했다.
- Kia 공식 가격표의 트림+옵션+합계가 정확히 일치한 Ray 9대를 재산출해 9행/45셀 CAS 적용·전건 재조회했다. 확정 정상 450→459, 다중후보 86→77.
- 최종 라이브 587대: 확정 459, 다중 77, 무후보 48, 수동 1, 원천충돌 1, 확정축불일치 1. blocked/nonexistent key reference 0. 미확정 총 128.
- PASS: writer snapshot/CAS/post-read, product live audit, backlog audit, 영구키 5,334 계약, official price regression, `npx tsc --noEmit`, fonts.
- 판정: 23대 배치 PASS, 전체 상품은 128대가 남아 진행 중이다.
# 2026-08-16 차종마스터 제조국·차급·최신순 정렬 검증

- SSOT: Google Sheets `1T_RrErmGoj_yG9S1u7n--2NDolTOw8wA8ROQjPWuAlg`, `차종마스터_규격검토` gid `271777427`, `차종마스터_매뉴얼` gid `1538866901`.
- 첫 열 표시명을 `제조국`으로 고정하고 값은 국내 중고차 시장 기준 `국산/수입`으로 유지했다. 실제 최종조립국과 혼합하지 않는 규칙을 매뉴얼에 명시했다.
- 르노코리아·쉐보레·KG모빌리티 274행의 `차종분류=미확인`을 명시적 모델/세대 규칙으로 보강했다. 결과: 차종분류 미확인 0, 차체형태 미확인 0.
- 정렬을 `국산/수입 → 제조사 → 모델 → 생산시작 내림차순`으로 변경했다. 모델 그룹 내 최신순 위반 0, 생산시작 공란은 그룹 마지막이다.
- 게시 전 스냅샷을 생성했고 게시 후 전체 범위 재조회로 2,097행과 24개 헤더를 확인했다. 매뉴얼 35행도 게시 후 재조회했다.
- 현재 상품 순수전기차 감사: 49대 중 43대 확정, 6대 검토대기. 불확실 원자를 추정해 확정한 건 0.
- 검증: `npx tsc --noEmit` PASS, `npm run check:fonts` PASS, 시트 게시 후 재조회 PASS.
- 남은 중요 불확실성: 폴스타 2 1대·테슬라 모델 3 1대·EV6 4대는 구동/배터리/인승/트림 원천 부족으로 운영 매칭 미완료. 따라서 현 상품 EV 전건 완료 판정은 아직 NO-GO다.
# 2026-08-16 상품 587대 ↔ 규격검토본 2,097행 직접 대조

- 입력: 라이브 `상품마스터` 587행 fingerprint `sha256:c264cdb3fae5ddcb6ef25cf1570832d3587d6ce411c141b8e6e6727e880a33f4`, 로컬 재현본 `tmp/hyundai-three-model-review.json` 2,097행.
- 읽기 전용 감사 `scripts/audit-product-against-review-master.mts`를 추가했다. 시트 write 0.
- 결과: 단일 후보 264, 다중 후보 127, 무후보 194, 제조사·모델 식별축 부족 2. 합계 587.
- 무후보 최초 탈락축: 세부트림 118, 세부모델 36, 구동 19, 연료 9, 배기량 8, 인승 3, 제조사·모델 1.
- 단일 후보도 공급사 원자의 구동 누락 96, 인승 누락 192, 트림 누락 9가 있어 운영 자동확정으로 승격하지 않았다.
- 규격검토본 자체 게이트: 차종분류 미확인 0, 모델 내 생산시작 최신순 위반 0.
- `npx tsc --noEmit` PASS.
# 2026-08-16 기본트림 누락축 보완정책 dry-run

- 공급사 원문은 불변으로 보존하고, 동일 제조사·모델·세부모델에서 명시축과 모순 없는 `t01` 후보의 실제 트림·연료·구동·인승 조합이 하나로 수렴할 때만 기본트림 후보를 산출했다.
- 직접 단일매칭 264, 트림이 비어 있어 기본트림으로 안전 보완 가능한 후보 39, 공급사가 명시한 트림과 규격본이 불일치해 대체 검토가 필요한 후보 55, 다중 87, 무매칭 140, 식별축 부족 2. 합계 587.
- 명시 트림 55건은 기본값으로 덮어쓰지 않았고 시트 write는 0건이다.
- `npx tsc --noEmit` PASS.
# 2026-08-16 제조사·모델·세부모델·세부트림 계층 한정 대조

- 연료·배기량·구동·인승을 후보 탈락 조건에서 제외하고 4계층만으로 587대를 재대조했다.
- 계층 직접 단일 353, 트림 공란의 기본트림 보완 15, 규격검토본 기존 트림행키와 현재 확정코드의 무충돌 교차연결 152, 다중 29, 무매칭 36, 식별축 부족 2. 합계 587.
- 현재 축 충돌이 표시된 행은 기존 코드 교차연결에서 제외했다. 시트 write 0.
- `npx tsc --noEmit` PASS.
# 2026-08-16 계층 매칭 2차 보강

- G80/GV80/GV70 초기형·FL 계보를 `G80 RG3`, `G80 RG3 FL`, `GV80 JX1`, `GV80 JX1 FL`, `GV70 JK1`, `GV70 JK1 FL`로 정규화했다.
- 상품 별칭 `더 2026 셀토스 SP3 → 디 올 뉴 셀토스 SP3`, `모닝 JA → 더 뉴 모닝 JA`, `쏘나타 비즈니스 → 비즈니스 1`, `아르카나 1.6 GTe Iconic → 아이코닉`을 제한 적용했다.
- 587대 계층 결과: 직접 단일 399, 기본트림 보완 41, 기존 확정코드 교차연결 110, 다중 12, 무매칭 23, 식별축 부족 2. 안전 연결 550, 미해결 37.
- 검토 탭 2,097행 게시 후 재조회 PASS, 차종분류 미확인 0, 생산시작 최신순 위반 0, `npx tsc --noEmit` PASS, `npm run check:fonts` PASS.
# 2026-08-16 계층 매칭 3차 보강 및 잔여 원인분리

- 제한 별칭 `E-클래스 W214 아방가르드 → E200 아방가르드`, `GV80 JX1 기본형 → 기본 사양`을 적용했다.
- 587대 결과: 직접 단일 411, 기본트림 보완 41, 기존코드 교차연결 102, 다중 12, 무매칭 19, 식별축 부족 2. 안전 계층 연결 554, 미해결 33.
- 미해결 33 분류: 기본트림 결정축 부족 12, 역사계보 신규근거 필요 9, 원문 세대·트림 충돌 5, 별칭·공식근거 추가검토 5, 제조사·모델 원문누락 2.
- 공식·의미축 근거 없는 별칭 승격과 blocked 과거 키 재사용은 0건이다. `npx tsc --noEmit` PASS.
# 2026-08-17 모델·연식 기반 세부모델 추론

- 공식 `연식시작/연식종료` 범위에서 제조사·모델의 canonical 세부모델이 하나일 때만 `연식 세부모델 추정매칭`으로 분리했다.
- 명시 연식이 없고 세부트림이 공란인 경우, 이미 확인된 세부모델 안에서 최초등록월 당시 생산 중인 `t01` 후보의 계층값이 하나로 수렴할 때만 `등록연월 기본트림 추정매칭`으로 분리했다.
- 신규 추론: `아반떼 J2 + 2026 + 인스퍼레이션 → 더 뉴 아반떼 CN7 + 인스퍼레이션` 1대, 등록연월 기본트림 3대(K8 1, 쏘렌토 2).
- 안전 계층 연결 554→558, 미해결 33→29. 공급사 원문 overwrite 0, 시트 write 0.
- 회귀 게이트: invalid_year_inference 0, invalid_registration_default_inference 0, `npx tsc --noEmit` PASS.
# 2026-08-17 전체 차명 내 트림 추출·등록연도 계보 추론

- 세부트림 전용 칸이 비어 있어도 동일 모델·세부모델의 규격 트림명이 공급사 전체 차명에 완전 포함되고 최장 일치가 하나일 때만 추출했다.
- 제한 변환: 폴스타 `Long range Single motor`, BMW `220i 어드밴티지`, `120i 스포츠→120i Sport`, `X1 20i xDrive x라인→20i xLine`, 벤츠 `C220d Avantgarde→C220d 아방가르드`, 아우디 `A6 C9 45 TFSI S line`.
- 등록연도 2020과 공식 연식범위로 `C-클래스 W206` 원문을 `C-클래스 W205 + C220d 아방가르드` 추정 후보로 분리했다.
- 587대: 안전 계층 연결 565, 다중 4, 무매칭 17, 제조사·모델 누락 1. 미해결 22.
- 공급사 원문 overwrite 0, 시트 write 0, invalid_year_inference 0, invalid_registration_default_inference 0, `npx tsc --noEmit` PASS.

# 2026-08-17 상품 차종매칭 조회 탭 게시

- SSOT: Google Sheet `상품마스터` 587행, fingerprint `sha256:c264cdb3fae5ddcb6ef25cf1570832d3587d6ce411c141b8e6e6727e880a33f4`; 규격본 `차종마스터_규격검토` 2,097행.
- `상품 차종매칭` 탭에 공급사 원문, `제조사 > 모델 > 세부모델 > 세부트림` 변환값, 판정방식, 추론근거, 미해결 사유를 게시했다. 운영 `상품마스터` 값 write는 0건이다.
- 게시 결과 재읽기: 587행 = 매칭완료 565 + 검토필요 22. 검토필요는 별칭·공식근거 4, 역사계보 9, 기본트림 결정축 4, 원문 충돌 4, 제조사·모델 누락 1이며 변환값을 비워 두었다.
- 상태는 명시 텍스트와 글자색만으로 구분하고 데이터 영역은 중립 흰색, 글꼴은 Noto Sans KR로 재검증했다. 게시 전 기존 탭 snapshot을 저장했다.
- 회귀 재감사: 계층 다중 4, 무매칭 17, 식별축 부족 1; invalid_year_inference 0, invalid_registration_default_inference 0, 차종분류 미확인 0, 생산순서 위반 0.
- `npx tsc --noEmit` PASS, `npm run check:fonts` PASS, 게시 후 dry-run 565/22 재현 PASS.

# 2026-08-17 상품 차종매칭 조회 판정 정정 — 게시 대기

- SSOT 재조회: Google Sheet `1T_RrErmGoj_yG9S1u7n--2NDolTOw8wA8ROQjPWuAlg`의 `상품마스터` 587행, fingerprint `sha256:c264cdb3fae5ddcb6ef25cf1570832d3587d6ce411c141b8e6e6727e880a33f4`. 직전 snapshot과 동일하다.
- 이전 조회 탭의 `매칭완료 565`는 영구 트림행키 확정이 아니라 제조사·모델·세부모델·세부트림의 참고 계층 후보 존재를 뜻한다. 엄격 감사 정본은 `확정 코드 정상 459 / 다중 자동후보 79 / 안전 후보 없음 46 / 수동후보 1 / 원천 입력 충돌 1 / 확정코드 명시축 불일치 1`로, 운영 미확정은 128대다.
- 교차대조: 엄격 확정 459대는 모두 계층 후보가 있다. 계층 후보가 있으나 엄격 미확정인 거짓 완료 표시는 106대(72/31/1/1/1)였다. 따라서 기존 565/22만으로 운영완료를 판정하는 것은 NO-GO다.
- 로컬 수정 후 dry-run: `strictConfirmed 459 / strictReview 128`, `hierarchyLinked 565 / hierarchyReview 22`, `strictConfirmedWithoutHierarchy 0`. 운영 확정과 참고 계층 후보를 별도 열·상태로 표시하며 추론 후보는 자동확정하지 않는다.
- PASS: 새 회귀 sim 9/9, 기존 커버리지 sim, 영구키 fixture sim, 라이브 영구키 계약 `기준판 5,334 = 라이브 5,334` 및 삭제·재사용·순번·의미변경 0, `npx tsc --noEmit`, `npm run check:fonts`.
- Claude 독립 검토와 Codex 병렬 감사 모두 `22대 보강 전 판정 분리 필수`, `565를 정확코드 확정으로 사용 NO-GO`, `원문 충돌·누락·추론을 자동해결 금지`에 일치했다. 중요한 이견은 없었다.
- 라이브 조회 탭 정정은 아직 실행하지 않았다. 사용자 직전 승인 후 snapshot → 게시 → 587행/헤더/459·128 post-read 검증이 남는다.

# 2026-08-18 차종마스터 규격채택 게시 전 최종 게이트

- SSOT: Google Sheet `1T_RrErmGoj_yG9S1u7n--2NDolTOw8wA8ROQjPWuAlg` (`ERP4 차종마스터 원천대장`), `차종마스터` sheetId `1159482177` A:AF, `차종마스터_규격검토` sheetId `271777427` A:X. 실행계정은 `pyh@teamjpk.com`으로 고정했다.
- 최신 read-only plan: `tmp/vehicle-master-review-adoption-plan.json`, SHA-256 `c9080a1859bc718c1da093d627b871c762804a624e06ead8962addd4f9d6e640`. 대상 `차종마스터_규격채택!A1:AD2107`은 미존재, write 0, 사용자 승인 미기록이다.
- 집계 재대조: 검토 2,097그룹 → 2,106키, 단일키 그룹 2,088 + 이중키 그룹 9. 채택예정 2,086 = 선택질문 없음 601 + 유지 1,485, 검토유지 20. reviewed tier 2,098/8/0, adopted tier 2,078/8/0, 운영상태 변경 0.
- PASS: 영구키 기준판/라이브 5,334 일치, 삭제·재사용·순번·의미변경 0, master generator drift 0, promotion sim, product-match sim 9/9, `npx tsc --noEmit`, fonts drift 0.
- writer SHA-256 `06a252c5783ed4fffbf8758a2861f11a67eb93046d3decd6443c4f8421ceeafc`가 plan에 결속됐다. plan/source snapshot/artifact/registry/local review/4개 구현 파일의 해시가 apply 전·staging·final·recovery에서 재검증된다.
- Cursor와 Claude의 최신 독립 읽기 전용 적대 검토는 모두 코드 논리 GO, 미해결 치명·중대 0으로 일치했다. 반복 검토에서 발견된 ambiguous add/rename/re-hide 및 rollback crash 반례는 모두 수정 후 재검증했다. Gemini CLI는 두 차례 제한시간 내 최종 판정을 내지 못해 게이트 근거로 사용하지 않았다.
- 현재 판정: 승인 요청 단계 GO, 라이브 게시 자체는 사용자 직전 승인 전 NO-GO. 기존 `차종마스터`·registry·artifact·운영상태는 쓰지 않으며, 실패 시 이번 실행이 소유한 신규 탭만 rollback 또는 동일 sheetId 숨김 복구한다.
- 기능 경계: 이 게시물은 영구키별 규격채택 companion 정본이다. 현 앱/상품매칭 runtime consumer 참조는 0이므로 운영 소비 전환 완료를 뜻하지 않는다.

# 2026-08-18 차종 3축 검토 재개 · 규격채택 게시 · 판매시트 정본 발행 (Claude Code)

- SSOT: `ERP4 차종마스터 원천대장`(`1T_RrErmGoj_yG9S1u7n--2NDolTOw8wA8ROQjPWuAlg`) 상품마스터 587행 · 차종마스터_규격검토 2,097행 · 신규 차종마스터_규격채택 2,106키 · 판매시트 `1Y1Mx1EcEpAuNer0y50Dq4eK92CpVjThO_suZLmo2vVs` 상품리스트.
- 규격검토 라이브 vs 로컬 고정본 diff: 2셀(D4·D5, 셀 끌기 실수) → 원복 후 diff 0 → 채택 계획 재생성 SHA `435ca252630a12a2bc574181ffdc0df41d1950d58ce35f5f4d6ecbb5d559a05f` → apply `applied_verified`, write 63,210셀, 기존 차종마스터·registry·artifact 변경 0.
- 상품마스터 차종코드 반영: 결정 9대(45셀) + 감사기 SAFE 3대(15셀), CAS·재조회 12/12. 최신 커버리지: 확정 코드 정상 411 / 다중 79 / 안전후보 없음 35 / 직접근거 재확인 51 / 명시축 불일치 7 / 단일 승인대기 3→0 / 수동 1. 백로그 176 = 검토완료 74 + 미검토 102.
- 판매시트: old 경로 dump vs new 경로 dump 397/397 차량번호 동일, **돈·상태·비고 칸 diff 0**, 차명 칸 307 변경, 이전 값이 빈 칸 0. `--apply` 후 「상품리스트 08.18 09:31 · 397대」.
- 서식: 원천대장 12탭 9pt·22px(안내 탭 자동맞춤) 재읽기 확인, 규격검토 조건부서식 75, 기존 규칙 수(42/15/3/12) 보존 확인.
- PASS: `npx tsc --noEmit`, `check-manual-drift` 22/22.
- 판정: 3축 검토는 진행 중(102대 잔여 — PLAN.md Cursor 오더), 정규화(규격채택)·판매시트 정본 발행·제공시트 AI 인계 20곳은 PASS. 커밋·푸시 없음.

# 2026-08-18 Cursor 3축 검토 산출 검수 (Claude Code)

- 결정 파일 176건(사람 44 + 자동합의 30 + Cursor 102) 전건: 코드 실재·automatic·3축=키·중복 0 PASS(`plan-product-vehicle-review-decisions`). Cursor 정정 2건(GV80 기본형 PARTIAL→TRIPLE), 필드 신설 `overrides_current_code`(K8 2대).
- 상품마스터 코드 반영 18대(교체 12·신규 6) CAS·재조회 18/18. 라이브 커버리지: 확정 코드 정상 432 / 다중 73 / 안전후보 없음 35 / 직접근거 재확인 39 / 명시축 불일치 7 / 수동 1. 백로그 155 = 검토완료 155, 미검토 0.
- 「상품 차종매칭」 재게시: 1차 post-read 실패(비8비트 색 서명 불일치, 값은 반영됨) → 색 hex 정정 후 2차 `applied`(587행·규칙 32, receipt f8a7a8b4…). sim 11/11, regressions PASS(시점 고정 수치 → 불변식).
- 판매시트 dump 전후 돈 diff 0 · 차명 37칸 → 「상품리스트 08.18 10:21 · 397대」.
- 미완: 오더 2 공식근거(39건 draft 빈칸), 「상품시트 ↔ ERP 연동」 A·B·C 미착수. `npx tsc --noEmit` PASS. 커밋 없음.

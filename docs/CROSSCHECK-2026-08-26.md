# 교차검증 판 — 2026-08-26 정산관리 접수·실적·청구

대상: 현재 작업 트리의 `app/settlement/ledger`, `app/api/settlement/{ledger,confirm,invoice}`, `lib/domain/settlement-*`.

원래 요구: 정산관리에서 접수된 계약이 실적으로 관리되고, 영업채널 실적 확인을 거쳐 공급사 청구·계산서 발행으로만 이어져야 한다.

| 번호 | Codex 원본 확인 | 판정 | Claude Code 확인 요청 | Cursor 확인 요청 |
|---|---|---|---|---|
| F1 | `invoice` POST가 확인 상태를 검사하지 않고 발행 기록을 쓴다. | 틀림 | 미확인 발행을 서버에서 막는 것이 업무 규칙과 일치하는지 판단 | POST/API 직접 호출까지 막는 구현·회귀시험 제안 |
| F2 | UI는 영업담당자, API는 영업채널 단위로 확인을 계산한다. | 틀림 | 어떤 축이 청구 확인의 SSOT인지 확정 | 필터·집계·발행 전 표기를 같은 축으로 정렬 |
| F3 | 설계는 인도일만 관문, 코드는 스타·아이카 분납완료 예외를 둔다. | 못정함 | 공급사별 예외를 유지/폐지할 사용자 결정안 작성 | 결정 전 규칙 변경 금지 |
| F4 | 현 API 쓰기 대상은 ERP DB가 아니라 Google 정산원장이다. | 틀림 | ‘ERP 직접 관리’의 실제 저장소/이관 경계 판정 | 설계 확정 전 저장소 교체 금지 |

Codex 증거와 실행 결과는 `VERIFICATION.md`의 ‘2026-08-26 정산관리’ 항목에 있다. 검토자는 이 표를 답으로 덮어쓰지 말고 본인 파일에 `맞음`·`틀림`·`못정함` 중 하나와 원본 근거를 남긴다.

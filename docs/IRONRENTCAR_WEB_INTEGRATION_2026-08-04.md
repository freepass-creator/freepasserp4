# 아이언렌트카 홈페이지 연동 — 2026-08-04

## 결정

- 사용자가 아이언렌트카의 연동 동의를 받았다고 확인했다.
- 연동 활성화 뒤 `RP006` 상품의 단일 정본은 `https://ironrentcar.com`이다.
- 아이언 Google Sheet는 웹 연동과 동시에 수집 대상에서 제외한다. 두 소스를 동시에 쓰지 않는다.
- 기존 계약의 대여료·보증금 snapshot은 변경하지 않는다. 웹 가격은 재고 상품에만 반영한다.
- 운영 v3 노드는 읽기만 하며 모든 향후 쓰기는 v4 overlay만 사용한다.

## 구현된 경계

- `lib/server/ironrentcar-source.ts`
  - 신차·중고 누적 목록과 상세 페이지를 전수 읽는다.
  - 차량번호, 상태, 연식, 색상, 옵션, 36/48/60개월 대여료·보증금, 사진, 보험 공개조건을 정규화한다.
  - 차량가는 공개 상품에서 제외하고 관리자 private 원자로 분리한다.
  - 상세 한 건이라도 실패하면 `complete=false`로 fail-closed한다.
- `lib/domain/ironrentcar-reconcile.ts`
  - 공급사+차량번호로 기존 ERP 재고와 매칭한다.
  - 완전한 웹 스냅샷에서만 웹 부재 ERP 재고를 출고불가 후보로 만든다.
  - 계약 잠금·중복 차량번호는 자동 후보에서 제외한다.
  - 웹에서 다시 나타난 웹 소유 차단 차량은 복원 후보로 만든다.
  - 웹 가격을 단일 정본으로 사용하므로 웹에 없는 과거 시트 가격기간도 후보 patch에서 제거한다.
  - 적용 기능이 승인되기 전 `executableOperations=0`을 강제한다.
- `GET /api/inventory/ironrentcar/preview`
  - 플랫폼 관리자 Bearer 인증 전용이다.
  - 웹 전수 수집과 운영 v3+v4 읽기 대조 결과만 반환하며 쓰지 않는다.
  - 비인증 요청은 403, 불완전 수집은 502다.

## 2026-08-04 운영 읽기 전용 실측

- 홈페이지: 전체 49대, 활성 24대, 판매완료 25대, 신차 20대, 중고 29대
- 상세 변환: 49/49, 오류·차번·가격·사진 누락·중복차번·공개원가 누수 모두 0
- ERP 안전 매칭: 27대
- 신규 활성 생성 후보: 1대
- 기존 ERP에만 있는 출고불가 후보: 7대
- 홈페이지 판매완료만 존재해 신규 생성하지 않는 항목: 18대
- ERP 중복 차량번호 차단: 3그룹
- 계약 잠금 충돌: 0대
- 실제 운영 write: 0건

### 기존 시트 가격과 홈페이지 가격

- 안전 매칭 27대의 비교 가능 기간은 61개다.
- 월대여료 동일 0개, 보증금 동일 56개, 월대여료+보증금 쌍 동일 0개다.
- 홈페이지에만 있는 기간 14개, ERP에만 있는 기간 2개다.
- 차량 전체 가격 구성이 같은 차량은 0대다.
- 따라서 전환 시 기존 시트 가격을 유지하거나 혼합하지 않고 홈페이지 가격으로 교체해야 한다.

## 운영 활성화 전 남은 사람 승인

1. 중복 차량번호 3그룹의 canonical ERP 상품을 확정한다.
2. 27대 가격·상품 보강, 신규 1대, 웹 부재 7대 차단 후보를 관리자 화면에서 승인한다.
3. `RP006` 시트 수집을 중지한 뒤 웹 apply를 켠다. 순서를 뒤집어 이중 writer를 만들지 않는다.
4. v4 CAS 적용·감사로그·재시도 잠금이 있는 apply 경로를 별도 변경으로 게시한다.
5. 사진은 원본 URL을 우선 사용한다. 자체 저장소 복제 범위는 동의 문구가 확인된 뒤 별도 승인한다.

현재 변경은 위 승인 전 단계인 전수 수집·대조·미리보기까지만 포함하며 운영 데이터를 바꾸지 않는다.

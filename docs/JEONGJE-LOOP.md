# 정제 상호보완 루프 — Cursor ↔ Codex 자가수정 (2026-08-29)

> **이 파일이 «공유 상태판»이다.** 커서·코덱스가 라운드마다 «자기 칸»에 append 하며 서로를 보완한다.
> 사장님: 「자동으로 돌면서 상호보완되게」. **결정·에스컬레이션은 사람(사장님/Claude)**, 두 AI는 수렴만 시킨다.
> 지침: 커서=`aiops/docs/cursor-jeongje-fix-2026-08-29.md` · 코덱스=`aiops/docs/codex-verify-jeongje-2026-08-28.md`.

## 규약 (라운드 R)

```
1. 커서   ← 최신 「검증 R-1」 읽음 → 반례·구멍 고침 → 커밋 → 아래 「## 구현 R」에 «한 것·커밋해시» append
2. 코덱스 ← 최신 커밋 독립 검증(A 오매칭 · B 구멍 · C 무결) → 「## 검증 R」에 PASS/FAIL·반례 append
3. FAIL(오매칭 있음 or 구멍 늘어남) → 커서가 R+1 로 계속
4. PASS 2연속(오매칭 0 AND 구멍 안 늘어남) → 수렴 · 종료 · 코덱스가 메일로 요약
```

## 멈춤·안전 (반드시)

- **수렴**: 오매칭 0 AND 구멍 안 늘어남이 «2라운드 연속» → 종료.
- **상한**: 5라운드. 안 되면 **사장님/Claude 에스컬레이션**(무한루프 금지). 「왜 안 수렴하는지」 3줄 요약.
- **안전선 위반 감지 시 즉시 중단·롤백**(둘 중 누구든):
  - 차종코드(`pickConfirmedMasterCode`)가 medium 으로 박힘 → 「절대 안 틀린다」 위반.
  - 원문에 없는 값을 지어냄.
  - 발행 대수 급감·「공급사 매칭 실패」 발생(회귀).
- **커서는 «안전 medium»만 채운다**(세대만 애매·트림 풀검증) — 위험 medium(모델 애매)은 안 채운다.
- **코덱스는 고치지 않는다** — 반례·근거만. 코드 수정은 커서.

## 판정 표 형식 (코덱스가 라운드마다 채움)

| 항목 | 판정 | 근거 |
|---|---|---|
| A 오매칭 | PASS/FAIL | `차번 \| 원문차명 \| 박힌값 \| 왜틀림` |
| B 구멍 | PASS/FAIL | 채울수있는데 빈 차번 |
| C 무결 | PASS/FAIL | 296대·매칭실패0 여부 |

---

## 라운드 0 — 시작점 (Claude 이 채움 2026-08-29)

**현 상태**: fill 이 medium 통째 완화(`softOk`, 커밋 `ba3b2bd`·`d497677`). 싼타페 프레스티지 등 채워짐. **아직 «안전/위험 medium» 안 가림** — 위험 medium 오매칭 가능성 열려 있음.

**전수조사 기준선**(`aiops/scripts/supplier-sheet/audit-jeongje.mjs`): 1153대 — 모델빈 2(손오공 렌트재고·J&J 「200」 엣지) · 배기량빈 21 · 세부트림빈 67(참고).

**루프 첫 과제**:
- 코덱스(검증 0): 지금 `softOk` 가 «위험 medium»에 오매칭을 냈는지 A 반례 찾기. → 「## 검증 0」.
- 커서(구현 1): 검증 0 반례 있으면 «안전 medium만 채우게» 좁힘(지침 §2). → 「## 구현 1」.

## 검증 0 — (코덱스가 여기부터 append)

## 구현 1 — (커서가 여기부터 append)

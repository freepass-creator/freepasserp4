# 교차 검증판 — 2026-08-21

사장님 2026-08-21 「커서, 클로드, 코덱스가 교차 검증할거야」.

**오늘 두 번 다 «만든 사람이 검증»해서 늦게 잡혔다.** 그래서 규칙을 셋만 둔다.

1. **찾은 사람은 자기 건을 검증하지 않는다.** 한 건에 **다른 둘 중 최소 하나**가 붙는다.
2. **검증은 원본을 연다.** 시트·라이브 데이터·Actions 로그를 직접 본다.
   **다른 AI의 도구 출력을 다시 인용하는 것은 검증이 아니다** — 그게 오늘 사고의 뿌리다.
3. **판정은 셋뿐** — `맞음` · `틀림` · `못정함`. 애매하면 `못정함`이다.
   갈리면 **고치지 말고 남긴다.** 결정은 사장님이 한다.

## 어떻게 적나

자기 파일에만 적는다. **남의 파일을 고치지 않는다**(세 도구가 한 파일을 같이 만지면 서로 덮는다).

```
docs/crosscheck/클로드.md
docs/crosscheck/커서.md
docs/crosscheck/코덱스.md
```

한 줄 = 한 판정. 형식은 파이프 셋.

```
F3 | 맞음 | 시트 「차종마스터」 탭에서 팰리세이드 LX3 줄을 직접 열어 차종구분 SUV 확인
F8 | 틀림 | 안내 탭 「제조사(정제)에는 공급사 표기」 — 르노·KGM 이 맞는 값이다
F4 | 못정함 | 시트에 하이브리드 줄이 없는 건 맞으나, 세부모델 이름이 어긋난 것인지 구분 못 함
```

확인:

```bash
npx tsx scripts/check-crosscheck.mts
```

이 도구가 **자기 건을 자기가 검증했는지**, **아직 아무도 안 본 건이 있는지**, **판정이 갈리는지**를 센다.

---

## 판 — 오늘 나온 것

`찾음` 이 자기 자신인 사람은 그 줄을 검증할 수 없다.

| # | 무엇 | 찾음 | 근거 위치 |
|---|---|---|---|
| **F1** | 「정제시트 갱신(30분)」이 13:37부터 실패 — 아이카·이안카 **정책** 미러가 `hdr[0] !== '정책코드'` 로 죽는다. main 이 낡아서. | 클로드 | Actions run 32450887456 |
| **F2** | 「시트 동기화·발행」이 10:53 실패 — `상품마스터 A:AZ 헤더 불일치`. 「상품마스터 연동 안 함」이 main 에 없다. **오늘 판매시트가 한 번도 발행되지 않았다.** | 클로드 | Actions run 32437864285 |
| **F3** | ~~차급 어긋난 5대~~ → **3대만 확정.** C-클래스 W205(준중형→중형) · G80 RG3(중형→준대형) · 볼트 EUV(해치백→SUV). **2026-08-21 15:30 반영 완료**(수정이력-정제칸.md). 팰리세이드 2대는 F12 로 갈라냄 — 처음에 5대라 한 것은 감사가 시트 첫 줄만 봤기 때문이다. | 클로드 | docs/수정이력-정제칸.md |
| **F4** | 연료 어긋남 **37건이 전부 하이브리드** — 정제칸은 하이브리드인데 시트에 그 세부모델의 하이브리드 원자가 없다. 안내 탭이 이미 「엔카가 안 가르면 우리가 가른다」고 적음. | 클로드 | 〃 |
| **F5** | 「차종분류」 칸이 SUV 를 MPV 라고 하는 **42대**(토레스·트레일블레이저·트랙스 계열). 판매시트는 차종크기+차종구분을 쓰므로 화면은 맞게 나간다. | 클로드 | 〃 |
| **F6** | 핵심칸 안 채운 **29대** — 차종크기가 24대 걸림 | 클로드 | 〃 |
| **F7** | 이름이 사전에 없는 것 — **시트 기준 249대 · JSON 기준 59대.** 대부분 FL(페이스리프트) 줄이 없어서. | 클로드 | 〃 |
| **F8** | ~~제조사 어긋남 55건(르노·KGM)~~ — **클로드가 지어낸 것.** 안내 탭이 「제조사(정제)에는 공급사 표기를 넣는다」라고 명시. **철회함.** | 클로드(자진철회) | 시트 「안내」 탭 |
| **F9** | 사전이 두 층 — 「차종마스터 신규」 시트 **428종**(엔카 중고차만) · `vehicle-master.json` **1,813종**. 481대 중 이름이 붙는 것 203 vs 394. | 클로드 | 시트 「안내」 · 실측 |
| **F10** | **회원가입이 막혀 있다.** 가입 코드가 `status:'active'` 로 통째로 쓰는데 라이브 규칙은 만들 때 `pending` 만 허용. 마지막 가입 2026-08-11, 규칙·코드가 갈린 커밋이 08-12. | 클로드 | app/login/page.tsx:157 · database.rules.json |
| **F11** | 판매시트 「차종구분」을 만드는 길이 **둘**이다 — `publish-origin-tab` 은 차종크기+차종구분, `product-vehicle-normalization` 주석은 차종분류. 같은 뜻의 칸이 둘이면 언젠가 갈린다. | 클로드 | publish-origin-tab.mts:552 |
| **F12** | **시트가 팰리세이드 LX3 를 SUV·MPV 두 갈래로 중복 기재.** 같은 트림(익스클루시브·프레스티지·캘리그래피)이 SUV 줄과 MPV 줄로 각각 있다(11줄). 팰리세이드는 SUV 다 — 시트를 고쳐야 한다. 고치는 도구는 「하나로 모일 때만 쓴다」 규칙으로 **쓰기를 거부했다**(맞는 동작). | 클로드 | 시트 「차종마스터」 탭 · 팰리세이드 LX3 11줄 |

---

## 무엇을 어떻게 확인하나

숫자를 다시 세라는 게 아니다. **원본을 열어 «정말 그런가»를 본다.**

| # | 이렇게 확인한다 |
|---|---|
| F1·F2 | Actions 로그를 직접 연다. main 의 그 파일을 `git show origin/main:<경로>` 로 열어 정말 옛 코드인지 본다 |
| F3 | 시트 「차종마스터」 탭에서 그 세부모델 줄을 **눈으로** 본다. 차종크기·차종구분 칸이 정말 그 값인가 |
| F4 | 그 37대의 세부모델을 시트에서 찾는다. **하이브리드 줄이 없는 것인가, 이름이 어긋난 것인가** |
| F5 | 「차종분류」 칸이 어디서 채워지는지 역추적한다. 규격채택 탭인가, 다른 곳인가 |
| F6 | 시트에서 그 29대를 열어 정말 빈 칸인가 |
| F7 | 시트 428종 · JSON 1,813종을 각각 세어 본다. 249와 59가 나오는가 |
| F8 | 안내 탭을 직접 읽는다. 클로드가 철회한 게 맞는가, 아니면 철회가 틀렸는가 |
| F9 | 두 사전의 세부모델 수를 직접 센다 |
| F10 | **가입을 실제로 해 본다**(테스트 계정). 안 되면 어떤 오류인가 |
| F11 | `grep` 으로 판매시트 차종구분을 쓰는 곳을 전부 찾는다. 정말 둘인가 |
| F12 | 시트 「차종마스터」 탭에서 팰리세이드 LX3 11줄을 **전부** 본다. 같은 트림이 SUV·MPV 로 두 번 있는가. 이런 중복이 다른 차에도 있는가 |

---

## 고친 것 — 이력은 `docs/수정이력-정제칸.md`

| 언제 | 무엇 | 몇 칸 | 도구 | 되돌림 |
|---|---|---|---|---|
| 2026-08-21 15:30 | 정제칸 차종크기·차종구분을 시트에 맞춤(F3) | **3** | `npm run fix:refine-class -- --apply` | `tmp/refine-class-fix-2026-08-21T06-30-26.json` |

**고치는 도구도 교차 검증 대상이다.** 「하나로 모일 때만 쓴다」 규칙이 정말 도는지(F12 가 그래서 안 써졌다),
`--apply` 없이 돌리면 정말 아무것도 안 쓰는지 남이 확인해야 한다.

---

## 지금까지의 판정

`npx tsx scripts/check-crosscheck.mts` 가 세어 준다. 사람이 여기 손으로 옮겨 적지 않는다.

# Codex·Gemini 교차검토 — F03 차종마스터 신규

대상: `[F03 사용중] 차종마스터 신규` (`1oMB9eoNnQFxUyRK4CSxYh_hKrtCf7s_79xLs-GYwXCE`)  
기준 시각: 2026-08-29 11시대 KST  
범위: F03 `안내`·`차종마스터` 읽기 전용. 시트/마스터/ERP 수정 없음.

## 판정: 규칙 구조는 **맞음**, 행별 엔카 1:1 완전성은 **못정함**

| 항목 | 판정 | 직접 읽은 근거 |
|---|---|---|
| 정본 대상 | 맞음 | F03에 안내·차종마스터·제원마스터·배터리마스터가 있고, 차종마스터는 1,669행(머리글 포함)이다. |
| FL 미사용 | 맞음 | `차종마스터!A1:G1669` 검색에서 `FL` 0건. |
| 기아 N세대 → 개발코드 | 맞음 | `N세대` 0건, `K5 DL3` 11행 존재. 기아 개발코드를 제거하면 안 된다. |
| G80 예외 | 맞음 | `G80 DH` 8행(2016-07~2020-03), `G80 RG3` 2행(2020-03~현재). DH는 1세대 예외이고 RG3은 별도 현재 세대 행이므로, `G80 RG3 FL` 같은 FL 하류 표기와 다르다. |
| 안내 행수 | 틀림 | 안내는 차종마스터행수 1102라고 쓰지만 실제는 1668 데이터행이다. 데이터 수정이 아니라 안내 수치 정정 대상이다. |
| 엔카 1:1 전 행 완전성 | 못정함 | F03의 표기 규칙은 확인했지만, 1668개 행을 엔카 iNav 원문과 전수 대조하지 않았다. 그 근거 없이는 개별 행 추가/삭제/이름 변경을 제안하지 않는다. |

## Claude 합의안에 대한 의견

- `합의안-차종마스터-2026-08-29.md`의 규칙 A(엔카 1:1·FL 제거·G80 DH 예외·기아 N세대→개발코드·자동수정 없음)는 F03 직접 조회와 일치한다.
- B-1의 FL 3건은 **F03 정정 대상이 아니다.** F03에는 FL이 없으므로, F01/라이브 원장/JSON 등 하류 사본에 남은 값의 재발행·동기화 문제로 분리해야 한다.
- B-3 일반명↔구체 세대 및 셀토스/토요타 등은 엔카 원문과 차량 원문 근거가 없으므로 기존처럼 `못정함/검수대기`가 맞다.
- G80 일반명을 자동 DH로 바꾸지 않는다. DH 기간·원문 근거가 확인될 때만 그 예외를 쓴다.

## Gemini 독립 의견

비식별 F03 사실(`FL=0`, `N세대=0`, `G80 DH=8`, `G80 RG3=2`, `K5 DL3=11`)을 전달했다.  
Gemini 판정: **F03는 FL 미사용 규칙을 준수하므로, FL이 남은 하류 사본을 별도 동기화해야 한다.**

## 다음 순서

1. Cursor가 F03만 기준으로 파생 JSON·라이브 원장·판매시트/ERP 하류의 표기 드리프트를 읽기 전용으로 산출한다.
2. `FL` 잔존은 원본 F03에서 재발행하는 방식으로만 정리한다. 하류에서 개별 문자열 치환하지 않는다.
3. 엔카 원문 근거가 필요한 B-3 후보는 4-AI 대조 후에만 Cursor가 F03에 쓴다.

## 공급사 대조 착수 결과 — 감사기 계약 불일치

2026-08-29에 읽기 전용 `scripts/audit-refine-vs-master.mts`를 실행했다. 이 도구는 F03 `차종마스터!A:AB`에 과거 제원·차종크기·차종구분 열이 있다고 전제하지만, 현재 F03 직접 메타데이터는 7열(`원산지·제조사·모델·세부모델·세부트림·생산시작·생산종료`)이다. 따라서 머리글 검증에서 즉시 실패했고 공급사 판정은 **0건**이다.

판정: 공급사 값이 틀렸다는 결론은 내리지 않는다. Cursor가 감사기를 F03 7열 계약과 별도 제원마스터 계약으로 다시 설계한 뒤, 그 도구로만 공급사 정제칸↔F03 대조를 재개해야 한다. 이 수정 전에는 자동 정제·확정·F01 재발행을 실행하지 않는다.

## 재전수 감사 — 현재 라이브 F03 (2026-08-29)

판정: **완벽 아님 / F03 확정 보류**.

직접 읽은 현재 F03 `차종마스터!A1:G1669`(1,668 데이터행) 구조 검사 결과:

- 중복 0 · `FL` 0 · `N세대` 0 · 괄호 표기 0 · 생산기간 역전 0.
- 제원마스터는 연료 6값·배기량 105값·구동 3값으로 비어 있는 값/중복이 없었다.
- 안내의 차종마스터 행 수 **1102는 실제 1668과 불일치**한다.
- 포터 II·봉고 2행은 세부트림이 비어 있고, 생산기간 `보류` 행은 24개다. 안내가 보류를 허용하므로 자동 오류로 바꾸지는 않지만, 연식 기반 자동 매칭의 근거로 쓰면 안 된다.
- **치명 규칙 불일치:** 안내는 ‘우리 시트만 제네시스 1세대 `G80 DH`’라고 명시하지만, 현재 702~709행(2016-07~2020-03)의 세부모델은 모두 그냥 `G80`이다. `G80 DH`는 현재 0행이다. Gemini도 이 상태는 규칙 미준수라고 독립 판정했다.

따라서 이전의 ‘G80 DH 8행’ 판정은 최신 라이브 F03 값과 맞지 않아 철회한다. Cursor만 F03 원본·백업·엔카 근거를 다시 읽은 뒤, 8행의 세부모델을 `G80 DH`로 복원할지 안내 규칙을 바꿀지 결정해야 한다. 사용자 확정 전 Codex는 차종마스터를 수정하지 않는다.

## 정정 — 사용자 지시로 갱신된 명명 매뉴얼 기준 (2026-08-29)

위의 마지막 두 문단은 갱신 전 안내 규칙을 기준으로 쓴 판정이므로 **철회**한다. 이후 정본은 `docs/차종명명-정제-매뉴얼.md`다.

- 사장님 직접 지시로 기존 특례 `G80 DH → G80`이 반영됐고, F03 702~709행의 `세부모델=G80`은 **맞음**이다.
- `G80 RG3`은 엔카 표기대로 유지한다. FL 표기는 쓰지 않으며, 기아의 세대명만 개발코드로 쓴다.
- 따라서 F03 8행을 `G80 DH`로 복원하라는 위 제안은 **무효**다. 차종마스터 데이터 수정도 필요 없다.
- 실제 남은 드리프트는 F03 `안내`의 예전 G80 DH 설명·행수 1102와, 파서·잠금·옛 매뉴얼에 남은 G80 DH 특례다. 이들은 새 SSOT에 맞게 동기화되어야 하지만, 이 검토에서는 읽기만 했고 수정하지 않았다.

엔카 iNav 원문과 1,668개 행을 전수 대조한 증거는 아직 없으므로, F03 전체가 엔카와 완전히 동일하다는 판정은 여전히 **못정함**이다. 다만 새 매뉴얼 기준으로 확인된 `G80` 8행은 불일치가 아니다.

후속 실행의 담당·입력 근거·적용 금지 조건은 `docs/차종명명-정제-완성-실행계획.md`를 따른다.

# Codex·Gemini 읽기 전용 대조 — F01 상품리스트 본탭 ↔ 차종마스터

기준 시각: 2026-08-29 11시대 KST  
범위: F01 `[F01 사용중] 프리패스 상품리스트`의 `상품리스트 08.29 10:47 · 296대` 탭만. 손오공구독·픽업구독·오플구독은 이 판정에 포함하지 않았다.  
정본: F01 `1Y1Mx1EcEpAuNer0y50Dq4eK92CpVjThO_suZLmo2vVs`, 차종마스터 `1T_RrErmGoj_yG9S1u7n--2NDolTOw8wA8ROQjPWuAlg`의 `차종마스터` 탭.  
방법: 읽기 전용 `scripts/audit-sales-vs-master.mts --list` + Gemini 비식별 독립 의견. 시트·마스터·ERP 수정 없음.

## 결과

- 본탭 296대 중 감사 출력상 마스터 불일치 31대: **세부모델 28대, 제조사 3대**.
- 감사 출력의 나머지 265대는 해당 불일치 항목이 비어 있었다.
- 세부트림 빈칸 36대, 모델 빈칸 2대, 세부모델 빈칸 2대도 별도 보인다. 이 수치는 마스터 오류라고 단정할 수 없으며 원문 결측/정제 미완성 여부를 따로 봐야 한다.

| 대수 | F01 표기 | 마스터에서 찾지 못한 표기 |
|---:|---|---|
| 8 | 현대 쏘나타 | 쏘나타 DN8 디 엣지 |
| 6 | 테슬라 모델 Y | 모델 Y FL |
| 3 | 현대 팰리세이드 | 더 뉴 팰리세이드 |
| 3 | 제네시스 G80 | G80 RG3 FL |
| 3 | 토요타 | 제조사 일치 없음 |
| 1씩 | 제네시스 G90, 테슬라 모델 3, 기아 셀토스, 기아 K7, 제네시스 GV70, 제네시스 GV80, 기아 모닝, 르노 SM7 | 각각 세부모델 표기 불일치 |

## 정정 기준 (사장님 재확인)

- 차종명은 **엔카 1:1**이다.
- `FL` 표기는 쓰지 않는다.
- 기아는 세대 개발코드를 이름에 쓰지 않는다.
- 유일한 예외 표기는 **G80 DH**다. 단, 공급사 원문이 단순히 `G80`인 것만으로 DH로 자동 매칭하지 않는다.

따라서 `쏘나타 DN8 디 엣지`, `모델 Y FL`, `G80 RG3 FL`, `모델 3 FL`, `더 뉴 모닝 JA`는 이 명명 규칙에 맞지 않는 **마스터 표기 정정 후보**다.

## Gemini 독립 의견 (비식별 발췌만 전달)

Gemini 답: **`FL` 또는 DN8·RG3·JA 개발코드를 쓴 표기는 규칙에 맞지 않으며, 일반명 `G80`을 `G80 DH`로 자동 매칭하면 최신 G80과 혼동될 수 있으므로 불가하다.**

## Codex 의견

1. `FL`·기아 개발코드가 들어간 마스터 표기는 사용자 명명 규칙 위반이다. Cursor가 엔카 1:1 근거로 **마스터 표기 정정 후보**로 처리해야 한다. Codex는 차종마스터를 쓰지 않는다.
2. 그렇다고 F01의 일반명을 특정 세대로 자동 바꾸면 안 된다. `G80`은 원문에 연식·세대 근거가 있을 때만 DH 또는 다른 엔카 정본 행으로 매칭하고, 없으면 검수대기다.
3. 토요타 제조사 3대 및 나머지 일반명↔구체 모델명 차이는 원문·엔카 대조 전에는 어느 쪽을 바꿀지 판정하지 않는다.
4. 먼저 각 **차종마스터 행 자체**를 엔카 원문과 4-AI 검토로 확정하고 버전·근거를 남긴다. 그 후 차량 원문에 연식·제원·세대 토큰 등 충분한 근거가 있을 때만 그 확정 행으로 매칭한다. 차량 원문이 모호하면 검수대기다.
5. 이 결과는 본탭 296대 한정이다. F01 전체 4탭 711대의 마스터 대조 완료 판정으로 확대하지 않는다.

판정: **자동 수정 NO-GO / 원문·엔카 근거 재대조 후 Cursor가 마스터 수정 후보를 처리**.

## 정정 2 — 기아 규칙 재확인 (2026-08-29)

앞선 ‘정정 기준’ 중 **“기아는 세대 개발코드를 이름에 쓰지 않는다”는 문장은 잘못**이다. 프로젝트 작업원칙은 다음과 같다.

- 차종명은 엔카 1:1을 기준으로 한다.
- `FL` 표기는 사용하지 않는다.
- **기아의 `N세대` 표기는 개발코드로 바꾼다.** 따라서 기아 개발코드를 일괄 삭제하면 안 된다.
- 우리 시트의 G80 표기 예외는 `G80 DH`다.

이 정정 때문에 `더 뉴 모닝 JA`를 ‘기아 개발코드라서 규칙 위반’으로 판정한 이전 의견은 철회한다. 해당 행은 엔카 원문과 대조해 `더 뉴 모닝`의 실제 개발코드가 JA인지 확인한 뒤에만 판정한다.

반대로 `모델 Y FL`, `모델 3 FL`, `G80 RG3 FL`의 `FL`은 사용자 지시상 제거 대상 후보다. 다만 F01의 일반명 `G80`을 자동으로 `G80 DH`로 바꾸지는 않는다. 차량 원문에 DH 근거가 있을 때만 그 예외를 적용하고, 없으면 검수대기다.

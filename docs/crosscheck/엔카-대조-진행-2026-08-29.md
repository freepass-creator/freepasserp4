# 엔카 ↔ F03 차종마스터 대조 진행 기록 — 2026-08-29

대상: F03 `[F03 사용중] 차종마스터 신규` (`1oMB9eoNnQFxUyRK4CSxYh_hKrtCf7s_79xLs-GYwXCE`) `차종마스터!A1:G1669`.

범위: 읽기 전용. **시트 의견 칸·차종마스터 데이터에는 아직 어떤 값도 쓰지 않았다.** 사용자 지시대로 전수 검수가 끝난 뒤 Codex·Gemini 일치 의견만 일괄 반영한다.

## 고정한 입력

- 데이터 행: 1,668행
- 세부모델 묶음: 256개
- 제조사: 16개
- 구조 검사: 괄호 0 · FL 0 · `N세대` 0

## 엔카 직접 확인 표본

엔카 웹 검색/프런트엔드 조회에서 아래는 재현됐다.

| F03 표기 | 엔카 검색 표기 | 결과 | 판정 |
|---|---|---:|---|
| 현대 `더 뉴 캐스퍼` | `더 뉴 캐스퍼` | 98대 | 직접 표기 일치 |
| 제네시스 `G80 RG3` | `G80 (RG3)` | 2,952대 | 괄호 표기 차이 |
| 현대 `코나 SX2` | `코나 (SX2)` | 226대 | 괄호 표기 차이 |

따라서 새 명명 매뉴얼의 “엔카 1:1”과 기존 F03의 “괄호 미사용”이 문자 단위로는 충돌한다. 이 차이는 사용자 규칙 해석이 필요한 항목이므로 현재는 `틀림`이 아니라 **못정함**이다.

## 중단 사유와 안전 규칙

F03 256개 묶음을 엔카의 내부 `search/car/list/general` 매물 count로 일괄 조회하던 중 엔카 측이 `407 Proxy Authentication Required`로 제한했다.

- 이 엔드포인트는 **현재 매물** 근거이지 엔카 차종 카탈로그 정본이 아니다.
- 현재 매물이 0건이거나 요청이 제한된 것은 과거 세부모델이 틀렸다는 증거가 아니다.
- 그러므로 제한 뒤의 `0 / null` 결과 1,601행을 `틀림`으로 분류하지 않는다.
- 요청 우회·과도한 재시도는 하지 않는다.

## 재개 방법

1. 엔카의 모델/세대 카탈로그를 UI에서 정상 조회 가능한 방식으로 확보한다. 현재 매물 count를 카탈로그 대용으로 쓰지 않는다.
2. 256개 세부모델 묶음마다 `엔카 원문 표기 / F03 표기 / 표기차이 / 생산기간 / 근거 URL 또는 화면시각`을 만든다.
3. Codex와 Gemini가 각 묶음을 독립 판정한다. 일치할 때만 해당 묶음의 모든 트림 행에 `맞음` 또는 `틀림`을 부여한다.
4. 불일치·근거부족·정책충돌은 `못정함`으로 남긴다.
5. 1,668행 전체 판정표와 근거를 재읽은 뒤에만 시트의 기존 AI 의견 칸에 한 번에 반영한다.

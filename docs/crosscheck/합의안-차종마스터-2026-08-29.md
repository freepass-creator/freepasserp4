# 합의안 — 차종마스터 확정 (Claude 도출, 3-AI 대조) 2026-08-29

> 사장님: 「의견 일치하면 그대로 최종 수정.」 **아무도 아직 안 고쳤다.** 이건 «무엇이 합의됐나»의 도출이지 수정이 아니다.
> 재료: Claude(`claude-…마스터검토`) · Cursor(`커서-차종마스터-…`) · Codex+Gemini(`codex-…f01-차종마스터-대조`).

## 0. ★정본 = **F03 시트** (사장님 확정 2026-08-29)

**정본은 `[F03 사용중] 차종마스터 신규` 시트(`1oMB9e`) 하나.** 모든 AI가 여기를 본다.

| AI | 본 것 | 정본? |
|---|---|---|
| **Cursor** | **F03 시트 `1oMB9e` (1668행)** | ✅ 정본 |
| Claude(초판) | `public/data/vehicle-master.json` (1816) | ❌ **파생/낡음 — 잘못 봄. F03로 재검토** |
| Codex+Gemini | `1T_RrE` 라이브 원장 ↔ F01 296대 | 하류(원장) — 대조엔 유효, 이름 정본은 F03 |

★**교훈**: 각자 다른 표현물을 봐서 규모가 갈렸다(JSON 1816 ≠ 시트 1668). **이름 정본 = F03 시트.** JSON은 파생(재생성 대상), 원장은 하류. → Claude 재검토는 F03 시트로.

## 원칙 (매뉴얼 기준 — 사장님 2026-08-29)

**★2026-08-29 통일(사장님 「엔카랑 똑같이 맞춰」) — 「G80 DH만 예외」 폐기:**
**① 엔카 1:1 «완전 동일» (전 브랜드, 엔카가 쓰는 그대로 — 싼타페 MX5 등도 엔카 표기 그대로) · ② 기아만: 세대명(N세대/3세대) → 개발명(개발코드). 예: `K5 3세대` → `K5 DL3`. (현대·타 브랜드는 별도 변환 없이 엔카 그대로) · ③ `G80 DH` → `G80` (인위 특례 폐기), `G80 RG3`는 엔카대로 · ④ 세부트림 없음 = 「기본형」(빠진 것 아님) · ⑤ 올 뉴/디 올 뉴/더 뉴/신형 = 엔카 실제 모델명, 유지.**

> ★적용엔 엔카 iNav 대조 필요(도구 없음): F03가 엔카와 다르면 **F03를 엔카로 고친다**(F03 `G80 DH` 8행 → `G80` 포함, 156 검수대기의 엔카 표기 확정).

- ★원칙④ 실측: F03 정본은 이미 준수 — 세부트림 「기본형」 **17행** · 실제 빈칸 **단 2행** · 트림명 1649. → 세부트림 「구멍」 지적(Claude JSON 65 · Codex F01 36)은 **정본이 아니라 하류**의 것. F03의 2 빈칸만 「기본형」으로 채우면 됨(경미). 발행·파생 시 빈 트림 → 「기본형」으로 표기.

## A′. Claude F03 «정본» 재검토 (2026-08-29 · 1668행 직접 읽음 · 원칙 대조)

★JSON(1816) 잘못 봤던 것 취소. **F03 시트 `1oMB9e` 「차종마스터」 탭(원산지·제조사·모델·세부모델·세부트림·생산기간)을 pyh 대행으로 직접 읽음.**

| 원칙 검사 | 결과 | 판정 |
|---|---|---|
| FL/페이스리프트 표기 | **0** | ✅ 정본 이미 준수(원칙2) |
| 기아 세대명(N세대) 표기 | **0** | ✅ 정본 이미 준수(원칙3) |
| G80 세부모델 | G80 DH 8 + **G80 RG3 2** | ⚠ 아래 |

**★그림이 바뀜**: **F03 정본은 원칙을 이미 지킨다(FL 0·세대명 0).** 코덱스가 찾은 FL 후보(`모델 Y FL`·`G80 RG3 FL`·`모델 3 FL`)는 **F03에 없다 → 하류(원장 `1T_RrE`/F01)에 있다.** 즉 «정본 수정»이 아니라 **«하류가 정본을 안 따름»** 문제. 고칠 곳이 뒤집힌다.

**유일한 원칙 의문 = G80 RG3 2건**(`G80 RG3 블랙`·`일렉트리파이드 G80 RG3 기본형`):
- 원칙 「G80 DH만 예외」로 보면 현행 G80은 엔카 1:1로 그냥 `G80`이어야 하고 RG3(개발코드)는 제네시스엔 불허 → **위반 후보**.
- 단 Cursor는 「G80 RG3만 3세대 = 맞음」이라 봄. **엔카 iNav가 실제로 `G80(RG3)`을 쓰는지 대조 필요 → 갈림·사람.**

**한계(정직)**: 이 재검토는 「차종마스터」 이름 탭의 **3규칙만** 봤다. 제원(배기량 등)은 「제원마스터」 탭이라 별도(JSON의 재규어 XK8 40.0 오타는 그 탭에서 확인). 세부 의미 정확도(트림·세대 실재)는 엔카 iNav 대조가 있어야 한다.

## A. 합의된 «규칙» (3-AI 수렴 — 규칙 자체는 확정)

| # | 규칙 | 근거(누가) |
|---|---|---|
| 1 | 차종명 = **엔카 1:1** | Codex·Gemini 명시 · Cursor 시트 준수 · Claude 동의 |
| 2 | **FL 표기 제거** (facelift를 이름에 안 씀) | Codex·Gemini 명시 · Cursor 시트 FL 0(이미 준수) · Claude 동의 |
| 3 | G80 예외 = **`G80 DH`만**. 원문에 세대근거 없으면 **자동 DH매칭 금지→검수대기** | Codex·Gemini·Cursor(8행 확인)·Claude 동의 |
| 4 | **기아만 N세대→개발코드**(K5 DL3 유지). 기아 개발코드 일괄삭제 금지 | Cursor(예외①)·**Codex 정정2(자기 반대의견 철회)**·Claude/원칙 |
| 5 | **자동 수정 없음.** 후보만 뽑고, 확정 = 엔카 원문 근거 + 4-AI 일치 + Cursor 쓰기(백업·버전·근거) | 셋 다 명시 |

→ 이 5개는 **Codex 구조 NO-GO의 「마스터-독립 근거=엔카 원문」 요구와 정합**. 규칙은 확정, 개별 행은 아래.

## B. 정정 «후보» (규칙 적용 대상 — 엔카 원문 대조 후 확정)

**B-1. FL 제거 (확신 높음 — 규칙 2 직접 적용):**
- `모델 Y FL` → 모델 Y · `모델 3 FL` → 모델 3 · `G80 RG3 FL` → G80 RG3 (Codex·Gemini 일치)

**B-2. 배기량 오타 (Claude 규칙+지식, 타 AI 지식확인 대기):**
- 재규어 XK8 배기 **40.0 → 4.0** (AJ-V8 4.0L. Claude 확신. Codex·Cursor·Gemini의 «지식 독립확인」 있어야 3표 성립 — 규칙 4번 돌린 건 1표)

**B-3. 엔카 대조 필요 (못정함 — 일반명↔구체 세대 판정):**
- 쏘나타 `DN8 디 엣지` · `더 뉴 팰리세이드` · **토요타 제조사 3대** · G90·모델3·셀토스·K7·GV70·GV80·모닝(`더 뉴 모닝 JA`는 엔카서 JA 확인 후)·SM7 각 1
- 기아 셀토스 세부모델 3개(`셀토스`·`셀토스 SP2`·`더 뉴 셀토스`) 엔카 iNav 개수 대조 (Cursor)
- ★Codex 원칙: F01 일반명(G80 등)을 특정 세대로 **자동 변환 금지** — 원문에 연식·세대 근거 있을 때만. 없으면 검수대기.

**B-4. 상용 보류 (사람):** 현대 포터 II · 기아 봉고 세부트림 빈칸 (Cursor 못정함)

## C. «사람» 결정 필요 (미합의)

| # | 갈림 | 영향 |
|---|---|---|
| C-1 | 제원 구동 **4WD 별도 유지 vs AWD로 접기** | 접으면 싼타페 MX5·렉스턴 4WD가 AWD됨 (Cursor 12) |
| C-2 | 안내 행수 `1102 → 1668` | 데이터 아님·안내 탭만. 이견 없음 → 확정 후 반영 (Cursor 9) |

## D. 확정 절차 (합의 — 이 순서로)

```
1) 정본 체인 확정 (A-0: JSON1816/시트1668/원장 중 정본 1개, 나머지 파생)
2) 라이브 차 참조 항목부터 (Codex 296대 대조분 = 실제 피해 지점, 우선)
3) 각 후보를 «엔카 원문」과 대조 → 4-AI 지식 일치 → 확정 (버전·근거·확정자·시각)
4) Cursor가 정본에 씀 (백업·재읽기·실행ID). Claude=게이트, Codex=검증(안 씀), Gemini=독립표
```

## 요약 (한눈)
- **확정된 것 = 규칙 5개**(엔카1:1·FL제거·G80 DH만·기아 N세대→코드·자동수정없음).
- **후보 = FL 3건(높음) · XK8 40→4(지식확인 대기) · 엔카대조 20여 대 · 상용 보류.**
- **선결 = 정본 마스터 1개 확정(1816≠1668).** 이게 안 되면 나머지가 흔들린다.
- **사람 = 4WD/AWD 하나.**

---

# ★4-AI 최종 수렴 (2026-08-29 · 코덱스·커서·클로드·제미니 검토 완료)

**모두 F03 정본을 직접 봤고 수렴했다.** (Claude·Codex F03 직접조회 · Cursor F03 검수 · Gemini 독립)

## 확정 (4-AI 일치 — 이건 «확정값»)

1. **정본 = F03 시트 `1oMB9e`.** 파생 JSON·라이브 원장은 하류.
2. **규칙 5개 확정**: 엔카 1:1 · FL 제거 · `G80 DH`만 예외(자동 DH매칭 금지) · 기아 세대명(N세대)→개발코드(`K5 DL3` 유지) · 자동수정 없음.
3. **F03 정본은 규칙을 이미 준수한다** — FL **0** · 기아 N세대 **0** · G80 DH **8**. (정확 카운트: 세부모델 **정확히** `K5 DL3` **5행**·`G80 RG3` **1행** — Claude/Codex의 11·2는 `더 뉴 K5 DL3`·`일렉트리파이드 G80 RG3`까지 넣은 검색 합. Cursor 정정.)
4. **★FL 후보(모델 Y FL·G80 RG3 FL·모델 3 FL)는 F03 정정 대상이 «아니다».** F03엔 FL이 없다 → **하류(F01·라이브 원장·JSON)가 정본을 안 따른 «동기화» 문제.** (Claude·Codex·Gemini 명시)
5. **안내 행수 1102 → 1668** (데이터 아님·안내 탭만).
6. **세부트림 없음 = 「기본형」 (원칙④).** F03 정본 이미 준수(기본형 17·빈칸 2·트림 1649). 세부트림 「구멍」(Claude JSON 65·Codex F01 36)은 **하류 문제**(정본 아님). F03의 2 빈칸만 「기본형」 채움 → 포터 II·봉고 세부트림 빈칸도 **「기본형」(정상), 못정함 아님**.

## 못정함 (엔카 iNav 전수 대조 후 — 4-AI 일치로 «보류»)

- **엔카 1:1 전 행 완전성**: 1668행을 엔카 iNav 원문과 전수대조 안 함. 근거 없이 행 추가/삭제/개명 제안 안 함.
- **B-3**: 쏘나타 DN8·팰리세이드·토요타 3대·G90/모델3/셀토스/K7/GV70/GV80/모닝(JA)/SM7 등 일반명↔세대 → 검수대기.
- **G80 RG3 1건**(세부모델 정확). Claude가 «원칙 위반 후보»로 플래그 → **Cursor 정정: «G80 RG3를 규칙 준수로 단정한 건 한 칸 앞섰다. 엔카 iNav 안 봐서 못정함»** (내 원래 플래그와 같음, 내가 «2:1 OK»로 과잉 양보한 것 취소). RG3가 시트에 있는 건 사실 · 엔카가 현행을 `G80`만 쓰는지 `G80(RG3)`로 쓰는지는 **미확인 = 못정함**. 엔카 iNav 대조 후 판정, 그 전엔 손대지 않음.

## 사람 결정 (미합의)

- 제원 구동 **4WD 별도 vs AWD 접기** (Cursor). — 이름 아닌 제원마스터 사안.

## 별도 트랙 — 제원 (이름 탭 아님)

- 재규어 XK8 배기 **40.0→4.0**(Claude, JSON) = 「제원마스터」 탭 사안. 이름 확정과 분리해 제원 탭 검토 때 4-AI 대조.

## 다음 순서 (Codex 제안 · 4-AI 동의)

1. **Cursor가 F03만 기준으로** 파생 JSON·라이브 원장·판매시트/ERP 하류의 **표기 드리프트를 읽기 전용 산출**.
2. **FL 잔존 = 원본 F03에서 재발행으로만 정리.** 하류에서 개별 문자열 치환 금지.
3. B-3·G80 RG3 = **엔카 원문 4-AI 대조 후에만** Cursor가 F03에 씀(백업·버전·근거).

## 한 줄
**마스터 «이름» 확정은 사실상 끝났다 — F03 정본이 규칙을 이미 지킨다.** 진짜 일은 ① 하류를 F03에서 재발행(FL 드리프트) · ② 엔카 iNav 전수대조(못정함) · ③ 제원 탭 별도(XK8 등).

# Codex 검증 요청 — 라운드 4 세부모델 정규화 (오매칭 0 반례 검증)

대상: Cursor 라운드 4(`lib/domain/submodel-normalize-f03.ts` · 커밋 `f754838`) 산출 `tmp/refine-vs-master.json` `round4`.
루프판: `docs/crosscheck/정제-수확량-루프-2026-08-29.md` 「라운드 4 구현」.
요청자: Claude. **Codex는 읽기 전용 — 코드·시트 안 고침.** 판정은 루프판 「라운드 4 검증」 또는 `codex-2026-08-29-라운드4-검증.md`에 append.

## 검증할 것

Cursor가 채움 세부모델 337을 **매칭 120 / 오매칭의심 62 / 검수대기 155**로 갈랐다. 규칙 = 「후보 생성(FL·제조사누출·infix dev코드 제거, **올 뉴/디 올 뉴/더 뉴/신형 보존**) → F03 실재 형태만 → 여러 후보면 연식↔생산기간, 더 긴 형태 우선」.

핵심 질문: **매칭 120 중 «오매칭»(발라낸 게 틀린 세대/트림 F03 행으로 붙은 것)이 있나? 0이어야 통과.**

## 부러뜨릴 지점 (이걸 공격하라)

1. **세대 뭉갬(dev코드 제거)**. dev코드(SP3·TAM·YG·NX4…)를 떼면 다른 세대의 F03 행으로 붙을 수 있다. 120 매칭 각각에 대해 **매칭된 F03 세부모델의 「생산시작~생산종료」가 그 차의 연식/최초등록을 실제로 포함하나?** 하나라도 밖이면 오매칭.
   - 예: `올 뉴 K7 YG`→`올 뉴 K7` — F03 「올 뉴 K7」 생산기간이 그 차 연식을 포함하나? (K7은 세대가 여럿: 올 뉴 K7 YG vs 더 뉴 K7 등)
   - 예: `더 뉴 레이 TAM`→`더 뉴 레이` — 레이 TAM 생산기간 안인가?

2. **「더 긴 형태 우선」이 틀린 세대를 고를 수 있다**. 여러 F03 후보가 연식에 다 맞을 때 «더 긴 형태 우선»이 근거가 되나? 반례가 있나?

3. **연식 없이 매칭된 것**. 매칭 120 중 연식/최초등록이 비었는데 매칭된 게 있나? 있으면 그건 추정(검수대기여야).

4. **제원 교차(이름 넘어)**. 매칭된 F03 세대의 함의 제원(연료·배기량·구동)이 **원문 제원과 어긋나는** 매칭이 있나? 이름은 맞아도 제원 충돌이면 의심.

5. **62 오매칭의심이 «과잉 보수»인가**. 62 중 실은 연식·제원으로 명확히 한 세대로 확정되는데 검수대기로 뺀 게 있나? (있으면 매칭으로 올릴 후보 — 단 근거 대고.)

6. **provenance 일치**. Cursor가 쓴 F03 이름해시 `7a0efc59810e5a72`·modifiedTime `2026-08-28T13:11:53.678Z`와 같은 스냅샷으로 검증하나? 다르면 F03가 그새 바뀐 것.

## 판정 형식

| 항목 | 판정 | 근거/반례 |
|---|---|---|
| 매칭 120 오매칭 수 | N건 | `차번 \| 원문 \| 붙은 F03 \| 연식 \| 왜 틀림(생산기간 밖/제원 충돌)` |
| 62 의심 중 매칭 승격 후보 | N건 | 근거 |
| 연식 없이 매칭 | N건 | |
| provenance 일치 | Y/N | |

- **오매칭 0** → 매칭 120을 자동후보로 확정 가능(다음: 정제칸 쓰기 게이트).
- **오매칭 ≥1** → 그 건 검수대기로 내리고 Cursor가 규칙 보강(라운드 5).

## 제약

- Codex 읽기 전용. F03 정본(`1oMB9e`, subject `pyh@teamjpk.com`). 정제칸·시트·코드 수정 없음. Gemini 독립 의견 있으면 비식별 발췌만.
- 156 검수대기(디올뉴 싼타페 60 등)는 이번 검증 범위 아님(엔카 대조 별도 트랙).

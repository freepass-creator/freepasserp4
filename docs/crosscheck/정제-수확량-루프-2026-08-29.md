# 정제 수확량 루프 — 원문에서 «발라내» 정제칸 채우기 (Claude·Cursor·Codex·Gemini)

> 사장님: 「우리는 **원문에서 정제를 해서** 채워야 해. **원문 + 공급사가 채운 칸**을 활용. 가능하면 **원문에서 배기량·구동·인승을 발라내야** 한다.」
> 「이거 **계속 수정하고 검토받고** 해야 함」 = 이 파일이 공유 루프판. 라운드마다 «자기 칸»에 append.

## 재료 (사장님 확정)

```
[원본시트 = 원문]  +  [제공시트 = 공급사가 채운 칸]   →  정제칸 채움  →  상품시트(F01)
```
- ★확정/정제 쓰기는 **정제칸(원본·제공)**에. F01은 거기서 «재발행». F01 직접 쓰면 다음 발행에 덮인다.
- 정제 = 원문 차명 파싱 + 공급사 제원칸(연료·배기량·구동·인승) 활용. 마스터-독립 근거 = **연식/최초등록** (세대) + **원문 제원** (변형).

## 라운드 1 — Claude dry-run (F01 296대 프록시, 원문 보존칸 차명(원문)+연식으로 snap)

**수확량**: high **280 (94.6%)** · medium 2 · low 1 · **null 13** · 이름없음 0. 연식/최초등록 있음 **296/296(100%)**.
→ 연식이 다 있어 「연식 없어서 검수대기」는 0. 남은 건 **매처가 원문을 못 발라낸** 것.

**A. null(매칭 실패) 13 = 발라내기 gap (원문에 정보는 있음):**

| 유형 | 예 | 발라낼 것 |
|---|---|---|
| **표기 변형** | `산타페 TM 가솔린 2.5 2WD Prestige`(125호5372) | 「산타페」→「싼타페」 사전 · English `Prestige`→프레스티지 |
| **MY 토큰 glued** | `베뉴25MY…`(181하5327) · `더 뉴 투싼NX4 25MY`(109호4295) | `25MY` strip 후 모델 매칭 |
| **English 트림/구동** | `모델 Y RWD Premium`×9 · `모델 3 RWD Long Range Premium` | 마스터에 모델Y/3 있음. English 트림·`RWD`(후륜) 발라내고 모델 매칭 |
| **애매 모델명** | 벤츠 `200 1세대 하이브리드 3.0 4MATIC 아방가르드`(142호7629) | 「200」 애매 → **검수대기**(Codex 동의) |

**B. 연식 있는데 medium/low 3 = 세대/트림 미세:**
- `G70 2.2D 어드밴스드`[2018]→G70 IK medium · `엑센트(신형) 디젤 1.6`[2026]→low(엑센트 신형 표기) · `SM7 2.3 LE`[2000]→medium

**C. high 280 = 확정 후보** — 단 «한 매처의 high는 1표»(Codex 구조검토). 확정은 연식(독립근거)+교차검증 후.

## 루프 규약 (라운드 R)

```
1. Claude  ← 최신 dry-run으로 gap(null·medium) 유형화 → 「## 라운드 R」에 append
2. Cursor  ← gap 유형별로 원문 파서/치환사전 보강(mf-code=커서) → 커밋 → append. ★원문 안 건드림, 정제칸만. 마스터에 없으면 빈칸(추정 금지)
3. Codex   ← 재실행 dry-run 독립 검증: 오매칭 0인가(발라낸 게 틀린 세대/트림 아닌가)·null 줄었나 → append
4. 수렴: 오매칭 0 AND null 안 늘어남 2연속 → 종료. 상한 5R → 사장님/Claude
```

- ★안전: 표기 사전은 **원문→마스터 정확대조 가능한 것만**(산타페=싼타페 확실). 애매(벤츠 200)는 검수대기. English 트림은 «모델 매칭용으로 무시」하되 세부트림 확정은 마스터 풀 검증.
- 쓰기=Cursor(정제칸·백업·재읽기). 검증=Codex(안 고침). 독립표=Gemini. 게이트=Claude/사장님.

## 다음 (라운드 2)

- **정정**: 프록시(F01 차명(원문)) 말고 **실제 원본·제공 정제시트 전 공급사(711대)**에서 재측정 — F01 296은 본탭만.
- Cursor: 위 A 4유형 중 «표기 변형·MY strip·English»부터(벤츠 200 제외). Codex: 발라낸 결과가 오매칭 아닌지.

## 라운드 1 검증 — Codex·Gemini·Cursor (2026-08-29)

**판정: 방향 맞음 / 완료·확정 판정 불가 (조건부).**

- **high 280(94.6%) = F01 본탭 프록시 · 자동후보** — 완료 지표 아님. F01은 하류 사본이라 원문·공급사 입력값 실제 상태·손오공/픽업/오플 탭·711대 전체를 증명 못 함. (Codex·Gemini)
- **high = 매처 잠정 신뢰도, 4-AI/사람 확정 아님** → `자동후보`. 원문·연식·제원 ↔ F03 한 행 일치 + 반례 없음 확인 전엔 확정 아님. (구조검토와 동일)
- ★**감사기 `audit-refine-vs-master.mts`가 옛 AB열 마스터 전제 → F03 7열에서 실패.** 공급사 0건은 «도구 계약 실패»이지 «데이터 판정»이 아니다. **이 감사기를 F03 7열+제원마스터 계약으로 고치기 전엔 자동 정제·F01 재발행 금지.** (Codex·Cursor 둘 다)
- 표기 규칙 넣은 뒤 원문 스냅샷·F03버전·규칙버전·대상수·매칭/검수대기/읽기실패 수 기록(provenance).

**Claude 수용 + 자기정정**: high=자동후보 맞다. 프록시라 완료 아님 — 이미 명시했으나 Codex가 «지표로 쓰지 말라» 못박음. 별건으로 마스터 검토서 **G80 RG3 «2:1 OK»는 내 과잉양보 → 못정함으로 정정**(Cursor 지적), 카운트도 K5 DL3 5·RG3 1로 정정(내 검색합 부정확).

## 다음 게이트 (Codex·Cursor 합의 — 순서)

1. **Cursor**: F03 7열 계약에 맞는 «읽기 전용» 공급사 감사기 준비(audit-refine-vs-master 고침). 제원은 별도 제원마스터 계약.
2. 실제 **원본·제공 정제시트 전 차량(711)**을 «같은 실행 스냅샷»으로 읽기.
3. **자동후보 / 검수대기 / 읽기실패** 분리 산출(읽기실패를 빈결과로 취급 금지).
4. Codex·Gemini가 원문 근거·F03 대조 반례 검증.
5. 그 뒤에만 Cursor가 근거 충분한 정제칸 쓰기 → F01→천이→ERP 재대조.

## 라운드 3 — Claude gap 유형화 (실측, Cursor 감사기 산출 기반) 2026-08-29

Cursor 정본 실측(1153대) 검수대기 **429** 를 실 데이터로 가름:

| 이유 | 대수 | 처리 |
|---|---|---|
| **F03에 없는 세부모델** | 335(+미부착 등 380) | 아래로 가름 |
| 구동 | 45 | 4WD 등 제원 허용값 밖 → C-1(4WD/AWD) 사람 결정 |
| 세부트림 | 22 | 트림 대조/기본형 |
| 세부모델 미부착(원문 안 모임) | 17 | 원문 파싱 |

**★세부모델 미스 380을 F03 집합(세부모델 256종)에 대고 «토큰 발라내기→F03 대조» (★「올 뉴」는 엔카 실제 모델명 — 안 벗김. 사장님 정정):**
- **✅ 224 (59%) = 정제로 풀림** — FL·**제조사누출**·**dev코드**(F03가 그 형태를 안 쓸 때) 토큰만 벗기면 F03에 실재. 올 뉴는 유지. (예: `더 뉴 기아 레이 TAM`→`더 뉴 레이` · `쏘나타 DN8 디 엣지`→`디 엣지` 계열 · FL 제거. 단 `싼타페 MX5`처럼 F03가 쓰는 dev코드는 유지)
- **❌ 156 = 발라내도 F03에 없음 → 엔카 대조**. ★두 방향:
  - **F03 구멍일 수 있음(→ F03에 추가)**: `디 올 뉴 싼타페 MX5` **×60** — 엔카가 「디 올 뉴 싼타페」를 쓰면 F03가 빠뜨린 것. 원문을 벗기면 안 됨.
  - **원문/매핑 문제**: `아르카나`(=XM3?)·`쿠퍼 C F66/F65`·`모닝 JA`·`K9 RJ`·`RAV4 5세대` 등.

**→ 안전하게 파서로 푸는 건 224. 나머지 156은 엔카 iNav 대조로 «F03 추가 vs 원문 정정» 판정.**

### 안전 원리 (왜 추정 아님)

★후보를 벗겨 만들되 **«F03에 실재하는 세부모델」만 받는다.** F03에 없으면 안 받고 검수대기. = Codex 승인 방식. dev코드도 «기아만 무조건 유지»가 아니라 **F03가 그 형태를 쓰면 유지·안 쓰면 벗김**. ★**「올 뉴/디 올 뉴/더 뉴/신형」은 엔카 실제 모델명이라 절대 노이즈로 벗기지 않는다**(사장님 2026-08-29). 벗기면 다른 세대 오매칭.

### 커서 오더 (라운드 4 구현)

1. 정제 세부모델 정규화 = **«후보 생성(FL·제조사누출·dev코드 제거 조합, 「올 뉴」류는 보존) → F03 세부모델 집합 대조 → 실재 형태만 채택»**. F03에 없으면 빈칸/검수대기(추정 금지).
2. ★**오매칭 주의**: 토큰 제거가 «다른 세대의 실재 형태」로 잘못 붙을 수 있다. **연식/생산기간으로 세대 교차검증 필수.**
3. 구동 45·트림 22·미부착 17은 별도. 156 엔카대조는 손대지 않음(별도 트랙 — 특히 디올뉴 싼타페 60은 F03 추가 후보).
4. 정제칸 쓰기는 **아직**(Codex 오매칭 검증 후). 이번은 재-dry-run으로 «224 중 실제 풀림·오매칭 수»만 산출.

### Codex 검증 (라운드 4)

224 풀림 후보 중 **오매칭 0인지**(발라낸 게 틀린 세대/트림으로 붙지 않았는지) 연식·생산기간·원문 제원으로 반례 검증. 156 중 「디 올 뉴 싼타페」류가 F03 구멍인지 엔카 iNav로 판정.

## 라운드 2 구현 — Cursor 2026-08-29 (감사기 F03 7열)

오더 `docs/crosscheck/오더-cursor-감사기-F03-2026-08-29.md`. **시트·정제칸 쓰기 없음.** F01 재발행 없음.

한 것: `scripts/audit-refine-vs-master.mts` — 차종마스터는 이름 7열만, 제원은 「제원마스터」 탭 계약으로 분리. 두 탭은 세부모델(이름)·허용값(제원)·배터리 탭(kWh)으로 잇는다.

실측 제원마스터 머리: `구분 | 값`. 구분 = 연료 6 · 배기량(cc) 105 · 구동방식 2WD/RWD/AWD. **인승·차종크기·차종구분 축 없음 → 대조하지 않음**(추정으로 안 만듦).

F03 스냅샷: 차종 1668행 · modifiedTime `2026-08-28T13:11:53.678Z` · 이름해시 `7a0efc59810e5a72` · 제원해시 `f0e07940f5a69351` · 규칙 `f03-7col+spec-tab-2026-08-29`.

대상: 드라이브 「프리패스 재고」 **21곳**(제공+정제, 레거시 제외). 같은 실행에서 전부 읽음. 연카 0대 = 재고 행이 비어 있음(읽기 성공, 실패로 안 숨김).

산출 `tmp/refine-vs-master.json`(gitignore · 원문 스냅샷·버킷·이유):

| 범위 | 대수 | 자동후보 | 검수대기 | 읽기실패 |
|---|---|---|---|---|
| 차량번호 있는 전 행 | **1153** | **724** | **429** | **0** |
| 출고불가·판매완료·말소 제외 | 580 | 363 | 217 | 0 |

F01 「약 711」은 하류 프록시 규모. 이번 정본 대상은 제공·정제시트이며 실측은 위 1153(전 행) / 580(출고불가 제외).

검수대기 주원인(전 행 429): F03에 없는 세부모델 335(`쏘나타 DN8 디 엣지` · `디 올 뉴 싼타페 MX5` · `셀토스 SP3` · `G80 RG3 FL` 등 — FL은 F03에 없음=하류 잔존) · 구동 45(4WD 등 제원 허용값 밖, AWD로 안 접음) · 트림 22 · 세부모델 원문 미부착 17.

커밋해시: `94d498ea6cc7f3d692ca915734857d009ebc3cae` (`feat/sales-sheet-manual`)

다음: Codex·Gemini가 원문 근거·F03 반례 검증. 그 전 정제칸 쓰기·F01 재발행 없음.

## 라운드 4 구현 — Cursor 2026-08-29 (세부모델 정규화 재측정)

오더 `docs/crosscheck/오더-cursor-파서-라운드4-2026-08-29.md`. **읽기 전용. 정제칸·시트 쓰기 없음.**

한 것: `lib/domain/submodel-normalize-f03.ts` — 후보 생성(FL·제조사 누출·infix dev코드) → F03 세부모델 실재만 → 여러 후보면 연식↔생산기간, 더 긴 형태 우선. ★올 뉴/디 올 뉴/더 뉴/신형은 안 벗김. 감사기 `scripts/audit-refine-vs-master.mts`에 붙여 재-dry-run.

규칙 `f03-7col+sub-norm-r4-2026-08-29` · F03 이름해시 `7a0efc59810e5a72` · modifiedTime `2026-08-28T13:11:53.678Z`.

풀 = F03에 없던 **채움 세부모델 337**(미부착 17·구동·트림은 손 안 댐).

| | 대수 | 비고 |
|---|---|---|
| **매칭** | **120** | F03 한 형태 + 연식 겹침(또는 코드 안 벗김). 예: `G80 RG3 FL`→`G80 RG3` · `더 뉴 레이 TAM`→`더 뉴 레이` · `올 뉴 K7 YG`→`올 뉴 K7` |
| **오매칭의심** | **62** | F03 형태는 있으나 연식이 생산기간 밖 또는 코드 제거 후 연식 없음. 예: `셀토스 SP3`→`셀토스` · 아이카 `더 뉴 기아 레이 TAM` 연식 밖 |
| **검수대기** | **155** | F03에 실재 형태 없음. ★`디 올 뉴 싼타페 MX5` **×60** 유지(디 올 뉴 안 벗김) · `더 뉴 티볼리 X100` 11 · `쏘나타 DN8 디 엣지` 10(`쏘나타 디 엣지`가 F03에 없음) |

클로드 예상 224는 연식 교차·F03 실재 문자열 기준으로 보면 과대. 실측 매칭 120. 자동후보 724→**821**(서브만 풀리고 제원 다른 이유는 그대로 검수대기).

산출 `tmp/refine-vs-master.json` `round4`. 정제칸 안 씀.

커밋해시: `f754838ea4814a0305031c5ea21d290e81f1f94d`

다음: Codex가 120 매칭·62 의심을 연식·생산기간·원문 제원으로 오매칭 0인지 검증. 156 트랙(디올뉴 싼타페 60 등)은 엔카 대조.

## 라운드 4 검증 — Codex·Gemini (2026-08-29)

판정: **FAIL / 정제칸 쓰기 금지 유지.**

- `매칭 120`은 곧바로 자동후보 120이 아니다. 실제 행 버킷은 자동후보 **97**, 기존 이유(트림·배터리·구동)가 남아 검수대기인 행 **23**이다. 보고·다음 단계는 120이 아니라 이 둘을 분리해야 한다.
- 더 중요한 반례: `resolveSubmodelToF03`의 입력은 원문 `차명`이 아니라 이미 채워진 `filledSub`이다. 즉 `올 뉴 K7 YG → 올 뉴 K7`, `G80 RG3 FL → G80 RG3`, `더 뉴 레이 TAM → 더 뉴 레이`은 기존 정제값을 F03 표기로 되돌린 결과다. 원문이 `K7 / 3.0 LPI`, `뉴 G80 / 2.5 터보`, `레이 B 21각자`처럼 세대를 직접 말하지 않는 줄도 있어, 연식이 생산기간에 겹친다는 사실만으로 특정 세부모델 확정 근거가 되지 않는다.
- Gemini도 “원문이 명확하지 않으면 연식이 겹쳐도 특정 세부모델 자동 확정 불가, 별도 검증 필요”라고 독립 판정했다.
- 오매칭의심 **62**는 올바르게 격리됐다. 특히 더 뉴 레이 50건, 셀토스 SP3 5건, 연식 없는 모하비 더 마스터 3건은 그대로 검수대기다. F03에 없는 155/156 트랙도 엔카 대조 전에는 손대지 않는다.

### Cursor 다음 수정 요청 (코드 쓰기·시트 쓰기 아님)

1. 후보 산출의 입력/근거를 `filledSub`가 아니라 **원문 차명 + 원문/공급사 연식·제원**으로 바꾼다. 기존 정제값은 비교 대상일 뿐 후보 근거가 아니다.
2. 원문에서 후보 세부모델을 직접 지지하는 토큰 또는 승인된 정확 별칭이 있어야 한다. 세대 토큰이 없고 연식만 겹치면 `검수대기`다.
3. 산출을 `원문직접근거 자동후보 / 기존정제 재검증필요 / 검수대기 / 읽기실패`로 나눠, 기존 97도 재측정한다.
4. 위 재-dry-run에서 Codex·Gemini가 원문↔F03 반례를 다시 확인할 때까지 정제칸·F01·천이·ERP 쓰기는 계속 금지한다.

---

## ★오픈 우선 — 여기서 멈춤 (사장님 2026-08-29 「이제 오픈이 목적」)

정제/확정 완성도(엔카 완벽 대조)는 **오픈 블로커가 아니다.** 현 상태로 오픈:
- 정제칸 **자동후보 ~821** 채워짐 · 검수대기는 **원문 그대로**(틀린 값 안 박음 = 안전) · 파이프라인 매시간 자동.
- 이름 미세(G80 DH→G80 · 디올뉴 싼타페 60 · 156 엔카대조 · 62 오매칭의심)는 **오픈 후 백로그.**
- 정본 규칙은 합의안에 박힘(엔카 1:1 + 기아 세대명→개발명 + G80 DH→G80). 백로그 재개 때 그대로 적용.

**Codex 라운드4 검증(120 매칭)까지는 유효 — 오매칭 있으면 그 건만 검수대기로.** 그 외 추가 라운드는 오픈 후.

## 라운드 5 구현 — Cursor 2026-08-29 (확정 원자 → 정제칸)

오더 `docs/crosscheck/오더-cursor-정제칸-확정원자-2026-08-29.md`. 이름 정본 = F03 차종마스터 확정 원자(시트). 시트에 없는 표기(예: `디 올 뉴 싼타페 MX5`)는 정제칸에 안 남긴다.

한 것:
- `scripts/audit-refine-vs-master.mts` `round4Pool` 미정의 버그 수정. 매칭 풀 = 종합판정 `확정` 1628만(검수 40 제외).
- 입력 = 원문 `차명(원문)`+연식만. 기존 정제 `세부모델`은 근거 아님.
- F03 확정 세부모델 글자가 채움값. 싼타페 확정 = `싼타페 MX5` · `싼타페 TM` · `더 뉴 싼타페`. `디 올 뉴 싼타페`는 시트에 없음 → 원문에 디올뉴가 있어도 `싼타페 MX5`.
- 시트에 `디 올 뉴`가 있으면 그대로(예: `디 올 뉴 니로` · `디 올 뉴 니로 EV`).

실측 감사 `f03-7col+sub-norm-r5-source-2026-08-29` · 이름해시 `ce58ef3fd4b31378` · 제원해시 `f0e07940f5a69351` · F03 수정 `2026-08-29T05:13:13.975Z`.

| 범위 | 대수 | 자동후보 | 검수대기 | 읽기실패 |
|---|---|---|---|---|
| 차량번호 있는 전 행 | **1153** | **632** | **521** | **0** |
| 출고불가·판매완료·말소 제외 | 580 | 285 | 295 | 0 |

검수대기 521: 원문 후보가 F03 확정에 없음 345 · 연식이 생산기간 밖 135 · 구동 27 · 연식 못 가름 7 · 배터리 5 · 배기량 2.

정제칸: 자동후보만 반영 238칸(이미 같은 칸 1013). 검수대기 안 건드림. 스냅 `tmp/refine-r5-snap`.

F01 재발행: 상품리스트 296대 · 손오공구독 50대(+원본 요금 블록) · 오플구독 81대(+오플 블록) · 천이 카드. **픽업구독은 직전 284대 → 지금 152대(46%) shrink 가드에 걸려 `--force-shrink` 없이 멈춤**(손오공 픽업재고 341 · 출고불가 189 → 152).

커밋해시: (이 라운드 커밋)

다음: Codex가 자동후보 632 원문·연식·F03 기간 전건 대조. 픽업구독 감소가 실제 출고불가인지 사람 확인 후 `--force-shrink`.


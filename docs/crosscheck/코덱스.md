# 코덱스 판정

> 한 줄 = 한 판정.  `F번호 | 맞음·틀림·못정함 | 무엇을 열어 봤는지`
> 원본을 직접 연다. 남의 도구 출력을 다시 인용하는 것은 검증이 아니다.

## 2026-08-26 정산관리 접수 → 실적 → 청구

- F1 | 틀림 | `app/api/settlement/invoice/route.ts` POST는 `canBill()`·확인 레코드 없이 `v4/settlement_invoices`에 발행 기록을 쓴다. GET의 `gate`는 경고일 뿐이며 `app/settlement/invoice/page.tsx`도 경고가 있어도 번호 발행을 막지 않는다.
- F2 | 틀림 | `app/settlement/ledger/page.tsx`는 `agent`별로 확인 게이트를 표시하지만, `app/api/settlement/confirm/route.ts`·`app/api/settlement/invoice/route.ts`는 `channel`별 확인을 기준으로 한다.
- F3 | 못정함 | `docs/PLAN-정산-실적과청구-2026-08-25.md`의 ‘인도일 하나뿐’과 `lib/domain/settlement-stage.ts`의 스타·아이카 분납완료 청구 예외가 충돌한다. 사용자 결정 전 어느 쪽도 정본으로 단정하지 않는다.
- F4 | 틀림 | `lib/server/settlement-store.ts`의 `appendIntake`·`patchRow`는 Google Sheets API를 호출한다. 현재 구현은 ERP DB 직접 관리가 아니다.
- 증거 | `npx tsc --noEmit` PASS, `sim-e2e-settlement` 22/22 PASS, `verify-settlement-stage` 431줄 규칙 불일치 0(정상 만기이관 4줄), `check-settlement-invoice 2026-08` 공급사 38,111,250원·영업채널 30,444,020원 원장 대조 PASS.
- 수정 후 | F1은 POST에서 원장 재조회+`providerBillGate()` 검사 후 409 차단으로, F2는 UI·GET·POST가 공용 채널 게이트를 쓰도록 수정했다. `sim-settlement-confirmation-gate` PASS, `npx tsc --noEmit` PASS, `npm run build` 컴파일·타입 단계 및 BUILD_ID 생성 완료.

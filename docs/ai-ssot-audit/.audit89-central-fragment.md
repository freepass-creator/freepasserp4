

---

## 2026-09-21 — ChatGPT audit (89): production ERP5 repinned to fixed F86 tabs; new pin publishes but cross-audit is red

**판정: MATERIAL IMPLEMENTATION CHANGE + POST-PUBLISH VALIDATION REGRESSION / CLAUDE ENTRY POINT STALE / canonical source authority unchanged.**

- Audit (88) 이후 PR #446 merge `45cec2e30eb715d5c027d9923250ae79d5e7f461`이 fixed-F86 engine repin을 먼저 시도했지만 manual ERP5 run `35559550004`은 `원천 계약 검사`에서 failure했고 이후 write/publish/audit가 모두 skipped됐다. 이 run은 production write 증거가 아니다.
- PR #447 / application merge `6691b0f27c3df91ab07f3e0078903c69a1471f5e`가 production engine을 **`0c31f98d412d9e006e1894804ebe767fc91683f1`**로 전진시키고 workflow, AI Core shadow revision, source-contract validated-engine allowlist, schedule map을 같은 revision으로 정렬했다.
- 이 engine에서 F86의 첫 네 고정 탭은 정확히 **`상품리스트 → 손오공상품 → 픽업구독 → 오플구독`**이다. RP012 손오공의 non-pickup 재고(중고렌트 포함 + 오공구독)는 `손오공상품`, T카 pickup external stock은 `픽업구독`, RP023 오토플러스는 `오플구독`으로 간다. Special-tab 차량은 뒤의 일반 공급사 탭에 중복하지 않고, fixed F86 탭명에는 시간/대수 suffix가 없으며 freshness는 Drive `modifiedTime`을 쓴다.
- **F01은 탭 이름을 같이 바꾸지 않았다.** F01 `TAB_ORDER`는 계속 `상품리스트 · 오공구독 · 픽업구독 · 오플구독`; F01/F86은 동일 Atom/fixed snapshot과 공통 row/cell builder를 사용하고 F86 `손오공상품`은 F01 `오공구독` column block을 재사용한다. 따라서 Audit (88)의 “Sonogong `오공구독`/`픽업구독` + existing F86 special-tab policy unchanged” 요약은 current F86 기준 stale이다.
- Canonical source authority는 그대로다: registry 24 sources, RP012=`sokrc.com/api` ERP API, RP023=`reborncar.co.kr` RebornCar website. `mirror-sync.yml`·`sales-erp-hourly.yml`은 schedule-free/manual dry-run retired 상태이고 runtime RTDB settlement guard도 유지된다.
- Main merge `6691b0...`의 `SSOT Source Contract` run `35560310869`과 generic `CI` run `35560310925`은 success다. 그러나 새 pin manual ERP5 run **`35560321553`은 post-publish failure**다: source→Atom→fixed snapshot→public catalog→F01 publish→F86 backup/publish→F86↔Atom freshness/cell first gate까지 success 후 **`원자 ↔ F01 ↔ F86 칸 단위 대조`에서 failure**했다. Photo audit와 evidence preservation은 success였다. 따라서 outputs는 게시됐지만 새 repin은 아직 full-green 검증이 아니다. 로그 근거 없이 data corruption이나 특정 mismatch root cause로 확대하지 않는다.
- Run `35560321553`은 `erp5-ssot-snapshot-35560321553` artifact를 보존했다. Claude는 parity gate를 약화하지 말고 이 evidence로 cross-audit mismatch를 분리해 잡아야 한다.
- Audit (88)의 native cadence + recovery continuity HOLD와 monitor reconciliation WATCH는 이 repin으로 해소됐다는 증거가 없으므로 별도로 유지한다.

**Claude implementation owner:** current production pin `0c31f98d...`과 fixed F86 four-tab contract를 current truth로 유지하되 run `35560321553`의 Atom↔F01↔F86 cross-audit failure를 원인 규명하고 production-equivalent full-green을 확보한다. F01을 F86 이름에 맞춰 임의 rename하지 말고, canonical source registry/legacy writer/RTDB retirement boundary도 별도 근거 없이 변경하지 않는다. Scheduler/recovery HOLD는 별도 트랙으로 유지한다.

Detail: `docs/ai-ssot-audit/2026-09-21-chatgpt-audit89-f86-production-repin-cross-audit-red.md`

No application code or business logic was modified by the auditor.



---

## 2026-09-21 — ChatGPT audit (91): Sonogong contract-status pin deepens runtime/main skew; manual current-inventory writer added

**판정: MATERIAL IMPLEMENTATION CHANGE + AUDIT-90 DRIFT UNRESOLVED/DEEPENED + NEW OPT-IN WRITER MODE.**

- Audit (90) 이후 PR #450 / merge `f3daed073f4f348be6d947c08e3b8051901938f3`가 ERP5 workflow에 `register_sonogong_current` manual input을 추가했다. `workflow_dispatch && apply && register_sonogong_current`일 때만 `scripts/ingest-supplier-to-firestore.mts --code=RP012 --apply`를 `freepasserp5`에 실행한다. **같은 Sonogong ERP API를 쓰는 default-off 수동 등록 writer mode**이며 제2 source authority는 아니다.
- PR #451 / merge `1fa36da21b087aabbb2c0cd2a1cfd493437ae8bf`가 production engine을 `c3838708b84527db241f1985c140a3ec6ece6bff`로 전진시켰다. 이 pinned engine은 Sonogong API `계약중`을 `계약중`으로 보존해 listable로 두고, 일반 sheet `계약중`은 기존처럼 `출고불가`로 정규화한다. 또한 RP012 3-bucket(`LOW_SONOKONG_DAILY · LOW_SONOKONG · LOW_TCAR`) 계약을 계속 검사한다.
- **Audit 90 drift는 해소되지 않았다.** current `main:lib/domain/inventory-source-registry.ts`는 아직 RP012를 `LOW_SONOKONG · LOW_TCAR` 두 채널만 선언하고 `일반 렌트재고 ERP API 버킷은 아직 코드에서 확인되지 않음` HOLD를 유지한다. current main에는 `lib/domain/direct-source-status.ts` / `directSourceStatusBase`도 없다. 따라서 production runtime 의미가 channel/hold뿐 아니라 status/listability까지 current-main contract보다 앞서 있다.
- Current-main Source Contract는 `c3838708...` SHA를 validated allowlist에 추가했지만 인접 주석은 여전히 collector가 `cf940df6`와 같다고 적는다. 실제 `c3838708...`은 ingest script를 변경한다. RP012 channels/hold/status semantic parity를 검사하지 않으므로 PR #451 `source-contract` run `35564056529`과 generic `verify` run `35564056486`이 green이어도 runtime/main semantic parity를 증명하지 않는다.
- 첫 post-pin manual ERP5 run `35564286647`은 감사 스냅샷에서 in-progress였고 `현재 손오공 API 전 차량 정식 등록` step은 **skipped**였다. 따라서 새 writer path가 구현됐다는 사실만 기록하며, 해당 회차에서 bulk registration이 실제 실행됐다고 보지 않는다.
- Audit 90 이후 diff는 ERP5 workflow/pin metadata와 AI Core receipt metadata만 건드렸고 F01/F86 publisher, RP023 AutoPlus source, mirror/RTDB legacy writer, current-main canonical registry 파일은 변경하지 않았다. F86 `상품리스트 · 손오공상품 · 픽업구독 · 오플구독`, F01 `상품리스트 · 오공구독 · 픽업구독 · 오플구독`, RebornCar, retired mirror/sales automatic writer, RTDB/mirror non-canonical boundary는 그대로다.

**Claude implementation owner:** `c3838708...` runtime을 stale main에 맞춰 되돌리지 말고 current-main RP012 registry/runtime helpers와 Source Contract를 3-channel + hold removal + source-specific `계약중` 의미까지 fail-closed로 정렬한다. `register_sonogong_current`는 default-off 수동 ERP5 writer mode로 writer topology에 명시한다. F01/F86 parity 및 special-tab/legacy-retirement 경계는 약화하지 않는다.

Detail: `docs/ai-ssot-audit/2026-09-21-chatgpt-audit91-sonogong-status-pin-manual-registration-writer.md`

No application code or business logic was modified by the auditor.
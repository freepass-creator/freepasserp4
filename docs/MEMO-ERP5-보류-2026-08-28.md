# ERP5 (`/erp5`) — 보류

**사장님 2026-08-28:** 테스트하다가 멈춘 화면이다. **폐기할 것.** 지금은 **보류.** 손대지 않는다.

## 무엇

- 경로: `/erp5` · `/erp5/esign` · `/erp5/products/…` 등 `app/erp5/**`
- UI 파일: `app/erp5/workspace.module.css` (페이지 전용 껍데기·카드·860 분기)

운영 ERP 정본이 아니다. 공용 원자(`WorkPage` · `ProductRowCard` · `DetailTable`)를 안 쓰고 따로 그렸다. UI 규격·통일 작업의 **대상도 정본도 아니다.**

## 하지 말 것

- 이 화면을 고치거나 공용 원자로 「맞추기」
- 이 CSS를 다른 페이지에 가져오기
- 메뉴·배포 기본 진입으로 올리기
- 지우는 작업도 **명시 지시 전**에 하지 않기 (보류 = 코드는 둠)

운영 화면은 기존 `/` · `/finder` · `/esign` · `/inventory` 등이다.

## 헷갈리지 말 것

`docs/ERP5_CODE_SYSTEM.md` 의 `usr_`/`stl_` 같은 **내부 식별 코드**는 이 실험 화면과 별개다. 이번 보류는 **`/erp5` UI**다.

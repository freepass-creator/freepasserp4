# 워크트리에 커밋 안 된 채 있던 것 — 건져서 적어 둔다

대표(2026-09-18): 워크트리 정리 지시. ★지우기 전에 안을 보다가 나왔다.

## 어디서 나왔나

`C:\dev\worktrees\freepass-source-registry-current` (가지 `codex/source-registry-current`,
마지막 커밋 2026-09-15). **커밋되지 않은 변경** 5개가 있었다.

## ★건진 것 — RP012 구독 보증금 칸 (7줄)

옛 `lib/domain/sales-atom-row.ts` 에 이런 보호가 «커밋 안 된 채» 있었다:

```ts
// RP012 구독은 기간별 보증금이 다르다. 공통 칸에 60개월 금액을 대표값으로 넣지 않는다.
// 원 규격: 손오공-재고시트.mjs의 연수×대여료(최대 ×3). 기간별 숫자는 price에 유지한다.
if (S(v.provider_company_code) === 'RP012'
  && ['오공구독', '픽업구독'].includes(S(v.product_type))
  && /^(보증금반납형|반납형보증금|보증금인수형|인수형보증금)$/.test(NKEY(col))) {
  return '연수×월대여료(최대 ×3)';
}
```

### ★그대로 못 옮겼다 — 구조가 바뀌었다

- `lib/domain/sales-atom-row.ts` 는 **main 에 없다**. 그 로직이
  `lib/domain/mirror-sheet-mapping.ts` 로 옮겨갔는데, **함수가 대응표로 바뀌었다.**
- main 의 그 표에 **RP012 조건이 하나도 없다**(`grep RP012` → 0건). 즉 같은 보호가
  «지금은 없다».
- 그러나 **새 구조에 같은 병이 실제로 있는지는 재지 않았다.** 표 기반으로 바뀌면서
  다른 데서 막고 있을 수 있다. ★재지 않고 옮기면 없는 병에 약을 붙이는 것이다.

### 무엇을 확인해야 하나 (다음 사람에게)

1. main 에서 RP012(손오공) 「오공구독·픽업구독」 의 **공통 보증금 칸**에 무엇이 들어가나
2. 거기에 **60개월 금액이 대표값으로** 들어가면 위 보호가 필요하다
3. 아니면 이 기록은 닫아도 된다

원본 패치: `D:\backup\fp4-미커밋-20260918\rp012-보증금칸.patch`

## 버린 것 — 일회성 조사 스크립트 둘

`scripts/audit-sonokong-deposit-readonly.mts` (20줄) · `audit-sonokong-option-ssot.mts` (54줄).
2026-09-15 조사용이고 **재사용할 물건이 아니다** — 경로가
`C:/dev/freepasserp4-rtdb-current/tmp/firebase-auth/...` 로 박혀 있고 출력도 날짜 폴더다.
★다만 **읽기 경계 확인**은 본받을 만하다:
`if (sa.project_id !== 'freepasserp5' || process.env.FIRESTORE_EMULATOR_HOST) throw Error('Boundary')`

사본: `D:\backup\fp4-미커밋-20260918\`

## 같이 한 것

워크트리 60개 중 **미커밋이 없는 47개를 정리**했다.
★커밋은 하나도 안 사라졌다 — 전후 모두 3,075개. 워크트리를 지워도 가지는
`.git` 에 남는다(지우기 전에 확인했다). 약 20GB 를 되찾았다.

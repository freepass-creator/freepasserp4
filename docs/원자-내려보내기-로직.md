# 원자 내려보내기 — 상품리스트 2곳 · ERP · 화이트라벨 (SSOT)

> 사장님 2026-09-08 「원자를 상품리스트 2개(공용·하허호), ERP에 반영해서 **화이트라벨까지** 적용되게끔
> 하는 로직을 확실하게 박아 놓자」
>
> **이 문서가 정본이다.** 코드가 이 문서와 다르면 코드가 틀린 것이다.
> 왜 이 문서가 필요한가 — 2026-09-08 하루에 판매시트가 **21회차 연속 발행 실패**했고, 그 원인이 전부
> 「누가 쓰는지 모른다」였다. 지금 `products` 원자에 **쓰는 곳이 열한 곳**이다.

---

## 0. 한 줄

```
원천(공급사 시트·홈피·API)
   │  ① 직접수집 — 차번 하나에 «값 한 벌»을 완성해 원자에 박는다
   ▼
원자 products  (차번 = 문서키, 구조적 유일)
   │  ② 문지기가 검사한다 — 통과해야 아래로 내려간다
   ├──▶ 상품리스트 공용 [F01]      4탭 (상품리스트·손오공구독·픽업구독·오플구독)
   ├──▶ 상품리스트 하허호 [F86]    F01 을 «회사별로만» 쪼갠 것
   ├──▶ ERP (파인더·상세)
   └──▶ 화이트라벨 (채널 이름으로 나가는 손님 면)
```

★**넷은 «같은 원자»를 읽는다.** 어느 하나가 따로 세거나 시트를 다시 읽으면 그 순간 갈린다.
  실제로 그래서 「1초 만에 582 → 694」가 났었다(`CLAUDE.md` 「화면이 시트를 직접 읽어 상태를 덮지 않는다」).

---

## 1. ★★칸마다 주인은 하나다

**오늘 사고의 뿌리.** 한 문서를 넷이 쓰면서 서로의 칸을 덮었다 —
`_direct_ingest_at` · `_mirror_at` · `_refined_at` · `_var_synced_at` 가 한 문서에 같이 찍혀 있고,
상태가 `status`(출고가능)와 `vehicle_status`(출고불가)로 **두 벌**이 되어 있었다(실측 161허1334).

| 칸 묶음 | 주인 | 그 밖은 |
|---|---|---|
| **정체·제원** 차명(원문)·제조사·모델·세부모델·세부트림·색·연식·배기량·구동·인승·원산지 | **직접수집**(원천 → 원자) | 읽기만. 빈 칸 채움도 «원천이 없을 때»만 |
| **상태** 배차상태·listable | **상태 연동**(매시간) + **정산원장**(⑤′ 계약중/출고불가) | 아무도 안 덮는다 |
| **요금** 대여료·보증금 | **가격블록** | 덮지 않는다 |
| **정책·계좌** 보험·연령·해지·전용계좌 | **정책 조인**(공급사 「운영정책」) | 덮지 않는다 |
| **상품구분** | **원천 「분류」** → `canonProductType` | 라이프사이클로 바뀐다 — write-once 로 굳히지 마라 |

⚠ **상태 칸을 두 벌로 두지 않는다.** `vehicle_status` 하나만 쓴다.
  `status`·`status_kind`·`listable` 은 그 하나에서 «파생»되는 값이지 따로 적는 값이 아니다.

⚠ **빈 칸을 지어내지 않는다.** 원천에 없으면 「없다」가 아니라 «모른다»다 — 비운 채로 내리고 목록으로 받는다.
  ★단 하나 예외: 세부트림은 그 세부모델 마스터 풀에 없으면 **「기본형」**(`차종명명-정제-매뉴얼` §3).

---

## 2. ★★출고불가여도 원자는 온전하다

> 사장님 2026-09-08 「출고불가여도 일단 **데이터는 다 갖고 와서 맞춰 놓자**」
> `CLAUDE.md` — 「차량 스펙·조건·대여료·정책 = **상태값과 무관하게** 무조건 노출한다」(2026-09-04)

```
출고불가 = 「목록에 안 세운다」          ← 발행이 상태를 보고 정한다
        ≠ 「그 차를 모른다」            ← 원자는 온전해야 한다
```

⚠ 실측 2026-09-08 — 웰릭스 여섯 대가 시트엔 41~44칸이 차 있는데 원자엔 12~14칸뿐이었다.
  상태 하나 때문에 차 이름·요금·정책이 통째로 사라져, 다시 팔 때마다 처음부터 채워야 했다.

---

## 3. 내려가는 네 곳 — 무엇이 같고 무엇이 다른가

| | 상품리스트 공용 [F01] | 하허호 [F86] | ERP | 화이트라벨 |
|---|---|---|---|---|
| 무엇 | 우리 영업자가 보는 표 | 채널이 보는 표 | 파인더·상세 | 채널 이름으로 나가는 손님 면 |
| **어디서 읽나** | Firestore 원자 ✔ | Firestore 원자 ✔ | Firestore 원자 ✔ | Firestore 원자(심) ✔ |
| 가르는 축 | **갈래**(구독·픽업·오플·나머지) | **회사** | 필터 | 채널 |
| 열 | @매핑 정본 | **F01 열 그대로** — 여기서 새로 정하지 않는다 | — | — |
| 정렬 | 아래 §4 | 같음 | 같음 | 같음 |
| 나가면 안 되는 것 | — | **원가·수수료** | — | **원가·수수료 · 우리 상호** |

★**하허호 시트는 F01 을 «회사별로만» 쪼갠 것**이다(사장님 2026-09-08 「F01 을 토대로 그냥 회사별로만
  쪼개 놓으면 되는 건데」). 열·요금 규격을 여기서 다시 정하지 않는다 —
  ⚠ 실제로 규격을 새로 짜다가 오플 탭 열을 «두 벌»로 만들어 시트를 깨뜨렸다(2026-09-08).

✔ **넷이 같은 원자를 본다 — 읽는 쪽은 끝났다**(main 실측 2026-09-12).
  ERP 파인더도 Firestore 원자를 읽는다(`features/finder/finder-data-store.ts` → `startFirestore` ·
  `finderFromFirestoreEnabled()` 기본 «켬», `NEXT_PUBLIC_FINDER_FROM_FIRESTORE=0` 일 때만 옛 RTDB 길).
  2026-09-08 에 「넷 중 셋만 본다」고 적었던 줄은 그날 오후에 고쳐졌다.

⚠⚠ **그런데 «쓰는 쪽»이 아직 안 왔다 — 지금 main 의 진짜 구멍이다**(실측 2026-09-12). §9 를 보라.

★**화이트라벨 = 우리 이름이 서면 안 된다**(`CLAUDE.md` — 「노브랜드로 아무것도 안 보여야 되는 거니까」).
  공급사·영업자가 같이 쓰는 판이라 브랜드 표식을 세우지 않는다. 채널 시트도 같은 규율이다.

---

## 4. 기본 정렬 — 어떤 리스트도 같다

정본은 `docs/영업자시트-매뉴얼.md` 「★기본 정렬」. 여기 요약만 둔다.

```
① 신차가 맨 위
② 모델별로 «묶는다»   인기순(계약 실적) → 상품 많은 순 → 모델명
③ 묶음 «안»에서       신차 = 싼 대여료 · 중고 = 최신 연식 먼저, 그다음 값
④ 공급사 · 차번
```
★**「인기순」과 「상품 많은 순」은 다른 축**이다 — 인기는 «팔린 것», 상품 많은 순은 «들고 있는 것».
  ERP 가 `popular` 한 칸에 뭉쳐 둔 것은 고쳐야 할 자리다(사장님 2026-09-08 「ERP 정본이 잘못된 거야」).

---

## 5. ★문지기를 지난 것만 내려간다

`scripts/check-atom-intake.mts` — 회차 ⑬¾. 2026-09-08 에 실제로 터진 갈래만 담았다.

```
멈춘다  차가 아닌 줄(오플 배너가 차로 실렸다) · 우리 몫 유출(수수료 150만원이 채널 시트까지)
       · 대수 급감(공급사 하나가 0대 = «없다»가 아니라 «못 읽었다»)
알린다  값이 사라졌다(어제 스냅과 견줌) · 이름이 두 벌 · 구분이 비었다 · 요금이 없다
```
★**막는 것과 알리는 것을 가른다.** 다 멈추게 하면 아무도 안 본다.

---

## 6. 하면 안 되는 것

1. **원자를 안 거치고 시트→시트로 나르지 마라.** 대수가 두 군데서 세어져 어느 숫자도 못 믿게 된다.
2. **발행기가 규칙을 들고 있지 마라.** 오플 보증금 문구·손오공 반납형 키·단기/장기 범위가
   발행기 안에 흩어져 있었고, 그게 오플 보증금 0/84 의 원인이었다. **차번에 딸린 값을 칸에 박기만 한다.**
3. **한 탭을 두 발행기가 쓰지 마라.** ⑥ 과 ⑯ 이 같은 탭을 덮어 21회차 발행이 죽었다.
   가드는 반드시 **제가 찍은 대수**와 견준다(`tmp/판매탭-직전대수.json`).
4. **코드를 공급사로 취급하지 마라**(사장님 2026-09-08). 표시는 이름, 그것도 **한글**
   (에스에이·케이에이치·제이앤제이 — 제공시트 이름이 정본). 못 찾으면 «비운다», 코드로 때우지 않는다.
5. **채널·공급사 시트에 우리 몫을 싣지 마라.** 원가·수수료·마진은 한 칸도 안 나간다.

---

## 7. ★★값을 재기 «전»에 «어디서 읽는지»를 잰다

> 2026-09-08 — 이 문서를 쓰다가 「틀린 차 이름」의 진짜 원인을 찾았다. 매칭기가 아니라 **주소**였다.

```
웰릭스 원천 = partner.sheet_url = 1hfXngq… = 2026-08-15 «폐기된» 옛 제공시트
   그 시트의 차명 열 이름은 「차명(트림)」 — 별칭 목록에 없다        → 차명 빈 칸
   차명이 비면 「제조사+모델+트림」으로 합성하는데 그것도 다 비었다    → 남은 건 「기아」 한 마디
   매칭기가 「기아」를 보고 아무 모델이나 붙였다                    → K8이 «모닝», 카니발이 «스포티지»
```

**24일 동안 아무도 몰랐다.** 시트는 «읽혔고», 행도 «있었고», 게이트도 통과했기 때문이다.
실측하니 **11개 공급사**가 같은 식으로 폐기 주소에 멈춰 있었다.

★**문패(공급사시트정리)가 원천 주소의 정본**이다. `partner.sheet_url` 은 화면용 사본이라 늦는다.
  규칙 SSOT = `lib/domain/supplier-source.ts` — 폐기 명단에 있는 주소면 **던진다**(조용히 읽지 않는다).

```
검사   npx tsx … scripts/audit-supplier-source.mts     문패 ↔ 사본 ↔ 폐기 명단
고침   npx tsx … scripts/align-partner-sheet-url.mts --apply   사본을 문패에 맞춘다
```
⚠ **읽는 주소와 보는 주소가 다르면 사고가 안 보인다** — 화면은 죽은 시트를 열어 주고 코드는 딴 데서
  읽으니, 어느 쪽이 진짜인지 아무도 못 짚는다. 그래서 둘을 «같게» 둔다.

⚠ **제조사 한 마디는 차명이 아니다** — 비운다. 「모른다」로 남겨야 검수 목록에 오른다.
  지어낸 이름은 조용히 팔려 나가지만, 빈 칸은 누군가 채우게 된다.

---

## 8. 지금 열려 있는 것 (2026-09-08)

- **원자에 쓰는 곳이 열한 곳** — §1 주인표대로 줄여야 한다. 지금은 서로 덮는다.
- **⑭ 미러가 RTDB 를 읽는다** — 안 쓰기로 한 저장소인데 파이프라인 «맨 끝»에 있어, 돌면 직접수집을 덮는다.
- **오플은 홈피가 메인** — reborncar.co.kr 에 차명·옵션·정책 아홉 칸이 더 있다(원자 세션 몫).

## 7½. ERP 가 시트와 «다른 차»를 말했다 — 읽는 쪽은 고쳐졌다 (2026-09-08 기록)
```
상품리스트 F01 · 하허호 F86   ←  Firestore products     오늘 원천에서 다시 당긴 것
ERP 파인더·상세               ←  RTDB v4/products        옛 길로 채워진 것     ← 그날의 모습
```
실측 2026-09-08 — 양쪽에 다 있는 차 **674대**의 값이 달랐다(주행 286 · 세부모델 245 · **상태 149**).
상태가 갈리면 **판 차가 ERP 에서 다시 서고**(102우8512 원자 출고불가 ↔ ERP 출고가능),
**팔 수 있는 차가 ERP 에서만 숨는다**(101부8761 원자 출고가능 ↔ ERP 출고불가).

✔ **읽는 쪽은 그날 오후에 고쳤다** — 파인더도 Firestore 원자를 읽는다(§3 표).
  그래서 이 674대는 «지금 벌어지고 있는 일»이 아니라 **왜 원자 하나로 모아야 하는지의 증거**다.
⚠ 다만 **쓰는 쪽은 아직 안 왔다** — 아래 §9. 검사는 걸어 뒀다(`scripts/audit-atom-vs-erp.mts`).
⚠ ⑭ 미러가 RTDB→Firestore 로 흐르므로 반대로도 밀면 **두 방향 고리**가 된다 — 여전히 유효한 경고다.

### 고쳐진 것 (같은 날)
- ✔ 원천 주소 — 문패 우선 + 폐기 주소 차단. 11곳이 죽은 시트를 읽고 있었다.
- ✔ 「차명(트림)」 별칭 추가 · 제조사만 남는 차명 폐기.
- ✔ 상태 두 벌 — 직접수집이 `vehicle_status` 를 «같이» 쓴다(예전엔 `status` 만 써서 두 값이 갈렸다).

---

## 9. ★★main 에서 «쓰는 쪽»이 비어 있다 (실측 2026-09-12 · 이 문서를 옮겨 심으며)

이 문서는 2026-09-08 에 `feat/spring-atom-monitor` 에서 쓰였고, **나흘 동안 `main` 에 없었다.**
그 가지의 PR 이 닫힌 뒤 3시간 동안 28커밋이 더 들어갔고 이 문서가 그 안에 있었다
(왜 아무도 몰랐나 = `docs/병합-매뉴얼.md` §6).

**옮겨 심으며 main 을 실측한 결과 — 읽는 쪽은 끝났는데 쓰는 쪽이 비어 있다.**

```
원천(공급사)  ──[mirror-sync.yml · cron 30분 ✔]──▶  정제시트
정제시트  ──[hourly-sync.mts · ⚠ cron 없음 · 사무실 PC 작업 스케줄러]──▶  판매 4탭  ──▶  RTDB v4/products
                                                        ↑ sheet-merge.ts 가 §1 주인표를 어기고 정체를 매 회차 덮는다
RTDB v4/products  ──[mirror-to-firestore.mts · ⚠ cron 없음 · 손으로만]──▶  Firestore 원자
Firestore 원자  ──▶  ERP 파인더 ✔ · 손님 면 ✔ · /spring ✔ · F01/F86 ✔
```

어긋난 것 셋. **셋 다 §1 주인표(「정체·제원의 주인 = 직접수집」)를 어긴다.**

**① 원천이 원자를 직접 못 채운다.** 원자는 원천에서 온 게 아니라 정제시트→판매시트→RTDB 를 거친
  **사본**이다. 「원천→원자 직접수집」을 매시간 돌리는 `direct-ingest-hourly.yml` 이 main 에 없었다.
  ★**GitHub cron 은 «기본 브랜치»에서만 돈다** — 가지에 둔 워크플로는 영원히 안 돈다.
  가지에 있던 그것을 이 PR 로 옮긴다.

**② 확정된 정체가 매 회차 덮어써진다.** `lib/domain/sheet-merge.ts` 의 판매시트 정본 경로
  (`_sales_sheet_authoritative` + `SALES_EXACT_PRODUCT_FIELDS`)는 제조사·모델·세부모델·세부트림·
  연료·배기량까지 **빈칸이어도** 기존 값을 덮는다. 2026-08-22 에 «의도해서» 그렇게 만든 것이라
  (「판매시트 정제만 잘해 두면 되니까」) **고치려면 사장님 승인이 먼저다** — 그때 문제였던
  「판매시트엔 있는데 ERP 만 빈 차 219대」가 되돌아올 수 있어 실데이터로 먼저 재야 한다.

**③ 미러가 「확정」을 매번 다시 판정한다.** `scripts/mirror-to-firestore.mts` 의 「유효보존」은
  **불변이 아니다** — 기존 Firestore 문서가 확정이었는지 «안 본다». RTDB 값이 마스터에 맞는 조합이면
  그대로 쓰는 것이라, 시트가 «다른 유효한 조합»으로 바꿔 두면 그 새 값이 확정으로 박힌다.
  차번 pin(재판정 안 함)은 `scripts/ingest-supplier-to-firestore.mts` 에만 있다.

★그래서 **「확정되면 절대 안 바뀐다」(`docs/방향-데이터플랫폼.md` §3-2 · 사장님 2026-09-12
  「차량 정보가 확정이 돼서 OK 사인을 받으면 그건 절대로 바뀌지 않아」)는 지금 main 의
  살아 있는 길에는 한 군데도 없다.** ①은 이 PR 로 옮기고, ②③은 승인·실측이 먼저다.

⚠ **자격증명이 없어 «실제로 며칠째 안 갱신됐나»는 못 쟀다.** 코드·워크플로 수준의 판정이다.
  재는 법 = `scripts/audit-atom-vs-erp.mts`(원자↔ERP) · 원자 문서의 `_direct_ingest_at`·`_mirror_at`.

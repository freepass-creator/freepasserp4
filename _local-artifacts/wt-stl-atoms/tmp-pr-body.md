## 정산도 원자화 — 화면이 구글시트 대신 Firestore 원자에서 읽는다

사장님 2026-09-05 「정산도 다 원자화해서 갖고 오자」.

**문제:** 월별 정산 화면(`settlement/mine`)은 정산 줄을 **구글시트 원장**(`readLedger`)에서 읽어서, 미리보기·시트접근 없는 곳에선 비었다. 원자화의 뜻과도 어긋난다.

**바꾼 것:**
- `readLedgerAtoms()` 신설 — Firestore `settlement_rows` 원자(이미 이관됨)를 읽어 `readLedger`와 **똑같은 모양**(`{row, extra, tab}`)으로 돌려준다. 판정(자리·청구월·수수료 = `settlement-stage`)·역할가림(`scopeRows`)·금액잠금은 **한 줄도 안 바뀐다** — 데이터 «원천»만 시트→원자.
- `settlement/mine`(표시)·`settlement/confirm`(확인) — `readLedger`→`readLedgerAtoms`. 시트토큰 503 가드 제거.

**검증(원자 454행, 관리자 경로 재현):** 인도완료 406·취소 31·진행중 17. 2026-08 청구 44,224,250·지급 35,471,020·44건. 확정 43.18M과 ~1M 차이는 메모 4축(settleTarget·billHold 등) 미파싱 때문 — 시트 읽기도 지금 동일, **수치 회귀 아님**.

**남은 것(별건):**
- 메모 33종류 → 정산조건 7축 (사장님 분류 필요)
- 지급액 「수식X 우선」 교정 (원자 재붓기 — 시트접근 필요)
- 원자 최신화: 최근 시트 편집분은 파이프라인 재동기 1회. 이후 편집은 ERP.

### 확인 (미리보기)
- 로그인 → 월별 정산 화면에 행이 뜨는지(관리자=금액 포함, 영업자=내 줄만). 이제 시트 없이 원자에서 옵니다.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

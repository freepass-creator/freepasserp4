/node_modules
/.next
/.next-verify
/.next-*
/out
/build
next-env.d.ts
*.tsbuildinfo
.env
.env.local
.env*.local
.DS_Store
npm-debug.log*

.vercel
.env*
/tmp/

# 운영 RTDB 덤프(PII) — 절대 커밋 금지
/backup.json
/backup-*.json
/companies.json
/partners.json
/hn.json
/*-rtdb-*.json

tmp/

# 로컬 AI/배포 검증 작업공간과 생성물
/.codex/
/.codex-deploy-*/
/output/

# 원본 계약자료·차량 마스터 수동 백업은 저장소/배포물에 포함하지 않는다
/계약서양식/
/public/data/*.bak*

# Firebase Rules 후보·에뮬레이터 생성물 — builder/probe 소스만 버전관리
/scripts/ruleprobe/release-candidate.rules.json
/scripts/ruleprobe/firebase-debug.log

# 에뮬레이터·로컬 실행 산출물 — 사람이 만든 파일이 아니다(2026-08-23)
*-debug.log
firebase.current-local.json
scripts/ruleprobe/firebase.current-local.json

# 파이썬 캐시
__pycache__/

# 오더 가드 시작 도장 (세션 로컬)
scripts/.order-base/

# 손오공 자격증명·토큰·원문 덤프 (절대 커밋 금지)
sonokong/lib/wonja/

# 임시 조사 스크립트 — 세션 중 만들고 버리는 것이다 (2026-09-16, 32개가 작업트리에 쌓여 있었다)
tmp-*.mjs
tmp-*.mts
tmp-pr-body*

# 다중 에이전트 lease/audit 런타임 산출물 — 사람이 만든 파일이 아니다
/.coordination/

# ★2026-09-16 — 공급사 재고 «백업 덤프»는 추적하지 않는다.
#   `sonokong/backups/sonokong-stock/…재고.json`(3.6MB)이 이 브랜치에 추적돼 있었다.
#   실측: 전화번호꼴 3,066건 · 주민번호꼴 2건. origin/main 에는 없어서 원격으로 나간 적은 없다.
#   ★백업은 «도구가 다시 만드는 것»이지 git 이 보관할 것이 아니다.
sonokong/backups/

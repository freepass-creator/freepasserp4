# erp3 → erp4 단독 운영 전환 (이관 계획)

> 확정일 2026-07-30. 이 문서가 전환의 SSOT. 세부 설계는 하단 §5에 계속 채운다.

## 1. 배경 — 왜 전환하는가

출시 전 통합 QA(87건 / 출시차단 12 · 긴급 12, `LAUNCH_QA_2026-07-30.md`)에서 **출시차단 다수가 "erp3와 DB·규칙을 공유"하는 구조 자체**에서 나왔다.

- `database.rules.json` == 콘솔 현행 규칙(`database.rules.PREV.json`)과 바이트 동일 → **구멍이 지금 라이브에 열려 있음**
- 규칙을 조이면 **운영 중인 erp3가 깨진다**(실측 확인):
  - erp3 계약완료는 `products/{uid}/vehicle_status`를 포함한 **원자적 multi-path** → 매물 쓰기 한 키만 막혀도 계약 전체 롤백
  - erp3는 알림톡 수신자(관리자·공급사 전화번호)를 **users 전체 목록**에서 얻음 → users read를 좁히면 알림이 **무음으로** 죽음
  - 로그인마다 `users/{uid}/agent_channel_code` 자동보정 write → 필드 화이트리스트 걸면 스코프 쿼리가 전부 빈 목록
  - erp3는 `status` 없는 계정을 active로 간주 → 화이트리스트 전환 시 레거시 계정 전원 잠김

**결론: 공존을 포기하고 절연한다.** 그러면 위 제약이 전부 사라지고, 출시차단 12건 중 8건이 구조적으로 증발한다.

## 2. 확정 결정

| # | 결정 | 근거 |
|---|---|---|
| D1 | **erp3와 완전 절연.** 일회성 데이터 이식 후 erp3 노드는 읽지도 쓰지도 않는다(연동 아님) | 규칙을 erp4 기준으로 자유롭게 설계 |
| D2 | **Firebase Auth는 그대로.** uid 불변 → 재가입 없음, 비밀번호 그대로 | 사용자 체감 변화 0 |
| D3 | **보여지는 코드는 재발급**(§3) | 현행 6종 혼재·역순까지 존재 |
| D4 | **v4/ 네임스페이스 단독 운영.** `BRIDGE_FROM_V3`를 끈다 | 쓰기는 이미 v4 전용 → 읽기 병합만 제거 |
| D5 | **매물은 시트에서 재생성.** 이관 대상 아님 | 시트가 원본, 트윈 5,640건 중 유효분만 |
| D6 | **감사로그(15,875건) 이관 제외** | 주민번호 등 PII 평문 누적 — 새 출발로 정리 |
| D7 | 진행 중 계약 30건은 **erp3에서 마무리**(신규만 erp4) → 이관 부담 없음 | 정산 14건은 전부 정산대기(지급 이력 0) |
| D8 | 회원은 **활성 사용자 선별 이관** | 158명 중 deleted 5·미사용 제외 |

## 3. 코드 체계 통일 (이관 시 재발급)

### 원칙 — 식별자에 "변하는 속성"을 넣지 않는다

역할(role)·파트너유형(partner_type)·소속 공급사는 **바뀐다**. 이걸 코드에 인코딩하면 값이 바뀔 때 코드도 바뀌어야 하고, 그 코드를 참조하던 계약·방·정산이 전부 깨진다.

근거(실측):
- `partner_type`이 `provider`/`영업채널`/`sales_channel`/`운영사`/`operator` **5종 혼재** → 유형 변경 이력 존재
- erp3에는 **관리자가 매물의 `provider_company_code`를 덮어쓰는 경로**가 있다(시트 동기화 `admin-ops.js:1032`) → 매물코드에 공급사를 박으면 코드와 실소유가 어긋난다

→ **코드는 기계용 불변 순번, 사람이 읽는 건 차량번호·이름**(화면·계약서·정산서 어디든 함께 표시되므로 가독성 손실 없음). 검색도 차량번호로 한다.

### 코드 표

| 대상 | 새 코드 | 예시 | 부가정보 | 현행 문제 |
|---|---|---|---|---|
| **회원** | `U-NNNN` | `U-0034` | `role` 필드 | `U0045`·`S0006`·`R0001` 접두어 3종 혼재 |
| **파트너** | `P-NNN` | `P-004` | `partner_type` 필드 | `OP001`·`SP002`·`RP004` — 유형이 코드에 박힘 |
| **매물** | `M-NNNNNN` | `M-000123` | 공급사·차량번호 필드 | **6종 혼재**: `RP004_12호1825`(4,172) · `PD-260504-003`(516) · `12호1825_RP004`**역순**(541) · `12호1825`(227) · `PT-0024_…`(74) · `현대_…`(58) |
| ↳ 신차(차번 없음) | 동일(`M-`) | | `is_new` 플래그 | 현행 해시 임시번호(`100신XXXX`)는 **색상이 해시 입력에 없어 동스펙 2색 신차가 1대로 병합**(QA SUS-1) → 순번이면 원천 해소 |
| 계약 | `C-YYMMDD-NN` | `C-260730-01` | | `TMP-2026-JD3OXS`·`TMP-260504-03` 2종, **전부 TMP(실코드 승격 안 됨)** |
| 대화방 | `CH-{매물}-{회원}` | `CH-M000123-U0034` | | 현행 Firebase push ID(`-OrI4-…`) — 사람이 못 읽음 |
| 정산 | `ST-YYMMDD-NN` | `ST-260730-01` | | **현행 유지**(이미 일관) |
| 정책 | `POL-NNNN` | `POL-0001` | | **현행 유지** |

**옛 코드 → 새 코드 매핑표를 `code_aliases`로 남긴다**(옛 코드로도 조회 가능, 화면엔 새 코드만).

## 4. 전환으로 해소되는 출시차단

| QA ID | 내용 | 전환 후 |
|---|---|---|
| B1 | 매물 쓰기 무제한 | 처음부터 소유자 규칙 — erp3 계약완료 호환 걱정 없음 |
| B2 | 승인게이트 우회(users 자가삭제) | 화이트리스트(`active`)로 시작 |
| B3 | 전 회원 PII 읽기 | 본인+admin만 |
| B4 | 원가·수수료 규칙 미분리 | `products_private` 분리를 규칙으로 강제 |
| B5·B6 | 서명 PII·Storage 덮어쓰기 | 토큰·규칙 재설계 |
| CONTRACT-1 | v3 레거시 계약 permission_denied | 브리지 소멸로 문제 자체가 없어짐 |
| (부수) | product_uid 리키잉·차명 런타임 조인 | 이관 시점에 확정 저장 → 조인 제거 |

**남는 것(앱 로직이라 어차피 수정 필요)**: B8 이중판매 가드, B9 계약 금액 스냅샷.

## 5. 실행 설계 (2026-07-30 확정)

### 5.0 설계 중 뒤집힌 전제 — 반드시 지킬 것

| 초안 | 실측 판정 |
|---|---|
| `users`를 v4로 이관 | **❌ 루트 유지.** 규칙에 `root.child('users')` **393회**(레거시 135 + v4 258). 규칙 배포와 데이터 이관은 **원자적이지 않아** 그 사이 전 사용자 락아웃. Auth uid 불변이므로 루트 `users`는 erp3 업무데이터가 아니라 **Auth 프로필 미러**로 재분류. `counters`도 동일(`auth.ts:200` 루트 트랜잭션) |
| 앱(클라 SDK)으로 이관 | **❌ 불가.** `v4/messages/$id`는 `!data.exists() && newData.sender_uid === auth.uid`, `v4/rooms/$id`는 소유 3필드 불변 `.validate` → 남의 데이터 이관 원천 차단. **owner 권한**(firebase-admin 서비스계정 또는 firebase-tools OAuth) 필수 |
| `BRIDGE_FROM_V3`만 비우면 됨 | **❌ 메시지 전량 소실.** `rtdb-adapter.ts:356` `if (entity === 'message' && bridge)` → bridge=false면 roomIds=[] → **v4 메시지도 0건**. 코드 선수정 필수 |
| 규칙 sim이 회귀를 막아준다 | **❌ 전부 무력.** `sim-authorization`은 정적 문자열 매칭(규칙에 `\|\| true` 붙여도 44/44 PASS), `sim-contract-rules`·`sim-chat-rules`는 손수 재구현 미러(드리프트 무탐지). → `scripts/ruleprobe/`(실제 에뮬레이터)로 대체 |

### 5.1 이관 전 선수정 (코드 결함 3건)

| # | 위치 | 증상 | 조치 |
|---|---|---|---|
| B1 | `rtdb-adapter.ts:356` | 브리지 끄면 메시지 1,904건 화면 소실 | `&& bridge` 제거 |
| B2 | `rtdb-adapter.ts:339` | 매물 `_policy` 조인이 루트 `policies`를 무조건 읽음 | `v4/policies`로 |
| B3 | `rtdb-adapter.ts:615` | 전 write 감사가 루트 `audit_logs`에 계속 쌓임 | `v4/audit_logs`로 + v4 규칙 신설 |

추가: `BRIDGE_FROM_V3`를 **env화**(`NEXT_PUBLIC_BRIDGE_V3`) → 브리지 제거 롤백이 env 한 줄(1분)이 된다.

### 5.2 데이터 선정리 (이관 전)

- **파트너 코드 충돌 2그룹** — 리키잉 시 49→46으로 **3건 소멸 + 이름 뒤바뀜**: `SP002`←PT-0002(에이카솔루션)·PT-0013(렌트야) / `RP011`←PT-0014(렌트존)
- **`user_code` 중복 U0001** — 현재 **남의 정산이 보이는** 구멍
- `SP999` 파트너 레코드 신설(users 96명이 참조하는데 레코드 없음)
- `_deleted` user 12건 + 빈껍데기 1건 정리 및 Auth disable
- 만료 `contract_sign` 1건 삭제(PII)

### 5.3 이관 매핑 요점

- **매물**: 삭제분 5,117건 **미이관**. 대신 방·계약에 차명·차번을 **확정 저장**(실측: 방 자체 필드로 142/142 해결, 런타임 조인 불필요). 차명 합성은 **`sub_model` 우선**(현행 `[maker,model,sub_model,trim]` 조합은 `기아 K5 더 뉴 K5 3세대`처럼 중복 발생)
- **방**: `product_uid`(옛 키)를 **지우지 말 것** — `room-display.ts`의 폴백 인덱스가 사용. 대신 `product_code`를 확정 채움(실측 141/142 성공)
- **메시지**: 버킷 구조 → flat + **`room_id` 실체화 필수**(원본에 room_id 필드 0건). 삭제된 방 소속 754건 제외
- **미이관**: `audit_logs`(15,875) · `vehicle_master`(앱은 `/data/vehicle-master.json` 사용) · `input_codes` · `code_sequences` · `home_notices` · `fcm_tokens` · `product_code_aliases`(참조복구에 1회만 사용 후 폐기)
- **원가 분리**: `v4/products_private` 신설 — `vehicle_price` 4,480 · `vin` 7 · `price.*.fee` 3,171 이동. ⚠ `fee_1..fee_60` 33건은 `PRIVATE_FIELDS` 목록에 없어 **별도 처리 필요**
- **계약**: `fee_rate_snapshot`·`payout_rate_snapshot` 백필 → `settlement-engine.ts:21`이 기본값 0.04로 조용히 계산하는 경로 제거

### 5.4 실행 순서 (11단계)

| 단계 | 무엇을 | 되돌림 |
|---|---|---|
| 0 | 백업 재추출(7/27→당일) + owner 도구 설치 + 게시 중 규칙본 확보 | 읽기전용 |
| **1** | **라이브 구멍 2건 즉시 봉합·게시**: `v4/products` 노드레벨 `.write` 제거 · `v4/users/$uid` 자가 admin 승격 차단 + `newData.exists()` | 콘솔 버전 복원(30초) |
| 2 | 코드 결함 3건 수정 + 브리지 env화 (배포 안 함) | 미배포 |
| 3 | 데이터 선정리(§5.2) | 0단계 스냅샷 |
| 4 | 드라이런 — 순수함수로 plan JSON + 리포트(네트워크 없이 몇 번이든 diff) | 파일만 |
| 5 | 이관 적용 (owner, `update()` 멀티패스, `set()` 금지, 500경로 청크, 경로별 before/after JSONL) | JSONL 역재생. **v4 통째 삭제 금지**(이관 무관 운영데이터 존재) |
| 6 | 검증 — 건수·참조무결성·필드손실·**멱등성**·PII·소유필드. 1축 FAIL이면 중단 | 5단계 로그 |
| 7 | 앱 배포(브리지 **유지**) + 1일 관측 | Vercel Instant Rollback |
| 8 | 브리지 제거 (env 한 줄, 저부하 시간대) | env 원복(1분) |
| 9 | 관측 1주 — 네트워크 탭에 루트 노드 요청 0회 확인 | 8단계 롤백 |
| 10 | 규칙 전면 강화 (단 `root.child('users')` 258개소는 유지) + 게시 전 `scripts/ruleprobe` 통과 필수 | 콘솔 복원(30초) |
| 11 | 레거시 실삭제 (`audit_logs` 포함) — **유일한 비가역, 별도 승인** | 0단계 스냅샷이 마지막 보루 |

**안전장치 3개**
1. **이관(5)·브리지(8)·규칙(10)을 서로 다른 배포에 둔다.** 이관은 순수 추가라 무해하고, 위험한 두 스위치는 각각 규칙 재게시·env 한 줄로 즉시 복구.
2. **10단계까지 루트 데이터를 지우지 않는다** → 언제든 브리지 복구 가능.
3. **1단계만은 이관보다 먼저** — 오늘 라이브인 구멍이고 owner 이관을 막지 않는다.

### 5.5 규칙 설계 시 반드시 지킬 것 (에뮬레이터 51케이스 실증)

- 매물 소유판정은 **`data` 기준**. `newData` 기준이면 남의 매물에 자기 코드를 같이 써넣어 **탈취 가능**(실제 200 확인)
- `users` `.read`는 **제거가 아니라 admin 강등**. RTDB는 자식 `.read`가 부모 읽기를 만들지 못해 관리자 회원목록이 401(catch가 삼켜 **빈 목록**으로 보임)
- 어댑터의 **소유코드 승계 스탬프**(`rtdb-adapter.ts:525-529`) 제거 금지 — 소유필드 없는 949건이 이것으로 통과
- 통째 PUT과 리프 PATCH는 **한 `.write`로 둘 다 커버**됨(캐스케이드)

## 6. 원칙

- **안정성 우선.** 각 단계는 되돌릴 수 있어야 하고, 이관 스크립트는 멱등(재실행 안전)이어야 한다.
- 이관 전 **드라이런 + 건수·참조 무결성 리포트**를 통과해야 실행한다.
- erp3는 전환 후에도 **읽기 참고용으로 살려두되**(진행 계약 30건 마무리), erp4는 절대 참조하지 않는다.

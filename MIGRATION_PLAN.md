# erp3 → erp4 단독 운영 전환 (이관 계획)

> 확정일 2026-07-30. 이 문서가 전환의 SSOT. 세부 설계는 하단 §5에 계속 채운다.

## 1. 배경 — 왜 전환하는가

출시 전 통합 QA(87건 / 출시차단 12 · 긴급 12, `LAUNCH_QA_2026-07-30.md`)에서 **출시차단 다수가 "erp3와 DB·규칙을 공유"하는 구조 자체**에서 나왔다.

- `database.rules.json` == 콘솔 현행 규칙(`database.rules.PREV.json`)과 바이트 동일 → **구멍이 지금 라이브에 열려 있음**
- 규칙을 조이면 **운영 중인 erp3가 깨진다**(실측 확인):
  - erp3 계약완료는 `products/{uid}/vehicle_status`를 포함한 **원자적 multi-path** → 매물 쓰기 한 키만 막혀도 계약 전체 롤백
  - erp3는 알림톡 수신자(관리자·공급사 전화번호)를 **users 전체 목록**에서 얻음 → users read를 좁히면 알림이 **무음으로** 죽음
  - 로그인마다 `users/{uid}/agent_channel_code` 자동보정 write → 필드 화이트리스트 걸면 스코프 쿼리가 전부 빈 목록
  - erp3는 `status` 없는 계정을 active로 간주 → 화이트리스트 전환 시 레거시 계정 전원 잠김

**결론: 공존을 포기하고 절연한다.** 그러면 위 제약이 전부 사라지고, 출시차단 12건 중 8건이 구조적으로 증발한다.

## 2. 확정 결정

| # | 결정 | 근거 |
|---|---|---|
| D1 | **erp3와 완전 절연.** 일회성 데이터 이식 후 erp3 노드는 읽지도 쓰지도 않는다(연동 아님) | 규칙을 erp4 기준으로 자유롭게 설계 |
| D2 | **Firebase Auth는 그대로.** uid 불변 → 재가입 없음, 비밀번호 그대로 | 사용자 체감 변화 0 |
| D3 | **보여지는 코드는 재발급**(§3) | 현행 6종 혼재·역순까지 존재 |
| D4 | **v4/ 네임스페이스 단독 운영.** `BRIDGE_FROM_V3`를 끈다 | 쓰기는 이미 v4 전용 → 읽기 병합만 제거 |
| D5 | **매물은 시트에서 재생성.** 이관 대상 아님 | 시트가 원본, 트윈 5,640건 중 유효분만 |
| D6 | **감사로그(15,875건) 이관 제외** | 주민번호 등 PII 평문 누적 — 새 출발로 정리 |
| D7 | 진행 중 계약 30건은 **erp3에서 마무리**(신규만 erp4) → 이관 부담 없음 | 정산 14건은 전부 정산대기(지급 이력 0) |
| D8 | 회원은 **활성 사용자 선별 이관** | 158명 중 deleted 5·미사용 제외 |

## 3. 코드 체계 통일 (이관 시 재발급)

### 원칙 — 식별자에 "변하는 속성"을 넣지 않는다

역할(role)·파트너유형(partner_type)·소속 공급사는 **바뀐다**. 이걸 코드에 인코딩하면 값이 바뀔 때 코드도 바뀌어야 하고, 그 코드를 참조하던 계약·방·정산이 전부 깨진다.

근거(실측):
- `partner_type`이 `provider`/`영업채널`/`sales_channel`/`운영사`/`operator` **5종 혼재** → 유형 변경 이력 존재
- erp3에는 **관리자가 매물의 `provider_company_code`를 덮어쓰는 경로**가 있다(시트 동기화 `admin-ops.js:1032`) → 매물코드에 공급사를 박으면 코드와 실소유가 어긋난다

→ **코드는 기계용 불변 순번, 사람이 읽는 건 차량번호·이름**(화면·계약서·정산서 어디든 함께 표시되므로 가독성 손실 없음). 검색도 차량번호로 한다.

### 코드 표

| 대상 | 새 코드 | 예시 | 부가정보 | 현행 문제 |
|---|---|---|---|---|
| **회원** | `U-NNNN` | `U-0034` | `role` 필드 | `U0045`·`S0006`·`R0001` 접두어 3종 혼재 |
| **파트너** | `P-NNN` | `P-004` | `partner_type` 필드 | `OP001`·`SP002`·`RP004` — 유형이 코드에 박힘 |
| **매물** | `M-NNNNNN` | `M-000123` | 공급사·차량번호 필드 | **6종 혼재**: `RP004_12호1825`(4,172) · `PD-260504-003`(516) · `12호1825_RP004`**역순**(541) · `12호1825`(227) · `PT-0024_…`(74) · `현대_…`(58) |
| ↳ 신차(차번 없음) | 동일(`M-`) | | `is_new` 플래그 | 현행 해시 임시번호(`100신XXXX`)는 **색상이 해시 입력에 없어 동스펙 2색 신차가 1대로 병합**(QA SUS-1) → 순번이면 원천 해소 |
| 계약 | `C-YYMMDD-NN` | `C-260730-01` | | `TMP-2026-JD3OXS`·`TMP-260504-03` 2종, **전부 TMP(실코드 승격 안 됨)** |
| 대화방 | `CH-{매물}-{회원}` | `CH-M000123-U0034` | | 현행 Firebase push ID(`-OrI4-…`) — 사람이 못 읽음 |
| 정산 | `ST-YYMMDD-NN` | `ST-260730-01` | | **현행 유지**(이미 일관) |
| 정책 | `POL-NNNN` | `POL-0001` | | **현행 유지** |

**옛 코드 → 새 코드 매핑표를 `code_aliases`로 남긴다**(옛 코드로도 조회 가능, 화면엔 새 코드만).

## 4. 전환으로 해소되는 출시차단

| QA ID | 내용 | 전환 후 |
|---|---|---|
| B1 | 매물 쓰기 무제한 | 처음부터 소유자 규칙 — erp3 계약완료 호환 걱정 없음 |
| B2 | 승인게이트 우회(users 자가삭제) | 화이트리스트(`active`)로 시작 |
| B3 | 전 회원 PII 읽기 | 본인+admin만 |
| B4 | 원가·수수료 규칙 미분리 | `products_private` 분리를 규칙으로 강제 |
| B5·B6 | 서명 PII·Storage 덮어쓰기 | 토큰·규칙 재설계 |
| CONTRACT-1 | v3 레거시 계약 permission_denied | 브리지 소멸로 문제 자체가 없어짐 |
| (부수) | product_uid 리키잉·차명 런타임 조인 | 이관 시점에 확정 저장 → 조인 제거 |

**남는 것(앱 로직이라 어차피 수정 필요)**: B8 이중판매 가드, B9 계약 금액 스냅샷.

## 5. 실행 설계

> 설계 워크플로 산출물로 채운다: ①이관 매핑 ②브리지 제거 영향 ③규칙 재설계 ④검증 하네스·컷오버 절차

### 5.1 이관 매핑
(작성 예정)

### 5.2 브리지 제거
(작성 예정)

### 5.3 규칙
(작성 예정)

### 5.4 검증·컷오버
(작성 예정)

## 6. 원칙

- **안정성 우선.** 각 단계는 되돌릴 수 있어야 하고, 이관 스크립트는 멱등(재실행 안전)이어야 한다.
- 이관 전 **드라이런 + 건수·참조 무결성 리포트**를 통과해야 실행한다.
- erp3는 전환 후에도 **읽기 참고용으로 살려두되**(진행 계약 30건 마무리), erp4는 절대 참조하지 않는다.

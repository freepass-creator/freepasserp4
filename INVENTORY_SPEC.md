# 재고 연동 기준 (2026-08-06)

「판매가능이 몇 대인가」가 매번 다르게 나오는 이유는 **기준이 글로 정해져 있지 않아서**다.
이 문서가 그 기준의 SSOT다. 숫자가 안 맞으면 데이터를 고치기 전에 이 문서와 먼저 대조한다.

측정은 전부 2026-08-06 실데이터(`freepasserp3` RTDB) 기준이며, 근거 스크립트를 각 절에 적었다.

---

## 0. 지금 무엇이 어긋나 있나

```
판매가능 (isOfferableProduct)              401대
├ 차종마스터 검토대기 (_needs_master_review) 396대   ← 사실상 전량
├ 제조사·차명이 둘 다 빈 것                  264대   ← 목록에 «빈 줄»로 뜬다
└ 차번이 빈 것                             314대

태윤 확인 (2026-08-05 기준)                363대
```

빈 줄로 뜨는 264대의 유입 경로는 **전부 시트**다. 원본은 `_raw_vehicle` 에 살아 있는데
차종마스터에 못 붙어 정규 필드로 승격되지 못했다.

> 근거: `scripts/audit-master-review-backlog.mts` · `scripts/audit-offerable-count.mts`

---

## 1. 정본 — 시트가 재고를 정한다

- 공급사가 등록한 **시트(또는 홈페이지) 원본이 재고의 정본**이다. `v4/products` 는 그 투영이다.
- 시트에서 사라진 차는 **삭제하지 않고 출고불가로 전환**한다(부재차단). 계약 스냅샷은 바꾸지 않는다.
- 예전 v3 스냅샷은 정본이 아니다. 시트와 어긋나면 시트를 따른다.

## 2. 연동 자격 — 어떤 공급사가 갱신되는가

| 방식 | 조건 | 곳 |
|---|---|---|
| 시트 | `sheet_url` 있음 | 15곳 |
| 홈페이지 | `inventory_source=ironrentcar_web` (RP006) | 1곳 |
| **없음** | 둘 다 없음 → **갱신되지 않는다** | PT-0001 5대 · 엘씨 2대 · 스위치플랜 2대 |

**연동 방식은 「행의 한 칸」이지 목록을 가르는 기준이 아니다.** 홈페이지를 표 맨 위에
따로 올리지 않는다(2026-08-06 반영).

> ⚠ **결정 필요 ①** — 연동 없는 9대를 어떻게 할 것인가.
> PT-0001 5대는 **파트너 레코드조차 없다**(유령). 엘씨·스위치플랜은 파트너는 있으나 시트가 없다.

## 3. 매물 식별 — 같은 차인지 무엇으로 보나

- **실번호판이 있으면 번호판**이 식별자다.
- 번호판이 없는 매물(재렌트·재구독 다수)은 `EXT_*` 해시 키로 들어온다.
  키 규약이 갈려 있다 — v3 이관분 `02하9002_RP006`(차번_공급사) / 시트 유입 `RP006_02하9002`(공급사_차번).
  같은 실물이 둘로 잡히면 **이중판매**가 된다.

> ⚠ **결정 필요 ②** — 번호판 없는 매물을 판매가능으로 둘 것인가.
> 현재 판매가능 401대 중 **314대가 번호판이 없다**. 영업자가 차를 특정할 수 없다.

## 4. 차종 확정 — 무엇을 «붙었다»고 보나

차종마스터(1,800세대) 매칭 신뢰도로 가른다.

| confidence | 처리 |
|---|---|
| `high` · `medium` | **확정** — 제조사·모델·세부모델·파워트레인을 마스터 값으로 승격 |
| `low` · 미매칭 | **검수 대기**(`_needs_master_review`) — 원자는 공란 유지 |

**`low` 는 «완료»가 아니라 «재시도 대상»이다.** 예전 `master-ingress.ts` 의 가드가 `low` 까지
「이미 스냅됨」으로 보고 재시도를 막아, 옛 매처로 굳은 레코드가 영원히 복구되지 않았다.
2026-08-06 수정했다.

실측: 검토대기 396대를 지금 매처에 다시 물리면 **363대(92%)가 high·medium 으로 확정**되고,
**제조사·차명 빈칸 264대가 채워진다.** 33대만 low 로 남는다.

> 근거: `scripts/sim-remsnap-backlog.mts` · `scripts/apply-remsnap-backlog.mts`(dry-run)

## 5. 판매가능 — 목록에 뜨는 조건

**현재 코드** (`lib/domain/product.ts:315`):
```js
isOfferableProduct = 삭제 아님 && 상태 ≠ 출고불가 && 가격 ≥ 1개
```

**차종이 붙었는지를 안 본다.** 그래서 제조사·차명이 빈 매물도 판매가능으로 집계되고,
영업자 화면엔 빈 줄로 뜬다. 대수가 실제와 어긋나는 직접 원인이다.

**✅ 결정 (2026-08-06 오후 사용자 · 최신)** — **매물 등록 최소 요건 = 차량번호 + 대여료.**
번호미정 신차는 임시번호(`100신…`)가 차번 역할을 한다. 차종 검수대기(`_needs_master_review`)는
목록에서 **빼지 않는다** — 차종을 아직 못 붙였다는 건 «우리 데이터가 덜 정리됐다»는 뜻이지
그 차가 없다는 뜻이 아니다. 빼 놓으면 재고는 있는데 팔 수 없는 상태가 된다.
미확정은 감추지 말고 화면 «표시»로 알린다.

```
isListableProduct = isOfferableProduct && car_number 있음   (hasMinimumListingFields)
```

> 회귀검사: `scripts/sim-product-offerability.mts` 23/23
> 실측(2026-08-06): 견적가능 450대 = 목록노출 450대 (차번 빈 매물 0)

<details><summary>뒤집히기 전 오전 결정 — 기록 보존</summary>

**결정 (2026-08-06 오전)** — 차종마스터에 못 붙은 매물은 판매가능에서 뺀다(안 B).

| 안 | 판정식 | 실측(재스냅 반영 가정) |
|---|---|---|
| A. 현행 | 삭제×·출고불가×·가격○ | 401대 (빈 줄 264 포함) |
| B. 당시 채택 | A + `_needs_master_review !== true` | 368대 |
| C. 참고 | B + 연동 없는 공급사 제외 | 353대 |

> 근거: `scripts/sim-offerable-rules.mts`
> 뒤집은 이유: 차종 미확정 33대가 목록에서 통째로 사라져, 실재하고 가격도 있는 매물을
> 영업자가 볼 수 없었다. 제조사가 비어도 모델명으로 마스터 전체를 훑어 붙이므로
> (`vehicle-master-score.ts:44`) «제조사 없음»이 곧 «못 파는 차»도 아니었다.

</details>

## 5-1. ✅ 오플 숨김 행 = 출고불가 (2026-08-06 사용자 결정)

오토플러스 시트는 숨김 행을 포함한다. **숨김 행은 출고불가로 본다.**
CLI 미리보기는 이 처리를 막아 두었으므로(`오토플러스 숨김 행 제외는 관리자 상품 검증/API 경로로 검증하세요`)
오플은 **관리자 상품 검증 화면에서만** 검증·반영한다. 현재 ERP 104대 vs 확인분 91대의 차이가 여기서 나온다.

## 5-2. ⏸ 미확정 33대 — 마스터 문제가 아니라 «제조사 열» 문제 (나중에 처리)

재스냅해도 안 붙는 33대를 실측한 결과, **마스터에 없는 차가 아니라 제조사 칸이 틀린 차**였다.

```
maker«아우디»    model«K5»            ← 기아 K5
maker«폭스바겐»  model«아반떼»         ← 현대 아반떼
maker«아우디»    model«모닝»           ← 기아 모닝
maker«KGM»      model«벤츠 A클래스»
maker«람보르기니» model«BMW 120i»
maker«혼다»      model«E200» sub«E-클래스 W214»
```

빌린카 16대 중 11대가 `maker=아우디` 로 몰려 있다 — 우연이 아니라 **시트 열 매핑이 어긋난 것**이다.
공급사별 분포: RP021 빌린카 16 · RP004 아이카 7 · RP023 오플 5 · PT-0023 2 · RP020 1 · RP030 1 · RP015 1.

**할 일은 「마스터 보강」이 아니라 「제조사 열 매핑 수정」이다.** 고치면 대부분 살아난다.

> 근거: `scripts/audit-unconfirmed-vehicles.mts`

## 6. 되돌리기

RTDB 는 롤백이 없다. 재고를 건드리는 작업 전에 반드시 덤프한다.

```bash
BACKUP_STAMP=YYYYMMDD-이유 npx tsx scripts/backup-products.mts
```

2026-08-06 재스냅 전 백업: `tmp/migration-backups/20260806-pre-resnap/v4_products.json` (6,078건)

---

## 실행 순서

1. **재스냅 반영** — `scripts/apply-remsnap-backlog.mts --apply` (high·medium 363대만, low 33대 유지)
2. 빈칸 0 확인 — `scripts/audit-master-review-backlog.mts`
3. 결정 ③ 반영 — B 를 택하면 `isOfferableProduct` 에 차종 확정 조건 추가
4. 결정 ①② 처리 — 유령·번호판 없는 매물
5. 공급사별 대수를 태윤 확인분과 재대조

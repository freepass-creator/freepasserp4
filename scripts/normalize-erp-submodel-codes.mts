/**
 * **폐기.** 세부모델에서 개발코드를 떼지 않는다 (08-23 오후 · K5 DL3).
 *
 * ERP 에 이미 깎인 이름은 시트 fill → 발행이 판매가능 차를 되돌린다.
 *
 *   npx tsx scripts/normalize-erp-submodel-codes.mts   # 거부만 한다
 */
console.error('거부: 세부모델 개발코드를 ERP 에서 떼지 않는다 (K5 DL3 · 싼타페 MX5).');
process.exit(1);

# 이어서 하기 — 2026-08-05 중단 시점

PC 재설치로 작업을 끊는다. **재설치 후 이 문서부터 읽으면 이어진다.**

---

## 0. 재설치 전에 반드시 백업할 것 — git 에 없는 것들

D 드라이브만 살리면 리포는 안전하다(`D:\dev\*`). 문제는 **C 드라이브에만 있는 것**이다.

| 대상 | 경로 | 왜 |
|---|---|---|
| **Claude 기억 44개** | `C:\Users\user\.claude\projects\C--Users-user\memory\` | 프로젝트 맥락 전부. git 에 없다. **이게 제일 위험** |
| Claude 설정 | `C:\Users\user\.claude\` | 권한·훅·설정 |
| 환경변수 | `D:\dev\freepasserp4\.env.local` | `.gitignore` 대상. D 라 살아남지만 확인할 것 |
| 서비스계정 키 | `D:\dev\freepasserp4\tmp\firebase-auth\sa.json` | `tmp/` 는 gitignore. 로컬 스크립트 전부가 이걸 쓴다 |

```powershell
# 백업 예시 — D 로 복사해두면 재설치 후 되돌리기만 하면 된다
Copy-Item -Recurse "C:\Users\user\.claude" "D:\dev\_backup\claude-config" -Force
```

## 1. ⚠ 커밋 안 된 작업이 53건 있다 — 내 것이 아니다

**공급사 상품연동(Codex 진행분)이 통째로 미커밋 상태다.** 재설치로 D 를 건드리면 전부 날아간다.

```
app/api/inventory/ironrentcar/*   lib/domain/sheet-*   lib/domain/ironrentcar-*
components/SheetSync.tsx          lib/server/google-sheet-visible.ts
scripts/sim-ironrentcar-*         data/sheet-ingress/*   외 다수
```

**재설치 전에 커밋하거나, 최소한 D 를 손대지 않는다는 것을 확인할 것.**
내(Claude) 작업은 전부 커밋·푸시 끝났다.

## 2. 오늘 끝낸 것 (커밋됨)

| 커밋 | 내용 |
|---|---|
| `c51eadf` | 채팅·계약·정산 **v3 의존 878건 이관** (계약27·문의방30·고객1·메시지820) |
| `bcdd31f` | **v3 브리지 해제** — 필드 단위 감사 후. 계약6·문의방46의 껍데기 레코드 보강 |
| `602fe37` | **구글시트 쓰기** 인프라 + 테스트 데이터 정리 |
| `5fcaa96` | **시작안내**(역할별 4단계) + 엑셀 다운로드 → 구글시트 열기 |

핵심 판단 셋:

1. **이관은 «키»가 아니라 «필드»까지 봐야 한다.** v4 에 키는 있는데 `agent_channel_code`
   한 필드뿐인 껍데기가 계약 6건·문의방 46건 있었다. `merged()` 의 필드 단위 병합이
   v3 로 빈칸을 메워 정상으로 보였을 뿐, 브리지를 끄면 계약번호도 고객명도 없이 뜬다.
   → `scripts/audit-v4-standalone-core.mts` 가 이걸 잡는다. **끄기 전에 반드시 돌린다.**
2. **보강 규칙은 «v4 에 키가 있으면 안 건드린다»** — `''` 는 의도적 클리어라 덮으면 사고.
3. **QA 계정 4개는 남긴다.** `OPEN_RUNSHEET.md` 1·8단계 5역할 스모크가 쓴다. 오픈 후 정리.

## 3. 지금 막혀 있는 것 — 사장님 클릭 한 번

구글시트 쓰기가 **권한 하나로** 막혀 있다. 코드는 다 됐다.

```
읽기  200 ✅      Sheets API 활성  ✅
쓰기  403 ❌      시트가 「링크 뷰어」로만 공유돼 있다
```

> 시트 우상단 **공유** → 아래를 **편집자**로 추가
> ```
> firebase-adminsdk-fbsvc@freepasserp3.iam.gserviceaccount.com
> ```

공유 뒤 바로 이거 한 줄이면 501대가 서식까지 얹혀 들어간다.

```bash
npx tsx scripts/export-products-to-sheet.mts --apply
```

대상 시트: <https://docs.google.com/spreadsheets/d/1G0tPyFI4JIfc-Ijd5qJNbgPGzcs2Ek5hQJwDHuml8VU/edit>
(주소 SSOT = `lib/product-sheet.ts` — 화면 버튼과 스크립트가 같은 값을 본다)

## 4. 검증 못 한 것 — 재설치 후 첫 할 일

`5fcaa96`(시작안내 + 시트 버튼)은 **타입체크·토큰·UI계약만 통과했고 화면으로 못 봤다.**
개발 서버가 `os error 1450`(스레드 고갈)로 500 을 뱉어 확인 전에 중단했다.

```bash
npm run dev            # 4004
# 확인할 것
#  1) 로그인 직후 「시작안내」 모달이 뜨는가 (역할별 4단계 · 줄바꿈 안 깨짐)
#  2) 엑셀 보기에서 우측 상단 아이콘이 구글시트로 열리는가
#  3) /chat /contract /settlement /m 이 v3 브리지 없이도 같게 보이는가
```

**3번이 특히 중요하다.** 데이터 감사는 통과했지만(키 누락 0·필드 누락 0)
실제 화면을 로그인해서 본 적은 없다. 프로덕션에는 이미 배포돼 있다(`bcdd31f`).

## 5. 재설치 후 환경 복구 순서

```bash
# 1. 도구 — D:\dev\pc-setup\ 의 재설치 목록 참고
#    Node · Git · VS Code · Firebase CLI

# 2. 리포
cd D:\dev\freepasserp4
npm install

# 3. 비밀값 되돌리기 (git 에 없다)
#    .env.local              ← 백업본
#    tmp/firebase-auth/sa.json ← 백업본

# 4. 동작 확인
npm run typecheck && npm run check:tokens && npm run check:ui
npx tsx scripts/audit-v4-standalone-core.mts    # 채팅·계약·정산 온전한가
npx tsx scripts/export-products-to-sheet.mts    # 상품 501대 집계되나 (쓰기 없음)
```

## 6. 오픈까지 남은 것

`OPEN_RUNSHEET.md` 10단계 중 **체크된 것 0개**. 성격이 둘로 나뉜다.

```
코드 (Codex)   9단계 아이언 재고 · product 브리지 제거
운영 (사람)    2 Production 환경변수 → 4 실계정 baseline → 5 백업
               → 7 Rules 게시 → 8 5역할 smoke → 10 도메인 전환
```

**순서가 강제된다 — Rules 게시가 Production 플래그보다 먼저 가면 딜이 전면 정지한다.**
RTDB 는 `freepasserp3` 하나뿐이고 Preview·Production 이 같은 인스턴스를 본다.
콘솔 게시는 Production 에 즉시 적용되므로, 그 시점 플래그가 OFF 면
영업자 「계약금 입금」·공급사 「입금 확인」이 둘 다 401 이 된다.
자세한 건 `LAUNCH_GONOGO.md` §1-1.

## 7. 미해결로 남기는 것

| | |
|---|---|
| 빌린카/엘씨 탭 분리 | 어느 탭이 구독 정본인지 미확인 |
| RP031 70대 | 미등록 파트너인데 게시 중 · 정체 불명 |
| 오플 14대 · 아이언 1대 | 시트 유령 행 / 상태 불일치 |
| `/faq` GUIDE 문구 | 카톡 운영 시절 기준(「접수방에 등록」) — 오픈 후 ERP 기준으로 |
| QA 계정·공급사 | 오픈 스모크 끝나면 정리 |
| PC 커밋 고갈 | 페이지파일 64GB 고정으로 완화했으나 **근본 원인 미규명** — 프로세스 밖 커밋 32~67GB 가 출렁였다. 재설치 후 재발하면 RAMMap 으로 분해할 것 |

# 출시 전 통합 QA 결과 보고서

- 점검일: 2026-07-30
- 대상: freepasserp4 (웹/모바일웹, freepasserp.com) + 연결된 erp3 라이브 RTDB
- 방식: 코드 정적 감사 7축 병렬(인증·비즈니스로직·보안PII·데이터정합·상태문구·운영·호환) → 고심각도 적대적 검증 → 완결성 비판
- 스코프 제외: 앱 내 결제/환불(미구현), 앱스토어 심사(네이티브 앱 없음)
- 총 87건 / 출시차단 12 · 긴급 12 · 중요 25 · 일반 25 · 개선 13

> ⚠ 아래 보안 항목 다수는 출시 여부와 무관하게 **현재 운영 DB(erp3 라이브)에 이미 열려 있는 상태**다. 출시 연기와 별개로 즉시 차단이 필요하다.

---

## 출시차단 (12건)

### [AUTH-1/AUTH] 승인된 아무 계정(영업자 포함)이나 매물 노드 전체를 삭제·변조할 수 있음 — 역할 검사 없는 노드 레벨 .write

- 플랫폼·화면: 공통(RTDB 규칙) / 전역 / 재고·매물 데이터(products, v4/products)
- 검증: 확정
- 근거: database.rules.json:37 `"products": { ".write": "auth != null && auth.token.firebase.sign_in_provider !== 'anonymous' && root.child('users').child(auth.uid).child('status').val() !== 'pending'" }` / 동일 패턴 :174 `v4/products`. 역할(provider/admin)·소유(provider_company_code) 조건이 전혀 없고 규칙이 컬렉션 노드에 걸려 있어 하위 전체에 상속됨. 대조군으로 contracts(:266)·rooms(:44)·settlements(:231)는 소유 필드 일치를 요구함.
- 재현: 1) 아무 영업자 계정(status=active)으로 freepasserp.com 로그인 2) 브라우저 콘솔에서 동일 Firebase 앱으로 `set(ref(db,'products'), null)` 또는 `update(ref(db,'products/{임의코드}'), {rent:0})` 실행 3) 규칙 통과 → erp3 라이브 매물 전량 삭제/가격 변조
- 기대: 매물 쓰기는 admin 또는 해당 provider_company_code 소속 공급사만, 그리고 레코드 단위로만 허용
- 실제: pending이 아닌 모든 인증 계정이 products/v4/products 서브트리 전체에 write(=삭제 포함) 가능. 복구 불가 데이터 유실 경로
- 조치: products·v4/products의 .write를 노드에서 제거하고 `$product` 자식 레벨로 내려, admin 또는 `newData/data.child('provider_company_code').val() === root.child('users').child(auth.uid).child('company_code').val()` 인 provider만 허용. 삭제도 동일 조건으로 제한(v4/products_private:183 규칙이 이미 올바른 형태 — 그 패턴을 공개 노드에도 적용).
- 검증 상세: 반박 전 항목 실패. (1) database.rules.json:37(products)·:174(v4/products) .write는 status!=='pending'만 요구, 역할·소유 조건 없음. 하위 .validate/자식 .write 부재로 서브트리 전체 상속, set(null) 삭제는 .validate 미적용이라 통과. (2) 라이브 규칙 백업본 database.rules.PREV.json(LAUNCH_GONOGO.md:38 규정상 콘솔 현행 복사본)과 강화안 database.rules.HARDENED.json 모두 동일 문자열 → 배포본에도 같은 구멍이고 대기 중 수정도 없음. (3) 완화 레이어 없음: App Check 미사용(레포 참조 0건), 매물 쓰기는 서버 admin SDK가 아니라 브라우저 클라 SDK 직결(lib/firebase/rtdb-adapter.ts:492,548,560) → RTDB 규칙이 유일 경계. (4) 도달 가능: 신규가입은 lib/firebase/auth.ts:214 status='pending'으로 막히나 승인 계정(active)은 통과, 운영 영업자 계정 다수. (5) 영향 실측: 루트 products는 lib/firebase/rtdb-adapter.ts:326·332 merged()가 v3 라이브∪v4 오버레이로 전 목록을 구성하는 기반 소스 → 삭제 시 전 사용자 매물 소멸, rent 변조는 계약 스냅샷·견적으로 전파. (6) 복구수단 없음: RTDB 스냅샷 자동백업·cron 부재, app/api/drive-backup/route.ts는 사용자 트리거 레코드 단위 문서 업로드라 노드 복구 불가. (7) scripts/sim-authorization.mts·sim-vehicle-lock.mts에 products 검증 0건 → 회귀 게이트 

### [AUTH-2/AUTH] 승인대기 계정이 자기 users/{uid} 노드를 삭제하면 status가 사라져 모든 pending 차단 규칙과 앱 게이트를 우회

- 플랫폼·화면: 공통(RTDB 규칙 + 클라 게이트) / 가입 승인 게이트(PendingApproval) 전역
- 검증: 확정
- 근거: database.rules.json:6 `"$uid": { ".write": "auth != null && ($uid === auth.uid || …admin)" }` — 본인 노드 삭제 허용. :14 status .validate는 RTDB 특성상 삭제(newData=null)에서 평가되지 않음. 모든 보호 규칙이 화이트리스트가 아닌 블랙리스트 `status.val() !== 'pending'` (:37, :174, :188, :301, :323, :327 등) → status 부재 시 통과. 클라도 동일: lib/auth-session.ts:24 `return String(s?.status || '') === 'pending'`, lib/firebase/auth.ts:75-96 프로필 {} 이면 role='agent'·status 미설정 세션 발급.
- 재현: 1) 아무 이메일로 가입(status=pending, PendingApproval 화면) 2) 콘솔에서 `remove(ref(db,'users/'+auth.currentUser.uid))` 3) 새로고침 → PendingApproval 사라지고 일반 영업자로 앱 진입 4) products·v4/products·customers·counters·input_codes 등 status만 검사하는 노드 전부 read/write 가능(AUTH-1과 결합 시 매물 전량 삭제까지)
- 기대: 승인 전 계정은 어떤 조작으로도 pending 상태를 벗어날 수 없어야 함
- 실제: 본인 프로필 삭제만으로 승인 게이트가 무력화되고, 프로필 없는 계정이 '승인된 영업자'로 취급됨
- 조치: ① 규칙을 화이트리스트로 전환: `root.child('users').child(auth.uid).child('status').val() === 'active'` (status 없는 구회원은 마이그레이션으로 active 백필). ② users/$uid .write에 `newData.exists()` 를 추가해 본인 노드 삭제 금지(삭제는 admin만). ③ lib/auth-session.ts:24 isPending도 `status !== 'active'` 화이트리스트로 맞춰 클라 게이트 동기화.
- 검증 상세: 반박 실패 — 4개 전제 모두 코드로 확인됨. (1) 삭제 허용: database.rules.json:6 `.write: auth != null && ($uid === auth.uid || admin)`, users 상위(:3-4)에는 .read만 있어 상위 거부 없음, $uid 레벨에 hasChildren류 .validate 없음. (2) 삭제 시 validate 미평가: RTDB는 newData=null 쓰기에서 .validate를 평가하지 않음 — 평가됐다면 :8 role(newData.val()===data.val() false)·:14 status가 거부했을 것. 서브트리 삭제든 update({status:null}) 부분삭제든 동일하게 통과. 앱 UI 불필요 — idToken으로 REST DELETE /users/{uid}.json?auth=<idToken> 만으로 도달하므로 '콘솔 조작이라 도달 불가' 반박 불성립. (3) 복구 경로 없음: users/{uid} 쓰기 전수조사 결과 lib/firebase/auth.ts:219(가입 1회, app/login/page.tsx:114에서만 호출)·:249(name/phone)·:294/:308/:337(admin 전용)뿐, initAuth(:75-76)는 read만. middleware.ts 없음, functions/ 없음 → 서버 게이트 부재. (4) 프로필 부재=승인 영업자: auth.ts:75 `.val() || {}` → :77 rawRole='' → mapRole('')='agent'(auth-session.ts:37) → :95 status='' → isPending(auth-session.ts:25) false → auth-context.tsx:89 PendingApproval 미렌더. 심각도 검토: role 기반 

### [AUTH-3/AUTH] 비로그인 공개면이 RTDB 규칙상 읽기 불가 — 손님에게 보낸 카탈로그·견적 링크가 빈 화면

- 플랫폼·화면: 공통(손님 웹) / /catalog(공개 카탈로그), /q/{매물코드}(공개 견적서)
- 검증: 확정
- 근거: lib/public-access.ts:3-5 '/q · /catalog · /sign 은 로그인 없이 매물·서명 조회 가능해야 함' + auth-context.tsx:78 `allowed = authed || onLogin || publicPage` 로 게이트만 통과시킴. 그러나 database.rules.json:36 `"products": { ".read": "auth != null && …" }`, :173 v4/products 동일, :4 users 동일. 실제 읽기는 클라 SDK 직접: app/catalog/page.tsx:36 `const all = await getStore().list('product', co)`, app/q/[code]/page.tsx:33-35 `getStore().list('user', co)` · `getStore().get('product', co, key)`. 익명 로그인 없음(코드 전역에 signInAnonymously 없음, 규칙도 anonymous 배제). 실패는 lib/firebase/rtdb-adapter.ts:398-401 catch → `return []`. 서버 프록시도 없음(app/api = dev/drive-backup/extract-photos/img/ocr/sheet).
- 재현: 1) 시크릿 창(비로그인)에서 https://freepasserp.com/catalog?a=U0001 접속 → 매물 0건 빈 목록 2) /q/{실제 매물코드} 접속 → '견적을 찾을 수 없습니다' 3) 설정 화면(app/settings/page.tsx:245-255)은 이 링크를 '카톡·문자로 손님에게 공유'하라고 안내
- 기대: 비로그인 손님이 공유 링크로 매물·견적을 조회하고 담당 영업자 연락 CTA를 볼 수 있어야 함
- 실제: 공개면 3종 중 /sign(contract_sign 규칙:93 비인증 read 허용)만 동작하고, /catalog·/q는 permission_denied → 항상 빈 화면. 영업 공유 퍼널 전면 불능
- 조치: 손님 노출 최소 필드만 담은 공개 노드(예: v4/public_catalog/{product_code}: 차량명·사진·월대여료·보증금·심사표기·담당자표시명·연락처)를 발행하고 `.read: true` 로 열되 원가·공급사·회원 노드는 계속 차단. /q의 담당자 조회도 users 전량 list(app/q/[code]/page.tsx:33) 대신 그 공개 노드에서 해결. 대안으로 서버 라우트(admin SDK)에서 화이트리스트 필드만 반환.
- 검증 상세: 반박 3경로 모두 실패. (1)완화장치 없음 — signInAnonymously/커스텀토큰 전무, 서버 프록시 없음(app/api는 dev·drive-backup·img·ocr·sheet뿐), 공개 노드(proposals·car_models)는 해당 페이지가 읽지 않음. (2)재현 경로 실재 — auth-context.tsx:78이 /catalog·/q 비로그인 렌더 허용 → catalog/page.tsx:36-37·q/[code]/page.tsx:31-34가 getStore().list 호출 → .env.local NEXT_PUBLIC_DATA_BACKEND=rtdb로 RtdbAdapter → rtdb-adapter.ts:326,339가 클라 SDK로 products/policies 직접 read → database.rules.json:4,26,31,36,173이 비인증 거부 → :398-401 catch가 []로 삼킴 + auth-context.tsx:57-63이 permission_denied 무음화 → 빈 목록/'견적을 찾을 수 없습니다'. seed.ts:96이 rtdb 백엔드 시드 금지라 폴백 데이터도 없음. (3)심각도 타당 — settings/page.tsx:245-255가 이 링크를 손님 공유·귀속 퍼널로 안내하는데 비로그인 손님(유일한 대상)에게 100% 무음 실패. 핵심 영업공유 시나리오 전면 불능 = 출시차단. 단 /sign만은 contract_sign 규칙(:93)으로 동작, ERP 내부 기능 무영향. 잔여 불확실성은 규칙 파일의 라이브 배포 여부뿐이나 firebase.json이 이 파일을 배포 SSOT로 지정하므로 출시 시점엔 어느 쪽이든 성립.

### [AUTH-4/AUTH] 승인 전 계정 포함 모든 인증 사용자가 전 회원 PII(이름·연락처·사업자번호·회사)를 통째로 읽을 수 있음

- 플랫폼·화면: 공통(RTDB 규칙) / users 노드(회원 디렉터리)
- 검증: 확정
- 근거: database.rules.json:4 `"users": { ".read": "auth != null && auth.token.firebase.sign_in_provider !== 'anonymous'" }` — status·역할·본인 제한 없음. 저장 필드: lib/firebase/auth.ts:208-218 `{ uid, name, phone, company_name, business_no, user_code, requested_type, status, role }`(email만 v4/users_private로 분리 시도, :207/:217). v4/users도 동일(:207).
- 재현: 1) 누구나 /login → 계정 만들기로 가입(승인 불필요) 2) 승인대기 상태 그대로 콘솔에서 `get(ref(db,'users'))` 3) 전 회원(공급사 직원·영업자·관리자)의 이름·휴대폰·사업자등록번호·소속 회사 일괄 수집
- 기대: 회원 정보는 admin과 본인만 조회. 화면에 필요한 표시용 필드는 최소 공개 노드로 분리
- 실제: self-service 가입만으로 외부인이 전체 회원 디렉터리를 덤프 가능(경쟁 공급사·영업자 명단 유출)
- 조치: users/v4/users의 컬렉션 .read 제거 → `$uid` 레벨에서 `$uid === auth.uid || admin` 만 허용. 목록이 필요한 관리자 화면은 admin 조건으로 유지하고, /q·채팅 표시명 등은 name·user_code만 담은 users_public 노드로 분리. phone은 users_private로 이동(현재 /q 연락 CTA 때문에 본노드 유지 중 — AUTH-3 공개 노드 도입 시 함께 정리).
- 검증 상세: 반박 실패 — 4개 전제 모두 코드로 확인됨.  1) 규칙 게이트: database.rules.json:4 `"users": { ".read": "auth != null && auth.token.firebase.sign_in_provider !== 'anonymous'" }` — status·role·$uid·query 제약이 전혀 없음. 하위에 좁히는 `$uid/.read`도 없음(:5-22는 .write/.validate만). v4 오버레이도 동일(:207). 대조군으로 같은 파일의 다른 노드는 전부 status!=='pending' + role/company_code/query.equalTo 스코프를 걸고 있어(rooms :41, contracts :58, settlements :62, customers :72, users_private :218-220) users만 의도적으로 열린 상태 — "다른 레이어가 좁혀준다"는 반박 근거 없음. RTDB는 클라이언트 SDK/REST 직결이라 규칙이 유일한 서버 게이트이며, 앱 화면 게이트(isPending)는 `GET /users.json?auth=<idToken>`에 아무 영향 없음.  2) 도달성(재현 1단계): 자가가입이 승인 없이 열려 있음 — app/login/page.tsx:165/187 "계정 만들기" → :103 `signup(...)` → lib/firebase/auth.ts:131-136 `createUserWithEmailAndPassword` 그대로 호출. 초대코드·이메일 인증·도메인 제한 없음(sendEmailVerification 저장소 전체 0건). 프로필 write가 실패해도 read 규칙은 auth 존재만 보므로 무관. 게스트 버튼은 익명 로그인이 아니라(signInAnonymously 0건) 로컬 세션이므

### [RACE-1/BIZ] 계약금 선점·이중판매 가드가 세션 캐시·역할 스코프에 의존해 교차 영업자 시나리오에서 무력화됨(같은 매물 이중 계약완료+정산 2건 가능)

- 플랫폼·화면: 공통 / 계약 진행(ContractPanel 스텝 체크)·완료 전이
- 검증: 확정
- 근거: settlement-engine.ts:237 `const contracts = await store.list('contract', co)` → rival 검사(:242-251)·완료 dup 가드(:261-267)가 이 목록과 store.get(product) 기반. 그런데 (a) 영업자 계약 read 스코프는 본인 것만(rtdb-adapter.ts:163-171 `orderByChild('agent_uid'), equalTo(auth.uid)`) → 타 영업자 rival 계약은 신선해도 안 보임. (b) 유일한 교차 방어인 상품 락 재확인(:246-247 `store.get('product', ...)`)은 DispatchStore.get이 세션 list 캐시를 우선 반환(store.ts:328-334 `_listResolved.get(ck) ... if (hit) return hit`), 캐시는 save에서만 무효화되고 update는 부분 patch만(store.ts:343-350, 226-233 '세션 한정') — 실시간 구독 없음(clearStoreCache는 auth 변경·dev 페이지뿐).
- 재현: 영업자 A·B가 서로 다른 기기에서 같은 매물에 각자 계약 진행. B가 앱을 연 시점(캐시 적재)이 A의 '계약금 입금' 선점 이전이면, 이후 B가 '계약금 입금' 클릭 시 rival(스코프상 불가시)·상품 락(스테일 캐시) 모두 통과 → 양쪽 선점, locked_by_contract가 B로 덮임. 이후 각자 5/5 완료 클릭(마지막 체크가 agent 스텝이면 dup 가드도 스코프상 불가시) → 계약완료 2건 + ST_ 정산 2건.
- 기대: 선점·완료 판정은 신선한 서버 상태 기준으로 원자적으로 결정(먼저 누른 계약만 성공, 두 번째는 차단).
- 실제: 판정 입력(계약목록·상품 락)이 세션 시작 시점 캐시 + 본인 스코프 데이터라, 캐시 적재 이후 타인이 만든 선점·완료를 못 보고 통과한다.
- 조치: ① 어댑터에 캐시 우회 fresh get(예: getFresh('product'))을 추가해 applyStepCheck의 2차 방어(:246)와 finalize 직전 dup 판정에 사용. ② 근본적으로는 RTDB 트랜잭션 또는 규칙으로 products/{key}/locked_by_contract를 '빈 값일 때만 자기 계약코드 기록' 조건부 write로 강제(선점 SSOT를 서버로). ③ 완료 dup 가드는 계약목록 대신 상품의 vehicle_status/locked_by_contract(신선값) 기준으로 판정.
- 검증 상세: 반박 실패 — 핵심 전제 전부 코드로 확인됨. (a) 영업자 계약 read는 rules가 스코프 쿼리를 강제(database.rules.json:263, rtdb-adapter.ts:164-171)해 신선 조회로도 타 영업자 rival 계약이 구조적으로 불가시 → rival 검사(settlement-engine.ts:242)·완료 dup 가드(:261-267)는 agent 클릭 시 무력. (b) 2차 방어 store.get('product')는 DispatchStore가 세션 list 캐시를 우선 반환(store.ts:328-334)하고, 캐시는 자기 write만 패치(343-350)·실시간 구독 전무(onValue 0건)·clearStoreCache는 auth 변경/dev뿐 → B의 탭이 A 선점 이전에 열려 있으면 무기한 스테일 '출고가능' 통과, syncVehicleLock(:174-176)이 A의 락을 B로 덮어씀(락 탈취). (c) 서버측 완화 없음: rules의 계약완료 validate(:280)는 자기 계약 체크만 검사(교차 계약·상품 제약 불가), v4/products write는 전 인증사용자 허용(:174), 정산 write도 교차 검증 없음(:231), 트랜잭션 미사용(dbUpdate만). 유일한 부분 완화는 주장이 놓친 것: 완료 필수인 provider_balance_confirmed가 DEPOSIT_CLAIM_KEY(contract.ts:42)라 공급사 스코프(양 계약 모두 가시)의 rival 검사를 거침 → 공급사 한 세션은 두 건을 모두 확인 못 함(자기 write가 캐시에 패치됨). 따라서 '계약완료 2건+정산 2건'은 공급사측 스테일 세션 2개(직원 2명·PC+폰 등)까지 겹쳐야 해 주장보다 발생 확률이 낮다. 그러나 그 완화 없이도 영업자 2명만으로 이

### [SNAP-1/BIZ] 계약 금액·기간 스냅샷이 손님 선택과 무관하게 '최저가 기간'으로 자동 동결되고 이후 수정 경로가 없음 — 정산·계약서·손님 서명페이지 금액이 전부 이 값 기준

- 플랫폼·화면: 공통 / 출고문의(계약 생성)→정산·계약서·/sign
- 검증: 확정
- 근거: ContractPanel.tsx:54-55 `const m = cheapest(product)?.m || priceList(product)[0]?.m || 0; createContractRequest(product, { period: m, ... })` → deal.ts:149,166-171 `rent_month_snapshot: opt.period, rent_amount_snapshot: pr?.rent ?? 0`(기간 선택 UI 없음, 유일한 기록 지점 — repo 전체에서 rent_amount_snapshot write는 deal.ts:167뿐, 수정 폼 없음). 소비처: settlement-engine.ts:46-48(R1·R2 산정), app/sign/[token]/page.tsx:90(손님에게 '대여기간 N개월·월 대여료' 표시), contract-send.ts:107-111(계약서 rent_amount·계약종료일).
- 재현: 손님이 12개월 계약을 원하는 매물(최저가 기간이 60개월·월 39만)에서 '출고 문의하기' 클릭 → 60개월/39만으로 동결. 계약서 발송 시 손님 /sign 요약에 '60개월·390,000원' 표시, 완료 시 정산 R1=39만×수수료율. 극단: 유효 가격이 없는 매물은 period=0·rent=0으로 생성돼 0원 계약서·0원 정산이 통과.
- 기대: 계약 생성 시 실거래 기간을 선택하고, 약정(발송) 전까지는 기간·금액 스냅샷을 수정할 수 있어야 함. rent=0 계약은 발송·완료 차단.
- 실제: 스냅샷이 최저가 기간으로 자동 확정되어 이후 어떤 화면에서도 못 고침. 관리자 R1/R2 수기 보정(app/contract/page.tsx:263-277)만 가능하고 손님 노출 값(서명페이지·계약서)은 틀린 채 감.
- 조치: doInquiry에 기간 선택(priceList 기간 칩) 추가, 계약요청~약정 전 단계에서 rent_month/rent_amount/deposit 스냅샷 수정 액션 제공(정산 생성 전 한정). createSignToken·sendContractLink에서 rent_amount_snapshot<=0이면 발송 거부.
- 검증 상세: 반박 실패 — 모든 전제가 코드로 확인됨. (1) ContractPanel.tsx:54-55가 기간 선택 UI 없이 cheapest(product)(product.ts:159, 전 기간 중 월대여료 최저=통상 최장기)로 기간을 결정해 createContractRequest에 전달하고, deal.ts:167이 repo 유일의 rent_amount_snapshot 기록 지점(프로덕션 호출자는 ContractPanel:55뿐). (2) 수정 경로는 주장보다 더 강하게 봉쇄됨: database.rules.json:274-276이 rent_month/rent_amount/deposit_amount_snapshot을 생성 후 불변으로 강제하며 admin 예외도 없고, 프로덕션 스토어는 RTDB 어댑터(store.ts:373-376)라 규칙이 실효 — 수정 UI를 추가해도 클라이언트로는 못 고침. (3) 완화 후보 전부 무효: contract-send.ts:130의 draft 오버라이드는 buildContractPayload/sendContractLink/saveDraft 호출자 전무(미배선), 실제 발송은 ContractSign.tsx:59 createSignToken뿐이고 손님 /sign 페이지(app/sign/[token]/page.tsx:90)는 contract_sign 공개 슬롯(contract-sign-public.ts:58)에 복사된 스냅샷을 그대로 표시, 약정 단계(ContractPanel.tsx:63-70)는 손님명·연락처만 확인, 관리자 보정(app/contract/page.tsx:263-277)은 정산 fee_amount/agent_payout만 가능해 손님 서명 문서는 틀린 채 확정. (4) 0원 극단도 도달 가능: 무가격 매물 필터는 공개 카탈로그(catalog/page.t

### [AUTH-1/SEC] 공개 회원가입으로 얻은 미승인(pending) 계정이 users·products·partners·policies 전체를 raw로 읽을 수 있음 — 전 회원 이름·연락처·사업자번호 유출

- 플랫폼·화면: 공통(RTDB 규칙) / 전 노드 / 회원가입(/login)
- 검증: 확정
- 근거: database.rules.json:4 `"users": {".read": "auth != null && auth.token.firebase.sign_in_provider !== 'anonymous'"}` (status 게이트 없음. 같은 노드 .write:6은 role 체크). products:36 · partners:26 · policies:31 · v4/users:207 도 동일하게 status 무검사. auth.ts:219 `set(ref(db,'users/'+uid), {... status:'pending', role:safeRole ...})` — 가입 즉시 users/{uid} 생성. auth-context.tsx:89 `if (isPending(session) ...) return <PendingApproval/>` = 클라 UI 게이트뿐. app/login/page.tsx:165 '계정 만들기' 공개 노출.
- 재현: 1) freepasserp.com/login → '계정 만들기'로 아무 이메일/사업자번호로 가입(승인 불필요). 2) 화면은 '가입 승인 대기 중'에서 멈춤. 3) 같은 브라우저 콘솔(또는 curl)에서 ID 토큰으로 `GET {DATABASE_URL}/users.json?auth={idToken}` 호출. 4) 전 회원의 name·phone·business_no·company_code·role(+users_private 미이관 계정은 email)이 통째로 반환됨. products.json·partners.json·policies.json도 동일.
- 기대: 승인(status active) 전 계정은 어떤 업무 노드도 읽을 수 없고, users는 본인+관리자만 읽혀야 함
- 실제: pending 계정이 users/products/partners/policies 전체를 무제한 조회. 승인 게이트가 클라이언트 UI에만 존재
- 조치: ① users .read를 `$uid === auth.uid || role==='admin'`으로 축소하고, 화면에 필요한 표시용 최소필드(이름·역할)는 별도 공개 인덱스 노드로 분리. ② products/partners/policies/v4/* 의 모든 .read에 `root.child('users').child(auth.uid).child('status').val() !== 'pending'` 추가(이미 .write에는 있는 조건과 대칭). ③ 가입 승인 전 계정은 커스텀클레임 없이 데이터 접근 불가하도록 규칙 기준을 role이 아닌 status+role 조합으로 통일.
- 검증 상세: 반박 실패, 실제 결함. 3개 전제 모두 코드로 확인: (1) database.rules.json:4(users)·26(partners)·31(policies)·36(products)·173(v4/products)·187(v4/policies)·191(v4/partners)·207(v4/users) 읽기 규칙이 `auth!=null && sign_in_provider!=='anonymous'`뿐 status 무검사. 형제 노드 rooms:41·messages:50·contracts:58·settlements:62는 모두 status!=='pending' 게이트를 포함 → pending 차단이 설계 의도인데 이 4노드만 누락된 설계 실수. (2) app/login/page.tsx:114 공개 signup → auth.ts:131-135 createUserWithEmailAndPassword(provider='password'≠anonymous), 이메일인증·승인 불필요로 토큰 즉시 발급, auth.ts:219 가입 즉시 users/{uid}(status:pending) 생성. 규칙은 email_verified 미검사 → REST GET /users.json?auth=idToken 통과. (3) auth.ts:208-217 users 레코드에 name·phone·business_no·company_name·role 상시 저장, emailMoved 실패 시 email까지 잔류. users_private·partners_private·products_private는 role 게이트로 보호되나 본 users 노드 PII는 무보호. 완화장치 무효: auth-context PendingApproval은 클라 UI뿐(REST 우회), RTDB는 상위경로 허용 시 하위트리 전체 허용, 별도 서버 검증

### [COST-1/SEC] 원가·수수료 스트리핑이 rules에 전혀 없음 — products 통째 get 한 번에 fee·commission·vehicle_price·vin 노출(클라 stripProductCost는 UI 전용)

- 플랫폼·화면: 공통(RTDB 규칙) / 매물(재고·파인더·카탈로그) 원가·수수료
- 검증: 확정
- 근거: database.rules.json:36 products `.read`=인증만, :173 v4/products `.read`=인증만, 자식 규칙·필드 제한 전무. 보호 대상 필드는 rtdb-products.ts:7 `const PRIVATE_PRICE_FIELDS = new Set(['fee','commission','fee_memo'])` + :42~49 vehicle_price·vin. 스트리핑은 클라에서만: rtdb-adapter.ts:419 `return shown.map((r) => (canSeeProductCost(r) ? r : stripProductCost(r)))`, :448(listRaw), :465(get). 분리 이관은 수동·기본 dryRun: migrate-products-private.ts:153 `migrateProductsPrivate(dryRun = true …)`.
- 재현: 영업자(또는 위 AUTH-1로 만든 pending 계정)로 로그인 → 콘솔에서 `get(ref(db,'products'))` 또는 `GET {DATABASE_URL}/products.json?auth={idToken}`. v4/products_private로 이관되지 않은 레코드는 `price.36.fee`, `price.36.commission`, `vehicle_price`, `vin`이 그대로 반환됨. 앱 UI(canSeeProductCost)만 우회하면 되므로 별도 도구 불필요.
- 기대: 타사 원가·수수료·차대번호는 rules 단에서 접근 불가(공급사 본인·관리자만)
- 실제: rules는 products 전체를 인증자 전원에게 열어둠. 분리(v4/products_private) 이관이 실제로 100% 끝났는지도 규칙이 보증하지 않으며, 이관 후에도 공개 노드에 fee를 다시 써넣는 것을 막는 validate가 없음
- 조치: ① 오픈 전 migrateProductsPrivate(dryRun=false) 완료를 배포 게이트로 강제하고 잔여 0건을 스크립트로 검증. ② 재유입 차단 validate 추가: `"products": {"$id": {"vehicle_price": {".validate": false}, "vin": {".validate": false}, "price": {"$period": {"fee": {".validate": false}, "commission": {".validate": false}, "fee_memo": {".validate": false}}}}}` (v4/products에도 동일 적용). ③ 원가는 v4/products_private에만 존재한다는 불변식을 데이터체크(app/data-check)에 상시 점검 항목으로 추가.
- 검증 상세: 반박 실패(단, 필드 범위는 축소 정정).  [전제1 — 규칙 레벨 보호 전무] 확인됨. database.rules.json:36 `products` `.read` = `auth != null && sign_in_provider !== 'anonymous'` 뿐(자식 규칙·필드 규칙 0개), :173 `v4/products` 동일. status pending 체크조차 없어 **가입 직후 승인대기 계정도 전체 읽기 가능**(auth.ts:131 signup은 공개 self-signup, :214 status:'pending' 기록). 원가 격리는 오직 `v4/products_private`(:177~184)에만 존재.  [전제2 — 스트리핑이 클라 전용] 확인됨. rtdb-adapter.ts:419(list)·:448(listRaw)·:465(get)에서 `canSeeProductCost(r) ? r : stripProductCost(r)`, canSeeProductCost는 rtdb-products.ts:86 `getSession()` 기반 = 순수 클라 판정. products 조회 경로는 서버 프록시 없이 firebase/database 클라 직결(rtdb-adapter.ts:322 `get(ref(db, node))`). 앱 UI만 우회하면 됨 — 주장대로.  [전제3 — 실제 공개 노드에 원가가 남아 있는가] **실데이터로 확인됨(결정타).** 최신 전체 스냅샷 D:\dev\freepasserp4\tmp\full-backups\backup-b4.json(2026-07-28) 파싱 결과: - `products`(v3): 5,686건 중 **1,868건에 `vehicle_price`(원가) 그대로 존재**. 예: `EXT_0021c3c77177` product_code=RP004_1

### [PII-1/SEC] 주민등록번호·면허번호·주소·서명이미지를 무인증 공개 읽기 노드(contract_sign/{token})에 평문 저장 — 승인 전까지 링크 보유자 전원이 열람

- 플랫폼·화면: 공통(웹 · 손님 공개면) / /sign/[token] 전자서명 제출 · contract_sign 노드
- 검증: 확정
- 근거: lib/domain/sign.ts:173-187 `patch = { customer_id: data.customer_id || '', driver_license_no…, customer_address… }` → `writeContractSign(token, {...patch, status:'pending_review'})` (contract-sign-public.ts:46 `dbUpdate(ref(db,'contract_sign/'+token), clean)`). database.rules.json:93 `".read": "(data.child('revoked_at').val() === null && (data.child('expires_at').val() === null || data.child('expires_at').val() > now)) || …"` — 앞 분기는 auth 조건이 없어 비로그인 전체 열람. :104 customer_id validate는 길이(≤30)만 확인. 마스킹·암호화 코드 없음(entities.ts:252 `note: '⚠ 민감정보 · 보관/마스킹 정책 보류'`). 스크럽은 승인 시점에만: sign.ts:214 `writeContractSign(token, {...scrubPublicSignSubmission(), status:'signed'})`.
- 재현: 1) 영업자가 계약서 발송(ContractSign.tsx:59 createSignToken) → 손님에게 /sign/{token} 링크 전달(카톡). 2) 손님이 성명·연락처·주민등록번호·면허·주소 입력 후 제출. 3) 그 링크를 받은 누구든(단체방 참여자·전달받은 제3자) 로그인 없이 `GET {DATABASE_URL}/contract_sign/{token}.json` 호출 → customer_id(주민번호 13자리)·driver_license_no·customer_address·customer_phone·sign_signature(PNG) 전부 평문 반환. 관리자가 승인하지 않으면 만료(7일)까지 그대로 유지.
- 기대: 주민등록번호는 암호화 저장(개인정보보호법 24조의2)·공개 노드 미보관, 제출 직후 공개 슬롯에서 제거
- 실제: 평문 그대로 무인증 공개 읽기 노드에 최대 7일 상주. 계약 노드·감사 로그에도 평문 복제
- 조치: ① /sign 제출은 주민번호를 공개 슬롯에 쓰지 않도록 변경 — sign.ts:173의 patch에서 customer_id·driver_license_no·customer_address 제외하고, 민감항목은 관리자만 읽는 별도 노드(예: contract_pii/{contract_code}, .read=admin·해당 provider 제외)에 쓰거나 수집 자체를 재검토. ② 불가피하면 최소 저장(뒤 7자리 미저장·앞 6자리만) + 앱단 암호화. ③ contract_sign .read를 '토큰 소유 = 열람'이 아니라 제출 완료 즉시 PII 필드 null 처리(제출 성공 시 scrubPublicSignSubmission을 승인이 아니라 제출 직후에 호출)로 노출창 제거.
- 검증 상세: 반박 실패 — 전제 전부 코드로 확인. (1) sign.ts:173-187 submitSign이 주민번호·면허번호·주소·서명PNG를 contract_sign/{token}에 평문 기록(contract-sign-public.ts:46 dbUpdate), 암호화·마스킹 코드 없음(entities.ts:252 정책 보류 명시). (2) database.rules.json:93 .read 첫 분기(revoked_at null && expires_at>now)에 auth·status 조건이 없어 pending_review 상태에서도 비로그인 REST GET으로 전체 반환 — :104 validate는 길이만 검사. (3) 스크럽은 승인(sign.ts:214)·반려(:240)에만, 미조치 시 7일(TTL, contract-sign-public.ts:15) 상주하고 만료 후에도 데이터는 미삭제. (4) /sign/[token]은 클라이언트가 RTDB 직접 접근, 서버 검증·완화 계층 없음. 반박 시도: 토큰이 24바이트 CSPRNG(sign.ts:101-109)+상위 노드 목록 불가라 무차별 열거는 불가하나, 주장 자체가 '링크 보유자'로 한정되어 있고 카톡 전달·단체방·기기분실 경로가 현실적이라 기각 사유 안 됨. 제출 후 UI가 PII를 숨기는 것(page.tsx:66-74)도 표시 계층일 뿐 REST에는 무력. 승인 시 contracts·audit_logs에도 평문 복제(rtdb-adapter.ts:562,620) — 인증 보호되나 주민번호 암호화 의무(개인정보보호법 24조의2)는 접근통제와 무관하게 위반. 핵심 계약 플로우 전 고객 대상 보안 유출 + 법정 의무 위반이라 출시차단 유지.

### [PUB-1/SEC] 비로그인 손님은 공개 견적·카탈로그가 전부 빈 화면 — rules가 products/policies에 인증을 요구하는데 공개면은 익명 로그인도 하지 않음

- 플랫폼·화면: 웹(손님 공개면) / /q/[code] 견적서 · /catalog 공개 카탈로그
- 검증: 미검증
- 근거: database.rules.json:36 products `.read`=`auth != null …`, :31 policies 동일. rtdb-adapter.ts:339 `if (entity === 'product') joinMap = (await get(ref(this.db(), 'policies'))).val() || {}` → 비로그인 시 PERMISSION_DENIED → :398 `catch { console.warn(...); return []; }`. app/q/[code]/page.tsx:34 `setP(await getStore().get('product', co, key))` → null → :40 `if (!p) return <CenterNote>견적을 찾을 수 없습니다.</CenterNote>`. app/catalog/page.tsx:35 동일 경로. 익명 로그인 없음(`signInAnonymously` 검색 0건), public-access.ts:5는 /login 리다이렉트 게이트만 우회. seed는 로컬 전용(lib/seed.ts:96 `if (!store.backend.startsWith('local')) return false`).
- 재현: 시크릿 창(비로그인)으로 https://freepasserp.com/q/{매물코드} 접속 → '견적을 찾을 수 없습니다'. /catalog 접속 → 목록 0건. 로그인 상태에서 같은 링크를 열면 정상 표시되어 영업자 본인 확인 시엔 문제가 드러나지 않음.
- 기대: 손님에게 공유한 견적·카탈로그 링크는 로그인 없이 열림
- 실제: 공개면 전체가 데이터 0건. 영업 공유 시나리오(귀속 ?a= 포함)가 손님 쪽에서 동작하지 않음
- 조치: 공개용 비민감 미러 노드(예: v4/public_products — 원가·vin·fee 제외, .read: true)를 매물 저장 시 함께 갱신하고 /q·/catalog는 그 노드만 읽게 한다. 익명 로그인 방식을 택할 경우엔 규칙의 `sign_in_provider !== 'anonymous'` 조건 때문에 기존 노드가 열리지 않으므로 반드시 별도 공개 노드로 분리할 것(민감필드 동반 노출 방지).

### [RACE-1/DATA] 계약금 선점(rival)·이중판매 가드가 세션 영구 캐시를 읽어 타 사용자의 선점·완료를 못 보고 통과됨

- 플랫폼·화면: 공통 / 계약 단계 체크(ContractPanel → applyStepCheck)
- 검증: 확정
- 근거: settlement-engine.ts:237 `const contracts = await store.list('contract', co)` → store.ts:273-282에서 list 결과 Promise를 `_listCache`에 영구 보관(실패 시에만 삭제, TTL·구독 없음). 2차 방어 settlement-engine.ts:246 `store.get('product', co, productCode)`도 store.ts:331-334에서 `_listResolved` 캐시 우선 반환. 중복완료 가드(settlement-engine.ts:264)도 같은 캐시 목록 사용
- 재현: 1) 영업자 A·B가 각자 브라우저에서 오전에 앱 접속(계약·매물 list 캐시 생성) 2) A가 오후에 같은 차 계약금입금 체크 → 계약중 락 3) B가 자기 세션에서 같은 차 계약금입금 체크 — B의 contracts 캐시(오전분)에 A 계약 없음, product 캐시에도 locked_by_contract 없음 → rival·락 검사 모두 통과 → 두 계약이 동시 선점, 완료까지 가면 이중판매
- 기대: 선점·이중판매 가드는 판정 시점의 서버 최신 계약·매물 상태로 검사(최소한 가드 직전 캐시 우회 신선 조회, 이상적으로는 RTDB 트랜잭션으로 locked_by_contract 원자 선점)
- 실제: 가드 입력이 세션 시작~마지막 자기 쓰기 시점의 스냅샷 — 타 사용자 변경이 세션 내 영영 반영 안 되어 경합창이 수 시간으로 확대
- 조치: applyStepCheck의 선점·중복완료 판정 경로에서만 캐시 우회: DispatchStore에 `listFresh`/`getFresh`(캐시 무시, base 직행) 추가해 settlement-engine.ts:237·246·264가 이를 사용. 근본책은 v4/products/{key}/locked_by_contract에 runTransaction(빈 값일 때만 자기 계약코드 기록) — user_code 채번(auth.ts:200)과 동일 패턴
- 검증 상세: 반박 전부 실패. (1) store.ts:271-283 list Promise 영구 세션 캐시 확인 — 무효화는 자기 쓰기뿐(update는 patchListCache로 자기 delta만 병합), TTL·구독 없음. 저장소 전체에 onValue류 리스너 0건, clearStoreCache는 dev 페이지·auth 변경뿐 → 타 사용자 쓰기는 세션 내 영영 미반영. (2) 2차 방어 store.get('product')도 store.ts:330-334에서 _listResolved 우선 반환 — 파인더를 한 번이라도 본 세션은 stale 상품행으로 락 검사 통과. (3) 완화 레이어 부재 확인: database.rules.json:280 계약완료 validate는 자기 스텝 필드만 검사(교차계약·상품락 조건 없음), runTransaction 미사용. 오히려 주장보다 악화 — rtdb-adapter.ts:146-179+rules:263이 agent 계약 list를 agent_uid 스코프로 강제해 rival 검사(settlement-engine.ts:242)는 신선 조회로도 타 영업자 계약을 구조적으로 못 봄 → 유일한 교차 영업자 가드가 캐시에 뚫리는 상품락 검사뿐. B의 계약 생성(save)도 product 캐시는 무효화 안 함. finalizeContractIfReady(settlement-engine.ts:82-87)가 locked_by_contract를 B로 덮어쓰고 중복완료 가드(:264)도 같은 스코프·캐시 목록이라 이중 계약완료+이중 정산 도달 가능. 경합창=세션 수명(수 시간), 선점락의 본래 목적(교차 영업자 병행 딜)에서 정확히 실패 — 돈·상태 정합 붕괴로 출시차단.

### [LAUNCH-1/OPS] 게시 규칙에서 손님 공개면 /q(견적 공유)·/catalog가 데이터 읽기 전면 거부되어 '견적을 찾을 수 없습니다'만 뜬다

- 플랫폼·화면: 공통(손님 브라우저) / /q/{code} 화이트라벨 견적서 · /catalog
- 검증: 확정
- 근거: D:\dev\freepasserp4\database.rules.json:36 `products ".read": "auth != null && ...!== 'anonymous'"` (policies:31, users:4, v4/products:173 동일) ↔ app/q/[code]/page.tsx:34 `setP(await getStore().get('product', co, key))`는 비로그인 클라 SDK 읽기. lib/firebase/rtdb-adapter.ts:398-401 merged()가 PERMISSION_DENIED를 catch해 [] 반환 → get()=null → page.tsx:40 '견적을 찾을 수 없습니다'. page.tsx:31 `getStore().list('user', co)`도 거부되어 담당 영업자 전화 CTA(?a= 귀속)도 비표시. 익명 로그인 코드 없음(lib 전체 grep signInAnonymously 0건). lib/public-access.ts:3 '로그인 없이 매물·서명 조회 가능해야 함' 의도와 규칙이 정면 충돌. LAUNCH_GONOGO.md:13 B1 ✅ 게시 완료(2026-07-28) 전제.
- 재현: 로그아웃/시크릿 창에서 freepasserp.com/q/{product_code}?a={user_code} 접속 (영업자가 손님에게 공유하는 그 링크). NEXT_PUBLIC_DATA_BACKEND=rtdb + database.rules.json 게시 상태.
- 기대: 화이트라벨 견적서(사진·요금·조건) + 담당 영업자 전화 CTA 표시
- 실제: products·policies·users 읽기 모두 PERMISSION_DENIED → '견적을 찾을 수 없습니다'. /catalog도 빈 목록. 규칙이 미게시라면 반대로 B1(유일 보안경계)이 거짓이므로 어느 쪽이든 출시차단
- 조치: products 전체 .read 완화는 금지(원가·전량 덤프 유출). 공유 시점에 원가·PII 제거한 공개 스냅샷을 v4/quote_public/{code}에 기록하고 /q는 그 노드만 읽게(.read true, 필드 화이트리스트 validate) 하거나, 서버 라우트 /api/q/{code}가 Admin SDK로 필요한 표시 필드만 응답. 담당자 연락처는 스냅샷에 agent_name·agent_phone만 포함. 런북 §8 스모크에 '비로그인 /q 열람' 항목 추가
- 검증 상세: 반박 전부 실패. (1)완화 레이어 부재: isPublicAccess는 Auth 게이트 통과용일 뿐 RTDB 읽기 권한과 무관(lib/store.ts:372 주석 명시), signInAnonymously·firebase-admin 저장소 전체 0건, /q는 'use client'가 클라 SDK로 직접 읽음(app/q/[code]/page.tsx:31,34), seedIfEmpty는 rtdb에서 즉시 false(lib/seed.ts:96). (2)재현 확정: .env.local:4 프로덕션 rtdb 백엔드 + 라이브 DB 실측 — 비로그인 REST GET /products.json=401 Permission denied, /users.json=401, /car_models.json(규칙상 .read:true)=200으로 배포 규칙이 repo database.rules.json과 일치·게시 상태 확인. 익명 읽기 거부→rtdb-adapter.ts:362-363,398-401 catch→[]→get()=null→page.tsx:40 '견적을 찾을 수 없습니다', /catalog도 빈 목록. (3)심각도: /sign은 contract_sign 공개 토큰 규칙(rules:93)+getStore 미사용으로 생존하나, /q·/catalog는 비로그인 방문자 100% 재현 전면 불능. public-access.ts:3이 요구사항으로 명시한 손님 대면 핵심 시나리오이고 이미 Go/라이브(LAUNCH_GONOGO.md:19) — 영업자가 공유하는 견적 링크 전부 사장. B1 스모크는 테넌트 격리만 검증, 공개면 미검증이 탐지 공백. 수정 방향: 공개 노출용 별도 노드(예: products_public 미러) 또는 서버 프록시(admin SDK) 중 택일 필요 — 규칙에서 products 전체 공개는 원

---

## 긴급 (12건)

### [AUTH-5/AUTH] 신규 파트너에 사업자번호를 넣을 방법이 앱에 없어 '공급/영업' 가입 승인이 영구 차단되고, 에러 메시지는 실행 불가능한 조치를 안내

- 플랫폼·화면: 공통 / 회원·파트너(/members) 가입 승인 · 로그인 가입 폼
- 검증: 미검증
- 근거: 승인 가드: lib/firebase/auth.ts:327-329 `if ((requested === '공급' || requested === '영업') && !matched_partner_code) throw new Error('미등록 사업자번호입니다. 파트너사(공급/영업)를 먼저 등록해 사업자번호를 매칭한 뒤 승인하세요.')`. 매칭 소스: 같은 파일 :157 `const partners = (await get(ref(db, 'partners'))).val()` — v3 루트 partners만 조회. 그런데 /members에서 만든 파트너는 lib/firebase/rtdb-adapter.ts:501 `dbUpdate(ref(this.db(), 'v4/partners/{key}'))` 로 저장되고, 파트너 엔티티에는 사업자번호 필드 자체가 없음: lib/intake/entities.ts:321-337(partner_code·name·alias·partner_type·fee_rate·contact·sheet_* 만). 폼 노출 필드도 app/members/page.tsx:273-279 로 동일.
- 재현: 1) 신규 공급사 직원이 가입(유형=공급, 사업자번호 입력) 2) 관리자가 /members → 파트너 탭 → 신규로 해당 회사 등록(사업자번호 입력란 없음, v4/partners에 저장) 3) 회원 탭에서 그 가입자 선택 → '가입 승인' 4) 매번 '미등록 사업자번호입니다…' 토스트 → 안내대로 파트너를 등록해도 영원히 매칭되지 않음
- 기대: 관리자가 앱에서 파트너(사업자번호 포함)를 등록하면 곧바로 매칭되어 승인 완료
- 실제: 승인 불가 루프. 유일한 탈출로는 회원 폼에서 company_code·role을 수동 입력해 신원을 먼저 박아넣는 우회(approveUser:311-317 hasIdentity 보존 경로)뿐이며 화면 어디에도 안내 없음
- 조치: ① lib/intake/entities.ts partner 필드에 `business_number` 추가(+/members 파트너 기본정보 폼에 노출). ② resolveIdentity(auth.ts:152-172)를 v3 `partners` ∪ `v4/partners` 병합 조회(getStore().list('partner'))로 교체 — 매칭 SSOT를 UI 등록 경로와 일치시킴. ③ 승인 실패 시 관리자에게 '파트너 직접 선택' 폴백(파트너 드롭다운 → company_code/role 배정 후 승인) 제공, 그때 opts.rematch도 노출.

### [AUTH-6/AUTH] 회원 '활성=아니오'(비활성)가 인증·권한 어디에도 반영되지 않아 퇴사·차단 대상 계정이 그대로 로그인·데이터 접근

- 플랫폼·화면: 공통 / 회원·파트너(/members) 소속·권한 > 활성
- 검증: 미검증
- 근거: 쓰기만 존재: lib/firebase/auth.ts:291 `if (fields.is_active != null) patch.is_active = …`(app/members/page.tsx:199에서 전달). 소비처는 표시·필터뿐 — components/list-rows.tsx:287,304(배지), features/members/member-filter.ts:49-50(필터), lib/domain/identity.ts:60(조직원 목록 제외). 세션 발급 경로(auth.ts:68-96)와 게이트(lib/auth-session.ts:24 isPending, lib/auth-context.tsx:89)는 status만 검사하고, database.rules.json 전체에 is_active 문자열이 한 번도 등장하지 않음. 반면 화면 설명은 app/members/page.tsx:298-300 '역할과 활성 상태는 메뉴 접근 및 데이터 범위의 기준입니다'.
- 재현: 1) 관리자가 퇴사자 회원을 선택 → 활성 '아니오' 저장(배지 '비활성'으로 바뀜) 2) 해당 계정으로 로그인 3) 홈·계약문의·계약진행·정산 전부 이전과 동일하게 접근됨(차단 없음)
- 기대: 비활성 회원은 로그인 세션이 거부되거나 최소한 모든 업무 데이터 접근이 차단되어야 함
- 실제: 비활성은 목록 배지·필터에만 반영되는 장식 필드. 관리자는 차단했다고 오인
- 조치: 단기: /members 활성 토글을 approveUser(uid,false)(status→pending)와 묶어 실제 차단으로 연결하고 힌트 문구 수정. 정식: initAuth(auth.ts:90-96)에서 `is_active === '아니오'` 면 setSession 하지 않고 즉시 signOut + 안내, 그리고 규칙의 status 조건에 `root.child('users').child(auth.uid).child('is_active').val() !== '아니오'` 를 AND로 추가.

### [AUTH-7/AUTH] 가입 폼에 이용약관·개인정보 수집·이용 동의 절차가 전혀 없음(이름·휴대폰·사업자번호 수집)

- 플랫폼·화면: 공통 / /login 계정 만들기
- 검증: 미검증
- 근거: app/login/page.tsx:177-188 가입 폼 전체 — 이메일·비밀번호·이름·연락처·회사명·가입유형·사업자번호 입력만 있고 동의 체크박스·약관 링크·동의 시각 저장이 없음. 제출 핸들러 doSignup(:96-125)의 검증도 이메일/비번/유형뿐. 저장 레코드에도 동의 흔적 없음: lib/firebase/auth.ts:208-218. 대조군으로 손님 서명 흐름은 5종 동의를 규칙으로 강제(database.rules.json:110 `sign_consents … 'rental_terms,privacy,credit,gps,cms'`, :111 sign_consent_version) — 회원 가입만 누락. terms 노드(:124-127)는 존재하지만 가입 흐름에서 참조되지 않음.
- 재현: 1) /login → 계정 만들기 2) 필드만 채우고 제출 3) 약관·개인정보 동의 없이 계정 생성 및 개인정보(이름·휴대폰·사업자등록번호) 저장 완료
- 기대: 개인정보 수집·이용 동의(필수)와 이용약관 동의를 분리 체크로 받고, 동의 항목·버전·시각을 계정에 기록
- 실제: 동의 절차 부재 — 개인정보보호법 제15조 수집·이용 동의 위반 소지, 분쟁 시 입증 자료 없음
- 조치: 가입 폼에 [필수] 이용약관 / [필수] 개인정보 수집·이용(항목·목적·보유기간 명시) / [선택] 마케팅 수신 체크를 추가하고 미체크 시 제출 차단. writeUserProfile 레코드에 `consents:'terms,privacy'`, `consent_version`, `consented_at` 저장(서명 흐름의 sign_consents 패턴 재사용), 약관 본문은 기존 terms 노드에서 버전과 함께 렌더.

### [STOR-1/SEC] 서명 계약서 경로가 무인증 업로드·덮어쓰기 + 무인증 공개 읽기 — 계약 문서 위조 및 임의 파일 호스팅 가능

- 플랫폼·화면: 공통(Storage 규칙) / Firebase Storage /contract-signed/**
- 검증: 확정
- 근거: storage.rules:37-40 `match /contract-signed/{token}/{fileName} { allow read: if true; allow create, update: if request.resource.size < 20 * 1024 * 1024; }` — create/update에 `request.auth != null` 조건 자체가 없음. 대조군으로 바로 아래 :42-46 contract-unsigned는 `request.auth != null`을 요구.
- 재현: 로그인/토큰 없이 Storage REST 업로드 호출: `POST https://firebasestorage.googleapis.com/v0/b/{bucket}/o?name=contract-signed/anything/x.pdf` (Authorization 헤더 없음, 20MB 미만) → 성공. 기존 서명본 경로/파일명을 알면 같은 경로로 덮어써 원본 교체 가능하고, 업로드한 파일은 read:true라 누구나 다운로드 가능.
- 기대: 서명 계약서는 인증된 당사자만 쓰고, 읽기도 권한 스코프 안에서만
- 실제: 인터넷의 누구나 임의 파일을 무제한 개수로 올리고(스토리지 과금·악성파일 배포), 기존 서명본을 덮어쓸 수 있음
- 조치: `allow read: if request.auth != null;` + `allow create: if request.auth != null && request.auth.token.firebase.sign_in_provider != 'anonymous' && request.resource.size < 20*1024*1024;` `allow update: if false;` 로 교체. 비로그인 손님 서명본 저장이 필요하면 서버 라우트(관리자 자격증명)로만 쓰게 하고 클라 직접 업로드 경로는 폐쇄.
- 검증 상세: 반박 실패(핵심): storage.rules:37-40의 `contract-signed` create/update에 인증 조건이 없고, 이 규칙 원문이 tmp/verification-artifacts/firebase-storage-production-deploy-20260727.log:130,140에서 ruleset 9a8cdcea-56e9-48f5-bcd9-445a63d0ebb2로 freepasserp3.firebasestorage.app에 실제 릴리스(PATCH 200, 2026-07-27)된 것이 로그로 확인됨. 완화 레이어 없음 — lib/firebase/client.ts:39-48에 initializeAppCheck 부재(강제 시 lib/firebase/storage-files.ts:98,143의 자체 업로드가 깨지므로 미강제 확정), Storage REST는 서버 프록시를 우회하므로 rules가 유일 관문. 버킷명은 .env.local:11의 NEXT_PUBLIC_* 로 번들 노출. 따라서 인터넷 누구나 20MB 미만 파일을 무제한 업로드하고 read:true로 공개 배포(악성·피싱 호스팅, 과금 남용) 가능 — 이 부분은 성립. 반박 성공(부분): "기존 서명 계약서 덮어쓰기=문서 위조"는 도달 불가 — ① 리포 전수 grep 결과 v4·v3 어느 코드도 contract-signed에 읽기/쓰기 하지 않음(문서·규칙 파일만 히트), ② v4 서명은 RTDB data URL 저장(lib/domain/sign.ts:38,177), v3 sign.html:161,200도 RTDB contract_sign/{token}만 갱신, ③ 운영 RTDB 전체 백업(tmp/full-backups/freepasserp3-rtdb-full-2026-07-27-105542.json)에 cont

### [SEC-2/SEC] 모든 agent 계정(미승인 pending 포함)이 v4/partners_private 전체를 읽어 전 공급사 fee_rate(상업기밀)를 획득 — 민감필드 분리의 목적이 규칙에서 무력화

- 플랫폼·화면: 공통(RTDB 규칙) / v4/partners_private (공급사 수수료율)
- 검증: 미검증
- 근거: database.rules.json:200 `"partners_private": {".read": "auth != null && … && (role==='admin' || role==='agent' || role==='agent_admin' || role==='agent_manager')"}` — 쿼리 스코프·status 조건 없이 노드 전체 읽기 허용. 보관 내용은 private-fields.ts:3 주석 `상업기밀(공급사 fee_rate)` + migrate-private.ts:69 `patch['v4/partners_private/'+code+'/fee_rate'] = fee`. 실제 소비는 단건뿐: settlement-engine.ts:26 `readPartnerPrivate(String(partner?.partner_code ?? contract.provider_company_code ?? ''))`.
- 재현: 영업자(또는 갓 가입한 pending 계정, 기본 role='agent' — auth.ts:195-196)로 `GET {DATABASE_URL}/v4/partners_private.json?auth={idToken}` → 전 공급사의 fee_rate가 파트너코드별로 반환됨.
- 기대: 공급사별 수수료율은 해당 공급사·관리자만. 영업자는 자기 계약에 동결된 스냅샷만 사용
- 실제: 경쟁 관계인 전 영업채널이 모든 공급사의 마진율을 한 번에 열람 가능
- 조치: partners_private 부모 .read에서 agent 계열 제거(관리자 전용). 영업자 정산 계산은 이미 계약 동결값(entities.ts:271 fee_rate_snapshot / :272 payout_rate_snapshot, deal.ts:171에서 저장)이 있으므로 settlement-engine.ts:26의 live 조회는 관리자/공급사 세션에서만 수행하도록 분기하고, 스냅샷 없는 레거시는 관리자 재계산 경로로 처리.

### [WRITE-1/SEC] 승인된 사용자면 누구나 남의 공급사 매물을 무제한 수정·소프트삭제 가능 — 소유자 검증 규칙 부재

- 플랫폼·화면: 공통(RTDB 규칙) / 매물(v4/products · products)
- 검증: 미검증
- 근거: database.rules.json:37 products `.write`=`auth != null && … && status !== 'pending'`, :174 v4/products 동일. 두 노드 모두 `$product` 자식 규칙·필드 화이트리스트가 없어 부모 .write가 전 하위에 그대로 적용됨(v4/products_private:181-184에는 소유자 조건이 있는 것과 대조). 클라는 영업자 write를 실제로 사용: rtdb-adapter.ts:521-528 주석 '영업자 락-write(계약금입금·취소·완료 → vehicle_status/locked_by_contract)'.
- 재현: 영업자 계정으로 `PATCH {DATABASE_URL}/v4/products/{남의매물코드}.json?auth={idToken}` 에 `{"price":{"36":{"rent":1}},"vehicle_status":"출고불가","_deleted":true}` 전송 → 성공. 병합 규칙(rtdb-adapter.ts:367-374)상 v4 오버레이가 v3 라이브를 덮으므로 전 사용자 화면에서 가격·상태가 바뀌고 매물이 목록에서 사라짐(list 필터 :414).
- 기대: 매물 본문 수정은 소유 공급사·관리자만, 영업자는 계약 진행에 필요한 상태 필드만
- 실제: 아무 승인 계정이나 전 매물의 가격·상태·삭제마커를 조작 가능(카탈로그 전체 무력화 가능)
- 조치: v4/products에 `$product` 규칙 신설 — `.write`: admin, 또는 provider이며 `data/newData.provider_company_code == 내 company_code`. 영업자에게는 하위 필드 화이트리스트만 허용(`vehicle_status`, `locked_by_contract`에 한해 `.write` 부여). v3 products 노드는 앱이 쓰지 않으므로 `.write: false`로 잠글 것.

### [MONEY-1/SEC] 정산 레코드 '생성' 시 금액·상태 검증이 없어 영업자가 자기 지급액을 임의 금액으로 만들어 넣을 수 있음

- 플랫폼·화면: 공통(RTDB 규칙) / 정산(v4/settlements · settlements_agent_private)
- 검증: 미검증
- 근거: database.rules.json:231 `$sid .write`에 agent 분기 존재(`role==='agent' && (newData.agent_code === user_code || === auth.uid)`), 금액 보호는 :233-237 `"fee_amount": {".validate": "!data.exists() || newData.val() === data.val() || role==='admin'"}` 형태 — `!data.exists()`(=신규 생성)면 무조건 통과. :251 settlements_agent_private도 `!data.exists()` 분기로 agent 생성 허용. 금액은 클라 계산: settlement-engine.ts:47-54 `const fee = Math.round(rent * feeRate); … agent_payout: payout, net_amount: fee - payout`.
- 재현: 영업자 계정으로 `PUT {DATABASE_URL}/v4/settlements/ST_{임의계약코드}.json?auth={idToken}` 에 필수 필드(contract_code·settlement_status·provider_company_code·agent_code=내코드·agent_channel_code=내채널)와 함께 `fee_amount`/`net_amount`를, 이어서 settlements_agent_private에 `agent_payout: 99999999`를 생성 → 규칙 통과. 관리자 정산 화면에는 조작된 금액이 정상 데이터로 표시됨.
- 기대: 정산 금액·상태는 서버(또는 관리자)만 확정
- 실제: 영업자가 자기 귀속 정산을 임의 금액·임의 계약코드로 생성 가능. 이후 수정만 admin으로 막혀 있어 '최초 1회 조작'이 그대로 확정치가 됨
- 조치: 정산 생성도 admin 전용으로 좁히고(현행 루트 settlements:63과 동일하게), 계약완료 시 정산 생성은 Cloud Function/관리자 배치로 이관. 즉시 조치가 어렵다면 `$sid` 생성 시 `newData.child('settlement_status').val() === '정산대기'` 강제 + `fee_amount`/`agent_payout`/`net_amount`는 `.validate: role==='admin'`(생성 포함)로 잠그고 계약 스냅샷에서 관리자가 채우는 흐름으로 전환.

### [PII-2/SEC] 계약 감사 로그에 before/after 원본 JSON이 통째로 저장돼 주민등록번호·면허번호·주소가 평문으로 영구 누적

- 플랫폼·화면: 공통 / 감사 로그(audit_logs)
- 검증: 미검증
- 근거: lib/domain/audit.ts:313-314 `before: before ? JSON.stringify(before).slice(0,1200) : '', after: after ? JSON.stringify(after).slice(0,1200) : ''` + :312 `changes`(fieldChanges, 값 원문 포함 :163). 호출: rtdb-adapter.ts:562 `this.writeAudit(entity, co, key, …, before, { ...(before||{}), ...p })`. 민감필드 유입 경로: sign.ts:206-210 `store.update('contract', co, code, { ...signSubmissionPatch(contract), sign_status:'서명완료' })` — signSubmissionPatch(sign.ts:44-58)에 customer_id·driver_license_no·customer_address 포함. 감사 노드에는 만료·마스킹 규칙 없음(database.rules.json:153-159).
- 재현: 손님 서명 제출 → 관리자/영업자가 '승인' 클릭 → audit_logs/AL-… 항목의 after 필드에 `"customer_id":"9001011234567"`, `"driver_license_no"`, `"customer_address"`가 그대로 기록됨(관리자 감사 화면 및 콘솔에서 확인 가능).
- 기대: 감사 로그는 '무엇이 바뀌었는지'만 남기고 민감정보 값은 남기지 않음
- 실제: 주민등록번호를 포함한 계약 PII가 별도 노드에 무기한 중복 보관 — 유출면이 하나 더 늘고 파기 요구 대응 불가
- 조치: audit.ts에 민감키 집합(customer_id·driver_license_no·customer_address·customer_phone·sign_signature 등)을 두고 ① fieldChanges에서 해당 키는 값 대신 '변경됨'으로 치환, ② before/after JSON 직렬화 전 민감키 제거. 기존 audit_logs의 해당 필드는 일회성 스크립트로 마스킹.

### [CONTRACT-1/SEC] erp3(v3) 레거시 계약은 v4 오버레이 첫 쓰기가 규칙 validate에 막혀 단계 진행·취소·서명 발송이 전부 permission_denied

- 플랫폼·화면: 공통(RTDB 규칙 ↔ 클라 패치) / 계약 진행(단계 체크·취소·서명 발송)
- 검증: 미검증
- 근거: database.rules.json:267 `".validate": "newData.hasChildren(['contract_code','contract_status','product_code','agent_uid','agent_channel_code','provider_company_code']) && (data.exists() || newData.child('contract_status').val() === '계약요청')"`. 클라 패치는 단일 필드: settlement-engine.ts:268 `await store.update('contract', co, code, { [key]: value })`. 승계 스탬프 목록에 contract_code·contract_status가 빠져 있음: rtdb-adapter.ts:517 `for (const f of ['agent_uid','agent_code','agent_channel_code','provider_company_code','provider_uid','product_code'] as const)`. v3 계약은 실제로 브리지됨: rtdb-adapter.ts:46 `BRIDGE_FROM_V3 = new Set([... 'contract' ...])`, :146-179 readContractsScoped('contracts').
- 재현: erp3에서 생성돼 v4/contracts에 아직 없는 계약을 연다 → 아무 단계 체크(예: 서류제출 yes) 클릭 → update가 v4/contracts/{code}에 {agent_docs_submitted, _key, updatedAt, +스탬프 6종}만 써서 newData에 contract_code·contract_status가 없음 → hasChildren 실패로 PERMISSION_DENIED. 취소(contract_status='계약취소')도 contract_code 부재로 동일 실패, createSignToken(sign.ts:125)의 sign_* 패치도 동일.
- 기대: 레거시 계약도 오픈 후 정상 진행·취소·서명 발송 가능
- 실제: v3 계약 전체가 읽기 전용으로 동결(관리자 포함). 규칙 미배포 상태에서 QA하면 드러나지 않고 배포 직후 전면 실패
- 조치: rtdb-adapter.ts:517 스탬프 목록에 `contract_code`, `contract_status`를 추가(before에서 승계)하고, 규칙 :267의 생성 조건을 `data.exists() || newData.child('contract_status').val() === '계약요청'` → `data.exists() || root.child('contracts').child($contract_id).exists() || newData.child('contract_status').val() === '계약요청'` 로 완화(레거시 승격 허용). 배포 전 규칙 시뮬레이터로 '레거시 계약 첫 단계 체크' 케이스를 반드시 통과 확인.

### [CACHE-1/DATA] 채팅 방목록·안읽음 뱃지·새 문의방이 상대방 행동으로는 세션 내 절대 갱신되지 않음(room/message/contract list 영구 캐시)

- 플랫폼·화면: 공통 / /chat 방목록 · 하단탭/햄버거 뱃지
- 검증: 확정
- 근거: store.ts:273-282(list 성공 결과 영구 캐시·무효화는 자기 쓰기뿐), app/chat/page.tsx:135 `refreshRooms`가 `store.list('room')`/`list('contract')` 재호출하지만 캐시 히트, menu-badges.ts:26 뱃지도 동일, messaging.ts:252 roomsWithUnread의 `list('message')`도 캐시. 무효화 트리거는 DispatchStore 자기 save/update뿐(store.ts:269·347-349) — 수신 이벤트 없음. 열린 스레드만 ChatThread.tsx:76 5초 폴 + listMessagesForRoom(store.ts:296-302, settle 후 캐시 해제)로 갱신됨
- 재현: 1) 공급사가 로그인해 /chat 1회 진입(캐시 생성) 후 앱 내 다른 화면 사용 2) 영업자가 새 문의방 생성 + 메시지 전송 3) 공급사 화면: focus/fp:unread 핸들러가 loadMenuBadges·refreshRooms를 돌려도 전부 캐시 반환 → 새 방·미리보기·안읽음 뱃지 전부 무변화. F5 전체 새로고침 전까지 신규 문의 인지 불가(SPA 라우팅은 모듈 캐시 유지)
- 기대: 방목록·뱃지 폴링/focus 갱신 시 서버 재조회(짧은 TTL 또는 강제 신선 조회)로 상대 발신·새 방 반영
- 실제: 세션 첫 list 시점 스냅샷에 동결 — 자기 markRead/전송으로 해당 방 한 줄만 patch됨
- 조치: ① room·message·contract 등 다중 사용자 가변 엔티티에 TTL(예: 15~30초) 부여 또는 ② refreshRooms/loadMenuBadges 경로에 캐시 무효화 후 재조회(export한 invalidate(entity) 호출) ③ 장기적으로 rooms에 onValue 구독. 최소 수정은 fp:unread·focus·5초 폴에서 `_invalidate('room'); _invalidate('message')` 후 list
- 검증 상세: 반박 실패 — 모든 전제가 코드로 확인됨. store.ts:271-283 list('room'/'contract'/'message')는 모듈 레벨 영구 캐시(TTL 없음)이고 무효화는 자기 클라이언트의 save/update/bulkPatch/remove/restore뿐(269·343-365). fp:unread는 messaging.ts:35-39 notifyUnread가 자기 창에만 쏘는 CustomEvent라 상대 행동으로는 발화 불가, rtdb-adapter는 전부 one-shot get()으로 onValue 등 실시간 구독 전무, FCM/서비스워커 푸시 없음. TopBar.tsx:191·AppTabBar.tsx:68·chat/page.tsx:216-221 focus/fp:unread 핸들러가 도는 refreshRooms(135)·loadMenuBadges(menu-badges.ts:26)·roomsWithUnread(messaging.ts:252)는 전부 캐시 히트. clearStoreCache는 인증 변경(auth.ts:46-47)·dev 페이지뿐이고 seedIfEmpty는 rtdb에서 no-op(seed.ts:96)이라 재마운트 무효화도 없음 → 재현 경로 도달 가능. 유일한 완화는 '열려 있는 방'만 listMessagesForRoom 일시 캐시(store.ts:284-305)+5초 폴(ChatThread.tsx:76)로 갱신되는 것 — 새 문의방·다른 방 미리보기·안읽음 뱃지는 세션 내 동결 확정. 심각도는 출시차단이 아닌 긴급으로 조정: 데이터 유실·유출 없음, 메시지는 F5·재로그인·해당 방 열람 시 복구되므로 '핵심 시나리오 불능'까지는 아니나, 문의 주도형 마켓에서 공급사가 세션 내 신규 문의를 인지할 수단이 전혀 없는 주요 기능 오동작.

### [RATE-1/UX] 수수료율(fee_rate·agent_payout_rate) 0~1 범위 검증이 전혀 없어 오입력이 정산 금액을 수십 배로 오계산

- 플랫폼·화면: 공통 / 회원·파트너 편집 폼 → 정산 엔진
- 검증: 확정
- 근거: app/members/page.tsx:171 검증은 `식별자는 필수입니다` 하나뿐. components/ui/form-grid.tsx:138 숫자필드는 `replace(/[^\d.]/g,'')`만(범위 무검증). lib/domain/settlement-engine.ts:30-31 `let feeRate = rawFee != null ? Number(rawFee) : 0.1;` 클램프 없음 → :47 `fee = Math.round(rent * feeRate)`
- 재현: 관리자가 파트너 수수료율에 15%를 의도하고 '15' 입력(라벨의 '(0~1)' 부가문구만 있음) → 저장 통과 → 해당 공급사 계약 완료 시 R1=월대여료×15(1500%)로 정산 생성. 계약 시점 스냅샷(fee_rate_snapshot)으로 동결되어 조용히 굳음
- 기대: 0~1 벗어나면 저장 차단 또는 '15% → 0.15로 저장할까요?' 확인. 최소한 1 초과 시 경고
- 실제: 임의 값 저장·정산 반영. 오류 안내 없음
- 조치: members save()에서 fee_rate·agent_payout_rate에 Number 범위검증(0≤x≤1) 추가, 1<x≤100이면 '%. /100 변환' 제안 confirm. settlement-engine.resolveRates에도 방어 클램프+console.error
- 검증 상세: 반박 실패 — 5개 레이어 전부 범위 검증 부재 확인. (1) form-grid.tsx:138 숫자필드는 replace(/[^\d.]/g,'')만, '15' 통과 (2) entities.ts:328·368 '(0~1)'은 라벨 문구일 뿐 제약 없음 (3) members/page.tsx:171 저장 검증은 식별자 필수 하나뿐, writePartnerPrivate(private-fields.ts:65-78)·adminUpdateUserIdentity(auth.ts:287-289 Number 캐스트만) 모두 무클램프 (4) database.rules.json:10-12·194-196·211·222-224 .validate는 작성자(admin) 권한만 검사, 값 범위 무검사 — 시나리오 행위자가 admin이라 전부 통과 (5) settlement-engine.ts:30-31 Number() 무클램프 → deal.ts:171 fee_rate_snapshot 동결(rules:277 이후 불변) → settlement-engine.ts:47 fee=rent×15. 재현 경로 코드로 완전 도달. 다만 출시차단은 과장: 관리자 오입력이 전제이고 정산화면에서 15배 금액이 가시적이며 admin이 정산 필드 수정 가능(rules:234-237)해 데이터 유실·복구불능 아님. 그러나 %↔소수 혼동은 고확률 실수이고 스냅샷 동결로 해당 공급사의 모든 후속 계약 정산(R1·R2·net)에 조용히 전파되므로 긴급(주요 기능 오동작) 유지.

### [SYNC-1/COMPAT] 상대방이 보낸 새 메시지·새 문의가 세션 중 방 목록·뱃지에 영영 반영되지 않음(list 캐시가 세션 영구 + 재조회 트리거 전무)

- 플랫폼·화면: 공통 / 문의(/chat) 방 목록 · TopBar/AppTabBar 안읽음 뱃지 · 계약/재고 목록
- 검증: 확정
- 근거: lib/store.ts:271-283 DispatchStore.list가 성공 결과를 세션 영구 캐시(무효화는 로컬 save/update뿐) · components/TopBar.tsx:189-193 focus 시 refreshBadges→loadMenuBadges→store.list(캐시 재사용) · app/chat/page.tsx:135 refreshRooms도 getStore().list('room')(캐시) · lib/domain/messaging.ts:35-39 fp:unread는 내 발신·열람 때만 dispatch. onValue 구독·주기 재조회·visibilitychange 재조회 0건(전 저장소 grep)
- 재현: 관리자/공급사가 /chat 목록을 열어둔 상태에서 영업자가 다른 기기로 새 문의·메시지 전송 → 몇 분을 기다려도, 탭을 나갔다 와도(focus 뱃지 갱신은 같은 캐시 재계산) 방 목록·안읽음 뱃지 무변. 새로고침해야 보임. 열린 스레드만 5초 폴링(listMessagesForRoom은 캐시 비영구)으로 갱신됨
- 기대: 복귀(focus/visibilitychange)·주기적으로 방·계약 목록이 서버에서 재조회되거나 RTDB onValue로 수신 반영
- 실제: 첫 list 결과가 세션 내내 SSOT — 타 사용자 변경(새 문의·수신 메시지·매물 수정)이 화면·뱃지에 안 뜸
- 조치: 최소: window 'focus'+document 'visibilitychange'에서 room/message/contract 캐시 무효화(_invalidate) 후 refreshRooms·refreshBadges 실행(예: store.ts에 invalidateFor(['room','message','contract']) export). 정공: room 노드만이라도 onValue 구독으로 전환해 _listResolved 갱신+fp:unread dispatch
- 검증 상세: 반박 실패 — 주장 전제 전부 코드로 확인. (1) lib/store.ts:271-283 list 캐시는 성공 시 영구(실패만 280에서 축출), 무효화는 자기 클라이언트의 save/update/remove뿐(269·343-349·356·359-364), clearStoreCache는 dev 페이지·auth 전환뿐. (2) TopBar.tsx:187-194·AppTabBar.tsx:59-70의 focus/fp:unread 갱신은 menu-badges.ts:26의 store.list('room'/'contract') 캐시 재사용 — 같은 stale 데이터 재계산. (3) fp:unread는 messaging.ts:97·159·182 내 발신·열람 때만 dispatch. (4) 전 저장소 grep에서 onValue/onChildAdded/visibilitychange 0건, setInterval은 ChatThread 5초 폴링(스레드 한정, store.ts:284-305 비영구 캐시)과 TopBar 시계뿐. FCM/푸시 없음. (5) chat/page.tsx:134-142 refreshRooms도 캐시된 list('room'). 유일한 부분 완화 = 사용자가 직접 메시지를 보내면 message 캐시가 무효화돼 안읽음 재스캔되지만, room 엔티티는 여전히 stale이라 새 문의방·last_message·정렬은 갱신 안 됨. 심각도는 출시차단→긴급으로 조정: 데이터 유실·보안 유출 아니고 새로고침으로 완전 복구되며 열린 스레드는 5초 폴링으로 수신되지만, 탭을 켜둔 관리자·공급사가 세션 내내 새 문의·메시지를 무신호로 놓치는 핵심 커뮤니케이션 기능의 무음 오동작.

---

## 중요 (25건)

| ID | 화면 | 내용 | 조치 |
|---|---|---|---|
| AUTH-8/AUTH | 인증 부팅(AuthProvider) / 승인대기 화면 | users 프로필 읽기 실패 시 status 없는 '최소 세션'을 발급해 승인대기 화면을 우회(fail-open) | catch 분기에서 setSession 대신 `setSession(null)` + 오류 상태 노출(AuthProvider에 '인증 정보를 불러오지 못했습니다 — 재시도/로그아웃' 화면). 부득이 최소 세션을 유지해야 하면 `status:'pending'` 을 명시해 보수적으로 잠글 것. |
| AUTH-9/AUTH | 회원·파트너(/members) 가입 승인 | requested_type이 없는 구가입자·'개인' 가입은 사업자번호 매칭 실패해도 경고 없이 agent/SP999로 승인됨 | 매칭 실패 시 requested_type과 무관하게 승인 확인 다이얼로그를 띄워 '개인영업(SP999)으로 승인 / 파트너 선택 / 취소' 를 고르게 하고, 승인 요약 토스트에 배정 결과(role·company_code)를 항상 표기. 사후 교정을 위해 approveUser의 opts.rematch 를 UI에 노출. |
| AUTH-10/AUTH | /login 계정 만들기 > 소속 사업자번호 | 사업자번호 실시간 매칭이 규칙상 항상 실패해 '임시소속(SP999)' 안내가 절대 표시되지 않음 | 사업자번호 매칭을 서버 라우트로 옮겨 `{matched:boolean, name?:string}` 만 반환(파트너 전체 노출 금지). 그 전까지는 실패 시 '확인할 수 없습니다 — 승인 시 관리자가 확인합니다' 같은 명시 문구를 표시해 무반응 상태를 없앨 것. |
| AUTH-11/AUTH | /login '로그인 없이 둘러보기' → 전 화면 / 설정 '데모 역할' | 프로덕션 둘러보기(guest) 진입점이 전 화면 빈 목록으로 동작하고, 설정에 데모 역할 전환이 노출됨 | 둘러보기 진입 시 LocalAdapter+seed로 강제 전환(getStore()에 guest 분기)하거나, 버튼을 '공개 카탈로그 보기'(/catalog, AUTH-3 선행 수정)로 교체. 데모 역할 칩은 개발 환경(process.env.NODE_ENV!=='production')에서만 렌더. |
| RATE-1/BIZ | 회원(파트너/사용자) 편집 → 정산 계산 | 수수료율(공급사 fee_rate·영업 payout_rate) 입력·해석에 범위(0~1)·숫자 검증이 전무 — '10' 오입력 시 10배 청구, 비숫자면 NaN으로 계약 생성·완료가 막힘 | members 저장 경로와 auth.ts 패치에 `const r=Number(v); if(!Number.isFinite(r)//r<0//r>1) throw` 검증 추가 + resolveRates/createSettlement에 동일 클램프(벗어나면 기본 0.1/0.04 + console.warn·감사로그). |
| TWIN-1/BIZ | 선점/완료 가드·카탈로그 | 트윈(같은 실물이 복수 product_code로 존재) 중복판매 방지 부재 확정 — 잠금·rival·완료중복 가드 전부 product_code 단위, vehicleIdentity는 표시 dedup에만 사용 | applyStepCheck 선점·완료 가드에서 계약 매물의 vehicleIdentity를 구해 listRaw('product')에서 동일 신원 매물 코드 집합을 만들고, 그 집합 전체로 rival/완료중복·락 상태를 검사. syncVehicleLock도 집합 전체에 스탬프. (병행: 트윈 병합 배치로 근원 제거.) |
| SIGN-1/BIZ | 계약취소 → 손님 /sign/[token] | 계약취소가 서명 공개링크를 해지하지 않음 — 취소된 계약에 손님이 최대 7일간 서명·주민번호 등 PII 제출 가능 | cancelContract에서 `if (fresh.sign_token) await writeContractSign(token, publicSignRevocationPatch(...))` 호출(실패는 best-effort 경고). applyStepCheck의 reject 취소 분기(:274-278)에도 동일 적용. |
| FIN-1/BIZ | 계약 진행 패널 needsFinalize 배너 | '완료 처리 재시도'(finalizeContractIfReady)에 이중판매·취소 가드가 없음 — applyStepCheck에만 있는 dup 가드가 우회됨 | finalizeContractIfReady 서두에 ① `if (fresh.contract_status==='계약취소') return false` ② 동일 매물(트윈 포함)의 타 계약완료 존재 시 throw — applyStepCheck의 dup 가드를 공용 함수로 추출해 공유. |
| CHAT-1/SEC | 채팅(레거시 v3 방) | v4/messages 읽기 규칙이 방 소유권을 v4/rooms에서만 판정 — v4로 승격되지 않은 v3 레거시 방의 새 메시지는 저장되지만 아무도 못 읽고 조용히 사라짐 | ① 규칙 :313 읽기 조건에 v3 폴백 추가 — 각 역할 분기를 `(root.child('v4').child('rooms').child(query.equalTo).child(X).val() === Y // root.child('rooms').child(query.equalTo).child(X).val() === Y)` 로 쓰기 규칙과 대 |
| INQ-1/SEC | 문의(inquiries — v3 레거시 노드) | 로그인만 하면 남의 문의를 덮어쓰거나 삭제 가능(방문자 이름·연락처 보유 노드) | `"$id": {".write": "(!data.exists() && newData.exists()) // (auth != null && newData.exists() && (root.child('users').child(auth.uid).child('role').val() === 'admin' // data.child('agent_cod |
| LEGACY-1/SEC | proposals / proposal_views (미사용 레거시 노드) | proposals 노드가 무인증 전체 읽기(.read: true)로 열려 있음 — 앱은 쓰지 않는 erp3 잔재 | 콘솔에서 노드 내용을 확인해 백업 후 삭제하고, 규칙에서 proposals·proposal_views 블록을 제거해 루트 `$other`(:169 .read/.write false) 캐치올에 걸리게 한다. |
| CACHE-2/DATA | /chat 차명 조인(listRaw 소비처) | raw:: 캐시가 저장·수정·삭제 후에도 무효화되지 않음 — _invalidate 접두사 불일치(오늘 추가분 확인 요청 건, 실제 버그) | store.ts:230 `_invalidate`에서 `k.startsWith(entityKey + '::') // k.startsWith('raw::' + entityKey + '::')` 두 접두사 모두 삭제(_listCache·_listResolved 동일 적용) |
| SYNC-1/DATA | 설정 → 시트 동기화(SheetSync/sync-all) | 시트 동기화 upsert·부재처리가 가공된 list(트윈 dedupe·제외 필터) 기준이라 숨은 트윈 매물의 상태 갱신이 영구 유실됨 | sheet-merge.ts:119·202의 `store.list('product')`를 `store.listRaw('product')`로 교체(DispatchStore.listRaw 존재, store.ts:311). CACHE-2 수정과 병행 필수(raw 캐시 무효화) |
| SYNC-2/DATA | 설정 → 시트 동기화 | 시트 soft-merge의 계약 락 보호(engineLocked)가 캐시된 기존 목록 기준이라 동기화 직전 걸린 계약중 락을 시트 상태로 덮을 수 있음 | commitSheetProducts·applyAbsentBlocked 진입 시 product 캐시 무효화 후 신선 조회(또는 RACE-1의 listFresh 사용). 추가 방어로 patch에 vehicle_status 포함 시 서버측 규칙/쓰기 직전 locked_by_contract 재확인 |
| ERR-1/UX | 홈(파인더)·문의·계약·정산·회원·정책·매물상세 전부 | 네트워크·권한 오류가 전 업무화면에서 '데이터 없음' 빈 상태로 위장되고 재시도 수단이 전무함 | RtdbAdapter.merged가 실패 시 빈 배열 대신 오류를 throw(또는 오류 플래그 동봉)하고, 각 목록 화면에 error 상태 추가: rows===null(로딩)/rows.length===0(빈)/loadError(오류+재시도 버튼→_invalidate 후 재list). 최소 공통 컴포넌트 <LoadError onRetry/> |
| STEP-1/UX | 문의·계약 — 계약 진행 패널(5단계) | 계약 단계 '출고 불가'·'부결' 원탭이 확인 대화 없이 즉시 계약취소로 전이되고 복구 불가 | setCheck에서 isReject 값과 '완료→해제' 전환에 한해 confirmDialog 삽입(문구: '이 응답은 계약을 취소합니다 / 입금확인을 해제하면 차량 선점이 풀립니다') |
| SETL-1/UX | 계약진행 및 정산 — 정산상태 패널 | 정산 '정산 확정'·'보류'·'환수 확정' 버튼이 확인 대화 없이 원클릭 전이되고 정산완료는 UI에서 되돌릴 수 없음 | setStatus 호출 전 confirmDialog(예: '₩N 정산을 확정할까요?'), 관리자 한정 '완료 해제(대기로)' 버튼 추가 또는 감사로그 기반 복구 안내 |
| EDIT-1/UX | 재고관리·회원·파트너·정책관리 편집 폼 | 재고·회원·정책 편집 중 다른 행 클릭·목록 복귀 시 수정 내용이 확인 없이 소실(탭 전환만 보호) | 각 페이지 select/clearSel(및 WorkPage onBack)에 dirty 가드 공통화 — members switchTab의 confirmDialog 패턴을 selectP·select·clearSel에 재사용 |
| SEO-1/OPS | /q/{code} · /sign/{token} · /catalog 공유  | 손님 공유 링크의 카톡 미리보기가 플랫폼 브랜드(freepasserp.com)로 노출 — 화이트라벨 title은 크롤러에 안 먹는 클라 전용이고 OG 태그 전무 | /q/[code]·/sign/[token]에 서버 generateMetadata 추가(LAUNCH-1의 공개 스냅샷/Admin SDK로 차량명·대표사진 조회) — title '{차량명} · 렌터카 견적', openGraph.images=대표사진, 플랫폼명 제거. /catalog·/sign도 최소 정적 title 오버라이드 |
| NET-1/COMPAT | 홈 파인더 · 문의 · 계약 · 재고 · 정산 등 list 사용 전 화면 | 일시 오프라인·읽기 실패가 '빈 목록 0건'으로 둔갑하고 그 빈 결과가 세션 영구 캐시에 고착(재연결해도 복구 안 됨) | rtdb-adapter merged()에서 양쪽(live·over) 모두 실패 시 throw로 승격(부분 성공만 병합 반환). store.ts는 기존 p.catch 경로가 캐시를 지우므로 그대로 동작. 화면단은 rows=null(오류)와 [](진짜 0건) 구분 + window 'online'에서 _invalidate 후 재조회 |
| VIEW-1/COMPAT | 채팅 컴포저 · 검색/필터 시트 입력 · 하단독 있는 전 화면 | interactive-widget=resizes-content는 iOS Safari(=아이폰 전 브라우저, WebKit 강제)가 무시 — '입력엔진 통일'이 안드로이드 크롬 계열에서만 성립, visualViewport 폴백 부재 | 폴백 필요 판정: 필요함. 설계 — lib/keyboard-inset.ts 신설: visualViewport 'resize'+'scroll' 리스너에서 inset=Math.max(0, window.innerHeight - vv.height - vv.offsetTop)를 rAF 스로틀로 document.documentElement에 --fp |
| BACK-1/COMPAT | 문의/계약/재고 상세(WorkPage selected) · 채팅 첨부 뷰 | 모바일 오버레이 상세(업무 화면 선택·첨부 뷰어·모달·시트·라이트박스)가 history 미통합 — 뒤로가기 시 닫히지 않고 페이지 자체를 이탈 | 공용 훅 useHistoryDismiss(open,onClose): open 전환 시 history.pushState({fp:1},'') 1회, popstate에서 onClose(), onClose 버튼 경로는 history.back()으로 수렴. overlays.tsx(Drawer/Modal)·BottomSheet·WorkPage sel |
| IMG-1/COMPAT | 채팅 첨부(📎) · 재고 매물 사진 업로드 | 클라이언트 리사이즈 없이 3MB 하드컷 — 최신 폰 카메라 사진(3~8MB, iOS HEIC→JPEG 변환 포함)이 채팅 첨부·매물 사진 업로드에서 상시 거부됨 | 업로드 전 클라 다운스케일 유틸(장변 1920px·JPEG q0.8, createImageBitmap+canvas.toBlob) 추가 후 messaging.sendFile·PhotoUpload.add에서 image/*에 적용, 3MB 검사는 리사이즈 결과에 수행. HEIC은 iOS가 picker에서 JPEG 변환하므로 별도 디코더 불요 |
| NET-2/COMPAT | 채팅 스레드 | 열린 채팅방에서 오프라인 되면 5초 내 대화 내용 전체가 사라지고 '첫 메시지를 남겨보세요'로 표시 | listMessagesForRoom에서 두 read 모두 실패면 throw(한쪽만 실패면 성공분 병합). ChatThread refresh의 기존 catch(console.warn)가 이전 msgs를 자연 보존. 추가로 setMsgs 전 '실패 시 이전 상태 유지' 가드 |
| IMG-2/COMPAT | 홈 파인더 카드 · 매물 상세 · 채팅 앨범 | 썸네일·목록·앨범이 축소본 없이 Storage/외부 원본(장당 최대 3MB)을 그대로 로드 — 상세 썸네일 스트립은 전 장 eager | IMG-1의 클라 리사이즈와 묶어 업로드 시 썸네일 한 벌(_thumb 320px) 병행 저장 후 목록·앨범·스트립은 thumb 참조(레거시 URL은 원본 폴백). 대안: Firebase Resize Images 확장 or /api/img에 w= 파라미터+sharp 리사이즈 추가(firebasestorage도 프록시 허용). 최소 조치로  |

---

## 일반 (25건)

| ID | 화면 | 내용 | 조치 |
|---|---|---|---|
| AUTH-12/AUTH | 로그아웃 후 재접속 / 설정 > 상품 환경설정 | 로그아웃해도 최근 본·찜 매물(차량번호·금액), 귀속코드, 데모 역할이 localStorage에 남아 다음 사용자에게 노출 | logout()에서 세션·게스트 외에 fp4_recent_products·fp4_fav_products·fp4_pass*·fp4_hidden*·fp4_attr·fp4_role·fp4_finder_* 를 일괄 제거하는 clearUserLocalState() 추가(설정의 doLogout·idle-logout·PendingApproval 로그아 |
| STATE-1/BIZ | 계약 스텝 엔진 | applyStepCheck에 계약상태 가드가 없어 취소·완료 계약에도 스텝 write 가능 — 취소 계약이 5/5 재체크로 계약완료 부활 가능(현 UI는 취소 계약을 패널에서 숨겨 노출 경로가 좁음) | applyStepCheck 서두(:236 이전)에 `const cur = await store.get('contract',co,code); if (String(cur?.contract_status)==='계약취소') throw new Error('취소된 계약은 진행할 수 없습니다')` 추가. |
| CANCEL-1/BIZ | 계약취소 → 정산 상태 | 지급된 적 없는 정산(정산대기·정산보류)도 계약취소 시 '환수대기'(환수액 0)로 전이 — 실지급 없는 건이 환수 플로우에 들어가 관리자가 0원 '환수 확정'을 처리해야 함 | onContractCancel에서 `if (st.settlement_status==='정산완료') 환수대기+clawback; else settlement_status:'정산보류'(또는 신규 '정산취소' 상태)`로 분기하고 상태 옵션·배지에 반영. |
| DOC-1/BIZ | 계약서 발송(빌드 payload) | 계약서 payload가 동결 스냅샷(rent_amount)과 매물 현재가(vehicle_price=기간별 가격표)를 혼용 — 계약 생성 후 가격 수정 시 한 문서 안에서 금액 불일치 | 계약 생성 시 priceList 전체를 계약 스냅샷(price_snapshot JSON)으로 저장하고 buildContractPayload의 vehicle_price는 그 스냅샷으로 렌더. |
| TIME-1/BIZ | /settlement 월 선택 | 정산 월 목록의 '현재 월' 산출이 UTC 기준 — KST 매월 1일 00:00~09:00엔 새 달 옵션이 안 생기고 빈 DB 기본값이 전월로 잡힘(계약일 채번은 로컬 KST라 기준 불일치) | `const d=new Date(); const ym=`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}`` 로 교체(93·132행, excel 파일명 등 toISOString 날짜 사용처 동일). |
| STOR-2/SEC | Firebase Storage 레거시 경로 | 레거시 경로(product-images·contract-files·chat-files)가 20MB·MIME 무제한이라 v4 경로의 3MB·image/* 강제를 그대로 우회 | erp3가 여전히 쓰는 경로만 남기고 나머지는 `allow create,update: if false`로 잠그기. 유지가 필요한 경로에도 v4와 동일한 kind별 용량·contentType 조건을 복제하고, product-images의 `read: if true`는 유지하되 create에 `request.resource.contentTyp |
| SYNC-3/DATA | 시트 동기화 전 과정 | 동기화 중간 실패 시 부분 반영 상태로 중단(롤백 없음) + save 예외 시 캐시 무효화 누락으로 화면과 서버 불일치 | DispatchStore.save/bulkPatch를 try/finally로 감싸 실패 시에도 _invalidate 실행. commitFetchedPartnerSheets는 공급사 라인별 try/catch로 계속 진행 후 실패 라인 요약(현재는 전체 abort) |
| MSG-1/DATA | 채팅 안읽음 뱃지 | 방 안읽음 카운터가 read-modify-write(트랜잭션 없음)라 동시 전송 시 증분 유실·last_message 역전 가능 | unread_for_* 는 firebase/database `increment(1)` 값으로 patch(스토어에 증분 표현 추가) 또는 last_message_at 비교 후 큰 값만 기록. roomsWithUnread(정밀 재계산) 경로가 있으므로 카운터는 increment 전환만으로 충분 |
| RESTORE-1/DATA | 감사(audit) → 휴지통 복구 | 휴지통 복구 시 소유필드 승계 스탬프가 항상 불발(before=null) — v3 레거시 room·contract 복구가 소유자 스코프 조회에서 계속 안 보임 | update()의 before 조회를 톰스톤 포함 버전으로(merged find에서 삭제 필터 제거한 내부 getAny) 바꿔 516-518 승계가 복구에도 작동하게 하고, restore 패치에 deletedReason:null 추가 |
| TIP-1/UX | 홈(상품찾기) 최초 로그인 | 모바일 첫 로그인 시 존재하지 않는 '웹 보기 전환(우측 상단)' 안내 다이얼로그가 뜸 | effect에 `if (mobile) return;` 추가(또는 isMobileViewport() 판정) |
| TERM-1/UX | 재고 폼·필터, 홈 엑셀뷰, 감사로그, 엑셀 다운로드, 저장 토스트 | 동일 필드 vehicle_status의 라벨이 화면·산출물마다 4종(상품상태/상태/출고상태/차량상태)으로 다름 | 라벨 SSOT 상수(예: FIELD_LABEL.vehicle_status) 하나로 entities·audit·excel-export·토스트 문구 참조 통일 |
| TERM-2/UX | 탭바·문의 목록·계약 패널·매물상세 | 문의 개념어 혼용(계약문의/출고문의/출고 문의/문의/상담/채팅) + 매물·상품 혼용, 모바일은 선택지 라벨까지 축약돼 웹과 다름 | 용어 사전 표 작성 후 NAV_LABEL·STEPS.label·entities.label·버튼 문구를 한 상수로 연결. 모바일 축약은 '가능/협의/불가' 대신 폭 확보(이미 짧음) 또는 동일 라벨 유지 |
| TERM-3/UX | 계약진행 및 정산 — 목록 필터 | 계약 목록 기본 필터 '진행'이 실제로는 계약완료를 포함해 상단 KPI '계약진행중' 정의와 어긋남 | 칩 라벨을 '진행·완료'로 바꾸거나 matchesFilter('진행')를 isContractInProgress와 일치시키고 '완료' 칩 기본 노출. 기본필터 상태에서도 0건이면 '취소 N건은 필터에 숨겨져 있습니다' 안내 |
| TONE-1/UX | 전역 토스트 | 토스트 문구 톤이 합쇼체·음슴체(~됨)·해요체 3종 혼재, 타입 미지정 토스트 1건 | 토스트 문구 스타일 가이드 1줄 결정 후 grep 일괄 정리(toast( 호출 약 120곳), SheetSync.tsx:56에 'ok' 지정 |
| EXCEL-1/UX | 홈 — 엑셀 보기 | 홈 엑셀뷰에서 헤더 필터 조합이 0건이면 빈 표(헤더만)로 남고 안내·해제 수단이 없음 | FinderResults 게이트를 `(view==='excel' ? excelRows : list).length === 0`으로 바꾸고 onReset은 colFilter 포함(이미 reset이 colFilter 초기화함) |
| INVVAL-1/UX | 재고관리 등록·편집 | 재고 등록 검증이 상품코드(자동생성)·공급사뿐 — 차명·차번·대여료 없이 저장되면 빈 깡통 상품이 전 영업자 목록에 노출 | save()에 차명/차번/price 최소검증 추가, 미완성은 vehicle_status 기본값을 목록 비노출 상태로 두거나 파인더에서 필수결측 상품 제외 |
| TRASH-1/UX | 감사·휴지통 — 휴지통 탭 | 감사·휴지통 '복구' 실패 시 아무 피드백 없음(미처리 promise) | restore에 try/catch + toast, 성공 시에도 '복구되었습니다' ok 토스트(다른 화면의 삭제/저장 피드백과 규격 통일) |
| ENV-1/OPS | 전 화면(데이터 백엔드) | NEXT_PUBLIC_DATA_BACKEND 누락 시 프로덕션이 조용히 seed 데모 데이터(LocalAdapter)로 부팅 — env 시작검증 대상에서 빠져 있음 | client.ts 시작검증 블록에 `process.env.NEXT_PUBLIC_DATA_BACKEND==='rtdb'` 확인 추가(프로덕션 SSR throw, 로컬은 경고 유지) |
| OBS-1/OPS | /q · /sign · /catalog · /login 오류 수집 | 비로그인(손님 공개면·로그인 화면) 오류는 v4/_client_errors에 기록 불가 — 운영자가 손님 쪽 장애를 인지할 수단이 0 | _client_errors $eid write를 `!data.exists()`만으로 완화(이미 msg≤600·stack≤2000·hasChildren 검증 있음) + ts 검증(newData.child('ts').val() <= now+5000 유사) 추가로 스팸 억제. 클라 CAP 20은 유지 |
| GONOGO-1/OPS | LAUNCH_GONOGO.md 런북 | LAUNCH_GONOGO.md 내부 모순 — §1 표는 B1~B4 ✅·Go인데 §4/§5/§6 체크박스 전부 미체크·각주는 'B1·B4·B5 대기'·배포 SHA 스테일, 운영자가 런북만으로 현재 상태 판정 불가 | §4·§5 체크박스를 실제 수행분으로 갱신하고 B4 리허설 기록(백업 파일명·주입 경로·확인 건수·삭제 시각) 1단락 추가, :102 각주 삭제, :19 SHA를 '최신 main' 문구로 변경 |
| ROLLBACK-1/OPS | Vercel 롤백 절차 | 코드 롤백에 '하한'이 존재 — 하드닝 규칙 게시 후 스코프 조회 도입 이전 커밋으로 되돌리면 비관리자 목록이 전면 PERMISSION_DENIED, 런북 §3에 미기재 | 런북 §3에 '롤백 하한 = RLS 스코프 조회 도입 배포(그 이전 금지)' 1줄과 해당 Vercel 배포 URL/커밋 기록 |
| BACKUP-1/OPS | RTDB 백업·복구 | 백업이 수동 1회 실행분뿐 — 정기(일 1회) export 자동화·스크립트가 저장소에 없어 런북 자체 RPO(24h) 충족을 보증할 수 없음 | scripts/backup-rtdb.mts(firebase database:get / → 날짜 파일명 저장, 26MB 규모) 추가 + Windows 작업 스케줄러/GitHub Actions cron 등록, 실행 결과를 런북에 기록하는 절차 1줄 |
| CSS-1/COMPAT | 전 화면(.fp-shell) · WorkPage · MobilePageS | 100dvh 폴백(100vh 선행 선언) 부재 — iOS 15.3 이하에서 앱 셸 높이가 소실돼 전 화면 스크롤 불능(효과적 지원 하한 = iOS 15.4) | 각 dvh 선언 앞에 vh 한 줄 선행(예: height:100vh; height:100dvh; — 인라인 스타일은 CSS 클래스로 이관 필요한 3곳 포함). README/LAUNCH_GONOGO에 '지원 하한 iOS 15.4·Android Chrome 108' 명문화 |
| NET-3/COMPAT | 채팅 컴포저 | 오프라인 채팅 전송이 무한 '전송 중' — RTDB 쓰기가 오프라인에선 거부되지 않고 pending이라 busy 고착·안내 없음 | sendText/sendFile 호출을 8초 Promise.race 타임아웃으로 감싸고 navigator.onLine===false면 선차단 토스트. 타임아웃 시 기존 실패 경로(옵티미스틱 제거+텍스트 복원) 그대로 태움 |
| PWA-1/COMPAT | PWA 홈화면 추가 | 홈화면 아이콘이 SVG 단일 — iOS는 apple-touch-icon SVG 미지원이라 홈화면 추가 시 스크린샷 기본 아이콘으로 대체 | public/에 apple-touch-icon.png(180×180)·icon-192.png·icon-512.png 생성, layout metadata icons.apple 교체 + manifest icons에 PNG 두 벌 추가(maskable은 512 PNG) |

---

## 개선 (13건)

| ID | 화면 | 내용 | 조치 |
|---|---|---|---|
| AUTH-13/AUTH | 설정 > 계정 / 회원 관리 | 회원 탈퇴(계정 삭제·개인정보 파기) 경로가 없음 | 설정에 '회원 탈퇴'(확인 다이얼로그 → status='withdrawn' 표시 + users/{uid}의 name·phone·business_no 마스킹 + Auth 계정 disable 요청 큐)와, /members에 관리자용 '탈퇴 처리' 액션을 추가. 계약·정산 귀속은 user_code만 남기고 PII는 users_private로 이 |
| AUTH-14/AUTH | v4/quote(견적서 노드) | 견적서 노드가 손님 이름·연락처를 담는 스키마인데 pending이 아닌 모든 인증 사용자에게 전량 read/write로 열려 있음 | contracts(:263-266)와 동일한 패턴으로 교체: read는 `query.orderByChild==='agent_code'` + 본인 user_code/uid, provider/채널 관리자 분기; write는 `$qid` 레벨에서 `newData.child('agent_code').val()` 소유 검증. 기능 미사용 상태인 지 |
| SET-1/BIZ | /settlement 월 집계 | 월 집계 카드의 R1/R2 합계는 환수·보류·취소 정산까지 포함하는데 '확정 순수익'만 정산완료 한정 — 카드 간 기준 불일치로 오독 여지 | 카드 라벨에 '(전체)'/'(확정)' 명시 또는 R1/R2도 정산완료 기준 + '전체' 토글 제공. |
| SIGN-2/BIZ | 계약서 서명(ContractSign) | 계약서 발송 부분 실패 시(공개 슬롯 저장 실패) contract는 이미 '발송' 상태로 남아 죽은 링크의 '링크 복사' 버튼이 노출됨 | createSignToken에서 writeContractSign 성공 후 contract update(순서 교환), 또는 실패 catch에서 sign_status 원복. |
| FMT-1/BIZ | 파인더 카드·엑셀뷰 등 man() 사용처 | 카드·목록의 만원 표기 man()이 반올림이라 상세(원 단위) 대비 최대 5천원 과대 표시 가능 | man()을 Math.floor로 변경(또는 천원 단위 유지 '45.5만'). |
| TOKEN-1/SEC | /sign 링크 토큰 발급 | CSPRNG 미지원 환경에서 Math.random 기반 서명 토큰으로 폴백 — 추측 가능 토큰이 PII 공개 슬롯의 유일한 자물쇠 | 폴백을 제거하고 `if (!globalThis.crypto?.getRandomValues) throw new Error('이 브라우저에서는 서명 링크를 발급할 수 없습니다')`로 차단(발송 실패가 예측가능 토큰보다 안전). |
| PERF-1/DATA | 채팅 전송 | 메시지 1건 저장마다 save dedup이 전 방·전 메시지(v3∪v4) 재조회 — 비용 과다·경합창 확대 | RtdbAdapter.save에서 entity==='message'일 때 dedup을 keyed 존재확인(v4/messages/{key} get 1회) 또는 생략으로 분기 |
| HINT-1/UX | 월별정산 목록 툴바 | 월별정산 '적용' 힌트줄에 월이 상시 표시되고 '해제'를 눌러도 월은 지워지지 않아 필터 잔존처럼 보임 | hints에서 month 제거(월은 listHeader 셀렉터가 이미 표시) 또는 별도 비해제 배지로 분리 |
| SIGN-1/UX | 계약 진행 — 전자서명 카드 | 전자서명 링크 '해지' 무확인 원클릭 + '반려' 사유 입력이 원시 window.prompt로 앱 다이얼로그 규격과 불일치 | revoke에 confirmDialog 추가, reject 사유는 BottomSheet/Input 기반 소형 다이얼로그로 교체 |
| PWA-1/OPS | PWA/홈화면 아이콘 | 앱 아이콘이 SVG 단일 — iOS는 apple-touch-icon SVG 미지원이라 홈화면 추가 시 스크린샷 아이콘으로 대체됨 | 180×180 apple-touch-icon.png + 192/512 PNG(manifest maskable) 추가 |
| ENV-2/OPS | /api/extract-photos (매물 사진 풀기) | .env.example에 DRIVE_API_KEY 미기재(Vercel에도 미설정) — 사진 추출의 Drive API 병행 경로가 상시 비활성 | .env.example에 `DRIVE_API_KEY=`(선택, Drive v3 files.list 공개폴더 조회용) 주석 추가 + Vercel 설정 여부 결정 |
| SEC-1/OPS | 전 화면(응답 헤더) | 보안 응답 헤더 부재 — next.config headers()에 Accept-CH만 있고 X-Frame-Options/CSP frame-ancestors가 없어 ERP 화면 iframe 임베드 가능 | headers()에 `X-Frame-Options: DENY`(단, /welrix·/sonogong 임베드 용도면 해당 경로만 SAMEORIGIN/제외)와 `Referrer-Policy: strict-origin-when-cross-origin` 추가 |
| IMG-3/COMPAT | 파인더 카드 · 매물 상세 · 채팅 | 이미지 로드 실패(onError) 대체 UI가 전무 — 깨진 URL·프록시 502 시 브라우저 깨진 이미지 아이콘 노출 | 공용 <SafeImg>(onError 시 숨김+CarGlyph 폴백) 도입 — CardThumb은 photo 상태를 error 시 ''로 리셋하면 기존 CarGlyph 분기 재사용 |

---

## 완결성 비판(사각지대·재배치)

QA 완결성 비판 결과. [실증] 표시는 이 세션에서 직접 코드를 열어 확인한 항목.

## 1) 7축 사각지대

**GAP-1 [실증] 공개면 수정 설계의 구조적 충돌 — PUB-1을 고치면 AUTH-4가 전 세계로 확대됨.**
`D:\dev\freepasserp4\app\q\[code]\page.tsx:31` — 손님 견적 페이지가 귀속코드(`?a=`) 해석을 위해 `getStore().list('user', co)` 즉 **전체 회원 목록을 손님 브라우저로 다운로드**함. LAUNCH-1/PUB-1의 자연스러운 수정("products·users 공개 read 허용")을 그대로 적용하면 비로그인 전 인터넷에 전 회원 PII가 열림. 감사 어디에도 "공개면 수정안이 다른 출시차단(AUTH-4)과 충돌한다"는 교차 분석이 없음. 수정 전 귀속 해석을 서버 라우트 또는 단건 조회 구조로 재설계해야 함. 확인법: PUB-1 수정 PR에서 /q가 요구하는 read 스코프를 노드별로 나열.

**GAP-2 [실증] 규칙 잔재 노드의 '실데이터 내용' 미확인 — 감사가 규칙 텍스트만 보고 DB를 안 봄.**
`database.rules.json:119` `"proposals": { ".read": true, ... }` 확인. v4 코드 전체에서 `proposal` 참조 0건(grep 확인) — 즉 감사가 '중요'로 매긴 근거는 "앱이 안 쓴다"뿐이고, **공유 프로덕션 DB(erp3 브리지)에 실제로 무엇이 들어있는지 아무도 안 봄**. erp3 견적 노드에 고객 이름·연락처가 있으면 이것은 무인증 PII 공개 = 출시차단. 덤으로 `:121` `proposal_views` 는 `auth != null || !data.exists()` 로 **무인증 신규 쓰기**가 열려 있음(어느 축에도 미보고). 확인법: 콘솔에서 `proposals` 상위 5건 export 1회.

**GAP-3 다운로드 산출물 축 부재 (xlsx·인쇄).**
`D:\dev\freepasserp4\lib\excel-export.ts` — 정산 xlsx는 계약자 실명+차량번호+금액을 평문 파일로 남김(:127), 상품 xlsx는 vin·차량가격 컬럼 보유(:53,:94). 역할별 방어는 확인됨(클라 strip이 export 전에 적용, `lib/firebase/rtdb-adapter.ts:419,448` + 정산 컬럼 role 분기 `excel-export.ts:130-132`) — 그러나 "다운로드된 파일 자체"의 PII 유통(공유 PC·카톡 전달)은 7축 어디도 안 다룸. 인쇄도 마찬가지: `public/contract-template/*.html`에 `@media print` 존재 확인했으나, 서명 완료 후 **손님이 본인 계약서 사본을 받을 경로**(다운로드/인쇄/재열람)가 있는지 검증 항목 자체가 없음. 확인법: 서명 완료 화면에서 사본 획득 시나리오 1회 주행.

**GAP-4 접근성 축 전무.** 알려진 목록에 ListRow role 등 파편만 있고, 모달 포커스 트랩·ESC 닫기·토스트 aria-live·키보드 단독 조작(계약 스텝 체크·정산 확정)이 어느 축에도 없음. B2B라도 계약·정산 같은 돈 버튼이 키보드로 도달 불가면 체감 결함. 확인법: contract 페이지 Tab 순회 1회.

**GAP-5 기기 시계·시간대 의존이 정산 월(TIME-1) 외 전반 미검증.** 계약코드 채번 "로컬 KST"는 실제로는 **사용자 기기 시간대**임 — 해외 체류·시계 오설정 기기에서 채번 날짜와 last_message 정렬(클라 `Date.now()`)이 어긋남. 확인법: `deal.ts` 채번과 `messaging.ts` ts 산출이 서버시간(`ServerValue.TIMESTAMP`)인지 grep.

**GAP-6 무제한 가입 = PII 크롤링 증폭기.** AUTH-4(인증만 하면 전 회원 PII)와 공개 회원가입이 결합하는데, App Check·이메일 인증·가입 레이트리밋 검증이 어느 축에도 없음. AUTH-4 수정 전까지는 봇 가입 1회 = 전 회원 DB 1회 덤프. 확인법: 가입 직후 이메일 미인증 상태로 users read 시도.

## 2) 수상한 checked_ok

**SUS-1 [실증] "임시번호 100신XXXX 충돌공간 36^4 위험 미미" — 위험 모델 자체가 틀림.**
`D:\dev\freepasserp4\lib\domain\sheet-import.ts:239-241` — 해시 입력이 `providerCode + maker+model+sub_model+trim_name+year`뿐이고 **색상·행 인덱스가 없음**. 같은 공급사의 동일 트림·연식 신차 2행(색상만 다름)은 랜덤 충돌이 아니라 **결정적으로 같은 임시번호**가 되어 차번 dedup에서 1대로 합쳐짐 — 시트 재고 1대 조용히 유실. 36^4는 다른 신원끼리의 충돌 얘기고, 실제 위험은 같은 신원 판정의 과소 식별. 확인법: 동스펙 2색 신차 2행 dry-run.

**SUS-2 "보증금≥5천만&＞월세×50→/10000 휴리스틱은 경계 주의 필요" — checked_ok에 있을 문장이 아님.** 스스로 실데이터 경계 사례를 인정하면서 정상 처리함. 수입 고가차 보증금 5천만원은 실존 가능하고, 발동 시 보증금이 1/10000로 붕괴된 채 손님 견적에 노출됨. finding(일반)으로 승격하고 발동 시 로그·관리자 알림을 요구해야 함.

**SUS-3 [실증·부분] "B3 관측: v4/_client_errors 수집" — 권한거부 회귀는 이 관측망에 사실상 안 잡힘.**
`lib/auth-context.tsx:56-63`이 permission_denied unhandledrejection을 `preventDefault`로 삼키고(`console.warn`만), 더 근본적으로는 목록 read 실패가 try/catch로 **빈 배열로 흡수**되어(NET-1·ERR-1) 애초에 rejection 이벤트가 발생하지 않음. ClientErrorReporter(`components/ClientErrorReporter.tsx:39-45`)는 리스너로서는 살아있으나, 규칙 하드닝 후 스코프 누락 회귀가 생겨도 `_client_errors`에 안 쌓일 개연성이 큼. "관측 있음 = 회귀 조기경보 있음"으로 읽으면 안 됨. 확인법: 규칙에서 임시로 한 노드 read 거부 후 `_client_errors` 적재 여부 실측.

**SUS-4 "승인 취소 시 데이터 접근 즉시 차단(세션 UI는 다음 로드)" — '다음 로드'의 근거 미제시.** `fp4_session` 캐시가 옛 status=active를 담고 있고 AuthProvider의 pending 게이트는 세션 객체 status만 봄(`lib/auth-context.tsx:89`). users 프로필 재조회가 실패하면 AUTH-8의 최소 세션(fail-open)으로 떨어지므로, 승인 취소된 계정이 **다음 로드에도** PendingApproval을 안 볼 수 있음. AUTH-8과 이 checked_ok는 논리적으로 충돌 — 하나는 틀림.

**SUS-5 (긍정 실증) "seedIfEmpty rtdb no-op"·"ContractPanel 취소계약 미바인딩"·"클라 원가 strip" — 셋 다 사실 확인됨.** `lib/seed.ts:96`(`!store.backend.startsWith('local')` 조기 return), `components/ContractPanel.tsx:40-41`(계약취소 제외), `lib/firebase/rtdb-products.ts:42-53`(vehicle_price·vin·fee 분리). 커버리지 증명.

## 3) 심각도 재배치

**SEV-1 [실증] STOR-1 긴급 → 출시차단.** `storage.rules:37-40` 확인: `contract-signed/{token}`이 `read: if true` + `create, update: if request.resource.size < 20MB` — **무인증 update(덮어쓰기) 허용, MIME 제한 없음, 버저닝 없음**. 서명 완료된 계약서 = 법적 증빙인데 토큰만 알면 누구나 원본을 영구 파괴·위조 가능. 루브릭의 '데이터 유실·보안 유출' 정의에 정확히 해당. PII-1과 같은 토큰이 유일한 자물쇠인데 그 토큰은 손님 카톡으로 유통되는 값임.

**SEV-2 RATE-1 축간 모순 — BIZ '중요' vs UX '긴급', 같은 결함.** 수수료율 무검증으로 정산이 10배 오계산되는 건 돈 사고라 최소 긴급으로 통일해야 함. 두 축이 같은 결함을 다른 등급으로 매긴 것 자체가 감사 정합성 결함.

**SEV-3 [실증] AUTH-14 개선 → 최소 중요(내용 확인 시 긴급).** `database.rules.json:326-329` 확인: `quotes`가 pending 아닌 전 인증 사용자 read/**write**. 손님 이름·연락처 스키마 노드가 전량 열림 — 루브릭 'PII 위험=긴급'을 적용하지 않고 '개선'으로 매긴 근거가 "아직 앱이 안 씀"뿐인데, 같은 논리의 LEGACY-1은 '중요'를 받음. 등급 기준이 노드마다 다름.

**SEV-4 LEGACY-1 중요 → 실데이터 확인 전 판정 보류(상향 가능성).** GAP-2와 동일 — 무인증 read 노드의 내용을 안 본 채 '중요' 확정은 불가. 고객 PII 존재 시 출시차단.

**SEV-5 SIGN-1(BIZ) 중요 → 긴급.** 취소된 계약의 살아있는 서명 링크로 주민등록번호가 **무인증 공개 슬롯**(PII-1과 같은 노드)에 7일간 계속 적재됨. 수집 근거(계약)가 소멸한 뒤의 주민번호 수집은 법적 리스크까지 겹침 — 루브릭 'PII 위험=긴급' 직접 해당.

**SEV-6 (소폭) STATE-1 일반 유지 가능하나 조건부.** UI 노출이 좁다는 근거는 실증했으나(ContractPanel.tsx:40-41), 차단이 클라 UI뿐이고 규칙 레벨 가드가 없으므로 AUTH-1(임의 write) 수정 전까지는 사실상 AUTH-1의 하위 경로임을 명기해야 함.
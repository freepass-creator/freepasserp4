'use client';
/** **접수 워크스테이션 — 미리보기 문.** 로그인 없이 «얼굴만». 값은 전부 지어낸 것이다. */
import { IntakeStationPreview } from '@/app/settlement/board/preview/page';
export default function Page() { return <IntakeStationPreview />; }

'use client';
/**
 * 손님 견적기 — **웰릭스 짜임 · 우리 데이터 · 우리 엔진**(사장님 2026-09-17).
 *
 * ★사장님 「이거야(welrixmobility) 우리꺼 신차 견적기랑 맞춰서 쓰려고」 ·
 *   「나는 **옵션까지 다 넣어서** 견적을 넣고 싶은거야」 · 「**우리걸로**」 ·
 *   「근데 **웰릭스 형태로 해서 손님들한테 견적기를 주려고** 하는거임」.
 *
 * ★무엇이 다른가(업무 `/estimate` 와) — **면이 아니라 역할이 가른다**:
 *   ㉠ 신차만 — 중고는 시세를 사람이 넣어야 해서 손님이 혼자 못 친다
 *   ㉡ 원가·마진·손익·잔가 칸이 없다 — 비로그인은 `showsCost` 가 거짓이라 **값이 내려오지도 않는다**
 *      (대여료는 서버가 세서 넷만 보낸다 · `/api/estimate/quote`)
 *   ㉢ 옵션은 그대로 고른다 — 제조사 공식 가격표 + 조합 규칙(배타·선행·배제)이 실제로 막는다
 *
 * ⚠ 이 자리는 **문지기가 없다**(손님 면). 대신 원가가 새지 않는 것은 역할이 지킨다.
 * ⚠ 노브랜드 — 우리 이름을 세우지 않는다(CLAUDE.md). 채널 간판은 채널 주소가 따로 입는다.
 */
import EstimateApp from '@/features/estimate/EstimateApp';

export default function GuestQuotePage() {
  return <EstimateApp surface="guest" />;
}

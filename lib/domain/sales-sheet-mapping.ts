/**
 * **판매시트 열 구성과 매핑 — 코드 쪽 한 곳.**
 *
 * ★런타임 정본은 **판매시트 숨긴 탭 「AI 인계」의 @매핑 표**다(사장님 2026-08-14 —
 *   「판매시트에 매뉴얼 숨겨서 그 매뉴얼대로 갖고오자」). 시트를 고치면 발행이 바로 따른다.
 *   여기 값은 두 가지 일을 한다 —
 *     ① 그 표를 **찍어내는 원본**(`publish-handover-tab`)
 *     ② 그 표를 **못 읽었을 때의 예비값**(`publish-origin-tab`)
 *
 * ★왜 모듈로 뺐나 — 둘이 따로 적혀 있어서 **서로 어긋나 있었다**(실측 2026-08-14).
 *   예비 열 목록엔 「배차상태·모델·(당시)파워트레인·세부트림」이 있는데 예비 매핑 키는
 *   「상태·차종분류·트림」이라, 매핑을 못 읽는 날엔 그 네 열이 **경고 없이 통째로 비었다.**
 *   한 곳에서 나오면 어긋날 수가 없다.
 *
 * ⚠ 후보는 **앞에서부터 먼저 맞는 것**을 쓴다. 그래서 정제칸을 앞에 둔다 —
 *   공급사 원문보다 우리가 맞춰 둔 값이 먼저다.
 * ⚠ 여기 없는 이름은 안 옮긴다. 짐작해서 붙이면 그게 곧 «우리가 만든 오류»다.
 */

/** [판매시트 열, 공급사시트 열 이름 후보]. **이 차례가 곧 판매시트 열 차례다.** */
export const SALES_MAPPING: [string, string[]][] = [
  ['배차상태', ['배차상태', '판매상태', '상태', '차량상태']],
  ['구분', ['구분', '분류', '상품구분']],
  ['차량번호', ['차량번호', '차번']],
  /**
   * ★**차대번호(VIN)는 상품리스트에 안 올린다**(사장님 2026-08-22 「차대번호는 상품리스트에 안 올라가」) — SALES_RETIRED_COLUMNS.
   *   영업자가 손님 앞에서 보는 표에 쓸 값이 아니다. VIN 은 공급사 시트·ERP 에 그대로 남아
   *   번호판 나오기 전 신차를 같은 차로 이어붙이는 일(product.vehicleIdentity: 실번호 → VIN → 임시번호)은 계속 한다.
   *   ⚠ 유입(공급사 시트 → ERP)에서 VIN 을 읽는 것과 «판매시트에 열로 세우는 것»은 다른 문제다 — 여기서 빼는 건 후자뿐이다.
   */
  ['제조사', ['제조사(정제)', '제조사', '메이커']],
  /**
   * ⚠ 「차종분류」를 모델 후보에 넣지 마라. 옛 매핑엔 들어 있었는데, 제공시트에 진짜
   *   「차종분류」 칸(중형 SUV·준대형)이 생긴 뒤로는 그 값이 모델 칸에 실린다.
   * ★**정제칸 「모델」이 먼저다**(2026-08-23 — 「있는 걸 그대로 나르기」).
   *   08-19 에는 「모델명」이 먼저였다(제공시트에 공급사가 채운 칸). 그런데 「모델명」은 정제칸이 아니라
   *   **미러가 원본 「차종」에서 제조사 말·연료 꼬리만 떼어 만든 검색용 칸**(mirror-sheet-mapping.splitMakerModel)이라
   *   공급사 원문 표기가 그대로 남는다. 그래서 정제칸이 「모델 Y」인데 판매시트에는 「모델Y」가 실렸다
   *   (이안카 테슬라 10대 · audit:passthrough 실측 2026-08-23 — 발행기가 공백을 «지운» 것이 아니라 «다른 칸»을 실었다).
   *   축 레지스트리도 이 축을 `fromRefined: true`(정제칸 이름 = 「모델」)로 선언한다 — 그 선언과 맞춘다.
   * ⚠ 정제칸이 비면 예전처럼 「모델명」으로 떨어진다(`cell` 은 «값이 든 첫 칸»을 쓴다) — 채움률은 안 떨어진다.
   */
  ['모델', ['모델', '모델명']],
  /**
   * ★**차명 자리를 세부모델·세부트림 두 칸으로 갈랐다**(사장님 2026-08-22 「판매시트 차명을 세부모델 세부트림으로 해 주면 되잖아」).
   *   공급사 정제칸이 축별로 정리돼 있어서(세부모델 100% · 세부트림 88%, 실측 2026-08-22) 붙여 놓을 이유가 없다.
   *   ERP 도 판매시트만 읽으므로(sheet-daily-sync) 이 두 칸이 곧 ERP 의 sub_model·trim_name 이 된다 —
   *   그전까지 ERP 세부축은 옛 차종마스터 스냅에 멈춰 있었다(트림에 연료·배기가 섞인 「2.5 T 가솔린」 따위).
   *   공급사 원문 차명은 버리지 않는다 — 아래 옵션 앞에 「차명」으로 그대로 선다.
   */
  ['세부모델', ['세부모델']],
  ['세부트림', ['세부트림']],
  ['외장', ['외장색상', '외장색', '외부색상', '외장', '색상']],
  ['내장', ['내장색상', '내장색', '내부색상', '내장']],
  // ★연식은 주행거리 «앞»이다(사장님 2026-08-14). 차를 고를 때 연식과 주행을 나란히 본다.
  ['연식', ['연식']],
  ['Km', ['주행거리', 'Km', 'km', '예상 주행거리']],
  // ★차종구분(사장님 2026-08-19 — 「차종구분 하나 넣어 주라, 준중형 SUV 이런 거」 → 「주행거리 다음으로」) ← 공급사 시트 정제칸 「차종분류」(차종마스터 규격채택 차종분류+차체형태, 코드 없는 차는 모델 이름 스냅).
  // ★열 차례(사장님 2026-08-19 「파워트레인 뺐으니 외장·내장·연식·주행거리·연료·배기량·차종구분 순」)
  ['연료', ['연료(정제)', '연료', '유종']],
  ['배기량', ['배기량(정제)', '배기량']],
  /**
   * ★차종구분은 «배기량 바로 뒤»(사장님 2026-08-22 「배기량 끝에 차종구분 넣어 주고, 준중형 세단 이런 거」).
   *   값 = 정제칸 「차종분류」 한 칸(준중형 세단·중형 SUV). 코드면 표에서 글자를 불러온다.
   *   크기+구분을 우리가 붙이지 않는다(실측 2026-08-21: 붙이면 A6=중형 SUV, 팰리세이드=대형 MPV로 어긋난다).
   */
  ['차종구분', ['차종분류', '차종분류코드']],
  ['단기보증', ['단기보증']],
  ['1개월', ['1개월', '월렌트', '월세']],
  /**
   * ★**6개월 — 공급사가 적어 두고도 못 담기던 요금**(사장님 2026-08-23 「대여료는 충분하게 수집할 수 있게끔」).
   *   실측: 이안카·제이앤제이렌트카·빌린카 **3곳**이 「6개월」 열을 쓰는데 판매시트에 열이 없어
   *   그 요금이 통째로 버려졌다. **판매시트에 열이 없으면 ERP 에 영영 안 들어간다.**
   * ⚠ 유입(`sheet-import`)은 이미 `/^(\d+)개월…/` 패턴으로 6·72·84개월을 읽을 줄 안다 —
   *   병목은 언제나 «판매시트에 열이 서 있느냐»다. 새 기간이 생기면 여기에 한 줄 세우면 된다.
   */
  ['6개월', ['6개월']],
  // ★오토플러스 주행 구간(사장님 2026-08-18 상품리스트 흡수) — 12개월은 3만km 예외, 그 밖은 2만km 기본(공급사 데이터 매뉴얼).
  ['12개월', ['12개월', '12개월 반납형', '12개월3만']],
  ['장기보증', ['장기보증', '보증금', '보증금 반납형']],
  ['24개월', ['24개월', '24개월 반납형', '24개월2만']],
  ['36개월', ['36개월', '36개월 반납형', '36개월2만']],
  ['48개월', ['48개월', '48개월 반납형']],
  ['60개월', ['60개월', '60개월 반납형']],
  /**
   * ★**차명 = 공급사가 직접 입력한 글자, 자리는 옵션 앞**(사장님 2026-08-22 「공급사가 직접 입력한 거는 옵션 앞에 차명으로 그대로 두는 거지」).
   *   정제 조합(차명(정제))은 후보에서 뺐다 — 규격 이름은 위 제조사·모델·세부모델·세부트림 네 칸이 이미 들고 있고,
   *   이 칸은 «공급사가 뭐라고 적었나»를 그대로 보는 자리다(대조·검수용).
   */
  /**
   * ★★**공급사 원문 두 칸 — 「2중 보관」**
   *   (사장님 2026-08-23 「차명이랑 옵션 공급사가 기본으로 입력한 칸은 별도로 수집해서 보관한다 2중 보관이지」).
   *
   *   정제칸이 정본이 된 뒤로 **공급사가 적은 글자가 버려지는 구멍이 둘** 있었다:
   *   · 「옵션」은 정제칸 「선택옵션」을 먼저 집어서, 정제칸이 차 있으면 원문이 통째로 사라졌다.
   *   · 「차명」은 이름이 `모델` 별칭과 겹쳐(`HEADER_ALIASES.차명 = 'model'`) 왼쪽의 「모델」이 먼저 자리를 잡는 바람에
   *     **ERP 로 아예 못 갔다.** 그래서 `supplier_vehicle_name` 은 폐기된 옛 상품마스터 유입분만 들고 있었다.
   *
   *   두 칸 다 이름 끝에 「(원문)」을 붙여 **별칭 충돌을 없애고**, 나란히 세워 «원문 두 칸»이 눈에 보이게 한다.
   *   정제가 틀렸을 때 되짚을 유일한 근거이고, 담당자가 공급사와 통화할 때 쓰는 말이다.
   */
  ['차명(원문)', ['차명(세부모델+트림)', '차명']],
  ['옵션(원문)', ['옵션', '선택사양', '주요옵션']],
  /**
   * ★**정제칸의 나머지 축은 옵션 뒤로**(사장님 2026-08-22 「원산지랑 그런 것들은 옵션 뒤로 적당히 넣어줘」).
   *   앞쪽(제조사~차종구분)은 차를 고르는 자리라 손대지 않고, 참고·근거 값은 여기 모은다.
   *   · 원산지 = 국산/수입. **보증금 배율(국산 ×2 · 수입 ×3)의 근거**라 표시용이 아니라 돈이 걸린 값이다.
   *     차종마스터 참조를 끊은 뒤로 이 열이 ERP `origin` 의 유일한 길이다(정제칸 98% · 실측 2026-08-22).
   *   · 구동 = 정제칸 「구동방식」. 값이 있는 차만 찬다(정제칸 50%).
   *     ⚠ 표기가 아직 섞여 있다(2WD·FWD·AWD·RWD·4WD·xDrive·콰트로·4MATIC…) — 규격을 정하면 색상마스터처럼 별칭을 매핑한다.
   */
  ['원산지', ['원산지']],
  ['구동', ['구동방식', '구동']],
  // 인승 — 정제칸 「인승」 신설(2026-08-22 · 사장님 「구동 뒤에 인승 넣어줘」). 숫자만(5·7·9).
  ['인승', ['인승', '승차인원', '승차']],
  /**
   * ★**배터리용량 — 전기차의 배기량 자리**(사장님 2026-08-23 「배터리용량 등등 쓸 수 있는 거 쭈욱 쓰는 거야」·
   *   「상품시트에 옵션 다음에 구동 인승 다음에 넣어 줘」).
   *   전기차는 「배기량」이 비는 게 정상이라 «차를 가르는 값»이 하나 모자랐다(실측 판매가능 474대 중 전기차 33대).
   *   자리는 인승 바로 뒤 — 제원 축(구동·인승)을 잇는 끝자리다.
   * ⚠ 정제칸 「배터리용량」은 **커서가 만든다**(사장님 2026-08-23 오더). 여기 열이 서 있어도
   *   정제칸이 생기고 값이 채워지기 전까지는 빈 칸이다 — 열이 없으면 값이 ERP 로 영영 못 가므로 자리부터 잡는다.
   */
  ['배터리용량', ['배터리용량(정제)', '배터리용량', '배터리 용량', '배터리(kWh)', 'kWh']],
  ['최초등록', ['최초등록', '최초등록일']],
  ['차고지', ['차고지', '지역']],
  /**
   * ★**정책 UID — ERP·견적·계약서가 «어느 조건인지»를 잡는 끈**(사장님 2026-08-21 「표준방식으로」).
   *   실측 2026-08-21: ERP 상품 808대의 policy_code 가 빈칸이었다. 판매시트에 정책을 실을 칸이 없어
   *   길이 끊겼기 때문이다(상품마스터 경로를 접으면서 같이 끊겼다).
   *   값은 정책 탭의 「정책UID」(pol_…) — 뜻 없는 대체키라 정책명·업무코드가 바뀌어도 안 깨진다.
   *   영업자에게 보일 값이 아니라 맨 뒤에 둔다.
   */
  /**
   * ★**그 밖 요금 — 우리 규격 밖 기간을 ERP 로 나르는 한 칸**
   *   (사장님 2026-08-23 「**ERP 에는 다 반영할 수 있고 기존 시트에는 우리 규격만**」).
   *
   *   판매시트 앞쪽 요금 열은 **우리 규격**(1·6·12·24·36·48·60개월)만 세운다 — 영업자가 보는 표라서다.
   *   그런데 공급사는 그 밖도 쓴다(실측 2026-08-23: 아이언 72·84개월 · 오토플러스 18개월2만/3만 …).
   *   열을 다 세우면 20곳이 빈칸인 열만 늘고, 안 세우면 **그 요금이 ERP 에 영영 안 들어간다.**
   *   그래서 규격 밖 요금을 **한 칸에 모아** 나른다 — 표는 안 넓어지고 ERP 는 다 받는다.
   *
   *   형식: `72개월:390000|84개월:350000|18개월2만:780000`  (구분 `|` · 이름 그대로 · 원 단위)
   *   ⚠ 영업자가 읽는 칸이 아니다. 맨 뒤에 둔다. 값은 발행기가 만들고 사람이 손대지 않는다.
   */
  /**
   * ★**사진 — 판매시트가 ERP 로 나르는 유일한 길**(2026-08-23).
   *   그동안 판매시트에 사진 열이 없어도 문제가 안 보였다. ERP 에 이미 값이 있었고
   *   `soft-merge` 가 빈 값을 안 덮었기 때문이다. **재고를 리셋하자 그 값이 사라져** 유입이
   *   「판매시트 정확치환 필드 누락(photo_link)」로 **공급사 19곳을 통째로 보류**시켰다.
   *   열이 없으면 값이 ERP 에 영영 안 들어간다 — 이 문서에서 제일 중요한 한 줄이 여기서도 그대로다.
   * ⚠ 영업자가 읽는 칸이 아니라 맨 뒤에 둔다. 값은 공급사 시트 「사진링크」가 정본이다.
   */
  ['사진', ['사진링크', '사진', '이미지', '사진url', '이미지링크']],

  /**
   * ── 값 — 금액이지만 «대여료가 아닌 것». 대여료는 앞자리(단기보증~60개월)에 있다.
   *   사장님 2026-08-24 「그 뒤로 놓아야 할 항목들을 좀 성격에 맞게끔 잘 정리해줘야 할 거 같어」
   */
  ['소비자가격', ['소비자가격', '차량가격', '소비자가']],
  ['그 밖 요금', []],
  ['정책UID', []],

  /**
   * ★여기부터 **정책에서 온다**(사장님 2026-08-14 — 「영업자한테는 사실 다 보여줘도 되는데」
   *   · 「오픈 못할 거는 없으니까」).
   *
   *   예전엔 공급사 재고탭에 «우연히 있는 칸»만 낱개로 긁었다. 그래서 실측 2026-08-14 기준
   *   차고지·운전자범위·정비가 77대(20%)·값 1종이었고, 대인이 「무한/30」과 「50만 / 무한」으로
   *   앞뒤가 뒤집혀 섞여 있었다. 같은 값이 「정책」 탭에는 18곳 전부 구조적으로 있다.
   *   차마다 적힌 정책코드로 조인하면 100% 가 되고 표기도 하나가 된다.
   *
   * ★**뺀 것은 «감춘 것»이 아니라 «중복»이다.** 대인보상한도는 「대인」 칸에 면책금과 함께
   *   「무한/30」으로 들어가므로 열을 또 세우지 않는다. 자차·대물·자손·승계·자격도 같다.
   * ⚠ 재고탭에 그 칸이 있으면 **그쪽이 이긴다** — 그 차만의 예외일 수 있다.
   */
  /**
   * ★연령 셋을 나란히 — 「기본 26세 · 21세까지 내려감 · 70세까지」가 한눈에 보여야
   *   상담에서 바로 답이 나온다(사장님 2026-08-14 — 「기본연령 다음에 최소연령 최대연령」).
   * ⚠ 「최소연령」의 정책 탭 항목 이름은 **「연령인하」 그대로**다. 18곳에 이미 값이 들어 있어
   *   이름을 바꾸면 그 값이 매칭에서 떨어져 나간다. 여기서는 열 이름만 바꿔 싣는다.
   */
  // ★심사조건(사장님 2026-08-19 「기본연령 앞에 심사조건 빠졌네 — 무심사 / 소득확인 / 신용조회 3가지」) ← 정책 탭 「심사조건」
  ['심사조건', []],
  ['기본연령', []],
  ['최소연령', []],
  ['최대연령', []],
  // ★연령 하향 요금은 **두 칸**이다 — 「21세+」·「23세+」(사장님 2026-08-21 「영업자 요청 · 21+ 23+ 있었잖아 이거는 유지해달래 ·
  //    연령하향 요금을 21세+ 23세+ 로 두 칸 해주자」). 그 나이까지 내려 주면 «그때 붙는 월 할증», 안 내려 주면 「불가」.
  //    값은 supplier-policy-read.ageCell(p, 21|23) 이 만든다(정책 탭 「연령인하」+「연령 하향 요금」).
  ['21세+', []],
  ['23세+', []],
  ['면허기간', []],
  // ★개인·법인을 한 칸으로 요약한다 — 「본인만 / 기본 / 협의」(supplier-policy-read.driverScopeCell)
  ['운전자범위', ['운전자범위']],
  // ★사장님 2026-08-18 — 「추가운전」은 가능 여부만, 「추가운전 요금」은 「N인까지 · 1인당 월 M만원」. 옛 열 이름은 SALES_RETIRED_COLUMNS.
  ['추가운전', ['추가운전', '추가운전자']],
  ['추가운전 요금', []],

  ['연주행', ['연주행', '약정주행', '기본주행']],
  ['1만+', ['1만+', '추가주행 금액']],
  // ★「추가주행 방식」 열은 뺐다(사장님 2026-08-19 「필요없을 거 같음」) — 「1만+」 값 표기(만원=정액 · 대여료의 N%=정률)가 방식이다. SALES_RETIRED_COLUMNS.
  // ★「초과주행 국산·수입」 열은 뺐다(사장님 2026-08-19 — 「상품시트 정책에 초과주행은 보여줄 필요 없고」). 정책 탭엔 남는다(계약서용). SALES_RETIRED_COLUMNS.

  /**
   * ★보험 블록(사장님 2026-08-19) — 「보험료 | 대인 | 대물 | 자손 | 무보험 | 자차 | 긴급출동」 차례.
   *   칸 하나에 「보상한도 / 면책금」(무한 / 30만원). 자손은 사망·후유장애 최대금액 / 면책금.
   *   자차는 「차량가액 / 수리비 20%, 최대 50~100만원」 — 자기부담률·최소~최대 면책을 한 칸에(「자차 자기부담률」 열은 뺌).
   *   보험료가 「별도」인 정책은 대인~자차 다섯 칸에 전부 「별도」. 「가입 보험사」·「지정 정비점」은 뺐다(사장님 — 보여줄 필요 없음).
   *   글자는 supplier-policy-read.policyCell 이 만든다.
   */
  ['보험료', []],
  ['대인', ['대인']],
  ['대물', ['대물']],
  ['자손', ['자손']],
  ['무보험', ['무보험', '무보험보상']],
  ['자차', ['자차']],
  /**
   * ★자차 바로 뒤 — 「무엇을 자차로 안 물어 주나」는 자차를 읽고 이어서 보는 것이다.
   *   전에는 정비와 GPS 사이에 껴 있어 성격이 갈렸다(사장님 2026-08-24
   *   「그 뒤로 놓아야 할 항목들을 좀 성격에 맞게끔 잘 정리해줘야할거 같어」).
   */
  ['자차 보상제외', []],

  // ── 정비·사고 서비스
  ['긴급출동', []],
  ['정비', ['정비']],
  ['사고·정비 대차', []],
  ['GPS 장착', []],

  ['대여지역', []],
  ['탁송비', []],

  ['분납', ['분납', '보증금분납']],
  ['대여료 카드결제', []],
  ['보증금 카드결제', []],

  ['중도해지 1년미만', []],
  ['중도해지 1년이상', []],
  ['승계', []],
  ['사고다발 해지기준', []],

  ['전용계좌', ['전용계좌', '계좌번호']],
  ['비고', ['비고', '메모', '특이사항', '기타']],
  // 문패의 공급사명이 자동으로 들어간다 — 공급사 시트에서 찾지 않는다.
  ['공급사', []],
];

/** 판매시트 열 차례. */
export const SALES_COLUMNS = SALES_MAPPING.map(([name]) => name);

/**
 * ★**뺀 열** — 시트 「AI 인계」 @매핑에 사람이 남겨 두었더라도 발행기·인계탭 발행기가 이 이름은 세우지 않는다.
 *   왜 목록이 필요한가 — 인계탭 발행기는 «시트에만 있는 줄»을 사람이 더한 것으로 보고 뒤에 남긴다(2026-08-14 규칙).
 *   그래서 코드에서 줄만 지우면 열이 사라지는 게 아니라 **맨 뒤로 옮겨 붙는다.** 지우려면 여기 적어야 한다.
 *   - 파워트레인: 사장님 2026-08-18 — 연료·배기량 열이 따로 있어 겹친다.
 *   - 세부모델·세부트림: 08-19 에 「판매는 제조사·모델만」으로 뺐다가 **2026-08-22 표 맨 뒤로 되살렸다**
 *     (ERP 가 판매시트만 읽어서, 여기 없으면 ERP 세부축이 옛 마스터 스냅에 멈춘다 — 위 SALES_MAPPING 주석).
 */
export const SALES_RETIRED_COLUMNS: string[] = [
  '파워트레인',
  /**
   * 「연령하향 요금」 한 칸은 「21세+」·「23세+」 **두 칸**으로 갈렸다
   * (사장님 2026-08-21 「연령하향 요금을 21세+ 23세+ 로 두 칸 해주자」).
   * 코드는 갈랐는데 시트 @매핑에 옛 열이 남아 71 대 70 으로 어긋나 있었다(2026-08-24 실측).
   */
  '연령하향 요금',
  // 차대번호(VIN): 사장님 2026-08-22 「차대번호는 상품리스트에 안 올라가」 — 영업자가 손님 앞에서 볼 값이 아니다.
  //   공급사 시트·ERP 에는 그대로 남는다(번호판 나오기 전 신차를 같은 차로 잇는 데 쓴다).
  '차대번호',
  // 옵션은 「옵션(원문)」 하나로 통합한다. ERP options도 그 원문을 그대로 쓴다.
  '옵션',
  /**
   * 「차명」 → 「차명(원문)」 로 이름을 바꿨다(2026-08-23 · 「2중 보관」).
   * 옛 이름 그대로 두면 `HEADER_ALIASES.차명 = 'model'` 과 겹쳐 **왼쪽의 「모델」이 먼저 자리를 잡고 값이 버려진다.**
   * 폐지 목록에 적어야 시트 「AI 인계」 @매핑에서도 옛 열이 안 선다.
   */
  '차명',
  '추가운전자', '추가운전자 요금', '초과주행 국산', '초과주행 수입',
  '자차 자기부담률', '가입 보험사', '지정 정비점', '추가주행 방식',
];

/** 열 이름 → 공급사 열 후보. */
export const SALES_ALIAS: Record<string, string[]> = Object.fromEntries(
  SALES_MAPPING.filter(([, cands]) => cands.length).map(([name, cands]) => [name, cands]),
);

export type PublishedSalesMapping = {
  columns: string[];
  aliases: Record<string, string[]>;
  retired: string[];
};

/**
 * 판매시트 숨김 탭 「AI 인계」의 @매핑 표를 발행 열·별칭으로 해석한다.
 * 발행기와 감사기가 같은 런타임 정본을 사용해야 열 추가/삭제를 한쪽만 놓치지 않는다.
 */
export function parsePublishedSalesMapping(rows: unknown[][]): PublishedSalesMapping {
  const text = (value: unknown) => String(value ?? '').trim();
  const from = rows.findIndex((row) => text(row[0]) === '@매핑');
  if (from < 0) throw new Error('@매핑 줄이 없다');

  const aliases: Record<string, string[]> = {};
  const order: string[] = [];
  let closed = false;
  for (const row of rows.slice(from + 1)) {
    if (text(row[0]) === '@매핑끝') { closed = true; break; }
    const column = text(row[1]);
    if (!column) continue;
    order.push(column);
    const candidates = text(row[2]).split(',').map((value) => value.trim()).filter((value) => value && !value.startsWith('('));
    if (candidates.length) aliases[column] = candidates;
  }
  if (!closed) throw new Error('@매핑끝 줄이 없다');
  if (!order.length || !Object.keys(aliases).length) throw new Error('@매핑 표가 비었다');

  const retired = order.filter((column) => SALES_RETIRED_COLUMNS.includes(column));
  let columns = order.filter((column) => !SALES_RETIRED_COLUMNS.includes(column));
  for (const column of retired) delete aliases[column];
  for (const column of ['제조사', '모델', '차명(원문)', '옵션(원문)'] as const) {
    if (SALES_ALIAS[column]) aliases[column] = SALES_ALIAS[column];
  }
  /**
   * **공급사 원문 두 칸을 원산지 바로 앞에 세운다**(사장님 2026-08-22 「공급사가 직접 입력한 거는 옵션 앞에 차명으로 그대로」·
   * 2026-08-23 「차명이랑 옵션 … 별도로 수집해서 보관한다 2중 보관이지」).
   * 규격 이름 넷(제조사·모델·세부모델·세부트림)이 앞에 서고, 원문은 대조용으로 원산지 앞에 나란히 선다.
   * ⚠ 차례가 곧 열 차례다 — 「차명(원문)」이 먼저, 그 다음 「옵션(원문)」.
   */
  for (const raw of ['차명(원문)', '옵션(원문)'] as const) {
    if (columns.includes(raw)) continue;
    const originAt = columns.indexOf('원산지');
    const at = originAt >= 0 ? originAt : columns.indexOf('공급사');
    columns = at >= 0
      ? [...columns.slice(0, at), raw, ...columns.slice(at)]
      : [...columns, raw];
  }
  const duplicates = columns.filter((column, index) => columns.indexOf(column) !== index);
  if (duplicates.length) throw new Error(`@매핑 중복 열: ${[...new Set(duplicates)].join(' · ')}`);
  for (const required of ['차량번호', '공급사']) {
    if (!columns.includes(required)) throw new Error(`@매핑 필수 열 없음: ${required}`);
  }
  return { columns, aliases, retired };
}

/**
 * 상품리스트에서 뺄 것 — 별도 탭이 따로 싣는 것들.
 * 같은 차가 두 탭에 서면 사고다. 한쪽만 갱신되면 영업자가 옛 값을 본다.
 * 적는 법: 「RP023」(그 공급사 전부) 또는 「RP012:구독」(탭 이름에 «구독»이 든 것만 · 부분일치).
 */
export const SALES_EXCLUDE: [string, string][] = [
  // ★사장님 2026-08-18 — 「오플 구독 탭 없애고 상품리스트에 흡수, 손오공 구독만 별도 탭」.
  //   오토플러스는 이제 정제시트(우리 규격)에서 상품리스트로 간다. 12개월←12개월3만 · 24/36개월←2만 구간(SALES_MAPPING 별칭).
  //   18개월·3만 구간 요금은 상품리스트 열이 없어 안 싣는다 — 정제시트·상품마스터(변형 요금 열)에 있다.
  // ★사장님 2026-08-18 — 「반납형 구독은 상품리스트에 넣고, 손오공 인수형 구독만 별도 탭」.
  //   손오공 구독재고도 상품리스트에 선다(12~60개월 ← 반납형 열, 장기보증 ← 보증금 반납형). 인수형 요금은 「손오공인수형구독」 탭(publish-sonogong-tab).
  //   그래서 여기 뺄 것이 없다 — 목록은 비어 있어도 규칙(발행기·인계탭)은 살아 있다.
  //   ['RP012:구독', …] 는 2026-08-18 19:5x 뺐다.
  // ★사장님 2026-08-19 — 「복구하자, 판매시트 탭 3개로 회귀: 상품리스트 · 손오공구독(반납/인수 한 탭) · 오플구독(정제시트에서)」
  //   → 다시 뺀다. 두 탭은 같은 발행기(publish-origin-tab --only=…)가 같은 정본 차명·같은 열로 찍고, 원본 요금 블록(인수형 / 오플 2만·3만)을 뒤에 덧붙인다.
  //   상품리스트에도 실으면 같은 차가 두 탭에 선다 — 한쪽만 갱신되면 영업자가 옛 값을 본다.
  ['RP023', '오토플러스 — 「오플구독」 탭이 정제시트 줄을 따로 싣는다(publish-origin-tab --only=RP023 --tab=오플구독)'],
  ['RP012:구독', '손오공 구독 — 「손오공구독」 탭(반납형+인수형)이 따로 싣는다(publish-origin-tab --only=RP012:구독 --tab=손오공구독)'],
  ['RP004:월렌트', '아이카 월렌트 — 시트에 올리는 대상이 아니다'],
  ['RP004:수수료', '아이카 수수료 — 재고 탭이 아니다'],
  ['RP013:정책', '웰릭스 정책 — 재고 탭이 아니다'],
  // ★규격화시트에도 「정책」 탭이 딸려 온다(create-supplier-sheet 가 같이 만든다).
  //   재고 탭이 아니라 못 읽는 게 정상이다 — 빼 두지 않으면 「못 읽은 것」에 늘 떠 있어
  //   진짜 구멍이 안 보인다.
  ['RP004:정책', '아이카 규격화시트의 정책 탭 — 재고 탭이 아니다'],
];

/**
 * **머리글 메모 — 「이 칸이 뭐지」를 그 자리에서 답한다.**
 *
 * ★손에 익은 이름은 **안 바꾼다**(대인·대물·자차·자손·1만+·21세…). 영업자가 몇 년 쓰던 말이라
 *   바꾸면 그게 더 혼란이다. 대신 뜻을 메모로 단다 — 이름은 그대로, 오해는 없게.
 * ★새로 만든 칸은 이름부터 제대로 쓴다(사장님 2026-08-14 — 「니가 할 때는 항목을 잘 써줘봐」).
 */
export const SALES_NOTES: Record<string, string> = {
  // ★말투(사장님 2026-08-19 — 「메모에 반말 갈겨 놓은 것 같은데」): 영업자가 읽는 글이라 **짧은 존댓말**로 통일한다. 뜻은 그대로.
  배차상태: '즉시출고 / 출고가능 / 상품화중 / 출고협의 / 계약중 / 출고불가',
  구분: '신차렌트 / 중고렌트 / 중고구독',
  차량번호: '파란 밑줄이 있으면 사진 링크입니다 — 누르면 공급사가 올린 사진으로 이동합니다. 검정 글자는 사진이 없는 차입니다.',
  모델: '정제 「모델」입니다. 세부모델·트림은 「차명」에 조합으로 실립니다.',
  차명: '정제 「차명(정제)」= 모델+세부모델+세부트림 한 칸입니다. 없으면 공급사 원문입니다.',
  차종구분: '정제 「차종분류」 한 칸입니다(준대형 세단). 코드(vc-15)면 그 글자를 불러옵니다.',
  연식: '차량 연식(년)',
  Km: '주행거리(km)',
  단기보증: '보증금(원) — 오른쪽 단기 기간(1·12개월)에 적용됩니다. 「-」는 그 기간을 운영하지 않는다는 뜻입니다.',
  '1개월': '월 대여료(원). 「-」는 그 기간을 운영하지 않는다는 뜻입니다(빈칸·「불가」도 「-」로 표시).',
  '12개월': '월 대여료(원). 「-」 = 운영하지 않음', '24개월': '월 대여료(원). 「-」 = 운영하지 않음', '36개월': '월 대여료(원). 「-」 = 운영하지 않음',
  // ── 갈래 탭(손오공구독·오플구독) 원본 요금 칸 — 2026-08-19 탭 3개 기본 세팅
  '보증금 반납형': '손오공 반납형 보증금 = 「연수×대여료」(계약 연수 × 월 대여료). 기간을 골라야 금액이 정해지므로 규칙을 글자로 둡니다.',
  '12개월 반납형': '손오공 반납형 월 대여료(원) — 만기에 차를 반납합니다.', '24개월 반납형': '손오공 반납형 월 대여료(원)', '36개월 반납형': '손오공 반납형 월 대여료(원)', '48개월 반납형': '손오공 반납형 월 대여료(원)', '60개월 반납형': '손오공 반납형 월 대여료(원)',
  '보증금 인수형': '손오공 인수형 보증금(원) — 만기에 차를 인수하는 상품. 「-」 = 인수형 없음.',
  '36개월 인수형': '손오공 인수형 월 대여료(원) — 만기 인수. 인수형은 36개월부터.', '48개월 인수형': '손오공 인수형 월 대여료(원)', '60개월 인수형': '손오공 인수형 월 대여료(원)',
  보증금: '오토플러스 보증금 산출방식(오플 공지사항 보증금표): 국산차 = 월 대여료 × 2 · 수입차 = 12개월은 월 대여료 × 3, 18개월 이상은 × 6. 12개월 이상 계약, 카드결제 불가, 보증금 입금 완료 고객에 한해 배정. 정확한 금액은 오플 공지사항 보증금표 기준(문의: 프리패스 담당자).',
  '12개월 2만km': '오토플러스 월 대여료(원) — 12개월 계약 · 연 20,000km. 초과 1km당 국산 100원/수입 200원. 「-」 = 운영하지 않음', '12개월 3만km': '오토플러스 월 대여료(원) — 12개월 · 연 30,000km(수입차는 2만km만)',
  '18개월 2만km': '오토플러스 월 대여료(원) — 18개월 · 연 20,000km', '18개월 3만km': '오토플러스 월 대여료(원) — 18개월 · 연 30,000km',
  '24개월 2만km': '오토플러스 월 대여료(원) — 24개월 · 연 20,000km', '24개월 3만km': '오토플러스 월 대여료(원) — 24개월 · 연 30,000km',
  '36개월 2만km': '오토플러스 월 대여료(원) — 36개월 · 연 20,000km(차령·주행거리로 36개월 거절될 수 있음)', '36개월 3만km': '오토플러스 월 대여료(원) — 36개월 · 연 30,000km',
  '48개월': '월 대여료(원). 「-」 = 운영하지 않음', '60개월': '월 대여료(원). 「-」 = 운영하지 않음',
  장기보증: '보증금(원) — 오른쪽 장기 기간(24~60개월)에 적용됩니다. 「-」는 그 기간을 운영하지 않는다는 뜻입니다.',
  옵션: '공급사가 적은 선택옵션입니다.',
  최초등록: '차량 최초등록일(연-월)입니다. 입고일이 아닙니다.',
  소비자가격: '차량 가격(원)입니다. 대여료가 아닙니다.',
  차고지: '차량이 있는 곳입니다.',
  운전자격: '운전 가능 연령과 면허 경력 — 예: 「만 26~65세 · 면허 1년 이상」',
  운전자범위: '본인만 / 기본(직계가족·임직원) / 협의. 개인·법인이 다르면 나누어 적혀 있습니다.',
  연주행: '연간 약정 주행거리(만km)입니다 — 「2만」 = 20,000km, 「2.5만」 = 25,000km. 초과하면 초과주행 요금이 붙습니다.',
  '1만+': '약정 주행거리를 1만km 올릴 때 붙는 월 요금입니다.',
  '추가주행 방식': '정액인지 대여료 비례(정률)인지 표시합니다.',
  '초과주행(1km)': '약정을 넘긴 거리 1km당 요금입니다.',
  대인: '대인배상 — 「보상한도 / 면책금」 예: 무한 / 30만원. 보험료가 별도인 상품은 「별도」로 표시됩니다.',
  대물: '대물배상 — 「보상한도 / 면책금」 예: 1억원 / 30만원',
  자손: '자기신체사고 — 「사망·후유장애 최대금액 / 면책금」 예: 1억원 / 30만원',
  무보험: '무보험차상해 보상한도입니다(없으면 「없음」).',
  자차: '자기차량손해 — 「보상기준 / 자기부담률, 면책 최소~최대」 예: 차량가액 / 수리비 20%, 최대 50~100만원',
  보험료: '대여료에 보험료가 포함인지 별도인지입니다. 「별도」면 오른쪽 보험 칸이 모두 「별도」로 표시됩니다.',
  정비: '정비 포함 여부입니다.',
  긴급출동: '연간 긴급출동 횟수입니다.',
  '사고·정비 대차': '사고나 정비로 차를 못 탈 때 대차 제공 여부입니다.',
  '자차 보상제외': '자차보험으로 처리되지 않는 경우입니다 — 계약 전에 손님께 안내해 주세요.',
  'GPS 장착': '차량 위치장치 장착 여부입니다.',
  대여지역: '전국 / 제주도 불가 등 대여 가능 지역입니다.',
  탁송비: '차량을 손님께 보내는 비용입니다.',
  심사조건: '무심사 / 소득확인 / 신용조회 — 공급사 정책 탭 「심사조건」 값입니다. 비어 있으면 아직 미입력(무심사로 단정하지 않습니다).',
  기본연령: '기본 운전 가능 연령입니다(예: 만 26세 이상). 이보다 어리면 「최소연령」(연령인하) 칸을 확인해 주세요.',
  최대연령: '운전 가능 최대 연령입니다(예: 만 65세 이하).',
  면허기간: '면허 취득 후 경과 기간 조건입니다 — 제한없음 / 1년 이상 등',
  추가운전: '추가 운전자 가능 여부입니다 — 가능 / 불가 / 협의',
  '추가운전 요금': '「N인까지 · 1인당 월 M만원」 — 몇 명까지, 한 명당 월 얼마인지입니다.',
  '중도해지 1년미만': '1년 미만에 해지할 때 잔여 대여료의 몇 %인지입니다.',
  '중도해지 1년이상': '1년 이상 이용 후 해지할 때 잔여 대여료의 몇 %인지입니다.',
  최소연령: '기본연령보다 아래로 어디까지 낮출 수 있는지입니다 — 만 21세까지 / 불가. 정책 탭의 「연령인하」 칸입니다.',
  '연령하향 요금': '연령을 낮출 때 붙는 월 할증입니다.',
  분납: '보증금 분납 가능 여부입니다.',
  '대여료 카드결제': '대여료를 카드로 낼 수 있는지 — 「불가」 아니면 수수료율(「1.5%」)입니다.',
  '보증금 카드결제': '보증금을 카드로 낼 수 있는지 — 「불가」 아니면 수수료율(「1.5%」)입니다.',
  '중도해지 위약금': '중도 해지 시 잔여 대여료의 몇 %인지입니다.',
  승계: '계약을 다른 사람에게 넘길 수 있는지입니다 — 불가 / 금액 / 협의',
  '사고다발 해지기준': '1년 내 과실 사고가 몇 회면 회사가 해지할 수 있는지입니다.',
  전용계좌: '이 공급사에 입금하는 계좌입니다.',
  공급사: '이 차량을 공급하는 회사입니다.',
};

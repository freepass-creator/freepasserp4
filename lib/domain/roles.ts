/**
 * **역할 셋 — 공용.** 누가 이 화면을 보는가. 세 채(찾기·견적·거래)가 다 물어보는 것이다.
 *
 * ★★**왜 갈랐나.** 이 타입은 `lib/domain/deal.ts`(거래 채 · 계약을 맺는 코드) 안에 살았다.
 *   그래서 **하단 홈바**(`lib/tabbar`)가 「이 사람이 영업자인가」를 물으려고 계약 파일을 부르고 있었다 —
 *   즉 **모든 화면**이 계약 코드에 이름으로 묶여 있었다.
 *   `import type` 이라 빌드에서는 지워지지만, 채를 뜯어낼 때는 파일 경계가 그대로 짐이 된다.
 *
 * ⚠ `deal.ts` 가 그대로 다시 내보낸다 — 부르던 쪽은 안 고친다.
 * ⚠ 여기에는 **바깥을 향한 import 가 없어야 한다.**
 */

/**
 * ```
 * agent      영업자 — 차를 찾아 손님에게 보낸다
 * provider   공급사 — 차를 대고 재고를 관리한다
 * admin      우리 — 다 본다
 * ```
 */
export type Role = 'agent' | 'provider' | 'admin';

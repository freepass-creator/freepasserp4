/**
 * **판매시트 열 차례 — 못 박은 자리.**
 *
 * ★사장님 2026-08-24
 *   「배차상태 구분 차량번호 제조사 모델 세부모델 세부트림 외장 내장 연식 Km 연료 배기량
 *     차종구분 단기보증 1개월 6개월 12개월 장기보증 24개월 36개월 48개월 60개월
 *     차명(원문) 옵션(원문) — 이건 고정이고 이 뒤로 정제칸 정보까지는 고정으로 해둬야 해」
 *   「그 뒤로 놓아야 할 항목들을 좀 성격에 맞게끔 잘 정리해줘야 할 거 같어」
 *   「그 뒤로 뭘 놓을지 정제시트와 정책값을 보고 판단하자」
 *
 * ★**열 정본은 판매시트 숨긴 탭 「AI 인계」의 @매핑 표다.** 여기 적힌 것은 그 표가
 *   지켜야 할 «차례»다 — 시트가 이 차례에서 벗어나면 `check:columns` 가 잡는다.
 *   열을 늘리고 줄이는 일은 여기와 시트를 «같이» 고친다.
 *
 * ★**어떻게 정했나 — 실측(2026-08-24)**
 *   정제시트 20곳 647줄: 원산지 98% · 구동 89% · 인승 74% · 최초등록 96% ·
 *     차량가격 70% · 사진 57% · **배터리용량 4%** · 차대번호 0%
 *   정책값 20곳 실제 45줄: 기본주행·연령인하 98% · 보험 계열 96% · 21세+/23세+ 87% ·
 *     심사조건 56% · 해지·승계 51% · **대여료 카드결제 0%**
 *   ⚠ 정책 탭은 빈 줄에도 제출서류 체크박스가 `FALSE` 로 들어가 207줄로 보인다.
 *     실제 정책은 45줄이다 — 분모를 207로 잡으면 채움률을 통째로 잘못 읽는다.
 *
 *   사장님 판단: 「대여료 카드결제」는 값이 0이어도 **자리를 지켜둔다**.
 *              「입고일자」는 **공급사만 관리**한다 — 판매시트에 안 세운다.
 */

/** 사장님이 글자 그대로 지정한 앞자리. 이 25칸은 차례까지 고정이다. */
export const SALES_HEAD_FIXED: readonly string[] = [
  '배차상태', '구분', '차량번호',
  '제조사', '모델', '세부모델', '세부트림', '외장', '내장', '연식', 'Km', '연료', '배기량', '차종구분',
  '단기보증', '1개월', '6개월', '12개월', '장기보증', '24개월', '36개월', '48개월', '60개월',
  '차명(원문)', '옵션(원문)',
];

/**
 * 앞자리 뒤를 «성격»으로 묶은 것. 묶음 안의 차례도 고정이다.
 * 한 줄로 읽으면 — 차를 알아보고(규격) → 그 한 대를 알고(개체) → 값을 보고 → 조건을 본다(정책).
 */
export const SALES_SECTIONS: readonly { title: string; why: string; columns: readonly string[] }[] = [
  {
    title: '규격 — 있으면 쓰는 것',
    why: '앞자리 규격과 같은 성격이나 «선택»이다. 사장님 「연식 연료 배기량 구동 인승 이런 거는 원자로 확보해서 선택적으로 있는 거만 활용」. 배터리용량은 4%지만 전기차에는 이것이 곧 차의 크기다.',
    columns: ['원산지', '구동', '인승', '배터리용량'],
  },
  {
    title: '이 차 한 대',
    why: '차종이 아니라 «그 차»에 붙는 것. 입고일자는 공급사만 관리한다(사장님 2026-08-24).',
    /**
     * ★「사진」 열은 **뺀 게 맞다**(사장님 2026-08-25 「사진링크는 필요없음 차량번호에 거니까」).
     *   사진은 **차량번호 셀에 파란 링크로** 걸린다 — 주소 글자를 또 보여 줄 이유가 없다.
     *
     * ⚠ 2026-08-28 에 「열이 없으면 값이 ERP 에 안 간다」며 숨김 열로 되살렸다가 바로 되돌렸다.
     *   그 걱정은 사진이 열로만 오던 시절 것이다 — 지금은 **차번 셀 링크**가 그 길을 든다
     *   (셀 링크는 values 로는 안 보이고 `includeGridData` 로 읽는다).
     *   `SALES_RETIRED_COLUMNS` 가 이 이름을 막고 있어, 되살려도 발행기가 세우지 않는다.
     *   되살리려면 그 목록부터 고쳐야 하고, 그건 사장님 결정을 되돌리는 일이다.
     */
    columns: ['최초등록', '차고지', '사진'],
  },
  {
    title: '값',
    why: '금액이지만 대여료가 아닌 것. 대여료는 앞자리에 있다.',
    columns: ['소비자가격', '그 밖 요금'],
  },
  /**
   * ★**여기부터 ERP 상세와 같은 차례로 읽힌다**(사장님 2026-08-28 「판매시트도 보여지는 거를
   *   ERP 순서를 거의 동일하게 해 주면 좋겠어」 · 「차명 옵션 원문 다음부터가」).
   *
   *   ERP 상세는 사진 → 차량스펙 → 대여료조건 → **보험조건** → 계약조건 → 기타사항 으로 읽는다.
   *   시트는 보험이 한참 뒤에 있어, 같은 차를 두 화면에서 볼 때 눈이 매번 다시 자리를 찾았다.
   *   위 「규격·이 차 한 대」=차량스펙 · 「값」=대여료조건 이고, **보험을 정책 맨 앞으로** 올려 맞춘다.
   *   그 뒤 계약조건 안쪽 차례도 ERP 와 같게 — 심사 → 주행 → 돈 → 해지 → 운전자 → 정비.
   */
  {
    title: '정책 — 보험',
    why: 'ERP 상세에서 대여료 바로 다음이 보험이다. 「자차 보상제외」는 자차 바로 뒤 — 자차를 읽고 이어서 보는 것이라 떼어 놓으면 안 된다.',
    /* 「보험료」·「자차 보상제외」는 **뺀 열**이라 여기 안 적는다(`SALES_RETIRED_COLUMNS`, 2026-08-25).
       보험료가 «별도»인 정책은 대인·대물·자손·무보험·자차 다섯 칸이 「별도」로 말하므로 정보가 안 사라진다.
       여기 적어 두면 발행기는 안 세우는데 검사만 어긋난다 — 실제로 2026-08-28 에 36자리가 밀렸다. */
    columns: ['대인', '대물', '자손', '무보험', '자차'],
  },
  {
    title: '정책 — 문패',
    why: '어느 정책을 보고 있는지. 심사조건은 사장님 지시로 정책 맨 앞이다.',
    /**
     * ★**「정책UID」를 되살렸다 — 단, 숨김 열이다**(사장님 2026-08-28 「판매시트에도 반영될 수
     *   있게끔」 · 「ERP 에서 받을 수 있게끔」).
     *
     *   2026-08-25 에 「정책 uid 이런것도 상품리스트에 필요없지」 하셔서 열을 통째로 뺐는데,
     *   그 뒤로 **ERP 가 정책을 받을 길이 없어졌다** — 매물 863대 중 정책이 붙은 차가 39대뿐이었다.
     *   열이 없으면 값이 ERP 에 영영 안 들어간다.
     *   `SALES_HIDDEN_COLUMNS` 에 이미 들어 있어 **영업자 화면엔 안 보이고 ERP 만 읽는다** —
     *   「안 보이게」와 「ERP 가 받게」가 둘 다 지켜지는 유일한 자리다.
     */
    columns: ['정책UID', '심사조건'],
  },
  {
    title: '정책 — 주행',
    why: 'ERP 계약조건에서 심사 다음이 주행 약정이다.',
    columns: ['연주행', '1만+'],
  },
  {
    title: '정책 — 계약·결제',
    why: '돈이 오가는 조건. ERP 계약조건의 보증금·카드결제·지역 자리다.',
    columns: ['대여지역', '탁송비', '분납', '대여료 카드결제', '보증금 카드결제'],
  },
  {
    title: '정책 — 해지·승계',
    why: '위약·승계. ERP 계약조건의 「결제 · 위약」 다음 자리다.',
    columns: ['중도해지 1년미만', '중도해지 1년이상', '승계', '사고다발 해지기준'],
  },
  {
    title: '정책 — 운전자',
    why: '누가 몰 수 있나. 나이 → 면허 → 범위 → 추가운전 차례로 읽힌다.',
    columns: ['기본연령', '최소연령', '최대연령', '21세+', '23세+', '면허기간', '운전자범위', '추가운전', '추가운전 요금'],
  },
  {
    title: '정책 — 정비·사고 서비스',
    why: 'ERP 계약조건 마지막 줄이 「정비 · 대차」다.',
    columns: ['긴급출동', '정비', '사고·정비 대차', 'GPS 장착'],
  },
  {
    title: '끝',
    why: '표를 다 읽고 나서 보는 것.',
    columns: ['전용계좌', '비고', '공급사'],
  },
];

/** 앞자리 + 묶음을 편 것 = 판매시트가 서야 할 차례 전부. */
export const SALES_COLUMN_ORDER: readonly string[] = [
  ...SALES_HEAD_FIXED,
  ...SALES_SECTIONS.flatMap((s) => s.columns),
];

/** 어느 칸이 어느 묶음에 드는가 — 화면·문서에서 구역을 말할 때. */
export const sectionOf = (column: string): string =>
  SALES_HEAD_FIXED.includes(column)
    ? '고정 앞자리'
    : SALES_SECTIONS.find((s) => s.columns.includes(column))?.title || '';

/**
 * **AI(프리패스 자동화)가 공급사 시트에서 «적고 만지는» 칸과 규칙** — 안내 탭(제공 「작성 안내」·정제 「정제시트 안내」)에 같은 글로 실린다.
 *
 * ★사장님 2026-08-18 — 「매뉴얼은 니가 담당해라 — 정제시트에도 공급사시트 AI가 작성하고 만지는 매뉴얼 만들어」 ·
 *   「제조사는 르노라고만 하고 KGM — 매뉴얼에 박아서 모든 시트 통일」 · 「정책코드 앞에 한 줄 넣어서 손대는 거 아닌 느낌」.
 * ★여기 적은 것만 기계가 쓴다. 이 목록에 없는 칸을 기계가 건드리면 그건 버그다.
 */
import { MAKER_STANDARD_NOTE } from './maker-display';
import { AI_TAIL_COLUMNS, DIVIDER_COLUMN } from './supplier-template-sheet';

export const AI_TOUCH_RULES: { what: string; how: string; when: string }[] = [
  { what: '정제시트 차명(세부모델+트림) · 옵션', how: '원본을 매번 따른다. 아이카=차종분류(모델 이름)+트림 · 오토플러스=차종+모델명(풀 문장) · 이안카=차종분류+세부모델+트림 · 아이언=홈페이지 제목. 정제시트에서 고쳐도 다음 미러가 원본으로 되돌린다. 정제된 이름은 오른쪽 정제칸. 색·연식·가격은 처음 한 번.', when: 'sync-mirror-all 마다' },
  { what: '★차명 정제 흐름(한 줄)', how: '신규 차종 → **우리 차종마스터에 먼저 넣는다** → fill 이 차량번호 왼쪽 차종·차명을 그 마스터에 걸어 정제칸을 박는다 → 상품시트. 배기량 칸으로 차명을 뒤집지 않는다. 엔카 M/SM/T는 그 이름과 맞을 때만. 마스터에 없으면 빈칸.', when: '새 차가 들어올 때마다 · 차종마스터가 바뀌면' },
  { what: `구분선 「${DIVIDER_COLUMN.name}」 오른쪽 전부(정책코드 · ${AI_TAIL_COLUMNS.map((c) => c.name).join(' · ')})`, how: '프리패스/AI 칸(머리 보라). 렌트사는 여기에 적지 않는다. 왼쪽(남색 머리)은 렌트사 칸 — 기계가 값을 안 덮는다.', when: '늘' },
  { what: '엔카 행키(M/SM/T)', how: '중고 시세용. 정제칸에 이미 박힌 **차종마스터 이름·연료와 같을 때만** 넣는다. 엔카 원자에 없다고 비슷한 모델 키를 붙이지 않는다. 어긋난 leftover 는 지운다. 모델·세부모델·세부트림 글자는 이 도구가 안 쓴다.', when: 'stamp-encar-codes-on-supplier --apply' },
  { what: '정제칸(모델·세부모델·세부트림·연료·배기량) · ERP 차종코드 · 선택옵션 · 외장/내장색상', how: '모델·세부모델·세부트림·연료(정제)·배기량(정제)은 **우리 차종마스터** 내용. 차종코드(mf-…)는 상품마스터가 가리키는 그 마스터 행. 선택옵션·외장/내장색상은 그 줄 왼쪽 원문에서 규격으로. 마스터에 없는 차는 이름 칸을 비운다.', when: 'fill-supplier-ai-columns --apply · realign-derived-cells' },
  { what: '정책코드', how: '정제시트: 원본 줄의 조건 칸(대인·대물·자차…)을 「정책」 탭 줄로 접어 코드를 붙인다(같은 조건이면 한 줄, ERP 에 같은 조건이 있으면 그 코드). 제공시트: 사람이 「정책」 탭 코드 중 하나를 고른다. 비면 「(프리패스 기본)」.', when: '정제시트는 미러마다 · 제공시트는 사람' },
  { what: '제조사 · 제조사(정제)', how: MAKER_STANDARD_NOTE + ' 규격 밖 이름(포드 등)은 그대로 둔다.', when: '적을 때 · 일괄 통일(unify-maker-names)' },
  { what: '상태', how: '규격값 6개(즉시출고·출고가능·상품화중·출고협의·계약중·출고불가)만. 비어 있으면 「출고협의」로 본다(ERP·발행기와 같은 판정) — 빈 칸은 그렇게 채워 둔다.', when: '정제시트는 미러마다 · 빈 칸 채움 때' },
  { what: '빈 칸 채움(같은 사실의 다른 표기만)', how: '제조사←제조사(정제) · 연료←연료(정제) · 배기량←배기량(정제) · 외부색상←외장색상 · 내부색상←내장색상 · 연식←최초등록일의 연도(모델연식이 아니라 «등록 연도») · 분류는 구독 탭이면 중고구독, 그 탭 값이 하나면 그 값. 그 밖(차량가격·옵션·주행거리·사진링크·최초등록일)은 지어내지 않는다 — 렌트사가 적는다.', when: 'audit-stock-gaps --fill' },
  { what: '값 표기 규격', how: '돈은 숫자만(콤마 있어도 됨) · 날짜 YYYY-MM-DD(「23-9-21」은 2023-09-21로) · 연료 가솔린/디젤/LPG/하이브리드/전기/수소 · 분류 신차렌트/중고렌트/신차구독/중고구독 · 정책 탭 표기는 머리글 메모(만 21세까지 · 연 20,000km · 50만원 …).', when: '적을 때 · normalize-policy-values' },
  { what: '줄·열', how: '기계는 줄을 지우지 않는다(안 파는 차는 상태만 출고불가) · 열 이름·차례는 표준(웰릭스) 그대로 — 이름으로 읽는다. 대여료 블록만 공급사 상품 구조에 따라 다를 수 있다(손오공 구독 인수형/반납형 · 오토플러스 2만/3만).', when: '늘' },
  { what: '서식', how: 'Roboto · 표(Table)+칩 드롭다운 · 대여료 배경(단기 청록·장기 파랑) · 머리 남색(렌트사)/보라(프리패스). 서식을 다시 입힐 때 값은 스냅샷→되쓰기로 보전한다(표 삭제가 값을 지운다).', when: 'reformat-supplier-stock-tabs' },
];

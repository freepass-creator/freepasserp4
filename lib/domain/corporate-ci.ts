/**
 * **법인 CI — 대외 문서에 찍히는 우리.** 순수 상수.
 *
 * ★사장님 2026-08-26 「ci 센터에 규격있어 제대로 확인하고」.
 *   값은 CI 센터(`C:\dev\ci_center/index.html`)의 브랜드 정의에서 그대로 가져왔다.
 *
 * ★★**CI(법인)와 BI(서비스)를 헷갈리지 않는다.**
 * ```
 * CI · 법인    teamjpk(팀제이피케이) · freepassmobility(프리패스모빌리티)
 * BI · 브랜드   chakhandeal(착한거래) · renman(렌터카매니저) · freepasserp.com(프리패스이알피닷컴)
 * ```
 *   청구서·지급명세서는 **법인이 발행하는 문서**다 — 그래서 `freepassmobility` 를 쓴다.
 *   `freepasserp.com` 은 서비스 브랜드라 화면(ERP)에는 맞지만 대외 문서 발행인 자리에는 안 맞다.
 *   ⚠ 처음에 그걸 틀리게 썼다(2026-08-26). 문서 발행인은 «법인»이다.
 *
 * ★워드마크는 두 무게다 — `freepass`(600) + `mobility`(300). 국문은 프리패스(600)+모빌리티(300).
 */

/** 법인 CI — 프리패스모빌리티. */
export const CORP = {
  /** 국문 상호(문서 표기) */
  name: '프리패스모빌리티 주식회사',
  /** 워드마크 앞·뒤 두 조각(무게가 다르다) */
  markMain: 'freepass',
  markSub: 'mobility',
  koMain: '프리패스',
  koSub: '모빌리티',
  /** 사업자등록번호 — `partners/OP001` 과 같은 값 */
  bizNo: '528-88-02988',
  ceo: '박영협',
  addr: '서울시 강서구 양천로 53길 30, 서서울모터리움 1004호',
  phone: '02-6956-8835',
  web: 'freepassmobility.com',
  /**
   * 문의 메일 — 홈페이지 푸터에서 확인(2026-08-26 `freepassmobility.com`).
   * ⚠ 대표 개인 메일이다. **정산 전용 주소가 생기면 여기를 바꾼다** — 문서마다 고치지 말 것.
   */
  email: 'pyh@teamjpk.com',
  /**
   * 정산 담당 — 정산서 「청구 안내」 칸에 찍힌다.
   *
   * ★**사람 이름을 안 쓴다.** 사장님 2026-08-27 「담당에 프리패스 매니저라고만 해두자」.
   *   맞다. 사람이 바뀌면 종이를 다시 찍어야 하고, 이미 나간 종이는 못 고친다.
   *   자리 이름으로 두면 누가 맡든 그대로 쓴다.
   */
  staff: '프리패스 매니저',
  /**
   * 담당 «직통» 번호. 비면 대표번호(`phone`)가 찍힌다.
   * ⚠ 지어내지 않는다 — 틀린 번호를 15장에 찍으면 회원사가 엉뚱한 데로 건다.
   */
  staffPhone: '',
  /**
   * 팩스. ⚠ **아직 모른다.** 지어내지 않는다 — 세금계산서·서류가 남에게 간다.
   */
  fax: '',
  /**
   * ERP 주소 — 회원사가 실제로 들어오는 곳(**BI · 서비스 브랜드**).
   * ★사장님 2026-08-27 「차라리 우측상단에 freepasserp.com 이거 브랜드 넣던가」.
   * ⚠ 발행인 자리(꼬리 상호)에는 안 쓴다 — 문서를 내는 건 «법인»이다.
   */
  erp: 'freepasserp.com',
  /** ERP 워드마크 두 조각 — 법인 CI 와 같은 규칙(앞 600 · 뒤 300). */
  erpMain: 'freepass',
  erpSub: 'erp.com',
  /** 한 줄 소개 — ERP 워드마크 아래. 사장님 2026-08-27 「장기렌터카 영업지원 플랫폼」. */
  tagline: '장기렌터카 영업지원 플랫폼',
} as const;

/**
 * CI 색 — CI 센터 정의 그대로.
 * ⚠ 여기 없는 색을 문서에 새로 만들지 않는다. 필요하면 CI 센터를 먼저 고친다.
 */
export const CORP_COLOR = {
  /** 본색 — 밴드·표머리·강조 */
  main: '#1B2A4A',
  /** 강조 — 밝은 하늘. 어두운 바탕 위 포인트 */
  accent: '#9EC5F3',
  /** 보조 — 흐린 청회색. 부제·라벨 */
  base: '#7F93B3',
  /** 본색보다 한 단계 짙게(그라데이션 끝) */
  deep: '#0F1B35',
} as const;

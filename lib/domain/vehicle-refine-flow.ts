/**
 * **차명 정제 흐름 — 우리 차종마스터에 넣고, 그 내용으로 정제시트 정제칸을 박고, 그걸 상품시트로 가져온다.**
 *
 * ★사장님 2026-08-21 — 「우리 상품에 신규 차종이 들어오면 그걸 차종마스터화하고, 그거에 맞게끔 차종마스터 내용대로 정제시트 정제칸에 박는다.」
 * ★사장님 2026-08-18 — 「차종마스터랑 상품마스터에 매칭 잘하고, 매칭된 걸 정제시트에 박아버리고, 그걸 상품시트로 갖고 온다.」
 *   같은 글이 원천대장 「시트 지도」·공급사 시트 「작성 안내」/「정제시트 안내」·docs/영업자시트-매뉴얼.md 에 실린다(publish-*-tab 이 여기서 읽는다).
 *
 * ⚠ 사전은 **우리 차종마스터**다(`public/data/vehicle-master.json` · 원천대장 「차종마스터_규격채택」).
 *   엔카 크롤 원자 시트는 중고 시세용이지, 정제칸 사전이 아니다. 엔카에 없다고 비슷한 차(A6 e-트론)로 붙이지 않는다.
 */
export const VEHICLE_REFINE_FLOW: { step: string; what: string; where: string }[] = [
  { step: '⓪ 정제시트 원문(차명·옵션)', what: '아이카·오토플러스·이안카·아이언은 원본에서 차명(세부모델+트림)·옵션을 **매번** 가져온다. 아이카=차종분류(모델 이름, 준중형 SUV 아님)+트림 · 오토플러스=차종+모델명(풀 트림 문장) · 이안카=차종분류+세부모델+트림 · 아이언=홈페이지 제목. 정제시트에서 고쳐도 다음 미러가 원본으로 되돌린다. 정제된 이름(모델·세부모델·세부트림)은 오른쪽 정제칸. 색·연식·가격은 처음 한 번.', where: 'sync-mirror-all --apply · 숨김 탭 「정제시트 안내」 · lib/domain/mirror-sheet-mapping.ts (MIRROR_FOLLOW_SOURCE)' },
  { step: '① 사전 = 우리 차종마스터', what: '한 줄 = 제조사·모델·세부모델(세대)·변형(연료·배기량·구동)·트림. 재고에 있는 차는 여기에 반영돼 있다. 라이브 ERP 「차종마스터」 탭에는 쓰지 않는다. 색은 색상마스터.', where: 'public/data/vehicle-master.json · 원천대장 「차종마스터_규격채택」' },
  { step: '①′ 신규 차종이 상품에 들어오면', what: '**먼저 차종마스터화한다.** 마스터에 없는 모델·세대·트림을 비슷한 차로 짐작해 정제칸에 박지 않는다. 넣은 뒤에만 ②를 돌린다. 마스터에 없으면 정제칸은 비우고 목록으로 남긴다.', where: 'vehicle-master.json / 규격채택에 행을 넣는다(사람·검토). 라이브 ERP 차종마스터 탭 금지' },
  { step: '② 정제칸 = 차종마스터 내용 그대로', what: '공급사가 준 정보는 **차량번호 왼쪽**이다(제조사·차종·차명(세부모델+트림)·연료·연식). 그 글자를 ①에 맞춰 제조사(정제)·모델·세부모델·세부트림·연료(정제)·배기량(정제)·차종분류를 박는다. 차명에 3.5·LPG 3.0이 있으면 그걸 이긴다 — 배기량 칸 숫자로 막지 않는다. 같은 세대 1.6/2.0은 변형이다. 트림은 차명에 그 글자가 있을 때만(PREMIUM=프리미엄). 상품마스터 차종코드가 있으면 그 코드가 가리키는 차종마스터 이름이 이긴다.', where: 'fill-supplier-ai-columns --apply --include-mirror' },
  { step: '③ 상품시트로 가져온다', what: '판매시트 「상품리스트」 차명은 ②정제칸(차종마스터 글자) → 공급사 원문 순으로 싣고, 원문은 「공급사표기」에 남긴다. 출고불가는 안 싣는다. 대여료 없는 칸은 「-」. 「손오공구독」·「오플구독」 탭은 같은 발행기가 갈래만 따로 찍는다.', where: 'publish-origin-tab --apply → publish-sonogong-tab --apply · sync-product-master-live --apply' },
  { step: '④ 엔카 행키(선택)', what: '모델행키 M · 세부모델행키 SM · 세부트림행키 T 는 중고 시세용. **②에서 박은 차종마스터 이름과 같을 때만** 붙인다. 엔카 원자에 가솔린 A6가 없다고 A6 e-트론 키를 넣지 않는다. 이름·연료와 어긋난 leftover 는 지운다.', where: 'stamp-encar-codes-on-supplier --apply (이름 락). 엔카 시트로 모델·세부모델을 덮지 않음' },
];

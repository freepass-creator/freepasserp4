/**
 * **차명 정제 흐름 — 차종마스터·상품마스터에 맞추고, 맞춘 것을 정제시트에 박고, 그것을 상품시트로 가져온다.**
 *
 * ★사장님 2026-08-18 — 「차종마스터랑 상품마스터에 매칭 잘하고, 매칭된 걸 정제시트에 박아버리고, 그걸 상품시트로 갖고 온다 — 이거지?」 → 맞다. 이 글이 그 규칙이다.
 *   같은 글이 원천대장 「시트 지도」·공급사 시트 「작성 안내」/「정제시트 안내」·docs/영업자시트-매뉴얼.md 에 실린다(publish-*-tab 이 여기서 읽는다).
 */
export const VEHICLE_REFINE_FLOW: { step: string; what: string; where: string }[] = [
  { step: '① 사전 = 차종마스터', what: '트림 한 줄이 곧 차종코드(트림행키). 제조사·모델·세부모델·세부트림·연료·정확배기량·연식시작/종료가 여기 있다. Gemini/사람이 관리(원장). 코드는 삭제·재사용·의미변경 금지.', where: '원천대장 「차종마스터」 · 「차종마스터_규격채택」(판매시트에 실을 이름)' },
  { step: '② 차량번호 정본 = 상품마스터 + 3축 결정', what: '차량번호마다 차종코드 하나(검증상태 확정). 코드가 없는 차는 결정 파일(CODE=코드 확정 · TRIPLE/PARTIAL=제조사·모델·세부모델(·트림) 이름만 · HOLD=보류)이 정본이다. 매칭은 사람·AI 검토로 한 번만 한다 — 글자 스냅으로 매번 다시 맞히지 않는다.', where: '원천대장 「상품마스터」 차종코드 열 · 리포 data/product-vehicle-review-decisions.json' },
  { step: '③ 정제시트에 박는다 = 정제칸', what: '공급사 시트(제공·정제 21곳) 구분선 「│」 오른쪽 정제칸(차종코드·제조사(정제)·모델·세부모델·세부트림·선택옵션·외장/내장색상·배기량(정제)·연료(정제)·차종분류)에 ②의 정본 값을 쓴다. 정본이 있으면 정본이 이긴다(옛 값 바로잡음). 정본이 없으면 차종마스터와 대조(세대코드·연료·배기량·등록연도·트림 글자)해 결정을 만들고(resolve-unmatched-vehicles → CODE/PARTIAL), 그래도 못 정하면 스냅 확신 high 일 때만 이름, 아니면 빈칸(틀린 값보다 빈 칸). 한 번 박히면 정본이 바뀌지 않는 한 그대로다.', where: 'fill-supplier-ai-columns --apply(--include-mirror) · resolve-unmatched-vehicles --apply · 정제칸은 프리패스/AI 칸(머리 보라)' },
  { step: '④ 상품시트로 가져온다', what: '판매시트 「상품리스트」 차명(제조사·모델·세부모델·세부트림)은 ②정본(코드→규격채택 이름 / 결정) → ③정제칸 → 공급사 원문 순으로 싣고, 원문은 「공급사표기」에 남긴다. 출고불가는 안 싣는다. 대여료 없는 칸은 「-」. 「손오공구독」·「오플구독」 탭은 같은 발행기가 갈래만 따로 찍고 원본 요금 블록(인수형 / 2만·3만)을 덧붙인다(탭 3개, 2026-08-19). 상품마스터도 같은 21곳에서 상태·정책·돈만 매일 갱신(차종코드·차명 칸은 안 덮음) → ERP.', where: 'publish-origin-tab --apply → publish-sonogong-tab --apply · sync-product-master-live --apply' },
  { step: '⑤ 새 차가 들어오면', what: '③을 다시 돌린다(빈 칸만 채움) → 못 정한 차는 resolve → 그래도 못 정하면 검토 큐(마스터에 없는 세부모델은 차종마스터 보강 후보) → ④ 발행. 공급사가 시트에 트림·배기량을 적어 주면 다음 채움 때 코드가 붙는다.', where: '매일: fill → (resolve) → publish → sync-product-master-live' },
];
